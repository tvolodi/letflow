---
id: TK-UI-07
title: Task sort & search
stage: F4
priority: SHOULD
status: DRAFT
type: frontend
---

# TK-UI-07 — Task sort & search `[SHOULD]`

> The inbox SHALL support sorting by created time (asc/desc) and free-text search by task name or instance correlation key.

**See:** TK-UI-01 (the inbox this sort/search operates on), API-04 (`GET /tasks` — search and sort parameters)
