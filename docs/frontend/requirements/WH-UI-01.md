---
id: WH-UI-01
title: Webhook subscription list
stage: F6
priority: MUST
status: DRAFT
type: frontend
---

# WH-UI-01 — Webhook subscription list `[MUST]`

> The Webhooks page SHALL display a table of subscriptions with columns: target URL, event filter tags, status (ACTIVE/PAUSED), created date.

**See:** EXT-01 (webhook subscription model), API-07 (`GET /webhooks`), WH-UI-02 (create action), WH-UI-03 (pause/resume per row), WH-UI-04 (delivery log per row)
