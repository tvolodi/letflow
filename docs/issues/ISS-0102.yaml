id: ISS-0102
title: "service_task default HTTP method (:POST) not verified against R-Co (service_task.md:408, OQ-5)"
discovered_by: DOC-UPDATER
discovered_in_run: WF02-REQ112-20260820
discovered_at: "2026-08-20T03:08:10Z"
severity: MINOR
description: >
  PROVENANCE (historical, not current decision authority):
  Triaged as CLASS B during REQ-112's sweep of ISS-0076 item 2's remaining
  "R-Co source unavailable" claims. OQ-5 (MINOR): method defaults to :POST
  -- "EXT-01's typical default for a body-carrying dispatch" -- not verified
  against R-Co source, which was not reachable at design time. Left as an
  open question, not swept.
  Letflow file:line: lib/letflow/design/service_task.md:410 (shifted from 408
  by REQ-112's own §0 edit earlier in the same file)
  R-Co source that could settle it: R-Co/src/engine/service_task.zig (R-Co
  tree reachable at c:\Users\tvolo\dev\ai-dala\R-Co; REQ-112 is docs-only and
  does not itself read R-Co to answer this).
affected_files:
  - lib/letflow/design/service_task.md
queue_task_id: null   # filed directly via gh (no ORCH intermediary in this session); registration pending reconciliation per ISSUE_QUEUE.md 2b
github_issue: https://github.com/tvolodi/letflow/issues/330
status: resolved
resolution: >
  PROVENANCE (historical, not current decision authority):
  R-Co/src/engine/service_task.zig confirms the `method` field's parse block
  defaults unconditionally to `.POST` when `"method"` is absent from the
  node's JSON config (`break :blk .POST;`), matching Letflow's design
  choice exactly. lib/letflow/design/service_task.md updated (§5.1 table
  row and §9 OQ-5) to record this as verified rather than an open question.
resolved_at: "2026-08-20T00:00:00Z"
