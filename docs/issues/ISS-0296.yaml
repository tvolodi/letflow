id: ISS-0296
title: >
  REQ-133 AC1-6: complete the live-browser login proof once a session with
  reliable Chromium/Playwright automation is available -- config fix already
  landed and curl-verified
discovered_by: FRONTEND-DEV
discovered_in_run: WF02-REQ133-20260823
discovered_at: 2026-08-23T00:00:00Z
severity: MINOR
status: resolved
resolved_in_run: WF03-ISS0296-20260826
resolved_at: "2026-08-26T21:00:00Z"
reverified_in_run: WF03-ISS0296-20260908
reverified_at: "2026-09-08T18:55:00Z"
queue_ref: Q-296
github_ref: GH-594
github_ref: GH-594
description: >
  REQ-133 (End-to-end login -- SPA through Keycloak to an authorized API
  call) had its config-fix scope landed and merged (PR #593): web/src/auth/
  OidcManager.ts, tenantConfig.ts, web/.env.example, and lib/letflow/routers/
  tenant_config.ex all corrected from R-Co-stale defaults (port 8081,
  client_id bpm-platform-api) to Letflow's real values (port 8082,
  client_id letflow-web), verified consistent against docker-compose.yml/
  config/dev.exs/priv/keycloak/realms/bpm-default.json, REVIEWER PASS.
  AC7 (npm run type-check/lint/test) and AC8 (route inventory) completed.

  What remains: AC1-6 (live browser proof -- admin login, worker login with
  narrower nav, an authenticated API call, a 403-on-forbidden-route check,
  and a browser-storage inspection). Extensive attempts in the REQ-133 session
  hit genuine reproducible Playwright/Chromium flakiness in that environment
  -- NOT an app defect.

  Two real environment workarounds discovered: (1) Vite dev server binds to
  IPv6 localhost only -- use 'localhost' not '127.0.0.1' in E2E_BASE_URL;
  (2) React 18 StrictMode double-invokes ProtectedRoute redirect, causing
  PKCE-state race in 'npm run dev' -- use 'npm run build && npm run preview'
  (port 4173) for e2e login tests.

  Strong prior confidence the underlying fix is correct: curl-verified GET
  /api/tenant-config returns {client_id: letflow-web, oidc_authority:
  http://localhost:8082/realms/bpm-default}; Keycloak /auth returns 200.
acceptance_criteria:
  - "a real browser login as PLATFORM_ADMIN completes the full OIDC redirect/callback flow and lands on an authenticated view"
  - "a real browser login as TASK_WORKER shows a demonstrably narrower nav than the admin, with both navs listed"
  - "at least one authenticated API call returns real data from Letflow, with the request and response quoted"
  - "an authenticated call to a route the caller's role does not permit returns REQ-131's 403 problem document"
  - "no token is written to localStorage or sessionStorage, confirmed by inspecting browser storage after login"
  - "REQ-133's status in docs/requirements.yaml is flipped from blocked to done with status-history event appended"

resolution_note: >
  Resolved 2026-08-26 (WF03-ISS0296-20260826, handoff
  handoffs/WF03-ISS0296-20260826/step-01-frontend-dev-login-proof.json): all
  9 Playwright tests (oidcf-login.e2e.spec.ts x5, req133-ac2-ac5.e2e.spec.ts
  x4) passed against real Keycloak + real Letflow. REQ-133 flipped to done
  in that run's commit a91e084c5.

  This file's own `status: open` was left stale after that run despite the
  commit message declaring "ISS-0296 closed" -- a bookkeeping gap, not a
  reopened defect. Corrected here.

  Re-verified independently 2026-09-08 (WF03-ISS0296-20260908, UAT-RUNNER,
  this checkout's HEAD 226cb9fb / base 04ec455f, after ISS-0529..0536
  landed): fresh `mix run --no-halt` backend (old process from Aug24 was
  stale, killed and restarted), fresh `npm run build && npm run preview`
  on :4173, both documented workarounds applied (localhost not 127.0.0.1;
  build+preview not dev). All 9 existing e2e tests re-ran green, plus an
  ad-hoc nav dump for PLATFORM_ADMIN. Evidence:
    AC1: TC-OIDCF-03 PASS -- real Keycloak login (admin-user/admin-pass)
      completes redirect/callback, workspace renders.
    AC2: PLATFORM_ADMIN nav (14 items) -- ["Instances","My Tasks",
      "Definitions","DLQ","Webhooks","Users","Groups","Tokens","Audit",
      "Health","Metrics","Register Tenant","Tenants","Services"];
      TASK_WORKER nav (1 item) -- ["My Tasks"].
    AC3: GET http://localhost:4000/api/v1/identity/users with admin
      bearer token -> HTTP 200, {"items":[operator-user, worker-user,
      admin-user], "count":3} -- real seeded data.
    AC4: same route with worker-user bearer token -> HTTP 403,
      {"status":403,"type":"https://bpm.example.com/problems/forbidden",
      "title":"Forbidden","detail":"insufficient permissions"}.
    AC5: after full OIDC login, page.evaluate confirms
      localStorage keys=[] and sessionStorage keys=[].
  No new regressions found. REQ-133 remains done; no requirements.yaml
  change needed since it was already flipped in the 2026-08-26 run.
