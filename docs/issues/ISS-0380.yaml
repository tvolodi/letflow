id: ISS-0380
title: >
  ModuleRegistry.register/1 timeout path leaves a Wasmtime native thread
  spinning after Task.shutdown(:brutal_kill)
discovered_by: TEST-DESIGNER
discovered_in_run: WF02-REQ166-20260828
discovered_at: "2026-08-28T04:42:27Z"
severity: MINOR
queue_task_id: 380   # registered 2026-08-31 (reconciliation); originally filed as ISS-0351 before an id existed
github_issue: 752
former_local_id: ISS-0351   # historical alias -- see note below
note_on_id: >
  Filed during WF02-REQ166-20260828 as ISS-0351, a filename-derived id assigned
  before this finding had been through register_task -- so it carried no
  queue_task_id and no GitHub issue, and was therefore invisible to the
  GitHub-issue control loop. Registered 2026-08-31 during a state-reconciliation
  pass; register_task is the only allocator of ids, and it allocated a new one.
  This record was renamed ISS-0351 -> ISS-0380 to match.
  Handoff files, handoffs/registry.json and the requirement_status volumes still
  cite ISS-0351 -- those are immutable run records and were deliberately NOT
  rewritten. Treat ISS-0351 and ISS-0380 as the same finding.
status: resolved
resolved_in_run: WF03-ISS0380-20260831
resolved_at: "2026-08-31T09:52:00Z"
resolution: >
  Not fixed at the native-thread level -- confirmed not currently possible.
  ISSUE-FIXER's diagnosis (handoffs/WF03-ISS0380-20260831/step-01-issue-fixer-diagnosis.json)
  checked the pinned wasmex dependency (0.15.1) directly: its fuel-metering
  and epoch-interruption mechanisms only bound Wasmex.call_function/4
  (invoking an already-instantiated module), never Wasmex.start_link/1
  (instantiation) -- the exact call this bug hangs inside. There is
  currently no wasmex-exposed way to bound a hanging instantiation's native
  execution; the real fix would require an upstream wasmex change or an
  unverified experiment, not something to attempt speculatively here.
  Precedent checked (docs/issues/ISS-0352.yaml, the same "hung wasmex NIF
  call leaks a native thread" mechanism class): this codebase's own prior
  resolution chose containment/isolation over fixing native-thread
  survival, confirming this is an established, accepted pattern rather
  than a gap unique to this module.
  No tenant-facing exploit path exists today (ModuleRegistry.register/1's
  only application-code caller, ModuleVersionRegistry, has no
  application-code caller of its own -- only tests reach either module).
  Per ORCH's direct-action sizing rule (docs/agents/ORCHESTRATOR.md §10):
  a single-file, comment-only documentation change, adding no public
  function/spec, no migration, no supervision-tree touch, no tenant-data
  path, and no test-behavior change -- so ORCH acted directly rather than
  routing through the full CODE-DESIGNER/ELIXIR-DEV/.../TEST-RUNNER chain.
  Added a "Residual risk" disclosure to
  lib/letflow/engine/wasm/module_registry.ex's moduledoc, at the exact
  call site (instantiate/1's timeout branch), mirroring
  lib/letflow/engine/wasm/plugin_handler.ex's own established pattern for
  disclosing the identical class of native-crash/hang residual risk --
  citing this issue by id, so a future implementer of a tenant-facing WASM
  module-registration or execution path is forced to confront the gap
  rather than discover it independently. Verified: mix compile
  --warnings-as-errors clean, mix format --check-formatted clean,
  test/letflow/engine/wasm/module_registry_test.exs 20/20 passed
  unchanged (doc-only diff, confirmed via git diff --stat).
description: >
  While mutation-testing lib/letflow/engine/wasm/module_registry.ex's stage-2
  timeout branch (Step 3, WF02-REQ166-20260828), TEST-DESIGNER live-verified
  (real register/1 call, not simulated) that a genuinely-hanging WAT module
  (an unconditional infinite loop in its start function) correctly drives
  register/1's real 5000ms timeout path, returning
  {:error, {:instantiation_failed, {:timeout, 5000}}} after ~5043-5061ms
  across two runs -- the timeout detection and Task.yield/2 bound are real
  and working as designed.

  However, the same check found that Task.shutdown(task, :brutal_kill) does
  not actually stop the underlying native Wasmtime execution: the BEAM's
  dirty-NIF scheduler thread running the guest module's infinite loop keeps
  consuming CPU after the Elixir Task is killed. Measured via
  /proc/<beam_pid>/stat utime+stime fields: ~305 extra clock ticks
  accumulated over a 3-second idle window with nothing else running,
  confirming the native thread is a non-preemptible busy loop that
  Task.shutdown/2 (an Erlang-process-level operation) cannot reach or stop.

  This is a real resource-leak path on a pathological/malicious WASM module
  input (an intentionally-hanging guest), though narrow in current blast
  radius: today only checked-in WAT test fixtures ever reach register/1
  (REQ-166 has no tenant-facing upload path yet), so this is not yet an
  exploitable tenant-facing DoS. It becomes one once a future requirement
  wires a tenant-facing module-upload/registration path to
  ModuleRegistry.register/1 -- flagging now while the mechanism is fresh,
  before that dependency exists, per the project's no-silent-decisions norm
  and this being the *first* Letflow module to call Wasmex.start_link/1 on
  content that could eventually be less trusted.

  Not fixed as part of REQ-166: out of TEST-DESIGNER's scope (diagnosis
  only, not implementation), and this is a real design decision (how to
  actually bound/kill a native Wasmtime execution thread -- e.g. an
  epoch-interruption / fuel-metering mechanism in wasmex/wasmtime, if
  exposed, rather than relying on Erlang-level task cancellation) that
  belongs to whichever requirement first exposes a tenant-facing WASM
  module-registration or execution path, not squeezed into this one.

  A committed automated test for this was deliberately NOT added: it would
  leave a permanently CPU-spinning orphaned native thread for the rest of
  that mix test process's lifetime on every CI run that reaches it. The live
  verification above is TEST-DESIGNER's own one-off reproduction, not part
  of the merged test suite.
affected_files:
  - lib/letflow/engine/wasm/module_registry.ex
related:
  - REQ-166
  - REQ-165  # wasmex native dependency, PluginHandler's own per-call fresh-instance / GenServer.stop pattern
source: >
  handoffs/WF02-REQ166-20260828/step-03-test-designer.json (AC4 finding,
  acceptance_criteria_results), WF02-REQ166-20260828 Step 3.
