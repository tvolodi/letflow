id: ISS-0690
title: QA lacks BilimBaga tenant/solution-pack/candidate seed data needed for real bilimbaga-scope UAT scenarios
discovered_by: ELIXIR-DEV
discovered_in_run: WF02-REQ359-20260916
discovered_at: "2026-09-16T10:45:57Z"
severity: MINOR
description: >
  Discovered during WF02-REQ359-20260916 (REQ-359, BA agent role requirement)
  executing its AC5 real-execution requirement: authored a real BilimBaga UAT
  scenario (test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml)
  and dispatched it for real via UAT-RUNNER against the live QA environment
  (https://qa.bizdala.com), using a real PLATFORM_ADMIN Keycloak token.
  GET /api/v1/tenants returns exactly one tenant (Default Tenant/bpm-default) --
  no BilimBaga tenant exists. GET /api/v1/identity/users?search=bilimbaga and
  ?search=candidate both return count: 0. GET /api/v1/definitions and
  ?search=exam return zero process definitions installed.
  ai-dala-infra/scripts/qa-login.sh only has 4 fixed seeded usernames
  (admin/designer/operator/worker), none a candidate identity. This is the
  same class of gap already recorded in REQ-358's own UAT report for the
  Meridian tenant fixture. Verified independently twice (once by ELIXIR-DEV,
  once again by a fresh UAT-RUNNER dispatch) with full quoted evidence --
  both runs recorded the real BLOCKED/FAIL verdict honestly rather than
  fabricating a pass.
affected_files:
  - test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
  - test/uat-reports/uat-2026-09-16-WF02-REQ359-20260916.yaml
  - test/uat-reports/ba-signoff-bilimbaga-WF02-REQ359-20260916.yaml
queue_ref: Q-690
github_ref: GH-1445
status: open
diagnosis:
  diagnosed_by: ISSUE-FIXER
  diagnosed_in_run: WF03-ISS0690-20260917
  diagnosed_at: "2026-09-17T00:00:00Z"
  note: >
    Root cause confirmed live, not a lib/ code defect. Letflow binds a tenant's
    identity 1:1 to a distinct OIDC realm (lib/letflow/identity/tenant.ex's
    idp_realm_id, unique/immutable; Letflow.Identity.resolve_tenant_by_realm/1
    resolves strictly from the caller's own token `iss` realm claim).
    POST /solution-packs/install installs only into
    conn.assigns.auth_context.tenant_id -- no caller-suppliable tenant id
    exists, by design (lib/letflow/routers/solution_packs.ex moduledoc, INV-1).
    So no sequence of Letflow HTTP calls against the existing bpm-default
    realm can create a second tenant's data; a second Keycloak realm must
    exist first, the same way bpm-default itself did before
    ai-dala-infra's T-0129. priv/packs/bilimbaga/pack.json is a real,
    installable pack -- the application side is ready and not the gap.
    Confirmed live against https://qa.bizdala.com with a real PLATFORM_ADMIN
    token: GET /api/v1/tenants still returns exactly one tenant; Keycloak
    admin API on ubuntu-16gb-nbg1-1 (letflow-qa-keycloak-1) confirms only
    `master` and `bpm-default` realms exist.

    None of ISS-NNNN.yaml's terminal status values (resolved/instrumented/
    no_defect) truthfully describe this outcome: the alleged defect (missing
    QA seed data) is real and confirmed (rules out no_defect), nothing was
    shipped or built this run (rules out instrumented, which also requires
    this run's own acceptance criteria to have been met -- none of the four
    were), and no root cause was removed (rules out resolved). Left as
    `status: open`, annotated, rather than forcing a false claim into the
    registry -- flagged explicitly for a later vocabulary review
    (docs/agents/protocols/ISSUE_QUEUE.md's "Issue status vocabulary" has no
    slot for "confirmed real gap, external-repo companion task required,
    nothing to ship on this side").
  blocked_by_external_task: ai-dala-infra/tasks/T-0132-provision-bilimbaga-keycloak-realm-qa.md
  next_action: >
    Once T-0132 lands (a bilimbaga Keycloak realm on letflow-qa-keycloak-1
    with a seeded candidate user), a follow-up letflow-side pass:
    POST /tenants (idp_realm_id = the new realm), POST /solution-packs/install
    using a token minted from that realm, configure >=1 active exam, then
    dispatch UAT-RUNNER to re-run
    test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
    against QA for the real result this issue's AC4 requires.
queue_release: >
  Queue task 690 released with status "blocked" (not "done", not no-status)
  to prevent a re-selection loop on an issue-type task that is fully
  investigated but not actionable again until the external T-0132 infra task
  lands -- see docs/agents/protocols/TASK_QUEUE.md's release_lock section.
  This is a pragmatic reuse of "blocked" alongside a local status of `open`
  rather than the doc's literal instrumented/no_defect pairing, for the
  reason stated in `diagnosis.note` above.
followup:
  checked_by: ORCH
  checked_at: "2026-09-18T13:53:00Z"
  note: >
    Re-checked T-0132's landing status directly (real HTTP probe, not
    assumed): GET https://auth.qa.bizdala.com/realms/bilimbaga/.well-known/openid-configuration
    returns 200 with a real OIDC discovery document (issuer, token/auth/
    userinfo/jwks endpoints all present); a control probe against a
    deliberately nonexistent realm on the same host returns 404, ruling
    out a wildcard/always-200 response. T-0132's Keycloak-realm half is
    confirmed landed -- this is a genuinely new fact, not a re-derivation
    of `diagnosis.note` above.

    However, the letflow-side follow-up `diagnosis.next_action` describes
    (POST /tenants with idp_realm_id = the new realm, POST
    /solution-packs/install using a token minted from that realm, seed a
    candidate account, re-run the UAT scenario) is STILL not actionable
    from this execution environment, for a reason distinct from the
    original T-0132 gap: no QA admin credential-fetch path exists here
    (no ai-dala-infra checkout, no SSH access to the QA host to run
    qa-login.sh or an equivalent) to obtain the real PLATFORM_ADMIN token
    those POST calls require, and no path to seed or discover a candidate
    identity inside the new bilimbaga realm either. This is the identical
    credential-access gap independently found the same day for REQ-369's
    AC6/AC7 (docs/requirements.yaml's REQ-369 entry, close_out_note) --
    not a coincidence, but the same underlying environment limitation
    surfacing on a second, unrelated item.

    Left `status: open` and the queue release at `blocked` unchanged --
    this finding narrows the blocker (Keycloak realm: done; credential
    access: still the wall) without closing it. next_action below
    supersedes the original T-0132-pending framing for whoever picks this
    up next with real QA/ai-dala-infra access.
  next_action: >
    T-0132 (Keycloak realm) is done -- do not re-verify that again. What's
    left, from a session with a real QA admin credential-fetch path: mint a
    PLATFORM_ADMIN token via qa-login.sh (or equivalent) against QA's
    bpm-default realm, POST /tenants with idp_realm_id set to the new
    bilimbaga realm, POST /solution-packs/install (priv/packs/bilimbaga/
    pack.json) using a token minted from that new realm, seed or locate at
    least one candidate account inside the bilimbaga realm (check whether
    T-0132 seeded one as part of realm provisioning -- if not, that's a
    fresh companion infra task, not a letflow-side one), configure >=1
    active exam, then dispatch UAT-RUNNER to re-run
    test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
    against QA for the real AC4 result this issue requires.
followup_2:
  checked_by: ISSUE-FIXER
  checked_at: "2026-09-18T14:35:00Z"
  note: >
    Credential access confirmed working this run (SSH/qa-login.sh path
    unnecessary -- ORCH had already fetched real Keycloak passwords for
    admin-user/bpm-default and candidate-user/bilimbaga this session).
    Executed the previous followup's next_action live end to end, with
    real HTTP responses at every step -- and refined ORCH's own
    hypothesis with a live result that turned out to be a DIFFERENT
    mechanism than predicted, not the one predicted.

    Step 1 (role/permission mapping, static read):
    lib/letflow/api/authorization.ex confirms POST /solution-packs/install
    is declared `authz_post "/install", :DefinitionsCreate` (line 167),
    `required_permission(:DefinitionsCreate)` maps to `:DefinitionsWrite`
    (lines 812-822), and `role_allows?/2`'s CANDIDATE clause (lines
    1023-1032) grants CANDIDATE exactly six ExamSession*/ExamCertificateIssue
    permissions and NOT :DefinitionsWrite. So the permission-mapping half
    of ORCH's hypothesis is correct as a static fact.

    Step 2 (POST /tenants, real HTTP): minted a real PLATFORM_ADMIN token
    for admin-user against bpm-default
    (POST https://auth.qa.bizdala.com/realms/bpm-default/protocol/openid-connect/token,
    200, real JWT with iss=.../realms/bpm-default). Then
    POST https://qa.bizdala.com/api/v1/tenants
    body {"slug":"bilimbaga","display_name":"BilimBaga","idp_realm_id":"bilimbaga"}
    -> real HTTP 201:
    {"display_name":"BilimBaga","id":"6ab093a1-d190-4063-9543-d66b80d261a1",
    "inserted_at":"2026-09-18T14:29:28Z","slug":"bilimbaga","status":"active",
    "updated_at":"2026-09-18T14:29:28Z"}. Tenant creation succeeded exactly
    as ORCH predicted (PLATFORM_ADMIN, no realm-match requirement on this
    route).

    Step 3 (POST /solution-packs/install, real HTTP): minted a real
    CANDIDATE token for candidate-user against the bilimbaga realm
    (POST https://auth.qa.bizdala.com/realms/bilimbaga/protocol/openid-connect/token,
    200, real JWT with iss=.../realms/bilimbaga, realm_access.roles
    containing "CANDIDATE"). Then
    POST https://qa.bizdala.com/api/v1/solution-packs/install
    with priv/packs/bilimbaga/pack.json as the body and that token as
    Bearer -> real HTTP **401**, body
    {"error":"unauthorized","detail":"invalid or expired bearer token"}.
    NOT the 403 ORCH's hypothesis predicted. Root-caused by reading
    lib/letflow/plugs/auth_pipeline.ex: the OIDC auth branch's step 1
    (`verify_token/1`, i.e. `Letflow.Oidc.TokenVerifier.verify_bearer_token/2`)
    runs BEFORE realm extraction/tenant resolution/permission evaluation,
    and validates every incoming JWT against exactly ONE registered
    `Oidcc.ProviderConfiguration.Worker` (lib/letflow/supervisor/infrastructure.ex
    lines 159-177), configured from a SINGLE `:oidc, :issuer` value
    (config/runtime.exs lines 140-141, `OIDC_ISSUER` env var -- QA's
    deployed value resolves to .../realms/bpm-default, confirmed by the
    admin token's own iss claim above and by the prior UAT report
    test/uat-reports/uat-2026-09-16-WF02-REQ359-20260916.yaml). A JWT
    issued by any OTHER realm (bilimbaga) fails this single-issuer
    verification outright -- wrong issuer, wrong signing JWKS -- and is
    rejected 401 by handle_auth_error/2's `{:error, {:verify, _}}` clause
    (auth_pipeline.ex line 156-157) before permission evaluation is ever
    reached. **This is a strictly earlier, deeper, role-independent
    failure than the :DefinitionsWrite 403 predicted**: no token from any
    non-bpm-default realm, of ANY role including PLATFORM_ADMIN, can
    authenticate against this QA deployment at all, right now.

    This single-issuer-per-deployment behavior is DOCUMENTED, deliberate
    design, not an undiscovered code defect: config/runtime.exs's own
    comment (lines 131-136) states "each deployed environment can point
    at its own Keycloak realm" (singular), and
    lib/letflow/design/req019-tenant-realm-binding.md line 244 states
    outright "REQ-016 configures a single realm/issuer". REQ-016 (OIDC
    foundation) and REQ-019 (tenant/realm binding, Tenant.idp_realm_id,
    resolve_tenant_by_realm/1, verify_realm_ownership/2) both shipped
    knowingly leaving this gap: the tenants table's data model supports N
    tenants each bound to a distinct realm, but the actual OIDC
    token-verification layer (Oidcc.ProviderConfiguration.Worker) trusts
    exactly one issuer per running deployment. Nothing in req019's or
    req021's (lib/letflow/design/req021-auth-plug-pipeline.md) acceptance
    criteria commits to multi-issuer verification within one deployment
    -- so this is NOT reported as a new Letflow code defect to route to
    CODE-DESIGNER; it is a real, confirmed, structural boundary of the
    shipped OIDC design that this UAT scenario's premise (BilimBaga as a
    second tenant coexisting with bpm-default on the SAME QA deployment,
    reachable via its own realm's OIDC login) runs directly into.

    Checked whether Letflow's OTHER auth path (the opaque `lf_tok_`
    API-token branch, REQ-076, lib/letflow/plugs/auth_pipeline.ex lines
    24-71/140-149) offers a legitimate route around this for the CURRENT
    admin-user token, since it resolves tenant from an `X-Tenant-Slug`
    header rather than a JWT `iss` claim. It does not: `POST /tokens`
    (lib/letflow/routers/identity.ex line 191) is `authz_post "/tokens",
    :TokensManage`, and its handler is called with
    `conn.assigns.scoped_opts` (line 192) -- the same caller's-own-tenant
    :prefix scoping INV-1 enforces everywhere else in this codebase. An
    admin-user token authenticated against bpm-default resolves
    scoped_opts to bpm-default's own schema, so any token it mints (and
    any user_id that token could reference) is confined to bpm-default,
    not bilimbaga. No sequence of calls against the current admin-user
    token reaches bilimbaga's schema by any path -- confirmed by reading
    the code, not assumed; not attempted live, since doing so would only
    reconfirm the same INV-1 boundary already traced twice
    (solution-packs/install above and here), and the run's own
    instructions rule out probing for an unscoped/superuser workaround.

    Left `status: open`, queue release stays `blocked` (unchanged from
    the prior followup) -- this narrows the blocker further (from "no
    credential access" to "Keycloak realm: done + credential access:
    done" to now "structurally cannot authenticate a second-realm caller
    against this single-issuer QA deployment at all") without closing it,
    same class of confirmed-real-external/architectural gap as before.
  next_action: >
    Do NOT re-attempt POST /solution-packs/install with a bilimbaga-realm
    token against the current QA deployment -- it is confirmed blocked at
    the OIDC issuer-verification layer (401, "invalid or expired bearer
    token"), before role/permission evaluation, for every role including
    PLATFORM_ADMIN, not just CANDIDATE. This is a decision point for
    ORCH/a human-equivalent architecture call, not a simple infra seed
    task: whichever of the following is chosen, it is NOT something
    ISSUE-FIXER should decide or implement --
      (a) accept the current one-realm-per-deployment design (REQ-016)
          as final, and give BilimBaga its own separate Letflow
          deployment/environment with OIDC_ISSUER pointed at the
          bilimbaga realm -- but this contradicts the existing UAT
          scenario's and this issue's own premise (BilimBaga as a
          second TENANT alongside bpm-default's Default Tenant on ONE
          shared qa.bizdala.com deployment, per GET /api/v1/tenants
          already listing both after this run's POST /tenants); or
      (b) scope a genuinely new requirement/capability: dynamic/
          multi-issuer OIDC token verification within a single Letflow
          deployment (a real code change to
          Letflow.Oidc.TokenVerifier/Oidcc.ProviderConfiguration.Worker's
          one-provider-per-application-instance shape), which is
          substantial enough that it should go through REQ-ANALYST/
          REQ-VALIDATOR/CODE-DESIGNER like any other requirement, not be
          folded into this issue's fix; or
      (c) bypass OIDC entirely for BilimBaga's exam-candidate flow via
          the already-built API-token branch (REQ-076, `lf_tok_`
          opaque bearer + X-Tenant-Slug header) -- requires a Letflow
          user row to exist inside the bilimbaga tenant's own schema
          (none exist yet -- 0 users, since no OIDC login into that
          tenant has ever succeeded to JIT-provision one) and a token
          minted by a caller whose OWN auth_context.tenant_id is already
          bilimbaga (which nothing today satisfies) -- this also needs a
          person/agent with direct DB/ops seeding access to that
          tenant's schema, mirroring how bpm-default's own seed users
          (admin-user etc.) were provisioned by ai-dala-infra rather than
          through the Letflow API, i.e. still an ai-dala-infra-side task,
          not a letflow-side one.
    Whoever picks this up next needs BOTH a decision among (a)/(b)/(c)
    above AND, if (a) or (c), a follow-on ai-dala-infra task -- do not
    default to (b) without ORCH/REQ-ANALYST sign-off, since it is the
    only option requiring new Letflow application code.
followup_3:
  checked_by: ORCH
  checked_at: "2026-09-19T00:00:00Z"
  note: >
    Option (b) landed: REQ-370 (multi-issuer OIDC token verification,
    merged as 21177271, PR #1535) is done. This run re-verified it live
    against QA rather than trusting the prior session's "CD (QA)
    succeeded" claim at face value, per this run's own explicit
    instruction not to assume the fix is deployed -- and that caution was
    justified: the first live re-test still got a 401, identical to
    followup_2's pre-fix result.

    Root-caused to a genuinely new, separate, higher-impact defect, now
    filed as ISS-0717 (Q-717/GH-1540): ai-dala-infra's
    deploy/redeploy-qa.sh builds and tags an image as `letflow-qa:latest`,
    but docker-compose.qa.yml's `app` service has no `image:` field, so
    `docker compose up --force-recreate` never actually picks up that
    freshly-built image -- it silently keeps running whatever
    `letflow-qa-app:latest` (compose's own auto-named image) last
    happened to be. Confirmed live: QA was running git ref b04a2bb4
    (pre-REQ-370) despite two "successful" CD (QA) runs after REQ-370
    merged (both were docs-only commits whose deploy step correctly
    no-op'd; the one CI run that would have deployed REQ-370's own commit,
    211772716c, was cancelled and no CD run ever reached it). This means
    QA backend deploys may have been silently no-op'ing for an unknown
    prior window -- see ISS-0717 for the full account and remediation
    options. Ad hoc fixed for THIS session only (rebuilt and
    force-recreated the container under the tag compose actually uses);
    NOT a durable fix, will not survive the next automated CD (QA) run.

    With QA genuinely running a6e899fc (current main, REQ-370 included),
    re-tested: a real bilimbaga-realm CANDIDATE token against
    GET /api/v1/definitions now returns a real 403 Forbidden
    (permission-correct) instead of the prior 401 unauthorized
    (auth-layer rejection). REQ-370's multi-issuer verification is
    confirmed genuinely working live, not just merged.

    AC2 (solution pack installed) is STILL not done, for a narrower reason
    than any previously recorded: the bilimbaga Keycloak realm has exactly
    one seeded user (candidate-user, CANDIDATE role) -- no admin/designer
    equivalent exists in that realm with :DefinitionsWrite, and
    POST /solution-packs/install is strictly scoped to the caller's own
    token's tenant_id (confirmed by reading
    lib/letflow/routers/solution_packs.ex -- no cross-tenant admin
    override exists, by INV-1 design). Attempted to seed a bilimbaga-realm
    admin user directly via Keycloak's admin API to unblock this myself;
    no master/realm-admin Keycloak credential was available from this
    environment (only the five seeded APPLICATION user passwords in
    seeded-users.env) -- this remains a genuine ai-dala-infra-side gap,
    same class as followup_2's option (c) discussion.

    Dispatched a FRESH UAT-RUNNER (not reusing any prior report) to
    actually re-run
    test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
    against QA with the real bilimbaga CANDIDATE token, per this run's own
    instruction to confirm live rather than assume. Real result: BLOCKED,
    not PASS -- full report at
    test/uat-reports/uat-2026-09-19-WF03-ISS0690-BILIMBAGA-20260919.yaml.
    No 401 anywhere in that run (REQ-370 confirmed holding up under a
    fresh token). Two distinct blockers recorded honestly:
      1. AC2's still-open gap above (no pack installed, so no active exam
         exists) -- POST /api/v1/exam-sessions with a synthetic exam_id
         correctly returned 422 "this exam is not currently active", not
         an auth error.
      2. A genuinely NEW defect, surfaced only now that the auth layer no
         longer blocks the request first: ExamListPage.tsx's exam-listing
         call (POST /entities/query, needs :EntitiesQuery) is outside
         CANDIDATE's ISS-0646 closed permission set (exactly six
         ExamSession*/ExamCertificateIssue permissions, nothing else) --
         real 403 Forbidden, confirmed live. Filed separately as ISS-0718
         (Q-718/GH-1541, BLOCKER) since it is a real, actionable,
         letflow-side authorization/design gap, independent of both
         ISS-0690 and ISS-0717.

    Net position: this issue narrows further with each pass (auth-layer
    blocker: closed by REQ-370, confirmed live. Deploy-pipeline blocker:
    discovered and ad hoc unblocked, durable fix filed as ISS-0717.
    Solution-pack install: narrowed to a specific missing
    bilimbaga-realm credential, ai-dala-infra-side. Exam-list
    authorization: newly discovered, filed as ISS-0718, letflow-side) but
    is NOT closed -- AC2 and AC4 (this issue's own acceptance criteria)
    are still not met. Left status: open, queue release stays blocked
    (unchanged convention from followup/followup_2), consistent with this
    file's own "None of ISS-NNNN.yaml's terminal status values truthfully
    describe this outcome" reasoning in diagnosis.note above -- still true
    here, now for a different, narrower set of remaining blockers.
  next_action: >
    Two independent follow-ups now exist, neither of which is
    ISSUE-FIXER's to resolve directly for this issue:
      1. ISS-0717 (ai-dala-infra-side): fix the redeploy-qa.sh/
         docker-compose.qa.yml image-tag mismatch durably, so future
         CD (QA) runs actually deploy. Also needs a bilimbaga-realm
         admin/designer-role Keycloak user seeded (no ticket filed for
         that specific piece yet -- whoever picks up ISS-0717 or a
         follow-on should file it, scoped narrowly: one user, one role,
         one realm) so AC2 (solution pack install) becomes reachable.
      2. ISS-0718 (letflow-side): resolve the CANDIDATE/:EntitiesQuery
         permission gap via CODE-DESIGNER, so the exam-list screen
         actually works for any candidate once seed data exists.
    Once BOTH land: install priv/packs/bilimbaga/pack.json into the
    bilimbaga tenant (PLATFORM_ADMIN- or DefinitionsWrite-capable token
    scoped to bilimbaga), configure >=1 active exam, then re-run
    test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
    via UAT-RUNNER one more time for the real AC4 result this issue
    requires before it can move to resolved.
