id: ISS-0702
title: >
  UAT-RUNNER has no browser-automation tool available -- gui_screen
  scenario legs recurringly BLOCKED
discovered_by: ORCH
discovered_in_run: WF02-REQ360-20260917
discovered_at: "2026-09-17T01:42:40Z"
severity: MAJOR
description: >
  Recurring across two consecutive real-QA WF-02 runs (REQ-358, 2026-09-16,
  test/uat-reports/uat-2026-09-16-WF02-REQ358-20260916.yaml; REQ-360,
  2026-09-17, test/uat-reports/uat-2026-09-17-WF02-REQ360-20260917.yaml).
  Every scenario branch with verification.method: gui_screen and no
  pipeline_test: Playwright spec cannot be executed for real -- no
  Playwright/browser-control tool is available in the UAT-RUNNER agent
  session (checked via ToolSearch both times, none found). UAT-RUNNER
  correctly reports BLOCKED/UNBUILT_FEATURE rather than fabricating
  screenshot evidence, but this means no REQ-360-class platform-scope
  scenario with a real login-routing/GUI assertion can ever produce the
  primary screenshot evidence its own acceptance criteria call for.

  Needs a real fix: either wire a Playwright/browser-automation MCP tool
  into the UAT-RUNNER agent definition, or write real pipeline_test:
  Playwright specs for the scenarios that need gui_screen verification
  (per .claude/agents/uat-runner.md's pipeline_test: key), so this class of
  scenario stops recurring-BLOCKED indefinitely.
affected_files:
  - .claude/agents/uat-runner.md
  - docs/agents/workflows/WF-05_uat_run.md
related_requirements:
  - REQ-358
  - REQ-360
queue_ref: Q-702
github_ref: GH-1476
status: open
pipeline_log:
  - step: ISSUE-FIXER
    at: "2026-09-17T08:32:00Z"
    result: >
      Root-caused: a category error, not a genuine environment gap. Option
      (a) -- an interactive MCP browser-control tool -- confirmed
      genuinely unavailable and not buildable in this pipeline (ToolSearch
      checked, none found). But .claude/agents/uat-runner.md already
      intends pipeline_test: specs to be driven via Bash (npx playwright
      test), which is real, working, and already proven against real
      Keycloak login in web/tests/e2e/uat-alice-login.e2e.spec.ts. Actual
      gap: test/fixtures/uat/scenarios/platform/platform-login-routing-by-role.yaml
      has no pipeline_test: key (28 other scenarios already have one), and
      the agent doc's wording is imprecise enough to cause this exact
      misdiagnosis twice. Recommended: author the missing spec (option b)
      + fix the doc's imprecise description of the driving mechanism.
      Separate PLATFORM_ADMIN HTTP 500 QA regression is already filed as
      ISS-0703, not folded in here.
  - step: CODE-DESIGNER
    at: "2026-09-17T08:44:00Z"
    result: >
      FAIL then PASS (rework). Design:
      lib/letflow/design/iss-0702-uat-runner-pipeline-test-gap-fix.md.
      First attempt CODE-DESIGN-VALIDATOR FAILed: real implementation code
      leaked into the three per-branch sections (complete compilable
      test() bodies, not shapes) and a fabricated citation (invented
      header-comment quote in req133-ac2-ac5.e2e.spec.ts that doesn't
      exist). Reworked to shape-level assertion lists + rationale, and
      replaced the fabricated citation with real verified line numbers
      (:64, :98, :122; also self-caught and fixed an adjacent wrong
      citation, :66 -> :67).
  - step: CODE-DESIGN-VALIDATOR
    at: "2026-09-17T08:50:00Z"
    result: >
      PASS on rework (FAIL on first attempt -- see CODE-DESIGNER entry).
      Independently verified every line citation against the real source
      file byte-accurate; confirmed §1.4-1.6 are now genuine prose
      shapes/rationale with no executable Playwright syntax; confirmed
      nothing else regressed (helper choice, EO mapping, nav-locator
      pattern, pipeline_test: YAML key format, §1.7 framing, scope
      discipline all intact).
  - step: FRONTEND-DEV
    at: "2026-09-17T09:10:00Z"
    result: >
      PASS. Implemented at commit 08652b81: new spec
      web/tests/e2e/pipelines/platform-login-routing-by-role.pipeline.e2e.spec.ts,
      pipeline_test: key added to the scenario yaml, uat-runner.md
      de-ambiguated per design §3. Real 3/3 branch run against local
      stack; unauthenticated branch additionally confirmed against real
      QA (qa.bizdala.com) since it needs no credentials -- QA credentials
      for the other two branches unavailable in this sandbox (no
      qa-login.sh access), noted explicitly rather than faked. Found and
      fixed a real test-instrumentation race (external Playwright polling
      loses the auth-loading placeholder to the redirect's speed on both
      local and QA) via an in-page MutationObserver recording through
      window.name across the cross-origin navigation -- observation only,
      no app-behavior mock. Schema validation: 32/32 clean. type-check/
      lint clean. playwright --list: 290 tests/50 files, no syntax
      errors. npm run guards 48/48. npm test 586/586.
  - step: SECURITY-REVIEWER
    at: "2026-09-17T09:18:00Z"
    result: >
      PASS -- ran full INV-1..INV-9 pass despite zero lib/letflow/ or
      migration touches. INV-4: admin-user/admin-pass/worker-user/
      worker-pass confirmed to be pre-existing established test fixture
      credentials already the default params in helpers.ts and used
      identically across dozens of pre-existing specs, not new/leaked
      secrets. INV-9 confirmed not-applicable: outbound calls are made by
      a Playwright test process against operator/CI-configured env vars,
      no tenant in the loop, not the class of request INV-9 governs. The
      window.name instrumentation carries only a static non-secret marker
      string, ships only in web/tests/, no real concern. No BLOCKER
      findings.
  - step: REVIEWER
    at: "2026-09-17T09:24:00Z"
    result: >
      PASS. Confirmed spec follows established Playwright e2e convention
      (reachability precheck, helper imports, nav-visibility assertions
      all match precedent files exactly, three independent test() blocks
      correctly chosen over createPipeline() since branches are mutually
      exclusive not sequential). The window.name/MutationObserver
      instrumentation FRONTEND-DEV added mid-implementation judged
      legitimate, not scope creep: test-file-only, fulfills a mechanism
      the design already flagged as open/hard (§1.6) without prescribing
      how, observation-only (changes nothing about app behavior), did not
      need a CODE-DESIGNER round-trip. Diff scope matches the reworked
      design exactly. No decision-record conflict. uat-runner.md doc fix
      confirmed accurate and genuinely clearer.
  - step: TEST-DESIGNER
    at: "2026-09-17T09:31:00Z"
    result: >
      No new test warranted -- the deliverable is itself an already-run,
      already-passing test. Verified all three branches' assertions
      against AppShell.tsx's real NAV_ITEMS source of truth, no stale
      link names, no untested branch. Branch 2's single-role sampling
      (TASK_WORKER of three disjunctive roles) is a reasoned,
      already-REVIEWER-passed design decision (§1.5), not re-litigated.
      Confirmed no doc-lint/schema mechanism exists for .claude/agents/*.md
      prose to register with. window.name workaround already proven by
      the spec's own hard assertion on every run, including against real
      QA -- no separate regression test would prove anything beyond
      restating browser API behavior.
  - step: TEST-DESIGN-VALIDATOR
    at: "2026-09-17T09:37:00Z"
    result: >
      FAIL. Independently ran the spec (3/3 passed, own output quoted)
      and confirmed nav-role assertions accurate against AppShell.tsx --
      but found a real coverage gap neither the design nor TEST-DESIGNER
      caught: the scenario yaml's EO-001 detail text carries a
      dashboard-tile sub-claim for branches 1 and 2 ("alongside the same
      dashboard tiles a tenant-scoped user sees" / "dashboard tiles
      reflect this tenant's own data only", both on_fail.severity:
      BLOCKER) that the spec asserts nothing about -- neither route/nav
      checks cover it. This sub-claim was silently dropped somewhere in
      CODE-DESIGNER's design (never mentioned in §1.4/§1.5), not a cited,
      reasoned out-of-scope decision. Requires either a minimal tile
      assertion or an explicit documented out-of-scope citation before
      this can PASS.
