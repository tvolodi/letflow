id: ISS-0706
title: >
  11 web/tests/e2e/*.ts specs poll the never-mounted GET /health/ready as a
  precondition and cannot pass against Letflow's real backend anywhere
discovered_by: TEST-DESIGNER
discovered_in_run: WF03-ISS0702-20260917
discovered_at: "2026-09-17T10:05:00Z"
severity: MAJOR
description: >
  Found while resolving a second TEST-DESIGN-VALIDATOR FAIL on ISS-0702
  (docs/issues/ISS-0702.yaml): a citation of
  web/tests/e2e/tenant-dashboard.e2e.spec.ts's TC-TD-UI-01-04 as proof of
  cross-tenant dashboard-tile isolation was found to be false, because that
  test's own assertServiceReadiness() precondition polls
  `GET ${API_BASE_URL}/health/ready` and throws if it is not `.ok()`.

  `lib/letflow/router.ex`'s own moduledoc (lines 51-55) documents this as a
  deliberate, permanent decision: the R-Co readiness endpoint was never
  ported ("requires S6 observability subsystem probes that do not yet
  exist"). Only `GET /health` (liveness) is mounted. This is not a sandbox
  artifact — `assertServiceReadiness()`'s readiness check cannot pass against
  Letflow's real backend in any environment, local or QA, now or before S6
  observability probes exist.

  A repo-wide grep shows this is not isolated to TC-TD-UI-01-04 — the
  identical `assertServiceReadiness()`/`GET /health/ready` pattern is
  duplicated (copy-pasted, not shared via a helper) across 11 e2e spec files:
    - web/tests/e2e/env04.e2e.spec.ts
    - web/tests/e2e/f5-admin-observability.e2e.spec.ts
    - web/tests/e2e/f6-webhooks.e2e.spec.ts
    - web/tests/e2e/sh05-06.shell.e2e.spec.ts
    - web/tests/e2e/tenants.e2e.spec.ts
    - web/tests/e2e/f5-admin-groups-tokens.e2e.spec.ts
    - web/tests/e2e/f5-admin-users.e2e.spec.ts
    - web/tests/e2e/f6-dlq.e2e.spec.ts
    - web/tests/e2e/iss-0063-oidc-redirect-loop.e2e.spec.ts
    - web/tests/e2e/tenant-dashboard.e2e.spec.ts
    - web/tests/e2e/uat-tenant-url.e2e.spec.ts

  Every test in every one of these files fails at its own precondition
  before reaching its real assertions, whenever actually run. CI excludes
  `tests/e2e/**` entirely (per ISS-0702's own finding), so nothing catches
  this in the normal pipeline — it only surfaces when a test is run for real,
  which is exactly what happened here.

  This is a close cousin of ISS-0532 (docs/issues/ISS-0532.yaml, resolved),
  which fixed the SAME `/health/ready` misconception in application code
  (web/src/api/health.ts) but did not touch the test suite — these 11 spec
  files were not in scope there and the pattern survived independently in
  web/tests/e2e/.

  TC-TD-UI-01-04 additionally has a second, independent defect: its
  cross-tenant leakage assertion is gated behind `if (swiftResp.ok())`, so
  even with a reachable backend it would silently no-op (pass with nothing
  checked) rather than reliably prove isolation if the second tenant weren't
  provisioned. Worth fixing alongside the readiness check, not a separate
  issue.

  Fix should either (a) point all 11 files' assertServiceReadiness() at the
  real `GET /health` (mirroring ISS-0532's resolution exactly), or (b)
  extract a single shared helper so this doesn't recur an 12th time --
  CODE-DESIGNER should evaluate which given how much of the duplication is
  otherwise identical. TC-TD-UI-01-04's `if (swiftResp.ok())` gate should be
  made unconditional (hard-fail if the second tenant can't be provisioned)
  as part of the same fix.
affected_files:
  - web/tests/e2e/env04.e2e.spec.ts
  - web/tests/e2e/f5-admin-observability.e2e.spec.ts
  - web/tests/e2e/f6-webhooks.e2e.spec.ts
  - web/tests/e2e/sh05-06.shell.e2e.spec.ts
  - web/tests/e2e/tenants.e2e.spec.ts
  - web/tests/e2e/f5-admin-groups-tokens.e2e.spec.ts
  - web/tests/e2e/f5-admin-users.e2e.spec.ts
  - web/tests/e2e/f6-dlq.e2e.spec.ts
  - web/tests/e2e/iss-0063-oidc-redirect-loop.e2e.spec.ts
  - web/tests/e2e/tenant-dashboard.e2e.spec.ts
  - web/tests/e2e/uat-tenant-url.e2e.spec.ts
related:
  - ISS-0532
  - ISS-0702
queue_ref: Q-706
github_ref: GH-1488
status: open
pipeline_log:
  - step: CODE-DESIGNER
    at: "2026-09-17T10:00:00Z"
    result: >
      FAIL then PASS (rework). Design:
      lib/letflow/design/iss-0706-e2e-readiness-precondition-fix.md.
      Real scope correction found by direct verification: sh05-06.shell.e2e.spec.ts
      (one of the original 11) is stale -- already fixed under ISS-0532,
      no assertServiceReadiness call left. Real scope is 10 files. Chose
      to extract shared assertBackendHealthy/assertServiceReadiness
      helpers into helpers.ts (already this codebase's established
      shared-helper location for all 10 in-scope files), pointed at real
      GET /health, mirroring ISS-0532's resolution pattern. 8 plain files
      delete their duplicate and import the shared version; 2 variant
      files only swap their backend-check block, preserving Keycloak's
      own legitimate /health/ready probe untouched. TC-TD-UI-01-04's
      if(swiftResp.ok()) silent-noop gate inverted to hard-fail,
      mirroring the same file's own TC-TD-UI-03-01 precedent. First
      attempt CODE-DESIGN-VALIDATOR FAILed: §5 contained real
      implementation code verbatim-identical to the real file. Reworked
      to prose/shape level only.
  - step: CODE-DESIGN-VALIDATOR
    at: "2026-09-17T10:20:00Z"
    result: >
      PASS on rework (FAIL on first attempt -- see CODE-DESIGNER entry).
      Independently verified all scope claims against real source (sh05-06
      staleness, byte-identical two-part shape across all 8 plain files,
      both variant files' real shape, helpers.ts convention, the
      TC-TD-UI-03-01 precedent). Confirmed §5 is now genuine prose with
      no leaked code (grep-verified against the whole file). Confirmed
      nothing else regressed during the rework.
  - step: FRONTEND-DEV
    at: "2026-09-17T10:47:00Z"
    result: >
      PASS. Implemented at commit a6b5e5ac: shared
      assertBackendHealthy/assertServiceReadiness in helpers.ts pointed
      at real GET /health; 8 plain files deduplicated to import it, 2
      variant files' Letflow-backend check swapped (Keycloak's own
      legitimate /health/ready untouched); TC-TD-UI-01-04's silent-noop
      gate inverted to hard-fail. sh05-06.shell.e2e.spec.ts correctly
      left untouched. Verified via curl (GET /health 200, /health/ready
      404) and two real Playwright runs against local backend/Keycloak:
      zero readiness-check failures across all 10 files, only unrelated
      downstream failures (missing env vars, unseeded tenant fixtures,
      the known ISS-0702 vite-proxy quirk). TC-TD-UI-01-04's exact hard-
      fail branch not directly reachable in this DB (missing both
      default/swiftroute tenants), but the byte-identical
      TC-TD-UI-03-01 pattern verified working for real against a
      confirmed-404 endpoint. eslint/tsc clean. npm test 586/586.
  - step: SECURITY-REVIEWER
    at: "2026-09-17T10:55:00Z"
    result: >
      PASS. Zero lib/letflow/ or migration touches confirmed. INV-4:
      apiBaseUrl/BPM_IDP_BASE_URL are parameters/env vars, not new
      hardcoded secrets; TEST_ADMIN_USER/PASSWORD fallbacks are
      pre-existing untouched fixture credentials. INV-9: outbound calls
      made by the Playwright test process against operator/CI-configured
      env vars, no tenant in the loop, same reasoning as ISS-0702.
      TC-TD-UI-01-04's guard inversion judged a net correctness
      improvement (silent false-pass -> hard failure), not a new risk.
  - step: REVIEWER
    at: "2026-09-17T11:02:00Z"
    result: >
      PASS. Confirmed the naming decision matches the design's own §7.1
      resolution (keep name, correct via doc-comment, ISS-0532
      precedent) -- not a FRONTEND-DEV unilateral call. Resolved the
      design's second open question (keep vs. drop a liveness-only
      precondition) with a real verdict: keep it -- fails fast with a
      clear diagnostic instead of an opaque downstream failure, doesn't
      claim to verify DB/auth/subsystem readiness, name-vs-semantics gap
      fully mitigated by centralizing the doc-comment. Spot-checked 3/8
      plain files + both variants, split matches design exactly. Scope
      creep confirmed clean (both incidental findings untouched).
      TC-TD-UI-01-04 confirmed to genuinely mirror TC-TD-UI-03-01, not
      superficially. No decision-record conflict.
