id: ISS-0709
title: >
  flatten_plain_text/1 in help_content.ex over-rejects legitimate content
  spanning paragraph/block boundaries
discovered_by: SECURITY-REVIEWER
discovered_in_run: WF02-REQ364-20260917
discovered_at: "2026-09-17T18:15:00Z"
severity: MINOR
description: >
  Found during REQ-364's round-4 security review, while actively probing
  the new AST-based markdown sanitizer for a new bypass class -- found
  none (this is the opposite class: false positive / over-rejection, not
  a security bypass).

  flatten_plain_text/1 in lib/letflow/help/help_content.ex concatenates an
  entire block's (or effectively the whole document's) prose together with
  no separator when building its fallback text scan for raw-tag detection
  split across inline-formatting nodes (the fix for REQ-364's round-3
  tag-split bypass). This means two unrelated < / > characters in separate
  paragraphs (e.g. used as comparison operators, or generic-type notation)
  can combine into something that looks like a raw HTML tag and get
  falsely rejected.

  Two reproduced examples:
    - Two adjacent paragraphs "Configure with a<" / "b>c or make it safe"
      -> falsely rejected.
    - "if a<b and *count* >d then continue" (bare < / > as comparison
      operators, split by emphasis) -> falsely rejected.

  Confirmed one-directional (never under-restrictive -- walk_node/1's
  structural checks run independently and can't be suppressed). Not a
  security defect, not gating REQ-364's merge (REVIEWER's own
  determination), but real and worth fixing: technical docs legitimately
  using bare < / > as comparison/generic-type notation isn't rare in
  in-app help content.

  FIX DIRECTION (per SECURITY-REVIEWER's own routing note): bound
  flatten_plain_text/1's projection to one paragraph/block's own children
  rather than concatenating across the whole document.
affected_files:
  - lib/letflow/help/help_content.ex
related_requirements:
  - REQ-364
queue_ref: Q-709
github_ref: GH-1508
status: open
pipeline_log:
  - step: CODE-DESIGNER
    at: "2026-09-18T01:25:00Z"
    result: >
      PASS (first attempt). Design:
      lib/letflow/design/iss-0709-flatten-plain-text-scope-fix.md.
      Traced root cause through the real call graph: walk/1's outermost
      call over the document's top-level block siblings is where
      flatten_plain_text/1's unbounded recursion first over-concatenates.
      Fix: no change to call sites, add a new @block_boundary_tags
      exclusion set (p, h1-h6, li, blockquote, td, th) alongside the
      existing code/pre exclusion. Flagged two real open questions
      (cross-adjacent-block detection gap -> REVIEWER; tag-list
      verification against earmark_parser's real source -> ELIXIR-DEV)
      rather than deciding them.
  - step: CODE-DESIGN-VALIDATOR
    at: "2026-09-18T01:39:00Z"
    result: >
      PASS (first attempt). Independently verified the call-graph root
      cause against real code line-by-line. Verified the
      @block_boundary_tags list against earmark_parser 1.4.46's actual
      source (grepped the real dependency's ast_renderer.ex/
      table_renderer.ex), closing that open question before
      implementation starts. Verified all three AST traces (round-3
      non-regression, both issue reproduction cases) against real
      EarmarkParser.as_ast/2 output, not narrative. Confirmed no
      implementation code leaked. Cleared for ELIXIR-DEV.
  - step: ELIXIR-DEV
    at: "2026-09-18T01:43:00Z"
    result: >
      PASS. Implemented at commit d9fba212: new @block_boundary_tags
      list (p, h1-h6, li, blockquote, td, th), new flatten_plain_text/1
      clause excluding those tags, kept separate from the existing
      code/pre clause per design. No change to walk/1/walk_node/1/
      walk_text/1 call sites or arity. Empirically pre-verified against
      the real create_changeset/2 before writing ExUnit tests (avoided
      hand-tracing). 3 new tests added covering both issue-reproduction
      cases plus the round-3 non-regression case. Full suite: 58/58 (55
      existing + 3 new). mix compile --warnings-as-errors clean.
