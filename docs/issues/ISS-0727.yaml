id: ISS-0727
title: >
  qa.bizdala.com tenant-config-by-host resolves to bpm-default realm for
  bilimbaga scenarios, not bilimbaga
discovered_by: UAT-RUNNER
discovered_in_run: gui-review-2026-09-19-bilimbaga-candidate-exam-rerun
discovered_at: "2026-09-19T19:00:24Z"
severity: MAJOR
tags:
  - infra
  - qa-host-config
  - oidc
  - bilimbaga
description: >
  Discovered by UAT-RUNNER during the bilimbaga-candidate-timed-exam-autograde
  GUI review re-run (2026-09-19), as a correction to ISS-0725 (the QA
  candidate-user credential issue), which was resolved on the strength of a
  direct token-endpoint check alone. Driving a real browser against the
  literal dispatched base_url https://qa.bizdala.com (no query string)
  reproduces the ORIGINAL pilot failure end to end: GET
  /api/tenant-config?host=qa.bizdala.com resolves oidc_authority to
  realms/bpm-default (the wrong realm), so candidate-user is rejected by
  Keycloak hosted login form (Invalid username or password) even though the
  credential itself is fine. Every per-tenant subdomain guess tried
  (bilimbaga.qa.bizdala.com, bilimbaga.bizdala.com, qa-bilimbaga.bizdala.com)
  returns the same bpm-default fallback -- this QA host has no per-tenant
  subdomain wired for bilimbaga. Only the realm=bilimbaga query-param
  override (web/src/auth/tenantConfig.ts, function resolveRealmFromUrl, built
  for exactly this local/QA multi-tenant-on-one-host case) reaches the
  correct realm and lets candidate-user authenticate through the real
  browser. This is ai-dala-infra QA-host-config territory (tenant-config-by-
  host wiring), not a web/ code defect.
affected_files:
  - web/src/auth/tenantConfig.ts
  - test/uat-reports/gui-review-2026-09-19-bilimbaga-candidate-exam-rerun.md
suggested_fix: >
  Two possible resolutions: (1) wire qa.bizdala.com tenant-config lookup to
  route bilimbaga scenarios correctly (e.g. a real subdomain, or a
  host-to-realm mapping), or (2) standardize that all future WF-05 dispatch
  base_url values for bilimbaga scenarios explicitly include the
  realm=bilimbaga query param so this is never assumed away again.
severity_rationale: >
  MAJOR -- blocks real browser login for the bilimbaga tenant on QA when
  reached via the realistic bare-host URL (no ?realm= override), which is
  exactly how a production candidate would arrive. Workaround exists
  (explicit ?realm=bilimbaga), so not a full outage, but the default/
  realistic path is broken.
related:
  - ISS-0725  # QA candidate-user credential issue, corrected by this finding
  - ISS-0726  # hard-nav OIDC redirect-target loss -- a related but separate frontend bug, whose own e2e test coverage explicitly names this issue as the source of its scoped-out realm-resolution gap
queue_ref: Q-727
github_ref: GH-1578
resolution: >
  Resolved via the second, repo-actionable option: PR #1583 documented in
  .claude/agents/uat-runner.md's "Environment target" section that every
  future WF-05 dispatch of a bilimbaga scenario against qa.bizdala.com MUST
  set base_url to include ?realm=bilimbaga, so this gap is never silently
  assumed away by a future dispatch again.

  Independently re-verified LIVE against qa.bizdala.com per this issue's own
  AC2 (not assumed) -- UAT-RUNNER's report
  (test/uat-reports/uat-2026-09-19-ISS0727-REALM-REVERIFY-20260919.yaml)
  confirms real network access and:

    GET https://qa.bizdala.com/api/tenant-config?realm=bilimbaga
    -> oidc_authority: "https://auth.qa.bizdala.com/realms/bilimbaga"

  with controls proving this is a real discriminating branch, not a fluke:
  both ?realm=bpm-default and the no-param host-default path return
  oidc_authority pointing at realms/bpm-default, distinct from the bilimbaga
  result above. The bilimbaga realm itself is confirmed live and real:
  GET https://auth.qa.bizdala.com/realms/bilimbaga/.well-known/openid-configuration
  returns a genuine Keycloak OIDC discovery document (HTTP 200, real
  authorization_endpoint/token_endpoint/jwks_uri under realms/bilimbaga).

  This satisfies AC1's OR clause (dispatch-convention update made) combined
  with AC2 (live re-verification, not assumed).

  NOT verified by this close-out: a full Playwright candidate-login
  walkthrough completing an actual Keycloak sign-in (no credential_source
  was supplied to this HTTP-level re-check) -- named honestly as still
  outstanding, not silently covered. The underlying qa.bizdala.com host-to-
  realm mapping (resolution option 1) also remains unfixed; the
  ?realm=bilimbaga query-param path is the sanctioned workaround going
  forward, not a claim that host-based resolution itself now works.
status: resolved
# NOTE: filed by ORCH after briefly, accidentally claiming this queue task
# via a duplicate get_next_task call and releasing it back to open
# (ai-dala-infra/QA-host-config territory, out of scope for a web/-only
# session). Per docs/agents/protocols/ISSUE_QUEUE.md, the local record
# should exist regardless of who eventually fixes it -- filed here to close
# that gap, discovered when ISS-0726's own test-spec coverage-table citation
# of this file was found broken (the file didn't exist yet) during
# TEST-DESIGN-VALIDATOR's review of PR #1581.
