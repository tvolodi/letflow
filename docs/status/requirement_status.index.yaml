# Requirement run-history index.
#
# THIS FILE IS THE ENTRY POINT. To append a status event, or to look one up, read
# this file first — it is small and always readable in one call — then act on the
# volume it names. Do not guess volume filenames; they are listed here.
#
# The run history is kept as a sequence of VOLUMES. Exactly one volume is
# `current` at a time; all others are `closed` and frozen. A volume is closed and
# a successor opened when it crosses the size ceiling below, so that the current
# volume is ALWAYS small enough to read in a single un-scoped Read call. That
# bound is the whole point: the append-only safeguard requires seeing the file's
# schema before appending, and a file too large to read cannot deliver it.
#
# The append-only rule is unchanged and absolute: entries are appended, never
# edited, never reordered, never deleted, in any volume, closed or current.

roll_rule:
  # Checked by DOC-UPDATER AFTER writing its entry, and by the Step 4 invariant
  # test on every `mix test` run. Measure the WORKING-TREE file (CRLF on Windows),
  # because that is what the Read tool measures.
  max_lines: 1200
  max_bytes: 120000
  read_tool_hard_limit_bytes: 262144   # headroom is deliberate, do not raise the ceiling
  read_tool_default_line_limit: 2000   # a "full read" past this silently truncates
  on_exceed: >
    Close the current volume (append the closure footer, set status: closed here),
    create the next volume with the SAME header text and an empty `history:` list,
    and set it current here. Full procedure: the "WHEN THIS VOLUME IS FULL" section
    of the current volume's own header.

volumes:
  - volume: 1
    path: docs/status/requirement_status.yaml
    status: closed
    opened: 2026-08-13
    closed: 2026-08-21
    entries: 185
    lines: 5766
    bytes_working_tree: 361376
    # SHA-256 of lines 1..5766 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 5766
    frozen_prefix_sha256: "5a1a64ab0b999da3fd86be90109ecee46b9d538d9e9945c0d39fddb10075a804"
    note: >
      Closed under ISS-0119 at 361,376 bytes, above the 262,144-byte read limit.
      Not split and not renamed: ~103 historical run records under handoffs/ and
      test/reports/ cite this path, and
      lib/letflow/design/iss-0078-pin-rebind-provenance.md:367 cites line 5158 by
      number. Freezing in place keeps every one of those citations valid.

  - volume: 2
    path: docs/status/requirement_status.v2.yaml
    status: closed
    opened: 2026-08-21
    closed: 2026-08-22
    entries: 44
    lines: 1429
    bytes_working_tree: 93926
    # SHA-256 of lines 1..1429 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1429
    frozen_prefix_sha256: "895b92989ccfcc3ebda222ab448e9303ddd5c201df5aad4fdc1540ffff62879e"
    closure_note: >
      Closed under ISS-0263 (WF03-ISS0263-20260822) at 1407 lines / 92601 bytes /
      43 entries -- over its own declared roll_rule ceiling (max_lines: 1200),
      confirmed failing test/docs/requirement_status_invariants_test.exs's
      assertion A3. Final entry count 44 includes this closing run's own
      `started` event (SCOPE-CHANGE), appended to this volume before the roll
      per HOW-TO-APPEND step 1 ("confirm you are writing to the current volume"
      -- this volume was still current at that moment). Reconciliation: 43
      entries present immediately before this run touched the file, 44 after
      (43 pre-existing + 1 new started-event), 0 rewritten/reordered/deleted --
      confirmed via `git diff --numstat` showing insertions-only on this file
      across the whole run. Successor: docs/status/requirement_status.v3.yaml.
    note: >
      Incremented to 43 by WF02-REQ085-20260822's own started+done event pair
      (2 appends, recounted from the composed file per this note's own
      recount-every-time rule -- 43 on-disk `- req:` entries in
      requirement_status.v2.yaml after rebasing this run's branch cleanly
      onto origin/main at 41).
      Incremented to 38 in the same commit as WF03-ISS0260-20260822's own done-event
      append (Step 5/Step Final, ISSUE-FIXER) -- 38 on-disk `- req:` entries at that
      point. That run's Step Final rebase conflicted at this file's tail against
      REQ-119's done-event (landed on main after this run's own Step 5 commit) and
      composed both appends -- REQ-119 done (08:24:39Z) then ISS-0260 done
      (08:36:06Z), kept in that chronological order -- re-deriving 39 from the
      composed file (39 on-disk `- req:` entries), per this note's own stated rule:
      recount from the composed file every time, never carry the previous attempt's
      number forward.
      Opened by ISS-0119's fix. Successor to volume 1. `entries:` corrected
      2026-08-21 (ISS-0199, run WF03-ISS0199-20260821) from a stale declared 0
      to the real on-disk count of 7 -- HOW-TO-APPEND step 6 previously had no
      instruction to keep this updated, so 7 real appends had landed with the
      declared count never moved. Now checked by assertion A9. Incremented to
      8 in the same commit as WF02-REQ067-20260821's own started-event append,
      then to 9 in the same commit as that run's own done-event append
      (Step 6/DOC-UPDATER), then to 11 when WF02-REQ067-20260821's Step Final
      rebase composed both of that run's entries with two concurrently-landed
      entries from main (REQ-068 done, REQ-069 done) that had independently
      incremented it to 9 there. Incremented to 21 in the same commit as
      WF02-REQ073-20260822's own done-event append (Step 6/DOC-UPDATER), then
      to 23 in the same commit as WF02-REQ074-20260822's own done-event append
      (Step 6/DOC-UPDATER), then to 24 in the same commit as
      WF02-REQ075-20260822's own started-event append. Two further increments
      landed concurrently from that point: WF03-ISS0224-20260822's own
      done-event append took it to 25 on main, while this run's own Step
      Final rebase was independently composing its started-event (24) with
      its own done-event append (originally also computed as 25 against the
      pre-ISS-0224 base). That Step Final rebase (WF02-REQ075-20260822)
      composed all three appends -- REQ-075 started, ISS-0224 done, REQ-075
      done -- kept in that chronological order by `at:` timestamp, bringing
      the true on-disk count to 26. Both sides of the conflict were kept --
      the file is append-only, so the resolution is a composition, never a
      choice between the two counts. Incremented to 27 by
      WF03-ISS0230-20260822's Step Final rebase, which had computed 25 against
      a pre-REQ-075 base and re-derived it as 27 after composing its own
      done-event append onto the REQ-075 and ISS-0224 entries that had landed
      on main in the interim -- the same composition rule, applied again.
      Incremented to 29 by WF03-ISS0229-20260822 Step Final, which had
      computed 25 against a pre-REQ-075 base; its rebase conflicted against a
      declared 28 on main, and the value was re-derived from the composed file
      itself (29 on-disk `- req:` entries) rather than taken from either side
      of the conflict. Its own done-event (04:49:20Z) sorts after all three
      entries it composed with, so no reordering was needed. Re-derived again as
      30 when that same Step Final rebased a second time onto a further-advanced
      main; the count is taken from the composed file each time, never carried
      over from the previous attempt.
      Then to 31 by WF03-ISS0227-20260822's Step Final, which rebased TWICE.
      That run appended its own done-event against a base declaring 26 and
      computed 27; its first rebase found main at 28 (ISS-0230, ISS-0231) and
      re-derived 29 from the composed file; main advanced again before the PR
      could merge (REQ-077, ISS-0229) and the second rebase re-derived 31 from
      the newly composed file. Its done-event (05:10:23Z) sorts after every
      entry it composed with on both attempts, so no reordering was needed
      either time. The point this run illustrates, and the reason the count is
      never carried forward: a rebase can happen more than once against a
      moving main, and each attempt's correct count is a fact about the file
      that attempt produced -- 29 was right when it was written and wrong an
      hour later. Recount from the composed file every time; never increment
      the previous attempt's number.
      Incremented to 32 by WF03-ISS0258-20260822's Step Final rebase, which
      had appended its own done-event against a base declaring 29 and
      computed 30; its rebase found main advanced to 31 (ISS-0227) and
      re-derived 32 from the composed file (32 on-disk `- req:` entries),
      per this note's own stated rule -- recount from the composed file,
      never carry the previous attempt's number forward.
      Then to 33 by WF02-REQ083-20260822's Step Final rebase (first attempt),
      whose own base declared 32; that rebase conflicted at the file's tail
      against ISS-0227's done-event (landed on main after this run's Step 00
      commit) and composed both appends -- ISS-0227 done (05:10:23Z) then
      REQ-083 started (05:23:40Z), kept in that chronological order -- to 33.
      Main advanced again before the PR could merge (ISS-0258, done
      06:40:00Z), and a second rebase re-derived 34 from the newly composed
      file: REQ-083 started (05:23:40Z), REQ-083 done (06:33:39Z), ISS-0258
      done (06:40:00Z), each conflict composed in chronological order by
      `at:` timestamp rather than either side's count being carried forward
      -- the same recount-every-time rule this note already states, applied
      across two rebase attempts in one run, as WF03-ISS0227-20260822's entry
      above illustrates for a different run.
      Incremented to 33 by WF02-REQ078-20260822's Step Final rebase, which had
      appended its own started-event against a base declaring 28; its rebase
      found main advanced to 32 (ISS-0231, REQ-077 blocked, ISS-0229,
      ISS-0258) and re-derived 33 from the composed file (33 on-disk `- req:`
      entries in requirement_status.v2.yaml), per this note's own stated rule.
      This run's started-event (04:58:01Z) sorted between ISS-0229's done
      (04:49:20Z) and ISS-0258's done (06:40:00Z), so it was reordered into
      that chronological position rather than appended after ISS-0258's
      entry. The run's own done-event (07:08:07Z) is later than every entry
      composed here and needs no reordering when it lands.
      Both of the two notes immediately above describe genuinely concurrent
      runs (WF02-REQ083-20260822 and WF02-REQ078-20260822) that each computed
      their own count independently from a shared base of 32 and diverged
      from there -- neither is wrong, each was accurate for the file its own
      rebase produced at the time. Composed here at 35, in
      WF02-REQ078-20260822's second Step Final rebase, which found main had
      advanced past its first rebase (REQ-083 started, REQ-083 done, both
      landed via PR #511) and re-derived 35 from the fully composed file (35
      on-disk `- req:` entries in requirement_status.v2.yaml) -- this run's
      own started-event (04:58:01Z) already sat, unmoved, between ISS-0229's
      done (04:49:20Z) and ISS-0227's done (05:10:23Z) in the v2.yaml
      three-way merge, which applied cleanly with no conflict on that file;
      only this index file's declared count and narrative needed manual
      composition.
      Incremented to 34 in the same Step Final rebase's second conflict, when
      that done-event was itself replayed: it landed after WF03-ISS0227-20260822's
      and WF03-ISS0258-20260822's entries with no reordering needed, exactly as
      predicted above, and 34 is the recounted on-disk `- req:` total.
      Composed to 36 in WF02-REQ078-20260822's THIRD Step Final rebase attempt
      (main had advanced again, past PR #511/REQ-083, before the second
      attempt's PR could merge). The done-event replay found no new conflict
      in requirement_status.v2.yaml itself -- it landed cleanly after
      ISS-0258's entry with no reordering, same as the second attempt found --
      only this index file's count needed recomputing: 36 on-disk `- req:`
      entries. Both prior notes in this composed block (the "35" and "34"
      narrations) are left as written, per the recount-every-time rule they
      each state.
      Incremented to 41 by WF02-REQ127-20260822 (FRONTEND-DEV), which appended
      its own started-event and done-event against a base declaring 39
      on-disk `- req:` entries and recounted 41 from the file it produced,
      per the same recount-every-time rule. Will be corrected again at Step
      Final if the rebase finds main advanced further.

  - volume: 3
    path: docs/status/requirement_status.v3.yaml
    status: closed
    opened: 2026-08-22
    closed: 2026-08-23
    entries: 43
    lines: 1263
    bytes_working_tree: 76551
    # SHA-256 of lines 1..1263 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1263
    frozen_prefix_sha256: "10dcc89c10a288695c4db188a9c80da799831d410f8adae8aa61f92e4e486f9b"
    closure_note: >
      Closed under WF02-REQ082-20260823's Step Final rebase at 1263 lines /
      76551 bytes / 43 entries -- over its own declared roll_rule ceiling
      (max_lines: 1200), confirmed failing
      test/docs/requirement_status_invariants_test.exs's assertion A3.
      Reconciliation: 43 entries present immediately before this run's Step
      Final rebase composed REQ-076's/ISS-0287's already-landed completions
      with this run's own done-event (0 rewritten/reordered/deleted --
      `git diff --numstat` on this file across the whole run showed
      insertions-only). Successor: docs/status/requirement_status.v4.yaml.
      WF02-REQ125-20260823's own Step Final rebase (2026-08-23) found this
      volume already closed by WF02-REQ082-20260823 (landed on main first),
      so its own REQ-125 started-event -- originally appended here before
      that run knew of REQ-082's roll -- was composed into volume 4 instead,
      per the established "one run's started/done land one in each volume
      across a roll boundary" pattern (ISS-0263's own note above). This
      volume's frozen content is therefore unchanged by that composition.
    note: >
      Incremented to 42 by WF03-ISS0287-20260823 (ORCH)'s own done-event
      append -- 42 on-disk `- req:` entries in requirement_status.v3.yaml,
      no rebase conflict on this file.
      Recounted to 41 by WF02-REQ076-20260822's Step Final rebase, which
      conflicted at requirement_status.v3.yaml's tail against
      WF02-REQ079-20260822's, WF02-REQ080-20260823's, and
      WF02-REQ081-20260823's own entries (all landed on main after this
      run's Step 00 commit). Composed in chronological `at:` order --
      REQ-076 started (18:40:59Z), REQ-079 started (19:00:00Z), REQ-079 done
      (19:05:59Z), REQ-076 done (21:58:43Z), REQ-080 started
      (2026-08-23T00:32:00Z), REQ-081 started (00:35:00Z), REQ-080 done
      (01:45:00Z), REQ-081 done (02:58:00Z) -- REQ-076's own two entries
      were reordered around REQ-079's pair rather than appended after it;
      REQ-080's and REQ-081's entries needed no reordering relative to each
      other or to REQ-076/REQ-079. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v3.yaml` = 41,
      rather than carrying forward either side's independently-computed
      count (39 from the REQ-081 side, 35 from this run's own pre-rebase
      commit), per this note's own recount-every-time rule.
      Incremented to 35 by DOC-UPDATER's own REQ-076 done-event append
      (WF02-REQ076-20260822, Step 6) -- 35 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file,
      recounted via `grep -c "^  - req:"
      docs/status/requirement_status.v3.yaml` per this note's own
      recount-every-time rule.
      Incremented to 34 by ORCH's own REQ-076 started-event append
      (WF02-REQ076-20260822, Step 00) -- 34 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file, recounted
      via `grep -c "^  - req:" docs/status/requirement_status.v3.yaml` per
      this note's own recount-every-time rule (this file's own declared
      count had drifted one behind the true on-disk count, caught by
      test/docs/requirement_status_invariants_test.exs's assertion A9 during
      WF02-REQ076-20260822's Step 4 run).
      Recounted to 33 during LOOP-20260822-orch (ORCH)'s Step Final rebase for
      ISS-0119: this run's own done-event (18:39:00Z) conflicted at
      requirement_status.v3.yaml's tail against a concurrently-landed sibling
      fork's ISS-0286 done-event (18:40:00Z, queue task 286, GH#564).
      Composed in chronological `at:` order -- ISS-0119 done (18:39:00Z) then
      ISS-0286 done (18:40:00Z), this run's own entry sorted first and needed
      no reordering. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v3.yaml` = 33,
      rather than carrying forward either side's independently-computed count
      (32 from the ISS-0286 side, 31 from this run's own pre-rebase commit),
      per this note's own recount-every-time rule.
      Incremented to 31 by LOOP-20260822-orch (ORCH)'s own done-event append
      for ISS-0119 -- 31 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file (computed
      pre-rebase, superseded by the recount above).
      Incremented to 30 by LOOP-20260822-orch (ORCH)'s own done-event append
      for ISS-0128 -- 30 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file.
      Incremented to 29 by LOOP-20260822-orch (ORCH)'s own done-event append
      for ISS-0285 -- 29 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file.
      Incremented to 28 by WF03-ISS0129-20260822 (ORCH)'s own done-event append
      -- 28 on-disk `- req:` entries in requirement_status.v3.yaml, no rebase
      conflict on this file.
      Incremented to 26 by WF03-ISS0277-20260822 (ORCH)'s own done-event
      append -- 26 on-disk `- req:` entries in requirement_status.v3.yaml,
      no rebase conflict on this file (appended cleanly at the tail after
      WF03-ISS0281-20260822's entry).
      Incremented to 27 by WF03-ISS0283-20260822 (ORCH)'s own done-event
      append -- 27 on-disk `- req:` entries in requirement_status.v3.yaml,
      no rebase conflict on this file (appended cleanly at the tail after
      WF03-ISS0277-20260822's entry).
      Opened by ISS-0263's roll (WF03-ISS0263-20260822), successor to volume 2.
      Created with volume 2's header text verbatim (volume number, opening date
      and predecessor path updated) and an empty `history:` list, per this
      index's `roll_rule.on_exceed` procedure. `entries:` will be incremented
      per HOW-TO-APPEND step 6 as real appends land, starting with this
      closing run's own `done` event.
      Incremented to 3 by WF02-REQ128-20260822 (ELIXIR-DEV)'s own
      started+done event pair (2 appends, recounted from the composed file
      per this note's own recount-every-time rule -- 3 on-disk `- req:`
      entries in requirement_status.v3.yaml). This run's Step Final rebase
      found ISS-0263's roll had landed on main mid-run: its own
      docs/status/requirement_status.v2.yaml commit (written against v2
      while v2 was still current) conflicted against the closure footer,
      resolved by discarding the v2 insertion entirely (v2 is now frozen,
      confirmed byte-identical to origin/main's version by `git diff`) and
      appending both events to v3.yaml instead, per the same "one run's
      started/done land one in each volume across a roll boundary" pattern
      ISS-0263's own entry above establishes.
      Incremented to 4 by WF03-ISS0264-20260822 (ISSUE-FIXER)'s Step Final
      rebase, which conflicted at requirement_status.v3.yaml's tail against
      WF02-REQ128-20260822's own started+done pair (landed on main after this
      run's Step 00 commit). Composed both sides in chronological `at:` order
      -- REQ-128 started (09:26:00Z), REQ-128 done (10:04:09Z), ISS-0264 done
      (10:13:48Z) -- no reordering needed since this run's own done-event
      already sorted last. Recounted from the composed file per the
      recount-every-time rule: 4 on-disk `- req:` entries in
      requirement_status.v3.yaml (up from 3 declared pre-rebase and 2
      computed by this run's own pre-rebase commit against its stale base).
      Incremented to 5 by WF02-REQ118-20260822 (ELIXIR-DEV)'s Step Final
      rebase, which conflicted at requirement_status.v3.yaml's tail against
      both WF02-REQ128-20260822's and WF03-ISS0264-20260822's entries (all
      landed on main after this run's Step 00 commit). Composed in
      chronological `at:` order -- REQ-128 started (09:26:00Z), REQ-128 done
      (10:04:09Z), REQ-118 started (10:09:04Z), ISS-0264 done (10:13:48Z) --
      this run's own started-event sorted between REQ-128 done and ISS-0264
      done, so it was reordered into that position rather than appended
      after ISS-0264's entry. Recounted from the composed file: 5 on-disk
      `- req:` entries in requirement_status.v3.yaml. This run's own
      done-event (appended after Step 00, ahead of this rebase) sorts after
      all of the above by `at:` and needs no reordering when it replays.
      Incremented to 6 when this run's own done-event (10:29:11Z) replayed
      during the same Step Final rebase's second conflict: it lands after
      ISS-0264's done-event (10:13:48Z) with no reordering needed, exactly
      as predicted immediately above. 6 is the recounted on-disk `- req:`
      total in requirement_status.v3.yaml.
      Incremented to 7 by WF03-ISS0265-20260822 (ISSUE-FIXER)'s own
      done-event append (7 on-disk `- req:` entries in
      requirement_status.v3.yaml), no rebase conflict on this file.
      Incremented to 9 by WF02-REQ120-20260822 (CODE-DESIGNER)'s own
      started+done event pair (2 appends, recounted from the composed file
      per this note's own recount-every-time rule -- 9 on-disk `- req:`
      entries in requirement_status.v3.yaml).
      Composed to 11 by WF02-REQ126-20260822 (ELIXIR-DEV)'s Step Final
      rebase, which had appended its own started+done event pair against a
      base declaring 6 and computed 8; its rebase found main advanced to 9
      (ISS-0265 done, REQ-120 started+done) and conflicted at this file's
      tail in both requirement_status.v3.yaml and here. Composed in
      chronological `at:` order -- ISS-0265 done (10:35:22Z), REQ-126
      started (11:15:00Z), REQ-120 started (11:20:23Z), REQ-120 done
      (11:20:23Z), REQ-126 done (11:30:40Z) -- REQ-126's started-event
      sorted between ISS-0265's done and REQ-120's started, so it was
      reordered into that position; REQ-126's own done-event sorted last
      and needed no reordering. Recounted from the composed file: 11
      on-disk `- req:` entries in requirement_status.v3.yaml.
      Incremented to 12 by WF02-REQ129-20260822 (ELIXIR-DEV)'s own
      done-event append (12 on-disk `- req:` entries in
      requirement_status.v3.yaml), no rebase conflict on this file.
      Incremented to 13 by WF02-REQ124-20260822 (ELIXIR-DEV)'s Step Final
      rebase (second attempt -- main advanced again past the first
      attempt's push, from REQ-129 landing before the PR could merge). The
      first attempt had appended its own started-event against a base
      declaring 6 and computed 7, then re-derived 12 after composing with
      main's ISS-0265 done, REQ-126 started+done, and REQ-120 started+done
      (11 on-disk entries pre-composition). This second attempt found main
      further advanced by REQ-129's own done-event (12:07:32Z, no conflict
      on requirement_status.v3.yaml itself -- it landed cleanly at the file's
      tail after REQ-126's done-event, while this run's own started-event
      from the first attempt's composition remained, unmoved, between
      ISS-0265's done and REQ-126's started). Only this index file's count
      and narrative needed manual re-composition here. Recounted from the
      composed file: 13 on-disk `- req:` entries in requirement_status.v3.yaml.
      Incremented to 14 in the same Step Final rebase's second conflict on
      this attempt, when this run's own done-event (12:04:29Z) replayed:
      it conflicted at requirement_status.v3.yaml's tail against
      WF02-REQ129-20260822's own done-event (12:07:32Z, landed on main
      after this attempt's rebase began). Composed in chronological `at:`
      order -- REQ-124 done (12:04:29Z) then REQ-129 done (12:07:32Z),
      REQ-124's done-event sorted immediately before REQ-129's and needed
      no reordering relative to any earlier entry. Recounted from the
      composed file: 14 on-disk `- req:` entries in requirement_status.v3.yaml.
      Incremented to 15 by WF03-ISS0273-20260822 (ORCH)'s own done-event
      append (15 on-disk `- req:` entries in requirement_status.v3.yaml),
      no rebase conflict on this file.
      Incremented to 17 by WF02-REQ115-20260822 (DOC-UPDATER)'s own
      done-event append (REQ-115's started-event was already entry 16,
      appended earlier in this same run by ORCH at Step 00) -- 17 on-disk
      `- req:` entries in requirement_status.v3.yaml, no rebase conflict
      on this file.
      Incremented to 19 by ORCH's own REQ-116 started-event append
      (ADHOC-20260822-001) -- recounted from the composed file per this
      note's own recount-every-time rule: 19 on-disk `- req:` entries in
      requirement_status.v3.yaml (the declared `entries: 18` above was
      already one behind the true on-disk count at the time this was
      written, from WF03-ISS0274-20260822's SCOPE-CHANGE done-event landing
      without its own index-note increment; both are folded into this
      recount rather than corrected retroactively).
      Incremented to 20 by DOC-UPDATER's own REQ-116 done-event append
      (ADHOC-20260822-001, Step E) -- 20 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file.
      Incremented to 21 by WF03-ISS0275-20260822 (ISSUE-FIXER)'s own
      done-event append -- 21 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file.
      Incremented to 23 by WF03-ISS0276-20260822 (ORCH)'s own done-event
      append -- recounted from the composed file per this note's own
      recount-every-time rule: 23 on-disk `- req:` entries in
      requirement_status.v3.yaml (the declared `entries: 21` above was
      already one behind the true on-disk count of 22 at the time this was
      written, from a concurrently-landed run's own SCOPE-CHANGE done-event;
      both are folded into this recount rather than corrected retroactively).
      Recounted to 24 by WF03-ISS0096-20260822 (REVIEWER)'s Step Final
      rebase, which conflicted at requirement_status.v3.yaml's tail against
      WF03-ISS0276-20260822's own SCOPE-CHANGE done-event (landed on main
      after this run's own append). This run's own done-event (16:19:49Z)
      sorts before WF03-ISS0276-20260822's (16:20:15Z), so it was composed
      first rather than appended after. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v3.yaml` = 24
      rather than incrementing either side's independently-computed 23,
      per this note's own recount-every-time rule.
      Separately, and concurrently, incremented to 24 by WF03-ISS0281-20260822
      (ORCH)'s own done-event append -- 24 on-disk `- req:` entries in
      requirement_status.v3.yaml at THAT run's own base, no rebase conflict
      found by that run against this file. This run resolved ISS-0281 (the
      A9 entry-count-drift finding filed by WF03-ISS0276-20260822 above) as
      already fixed: this same commit's predecessor (d88b4ec) had already
      corrected the declared count from 21 to 23 moments before ISS-0281
      was filed against the transient pre-fix state; see ISS-0281's own
      run-history entry for the full root-cause account.
      Both notes immediately above independently and correctly computed 24
      -- each was accurate for the file its own run produced, from a shared
      base of 23 (WF03-ISS0276-20260822's own done-event), before either had
      seen the other's append. Composing both onto this branch's own rebase
      (ORCH, this Step Final) found all three done-events already present in
      requirement_status.v3.yaml in correct chronological `at:` order with
      NO conflict on that file itself: ISS-0096 done (16:19:49Z), ISS-0276
      SCOPE-CHANGE done (16:20:15Z), ISS-0281 done (16:29:20Z) -- only this
      index file's declared count and narrative needed manual composition.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v3.yaml` = 25, rather than carrying
      forward either side's independently-computed 24, per this note's own
      recount-every-time rule.
      Incremented to 28 by WF03-ISS0284-20260822 (ORCH)'s own done-event
      append -- 28 on-disk `- req:` entries in requirement_status.v3.yaml,
      no rebase conflict on this file, THIS run's own pre-push count.
      Separately, and concurrently, incremented to 28 by
      WF03-ISS0129-20260822 (ORCH)'s own done-event append and to 29 by
      LOOP-20260822-orch (ORCH)'s own ISS-0285 done-event append -- both
      landed on main, each accurate for the file its own run produced,
      from the same shared base of 27 (WF03-ISS0283-20260822's own
      done-event) this run also started from. Composing all three onto
      this branch's own rebase found all three done-events already
      present in requirement_status.v3.yaml with NO conflict on that file
      needing more than chronological `at:` reordering: ISS-0129 done
      (18:19:15Z), ISS-0284 done (18:28:43Z), ISS-0285 done (18:35:00Z) --
      only this index file's declared count and narrative needed manual
      composition. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v3.yaml` = 30, rather than carrying
      forward any side's independently-computed 28/29, per this note's
      own recount-every-time rule.
      Incremented to 31 by ISS-0128's own done-event append (queue task 128,
      stale-duplicate release, no code change), no rebase conflict on this file.
      Incremented to 32 by ORCH's own ISS-0286 done-event append (watch-item,
      docs-only, no code change) -- 32 on-disk `- req:` entries in
      requirement_status.v3.yaml, no rebase conflict on this file.
      Incremented to 33 by a concurrent sibling session's own append (queue
      task 119, fourth ISS-0285-class stale-duplicate occurrence, commit
      c2e4105) -- landed on main without its own index-note increment;
      recounted here rather than corrected retroactively, per this note's
      own recount-every-time rule.
      Incremented to 35 by WF02-REQ079-20260822 (ORCH acting directly through
      the full pipeline)'s own started+done event pair (2 appends) -- 35
      on-disk `- req:` entries in requirement_status.v3.yaml, no rebase
      conflict on this file.
      Incremented to 37 by WF02-REQ080-20260823 (fork of ORCH, full pipeline
      executed directly)'s own started+done event pair (2 appends) -- 37
      on-disk `- req:` entries in requirement_status.v3.yaml, no rebase
      conflict on this file.
      Incremented to 39 by WF02-REQ081-20260823 (fork of ORCH, full pipeline
      executed directly)'s own started+done event pair (2 appends) -- 39
      on-disk `- req:` entries in requirement_status.v3.yaml, no rebase
      conflict on this file.
      Incremented to 40 by WF02-REQ082-20260823 (fork of ORCH, full pipeline
      executed directly)'s own done-event append, against a base declaring
      39. Composed to 43 at that same run's Step Final rebase, which
      conflicted at requirement_status.v3.yaml's tail against
      WF02-REQ076-20260822's own Step Final composition (landed on main at
      41) and WF03-ISS0287-20260823's own done-event (landed on main at 42)
      -- both landed after this run's own append had already been written
      against a stale pre-REQ-076 base. Composed in chronological `at:`
      order -- REQ-076 done, ISS-0287 done (23:21:37Z), REQ-082 done
      (04:21:00Z on 2026-08-23, sorting last, no reordering needed) --
      recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v3.yaml` = 43, rather than carrying
      forward either side's independently-computed 40/42, per this note's
      own recount-every-time rule. Rolled immediately after this recount
      (this file crossed its own roll_rule ceiling) -- see volume 4 below.
      WF02-REQ125-20260823's own Step Final rebase found this volume already
      closed at 43 by WF02-REQ082-20260823 above; its own REQ-125
      started-event (independently appended here against a stale
      pre-REQ-082 base, also computing 43) was composed into volume 4
      instead rather than counted against this volume, since a closed
      volume never regains an entry once rolled -- see volume 4's note
      below.

  - volume: 4
    path: docs/status/requirement_status.v4.yaml
    status: closed
    opened: 2026-08-23
    closed: 2026-08-26
    entries: 56
    lines: 1203
    bytes_working_tree: 68911
    # SHA-256 of lines 1..1203 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1203
    frozen_prefix_sha256: "41596491a7bd8020820f0bd32fd6cae6220a32958d7d27e8afb81d7313ab6fde"
    closure_note: >
      Closed under WF02-REQ151-20260826's Step Final rebase at 1203 lines /
      68911 bytes / 56 entries -- over its own declared roll_rule ceiling
      (max_lines: 1200), confirmed failing
      test/docs/requirement_status_invariants_test.exs's assertion A3.
      Composing main's concurrently-landed ISS-0332-done/REQ-151-started/
      REQ-149-done chain with this run's own REQ-151-done append is what
      brought the count to 56 -- 0 entries rewritten/reordered/deleted
      (`git diff --numstat` on this file across the whole run showed
      insertions-only, aside from restoring the ISS-0332 note body a
      same-line direct-to-main edit, fc4d240, had trimmed -- see the volume's
      own history for that entry). Successor:
      docs/status/requirement_status.v5.yaml.
    note: >
      Incremented to 56 during WF02-REQ151-20260826's own Step Final rebase:
      composed main's REQ-151-started/REQ-149-done chain (which had reached
      55) with this branch's own REQ-151-done append (independently computed
      against a stale pre-REQ-149 base of 53->54) by interleaving the latter
      after REQ-149's done-event in true chronological order -- 56 on-disk
      `- req:` entries in requirement_status.v4.yaml after composition, which
      also crossed this volume's roll ceiling and triggered its closure
      above. Also restored the SCOPE-CHANGE/ISS-0332 done-event's full note
      body, trimmed by a same-line direct-to-main edit (fc4d240) during its
      own vocab fix; restoring it does not change the entry count.
      Incremented to 55 by WF02-REQ149-20260826 (DOC-UPDATER) REQ-149 done-event
      append (Step 6) -- 55 on-disk `- req:` entries in
      requirement_status.v4.yaml, no rebase conflict on this file.
      Incremented to 54 by WF02-REQ149-20260826 (TEST-RUNNER) A9 fix: vocab-fix commit added one entry.
      Incremented to 53 by WF03-ISS0332-20260826 (ORCH) ISS-0332 done-event append.
      Incremented to 52 by WF02-REQ077-20260826 (DOC-UPDATER)'s own done-event
      append (Step 6) -- 52 on-disk `- req:` entries in
      requirement_status.v4.yaml, no rebase conflict on this file.
      Opened by WF02-REQ082-20260823's own Step Final roll, successor to
      volume 3. Created with volume 3's header text verbatim (volume number,
      opening date and predecessor path updated) and an empty `history:`
      list, per this index's `roll_rule.on_exceed` procedure. `entries:`
      will be incremented per HOW-TO-APPEND step 6 as real appends land.
      Incremented to 4 by WF02-REQ135-20260823 (ORCH)'s own started+done
      event pair (2 appends, no rebase conflict on this file) -- 4 on-disk
      `- req:` entries in requirement_status.v4.yaml.
      Incremented to 5 by ORCH's own ISS-0288 done-event append (docs-only
      citation-path fix, no rebase conflict on this file) -- 5 on-disk
      `- req:` entries in requirement_status.v4.yaml.
      Incremented to 6 by WF02-REQ125-20260823 (ELIXIR-DEV)'s Step Final
      rebase, which found this volume already opened and populated by
      WF02-REQ082-20260823/WF02-REQ135-20260823/ISS-0288 (landed on main
      first). REQ-125's own started-event -- originally appended to volume 3
      before that volume's roll -- was composed here instead, appended after
      the existing 5 entries (its own `at:` timestamp, 2026-08-22T23:25:09Z,
      sorts before all five existing entries here, but it is appended rather
      than reordered since this composition is across a volume boundary, not
      a same-file chronological merge -- volume 3 is frozen and cannot regain
      an entry once closed). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v4.yaml` = 6.
      Incremented to 7 by WF02-REQ125-20260823 (DOC-UPDATER)'s own REQ-125
      done-event append (Step 6), composed in this same Step Final rebase --
      7 on-disk `- req:` entries in requirement_status.v4.yaml.
      Incremented to 8 by ORCH's own REQ-117 blocked-event append -- 8
      on-disk `- req:` entries in requirement_status.v4.yaml, no rebase
      conflict on this file.
      Incremented to 9 by ORCH's own REQ-121 started-event append (Step 00)
      -- 9 on-disk `- req:` entries in requirement_status.v4.yaml, no rebase
      conflict on this file.
      Incremented to 10 by WF02-REQ121-20260823 (DOC-UPDATER)'s own REQ-121
      done-event append (Step 6) -- 10 on-disk `- req:` entries in
      requirement_status.v4.yaml, no rebase conflict on this file.
      Recounted to 14 by ORCH's own REQ-137 started-event append
      (WF02-REQ137-20260823, Step 00) -- `grep -c "^  - req:"
      docs/status/requirement_status.v4.yaml` = 14 on-disk entries. Three
      further appends (ISS-0289, REQ-131 done, plus this run's own
      predecessor bookkeeping) landed on main between REQ-121's done-event
      and this one without their own index-note increments; folded into this
      recount rather than corrected retroactively, per this note's own
      recount-every-time rule.
      Incremented to 15 by WF02-REQ137-20260823 (DOC-UPDATER)'s own REQ-137
      done-event append (Step 6) -- 15 on-disk `- req:` entries in
      requirement_status.v4.yaml, no rebase conflict on this file.
      Incremented to 17 by WF02-REQ136-20260823 (ORCH)'s own started+done
      event pair (2 appends, no rebase conflict on this file) -- 17
      on-disk `- req:` entries in requirement_status.v4.yaml.
      Incremented to 18 by ORCH's own ISS-0290 done-event append (docs-only
      handoff-timestamp fix, no rebase conflict on this file) -- 18 on-disk
      `- req:` entries in requirement_status.v4.yaml.
      Incremented to 19 by ORCH's own REQ-132 started-event append (Step 00)
      -- 19 on-disk `- req:` entries in requirement_status.v4.yaml.
      Renumbered during WF02-REQ132-20260823 (ELIXIR-DEV)'s Step Final
      rebase onto REQ-136/ISS-0290 (which landed on main after this
      branch was created); original note said 16 against this branch's
      stale base.
      Incremented to 20 by WF02-REQ132-20260823 (DOC-UPDATER)'s own REQ-132
      done-event append (Step 6), also composed in this same Step Final
      rebase -- 20 on-disk `- req:` entries in requirement_status.v4.yaml;
      original note said 17 against this branch's stale base.
      Incremented to 22 by WF02-REQ138-20260823 (ORCH)'s own started+done
      event pair, composed after REQ-132's done-event during this same
      Step Final rebase (both this run's own events sort chronologically
      after REQ-132's, no reordering needed) -- recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v4.yaml` = 22
      rather than carrying forward the pre-rebase count of 20, per this
      note's own recount-every-time rule.
      Incremented to 23 by ORCH's own ISS-0291 done-event append (test-only
      fix, no rebase conflict on this file) -- 23 on-disk `- req:` entries
      in requirement_status.v4.yaml.
      Incremented to 24 by ORCH's own ISS-0294 done-event append (docs-only,
      no code change, no rebase conflict on this file) -- 24 on-disk
      `- req:` entries in requirement_status.v4.yaml.
      Incremented to 25 by ORCH's own REQ-139 done-event append (docs-only,
      GIT_MERGE.md correction, no rebase conflict on this file) -- 25
      on-disk `- req:` entries in requirement_status.v4.yaml.
      Incremented to 26 by ORCH's own ISS-0295 done-event append (WF-03
      full pipeline, merged via PR #592, no rebase conflict on this file)
      -- 26 on-disk `- req:` entries in requirement_status.v4.yaml.
      Incremented to 27 by ORCH's own REQ-134 started-event append (Step 00)
      -- 27 on-disk `- req:` entries in requirement_status.v4.yaml, against a
      base declaring 26, no rebase conflict on this file at that point.
      Incremented to 28 by DOC-UPDATER's own REQ-134 done-event append
      (Step 6) -- 28 on-disk `- req:` entries in requirement_status.v4.yaml,
      no rebase conflict on this file at that point.
      This run's Step Final rebase then found main had independently
      advanced past both of the above via FRONTEND-DEV's own REQ-133
      blocked-event append (landed on main after this run's Step 00 commit).
      Composed in chronological `at:` order -- REQ-134 started (10:43:56Z),
      REQ-134 done (13:24:48Z), REQ-133 blocked (13:50:00Z) -- all three
      already sorted correctly with no reordering needed. Recounted directly
      from `grep -c "^  - req:" docs/status/requirement_status.v4.yaml` = 29,
      rather than carrying forward either side's independently-computed
      27/28, per this note's own recount-every-time rule.
      Incremented to 31 by ORCH's own REQ-140 started+done event pair (2
      appends), whose own Step Final rebase found main already advanced by
      the REQ-134+REQ-133 composition above (this run's base predated it,
      so its own pre-rebase count of 27 was stale). REQ-140 started
      (14:00:00Z) and done (15:35:00Z) both sort after REQ-133's blocked
      event (13:50:00Z), so both append cleanly with no reordering.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v4.yaml` on the composed post-rebase
      file = 31, per this note's own recount-every-time rule.
      Incremented to 36 by ORCH's own ISS-0330 done-event append (WF-03
      docs-only fix, no rebase conflict on this file) -- 36 on-disk `- req:`
      entries in requirement_status.v4.yaml. Also corrected volume 1's
      entries: 182 -> 185 in this same commit (A9 fix).
      Incremented to 37 by ORCH's own REQ-143 started-event append (Step 00)
      -- 37 on-disk `- req:` entries in requirement_status.v4.yaml, no rebase
      conflict on this file.
      Recounted to 51 by ORCH's own WF02-REQ077-20260826 fix, catching a
      missed increment on its own Step 00 started-event append (TEST-RUNNER's
      A9 failure) -- `grep -c "^  - req:" docs/status/requirement_status.v4.yaml`
      = 51 on-disk entries, folding in every append landed on main between
      REQ-143's started-event and this one without its own index-note
      increment.

  - volume: 5
    path: docs/status/requirement_status.v5.yaml
    status: closed
    opened: 2026-08-26
    closed: 2026-08-28
    entries: 47
    lines: 1204
    bytes_working_tree: 69312
    # SHA-256 of lines 1..1204 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1204
    frozen_prefix_sha256: "675f6be1c5474c22a70ca6c3aa67fa64c13439fc4cce45325ec964904aae3809"
    closure_note: >
      Closed under WF02-REQ170-20260828's Step 6 DOC-UPDATER append at 1204
      lines / 69312 bytes / 47 entries -- over its own declared roll_rule
      ceiling (max_lines: 1200), confirmed via `wc -l` per this volume's own
      "WHEN THIS VOLUME IS FULL" procedure. `git diff --numstat` on this file
      across this append showed insertions-only (0 deletions). Successor:
      docs/status/requirement_status.v6.yaml.
    note: >
      Incremented to 47 by WF02-REQ170-20260828 (DOC-UPDATER)'s own REQ-170
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 47 on-disk entries, no rebase
      conflict on this file. This append crossed the roll ceiling (1204
      lines), so this volume was closed and volume 6 opened in the same
      commit per this index's `roll_rule.on_exceed` procedure.

      Incremented to 46 by ORCH's REQ-170 started-event append (Step 00,
      WF02-REQ170-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 46 on-disk entries.

      Incremented to 42 by ORCH's REQ-168 started-event append (Step 00,
      WF02-REQ168-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 42 on-disk entries.

      Incremented to 40 by ORCH's REQ-167 started-event append (Step 00,
      WF02-REQ167-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 40 on-disk entries.

      Incremented to 38 by ORCH's own REQ-166 started-event append (Step 00,
      WF02-REQ166-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 38 on-disk entries.

      Incremented to 37 by WF02-REQ165-20260828 (DOC-UPDATER)'s own REQ-165
      done-event append (Step 6, RELEASE-VALIDATOR PASS commit f518c8a) --
      `grep -c "^  - req:" docs/status/requirement_status.v5.yaml` = 37
      on-disk entries, no rebase conflict on this file. First native-code
      build dependency (wasmex) and first WASM-half requirement with real
      code; unblocks REQ-166.

      Corrected to 36 by WF02-REQ123-20260828 Step 6 append verification --
      this run appended REQ-123 started/done and re-counted on-disk entries
      with `grep -c "^  - req:" docs/status/requirement_status.v5.yaml`.

      Incremented to 12 by conflict resolution at Step Final (WF02-REQ157-20260827
      rebase onto main) -- main already carried REQ-157's own started event
      (appended by a sibling/earlier session of this same run) plus
      WF02-REQ154-20260827's two done-event appends and its own composed
      count of 11; this run's own REQ-157 done-event append composes on top
      of that, `grep -c "^  - req:" docs/status/requirement_status.v5.yaml`
      = 12 on-disk entries. This run's own done-event note originally
      claimed REQ-157 had no started event anywhere in this volume -- that
      was written before this rebase surfaced main's already-landed started
      event; corrected in the done-event's own note rather than silently
      fixed, per this file's append-only, never-rewrite convention.

      Incremented to 11 by conflict resolution at Step Final (WF02-REQ154-20260827 rebase onto
      main) — main had REQ-157 started event appended by a sibling session; both entries composed.
      `grep -c "^  - req:" docs/status/requirement_status.v5.yaml` = 11 on-disk entries.

      Incremented to 10 by DOC-UPDATER done-event append (WF02-REQ154-20260827, Step 06) --
      `grep -c "^  - req:" docs/status/requirement_status.v5.yaml` = 10 on-disk entries.

      Incremented to 9 by TEST-RUNNER Step 04 done-event append (WF02-REQ154-20260827) --
      `grep -c "^  - req:" docs/status/requirement_status.v5.yaml` = 9 on-disk entries.

      Incremented to 8 by ORCH's own REQ-154 started-event append (Step 00,
      WF02-REQ154-20260827) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 8 on-disk entries.

      Incremented to 7 by WF02-REQ153-20260827 (DOC-UPDATER)'s own REQ-153
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 7 on-disk entries, no rebase
      conflict on this file.

      Incremented to 3 by WF02-REQ150-20260826's own Step Final rebase, which
      appended the REQ-150-done event (DOC-UPDATER) alongside the pre-existing
      REQ-150-started event already on this volume -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 3 on-disk entries.
      Incremented to 4 by ORCH's own REQ-152 started-event append (Step 00,
      WF02-REQ152-20260826) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 4 on-disk entries.
      Incremented to 5 by WF02-REQ152-20260826 (DOC-UPDATER)'s own REQ-152
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 5 on-disk entries, no rebase
      conflict on this file.

      ISS-0346/GH#665: entries: corrected from 0 to 2 (A9 drift -- the
      declared count was never bumped when the REQ-133-done and REQ-150-
      started entries were appended). See this volume's own history for
      those two entries.

      Opened by WF02-REQ151-20260826's own Step Final roll, successor to
      volume 4. Created with volume 4's header text verbatim (volume number,
      opening date and predecessor path updated) and an empty `history:`
      list, per this index's `roll_rule.on_exceed` procedure -- volume 4's
      full 56 entries were kept in volume 4 rather than split across the
      roll, matching every prior roll's own pattern (volume 4's own footer:
      "no entry above this footer was moved, edited or deleted"). `entries:`
      will be incremented per HOW-TO-APPEND step 6 as real appends land.

  - volume: 7
    path: docs/status/requirement_status.v7.yaml
    status: closed
    opened: 2026-08-30
    closed: 2026-09-01
    entries: 42
    lines: 1220
    bytes_working_tree: 71927
    # SHA-256 of lines 1..1220 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1220
    frozen_prefix_sha256: "f7da0f6cf41fad2aeceb69dbd457e32c73dca22b5d94e9193a1e0b9a4909435d"
    closure_note: >
      Closed under WF02-REQ208-20260901's Step 6 DOC-UPDATER append at 1220
      lines / 71927 bytes / 42 entries -- over its own declared roll_rule
      ceiling (max_lines: 1200), confirmed via `wc -l` per this volume's own
      "WHEN THIS VOLUME IS FULL" procedure. `git diff --numstat` on this file
      across this append showed insertions-only (0 deletions). Successor:
      docs/status/requirement_status.v8.yaml.
    note: >
      Incremented to 42 by WF02-REQ208-20260901 (DOC-UPDATER)'s own REQ-208
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 42 on-disk entries, no rebase
      conflict on this file at the time it was written. This append crossed
      the roll ceiling (1220 lines), so this volume was closed and volume 8
      opened in the same commit per this index's `roll_rule.on_exceed`
      procedure.

      Incremented to 41 after WF02-REQ208-20260901's own started-event
      append (Step 00). `grep -c "^  - req:" docs/status/requirement_status.v7.yaml`
      = 41 on-disk entries; `wc -l` = 1165 lines, under the 1200-line roll
      ceiling. This supersedes the 40-count note below.
      Incremented to 40 by WF02-REQ207-20260901 (ORCH)'s own Step Final
      SCOPE-CHANGE done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 40 on-disk entries, no
      rebase conflict on this file. `wc`: 1145 lines / well under the
      1200-line/120000-byte roll ceiling, so no roll triggered. Records
      PR #771's merge, the CI lint-violation and wasm-timing-flake fixes,
      queue task 384 release, and ISS-0392/ISS-0393 filing.

      Incremented to 39 by WF02-REQ207-20260901 (DOC-UPDATER)'s own REQ-207
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 39 on-disk entries, no
      rebase conflict on this file at the time it was written (`git diff
      --numstat` showed 52 insertions / 0 deletions on this file). `wc`:
      1107 lines / 65975 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. This closes S7's Vortex scenario
      batch: REQ-205 (harness), REQ-206 (SwiftRoute) and REQ-207 (Vortex)
      are all done.

      Corrected to 38 during ORCH's Step-Final rebase of
      feature/WF02-REQ206-20260901 onto main: DOC-UPDATER's own commit
      (fe0b331a) had counted 37 on a branch that did not yet include
      main's own intervening REQ-207-started append (commit fd3cbfa6,
      "WF02-REQ207-20260901 Step 00"), landed on main while REQ-206's
      branch was in flight. The rebase produced a genuine append-order
      conflict in requirement_status.v7.yaml (REQ-207-started vs.
      REQ-206-done, both legitimate, independent appends) -- resolved by
      composing them chronologically (REQ-207 started 04:12:36Z before
      REQ-206 done 05:59:58Z), losing neither entry. `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 38 on-disk entries after
      the merge, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered. This supersedes the 37-count note below, which was
      correct only for DOC-UPDATER's own pre-rebase branch state. Also
      fixed, in that same DOC-UPDATER commit (preserved through the
      rebase), a genuine vocabulary defect
      A5 caught in this CURRENT (not yet closed) volume: the intervening
      ISS-0388 done-event append had recorded `req: ISS-0388`, a value with
      zero precedent under this volume's own header (only `REQ-NNN` and
      `SCOPE-CHANGE` are legal `req:` values; an issue id belongs in
      `note:` under `req: SCOPE-CHANGE`). Per this volume's own header
      ("A vocabulary defect in the CURRENT volume is FIXED in the working
      tree before it is committed -- it can never be declared away here")
      and test/docs/requirement_status_invariants_test.exs's A5 assertion,
      this is corrected in place -- not the append-only rewrite the project
      forbids, since only a CLOSED volume's entries are frozen against
      correction. Changed `req: ISS-0388` to `req: SCOPE-CHANGE` on that
      entry (line ~971); its `note:` already names ISS-0388 explicitly, so
      no information was lost. `mix test
      test/docs/requirement_status_invariants_test.exs` confirmed 20/20
      passing after the fix (was 18/20 before, both A5 and A9 failing; A9
      alone was already resolved by this run's own entries: count
      increment above).

      Recounted to 35 after WF02-REQ205-20260831's own DOC-UPDATER done event
      appended. `grep -c "^  - req:" docs/status/requirement_status.v7.yaml`
      = 35 on-disk entries (`wc`: 969 lines / 56578 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered). This
      supersedes the 34-count note below.

      Recounted to 34 after rebasing WF03-ISS0380-20260831 onto main a
      second time, which by then included WF02-REQ199-20260831's own
      DOC-UPDATER done event (merged as PR #761) landing alongside this
      run's own SCOPE-CHANGE append. `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 34 on-disk entries (`wc`:
      906 lines / 52205 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered). This supersedes the 33-count note
      below, which was correct for this branch's state before REQ-199's
      concurrent append landed on main.

      Recounted to 33 after WF03-ISS0380-20260831's own SCOPE-CHANGE done
      event (ORCH) appended. `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 33 on-disk entries (`wc`:
      892 lines / 51597 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered). This supersedes the 32-count note
      below.

      Recounted to 32 after rebasing WF03-ISS0381-20260831 onto main, which
      by then included ORCH's retroactive-evidence-comment append (31) plus
      this run's own SCOPE-CHANGE done event (ISSUE-FIXER, WF03-ISS0381-20260831).
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 32
      on-disk entries (`wc`: 857 lines / 49488 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered). This
      supersedes both the 31-count and 29-count notes below, which were
      each correct for their own branch's state before the other's append
      landed on main.

      Recounted to 31 on 2026-08-31 after ORCH posted retroactive evidence
      comments on 4 closed issues to clear mix letflow.audit_issue_closures'
      new-violation set (0 new violations after; 30 grandfathered unaffected).
      `wc`: 826 lines, still well under the 1200-line/120000-byte roll ceiling.
      This supersedes the 30-count note below.

      Recounted to 30 on 2026-08-31 after ORCH's state-reconciliation pass
      appended two SCOPE-CHANGE entries (the orphaned-issue registration and
      S7's WF-01 expansion into REQ-205..REQ-210).
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 30
      on-disk entries (`wc`: 808 lines, still well under the 1200-line/
      120000-byte roll ceiling, so no roll triggered). This supersedes the
      28-count note below, which was correct before those two appends.

      Recounted to 29 after WF03-ISS0381-20260831's own done event
      (ISSUE-FIXER) appended. `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 29 on-disk entries (`wc`:
      793 lines / 45630 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered). This supersedes the prior 28-count
      note below, which was correct after a second rebase of
      WF02-REQ203-20260831 onto main, which by then also included the
      merged WF02-REQ204-20260831 branch (REQ-204 started/done) and
      WF02-REQ199-20260831's started event. That note in turn superseded
      a prior 25-count note, which was correct for the branch's state
      after only the first rebase (before REQ-204's concurrent append
      landed on main).

      Recounted to 28 after a second rebase of WF02-REQ203-20260831 onto
      main, which by then also included the merged WF02-REQ204-20260831
      branch (REQ-204 started/done) and WF02-REQ199-20260831's started
      event. `grep -c "^  - req:" docs/status/requirement_status.v7.yaml`
      = 28 on-disk entries (`wc`: 762 lines / 43729 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered). This
      supersedes the prior 25-count note below, which was correct for
      the branch's state after only the first rebase (before REQ-204's
      concurrent append landed on main).

      Recounted to 25 after rebasing WF02-REQ203-20260831 onto main, which
      by then included the merged WF02-REQ201-20260830 concurrent branch.
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 25
      on-disk entries (`wc`: 725 lines / 42137 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered). This
      supersedes WF02-REQ203-20260831's own pre-rebase count of 23 --
      that count was correct for the branch's state at the time it was
      written, before REQ-201's concurrent append landed on main.

      Incremented to 23 by WF02-REQ203-20260831 (DOC-UPDATER)'s own REQ-203
      done-event append (Step 6) -- branch-local count before rebase, no
      conflict on this file at the time it was written (checked for a
      concurrent REQ-204 session's own append; none had landed on this
      volume yet).

      Recounted to 24 after merge of WF02-REQ201-20260830 and
      WF02-REQ202-20260830 concurrent branches (REQ-201 started/done from
      one session; REQ-195 started/done + REQ-196 started/done + REQ-202
      done from the other). `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 24 on-disk entries.

      Incremented to 19 by ORCH (DOC-UPDATER rate-limited) REQ-201
      done-event append (WF02-REQ201-20260830) -- branch-local count
      before merge.

      Incremented to 18 by WF02-REQ201-20260830 ORCH started-event append
      (Step 00) -- branch-local count before merge.

      Incremented to 22 by WF02-REQ202-20260830 (DOC-UPDATER)'s own REQ-202
      done-event append (Step 6) -- main count before merge.

      Incremented to 21 by WF02-REQ196-20260830 (DOC-UPDATER)'s own REQ-196
      done-event append (Step 6) -- main count before merge.

      Incremented to 20 by ORCH's REQ-196 started-event append (Step 00,
      WF02-REQ196-20260830) -- main count before merge.

      Incremented to 19 by WF02-REQ195-20260830 (DOC-UPDATER)'s own REQ-195
      done-event append (Step 6) -- main count before merge.

      Incremented to 18 by ORCH's REQ-195 started-event append (Step 00,
      WF02-REQ195-20260830) -- main count before merge.

      Incremented to 17 by WF02-REQ194-20260830 (DOC-UPDATER)'s own REQ-194
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 17 on-disk entries, no
      rebase conflict on this file at the time it was written. This
      resolves all three of REQ-078's deliberately-recorded-open
      divergences (auth, scope, format) and adds a new direct :telemetry
      dependency, REVIEWER-signed-off.

      Incremented to 17 by WF02-REQ194-20260830 (DOC-UPDATER)'s own REQ-194
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 17 on-disk entries, no
      rebase conflict on this file at the time it was written. This
      resolves all three of REQ-078's deliberately-recorded-open
      divergences (auth, scope, format) and adds a new direct :telemetry
      dependency, REVIEWER-signed-off.

      Incremented to 16 by ORCH's REQ-194 started-event append (Step 00,
      WF02-REQ194-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 16 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Recounted to 15 by ORCH's Step Final rebase composition for
      WF02-REQ200-20260830, composing this branch's own REQ-200 done-event
      append (Step 6) against origin/main's concurrently-advanced state --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 15
      on-disk entries after composition, recounted directly from the
      composed file per this note's own recount-every-time rule. This
      closes a real broken-contract gap left by REQ-080: the already-
      shipped SPA's TimelineFeedItem.tsx was reading actor_display_name,
      description, timestamp and sequence_num, none of which the REQ-080
      route ever emitted.

      Recounted to 14 by ORCH's Step Final rebase composition for
      WF02-REQ200-20260830, composing this branch's own REQ-200
      started-event append against origin/main's concurrently-appended
      REQ-193 done-event -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 14 on-disk entries after
      composition, recounted directly from the composed file per this
      note's own recount-every-time rule.

      Recounted to 13 by ORCH's Step Final rebase composition for
      WF02-REQ193-20260830, composing this branch's own REQ-193 done-event
      append against origin/main's concurrently-appended REQ-184 done-event --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 13
      on-disk entries after composition, recounted directly from the composed
      file per this note's own recount-every-time rule.

      Incremented to 14 by WF02-REQ200-20260830 (DOC-UPDATER)'s own REQ-200
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 14 on-disk entries, no
      rebase conflict on this file at the time it was written. This closes
      a real broken-contract gap left by REQ-080: the already-shipped
      SPA's TimelineFeedItem.tsx was reading actor_display_name,
      description, timestamp and sequence_num, none of which the REQ-080
      route ever emitted.

      Incremented to 13 by ORCH's REQ-200 started-event append (Step 00,
      WF02-REQ200-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 13 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Recounted to 12 by ORCH's Step Final rebase composition for
      WF02-REQ184-20260830, composing this branch's own REQ-184 done-event
      append (Step 6) against origin/main's concurrently-advanced state --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 12
      on-disk entries after composition, recounted directly from the
      composed file per this note's own recount-every-time rule. This
      closes S6's webhooks subsystem in full: REQ-181 (core), REQ-182
      (subscription routes), REQ-183 (delivery dispatch) and REQ-184
      (deliveries route) are all done.

      Recounted to 11 by ORCH's Step Final rebase composition for
      WF02-REQ184-20260830, composing this branch's own REQ-184
      started-event append against origin/main's concurrently-appended
      REQ-193 started-event -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 11 on-disk entries after
      composition, recounted directly from the composed file per this
      note's own recount-every-time rule.

      Incremented to 11 by WF02-REQ184-20260830 (DOC-UPDATER)'s own REQ-184
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 11 on-disk entries, no
      rebase conflict on this file at the time it was written. This
      closes S6's webhooks subsystem in full: REQ-181 (core), REQ-182
      (subscription routes), REQ-183 (delivery dispatch) and REQ-184
      (deliveries route) are all done.

      Incremented to 10 by ORCH's REQ-184 started-event append (Step 00,
      WF02-REQ184-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 10 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Recounted to 9 by ORCH's Step Final rebase composition for
      WF02-REQ198-20260830, composing this branch's own REQ-198 done-event
      append (Step 6) against origin/main's concurrently-advanced state --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 9
      on-disk entries after composition, recounted directly from the
      composed file per this note's own recount-every-time rule. This
      closes S6's expr subsystem in full (REQ-197 + REQ-198 both done).

      Recounted to 8 by ORCH's Step Final rebase composition for
      WF02-REQ198-20260830, composing this branch's own REQ-198
      started-event append against origin/main's concurrently-appended
      REQ-183 done-event (which itself required a recount to 7 during
      WF02-REQ183-20260830's own second Step Final rebase) --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 8
      on-disk entries after composition, recounted directly from the
      composed file per this note's own recount-every-time rule.

      Recounted to 7 by WF02-REQ183-20260830 (ELIXIR-DEV)'s own second Step
      Final rebase attempt (main advanced again, past WF02-REQ197-20260830's
      own started+done pair, before the first attempt's PR could merge).
      requirement_status.v7.yaml itself merged with no conflict on the
      REQ-183 done-event's own content (it interleaved cleanly between
      REQ-197's started (08:19:08Z) and done (10:08:27Z) events by `at:`
      order); `grep -c "^  - req:" docs/status/requirement_status.v7.yaml`
      = 7 on-disk entries after composition. Only this index file's
      declared count and narrative needed manual composition. Recounted
      directly from the composed file rather than carrying forward either
      side's independently-computed 5/6, per this note's own
      recount-every-time rule.

      Incremented to 8 by WF02-REQ198-20260830 (DOC-UPDATER)'s own REQ-198
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 8 on-disk entries, no
      rebase conflict on this file at the time it was written. This closes
      S6's expr subsystem in full (REQ-197 + REQ-198 both done).

      Incremented to 7 by ORCH's REQ-198 started-event append (Step 00,
      WF02-REQ198-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 7 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 6 by WF02-REQ197-20260830 (DOC-UPDATER)'s own REQ-197
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 6 on-disk entries, no
      rebase conflict on this file.

      Incremented to 5 by ORCH's REQ-197 started-event append (Step 00,
      WF02-REQ197-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 5 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Recounted to 5 by WF02-REQ183-20260830 (ELIXIR-DEV)'s own Step Final
      rebase, composing this branch's own REQ-183 done-event append (Step 6,
      09:28:43Z) against origin/main's concurrently-landed
      WF03-ISS0378-20260830 SCOPE-CHANGE started/done pair (06:51:14Z,
      07:19:11Z) and REQ-183's own resumed-started event (07:24:20Z) --
      `grep -c "^  - req:" docs/status/requirement_status.v7.yaml` = 5
      on-disk entries after composition. requirement_status.v7.yaml itself
      merged with no conflict (this run's done-event landed cleanly at the
      file's tail, already sorting last by `at:`); only this index file's
      declared count and narrative needed manual composition. Recounted
      directly from the composed file rather than carrying forward either
      side's independently-computed 3/4, per this note's own
      recount-every-time rule.

      Incremented to 4 by ORCH's Step Final rebase composition for
      WF03-ISS0378-20260830, composing this branch's own SCOPE-CHANGE
      done-event append (Step 5) against origin/main's concurrently-
      appended REQ-183 resumed-started event -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 4 on-disk entries after
      composition, entries chronologically ordered by `at:` (ISS-0378
      done at 07:19:11Z before REQ-183's resumed-started at 07:24:20Z),
      neither dropped nor reordered.

      Incremented to 3 by ORCH's Step Final rebase composition for
      WF03-ISS0378-20260830, composing this branch's own SCOPE-CHANGE
      started-event append against origin/main's concurrently-appended
      REQ-183 resumed-started event -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 3 on-disk entries after
      composition, entries chronologically ordered by `at:` (ISS-0378
      06:51:14Z before REQ-183's 07:24:20Z), neither dropped nor reordered.

      Incremented to 3 by WF02-REQ183-20260830 (DOC-UPDATER)'s own REQ-183
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 3 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 2 by ORCH's REQ-183 resumed started-event append
      (WF02-REQ183-20260830 resume) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 2 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 3 by ISSUE-FIXER's SCOPE-CHANGE done-event append
      (Step 5, WF03-ISS0378-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 3 on-disk entries, no
      rebase conflict on this file.

      Incremented to 2 by ORCH's SCOPE-CHANGE started-event append (Step 00,
      WF03-ISS0378-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v7.yaml` = 2 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 1 by ORCH's Step Final rebase composition for
      WF02-REQ192-20260830, appending the REQ-192 done-event (DOC-UPDATER,
      Step 6) here instead of volume 6 -- volume 6 was concurrently closed/
      frozen by the sibling WF02-REQ190-20260830 Step Final before this
      branch's rebase landed, so the done-event that DOC-UPDATER originally
      wrote against volume 6 was moved to volume 7 during conflict
      resolution rather than appended to the frozen file. `grep -c
      "^  - req:" docs/status/requirement_status.v7.yaml` = 1 on-disk entry.

      Opened by WF02-REQ190-20260830's own Step Final rebase composition
      (ELIXIR-DEV), successor to volume 6. Created with volume 6's header
      text verbatim (volume number, opening date and predecessor path
      updated) and an empty `history:` list, per this index's
      `roll_rule.on_exceed` procedure. `entries:` will be incremented per
      HOW-TO-APPEND step 6 as real appends land.

  - volume: 6
    path: docs/status/requirement_status.v6.yaml
    status: closed
    opened: 2026-08-28
    closed: 2026-08-30
    entries: 42
    lines: 1202
    bytes_working_tree: 73118
    # SHA-256 of lines 1..1202 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1202
    frozen_prefix_sha256: "4e354ab27116f35fc6c824228b14e5773158923f410706b952e94b2bfcf64b96"
    note: >
      Closed by WF02-REQ190-20260830's own Step Final rebase composition
      (ELIXIR-DEV) -- composing this branch's own REQ-190 done-event append
      against origin/main's five concurrently-appended events (REQ-190
      started, REQ-191 done, REQ-183 started, REQ-183 blocked, REQ-192
      started) brought the file to 42 on-disk entries (`grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 42) across 1202 lines /
      73118 bytes working-tree (CRLF) -- over the declared 1200-line roll
      ceiling (though under the 120000-byte ceiling). Rolled to volume 7
      per the roll_rule.on_exceed procedure; all 42 entries kept in volume
      6 rather than split, matching every prior roll's own pattern.

      Incremented to 41 by ORCH's REQ-192 started-event append (Step 00,
      WF02-REQ192-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 41 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 40 by ORCH's REQ-183 blocked-event append (WF02-REQ183-20260830
      -- REQ-190 dependency not yet on main) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 40 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 39 by ORCH's REQ-183 started-event append (Step 00,
      WF02-REQ183-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 39 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 38 by WF02-REQ191-20260830 (DOC-UPDATER)'s own REQ-191
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 38 on-disk entries, no
      rebase conflict on this file at the time it was written. 1116 lines /
      67176 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 37 by ORCH's REQ-190 started-event append (Step 00,
      WF02-REQ190-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 37 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 36 by ORCH's REQ-183 blocked-event append
      (WF02-REQ183-20260830 Step 1b FAIL / resequencing) -- `grep -c
      "^  - req:" docs/status/requirement_status.v6.yaml` = 36 on-disk
      entries, no rebase conflict on this file at the time it was
      written.

      Incremented to 35 by ORCH's REQ-183 started-event append (Step 00,
      WF02-REQ183-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 35 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 34 by ORCH's REQ-191 started-event append (Step 00,
      WF02-REQ191-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 34 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 33 by ORCH appending ISS-0377's own missing done-event
      (WF03-ISS0377-20260830 Step 5/ISSUE-FIXER did not append one) -- `grep -c
      "^  - req:" docs/status/requirement_status.v6.yaml` = 33 on-disk
      entries, no rebase conflict at the time it was written.

      Incremented to 32 by ORCH's ISS-0377 started-event append (Step 00,
      WF03-ISS0377-20260830) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 32 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 31 by WF02-REQ188-20260829 (DOC-UPDATER)'s own REQ-188
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 31 on-disk entries, no
      rebase conflict on this file (rebased cleanly onto origin/main, which
      had independently advanced with REQ-189's full started+done pair in
      the interim). 960 lines / 57953 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered. This closes
      S6's scheduler half in full (REQ-185/186/187/188); the secrets half
      (REQ-189 onward) is separate and remains open.

      Incremented to 30 by WF02-REQ189-20260830 (DOC-UPDATER)'s own REQ-189
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 30 on-disk entries, no
      rebase conflict on this file at the time it was written. 913 lines /
      55812 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 29 by ORCH's REQ-189 started-event append (Step 00,
      dispatch routed while resolving GH#701/REQ-183's dependency chain)
      -- `grep -c "^  - req:" docs/status/requirement_status.v6.yaml` = 29
      on-disk entries, no rebase conflict on this file at the time it was
      written.

      Incremented to 28 by ORCH's REQ-188 started-event append (Step 00,
      WF02-REQ188-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 28 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 27 by WF02-REQ187-20260829 (DOC-UPDATER)'s own REQ-187
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 27 on-disk entries, no
      rebase conflict on this file at the time it was written. 832 lines /
      49993 bytes, well under the 1200-line/120000-byte roll ceiling, so
      no roll triggered.

      Incremented to 26 by ORCH's REQ-187 started-event append (Step 00,
      WF02-REQ187-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 26 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 25 by WF02-REQ186-20260829 (DOC-UPDATER)'s own REQ-186
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 25 on-disk entries, no
      rebase conflict on this file at the time it was written. 783 lines /
      47038 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 24 by ORCH's REQ-186 started-event append (Step 00,
      WF02-REQ186-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 24 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 23 by WF02-REQ185-20260829 (DOC-UPDATER)'s own REQ-185
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 23 on-disk entries, no
      rebase conflict on this file at the time it was written. 742 lines /
      44674 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 22 by ORCH's REQ-185 started-event append (Step 00,
      WF02-REQ185-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 22 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 21 by WF02-REQ182-20260829 (DOC-UPDATER)'s own REQ-182
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 21 on-disk entries, no
      rebase conflict on this file at the time it was written. 681 lines /
      40909 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 20 by ORCH's REQ-182 started-event append (Step 00,
      WF02-REQ182-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 20 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 19 by WF02-REQ181-20260829 (DOC-UPDATER)'s own REQ-181
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 19 on-disk entries, no
      rebase conflict on this file at the time it was written. 652 lines /
      39369 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 18 by ORCH's REQ-181 started-event append (Step 00,
      WF02-REQ181-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 18 on-disk entries, no
      rebase conflict on this file at the time it was written.

      Incremented to 17 by WF02-REQ178-20260829 (DOC-UPDATER)'s own REQ-178
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 17 on-disk entries, no
      rebase conflict on this file at the time it was written. 610 lines /
      36844 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 16 by ORCH's REQ-178 started-event append (Step 00,
      WF02-REQ178-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 16 on-disk entries, same
      commit as the append.

      Incremented to 15 by ORCH's Step Final rebase of
      WF02-REQ177-20260829 onto main, which composed this branch's own
      REQ-177 done-event append (bringing this branch's own count to 13)
      with two concurrently-landed SCOPE-CHANGE entries from a sibling
      session's WF01-S6-EXPANSION-20260829 run (the second S6 batch
      registration, and its own probe-misuse self-correction) that had
      landed on main in the interim -- append-only composition, both
      sides kept, neither rewritten, per this project's own established
      rebase-conflict resolution pattern. `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 15 on-disk entries after
      composition. Well under the 1200-line/120000-byte roll ceiling
      (553 lines), so no roll triggered.

      Incremented to 13 by WF02-REQ177-20260829 (DOC-UPDATER)'s own REQ-177
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 13 on-disk entries, no
      rebase conflict on this file at the time it was written. Well
      under the 1200-line/120000-byte roll ceiling (464 lines / 27562
      bytes), so no roll triggered.

      Incremented to 12 by ORCH's REQ-177 started-event append (Step 00,
      WF02-REQ177-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 12 on-disk entries, same
      commit as the append.

      Incremented to 11 by WF02-REQ176-20260829 (DOC-UPDATER)'s own REQ-176
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 11 on-disk entries, no
      rebase conflict on this file. Well under the 1200-line/120000-byte
      roll ceiling (409 lines / 24173 bytes), so no roll triggered.

      Incremented to 10 by ORCH's REQ-176 started-event append (Step 00,
      WF02-REQ176-20260829) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 10 on-disk entries, same
      commit as the append.

      Incremented to 9 by ORCH's SCOPE-CHANGE append reconciling stale
      letflow-queue task 327 (REQ-175, already cancelled on disk since
      REQ-164's own Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 9 on-disk entries, same
      commit as the append.

      Incremented to 8 by WF02-REQ174-20260828 (DOC-UPDATER)'s own REQ-174
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 8 on-disk entries, no rebase
      conflict on this file. Well under the 1200-line/120000-byte roll
      ceiling (341 lines / 20203 bytes), so no roll triggered.

      Incremented to 7 by ORCH's REQ-174 started-event append (Step 00,
      WF02-REQ174-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 7 on-disk entries, in the
      SAME commit as the append this time (see REQ-173's own note below
      for the prior miss this avoids repeating).

      Incremented to 6 by WF02-REQ173-20260828 (DOC-UPDATER)'s own REQ-173
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 6 on-disk entries, no rebase
      conflict on this file. Well under the 1200-line/120000-byte roll
      ceiling (291 lines / 17123 bytes), so no roll triggered.

      Incremented to 5 by ORCH's REQ-173 started-event append (Step 00,
      WF02-REQ173-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 5 on-disk entries. This
      increment was missed in the same commit as the append (5d32726) and
      is corrected here once found, during REQ-173's own Step 2a, rather
      than silently going unnoticed further -- ISS-0199's own concern
      about a declared count nobody updates.

      Incremented to 4 by WF02-REQ172-20260828 (DOC-UPDATER)'s own REQ-172
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 4 on-disk entries, no rebase
      conflict on this file. Well under the 1200-line/120000-byte roll
      ceiling (242 lines / 14191 bytes), so no roll triggered.

      Incremented to 3 by ORCH's REQ-172 started-event append (Step 00,
      WF02-REQ172-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 3 on-disk entries.

      Incremented to 2 by WF02-REQ171-20260828 (DOC-UPDATER)'s own REQ-171
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 2 on-disk entries, no rebase
      conflict on this file.

      Incremented to 1 by ORCH's REQ-171 started-event append (Step 00,
      WF02-REQ171-20260828) -- `grep -c "^  - req:"
      docs/status/requirement_status.v6.yaml` = 1 on-disk entry, the first
      in this newly-opened volume.

  - volume: 8
    path: docs/status/requirement_status.v8.yaml
    status: closed
    opened: 2026-09-01
    closed: 2026-09-04
    entries: 30
    lines: 1245
    bytes_working_tree: 75455
    # SHA-256 of lines 1..1213 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1213
    frozen_prefix_sha256: "c0595ffaa12ab9db8b19c216ef1dd8f4a7af16a2af3b4646fe061e2f613420ce"
    closure_note: >
      Closed at feature/WF02-REQ219-20260903's Step Final rebase, which
      composed this branch's own REQ-219 done-event append against a
      concurrent sibling's two SCOPE-CHANGE entries for decision 0017
      (2026-09-03T19:11:25Z and T19:24:26Z) -- bringing this file to 1213
      frozen-prefix lines / 30 entries, over its own declared roll_rule
      ceiling (max_lines: 1200), confirmed via `wc -l` per this volume's
      own "WHEN THIS VOLUME IS FULL" procedure. Both sides independently
      computed 28 from a shared base of 26 (WF02-REQ219-20260903 appended
      one REQ-219-started + one REQ-219-done event; the sibling appended
      two SCOPE-CHANGE events) -- each count was correct pre-merge but
      referred to a different tree; recounted directly on disk (30) rather
      than carried forward from either side's independently-computed 28.
      No content lost, no edit to either side's entries. Successor:
      docs/status/requirement_status.v9.yaml.
    note: >
      Incremented to 28 by WF02-REQ219-20260903 (DOC-UPDATER)'s own
      REQ-219 done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 28 on-disk entries, no
      rebase conflict on this file at the time it was written. `wc`:
      1138 lines / 68500 bytes, well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered.

      Composed to 26 at feature/WF02-REQ215-20260903's Step Final rebase
      (second conflict, this branch's own REQ-215 done-event append,
      14:14:17Z). That event sorts chronologically AFTER the entire
      REQ-217/REQ-218/ISS-0426 chain below (latest at 13:17:18Z), so it
      was appended at the tail of the volume file rather than interleaved
      -- `grep -c "^  - req:" docs/status/requirement_status.v8.yaml` = 26
      on-disk entries after composition, no content lost, recounted
      directly rather than carrying forward either side's independently-
      computed count (24 declared here pre-rebase, 21 computed by this
      branch's own pre-rebase DOC-UPDATER commit against its stale base
      of 20). `wc`: 1075 lines / 65895 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Composed to 25 at this same branch's Step Final rebase (first
      conflict, this branch's own REQ-215 started-event, 05:01:17Z),
      which sorted chronologically BEFORE the REQ-217/REQ-218/ISS-0426
      chain below (earliest at 06:47:02Z) and was interleaved into that
      position rather than appended after -- 25 on-disk entries at that
      point in the rebase, since superseded by the done-event composition
      above.

      Incremented to 24 by DOC-UPDATER's WF02-REQ218-20260903 done-event
      append (REQ-218, closing ISS-0431 entirely).
      Incremented to 22 by ORCH's Step-Final rebase of
      WF02-REQ217-20260903 (second rebase round, absorbing REQ-217's own
      DOC-UPDATER done-event append from `entries: 18` interleaved with
      REQ-214/ISS-0426's own concurrently-landed entries) -- `grep -c
      "^  - req:" docs/status/requirement_status.v8.yaml` = 22 on-disk
      entries, no content lost.

      Incremented to 21 by ORCH's Step-Final rebase of
      WF02-REQ217-20260903: this branch's own entries (REQ-217 started at
      02:45, plus REQ-217 done appended by DOC-UPDATER later in this same
      branch) interleaved with REQ-214/ISS-0426's own concurrently-landed
      entries during the rebase -- reconstructed via git show + set-diff,
      no content lost, confirmed 21 on-disk entries via
      `grep -c "^  - req:" docs/status/requirement_status.v8.yaml`.

      Incremented to 20 by WF03-ISS0426-20260902 (ORCH)'s own ISS-0426
      done-event append at Step Final -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 20 on-disk entries. `wc`:
      901 lines, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 19 by ORCH's own REQ-214 done-event append, superseding
      the blocked/revised entries below for the same requirement now that
      PR #847 is genuinely merged (commit 81e2dea8) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 19 on-disk entries, no
      rebase conflict on this file. `wc`: 849 lines / well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 18 by ORCH's Step-Final rebase of
      feature/WF02-REQ214-20260902 onto main, which by then included
      REQ-216's own 2 entries (started + done) landing concurrently with
      this run's own REQ-214 blocked-event (Step 6, DOC-UPDATER) and its
      superseding revised-event (Step-Final rebase, ORCH) -- `grep -c
      "^  - req:" docs/status/requirement_status.v8.yaml` = 18 on-disk
      entries after the merge, no entry lost. `wc`: 822 lines / 50550
      bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 18 by WF02-REQ217-20260903 (DOC-UPDATER)'s own REQ-217
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 18 on-disk entries, no
      rebase conflict on this file at the time it was written. `wc`: 796
      lines / 48188 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 16 by WF02-REQ216-20260902 (DOC-UPDATER)'s own REQ-216
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 16 on-disk entries, no
      rebase conflict on this file at the time it was written. `wc`: 736
      lines / 44511 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 15 by ORCH's WF02-REQ216-20260902 started-event append
      -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 15 on-disk entries, no roll
      triggered (705 lines / well under the 1200-line/120000-byte ceiling).
      This increment was itself missed in the entry's own commit (Step 00,
      commit 3e29072f) and caught by CI's A9 invariant test, corrected
      here in a follow-up commit on the same branch before merge. Prior
      history: incremented to 14 by WF03-ISS0411-20260902's done-event
      append (PR #823), same miss-then-fix pattern.

      Incremented to 13 by ORCH's WF03-ISS0419-20260902 done-event append
      (SCOPE-CHANGE) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 13 on-disk entries, no roll
      triggered (655 lines / well under the 1200-line/120000-byte ceiling).

      Incremented to 12 by ORCH's WF03-ISS0404-20260902 done-event append
      (SCOPE-CHANGE) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 12 on-disk entries, no roll
      triggered (606 lines / well under the 1200-line/120000-byte ceiling).
      Also filed ISS-0412 (queue task 412, GH#808) and ISS-0413 (queue task
      413, GH#809) as incidental-finding follow-ups, not themselves
      run-history events.

      Incremented to 11 by ORCH's WF03-ISS0408-20260902 done-event append
      (SCOPE-CHANGE) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 11 on-disk entries, no roll
      triggered (564 lines / 34449 bytes, well under ceiling). Also filed
      ISS-0409 (queue task 409, GH#805) as a follow-up, not itself a
      run-history event.

      Incremented to 10 by WF02-REQ210-20260902 (DOC-UPDATER)'s own REQ-210
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 10 on-disk entries, no
      rebase conflict on this file at the time it was written. `wc`: 515
      lines / 31322 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Closes S7's initial 6-requirement
      batch (REQ-205..REQ-210 all done).

      Incremented to 9 by ORCH's Step-Final rebase of
      feature/WF02-REQ213-20260902 onto main, which by then included a
      concurrent sibling session's WF02-REQ209-20260902 run (2 entries:
      started + done) landing alongside this run's own REQ-213 done-event
      append. Composed chronologically: REQ-209 started (02:33:20Z),
      REQ-213 done (03:37:23Z), REQ-209 done (03:45:16Z) -- REQ-213's
      done-event genuinely landed between REQ-209's own started and done
      events by wall-clock time. `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 9 on-disk entries after
      the merge, no entry lost. `wc`: 462 lines / 28021 bytes, well under
      the 1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 8 by WF02-REQ209-20260902 (DOC-UPDATER)'s own REQ-209
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 8 on-disk entries, no
      rebase conflict on this file. `wc`: 414 lines / 24895 bytes, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 7 by ORCH's own WF02-REQ209-20260902 started-event
      append (Step 00) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 7 on-disk entries, no
      rebase conflict on this file.
      Composed to 6 by ORCH's Step-Final rebase of
      feature/WF03-ISS0401-20260902 onto main, which by then included
      WF02-REQ212-20260901's own Step-Final composition (REQ-211 +
      ISS-0400's started+done + REQ-212 done, 4 entries) landing
      concurrently with this run's own started+done event pair --
      `grep -c "^  - req:" docs/status/requirement_status.v8.yaml` = 6
      on-disk entries after the merge (1 REQ-211 + 2 ISS-0400 + 1 REQ-212
      + 2 ISS-0401), no entry lost, per this note's own recount-every-time
      rule rather than carrying forward either side's independently
      computed "4". `wc`: 369 lines / 22384 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 4 by ORCH's Step-Final rebase of
      feature/WF02-REQ212-20260901 onto main, which by then included a
      concurrent sibling session's WF03-ISS0400-20260901 run (2 entries:
      started + done) landing alongside this run's own REQ-212 done-event
      append -- `grep -c "^  - req:" docs/status/requirement_status.v8.yaml`
      = 4 on-disk entries after the merge (1 REQ-211 + 2 ISS-0400 + 1
      REQ-212), no entry lost. `wc`: 322 lines / 19790 bytes, well under
      the 1200-line/120000-byte roll ceiling, so no roll triggered. Closes
      GH#769/ISS-0390 in full (REQ-211 core + REQ-212 routes both done).

      Incremented to 4 by ORCH's own WF03-ISS0401-20260902 started-event
      append (Step 00) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 4 on-disk entries at that
      run's own pre-rebase base, no rebase conflict on this file at that
      point.
      Incremented to 3 by ORCH's own WF03-ISS0400-20260901 done-event
      append (Step Final) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 3 on-disk entries, no
      rebase conflict on this file.
      Incremented to 2 by ORCH's own WF03-ISS0400-20260901 started-event
      append (Step 00) -- `grep -c "^  - req:"
      docs/status/requirement_status.v8.yaml` = 2 on-disk entries, no
      rebase conflict on this file.
      Incremented to 1 by ORCH's Step-Final rebase of
      feature/WF02-REQ211-20260901 onto main, appending REQ-211's own
      done-event (originally written into the just-closed volume 7 before
      this branch was rebased) as this volume's first entry. `grep -c "^  -
      req:" docs/status/requirement_status.v8.yaml` = 1 on-disk entry. `wc`:
      223 lines / 13574 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Opened by WF02-REQ208-20260901's own Step 6 DOC-UPDATER roll, successor
      to volume 7. Created with volume 7's header text verbatim (volume
      number, opening date and predecessor path updated) and an empty
      `history:` list, per this index's `roll_rule.on_exceed` procedure.
      `entries:` will be incremented per HOW-TO-APPEND step 6 as real
      appends land.

  - volume: 9
    path: docs/status/requirement_status.v9.yaml
    status: closed
    opened: 2026-09-04
    closed: 2026-09-05
    entries: 44
    lines: 1251
    bytes_working_tree: 72679
    # SHA-256 of lines 1..1223 with CRLF normalised to LF, i.e. the frozen prefix.
    # The hashed stream terminates EVERY line with a single \n, including the
    # last, so it ends in \n. Exact definition and reference implementations:
    # lib/letflow/design/iss-0119-status-file-readability.md §8.
    # Asserted unchanged by test/docs/requirement_status_invariants_test.exs.
    frozen_prefix_lines: 1223
    frozen_prefix_sha256: "a553b464cc6d715e40f079eb2a256399780da0ac7689481f89edfd04131f6dee"
    closure_note: >
      Closed at feature/WF02-REQ265-20260905's Step Final, whose own REQ-265
      done-event append brought this file to 1223 lines / 70772 bytes / 44
      entries -- over its own declared roll_rule ceiling (max_lines: 1200),
      confirmed via `wc -l` per this volume's own "WHEN THIS VOLUME IS FULL"
      procedure. `git diff --numstat` on this file across this append showed
      insertions-only (0 deletions). Successor:
      docs/status/requirement_status.v10.yaml.
    note: >
      Incremented to 44 by DOC-UPDATER's own REQ-265 done-event append
      (WF02-REQ265-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 44 on-disk entries, no
      rebase conflict on this file at the time it was written. This append
      crossed the roll ceiling (1223 lines), so this volume was closed and
      volume 10 opened in the same commit per this index's `roll_rule.on_exceed`
      procedure.
      Recounted to 43 by WF03-ISS0512-20260905's second Step Final rebase,
      which found main further advanced by WF02-REQ260-20260905's own
      started+done event pair (landed while this branch's own recount to
      41 was already committed). Composed cleanly in
      requirement_status.v9.yaml (no conflict beyond the two already-
      composed entries described below -- this run's own ISS-0481
      done-event sorts before REQ-260's started/done pair by `at:`, no
      reordering needed). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 43 on-disk entries, rather
      than carrying forward either side's independently-computed 41, per
      this note's own recount-every-time rule.
      Recounted to 41 by WF03-ISS0512-20260905's first Step Final rebase, which
      found main further advanced by WF03-ISS0511-20260905's own
      closeout-bookkeeping commit (landed while this branch's own append,
      independently computing 39 -- itself already including a
      previously-missed ISS-0511 done-event this run had added -- was
      already committed). Composed both sides' distinct ISS-0511
      started/done entries alongside this run's own ISS-0511/ISS-0512
      done-events in chronological `at:` order in
      requirement_status.v9.yaml itself (a genuine conflict there,
      resolved by keeping both sides' entries, none dropped or
      rewritten). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 41 on-disk entries,
      rather than carrying forward either side's independently-computed
      39, per this note's own recount-every-time rule.
      Incremented to 39 by ORCH's own ISS-0512 SCOPE-CHANGE done-event
      append (WF03-ISS0512-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 39 on-disk entries, no
      rebase conflict on this file (this run's own append also included
      a previously-missed ISS-0511 SCOPE-CHANGE done-event, bringing 37
      -> 39 in the same append). (Superseded by the recount to 43 above.)
      Incremented to 41 by WF02-REQ260-20260905's own done-event append
      (ORCH) -- `grep -c "^  - req:" docs/status/requirement_status.v9.yaml`
      = 41 on-disk entries, no rebase conflict on this file. 1130 lines,
      66066 bytes measured this append, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered. (Separate,
      concurrently-landed count of 41; also superseded by the recount to
      43 above.)
      Recounted to 40 by WF02-REQ260-20260905's own Step 00, appending one
      `started` event for REQ-260 (grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml = 40 on-disk entries).
      Recounted to 39 by WF03-ISS0511-20260905's own closeout-bookkeeping
      rebase onto main, which found main further advanced by
      WF02-REQ258-20260905's own recount to 37 (its second Step Final
      rebase, itself finding REQ-259's done-event append already landed)
      -- no conflict on requirement_status.v9.yaml itself, this run's own
      started+done SCOPE-CHANGE pair auto-merged cleanly onto the 37-entry
      base. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 39 on-disk entries, rather
      than carrying forward either side's independently-computed 37/38,
      per this note's own recount-every-time rule. 1080 lines, 63213
      bytes measured this append, well under the 1200-line/120000-byte
      roll ceiling. (Separate, concurrently-landed count of 39; also
      superseded by the recount to 41 above.)
      Recounted to 37 by WF02-REQ258-20260905's second Step Final rebase,
      which found main further advanced by WF02-REQ259-20260905's own
      REQ-259 done-event append (landed via PR #997 while this branch's
      first rebase, recounting to 36, was already committed) -- no
      conflict on requirement_status.v9.yaml itself, it auto-merged
      cleanly. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 37 on-disk entries, rather
      than carrying forward either side's independently-computed 36, per
      this note's own recount-every-time rule.
      Recounted to 36 by WF02-REQ258-20260905's first Step Final rebase, which
      found main further advanced by WF02-REQ257-20260905's own REQ-257
      done-event append (landed while this branch's own REQ-258 append,
      independently computing 35, was already committed). Composed in
      chronological `at:` order -- REQ-258 done (18:01:58Z) before REQ-257
      done (18:05:05Z). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 36 on-disk entries, rather
      than carrying forward either side's independently-computed 35, per
      this note's own recount-every-time rule.
      Incremented to 35 by DOC-UPDATER's own REQ-258 done-event append
      (WF02-REQ258-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 35 on-disk entries, no
      rebase conflict on this file at the time it was written. (Superseded
      by the recount to 36 above.)
      Incremented to 35 by WF02-REQ257-20260905 (ORCH)'s own REQ-257
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 35 on-disk entries, no
      rebase conflict on this file (this run's requirements.yaml/registry
      conflicts during rebase were on other files; this volume's own tail
      applied cleanly). (Separate, concurrently-landed count of 35; also
      superseded by the recount to 36 above.)
      Recounted to 34 by WF02-REQ253-20260905's fourth Step Final rebase,
      which found main further advanced by WF02-REQ256-20260905's own
      Step Final bookkeeping (REQ-256 started+done, 2 appends) -- no
      conflict on requirement_status.v9.yaml itself, it auto-merged
      cleanly. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 34 on-disk entries, rather
      than carrying forward either side's independently-computed 32/33,
      per this note's own recount-every-time rule.
      Recounted to 32 by WF02-REQ253-20260905's third Step Final rebase,
      which found main further advanced by WF02-REQ254-20260905's own
      Step Final closeout (PR #990, its own REQ-254 done-event append) --
      no conflict on requirement_status.v9.yaml itself, it auto-merged
      cleanly with REQ-254's done-event already appended after its own
      started-event. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 32 on-disk entries, rather
      than carrying forward either side's independently-computed 31, per
      this note's own recount-every-time rule.
      Recounted to 31 by WF02-REQ253-20260905's second Step Final rebase,
      which found main further advanced by WF02-REQ254-20260905's own
      REQ-254 started-event append (landed via PR #988 while this branch's
      first rebase attempt, recounting to 30 below, was still in flight).
      No conflict on requirement_status.v9.yaml itself beyond the two
      already-composed entries described below -- REQ-254's started-event
      appends cleanly at the tail after REQ-254 (started 15:31:17Z, sorts
      after REQ-253 done 15:18:45Z). Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v9.yaml` = 31 on-disk
      entries, rather than carrying forward either side's
      independently-computed 30, per this note's own recount-every-time
      rule.
      Recounted to 30 by WF02-REQ253-20260905's first Step Final rebase, which
      conflicted at this volume's tail against a concurrent sibling
      session's own REQ-245 done-event append (PR #985, landed on main
      after this branch's own REQ-253 done-event was written against a
      base declaring 29). Composed in chronological `at:` order -- REQ-245
      done (2026-09-05T15:18:39Z) before REQ-253 done (2026-09-05T15:18:45Z)
      -- REQ-245's entry was interleaved ahead of REQ-253's rather than the
      reverse. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 30 on-disk entries, rather
      than carrying forward either side's independently-computed 29, per
      this note's own recount-every-time rule. With both REQ-245 and
      REQ-253 done, all 11 REQ-238-replacement chunks (REQ-243..253) are
      now complete.
      Incremented to 29 by DOC-UPDATER's REQ-253 done-event append
      (WF02-REQ253-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 29 on-disk entries, no
      rebase conflict on this file (superseded above; this run's own
      pre-rebase count did not yet know of the concurrently-landed
      REQ-245 done-event).
      Incremented to 33 by ORCH's own REQ-256 started+done event pair
      (WF02-REQ256-20260905, Step Final bookkeeping) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 33 on-disk entries, no
      rebase conflict on this file (queue task 494, chunk 239c, closes
      ISS-0424 part 3c-3). (Superseded by the recount to 34 at the top of
      this note.)
      Incremented to 31 by ORCH's own REQ-254 done-event append
      (WF02-REQ254-20260905, Step Final) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 31 on-disk entries, no
      rebase conflict on this file. (Superseded by the recount to 32 at
      the top of this note.)
      Incremented to 30 by ORCH's own REQ-254 started-event append
      (WF02-REQ254-20260905, Step 00) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 30 on-disk entries, no
      rebase conflict on this file. Caught by CI's own A9 invariant test
      (test/docs/requirement_status_invariants_test.exs) failing on PR #988
      after the started-event append landed without its own index-note
      increment; corrected here in the same branch before merge. (This is
      a separate, concurrently-landed count of 30 from the one immediately
      above -- both are superseded by the recount to 31 at the top of this
      note.)
      Incremented to 28 by DOC-UPDATER's REQ-252 done-event append
      (WF02-REQ252-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 28 on-disk entries, no
      rebase conflict on this file.
      Incremented to 27 by DOC-UPDATER's REQ-251 done-event append
      (WF02-REQ251-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 27 on-disk entries, no
      rebase conflict on this file.
      Incremented to 25 by DOC-UPDATER's REQ-249 done-event append
      (WF02-REQ249-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 25 on-disk entries, no
      rebase conflict on this file.
      Incremented to 24 by DOC-UPDATER's REQ-248 done-event append
      (WF02-REQ248-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 24 on-disk entries, no
      rebase conflict on this file.
      Incremented to 23 by DOC-UPDATER's REQ-247 done-event append
      (WF02-REQ247-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 23 on-disk entries, no
      rebase conflict on this file.
      Incremented to 22 by DOC-UPDATER's REQ-246 done-event append
      (WF02-REQ246-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 22 on-disk entries, no
      rebase conflict on this file.
      Incremented to 21 by DOC-UPDATER's REQ-244 done-event append
      (WF02-REQ244-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 21 on-disk entries, no
      rebase conflict on this file.
      Incremented to 20 by DOC-UPDATER's REQ-243 done-event append
      (WF02-REQ243-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 20 on-disk entries, no
      rebase conflict on this file.
      Incremented to 19 by DOC-UPDATER's REQ-242 done-event append
      (WF02-REQ242-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 19 on-disk entries, no
      rebase conflict on this file.
      Incremented to 17 by DOC-UPDATER's REQ-240 done-event append
      (WF02-REQ240-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 17 on-disk entries, no
      rebase conflict on this file.
      Incremented to 16 by DOC-UPDATER's REQ-237 done-event append
      (WF02-REQ237-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 16 on-disk entries, no
      rebase conflict on this file.
      Incremented to 15 by DOC-UPDATER's REQ-235 done-event append
      (WF02-REQ235-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 15 on-disk entries, no
      rebase conflict on this file.
      Incremented to 14 by DOC-UPDATER's REQ-234 done-event append
      (WF02-REQ234-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 14 on-disk entries, no
      rebase conflict on this file.
      Incremented to 13 by DOC-UPDATER's REQ-233 done-event append
      (WF02-REQ233-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 13 on-disk entries, no
      rebase conflict on this file.
      Incremented to 12 by ORCH's ISS-0422 SCOPE-CHANGE done-event append
      (ORCH-direct-20260905-ISS0422) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 12 on-disk entries, no
      rebase conflict on this file.
      Incremented to 11 by DOC-UPDATER's REQ-232 done-event append
      (WF02-REQ232-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 11 on-disk entries, no
      rebase conflict on this file.
      Recounted to 10 by WF03-ISS0430-20260905's Step Final rebase, which
      conflicted at this volume's tail against WF03-ISS0469-20260904's own
      done-event append (landed on main after this branch's own commit was
      written). Composed in chronological `at:` order -- ISS-0469 done
      (22:22:00Z) before ISS-0430 done (23:11:29Z) -- ISS-0430's entry was
      appended after ISS-0469's rather than the reverse. Recounted directly
      from `grep -c "^  - req:" docs/status/requirement_status.v9.yaml` = 10
      on-disk entries, rather than carrying forward either side's
      independently-computed 9, per this note's own recount-every-time rule.
      Incremented to 1 by ORCH's own ISS-0451 done-event append
      (WF03-ISS0451-20260904) -- `grep -c "^  - req:"
      docs/status/requirement_status.v9.yaml` = 1 on-disk entry, no rebase
      conflict on this file.
      Opened by ORCH at feature/WF02-REQ219-20260903's Step Final rebase,
      successor to volume 8. Created with volume 8's header text verbatim
      (volume number, opening date and predecessor path updated) and an
      empty `history:` list, per this index's `roll_rule.on_exceed`
      procedure. `entries:` will be incremented per HOW-TO-APPEND step 6
      as real appends land.

  - volume: 10
    path: docs/status/requirement_status.v10.yaml
    status: closed
    opened: 2026-09-05
    closed: 2026-09-07
    entries: 24
    lines: 1210
    bytes_working_tree: 73372
    frozen_prefix_lines: 1210
    frozen_prefix_sha256: "c23f0b80ea3fb06953c0d8f20ae77131ba21e47db1699ba17197e519162c5c37"
    note: >
      Closed 2026-09-07 by ORCH's own ISS-0437 SCOPE-CHANGE done-event append
      (WF03-ISS0437-20260907), which brought this file to 1210 lines / 73372
      bytes -- over the 1200-line roll ceiling flagged as imminent by the
      prior note below. Rolled to volume 11
      (docs/status/requirement_status.v11.yaml) per the standard procedure:
      the triggering entry itself stays in this volume (matching the v9->v10
      precedent), a closure footer was appended, and v11 was created with
      this volume's header text carried forward verbatim (volume number,
      opening date, predecessor path updated).
      Incremented to 24 by ORCH's own ISS-0437 SCOPE-CHANGE done-event
      append (WF03-ISS0437-20260907) -- 1210 lines at that point (73372
      bytes), over the 1200-line/120000-byte roll ceiling, triggering the
      roll documented above.
      Previously incremented to 23 by ORCH's own ISS-0418 SCOPE-CHANGE done-event
      append (WF03-ISS0418-20260907) -- 1181 lines at that point
      (71589 bytes), STILL under the 1200-line/120000-byte roll ceiling
      but with only ~19 lines of headroom left.
      Previously incremented to 22 by ELIXIR-DEV's own REQ-231 done-event
      Previously incremented to 21 by ELIXIR-DEV's own REQ-230 done-event
      append (WF02-REQ230-20260907) -- 1063 lines at that point.
      Previously incremented to 20 by ORCH's own ISS-0519 SCOPE-CHANGE done-event
      append (WF03-ISS0519-20260907) -- 994 lines at that point, well
      under the roll ceiling.
      Previously incremented to 19 by ELIXIR-DEV's own REQ-229 done-event append
      (WF02-REQ229-20260906) -- 934 lines at that point, well under the
      roll ceiling.
      Previously incremented to 18 by ELIXIR-DEV's own REQ-228 done-event append
      (WF02-REQ228-20260906) -- 862 lines at that point, well under the
      roll ceiling.
      Previously recomposed to 17 during feature/WF02-REQ226-20260906's rebase onto
      main post-ISS-0515 merge: this branch's own REQ-226 append (15,
      counted before REQ-227/ISS-0515 existed on main) was reordered
      chronologically ahead of REQ-227's (16) and ISS-0515's (17, the
      tip of main at rebase time) entries, all disjoint, none dropped --
      `grep -c "^  - req:" docs/status/requirement_status.v10.yaml` = 17
      on-disk entries after composition. 786 lines, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      Previously incremented to 16 by ORCH's own ISS-0515 SCOPE-CHANGE
      done-event append (WF03-ISS0515-20260906) -- 715 lines at that
      point. Previously incremented to 15 by ELIXIR-DEV's own REQ-227
      done-event append (WF02-REQ227-20260906) -- 672 lines at that
      point. Previously incremented to 14 by ELIXIR-DEV's own REQ-225
      done-event append (WF02-REQ225-20260906) -- 621 lines at that
      point, well under the roll ceiling.
      Recounted to 13 by chore/ISS0424-resolved-20260906's Step Final
      rebase, which found main further advanced by
      WF02-REQ263-20260906's own closeout append (REQ-263 done,
      07:00:40Z, landed via PR #1026 while this branch's own ISS-0424
      SCOPE-CHANGE append, independently computing 12, was already
      committed). Composed in chronological `at:` order in
      requirement_status.v10.yaml itself -- REQ-263 done (07:00:40Z)
      before ISS-0424 done (07:09:08Z). Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v10.yaml` = 13
      on-disk entries, rather than carrying forward either side's
      independently-computed 12, per this note's own recount-every-time
      rule.
      Incremented to 12 by ORCH's own ISS-0424 SCOPE-CHANGE done-event
      append -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 12 on-disk entries, no
      rebase conflict on this file. 533 lines, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      (Superseded by the recount to 13 above.)
      Recounted to 12 by WF02-REQ263-20260906's closeout append (REQ-263
      done, 07:00:40Z), which found the declared count still at 11 from
      the prior WF02-REQ262-20260906 recount below -- the closeout
      commit updated the count in the same commit as the append, per
      HOW-TO-APPEND step 6, after CI caught the drift live (A9 invariant
      failure on PR #1026, `mix letflow.check`, run 34018087236) rather
      than before. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v10.yaml` = 12
      on-disk entries, no rebase pending at the time of this recount.
      (Separate, concurrently-landed count of 12; also superseded by
      the recount to 13 above.)

      PRIOR NOTE (recount to 11): Recounted to 11 by WF02-REQ262-20260906's Step Final rebase, which
      found main further advanced by WF02-REQ261-20260906's own REQ-261
      done-event append (landed via PR #1023 while this branch's own
      recount to 10 was already committed). Composed in chronological
      `at:` order in requirement_status.v10.yaml itself -- REQ-262 done
      (05:14:49Z) before REQ-261 done (05:21:54Z). Recounted directly
      from `grep -c "^  - req:" docs/status/requirement_status.v10.yaml`
      = 11 on-disk entries, rather than carrying forward either side's
      independently-computed 10, per this note's own recount-every-time
      rule.
      Incremented to 10 by DOC-UPDATER's own REQ-262 done-event append
      (WF02-REQ262-20260906) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 10 on-disk entries, no
      rebase conflict on this file at the time it was written. 481
      lines, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered. (Superseded by the recount to 11 above.)
      Incremented to 10 by ORCH's own REQ-261 done-event append
      (WF02-REQ261-20260906) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 10 on-disk entries,
      no rebase conflict on this file. 475 lines, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      (Separate, concurrently-landed count of 10; also superseded by
      the recount to 11 above.)
      Incremented to 9 by DOC-UPDATER's own REQ-255 done-event append
      (WF02-REQ255-20260906) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 9 on-disk entries, no
      rebase conflict on this file at the time it was written. 449
      lines, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.
      Recounted to 8 by WF02-REQ270-20260906's second Step Final rebase,
      which found main further advanced by WF02-REQ271-20260906's own
      REQ-271 started+done event pair (2 appends, landed while this
      branch's own recount to 6 was already committed) -- composed in
      requirement_status.v10.yaml itself in chronological `at:` order
      (REQ-270 done 01:43:12Z, REQ-271 started 02:03:01Z, REQ-271 done
      02:12:35Z), none dropped or rewritten. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v10.yaml` = 8
      on-disk entries, rather than carrying forward either side's
      independently-computed 6/7, per this note's own recount-every-time
      rule. With REQ-271 done, all 8 lib/letflow/design/-scope chunks
      (REQ-264..271) are now complete.
      Recounted to 6 by WF02-REQ270-20260906's first Step Final rebase, which
      found main further advanced by WF02-REQ268-20260906's own closeout
      (REQ-268 done-event, landed via PR #1015 while this branch's own
      REQ-270 append, independently computing 5, was already committed)
      -- no conflict on requirement_status.v10.yaml itself, it
      auto-merged cleanly. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 6 on-disk entries,
      rather than carrying forward either side's independently-computed
      5, per this note's own recount-every-time rule.
      Incremented to 5 by DOC-UPDATER's own REQ-270 done-event append
      (WF02-REQ270-20260906) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 5 on-disk entries, no
      rebase conflict on this file at the time it was written. 304
      lines, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered. (Superseded by the recount to 8 above.)
      Recounted to 7 by WF02-REQ271-20260906 (DOC-UPDATER)'s own REQ-271
      done-event append (Step 01) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 7 on-disk entries (the
      declared 5 immediately prior to this append was already one behind
      the true on-disk count, since ORCH's own REQ-271 started-event
      append at Step 00 had landed without its own index-note increment;
      both are folded into this recount rather than corrected
      retroactively, per this note's own recount-every-time rule). No
      rebase conflict on this file at the time it was written. 376 lines /
      22462 bytes, well under the 1200-line/120000-byte roll ceiling, so
      no roll triggered. (Separate, concurrently-landed count of 7; also
      superseded by the recount to 8 above.)
      Recounted to 5 by WF02-REQ268-20260906-closeout's Step Final rebase,
      which found main further advanced by WF02-REQ269-20260906's own
      done-event append (landed while this branch's own REQ-268 done-event
      append, independently computing 4, was already committed) -- no
      lost content: both REQ-268's and REQ-269's done-events are present
      in requirement_status.v10.yaml after composing both sides
      chronologically (REQ-268 at 00:48:06Z before REQ-269 at
      01:22:17Z). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 5 on-disk entries, rather
      than carrying forward either side's independently-computed 4, per
      this note's own recount-every-time rule. 296 lines / 17442 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. (Separate, concurrently-landed count of 5; also
      superseded by the recount to 6 above.)
      Incremented to 4 by DOC-UPDATER's own REQ-269 done-event append
      (WF02-REQ269-20260906) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 4 on-disk entries, no
      rebase conflict on this file at the time it was written. 267 lines,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. (Superseded by the recount to 5 above.)
      Incremented to 4 by ORCH's own REQ-268 done-event append
      (WF02-REQ268-20260906 closeout), independently computed against a
      base declaring 3 -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 4 on-disk entries at that
      point. (Superseded by the recount to 5 above.)
      Recounted to 3 by WF02-REQ266-20260905's second Step Final rebase,
      which found main further advanced by WF02-REQ267-20260906's own
      closeout (REQ-267 done-event, landed via PR #1013 while this
      branch's own recount to 2 was already committed) -- no conflict on
      requirement_status.v10.yaml itself, it auto-merged cleanly.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 3 on-disk entries, rather
      than carrying forward either side's independently-computed 2, per
      this note's own recount-every-time rule.
      Recounted to 2 by WF02-REQ266-20260905's first Step Final rebase, which
      found main further advanced by WF02-REQ264-20260905's own closeout
      (REQ-264 done-event, landed via PR #1011 while this branch's own
      REQ-266 append, independently computing 1, was already committed).
      Composed in chronological `at:` order in
      requirement_status.v10.yaml itself -- REQ-266 done (23:11:24Z)
      before REQ-264 done (23:13:25Z). Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v10.yaml` = 2 on-disk
      entries, rather than carrying forward either side's
      independently-computed 1, per this note's own recount-every-time
      rule.
      Incremented to 1 by DOC-UPDATER's own REQ-266 done-event append
      (WF02-REQ266-20260905) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 1 on-disk entry, no
      rebase conflict on this file at the time it was written. (Superseded
      by the recount to 2 above.)
      Incremented to 2 by ORCH's own REQ-267 done-event append
      (WF02-REQ267-20260906 closeout) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 2 on-disk entries, no
      rebase conflict on this file. 212 lines / 12476 bytes, well under
      the 1200-line/120000-byte roll ceiling, so no roll triggered.
      (Separate, concurrently-landed count of 2; also superseded by the
      recount to 3 above.)
      Incremented to 1 by ORCH's own REQ-264 done-event append
      (WF02-REQ264-20260905 closeout) -- `grep -c "^  - req:"
      docs/status/requirement_status.v10.yaml` = 1 on-disk entry, no rebase
      conflict on this file. 188 lines / 11133 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered. (Separate,
      concurrently-landed count of 1; also superseded by the recount to 2
      above.)
      Opened by ORCH at feature/WF02-REQ265-20260905's Step Final, successor
      to volume 9. Created with volume 9's header text verbatim (volume
      number, opening date and predecessor path updated) and an empty
      `history:` list, per this index's `roll_rule.on_exceed` procedure.
      `entries:` will be incremented per HOW-TO-APPEND step 6 as real
      appends land.

  - volume: 11
    path: docs/status/requirement_status.v11.yaml
    status: closed
    opened: 2026-09-07
    closed: 2026-09-09
    entries: 34
    lines: 1212
    bytes_working_tree: 74041
    frozen_prefix_lines: 1212
    frozen_prefix_sha256: "78427a0febd3802257a1cfa1a8ba1f0f185e3f5b7ec693abc8172730722f8e3d"
    note: >
      Closed 2026-09-09 by ORCH's own REQ-282 done-event append
      (WF02-REQ282-20260909), which brought this file to 1212 lines / 74041
      bytes -- over the 1200-line roll ceiling flagged as imminent by the
      prior note below. Rolled to volume 12
      (docs/status/requirement_status.v12.yaml) per the standard procedure:
      the triggering entry itself stays in this volume (matching the
      v9->v10 and v10->v11 precedents), a closure footer was appended, and
      v12 was created with this volume's header text carried forward
      verbatim (volume number, opening date, predecessor path updated).
      NOTE (added at WF02-REQ277-20260909 Step 00b rebase, 2026-09-09):
      this branch's own DOC-UPDATER commit (7f459464) originally recorded
      REQ-277's done-event as an append to THIS volume (v11) plus a
      composed entries:30 count reflecting a since-superseded pre-rebase
      state. Volume 11 had already closed (frozen at entries:34, the count
      recorded above) by the time this branch was rebased onto current
      main, so per this volume's own "DO NOT APPEND" rule that stale
      append was redirected to volume 12 (the current volume) instead of
      composed here -- see volume 12's own entries:5 note for the
      redirected REQ-277 done-event. This volume's own entries/lines/bytes/
      frozen_prefix fields above are main's, unmodified by this rebase.
      Incremented to 34 by ORCH's own REQ-282 done-event append
      (WF02-REQ282-20260909) -- 1168 lines at that point BEFORE this
      append (71349 bytes), crossing to 1212 lines (74041 bytes) after,
      triggering this closure.
      Previously incremented to 33 by ORCH's own REQ-281 done-event append
      (WF02-REQ281-20260909) -- 1168 lines at that point (71349 bytes).
      ONLY ~32 LINES OF MARGIN LEFT before the 1200-line ceiling. The
      VERY NEXT append to this volume MUST roll to volume 12 first --
      check line count BEFORE writing your entry (a typical entry is
      25-50 lines), and if writing it would cross 1200, close this
      volume (append the closure footer, set status: closed here) and
      create docs/status/requirement_status.v12.yaml per the "WHEN THIS
      VOLUME IS FULL" procedure in this volume's own header, THEN write
      your entry into volume 12 instead. Do not write past the ceiling.
      Previously incremented to 32 by ORCH's own REQ-278 done-event append
      (WF02-REQ278-20260909) -- 1127 lines at that point (68845 bytes).
      STILL UNDER the 1200-line ceiling but only ~73 lines of margin left --
      the NEXT append to this volume will very likely need to roll to
      volume 12 first. Whoever appends next: check line count BEFORE
      writing, and if it would cross 1200, close this volume and open
      volume 12 per the "WHEN THIS VOLUME IS FULL" procedure in this
      file's own header, rather than writing past the ceiling.
      Previously incremented to 31 by ORCH's own ISS-0557 SCOPE-CHANGE cancelled-event
      append (WF03-ISS0557-20260909) -- 1088 lines at that point (66559
      bytes), still under the roll ceiling (1200 lines/120000 bytes) but
      close -- a roll to volume 12 is likely within the next few appends.
      Previously incremented to 30 by ORCH's own ISS-0558 SCOPE-CHANGE done-event append
      (WF03-ISS0558-20260909) -- 1062 lines at that point (65124 bytes),
      still under the roll ceiling (1200 lines/120000 bytes) but getting
      closer -- watch for a roll within the next several appends.
      Previously incremented to 29 by ORCH's own ISS-0559 SCOPE-CHANGE done-event append
      (WF03-ISS0559-20260909) -- 1006 lines at that point (61542 bytes),
      approaching but still under the roll ceiling (1200 lines/120000 bytes) --
      worth watching for a roll soon.
      Previously incremented to 28 by ORCH's own REQ-276 done-event append
      (WF02-REQ276-20260909) -- 967 lines at that point (59178 bytes), well
      under the roll ceiling, composed after rebasing through a large
      structural conflict in docs/requirements.yaml against a concurrent
      15-requirement DOC-UPDATER filing batch (PR #1130).
      Incremented to 29 by WF02-REQ277-20260909 (DOC-UPDATER)'s own REQ-277
      done-event append (Step 6) -- `grep -c '^  - req:'
      docs/status/requirement_status.v11.yaml` = 29 on-disk entries, no rebase
      conflict on this file at the time it was written. 954 lines (59141
      bytes) at that point, well under the roll ceiling, so no roll
      triggered.
      Previously incremented to 28 by ORCH's own REQ-288 done-event append
      (WF02-REQ288-20260908) -- 914 lines at that point (55812 bytes), well
      under the roll ceiling, composed after rebasing through two real
      conflicts in docs/requirements.yaml against siblings this run.
      Previously incremented to 26 by this run's (WF02-REQ287-20260909) second Step Final
      rebase onto origin/main (main advanced again between the first rebase
      and merge attempt), composing this branch's own two REQ-287 appends
      (started-event Step 00 and DOC-UPDATER's done-event Step 6) with main's
      own concurrently-landed appends from sibling runs -- ORCH's own REQ-280
      done-event append (WF02-REQ280-20260908), the WF03-ISS0548-20260908
      SCOPE-CHANGE done-event append (--z-* CSS token scale), and the
      WF03-ISS0549-20260908 SCOPE-CHANGE done-event append (Button
      data-testid passthrough) -- through two real rebase conflicts (once in
      requirement_status.v11.yaml, once here), all five appends composed,
      none discarded. 26 `- req:` entries confirmed by
      `grep -c '^  - req:' docs/status/requirement_status.v11.yaml`, 859 lines
      / 53232 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered. This closes the design-system-primitives batch:
      REQ-272, REQ-274, REQ-275 and REQ-287 are all now done.
      Previously incremented to 22 by ORCH's own ISS-0548 SCOPE-CHANGE done-event append
      (WF03-ISS0548-20260908) -- --z-* CSS token scale.
      Previously incremented to 21 by this run's (WF02-REQ275-20260909) rebase composing
      a REQ-275 started-event append (Step 00, landed pre-rebase as commit
      1b6baf24, which had bumped entries 17 -> 18 with no new note paragraph)
      and DOC-UPDATER's REQ-275 done-event append (Step 6, which on its own
      pre-rebase branch had bumped entries 18 -> 19) with main's own
      concurrently-landed REQ-274 done-event and ISS-0547 SCOPE-CHANGE
      done-event appends (which had independently bumped entries to 19) --
      21 `- req:` entries confirmed by
      `grep -c '^  - req:' docs/status/requirement_status.v11.yaml`, well
      under the roll ceiling.
      Previously incremented to 19 by ORCH's own ISS-0547 SCOPE-CHANGE done-event append
      (WF03-ISS0547-20260908) -- 707 lines at that point (43439 bytes), well
      under the roll ceiling.
      Previously incremented to 18 by ORCH's own REQ-274 done-event append
      (WF02-REQ274-20260908) -- 687 lines at that point (42399 bytes), well
      under the roll ceiling, composed after rebasing onto a sibling
      session's ISS-0546 fix.
      Previously incremented to 17 during this run's (WF02-REQ272-20260908) Step Final
      rebase onto origin/main, reconciling two independent append chains that
      both incremented from the same pre-rebase base: ORCH's ISS-0545
      SCOPE-CHANGE done-event append (WF03-ISS0545-20260908) and ORCH's
      REQ-273 done-event append (WF02-REQ273-20260908) landed on main first;
      this branch's own REQ-272 started-event append (Step 00) and
      DOC-UPDATER's REQ-272 done-event append (Step 6) were then replayed on
      top. All four appends are present in
      docs/status/requirement_status.v11.yaml (17 `- req:` entries, confirmed
      by `grep -c '^  - req:'`), well under the roll ceiling.
      Previously incremented to 15 by ORCH's own ISS-0545 SCOPE-CHANGE
      done-event append (WF03-ISS0545-20260908) -- 599 lines at that point
      (36918 bytes), well under the roll ceiling.
      Previously incremented to 14 by ORCH's own REQ-273 done-event append
      (WF02-REQ273-20260908) -- 574 lines at that point (35609 bytes), well
      under the roll ceiling.
      Previously incremented to 13 by ORCH's own ISS-0296 SCOPE-CHANGE verified-event append
      (WF03-ISS0296-20260908) -- 540 lines at that point (33574 bytes), well
      under the roll ceiling.
      Previously incremented to 12 by ORCH's own ISS-0531 SCOPE-CHANGE done-event append
      (WF03-ISS0531-20260908) -- 505 lines at that point (31361 bytes), well
      under the roll ceiling.
      Previously incremented to 11 by ORCH's own ISS-0529 SCOPE-CHANGE done-event append
      (WF03-ISS0530-20260908) -- CORS localhost:4173 fix, rebased on main.
      Previously incremented to 10 by ORCH's own ISS-0532 SCOPE-CHANGE done-event append
      (WF03-ISS0532-20260908) -- 469 lines at that point (29011 bytes), well
      under the roll ceiling, composed on rebase with a concurrently-landed
      sibling append.
      Previously incremented to 9 by ORCH's own ISS-0536 SCOPE-CHANGE done-event append
      (WF03-ISS0536-20260908) -- drop_schema retry-once fix, rebased on main.
      Previously incremented to 8 by ORCH's own ISS-0534 SCOPE-CHANGE done-event append
      (WF03-ISS0534-20260908) -- 404 lines at that point (25001 bytes), well
      under the roll ceiling, composed on rebase with a concurrently-landed
      sibling append.
      Previously incremented to 7 by ORCH's own ISS-0535 SCOPE-CHANGE done-event append
      (WF03-ISS0535-20260908) -- environment-only issue fix, no code change.
      Previously incremented to 6 by ORCH's own ISS-0391 SCOPE-CHANGE done-event append
      (chore/status-iss0391-20260907) -- 342 lines at that point (20861 bytes),
      well under the roll ceiling.
      Previously incremented to 5 by ORCH's own ISS-0527 SCOPE-CHANGE done-event append
      (WF03-ISS0527-20260907) -- 325 lines at that point (19925 bytes), well
      under the roll ceiling.
      Previously incremented to 4 by ORCH's own ISS-0526 SCOPE-CHANGE done-event append
      (WF03-ISS0526-20260907) -- 288 lines at that point (17534 bytes), well
      under the roll ceiling.
      Previously incremented to 3 by ORCH's own ISS-0393 SCOPE-CHANGE revised-event append
      (WF03-ISS0393-20260907) -- 260 lines at that point (15752 bytes), well
      under the roll ceiling.
      Previously incremented to 2 by ORCH's own ISS-0525 SCOPE-CHANGE done-event append
      (WF03-ISS0525-20260907) -- 228 lines at that point (13724 bytes), well
      under the roll ceiling.
      Previously incremented to 1 by ORCH's own ISS-0524 SCOPE-CHANGE done-event append
      (WF03-ISS0524-20260907) -- 197 lines at that point (11705 bytes), well
      under the roll ceiling.
      Opened by ORCH at feature/WF03-ISS0437-20260907's Step Final, successor
      to volume 10. Created with volume 10's header text verbatim (volume
      number, opening date and predecessor path updated) and an empty
      `history:` list, per this index's `roll_rule.on_exceed` procedure.
      `entries:` will be incremented per HOW-TO-APPEND step 6 as real
      appends land.

  - volume: 12
    path: docs/status/requirement_status.v12.yaml
    status: closed
    opened: 2026-09-09
    closed: 2026-09-10
    entries: 22
    lines: 1233
    bytes_working_tree: 75617
    frozen_prefix_lines: 1233
    frozen_prefix_sha256: "44005b55709b02651a17fe6ce106046aef700c746b472308b37c411edc1d0722"
    note: >
      Closed 2026-09-10 by WF02-REQ300-20260910's own Step Final rebase,
      which composed this branch's own REQ-300 done-event append against
      main's concurrently-merged REQ-301 done-event (WF02-REQ301-20260910,
      DOC-UPDATER) in chronological `at:` order (REQ-301 done 08:45:52Z,
      REQ-300 done 10:49:42Z), bringing this file to 22 entries across
      1233 lines / 75617 bytes -- over the 1200-line roll ceiling flagged
      as imminent by the prior note below. Rolled to volume 13
      (docs/status/requirement_status.v13.yaml) per the standard
      procedure: closure footer appended to this file, frozen_prefix_sha256
      above computed via `sha256sum` on the working-tree file BEFORE the
      footer was appended (the frozen prefix excludes the footer), volume
      13 created with this volume's own header text verbatim (volume
      number/opening date/predecessor path updated) and an empty
      `history:` list.
      NOTE (WF02-REQ290-20260910's own Step Final rebase): this branch's
      Step 00 (commit afc61205) had appended a REQ-290 started-event to
      v12.yaml against a stale pre-closure base, and later branch commits
      (dc1ea7db, db711a3b) bumped this index's declared count and closure
      metadata to match a second, competing closure of v12 -- both
      superseded here. v12 was independently closed on main first (via
      WF02-REQ300-20260910, above) with its own frozen 22-entry content
      that does NOT include REQ-290's started-event. Per this file's own
      established pattern for a started-event that predates a roll it
      wasn't present for ("one run's started/done land one in each volume
      across a roll boundary"), neither of REQ-290's started/done events
      was inserted into this now-frozen volume; both are composed into
      volume 13 instead as part of this same rebase. v12's on-disk
      content and this entry's fields are therefore main's, unchanged.
      Recounted to 21 by WF02-REQ301-20260910's Step Final rebase, which
      conflicted at this file's tail a fourth time composing DOC-UPDATER's
      own REQ-301 done-event append (Step 6, against a stale base of 20)
      with main's REQ-298 done-event composition (already recounted to
      20 by this same run's own third-conflict composition below), both
      already composed into docs/status/requirement_status.v12.yaml in
      chronological `at:` order (REQ-298 done 05:33:45Z, REQ-301 done
      08:45:52Z). Recounted directly from the composed file
      (`grep -c "^  - req:" docs/status/requirement_status.v12.yaml` = 21
      on-disk entries) rather than carrying forward either side's
      independently-computed 20, per this note's own recount-every-time
      rule. 1160 lines / 72214 bytes at this point, still under the
      1200-line/120000-byte roll ceiling but with markedly less margin --
      the next append to this volume should check whether it crosses the
      ceiling.
      Recounted to 20 by WF02-REQ301-20260910's Step Final rebase, which
      conflicted at this file's tail a third time composing this branch's
      own A9 self-fix (recounted 19, against a stale base of 18) with
      main's REQ-298 done-event composition (recounted 19 independently,
      against a stale base of 17), both already composed into
      docs/status/requirement_status.v12.yaml in chronological `at:` order
      (REQ-301 started 04:35:04Z, REQ-298 done 05:33:45Z). Recounted
      directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 20 on-disk entries)
      rather than carrying forward either side's independently-computed
      19, per this note's own recount-every-time rule. 1111 lines / 69004
      bytes at this point, still under the 1200-line/120000-byte roll
      ceiling but with less margin.
      Separately, and concurrently, recounted to 19 by WF02-REQ298-20260910's
      Step Final rebase, which conflicted at this file's tail composing
      ORCH's own REQ-298 done-event append (against a stale base of 17)
      with main's REQ-299 done-event (WF02-REQ299-20260910, DOC-UPDATER),
      both composed into docs/status/requirement_status.v12.yaml in
      chronological `at:` order (REQ-299 done 04:07:42Z, REQ-298 done
      05:33:45Z). Recounted directly from the composed file
      (`grep -c "^  - req:" docs/status/requirement_status.v12.yaml` = 19
      on-disk entries) rather than carrying forward either side's
      independently-computed 17, per this note's own recount-every-time
      rule. 1089 lines / 66743 bytes at this point, still under the
      1200-line/120000-byte roll ceiling but with less margin.
      Separately, and concurrently, incremented to 20 by DOC-UPDATER's own
      REQ-301 done-event append (Step 6, WF02-REQ301-20260910) -- `grep -c
      "^  - req:" docs/status/requirement_status.v12.yaml` = 20 on-disk
      entries at that run's own pre-rebase base, no rebase conflict found
      by that run against this file. 1098 lines / 68312 bytes, well under
      the 1200-line/120000-byte roll ceiling.
      Separately, and concurrently, incremented to 19 by ORCH's own REQ-301
      started-event append (Step 00, WF02-REQ301-20260910) -- `grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 19 on-disk entries. The
      Step 00 commit (9f4466ce) appended the entry to v12.yaml without
      also bumping this index count in the same commit, caught by
      test/docs/requirement_status_invariants_test.exs's A9 assertion
      during this run's own TEST-RUNNER Step 4 pass. Fixed here rather
      than left for a later reconciliation, per this file's own
      recount-every-time rule.
      Recounted to 18 by WF02-REQ299-20260910's Step Final rebase, which
      conflicted at this file's tail a second time composing DOC-UPDATER's
      own REQ-299 done-event append (Step 6, against a stale base of 17)
      with main's REQ-297 done-event (WF02-REQ297-20260909) and this
      branch's own Step 00 started-event, both already composed into
      docs/status/requirement_status.v12.yaml in chronological `at:` order
      (REQ-299 started 01:15:02Z, REQ-297 done 01:36:48Z, REQ-299 done
      04:07:42Z). Recounted directly from the composed file
      (`grep -c "^  - req:" docs/status/requirement_status.v12.yaml` = 18
      on-disk entries) rather than carrying forward either side's
      independently-computed 17, per this note's own recount-every-time
      rule.
      Incremented to 17 by DOC-UPDATER's own REQ-299 done-event append
      (WF02-REQ299-20260910, Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 17 on-disk entries at that
      run's own pre-rebase base, no rebase conflict on this file at the
      time it was written. 973 lines / 60538 bytes at this point, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.
      Separately, and concurrently, recounted to 17 by this same run's
      earlier Step Final rebase attempt (first conflict on this file),
      which composed two independent bases-of-15 increments (below) with
      this branch's own Step 00 REQ-299 started-event (commit 3dd28614)
      and main's REQ-297 done-event (WF02-REQ297-20260909).
      Incremented to 16 by ORCH's own REQ-297 done-event append
      (WF02-REQ297-20260909) -- 981 lines at that point (60390 bytes),
      well under the roll ceiling, against a base of 15.
      Separately, and concurrently, incremented to 16 by ELIXIR-DEV during
      WF02-REQ299-20260910 Step 2a (re-attempt): A9 caught this branch's
      own Step 00 REQ-299 started-event append (commit 3dd28614) having
      landed without the matching index bump (declared stayed at 15 while
      the volume gained its 16th on-disk entry, from that branch's own
      perspective). Recounted directly from the file
      (`grep -c "^  - req:" docs/status/requirement_status.v12.yaml` = 16
      on-disk entries) rather than guessing, per this index's own
      established recount precedent. Unrelated to REQ-299's actual code
      change (lib/letflow/entities/query/allowlist.ex); fixed here only
      because it was the sole `mix letflow.check` failure blocking this
      step.
      Previously incremented to 15 by ORCH's own REQ-296 done-event append
      (WF02-REQ296-20260909) -- 927 lines at that point (57052 bytes),
      well under the roll ceiling.
      Previously recounted to 14 by WF02-REQ302-20260909's own rebase onto origin/main
      (docs/status/requirement_status.v12.yaml conflicted: this run's own
      REQ-302 done-event append collided with the concurrently-merged
      REQ-303..306 batch's own started event, PR #1176 merged as 9b443129).
      Resolved by composing both events in chronological `at:` order
      (REQ-302 done 19:55:43Z -> REQ-303 started 20:03:55Z) rather than
      discarding either side, then recounting directly from the composed
      file (`grep -c "^  - req:" docs/status/requirement_status.v12.yaml`
      = 14 on-disk entries) rather than carrying forward either side's
      number, per this index's own established composition precedent.
      Previously incremented to 13 by ORCH's own REQ-303 started-event append for the
      REQ-303..306 S10-gap-13 batch (pack format carrying entity definitions,
      PR #1176 merged as 9b443129) -- 822 lines at that point (51232 bytes),
      well under the roll ceiling.
      Previously incremented to 13 by ORCH's own REQ-302 done-event append
      (WF02-REQ302-20260909) -- 796 lines at that point (48790 bytes),
      well under the roll ceiling.
      Previously recounted to 12 by WF02-REQ295-20260909's own merge of origin/main
      (docs/status/requirement_status.v12.yaml conflicted: this run's own
      REQ-295 done-event append collided with a concurrently-merged
      REQ-289 started/done pair and the REQ-295..302 batch's own started
      event, PR #1166 merged as afe964bf). Resolved by composing all four
      events in chronological `at:` order (REQ-289 started 16:33:54Z ->
      REQ-295 done 17:26:12Z -> REQ-289 done 17:33:49Z -> REQ-295 started
      18:03:52Z) rather than discarding either side, then recounting
      directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 12 on-disk entries)
      rather than carrying forward either side's number, per this
      index's own established composition precedent.
      Previously incremented to 11 by ORCH's own REQ-295 started-event append for the
      REQ-295..302 entity-storage batch (decision 0023, PR #1166 merged as
      afe964bf) -- 670 lines at that point (41677 bytes), well under the
      roll ceiling.
      Previously incremented to 10 by WF02-REQ289-20260909 (DOC-UPDATER)'s own
      REQ-289 done-event append (Step 6, RELEASE-VALIDATOR PASS commit
      38b4e8d7) -- 598 lines at that point (37085 bytes), well under
      the roll ceiling, no rebase conflict on this file.
      Previously incremented to 9 by ORCH's own REQ-289 started-event append
      (WF02-REQ289-20260909) -- 548 lines at that point (33979 bytes),
      well under the roll ceiling.
      Previously incremented to 8 by ORCH's own REQ-283 done-event append
      (WF02-REQ283-20260909) -- 530 lines at that point (32481 bytes),
      well under the roll ceiling.
      Previously incremented to 7 by ORCH's own REQ-279 done-event append
      (WF02-REQ279-20260909) -- 484 lines at that point (29689 bytes),
      well under the roll ceiling.
      Previously recounted to 6 by this run's (WF02-REQ277-20260909, Step Final) own
      rebase, which composed REQ-277's done-event append (originally
      authored against volume 11 while it was still current, redirected
      here per the closed-volume rule) with REQ-291's independently-landed
      done-event append (WF02-REQ291-20260909) -- both sides had
      independently computed 5 against a shared pre-conflict base;
      recounted directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 6 on-disk entries) rather
      than carrying forward either side's number, per this index's own
      recount-every-time rule. 448 lines / 28068 bytes, well under the roll
      ceiling. This supersedes a duplicate independently-computed "5" note
      from a concurrent redispatch commit (origin/feature/WF02-REQ277-20260909,
      bookkeeping-only, made no content change to v12.yaml itself) merged
      into this branch afterward -- that note's own "5" was already stale
      against this recount at merge time; superseded rather than composed,
      since it describes the same underlying append this note already
      accounts for, not a distinct one.
      Previously recounted to 5 by this run's own earlier rebase conflict,
      which composed REQ-277's started-event append with REQ-291's
      done-event append (`grep -c "^  - req:"
      docs/status/requirement_status.v12.yaml` = 5 on-disk entries at that
      point). 412 lines / 25681 bytes at that point, well under the roll
      ceiling.
      Previously incremented to 3 by ORCH's own REQ-284 done-event append
      (WF02-REQ284-20260909) -- 331 lines at that point (20164 bytes),
      well under the roll ceiling.
      Previously incremented to 2 by ORCH's own REQ-286 done-event append
      (WF02-REQ286-20260909) -- 273 lines at that point (16511 bytes),
      well under the roll ceiling.
      Previously incremented to 1 by ORCH's own REQ-285 done-event append
      (WF02-REQ285-20260909) -- 221 lines at that point (13240 bytes),
      well under the roll ceiling.
      Opened by ORCH at feature/WF02-REQ282-20260909's Step Final, successor
      to volume 11. Created with volume 11's header text verbatim (volume
      number, opening date and predecessor path updated) and an empty
      `history:` list, per this index's `roll_rule.on_exceed` procedure.
      `entries:` will be incremented per HOW-TO-APPEND step 6 as real
      appends land.

  - volume: 13
    path: docs/status/requirement_status.v13.yaml
    status: closed
    opened: 2026-09-10
    closed: 2026-09-11
    entries: 30
    lines: 1231
    bytes_working_tree: 73235
    frozen_prefix_lines: 1201
    frozen_prefix_sha256: "d4d92201bf111e8b06456ed2f007eb5475f621f87dc7f62459dd5df1f192b577"
    note: >
      Closed 2026-09-11 by fix/ISS-0595-20260911's own Step Final rebase,
      which composed this branch's own ISS-0595 done-event append against
      main's concurrently-merged REQ-307 done-event, bringing this file to
      30 entries across 1201 lines / 71184 bytes -- over the 1200-line
      roll ceiling. Rolled to volume 14
      (docs/status/requirement_status.v14.yaml) per the standard
      procedure: closure footer appended to this file, frozen_prefix_sha256
      above computed via `sha256sum` on the working-tree file BEFORE the
      footer was appended (the frozen prefix excludes the footer), volume
      14 created with this volume's own header text verbatim (volume
      number/opening date/predecessor path updated) and an empty
      `history:` list.
      Recounted to 30 on ORCH's fix/ISS-0595-20260911 second rebase onto
      main, composing this branch's own already-resolved 29-entry state
      with main's own newly-landed REQ-307 done-event (declaring 27
      against a now-stale base) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 30 on-disk entries;
      1201 lines / 71184 bytes, well under the roll ceiling. Recounted
      directly from the composed file per this note's own
      recount-every-time rule.
      WF03-ISS0594-20260911's own Step Final rebase found this volume
      already independently closed at 30 entries by
      fix/ISS-0595-20260911 above -- since a closed volume never regains
      an entry once rolled, this run's own two SCOPE-CHANGE events
      (started 03:39:58Z, done 04:19:50Z) were composed into volume 14
      instead, per the established "one run's started/done land one in
      each volume across a roll boundary" pattern. This volume's frozen
      content (30 entries, 1201-line frozen prefix) is unchanged by that
      composition -- confirmed identical to the pre-rebase working-tree
      file.
      Recounted to 29 on WF02-REQ307-20260911's second merge of
      origin/main. This is the same concurrent-append collision as the
      previous recount, now three-way: this branch carried REQ-307's
      done-event (declaring 27 after composing ISS-0588's), while main
      carried ISS-0590's SCOPE-CHANGE done-event (declaring 28). Each
      figure was correct against the file its own side could see. All
      three events are present after composition and the real figure is
      29.

      Resolved by keeping every event in timestamp order -- 02:26:04Z,
      03:03:59Z, then REQ-307 at 03:22:38Z -- and recounting from the
      composed file rather than carrying forward any side's number,
      which is what this note's recount-every-time rule exists for. No
      event from either side is dropped;
      `grep -c "^  - req:" docs/status/requirement_status.v13.yaml` = 29
      on-disk entries; 1179 lines / 70080 bytes, still under the roll
      ceiling. The A9 invariant in test/docs/ verifies this figure and
      is re-run after resolution.

      Prior state, retained as history: 25 on WF02-REQ293-20260910's
      Step Final rebase onto main (deferral-reconciliation commit
      replay), composing that branch's own SCOPE-CHANGE done-event
      append (REQ-294 registered as queue task 585, GH#1205,
      locked+released as status: "blocked") with
      Recounted to 29 on ORCH's fix/ISS-0595-20260911 Step Final rebase
      onto main, composing this branch's own ISS-0595 SCOPE-CHANGE
      done-event (independently computed as 27, against a stale base of
      26) with main's own ISS-0590 started+done event pair
      (independently computed as 28) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 29 on-disk entries;
      1171 lines / 69383 bytes, well under the roll ceiling. Recounted
      directly from the composed file rather than carrying forward
      either side's independently-computed 27/28, per this note's own
      recount-every-time rule.
      Recounted to 28 on WF03-ISS0590-20260911's own Step Final rebase
      onto main (Step 5 commit replay), composing this branch's own
      ISS-0590 SCOPE-CHANGE done-event append (Step 5, ISSUE-FIXER) with
      main's own ISS-0588 SCOPE-CHANGE done-event, already folded into
      the on-disk file by the prior conflict resolution above -- `grep -c
      "^  - req:" docs/status/requirement_status.v13.yaml` = 28 on-disk
      entries; 1150 lines / 69430 bytes, well under the roll ceiling.
      Recounted directly from the composed file rather than carrying
      forward either side's independently-computed count, per this
      note's own recount-every-time rule.
      Recounted to 27 on WF03-ISS0590-20260911's own Step Final rebase
      onto main (Step 00 commit replay), composing this branch's own
      ISS-0590 started-event append with main's own ISS-0588 SCOPE-CHANGE
      done-event, no conflict on requirement_status.v13.yaml itself
      (auto-merged cleanly) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 27 on-disk entries; 1119
      lines / 67558 bytes, well under the roll ceiling. Recounted directly
      from the composed file rather than carrying forward either side's
      independently-computed count, per this note's own recount-every-time
      rule.
      Incremented to 27 by ORCH's own ISS-0595 SCOPE-CHANGE done-event
      append (fix/ISS-0595-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 27 on-disk entries;
      1122 lines, well under the roll ceiling.
      Recounted to 26 on ORCH's fix/ISS-0588-20260911 Step Final rebase
      onto main, appending this branch's own ISS-0588 SCOPE-CHANGE
      done-event on top of main's already-composed 25 entries
      (requirement_status.v13.yaml itself auto-merged cleanly by git,
      no conflict) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 26 on-disk entries;
      1102 lines / 65600 bytes, well under the roll ceiling. Recounted
      directly from the composed file rather than carrying forward
      either side's independently-computed count, per this note's own
      recount-every-time rule.
      Recounted to 25 on WF02-REQ293-20260910's own Step Final rebase onto
      main (deferral-reconciliation commit replay), composing this
      branch's own SCOPE-CHANGE done-event append (REQ-294 registered as
      queue task 585, GH#1205, locked+released as status: "blocked") with
      main's own ISS-0583/ISS-0589 SCOPE-CHANGE done-events, no conflict
      on requirement_status.v13.yaml itself (auto-merged cleanly) --
      1084 lines / 65761 bytes at that point.
      Recounted to 24 on WF02-REQ293-20260910's own Step Final rebase onto
      main (Step 6 commit replay), composing this branch's own REQ-293
      done-event append (Step 6) with main's own ISS-0583/ISS-0589
      SCOPE-CHANGE done-events, already folded into the on-disk file by
      the prior conflict resolution, with no conflict on
      requirement_status.v13.yaml itself (auto-merged cleanly) -- `grep -c
      "^  - req:" docs/status/requirement_status.v13.yaml` = 24 on-disk
      entries; 1061 lines / 64482 bytes, well under the roll ceiling.
      Recounted directly from the composed file rather than carrying
      forward either side's independently-computed count, per this note's
      own recount-every-time rule.
      Recounted to 23 on WF02-REQ293-20260910's own Step Final rebase onto
      main (Step 00 commit replay), composing this branch's own REQ-293
      started-event append with main's own ISS-0583 and ISS-0589
      SCOPE-CHANGE done-event appends (fix/ISS-0583-20260911,
      fix/ISS-0589-20260911), all landing on requirement_status.v13.yaml
      with no conflict on that file itself (auto-merged cleanly by git) --
      `grep -c "^  - req:" docs/status/requirement_status.v13.yaml` = 23
      on-disk entries; 976 lines / 58801 bytes, well under the roll
      ceiling. Recounted directly from the composed file rather than
      carrying forward either side's independently-computed count, per
      this note's own recount-every-time rule.
      Incremented to 20 by ORCH's own SCOPE-CHANGE done-event append
      (reopening queue task 584, GH#1202, left stuck in status: blocked
      by a sibling session's own race, via set_lock + release with
      explicit status: "open") -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 20 on-disk entries; 902
      lines / 54444 bytes, well under the roll ceiling, so no roll
      triggered.
      Recounted to 19 on WF02-REQ292-20260910's own Step Final rebase onto
      main (deferral-reconciliation commit replay), composing this
      branch's own SCOPE-CHANGE done-event append (REQ-293 registered as
      queue task 584, GH#1202; REQ-294 re-scoped to blocked-by: REQ-293)
      with main's own REQ-293 blocked-event append (filed by a sibling
      session that raced this reconciliation), chronologically
      interleaved into docs/status/requirement_status.v13.yaml (REQ-292
      done 22:23:15Z, SCOPE-CHANGE reconciliation done 22:29:06Z, REQ-293
      blocked 22:50:22Z, sorting last, no reordering needed). Recounted
      directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 19 on-disk entries; 881
      lines / 53265 bytes, well under the roll ceiling) rather than
      carrying forward either side's independently-computed count, per
      this note's own recount-every-time rule.
      Recounted to 17 on WF02-REQ292-20260910's own Step Final rebase onto
      main (Step 6 commit replay), composing this branch's own REQ-292
      done-event append with main's own REQ-293 blocked-event append
      (chore/req293-block-20260910, filed by a sibling session before
      REQ-292's own merge landed), chronologically interleaved into
      docs/status/requirement_status.v13.yaml (REQ-292 done 22:23:15Z,
      REQ-293 blocked 22:50:22Z, sorting last, no reordering needed).
      Recounted directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 18 on-disk entries; 863
      lines / 52287 bytes, well under the roll ceiling) rather than
      carrying forward either side's independently-computed count, per
      this note's own recount-every-time rule.
      Recounted to 17 on WF02-REQ292-20260910's own Step Final rebase onto
      main (Step 00 commit replay), composing this branch's own REQ-292
      started-event append with main's own REQ-293 blocked-event append
      (chore/req293-block-20260910), chronologically interleaved into
      docs/status/requirement_status.v13.yaml (ISS-0583 blocked 21:54:38Z,
      REQ-292 started 20:06:37Z, REQ-293 blocked 22:50:22Z sorting last,
      no reordering needed). Recounted directly from the composed file
      (`grep -c "^  - req:" docs/status/requirement_status.v13.yaml` = 17
      on-disk entries; 783 lines / 47129 bytes, well under the roll
      ceiling) rather than carrying forward either side's
      independently-computed count, per this note's own recount-every-time
      rule.
      Incremented to 15 by ORCH's own ISS-0583 SCOPE-CHANGE blocked-event
      append (chore/iss0583-block-20260910) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 15 on-disk entries; 742
      lines, well under the roll ceiling.
      Recounted to 14 on ORCH's fix/ISS-0582-20260910 Step Final rebase
      onto main, composing this branch's own ISS-0582 SCOPE-CHANGE
      done-event (independently computed as 12, against a stale base of
      11) with main's own REQ-306 started+done event pair (independently
      computed as 13), chronologically interleaved into
      docs/status/requirement_status.v13.yaml (REQ-306 started 18:53:41Z,
      REQ-306 done 19:50:47Z, ISS-0582 done 19:55:18Z). This rebase also
      hit a real content conflict (not just the usual append-only
      status-file kind) in test/letflow/definitions/solution_pack_test.exs
      against REQ-306's own coverage, resolved by fixing a test fixture
      (see the ISS-0582 done-event's own note above for detail); all 17
      tests in the composed file pass. Recounted directly from the
      composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 14 on-disk entries; 718
      lines / 42749 bytes, well under the roll ceiling) rather than
      carrying forward either side's independently-computed 12/13, per
      this note's own recount-every-time rule.
      Recounted to 13 on WF02-REQ306-20260910's own Step Final rebase onto
      main, composing this branch's own REQ-306 done-event append
      (Step 6, DOC-UPDATER, independently computed as 12 against a stale
      base of 11) with main's own ISS-0580 SCOPE-CHANGE done-event, already
      folded into the on-disk file by the prior conflict resolution above
      -- `grep -c "^  - req:" docs/status/requirement_status.v13.yaml` = 13
      on-disk entries; 666 lines / 40055 bytes, well under the roll
      ceiling, so no roll triggered. Recounted directly from the composed
      file rather than carrying forward either side's independently-
      computed 12, per this note's own recount-every-time rule.
      Recounted to 12 on WF02-REQ306-20260910's own Step Final rebase onto
      main, composing this branch's own REQ-306 started-event append
      (independently computed as 11, against a stale
      base of 10) with main's own ISS-0580 SCOPE-CHANGE done-event
      (independently computed as 11 there too, from a shared base of 10),
      chronologically interleaved into
      docs/status/requirement_status.v13.yaml (ISS-0580 done 18:42:49Z,
      REQ-306 started 18:53:41Z, sorting last, no reordering needed).
      Recounted directly from the composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 12 on-disk entries; 611
      lines / 36521 bytes, well under the roll ceiling) rather than
      carrying forward either side's independently-computed 11, per this
      note's own recount-every-time rule.
      Incremented to 12 by ORCH's own ISS-0582 SCOPE-CHANGE done-event
      append (fix/ISS-0582-20260910) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 12 on-disk entries; 633
      lines, well under the roll ceiling.
      Recounted to 11 on ORCH's fix/ISS-0580-20260910 rebase onto main,
      composing this branch's own ISS-0580 SCOPE-CHANGE done-event
      (independently computed as 9, against a stale base of 8) with main's
      REQ-305 started+done event pair (independently computed as 10),
      chronologically interleaved into docs/status/requirement_status.v13.yaml
      (REQ-305 started 17:26:51Z, REQ-305 done 18:38:37Z, ISS-0580 done
      18:42:49Z). Recounted directly from the composed file (`grep -c
      "^  - req:" docs/status/requirement_status.v13.yaml` = 11 on-disk
      entries; 595 lines / 35086 bytes, well under the roll ceiling) rather
      than carrying forward either side's independently-computed 9/10, per
      this note's own recount-every-time rule.
      Incremented to 10 by WF02-REQ305-20260910 (DOC-UPDATER)'s own REQ-305
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 10 on-disk entries; 558
      lines / 33202 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.
      Incremented to 9 by ORCH's own REQ-305 started-event append (Step 00,
      WF02-REQ305-20260910) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 9 on-disk entries; 480
      lines / 28150 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.
      Recounted to 9 on ORCH's fix/ISS-0580-20260910 rebase onto main,
      appending this branch's own ISS-0580 SCOPE-CHANGE done-event on top of
      main's already-composed 8 entries (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 9 on-disk entries).
      Recounted to 8 on WF03-ISS0581-20260910's own Step Final rebase onto
      main, composing this branch's own SCOPE-CHANGE started+done event
      pair for ISS-0581 with main's own ISS-0578 SCOPE-CHANGE done-event,
      chronologically interleaved into
      docs/status/requirement_status.v13.yaml (ISS-0578 done 14:29:44Z,
      ISS-0581 started 15:02:52Z, ISS-0581 done -- Step 5 close-out --
      both this run's own entries sorting after ISS-0578's with no
      reordering needed). Recounted directly from the composed file
      (`grep -c "^  - req:" docs/status/requirement_status.v13.yaml` = 8
      on-disk entries; 460 lines / 27145 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered) rather
      than carrying forward either side's independently-computed count,
      per this note's own recount-every-time rule.
      Recounted to 6 on ORCH's fix/ISS-0578-20260910 rebase onto main,
      composing this branch's own ISS-0578 SCOPE-CHANGE done-event append
      (independently computed as 4, against a stale base of 3) with main's
      REQ-304 started+done event pair (independently computed as 5),
      chronologically interleaved into docs/status/requirement_status.v13.yaml
      (REQ-304 started 13:01:54Z, ISS-0578 done 14:29:44Z, REQ-304 done
      14:49:53Z). Recounted directly from the composed file (`grep -c
      "^  - req:" docs/status/requirement_status.v13.yaml` = 6 on-disk
      entries) rather than carrying forward either side's
      independently-computed 4/5, per this note's own recount-every-time
      rule.
      Incremented to 5 by WF02-REQ304-20260910 (DOC-UPDATER)'s own REQ-304
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 5 on-disk entries, no
      rebase conflict on this file at the time it was written.
      Incremented to 4 by ORCH's own REQ-304 started-event append (Step 00,
      WF02-REQ304-20260910) -- `grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 4 on-disk entries. The
      Step 00 commit (0d8121af) appended the entry to v13.yaml without
      also bumping this index count in the same commit, caught by
      ELIXIR-DEV during this run's own Step 2a full-suite run (A9
      assertion). Fixed here rather than left for a later reconciliation,
      per this file's own recount-every-time rule.
      Recounted to 4 (was 3) after ISS-0578's SCOPE-CHANGE done event
      (fix/ISS-0578-20260910). Recounted to 3 by WF02-REQ290-20260910's
      Step Final rebase, which conflicted at this file's tail composing
      this branch's own REQ-290
      started/done event pair (against a stale base of 2) with main's
      REQ-303 done-event append (independently computed as 1), both
      already composed into docs/status/requirement_status.v13.yaml in
      chronological `at:` order (REQ-290 started 09:57:51Z, REQ-290 done
      11:50:53Z, REQ-303 done 11:56:54Z). Recounted directly from the
      composed file (`grep -c "^  - req:"
      docs/status/requirement_status.v13.yaml` = 3 on-disk entries)
      rather than carrying forward either side's independently-computed
      1/2, per this note's own recount-every-time rule.
      Incremented to 1 by ORCH's own REQ-303 done-event append
      (WF02-REQ303-20260910) -- 234 lines at that point (13704 bytes),
      well under the roll ceiling.
      Previously opened by ORCH at feature/WF02-REQ300-20260910's Step Final, successor
      to volume 12. Created with volume 12's header text verbatim (volume
      number, opening date and predecessor path updated) and an empty
      `history:` list, per this index's `roll_rule.on_exceed` procedure.
      Incremented to 2 by WF02-REQ290-20260910's own Step Final rebase,
      which composed this branch's REQ-290 started- and done-event pair
      into this volume rather than the now-closed volume 12 -- see volume
      12's own note above for the full reasoning. Both events' own `at:`
      timestamps (09:57:51Z, 11:50:53Z) predate this volume's `opened:`
      date only nominally; they are appended here, not reordered into
      volume 12, per the same "one run's started/done land one in each
      volume across a roll boundary" precedent.

  - volume: 14
    path: docs/status/requirement_status.v14.yaml
    status: closed
    opened: 2026-09-11
    closed: 2026-09-12
    entries: 34
    lines: 1240
    bytes_working_tree: 72197
    frozen_prefix_lines: 1213
    frozen_prefix_sha256: "c322b1c39988d54d942574dd40841bd71d7ce05c9424f6d9ca5a28c180e0e622"
    note: >
      Closed by ORCH at feature/WF02-REQ322-20260912's Step Final: REQ-322's
      own done-event append (34th entry) brought the file to 1213
      frozen-prefix lines -- over the 1,200-line roll ceiling. Closure
      footer appended per the roll rule; `frozen_prefix_sha256` computed via
      `sha256sum` BEFORE the footer was appended, over exactly the 1213
      frozen-prefix lines. Successor: docs/status/requirement_status.v15.yaml,
      created with this volume's own header text verbatim (volume number,
      opening date and predecessor path updated only -- diffed against this
      volume's original header to confirm).
      Incremented to 33 by WF02-REQ320-20260912 (DOC-UPDATER)'s own REQ-320
      close-out done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 33 on-disk entries, no
      rebase conflict on this file. 1160 lines / 68234 bytes, still under
      the 1,200-line / 120,000-byte roll ceiling.
      Recounted to 32 by WF02-REQ320-20260912's Step Final SECOND rebase,
      which conflicted at this file's tail between this run's own
      already-composed note (previous recount: 31, composing REQ-320
      started + REQ-319 done) and WF02-REQ321-20260912 (ORCH)'s own
      REQ-321 done-event append (independently computed as 31, before
      either side saw the other). docs/status/requirement_status.v14.yaml
      itself auto-merged cleanly with no conflict, appending REQ-321's
      done-event after this branch's already-composed REQ-320
      started/REQ-319 done entries with no reordering needed -- only this
      index file's declared count and narrative needed manual
      composition. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 32 on-disk entries,
      rather than carrying forward either side's independently-computed
      31, per this note's own recount-every-time rule. 1125 lines --
      still under the 1,200-line roll ceiling but close; the NEXT append
      to this volume should check whether a roll is warranted before
      appending further.
      Incremented to 31 by ORCH's own REQ-321 done-event append
      (WF02-REQ321-20260912) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 31 on-disk entries;
      1109 lines / 64185 bytes. ⛔ CLOSE TO THE 1,200-LINE ROLL CEILING
      -- the NEXT agent appending to this volume MUST check line/byte
      count first and roll to v15 before appending if either bound
      would be exceeded, per the roll procedure this file's own header
      documents (see v13's own roll to v14 for the exact steps).
      Recounted to 31 by WF02-REQ320-20260912's Step Final rebase, which
      conflicted at this file's tail between this run's own REQ-320
      started-event append (independently computed as 30 against a
      stale pre-REQ-319 base) and WF02-REQ319-20260911's own Step Final
      SECOND-rebase note (also 30, before either side saw the other).
      docs/status/requirement_status.v14.yaml's own tail required
      manual composition -- REQ-320 started (00:48:45Z) sorts before
      REQ-319 done (00:51:40Z), so REQ-320's own started-event entry was
      reordered ahead of REQ-319's done-event entry rather than appended
      after it. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 31 on-disk entries,
      rather than carrying forward either side's independently-computed
      30, per this note's own recount-every-time rule. 1071 lines --
      still under the 1,200-line roll ceiling but close; the NEXT append
      to this volume should check whether a roll is warranted before
      appending further.
      Recounted to 30 on WF02-REQ319-20260911's Step Final SECOND
      rebase, which conflicted at this file's tail between this run's
      own already-composed note (previous recount: 29) and
      WF02-REQ317-20260912's own close-out note (also independently
      computed as 29, before either side saw the other).
      docs/status/requirement_status.v14.yaml's own tail required
      manual composition (REQ-317's close-out done-event, already on
      main, sorted before REQ-319's done-event by simple concatenation
      -- REQ-317 done at 00:30:36Z predates REQ-319 done at
      00:51:40Z). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 30 on-disk entries,
      rather than carrying forward either side's independently-computed
      29, per this note's own recount-every-time rule. 1055 lines /
      60922 bytes -- still under the 1,200-line roll ceiling but close;
      the NEXT append to this volume should check whether a roll is
      warranted before appending further.
      Incremented to 29 by DOC-UPDATER's own REQ-317 done-event append
      (Step 6, WF02-REQ317-20260912 close-out) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 29 on-disk entries, no
      rebase conflict on this file.
      Incremented to 28 by ORCH's own REQ-317 started-event append
      (Step 00, WF02-REQ317-20260912)'s Step Final rebase, which found
      main advanced past WF02-REQ318-20260911's Step Final SECOND rebase
      (below) in the interim. REQ-317's started-event (22:35:37Z) sorts
      after both REQ-318's done-event (22:14:49Z) and REQ-315's close-out
      done-event (22:16:36Z) already present in
      docs/status/requirement_status.v14.yaml, so it appended cleanly at
      the file's tail with no reordering needed -- no conflict on that
      file itself, only this index file's count and narrative needed
      manual composition. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 28 on-disk entries,
      rather than carrying forward either side's independently-computed
      26/27, per this note's own recount-every-time rule. 966 lines /
      56513 bytes, well under the roll ceiling.
      Recounted to 27 on WF02-REQ318-20260911's Step Final SECOND rebase
      (main moved again during this PR's CI run, landing PR #1278 --
      WF02-REQ315-20260911's own close-out commit -- in the meantime).
      Conflicted at this file's tail between this run's own already-
      composed note (previous recount: 26) and PR #1278's close-out note
      (also claiming 26, computed independently before either side saw
      the other). docs/status/requirement_status.v14.yaml's own tail
      required manual composition (both sides inserted at the same
      point relative to the file's prior tail) -- REQ-318's done-event
      placed before REQ-315's close-out done-event, preserving each
      side's own text verbatim. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v14.yaml` = 27 on-disk
      entries, rather than carrying forward either side's independently-
      computed 26, per this note's own recount-every-time rule. 951
      lines / 54882 bytes, well under the roll ceiling.
      Incremented to 26 by this close-out run's own REQ-315 done-event
      append (WF02-REQ315-20260911 close-out, DOC-UPDATER) -- `grep -c
      "^  - req:" docs/status/requirement_status.v14.yaml` = 26 on-disk
      entries, no rebase conflict on this file. This closes out REQ-315
      (its own started-event, entry 25 below, had already landed without
      a matching done-event before the requirement's implementation was
      merged).
      Recounted to 25 on WF02-REQ315-20260911's Step Final rebase, which
      conflicted at this file's tail and at
      docs/status/requirement_status.v14.yaml's tail against
      WF03-ISS0613-20260911's SCOPE-CHANGE done-event and
      WF02-REQ316-20260911's own done-event (both landed on main after
      this run's own Step 00 started-event append). Composed in
      chronological `at:` order -- REQ-315 started (18:21:41Z), ISS-0613
      SCOPE-CHANGE done (18:40:34Z), REQ-316 done (20:22:14Z) -- REQ-315's
      own started-event sorted first and needed no reordering; the other
      two appended after it in their existing order. Recounted directly
      from `grep -c "^  - req:" docs/status/requirement_status.v14.yaml`
      = 25 on-disk entries, rather than carrying forward either side's
      independently-computed 23/24, per this note's own recount-every-time
      rule. 878 lines / 51458 bytes, well under the roll ceiling.
      Incremented to 24 by ORCH's own REQ-316 done-event append
      (WF02-REQ316-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 24 on-disk entries;
      864 lines / 49881 bytes, well under the roll ceiling.
      Incremented to 23 by ORCH's own ISS-0613 SCOPE-CHANGE done-event
      append (fix/ISS-0613-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 23 on-disk entries;
      805 lines, well under the roll ceiling.
      Incremented to 22 by ORCH's own WF01-S10gaps-2-3-12-impl-20260911
      SCOPE-CHANGE done-event append (REQ-315..320 filed and
      registered) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 22 on-disk entries;
      767 lines / 44840 bytes, well under the roll ceiling.
      Incremented to 21 on ORCH's fix/WF02-REQ314-20260911 Step Final
      rebase onto main, appending this branch's own REQ-314 done-event
      on top of main's already-composed 20 entries (REQ-313's
      done-event, REQ-312's done-event, and REQ-313's started-event,
      all already folded in by a prior rebase) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 21 on-disk entries;
      738 lines / 42455 bytes, well under the roll ceiling. Recounted
      directly from the composed file rather than carrying forward
      either side's independently-computed count, per this note's own
      recount-every-time rule.
      Incremented to 20 composing, at this branch's own rebase onto
      main, DOC-UPDATER's REQ-313 done-event append
      (WF02-REQ313-20260911) with REQ-312's done-event append
      (feature/WF02-REQ312-20260911, a concurrent sibling run) and
      ORCH's own REQ-313 started-event append (Step 00), per the
      established "one run's started/done land one in each volume,
      chronological by `at:`" pattern -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 20 on-disk entries;
      694 lines / 40581 bytes, well under the roll ceiling.
      Incremented to 19 by ORCH's own REQ-314 done-event append
      (feature/WF02-REQ314-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 19 on-disk entries;
      668 lines, well under the roll ceiling.
      Incremented to 18 by ORCH's own REQ-312 done-event append
      (feature/WF02-REQ312-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 18 on-disk entries;
      625 lines, well under the roll ceiling.
      Incremented to 17 by ORCH's own WF01-S10gaps-2-3-12-20260911
      SCOPE-CHANGE done-event append (REQ-312..314 filed and
      registered) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 17 on-disk entries;
      588 lines / 34228 bytes, well under the roll ceiling.
      Incremented to 16 by ORCH's own ISS-0601 SCOPE-CHANGE done-event
      append (fix/ISS-0601-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 16 on-disk entries;
      567 lines, well under the roll ceiling.
      Incremented to 13 by ORCH's own ISS-0600 SCOPE-CHANGE done-event
      append (fix/ISS-0600-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 13 on-disk entries;
      489 lines, well under the roll ceiling.
      Incremented to 9 by ORCH's own ISS-0599 SCOPE-CHANGE done-event
      append (chore/iss0599-queue-race-mitigation-20260911) -- `grep -c
      "^  - req:" docs/status/requirement_status.v14.yaml` = 9 on-disk
      entries; 400 lines, well under the roll ceiling.
      Incremented to 8 by ORCH's own ISS-0593 SCOPE-CHANGE done-event
      append (chore/iss0593-branch-protection-20260911) -- `grep -c
      "^  - req:" docs/status/requirement_status.v14.yaml` = 8 on-disk
      entries; 377 lines, well under the roll ceiling.
      Incremented to 7 by DOC-UPDATER's own REQ-308 done-event append
      (Step 6, WF02-REQ308-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 7 on-disk entries; 352
      lines / 20306 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.
      Incremented to 6 by ORCH's own Step Final done-event (PR #1229 merged,
      commit e6b97ab5, queue tasks 591/592 released done) plus REQ-308
      started-event append (Step 00, WF02-REQ308-20260911) --
      `grep -c "^  - req:" docs/status/requirement_status.v14.yaml` = 6
      on-disk entries; 314 lines / 18027 bytes, well under the
      1200-line/120000-byte roll ceiling, no roll triggered.
      Incremented to 4 by ISSUE-FIXER's own SCOPE-CHANGE done-event append
      (Step 5, WF03-ISS0591-ISS0592-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 4 on-disk entries, well
      under the 1200-line/120000-byte roll ceiling, no roll triggered.
      Incremented to 3 by ORCH's own SCOPE-CHANGE started-event append
      (Step 00, WF03-ISS0591-ISS0592-20260911) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 3 on-disk entries; 251
      lines / 14616 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.
      Incremented to 2 by ORCH's own WF03-ISS0594-20260911 started+done
      SCOPE-CHANGE event pair (composed here at this branch's own Step
      Final rebase, since volume 13 was independently closed by
      fix/ISS-0595-20260911 before this run's own rebase, per the
      established "one run's started/done land one in each volume across
      a roll boundary" pattern) -- `grep -c "^  - req:"
      docs/status/requirement_status.v14.yaml` = 2 on-disk entries; 225
      lines / 13086 bytes, well under the roll ceiling.
      Opened by ORCH at fix/ISS-0595-20260911's Step Final rebase,
      successor to volume 13. Created with volume 13's header text
      verbatim (volume number, opening date and predecessor path
      updated) and an empty `history:` list, per this index's
      `roll_rule.on_exceed` procedure. `entries:` will be incremented
      per HOW-TO-APPEND step 6 as real appends land.

  - volume: 15
    path: docs/status/requirement_status.v15.yaml
    status: closed
    opened: 2026-09-12
    closed: 2026-09-13
    entries: 32
    lines: 1278
    bytes_working_tree: 74715
    frozen_prefix_lines: 1245
    frozen_prefix_sha256: "23c8df1211f1f694feab21962eae389b79a2e1b09eef200f9ba55c7a18c6812d"
    note: >
      Closed by ORCH at issue/ISS-0631-20260913's own ISS-0631 done-event
      append (32nd entry, SCOPE-CHANGE), which brought the file to 1245
      frozen-prefix lines -- over the 1,200-line roll ceiling. Closure footer
      appended per the roll rule; `frozen_prefix_sha256` computed via
      `sha256sum` BEFORE the footer was appended, over exactly the 1245
      frozen-prefix lines. Successor: docs/status/requirement_status.v16.yaml.
      This append landed while resolving a real merge conflict against
      origin/main (REQ-333/REQ-334 had already merged and appended to this
      same file's tail): resolved by composing both branches' entries in
      `at:` timestamp order with no reordering needed (SCOPE-CHANGE started
      09:29:24Z precedes REQ-333 done 09:51:21Z), then appending the
      ISS-0631 done-event on top of the composed tail, which is what pushed
      the file over the ceiling. Recount verified directly from `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 32 rather than
      carrying forward either branch's independently-computed count, per
      this note's own recount-every-time rule.
      Incremented to 27 by ORCH's own ISS-0631 started-event append
      (SCOPE-CHANGE, queue task 631) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 27 on-disk entries;
      1048 lines / 61247 bytes -- approaching but still under the roll
      ceiling (1200 lines / 120000 bytes). No rebase conflict on this
      file.
      Incremented to 30 by ORCH's own REQ-334 done-event append (Step
      Final, WF02-REQ334-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 30 on-disk entries;
      1180 lines / 67692 bytes -- close to the 1200-line roll ceiling,
      the NEXT append to this volume will very likely need to roll it.
      No rebase conflict on this file.
      Incremented to 29 by ORCH's own REQ-334 started-event append
      (Step 00, WF02-REQ334-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 29 on-disk entries;
      1139 lines / 65308 bytes -- approaching but still under the roll
      ceiling (1200 lines / 120000 bytes). No rebase conflict on this
      file.
      Recounted to 28 at feature/WF02-REQ333-20260913's second (Step
      Final close-out) rebase, which conflicted at this file's tail
      between main's own already-composed 27-count note (from the
      first conflict, above) and this branch's own REQ-333 done-event
      append (independently computed as 27 against a base predating
      REQ-334's blocked-event). Both appends land at the same v15.yaml
      tail position with no overlapping lines -- composed order there
      is REQ-334 blocked (08:29:39Z) then REQ-333 done (09:51:21Z),
      already correct by `at:` timestamp with no reordering needed --
      so only this index file's declared count and narrative needed
      manual composition. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 28 rather
      than carrying forward either side's independently-computed 27,
      per this note's own recount-every-time rule. 1112 lines / 63758
      bytes, well under the roll ceiling.
      Recounted to 27 at feature/WF02-REQ333-20260913's first Step
      Final rebase, which conflicted at this file's tail between
      main's own REQ-334 blocked-event append (independently computed
      as 26, appended after REQ-333's own started-event which already
      sat unmoved in v15.yaml) and this branch's own REQ-333
      started-event append (also independently computed as 26, from a
      base predating REQ-334's blocked-event). Both appends target the
      same v15.yaml tail position with no overlapping lines -- the
      composed order there is REQ-333 started (08:00:09Z) then REQ-334
      blocked (08:29:39Z), already correct by `at:` timestamp with no
      reordering needed -- so only this index file's declared count
      and narrative needed manual composition. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v15.yaml` =
      27 rather than carrying forward either side's
      independently-computed 26, per this note's own
      recount-every-time rule. 1049 lines / 60262 bytes, well under
      the roll ceiling.
      Incremented to 26 by ORCH's own REQ-334 blocked-event append
      (direct-action, dependency REQ-333 not yet done) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 26
      on-disk entries; 1024 lines / 59874 bytes -- approaching but
      still under the roll ceiling (1200 lines / 120000 bytes). No
      rebase conflict on this file.
      Incremented to 27 by ORCH's own REQ-333 done-event append (Step
      Final, WF02-REQ333-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 27 on-disk entries;
      1093 lines / 62739 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 26 by ORCH's own REQ-333 started-event append
      (Step 00, WF02-REQ333-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 26 on-disk entries;
      1030 lines / well under the roll ceiling. No rebase conflict on
      this file.
      Recounted to 25 at feature/WF02-REQ332-20260913's Step Final
      SECOND rebase, which conflicted at this file's tail between
      main's own already-composed 24-count note (from the first
      conflict, above) and this branch's own REQ-332 done-event append
      (independently computed as 22 against a base that predated the
      REQ-333 blocked-event). Both appends land at the SAME tail
      position in v15.yaml with no overlapping lines -- the composed
      resolution there is REQ-331 done, REQ-333 blocked, REQ-332 done,
      in that chronological `at:` order (06:46:53Z, 06:59:26Z,
      07:02:32Z) -- so only this index file's declared count and
      narrative needed manual composition. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v15.yaml` =
      25 rather than carrying forward either side's
      independently-computed 22/24, per this note's own
      recount-every-time rule. 1005 lines / 57831 bytes, well under
      the roll ceiling.
      Recounted to 24 at feature/WF02-REQ332-20260913's Step Final
      rebase, which conflicted at this file's tail between main's own
      REQ-331 done-event/REQ-333 blocked-event pair (independently
      computed as 23) and this branch's own REQ-332 started-event
      append (independently computed as 21 against a stale
      pre-REQ-331-done base). Composed in chronological `at:` order --
      REQ-332 started (2026-09-13T05:50:35Z) sorts between REQ-331's
      own started (05:39:39Z) and done (06:46:53Z) events, so it was
      reordered into that position rather than appended after both of
      main's entries; REQ-333's blocked-event (06:59:26Z) sorts last
      and needed no reordering. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 24 rather
      than carrying forward either side's independently-computed
      21/23, per this note's own recount-every-time rule. 944 lines /
      54188 bytes, well under the roll ceiling.
      Incremented to 23 by ORCH's own REQ-333 blocked-event append
      (direct-action, dependency REQ-332 not yet done) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 23
      on-disk entries; 925 lines / 54090 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Incremented to 22 by DOC-UPDATER's own REQ-331 done-event append
      (close-out for WF02-REQ331-20260913) --
      `grep -c "^  - req:" docs/status/requirement_status.v15.yaml` =
      22 on-disk entries; 900 lines / 52588 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Incremented to 22 by ORCH's own REQ-332 done-event append (Step
      Final, WF02-REQ332-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 22 on-disk entries;
      907 lines / 52325 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 21 by ORCH's own REQ-332 started-event append
      (Step 00, WF02-REQ332-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 21 on-disk entries;
      846 lines / well under the roll ceiling. No rebase conflict on
      this file.
      Recounted to 20 at feature/WF02-REQ330-20260913's Step Final
      SECOND rebase, which conflicted at this file's tail between
      main's own ORCH-direct REQ-331 blocked-event append (independently
      computed as 19) and this branch's own already-composed REQ-329/
      REQ-330 recount note (also independently computed as 19, from a
      base that predated the REQ-331 blocked-event). Both appends land
      on the SAME on-disk v15.yaml tail with no overlapping lines (the
      REQ-331 blocked-event is simply the next entry after REQ-329
      done), so no `at:`-order reordering was needed -- only this index
      file's declared count and narrative required manual composition.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 20 rather than
      carrying forward either side's independently-computed 19, per
      this note's own recount-every-time rule. 827 lines / 47659 bytes,
      well under the roll ceiling.
      Incremented to 19 by ORCH's own REQ-331 blocked-event append
      (direct-action, dependency REQ-330 not yet done) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 19
      on-disk entries; 777 lines / 45429 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Recounted to 19 at feature/WF02-REQ330-20260913's Step Final
      rebase, which conflicted at this file's tail between main's own
      REQ-329 started+done event pair (landed via WF02-REQ329-20260913,
      independently computed as 18) and this branch's own REQ-330
      done-event append (independently computed as 17 against a stale
      pre-REQ-329 base). Composed in chronological `at:` order --
      REQ-329 started (2026-09-13T03:42:20Z), REQ-330 done
      (2026-09-13T04:23:39Z), REQ-329 done (2026-09-13T04:58:22Z) --
      REQ-330's done-event was reordered between REQ-329's two events
      rather than appended after both. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 19 rather
      than carrying forward either side's independently-computed 17/18,
      per this note's own recount-every-time rule. 809 lines / 46674
      bytes, well under the roll ceiling.
      Incremented to 18 by DOC-UPDATER's own REQ-329 close-out
      done-event append (Step 6, WF02-REQ329-20260913) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 18
      on-disk entries; 759 lines / 44426 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Incremented to 17 by ORCH's own REQ-329 started-event append --
      `grep -c "^  - req:" docs/status/requirement_status.v15.yaml` =
      17 on-disk entries; 716 lines / 41982 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Incremented to 17 by ORCH's own REQ-330 done-event append
      (WF02-REQ330-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 17 on-disk entries;
      744 lines, well under the roll ceiling.
      Incremented to 16 by ORCH's own REQ-328 final done-event append
      (docs/close-REQ328-20260913, correcting ORCH's own earlier
      oversight in the prior ISS-0624 entry's status-field claim) --
      `grep -c "^  - req:" docs/status/requirement_status.v15.yaml` =
      16 on-disk entries; 694 lines, well under the roll ceiling.
      Recounted to 15 on WF03-ISS0624-20260912's Step Final SECOND
      rebase, which conflicted at this file's tail between this run's
      own already-composed note (previous recount: 14) and its own
      later REQ-328 addendum note (independently computed as 13 before
      the two saw each other, since both were composed from the SAME
      run in sequence). docs/status/requirement_status.v15.yaml's own
      tail required manual composition -- ISS-0623's close-out
      done-event (23:39:02Z) sorted before this run's own REQ-328
      addendum revised-event (23:50:31Z), both independent appends
      with no overlapping lines. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 15
      on-disk entries, rather than carrying forward either side's
      independently-computed 13/14, per this note's own
      recount-every-time rule. 653 lines / 37680 bytes, well under the
      roll ceiling.
      Recounted to 14 on WF03-ISS0624-20260912's Step Final rebase,
      which conflicted at this file's tail between this run's own
      already-composed note (previous recount: 12) and DOC-UPDATER's
      own ISS-0623 close-out note (independently computed as 13,
      before either side saw the other). docs/status/
      requirement_status.v15.yaml's own tail required manual
      composition -- ISS-0623's started-event (20:26:16Z), then this
      run's ISS-0624 done-event (22:56:11Z, chronologically between
      ISS-0623's started and done), then ISS-0623's own done-event
      (23:39:02Z), all independent appends with no overlapping lines.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 14 on-disk entries,
      rather than carrying forward either side's independently-computed
      12/13, per this note's own recount-every-time rule. 625 lines /
      36078 bytes, well under the roll ceiling.
      Incremented to 13 by DOC-UPDATER's own ISS-0623 close-out
      done-event append (issue/ISS-0623-20260913, Step 6) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 13
      on-disk entries; 573 lines / 33641 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Recounted to 12 on issue/ISS-0623-20260913's Step Final rebase
      onto main, composing REQ-327's own done-event append and the
      ISS-0623-filed SCOPE-CHANGE done-event append (both landed on
      main first, at 20:22:08Z) with this run's own ISS-0623
      started-event append (20:26:16Z) chronologically -- all three
      are independent appends with no overlapping lines, recounted
      directly from `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 12 on-disk entries
      rather than carrying forward either side's independently-computed
      count, per this note's own recount-every-time rule. 542 lines /
      31822 bytes, well under the roll ceiling.
      Incremented to 10 by ORCH's own ISS-0623 started-event append
      (SCOPE-CHANGE, queue task 623) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 10 on-disk entries;
      490 lines / 28739 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 9 by ORCH's own REQ-328 blocked-event append
      (direct-action, dependency REQ-327 not yet done) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 9
      on-disk entries; 471 lines / 27712 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Recounted to 8 on docs/close-REQ324-20260913's Step Final rebase
      onto main, composing REQ-326's own done-event append (landed on
      main first, at 19:38:08Z) with this run's own REQ-324 close-out
      done-event append (19:54:00Z) chronologically -- both are
      independent appends with no overlapping lines, recounted
      directly from `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 8 on-disk entries
      rather than carrying forward either side's independently-computed
      count, per this note's own recount-every-time rule. 444 lines /
      26161 bytes, well under the roll ceiling.
      Incremented to 7 by WF02-REQ326-20260912's own Step Final rebase,
      composing REQ-324's started-event note with REQ-326's done-event
      append -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 7 on-disk entries;
      400 lines / 23082 bytes, well under the roll ceiling.
      Incremented to 6 by ORCH's own REQ-324 started-event append
      (Step 00, WF02-REQ324-20260912), composed chronologically after
      REQ-325's done-event append (WF02-REQ325-20260912) since REQ-324
      started at 17:15:28Z, before REQ-325's done at 18:06:01Z -- both
      appends landed on `main` independently and were reconciled by a
      rebase of feature/WF02-REQ324-20260912 onto origin/main -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 6 on-disk
      entries; well under the roll ceiling.
      Incremented to 4 by ORCH's own ISS-0617 SCOPE-CHANGE done-event
      append (fix/ISS-0617-20260912) -- `grep -c "^  - req:"
      docs/status/requirement_status.v15.yaml` = 4 on-disk entries;
      290 lines / well under the roll ceiling.
      Incremented to 1 by ORCH's own ISS-0616 SCOPE-CHANGE done-event
      append (chore/ISS-0616-20260912, direct-action fix) -- `grep -c
      "^  - req:" docs/status/requirement_status.v15.yaml` = 1
      on-disk entry; 196 lines / 11474 bytes, well under the roll
      ceiling.
      Opened by ORCH at feature/WF02-REQ322-20260912's Step Final,
      successor to volume 14. Created with volume 14's header text
      verbatim (volume number, opening date and predecessor path
      updated) and an empty `history:` list, per this index's
      `roll_rule.on_exceed` procedure -- diffed against volume 14's
      original header to confirm only those fields changed. `entries:`
      will be incremented per HOW-TO-APPEND step 6 as real appends land.

  - volume: 16
    path: docs/status/requirement_status.v16.yaml
    status: closed
    opened: 2026-09-13
    closed: 2026-09-14
    entries: 27
    lines: 1274
    bytes_working_tree: 71591
    frozen_prefix_lines: 1233
    frozen_prefix_sha256: "f6cce139d16c1e853711ad26e04f7ae3229cc0368784d3441a0943a18ee893d1"
    note: >
      Closed by ORCH while resolving issue/ISS-0646-candidate-role-20260914's
      PR #1384 merge conflict against main, across TWO conflict rounds on this
      same file. Round 1 (26 entries, 1208 lines): a sibling session's
      ISS-0655 SCOPE-CHANGE started+done pair had already merged and appended
      to this file's tail -- composed both branches' entries in `at:`
      timestamp order (ISS-0655 started 06:47:07Z, ISS-0655 done 07:18:00Z,
      both before this branch's own ISS-0646 done 07:18:56Z, already correct
      order, no reordering needed) and closed/rolled to volume 17 on that
      basis. Round 2 (27 entries, 1233 lines), discovered before round 1's
      close ever reached a merge -- a further sibling REQ-344 done-event
      append (08:05:00Z) had ALSO landed on main in the interim: recomposed
      by inserting REQ-344's entry (chronologically last of the three) before
      re-deriving the closure footer/counts/digest from scratch against the
      correct 27-entry composition, rather than layering a second roll on top
      of round 1's premature one -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 27 on-disk entries after
      composition, 1233 lines -- over the 1,200-line roll ceiling, confirmed
      via `wc -l` on the working-tree file. Closure footer appended per the
      roll rule; `frozen_prefix_sha256` computed BEFORE the footer was
      appended, over exactly the 1233 frozen-prefix lines, using this file's
      documented normalised-line-terminator digest (each line's trailing
      "\n" then "\r" trimmed, single "\n" re-appended, per design §8) rather
      than a raw `sha256sum` of the CRLF working-tree bytes -- this session's
      checkout is CRLF, and a raw sha256sum over CRLF bytes does not match
      A6's own computation, which normalises before hashing.
      Successor: docs/status/requirement_status.v17.yaml, created with volume
      16's header text verbatim (volume number, opening date and predecessor
      path updated) and an empty `history:` list -- diffed against volume 16's
      original header to confirm only those fields changed.
      Recounted to 24 by DOC-UPDATER's own ISS-0646 done-event append
      (SCOPE-CHANGE, fix/ISS-0646-20260914), composed at Step Final
      onto ORCH's own concurrently-landed ISS-0649 started+done pair
      (this run's working tree had been created against a
      pre-ISS-0649 base) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 24 on-disk entries
      after composition (ISS-0649 started 04:23:00Z, ISS-0649 done
      05:25:00Z, ISS-0646 done 07:18:56Z, all already in correct
      chronological order, no reordering needed); 1157 lines / 65820
      bytes, well under the roll ceiling. Recounted from the composed
      file per the recount-every-time rule rather than carrying
      forward either side's independently-computed 22/23.
      Incremented to 23 by ORCH's own ISS-0649 done-event append
      (SCOPE-CHANGE, fix/ISS-0649-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 23 on-disk entries;
      well under the roll ceiling. No rebase conflict on this file.
      Incremented to 21 by ORCH's own ISS-0654 done-event append
      (SCOPE-CHANGE, fix/ISS-0654-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 21 on-disk entries;
      1050 lines / 58854 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 20 by ORCH's own ISS-0654 started-event append
      (SCOPE-CHANGE, fix/ISS-0654-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 20 on-disk entries;
      1016 lines -- approaching but still under the roll ceiling. No
      rebase conflict on this file.
      Incremented to 19 by ORCH's own ISS-0650 done-event append
      (SCOPE-CHANGE, fix/ISS-0650-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 19 on-disk entries;
      1000 lines / 56262 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 18 by ORCH's own ISS-0650 started-event append
      (SCOPE-CHANGE, fix/ISS-0650-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 18 on-disk entries;
      955 lines, approaching but still under the roll ceiling. No
      rebase conflict on this file.
      Incremented to 17 by ORCH's own ISS-0653 done-event append
      (SCOPE-CHANGE, fix/ISS-0653-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 17 on-disk entries;
      933 lines / 52379 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 16 by ORCH's own ISS-0653 started-event append
      (SCOPE-CHANGE, fix/ISS-0653-20260914) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 16 on-disk entries;
      894 lines, well under the roll ceiling. No rebase conflict on
      this file.
      Recounted to 15 at fix/ISS-0651-20260913's second (Step Final
      close-out) rebase, which conflicted at this file's tail between
      main's own already-composed 14-count note (from the first
      conflict, above) and this branch's own ISS-0651 done-event
      append (independently computed as 12 against a base predating
      REQ-342/REQ-343/REQ-339's done-events). Both appends land at the
      same v16.yaml tail position with no overlapping lines -- composed
      order there is REQ-342 done, REQ-343 done, REQ-339 done, then
      ISS-0651 done (23:51:02Z, sorting last), already correct by
      `at:` timestamp with no reordering needed -- so only this index
      file's declared count and narrative needed manual composition.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 15 rather than
      carrying forward either side's independently-computed 12/14, per
      this note's own recount-every-time rule. 874 lines, well under
      the roll ceiling.
      Recounted to 14 at fix/ISS-0651-20260913's Step Final rebase,
      which conflicted at this file's tail between main's own
      REQ-342/REQ-343/REQ-339 done-event chain (independently computed
      as 13) and this branch's own ISS-0651 started-event append
      (independently computed as 11, from a base predating that
      chain). Composed in chronological `at:` order -- ISS-0651
      started (21:57:13Z) sorts between REQ-340's own done-event
      (already in this volume) and REQ-342's done-event (22:14:10Z),
      so it was reordered into that position; REQ-343 (22:53:16Z) and
      REQ-339 (23:01:45Z) sort last and needed no reordering.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 14 rather than
      carrying forward either side's independently-computed 11/13, per
      this note's own recount-every-time rule. 827 lines, well under
      the roll ceiling.
      Incremented to 13 by DOC-UPDATER's own REQ-339 done-event append
      (pure documentation, no code change) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 13 on-disk entries;
      803 lines / 46152 bytes, well under the roll ceiling. No rebase
      conflict on this file. This is P4's own close-out requirement
      (mirroring REQ-334's role for P3) and closes out ALL of S10 P4:
      REQ-335, REQ-336, REQ-338, REQ-339, REQ-340, REQ-342 and REQ-343
      are now all done.
      Incremented to 12 by WF02-REQ343-20260914 (DOC-UPDATER)'s own
      REQ-343 done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 12 on-disk entries;
      744 lines / 43018 bytes, well under the roll ceiling. No rebase
      conflict on this file at the time it was written. This closes
      out ALL of S10 P4 (REQ-335/336/338/340/342/343 all done).
      Recounted to 11 by WF02-REQ342-20260914 (DOC-UPDATER)'s Step Final
      rebase, which found v16.yaml itself auto-merged cleanly (this
      branch's own REQ-342 done-event append landed at the file's tail
      after main's independently-landed ISS-0652/REQ-340 composition, no
      overlapping lines) but conflicted here on the declared count
      between main's own 10 (fix/ISS-0652-20260913's fourth Step Final
      recount) and this branch's own 9 (computed against a base
      predating that composition). Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v16.yaml` = 11 rather
      than carrying forward either side's stale 9/10, per this note's
      own recount-every-time rule. 672 lines / 39060 bytes, well under
      the roll ceiling.
      Incremented to 12 by ORCH's own ISS-0651 done-event append
      (SCOPE-CHANGE, fix/ISS-0651-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 12 on-disk entries;
      695 lines / 39477 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 11 by ORCH's own ISS-0651 started-event append
      (SCOPE-CHANGE, fix/ISS-0651-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 11 on-disk entries;
      648 lines, well under the roll ceiling. No rebase conflict on
      this file.
      Recounted to 10 at fix/ISS-0652-20260913's fourth Step Final
      rebase, composing main's own REQ-340 done-event (independently
      computed as 8) with this branch's own ISS-0652 done-event
      (independently computed as 9 from a base predating REQ-340).
      Both land at the same v16.yaml tail with no overlapping lines --
      composed order is ISS-0652 done (21:16:59Z) then REQ-340 done
      (21:32:34Z), already correct by `at:` timestamp with no
      reordering needed -- so only this index file's declared count
      and narrative needed manual composition. Recounted directly from
      `grep -c "^  - req:" docs/status/requirement_status.v16.yaml` =
      10 rather than carrying forward either side's independently-
      computed 8/9, per this note's own recount-every-time rule. 624
      lines, well under the roll ceiling.
      Recounted to 9 at fix/ISS-0652-20260913's third Step Final
      rebase, which found v16.yaml itself auto-merged cleanly (main's
      REQ-340 done-event landed after this file's own tail, no
      overlapping lines with this branch's own content) but conflicted
      here on the declared count between main's own 8 (DOC-UPDATER's
      REQ-340 done-event append) and this branch's own 8 (from the
      prior Step Final rebase's composition, computed before REQ-340
      landed). Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 9 rather than
      carrying forward either side's stale 8, per this note's own
      recount-every-time rule. 578 lines, well under the roll ceiling.
      Incremented to 8 by DOC-UPDATER's own REQ-340 done-event append
      (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 8 on-disk entries; 561
      lines / 32762 bytes, well under the roll ceiling. No rebase
      conflict on this file at the time it was written.
      Recounted to 9 at fix/ISS-0652-20260913's second (Step Final
      close-out) rebase, which conflicted at this file's tail between
      main's own already-composed 8-count note (from the first
      conflict, above) and this branch's own ISS-0652 done-event
      append (independently computed as 7 against a base predating
      REQ-338/REQ-336's done-events). Both appends land at the same
      v16.yaml tail position with no overlapping lines -- composed
      order there is REQ-338 done (19:27:01Z), REQ-336 done
      (20:55:45Z), ISS-0652 done (21:16:59Z), already correct by `at:`
      timestamp with no reordering needed -- so only this index file's
      declared count and narrative needed manual composition.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 9 rather than
      carrying forward either side's independently-computed 7/8, per
      this note's own recount-every-time rule. 578 lines, well under
      the roll ceiling.
      Recounted to 8 at fix/ISS-0652-20260913's Step Final rebase,
      which conflicted at this file's tail between main's own
      REQ-338/REQ-336 done-event pair (independently computed as 7)
      and this branch's own ISS-0652 started-event append
      (independently computed as 6, from a base predating both
      done-events). Composed in chronological `at:` order -- ISS-0652
      started (19:22:16Z) sorts between REQ-338's own started-event
      (already in this volume) and REQ-338's done-event (19:27:01Z),
      so it was reordered into that position; REQ-336's done-event
      (20:55:45Z) sorts last and needed no reordering. Recounted
      directly from `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 8 rather than
      carrying forward either side's independently-computed 6/7, per
      this note's own recount-every-time rule. 532 lines, well under
      the roll ceiling.
      Incremented to 7 by DOC-UPDATER's own REQ-336 done-event append
      (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 7 on-disk entries; 515
      lines / 30180 bytes, well under the roll ceiling. No rebase
      conflict on this file at the time it was written.
      Incremented to 6 by DOC-UPDATER's own REQ-338 done-event append
      (WF02-REQ338-20260914, Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 6 on-disk entries; 471
      lines / 27573 bytes, well under the roll ceiling. No rebase
      conflict on this file at the time it was written.
      Incremented to 7 by ORCH's own ISS-0652 done-event append
      (SCOPE-CHANGE, fix/ISS-0652-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 7 on-disk entries;
      436 lines / 24542 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 6 by ORCH's own ISS-0652 started-event append
      (SCOPE-CHANGE, fix/ISS-0652-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 6 on-disk entries;
      391 lines, well under the roll ceiling. No rebase conflict on
      this file.
      Recounted to 5 at fix/ISS-0647-20260913's second (Step Final
      close-out) rebase, which conflicted at this file's tail between
      main's own already-composed 4-count note (from the first
      conflict, above) and this branch's own ISS-0647 done-event
      append (independently computed as 4 against a base predating
      REQ-336's blocked-event). Both appends land at the same v16.yaml
      tail position with no overlapping lines -- composed order there
      is REQ-336 blocked (16:13:04Z) then ISS-0647 done (18:30:49Z),
      already correct by `at:` timestamp with no reordering needed --
      so only this index file's declared count and narrative needed
      manual composition. Recounted directly from `grep -c
      "^  - req:" docs/status/requirement_status.v16.yaml` = 5 rather
      than carrying forward either side's independently-computed 4,
      per this note's own recount-every-time rule. 374 lines / 21307
      bytes, well under the roll ceiling.
      Recounted to 4 at fix/ISS-0647-20260913's Step Final rebase,
      which conflicted at this file's tail between main's own REQ-336
      blocked-event append (independently computed as 3) and this
      branch's own ISS-0647 started-event append (also independently
      computed as 3, from a base predating REQ-336's blocked-event).
      Both appends land at the same v16.yaml tail position with no
      overlapping lines -- ISS-0647's own started-event (15:49:56Z)
      sorts before REQ-336's blocked-event (16:13:04Z), so it was
      composed first rather than appended after. Recounted directly
      from `grep -c "^  - req:" docs/status/requirement_status.v16.yaml`
      = 4 rather than carrying forward either side's
      independently-computed 3, per this note's own recount-every-time
      rule. 310 lines, well under the roll ceiling.
      Incremented to 3 by ORCH's own REQ-336 blocked-event append
      (ISS-0648 discovered, queue task 633 released back to open) --
      `grep -c "^  - req:" docs/status/requirement_status.v16.yaml` = 3
      on-disk entries; 295 lines / 17329 bytes, well under the roll
      ceiling. No rebase conflict on this file.
      Incremented to 4 by ORCH's own ISS-0647 done-event append
      (SCOPE-CHANGE, fix/ISS-0647-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 4 on-disk entries;
      345 lines / 19561 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 3 by ORCH's own ISS-0647 started-event append
      (SCOPE-CHANGE, fix/ISS-0647-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 3 on-disk entries;
      281 lines, well under the roll ceiling. No rebase conflict on
      this file.
      Incremented to 2 by ORCH's own REQ-335 done-event append (Step
      Final, WF02-REQ335-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 2 on-disk entries;
      266 lines / 15288 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Incremented to 1 by ORCH's own REQ-335 started-event append
      (Step 00, WF02-REQ335-20260913) -- `grep -c "^  - req:"
      docs/status/requirement_status.v16.yaml` = 1 on-disk entry; 198
      lines / 11425 bytes, well under the roll ceiling. No rebase
      conflict on this file.
      Opened by ORCH at issue/ISS-0631-20260913's Step Final rebase,
      successor to volume 15. Created with volume 15's header text verbatim
      (volume number, opening date and predecessor path updated) and an
      empty `history:` list, per this index's `roll_rule.on_exceed`
      procedure. `entries:` will be incremented per HOW-TO-APPEND step 6 as
      real appends land.

  - volume: 17
    path: docs/status/requirement_status.v17.yaml
    status: closed
    opened: 2026-09-14
    closed: 2026-09-15
    entries: 26
    lines: 1251
    bytes_working_tree: 73413
    frozen_prefix_lines: 1223
    frozen_prefix_sha256: "37753c1d390d4aedef409ee9ff32225c26d24f4ff7bdc08190e18e3cb34aab5e"
    note: >
      Closed by ORCH's own REQ-356 done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 26 on-disk entries after
      the append, 1223 lines -- over the 1,200-line roll ceiling, confirmed
      via `wc -l` on the working-tree file. No concurrent merge conflict on
      this specific file at closure time. Closure footer appended per the
      roll rule; `frozen_prefix_sha256` computed over exactly the 1223
      frozen-prefix lines using this file's documented
      normalised-line-terminator digest (test/support/status_history.ex's
      frozen_prefix_digest/2), not a raw sha256sum of the CRLF
      working-tree bytes. Successor: docs/status/requirement_status.v18.yaml,
      created with volume 17's header text verbatim (volume number, opening
      date and predecessor path updated) and an empty `history:` list --
      diffed against volume 17's original header to confirm only those
      fields changed.

      WF03-ISS0676-20260915's own started/done-event appends were originally
      written to this volume while it was still current from that branch's
      point of view; this volume rolled to closed upstream before that
      branch's Step Final rebase. Per the established precedent (REQ-125's
      Step Final composition across volume 3->4's roll, see that entry
      above), those two entries were composed into volume 18 instead during
      the rebase, not kept here -- this volume's frozen content is
      therefore unchanged by that composition.
      Incremented to 25 by ORCH's own SCOPE-CHANGE (ISS-0679, blocked on
      REQ-356 not yet merged) blocked-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 25 on-disk entries; 1152
      lines / 66365 bytes, well under the roll ceiling, no roll triggered.

      Incremented to 24 by DOC-UPDATER (WF02-REQ357-20260915, Step 6) after
      appending REQ-357's own `done`-event -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 24 on-disk entries; 1127
      lines / 64,982 bytes, well under the roll ceiling (1200 lines /
      120000 bytes), no roll triggered.

      Incremented to 23 by TEST-DESIGNER (WF02-REQ357-20260915, Step 3),
      fixing a gap left by this same branch's own REQ-357 `started`-event
      append (ELIXIR-DEV/ORCH, at 2026-09-15T03:08:33Z) which added the
      entry but never bumped this declared count -- caught by
      `test/docs/requirement_status_invariants_test.exs`'s A9 assertion
      during this run's own `mix letflow.check.test` (declared 22, actual
      23 on disk). `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 23 on-disk entries; 1035
      lines, well under the roll ceiling, no roll triggered.

      Incremented to 22 by ORCH's own SCOPE-CHANGE (ISS-0677) done-event
      append (PR #1419 merged as f0afa83f) -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 22 on-disk entries; 1015
      lines, well under the roll ceiling, no roll triggered.

      Incremented to 21 by ORCH's own REQ-355 done-event append (PR #1416
      merged as 1fd89bb1) -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 21 on-disk entries; 986
      lines / well under the roll ceiling, no roll triggered.
      Incremented to 20 by ORCH's own SCOPE-CHANGE (ISS-0676, blocked on
      REQ-355 not yet merged) blocked-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 20 on-disk entries; 953
      lines / well under the roll ceiling, no roll triggered.

      Recounted to 19 during WF02-REQ352-20260914's Step Final rebase onto
      main, second conflict (Step 6's done-event replay): this branch's
      own REQ-352 done-event (originally appended against a stale
      pre-REQ-354 base and computed as 13 there) composed with main's
      six independently-landed entries (REQ-351 blocked, ISS-0674 done,
      REQ-351 done, REQ-353 done, ISS-0675 done, REQ-354 done) -- all
      six sort before REQ-352's own done-event (2026-09-15T00:36:22Z is
      later than all of them, latest being REQ-351's 2026-09-15T00:00:00Z),
      so REQ-352's done-event was appended after rather than interleaved.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 19 on-disk entries,
      rather than carrying forward either side's independently-computed
      count (18 from this branch's first-conflict recount, 13 from this
      branch's stale second pre-rebase side), per this note's own
      recount-every-time rule.

      Recounted to 18 during WF02-REQ352-20260914's Step Final rebase onto
      main, first conflict (Step 00's started-event replay): this branch's
      own REQ-352 started-event (originally appended against a stale
      pre-REQ-350 base and computed as 12 there) composed with the same
      six main entries above in chronological `at:` order -- REQ-352's
      own 22:11:09Z timestamp sorts before all six (earliest is
      23:30:49Z), so it was composed first rather than appended after.
      Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 18 on-disk entries at
      that point, rather than carrying forward either side's
      independently-computed count (17 from main's side, 12 from this
      branch's stale pre-rebase side).

      Incremented to 17 by DOC-UPDATER's own REQ-354 done-event append
      (close-out of the 0033 pdf-qr-rendering-dependencies decision
      record) -- `grep -c "^  - req:" docs/status/requirement_status.v17.yaml`
      = 17 on-disk entries. `wc -l`/`wc -c` = 871 lines / 50749 bytes,
      well under the roll ceiling, no roll triggered.

      Incremented to 16 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0675
      resolution) done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 16 on-disk entries.
      `wc -l`/`wc -c` = 844 lines / 49191 bytes, well under the roll
      ceiling, no roll triggered.

      Incremented to 15 by DOC-UPDATER's own REQ-353 done-event append
      (close-out of the 0032 bucket-rule-2-scope-amendment decision
      record) -- `grep -c "^  - req:" docs/status/requirement_status.v17.yaml`
      = 15 on-disk entries. `wc -l`/`wc -c` = 809 lines / 47128 bytes,
      well under the roll ceiling, no roll triggered.

      Incremented to 14 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0674
      resolution) and REQ-351 done-event appends -- `grep -c "^  - req:"
      docs/status/requirement_status.v17.yaml` = 14 on-disk entries.
      `wc -l`/`wc -c` = 777 lines / 45109 bytes, well under the roll
      ceiling, no roll triggered.

      Incremented to 12 by REVIEWER's own REQ-351 blocked-event append
      (2026-09-15, ISS-0674 filed) -- 12 on-disk `- req:` entries in
      requirement_status.v17.yaml at that point.

      Incremented to 10 by FRONTEND-DEV's own SCOPE-CHANGE (ISS-0664)
      done-event append -- `grep -c "^  - req:" docs/status/requirement_status.v17.yaml`
      = 10 on-disk entries. `wc -l`/`wc -c` = 609 lines / 34921 bytes, well
      under the roll ceiling, no roll triggered.

      Opened by ORCH while resolving issue/ISS-0646-candidate-role-20260914's
      PR #1384 merge conflict against main, successor to volume 16. Created
      with volume 16's header text verbatim (volume number, opening date and
      predecessor path updated) and an empty `history:` list, per this
      index's `roll_rule.on_exceed` procedure -- diffed against volume 16's
      original header to confirm only those fields changed.
      Incremented to 2 by ORCH -- corrected a stale declared 0 to the real
      on-disk count after finding REQ-345's own done-event append (landed
      earlier this session) had not updated this field, then added
      REQ-346's own done-event append in the same commit -- 2 on-disk
      `- req:` entries in requirement_status.v17.yaml at that point,
      recounted via `grep -c "^  - req:"` per this note's
      recount-every-time rule.
      Incremented to 3 by ORCH's own REQ-347 done-event append -- 3
      on-disk `- req:` entries in requirement_status.v17.yaml.
      Incremented to 8 by ORCH's own SCOPE-CHANGE (ISS-0663) done-event
      append -- `grep -c "^  - req:" docs/status/requirement_status.v17.yaml`
      = 8 on-disk entries at that point (folding in the intervening
      REQ-349 started+done and REQ-348 started+done appends, which had
      landed without their own index-note increments), per this note's own
      recount-every-time rule. `wc -l`/`wc -c` = 521 lines / 29754 bytes,
      well under the roll ceiling, no roll triggered.

  - volume: 18
    path: docs/status/requirement_status.v18.yaml
    status: closed
    opened: 2026-09-15
    closed: 2026-09-17
    entries: 26
    lines: 1238
    bytes_working_tree: 75914
    frozen_prefix_lines: 1211
    frozen_prefix_sha256: "25436a24542cf85b56f6bb36749c6fab68cb3f034aa1ef0477c95be620d074ea"
    note: >
      Closed by ORCH's own ISS-0703 done-event (SCOPE-CHANGE) append --
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 26
      on-disk entries after the append, 1211 lines -- over the 1,200-line
      roll ceiling, confirmed via `wc -l` on the working-tree file. No
      concurrent merge conflict on this specific file at closure time.
      Closure footer appended per the roll procedure; successor volume 19
      created with the same header text.

      This ORCH session's own DOC-UPDATER SCOPE-CHANGE (ISS-0700
      partial-action, REQ-360 stale-clause correction) entry had
      originally been appended to this volume against a pre-roll base
      (would have been the 27th entry) -- redirected to volume 19 instead
      during this branch's rebase onto origin/main, since this volume was
      already closed and frozen by the time of that rebase. Volume 18
      itself is unmodified here, byte-for-byte identical to origin/main's
      closed state.

      Incremented to 24 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0707)
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 24 on-disk entries; 1129
      lines / 67530 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

  - volume: 19
    path: docs/status/requirement_status.v19.yaml
    status: closed
    opened: 2026-09-17
    closed: 2026-09-18
    entries: 23
    lines: 1239
    bytes_working_tree: 76560
    frozen_prefix_lines: 1212
    frozen_prefix_sha256: "a7a73830bd63e4358a8b71aa00da4c4911b1d000cefb3a2dbaaa66e84a1b66e1"
    note: >
      Closed by ORCH's own ISS-0690 blocked-event (SCOPE-CHANGE) append --
      `grep -c "^  - req:" docs/status/requirement_status.v19.yaml` = 23
      on-disk entries after the append, 1212 lines -- over the 1,200-line
      roll ceiling, confirmed via `wc -l` on the working-tree file. No
      concurrent merge conflict on this specific file at closure time.
      Closure footer appended per the roll procedure; successor volume 20
      created with the same header text.

      Incremented to 21 by ORCH's own REQ-370 Step-Final done-event append
      (recovery/completion run, branch feature/WF02-REQ370-20260918, PR
      #1535 squash-merged to main as 21177271, queue task 714, GH#1529 --
      multi-issuer OIDC token verification) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 21 on-disk entries; 1152
      lines, well under the 1200-line/120000-byte roll ceiling, no roll
      triggered.

      Prior note (20 entries): Incremented to 20 by DOC-UPDATER's own
      REQ-370 done-event append (recovery/completion run, branch
      feature/WF02-REQ370-20260918, PR #1535, queue task 714, GH#1529 --
      multi-issuer OIDC token verification) -- 20 on-disk entries; 1084
      lines / 66752 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Prior note (19 entries): Incremented to 19 by ORCH's own REQ-370
      started-event append (WF02-REQ370-20260918, queue task 714, GH#1529 --
      multi-issuer OIDC token verification), merged on top of origin/main's
      own DOC-UPDATER REQ-369 revised-event append
      (WF02-REQ369-closeout-final-20260918) that landed independently at
      nearly the same time (both entries preserved on rebase, append-only,
      ordered by their own `at:` timestamps: REQ-370 started at 17:11:13Z,
      then REQ-369 revised at 19:50:13Z) -- 19 on-disk entries; 1021 lines,
      well under the 1200-line/120000-byte roll ceiling, no roll triggered.

      Prior note (18 entries): Incremented to 18 by ORCH's own REQ-370
      started-event append (WF02-REQ370-20260918, queue task 714, GH#1529),
      merged on top of origin/main's own ISS-0711 done-event append that
      landed independently at nearly the same time (both entries preserved
      on rebase, append-only, ordered by their own `at:` timestamps:
      ISS-0711 done at 16:22:39Z, then REQ-370 started at 17:11:13Z) -- 18
      on-disk entries; 985 lines, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Prior note (17 entries): Incremented to 17 by DOC-UPDATER's own
      ISS-0711 done-event append (WF03-ISS0711-20260918 -- full pipeline run
      resolving the TenantListResponse `total`/`count` field mismatch that
      blocked REQ-369's AC6), merged on top of a concurrent ORCH ISS-0713
      append that landed independently at nearly the same time (both entries
      preserved on rebase, append-only, ordered by their own `at:`
      timestamps: ORCH's ISS-0713 entry at 16:01:06Z, then this ISS-0711
      entry at 16:22:39Z) -- 17 on-disk entries; 951 lines / 57452 bytes,
      well under the 1200-line/120000-byte roll ceiling, no roll triggered.

      Prior note (17 entries, ISS-0711): Incremented to 17 by DOC-UPDATER's
      own ISS-0711 done-event append (WF03-ISS0711-20260918 -- full
      pipeline run resolving the TenantListResponse `total`/`count` field
      mismatch that blocked REQ-369's AC6), merged on top of a concurrent
      ORCH ISS-0713 append that landed independently at nearly the same
      time (both entries preserved on rebase, append-only, ordered by
      their own `at:` timestamps: ORCH's ISS-0713 entry at 16:01:06Z, then
      this ISS-0711 entry at 16:22:39Z) -- 951 lines / 57452 bytes, well
      under the roll ceiling, no roll triggered.

      Prior note (16 entries, ISS-0713): Incremented to 16 by ORCH's own
      ISS-0713 done-event append (WF03-ISS0713-20260918, queue task Q-713
      -- full WF-03 run resolving the platform-login-routing-by-role
      pipeline_test's unscoped Tenants-link locator strict-mode violation,
      live-verified against QA, merged via PR #1526; ORCH took over Step
      Final directly after the dispatched FRONTEND-DEV subagent stalled
      mid-protocol) -- 16 on-disk entries; 910 lines / 55822 bytes. This
      entry deliberately uses `req: SCOPE-CHANGE` (not `req: ISS-0713`) per
      this volume's own header vocabulary rule -- see the entry's own
      closing note for why that matters (the immediately-prior ISS-0712
      entry got this wrong).

      Prior note (15 entries), retained for history: Incremented to 15 by ORCH's own ISS-0712 done-event append
      (WF03-ISS0712-20260918, queue task Q-712 -- full WF-03 run resolving
      the pipeline_test's real-OIDC-redirect login gap, live-verified against
      QA, merged via PR #1524) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 15 on-disk entries; 824
      lines / 50097 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Prior note (14 entries), retained for history: Incremented to 14 by ORCH's own REQ-369 revised-event append
      (WF02-REQ369-AC6AC7-20260918T143000Z, queue task 708 AC6/AC7
      re-verification -- credential-access blocker resolved, two new issues
      ISS-0711/ISS-0712 found and registered) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 14 on-disk entries; 774
      lines / 46745 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Prior note (13 entries), retained for history: Incremented to 13 by
      DOC-UPDATER's own REQ-369 revised-event append
      (WF02-REQ369-closeout-update-20260918, close-out note update) -- `grep -c
      "^  - req:" docs/status/requirement_status.v19.yaml` = 13 on-disk
      entries; 735 lines / 43251 bytes, well under the 1200-line/120000-byte
      roll ceiling, no roll triggered.

      Recounted to 12 while resolving this run's (WF02-REQ365-20260918)
      Step Final rebase onto origin/main: composing DOC-UPDATER's own
      REQ-365 done-event append (Step 6) with the already-landed REQ-366
      done-event append (Step 6, WF02-REQ366-20260918) and this run's own
      REQ-365 started-event append (Step 00), each side having computed a
      lower count independently, unaware of the others -- `grep -c
      "^  - req:" docs/status/requirement_status.v19.yaml` = 12 on-disk
      entries; 688 lines / 40860 bytes, well under the 1200-line/120000-byte
      roll ceiling, no roll triggered.

      Recounted to 11 while resolving this run's (WF02-REQ365-20260918)
      rebase onto origin/main: a real concurrent-append collision on this
      volume's note, composing DOC-UPDATER's REQ-366 done-event append
      (Step 6, WF02-REQ366-20260918, already landed on main) with this
      run's own REQ-365 started-event append (Step 00) -- `grep -c
      "^  - req:" docs/status/requirement_status.v19.yaml` = 11 on-disk
      entries; 615 lines / 36991 bytes, well under the 1200-line/120000-byte
      roll ceiling, no roll triggered.

      Separately, and concurrently, incremented to 10 by DOC-UPDATER's own
      REQ-365 done-event append (Step 6, WF02-REQ365-20260918) -- `grep -c
      "^  - req:" docs/status/requirement_status.v19.yaml` = 10 on-disk
      entries; 582 lines / 34122 bytes, well under the 1200-line/120000-byte
      roll ceiling, no roll triggered.

      Recounted to 10 during this run's rebase onto origin/main: a real
      concurrent-append collision on this volume's note, composing
      DOC-UPDATER's ISS-0709 done-event append (already landed on main) with
      this run's own REQ-365 started-event append (this branch's own commit,
      each side computing 9 independently, unaware of the other) --
      `grep -c "^  - req:" docs/status/requirement_status.v19.yaml` = 10
      on-disk entries; 549 lines / 32796 bytes, well under the
      1200-line/120000-byte roll ceiling, no roll triggered.

      Incremented to 10 by DOC-UPDATER's own REQ-366 done-event append
      (Step 6, WF02-REQ366-20260918) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 10 on-disk entries; 600
      lines / 35676 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 9 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0709)
      done-event append (WF03-ISS0709-20260918) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 9 on-disk entries; 534
      lines / 31547 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Separately, and concurrently, incremented to 9 by ORCH's own REQ-365
      started-event append (Step 00, WF02-REQ365-20260918) -- `grep -c
      "^  - req:" docs/status/requirement_status.v19.yaml` = 9 on-disk
      entries; 509 lines, well under the 1200-line/120000-byte roll ceiling,
      no roll triggered.

      Incremented to 8 by DOC-UPDATER's own REQ-364 done-event append (Step
      6, WF02-REQ364-20260917) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 8 on-disk entries; 494
      lines / 29117 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 7 by DOC-UPDATER's own REQ-369 blocked-event append (Step
      6, WF02-REQ369-20260917) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 7 on-disk entries; 364
      lines / 21211 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 5 by DOC-UPDATER's own REQ-367 done-event append (Step
      6, WF02-REQ367-20260917) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 5 on-disk entries; 321
      lines / 18581 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 4 by ORCH's own REQ-367 started-event append (Step 00,
      WF02-REQ367-20260917) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 4 on-disk entries; well
      under the 1200-line/120000-byte roll ceiling, no roll triggered.

      Incremented to 3 by WF02-REQ361-20260917 (DOC-UPDATER)'s own REQ-361
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 3 on-disk entries; 253
      lines / 14849 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 1 by this ORCH session's own DOC-UPDATER SCOPE-CHANGE
      (ISS-0700 partial-action, REQ-360 stale-clause correction) done-event
      append, redirected here from volume 18 during this branch's rebase
      onto origin/main (volume 18 had already been closed by a concurrent
      session's ISS-0703 append before this rebase) -- `grep -c "^  - req:"
      docs/status/requirement_status.v19.yaml` = 1 on-disk entry; 209 lines
      / 12120 bytes, well under the 1200-line/120000-byte roll ceiling, no
      roll triggered.

      Opened by ORCH as volume 18's roll successor, immediately after
      volume 18 crossed the 1,200-line roll ceiling on the ISS-0703
      done-event append. No entries yet at creation time.

      Incremented to 23 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0706)
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 23 on-disk entries; 1100
      lines / 65832 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Incremented to 22 by DOC-UPDATER's own SCOPE-CHANGE (ISS-0702)
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 22 on-disk entries; 1056
      lines / 63163 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Recounted to 21 during this run's rebase onto origin/main (real
      merge conflict on this file and requirement_status.v18.yaml,
      resolved by composing origin/main's full 18-entry sequence with
      this run's own ISS-0696 done-event appended at the tail, since its
      2026-09-17T07:49:00Z timestamp postdates every entry origin/main
      had landed) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 21 on-disk entries; 1018
      lines / 60854 bytes, well under the 1200-line/120000-byte roll
      ceiling, no roll triggered.

      Prior note (origin/main side, entries: 19, itself already off by
      one against the pre-rebase on-disk count of 18) preserved below for
      history:
    note: >
      Incremented to 18 by WF02-REQ360-20260917 (DOC-UPDATER)'s own REQ-360
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 18 on-disk entries, no
      rebase conflict on this file. 871 lines / 52488 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 17 by ORCH's own REQ-360 started-event append (Step 00,
      WF02-REQ360-20260917) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 17 on-disk entries, no
      rebase conflict on this file.
      Incremented to 16 by WF03-ISS0690-20260917 (ORCH)'s own SCOPE-CHANGE
      blocked-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 16 on-disk entries, no
      rebase conflict on this file. 807 lines / well under the roll
      ceiling, so no roll triggered.

      Incremented to 15 by WF03-ISS0692-20260917 (ORCH)'s own ISS-0692
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 15 on-disk entries, no
      rebase conflict on this file. 760 lines / well under the roll
      ceiling, so no roll triggered.

      Incremented to 14 by WF03-ISS0698-20260917 (ORCH)'s own ISS-0698
      done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 14 on-disk entries, no
      rebase conflict on this file. 695 lines / well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 13 by WF03-ISS0697-20260917 (DOC-UPDATER)'s own
      ISS-0697 done-event append -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 13 on-disk entries, no
      rebase conflict on this file. 642 lines / 37993 bytes, well under
      the 1200-line/120000-byte roll ceiling, so no roll triggered.

      Recounted to 12 after WF03-ISS0695-20260916's recovery run appended
      its own done-event SCOPE-CHANGE entry -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 12 on-disk entries.
      Recounted to 11 by WF03-ISS0695-20260916's Step 4 (TEST-RUNNER), which
      found assertion A9 failing on a fresh `mix letflow.check.test` baseline:
      commit e8ab8e0e (chore(WF03-ISS0694): append run-history done event)
      appended its done-event to docs/status/requirement_status.v18.yaml but
      never touched this index file, leaving `entries:` stale at 10 against
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 11
      on-disk entries -- unrelated to ISS-0695 itself, fixed here only to
      unblock a clean full-suite baseline before the 10-run non-flakiness
      check. 530 lines / 31014 bytes, well under the roll ceiling, no roll
      triggered.

      Recounted to 10 during WF02-REQ362-20260916's Step Final rebase,
      second conflict (Step 6's done-event replay): composed this run's
      own REQ-362 done-event (originally appended against a stale
      pre-rebase base and computed as 6 there) with the 9 entries this
      same rebase's first conflict had already composed -- `grep -c
      "^  - req:" docs/status/requirement_status.v18.yaml` = 10 on-disk
      entries; 485 lines / 27704 bytes, well under the roll ceiling, no
      roll triggered.

      Recounted to 9 during WF02-REQ362-20260916's Step Final rebase onto
      a further-advanced origin/main: composed this run's own REQ-362
      started-event (appended at the tail, not reordered into strict
      chronological position, per that entry's own composition note) and
      REQ-362 done-event with the 8 entries WF02-REQ359-20260916's own
      Step Final rebase had already composed -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 9 on-disk entries,
      rather than carrying forward either side's independently-computed
      count, per this note's own recount-every-time rule.

      Recounted to 8 during WF02-REQ359-20260916's Step Final rebase onto
      origin/main (7bab6d2a), whose second conflict replayed this run's own
      REQ-359 done-event (DOC-UPDATER, Step 6) after the REQ-363
      started/REQ-359 started/REQ-363 done composition recounted below --
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 8
      on-disk entries, rather than carrying forward either side's
      independently-computed 6/7, per this note's own recount-every-time
      rule. 299 lines / 17021 bytes (plus this rebase's composition text),
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered.

      Recounted to 7 during the same Step Final rebase's first conflict,
      which conflicted at requirement_status.v18.yaml's tail against
      WF02-REQ363-20260916's own REQ-363 started+done pair (landed on main
      after this run's own Step 00 commit). Composed in chronological `at:`
      order -- REQ-363 started (10:28:14Z), REQ-359 started (10:30:49Z),
      REQ-363 done (10:35:01Z) -- this run's own started-event was
      reordered into that position rather than appended after REQ-363's
      done-event. Recounted directly from `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 7 on-disk entries, rather
      than carrying forward either side's independently-computed 5/6, per
      this note's own recount-every-time rule.

      Incremented to 6 by ORCH's own REQ-363 done-event append --
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 6
      on-disk entries, no roll triggered.

      Incremented to 5 by ORCH's own REQ-363 started-event append --
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 5
      on-disk entries, no roll triggered.

      Incremented to 6 by WF02-REQ359-20260916 (DOC-UPDATER)'s own REQ-359
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 6 on-disk entries at that
      run's own pre-rebase base, no rebase conflict found by that run
      against this file at the time (superseded by the recount above).

      Incremented to 5 by ORCH's own REQ-359 started-event append (Step 00,
      WF02-REQ359-20260916) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 5 on-disk entries (computed
      pre-rebase, superseded by the recount above).

      Incremented to 6 by WF02-REQ362-20260916 (DOC-UPDATER)'s own
      REQ-362 done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 6 on-disk entries at
      that run's own pre-rebase base (superseded by the recount above),
      323 lines / 18314 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 5 by ORCH's own REQ-362 started-event append (redo,
      after a first attempt was lost to a concurrent-checkout collision
      in the same shared directory -- see that entry's own note) --
      `grep -c "^  - req:" docs/status/requirement_status.v18.yaml` = 5
      on-disk entries at that run's own pre-rebase base (superseded by
      the recount above).

      Incremented to 4 by WF02-REQ358-20260916 (DOC-UPDATER)'s own REQ-358
      done-event append (Step 6) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 4 on-disk entries, no
      rebase conflict on this file. 251 lines / 14263 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 3 by ORCH's own REQ-358 started-event append (Step 00,
      WF02-REQ358-20260916) -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 3 on-disk entries.

      Incremented to 2 by ORCH's own SCOPE-CHANGE (ISS-0676) done-event
      append, composed here during fix/ISS-0676-20260915's Step Final
      rebase after volume 17 rolled from under it -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 2 on-disk entries.

      Incremented to 1 by ORCH's own SCOPE-CHANGE (ISS-0676) started-event
      append, composed here during fix/ISS-0676-20260915's Step Final
      rebase after volume 17 rolled from under it -- `grep -c "^  - req:"
      docs/status/requirement_status.v18.yaml` = 1 on-disk entry.

      Opened by ORCH's own REQ-356 close-out, successor to volume 17.
      Created with volume 17's header text verbatim (volume number,
      opening date and predecessor path updated) and an empty `history:`
      list, per this index's `roll_rule.on_exceed` procedure -- diffed
      against volume 17's original header to confirm only those fields
      changed. `entries:` will be incremented per HOW-TO-APPEND step 6 as
      real appends land.

  - volume: 21
    path: docs/status/requirement_status.v21.yaml
    status: closed
    opened: 2026-09-20
    closed: 2026-09-22
    entries: 32
    lines: 1271
    bytes_working_tree: 79932
    frozen_prefix_lines: 1235
    frozen_prefix_sha256: "a3c3cc240566775702ddef4322aefdc1050b3f7611f2c624d7e73bdec435c5bc"
    note: >
      Closed by DOC-UPDATER's own REQ-383 done-event append, composed during
      feature/WF02-REQ383-20260923's own Step Final rebase onto fresh
      origin/main (which had independently gained REQ-384's and REQ-385's own
      appends since REQ-383's entry was first written against a stale
      baseline). Real rebase conflict on this file (both sides appended after
      the same tail entry), resolved by keeping BOTH -- origin's REQ-384/
      REQ-385 entries first, this branch's REQ-383 entry appended last -- and
      on this index file's own `entries:`/`note:` field, resolved the same
      way (both sides' increments are real and additive; final count is
      origin's 31 plus this branch's own +1). Recounted directly post-rebase:
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 32
      on-disk entries, 1235 lines -- over the 1,200-line roll ceiling (bytes,
      77556, stayed under the 120000-byte ceiling; only the line ceiling was
      crossed) -- rolled to volume 22 per the roll rule. No entry was moved,
      edited or deleted.

      Incremented to 31 by DOC-UPDATER's own REQ-385 done-event append
      (WF02-REQ385-20260922's Step 6 -- an unusual close-out via
      documented-vacuous-conclusion, no code diff, see the entry's own
      note). Recounted directly post-append: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 31 on-disk entries. 1182
      lines / 72761 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 30 by DOC-UPDATER's own REQ-384 done-event append
      (WF02-REQ384-20260922's Step 6, on feature/WF02-REQ384-20260922 after
      merging origin/main and resolving a real, additive-only conflict
      against concurrently-landed REQ-382 in lib/letflow/identity.ex and
      test/letflow/api/authorization_enforcement_test.exs). Recounted
      directly post-append: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 30 on-disk entries. 1140
      lines / 70178 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 29 during WF02-REQ382-20260922's own Step Final rebase
      onto origin/main, resolving a conflict on this same field: origin/main
      had already advanced to 28 via WF02-REQ381-20260922's own Step Final
      merge (itself resolving REQ-381 vs REQ-377's 26-vs-27 conflict, see
      the immediately-preceding note below), while this branch's own
      DOC-UPDATER had independently incremented to 28 for REQ-382's own
      done-event append from a now-stale 27 baseline. Both sides' real
      entries were preserved in the merged requirement_status.v21.yaml
      (REQ-381 done ahead of REQ-382 done, matching their real `at:`
      timestamps -- 16:08:43Z then 19:09:21Z). Recounted directly
      post-rebase: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 29 on-disk entries. 1086
      lines / 67783 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 28 during WF02-REQ381-20260922's own Step Final merge
      of origin/main, resolving a conflict on this same field: this
      branch's own DOC-UPDATER had incremented to 26 for REQ-381's
      done-event append, while origin/main had independently advanced to
      27 for WF02-REQ377-20260922's own REQ-377 started+done pair (a
      DIFFERENT pair of real entries, appended from a common ancestor).
      Both sides' entries were preserved in the merged
      requirement_status.v21.yaml (REQ-377 started/done ahead of REQ-381
      done, matching their real `at:` timestamps). Recounted directly
      post-merge: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 28 on-disk entries. 1033
      lines / 63097 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below verbatim.
      Corrected to 27 during WF02-REQ377-20260922's own Step Final rebase
      onto origin/main, resolving a nested conflict on this same field:
      both sides had independently incremented from a common ancestor
      with DIFFERENT real entries appended (main's own ISS-0781
      SCOPE-CHANGE/done-event append at 25, via
      WF03-ISS0781-20260922's Step 6; and this run's own REQ-377
      started+done pair, appended on top of a stale 24 baseline at what
      this run's own DOC-UPDATER incorrectly declared as 26). Recounted
      directly post-rebase: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 27 on-disk entries. 987
      lines / 61089 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below
      verbatim.

      Incremented to 25 by DOC-UPDATER's own ISS-0781 SCOPE-CHANGE/done-event
      append (WF03-ISS0781-20260922, Step 6), merged on top of a concurrent
      origin/main advance (REQ-376's two events, already present on main by
      the time this branch's PR was opened). Recounted directly post-merge:
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 25
      on-disk entries. 911 lines, well under the 1200-line roll ceiling, so
      no roll triggered. Prior notes preserved below verbatim.

      Corrected to 26 by WF02-REQ377-20260922's own DOC-UPDATER Step 6.
      The declared count had drifted to 24 while 25 real entries were
      already on disk (the REQ-380 done-event append plus the REQ-376
      started/done pair and REQ-377's own started event all landed after
      the last count update, without a corresponding increment) -- this
      is exactly the count-drift TEST-RUNNER's Step 4 run for this
      requirement flagged as one of its 3 non-passing results (expected
      to self-resolve here). Recounted directly, not assumed: `grep -c
      "^  - req:" docs/status/requirement_status.v21.yaml` = 25 on-disk
      entries immediately before this append, then 26 immediately after
      appending REQ-377's own done event. 944 lines / 58307 bytes, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 24 during WF02-REQ376-20260922's own Step Final rebase
      onto origin/main, resolving a second, nested conflict on this same
      field: both sides had independently incremented from a common
      ancestor with DIFFERENT real entries appended (main's own REQ-380
      done-event append at 22, and this run's own REQ-376
      done-event-plus-count-correction sequence at 21). Recounted directly
      post-rebase: `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 24 on-disk entries. 868
      lines / 53571 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 19 during WF03-ISS0780-20260922's own Step Final rebase
      onto origin/main: this hunk conflicted because both sides had
      independently incremented from the same 18-entry ancestor with a
      DIFFERENT real entry (REQ-375's own `done` event below, and this run's
      ISS-0780 SCOPE-CHANGE `resolved` event) -- composed chronologically by
      `at:` timestamp (REQ-375 done 2026-09-22T01:46:38Z, then ISS-0780
      resolved 2026-09-22T03:22:13Z), losing neither entry. `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 19 on-disk entries,
      recounted directly rather than assumed after composition. Well under
      the 1200-line/120000-byte roll ceiling, no roll triggered. Prior notes
      (each describing an earlier, incomplete count from only one side of one
      collision) preserved below verbatim.

      Incremented to 18 during WF02-REQ375-20260921's own Step Final rebase
      onto origin/main (second pass, after main advanced further with
      REQ-373's/ISS-0777's own done-event landings): composed REQ-375's own
      done event (2026-09-22T01:46:38Z -- operator-facing rollout-status
      screen) after the already-reconciled 17-entry state left by the prior
      rebase. `grep -c "^  - req:" docs/status/requirement_status.v21.yaml`
      = 18 on-disk entries after composition, recounted directly rather than
      assumed. Well under the 1200-line/120000-byte roll ceiling, no roll
      triggered. Prior notes (each describing an earlier, incomplete count
      from only one side of one collision) preserved below verbatim.

      Incremented to 18 by ISSUE-FIXER's own ISS-0780 SCOPE-CHANGE
      resolved-event append (Step 5, WF03-ISS0780-20260922) --
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 18
      on-disk entries, recounted directly rather than assumed. Well under
      the 1200-line/120000-byte roll ceiling, no roll triggered. Prior
      notes preserved below verbatim.

      Incremented to 17 during WF02-REQ373-20260921's own Step Final rebase
      onto origin/main (second pass, after main advanced further with
      ISS-0777's own done-event landing): composed REQ-373's done event
      (2026-09-21T20:58:11Z) chronologically before ISS-0777's SCOPE-CHANGE
      done event (2026-09-22T00:46:45Z -- two-tier entity_type/attribute
      identifier-format validation closing the unhandled-ArgumentError
      crash path). `grep -c "^  - req:" docs/status/requirement_status.v21.yaml`
      = 17 on-disk entries after composition, recounted directly rather
      than assumed. Well under the 1200-line/120000-byte roll ceiling, no
      roll triggered. Prior notes (each describing an earlier, incomplete
      count from only one side of one collision) preserved below verbatim.

      Incremented to 15 during WF02-REQ373-20260921's own Step Final rebase
      onto origin/main, composing THREE independent appends that collided
      on this hunk across two separate rebase conflicts: (1) ORCH's own
      WF02-REQ373-20260921 Step 00 append (REQ-373 started event,
      2026-09-21T17:09:10Z, caught as a bookkeeping gap by REQ-373's own
      Step 2a mix letflow.check run's A9 failure); (2) DOC-UPDATER's
      ISS-0775 SCOPE-CHANGE done-event append (2026-09-21T18:20:11Z --
      resolve_group_ids_for_role_names/2's OIDC role-claim write path
      scoped to kind == :platform_role, closing the
      process-routing-role-kind-name-collision gap ISS-0774's own gate
      surfaced); (3) DOC-UPDATER's own WF02-REQ373-20260921 Step 6 append
      (REQ-373 done event, 2026-09-21T20:58:11Z -- backend version-status
      lifecycle for service_catalog entries plus the real PinResolver.Lookup
      wiring). All three preserved, composed chronologically by `at:`
      timestamp (17:09:10Z, 18:20:11Z, 20:58:11Z in that order).
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 15
      on-disk entries after composition, recounted directly rather than
      assumed. Well under the 1200-line/120000-byte roll ceiling, no roll
      triggered. Prior notes (each describing an earlier, incomplete count
      from only one side of one collision) preserved below verbatim.

      [Superseded pre-rebase note from this same REQ-375 append, kept for
      the record per this file's never-delete-a-prior-note convention: at
      the time this note was first written (2026-09-22T01:46:38Z, before
      the second rebase below composed it with REQ-373's/ISS-0777's own
      concurrently-landed entries), it read: "Incremented to 16 by
      DOC-UPDATER's own REQ-375 done-event append (2026-09-22T01:46:38Z --
      operator-facing rollout-status screen, feature/WF02-REQ375-20260921).
      Previously incremented to 14 by DOC-UPDATER's own REQ-374 done-event
      append (2026-09-21T20:40:11Z -- platform-wide tenant-migration fanout
      runner backend, feature/WF02-REQ374-20260921). Previously incremented
      to 13 by DOC-UPDATER's own ISS-0775 SCOPE-CHANGE done-event append
      (2026-09-21T18:20:11Z -- resolve_group_ids_for_role_names/2's OIDC
      role-claim write path scoped to kind == :platform_role, closing the
      process-routing-role-kind-name-collision gap ISS-0774's own gate
      surfaced)." That count (16/13) was accurate against the pre-rebase
      tree it was written against, but is stale now; the 18-entry count
      above supersedes it.]

      Incremented to 12 during WF03-ISS0774-20260921's own Step Final rebase
      onto origin/main: DOC-UPDATER's own ISS-0774 SCOPE-CHANGE done-event
      append (2026-09-21T16:23:31Z -- process-routing-role vs. platform-role
      tenant_role domain split, plus the copy_tenant_roles/2 legacy-table
      regression found and fixed mid-pipeline) landed on top of the
      already-reconciled REQ-372/ISS-0773 state (11 entries) below -- v21.yaml
      itself produced a real rebase conflict on this hunk (both sides had
      appended independently), resolved by composing chronologically
      (REQ-372 done at 13:28:21Z before ISS-0774 done at 16:23:31Z), losing
      neither entry. `grep -c "^  - req:" docs/status/requirement_status.v21.yaml`
      = 12 on-disk entries, recounted directly rather than assumed after the
      merge. 454 lines / 26719 bytes, well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered. Prior notes preserved below for
      history.

      Incremented to 11 during WF02-REQ372-20260921 Step Final's THIRD
      rebase-conflict resolution on this index entry: DOC-UPDATER's own
      REQ-372 `done` append (Step 6, 13:28:21Z) landed on top of the
      already-reconciled ISS-0773 addition (13:14:36Z) above, and v21.yaml
      itself auto-merged cleanly for this hunk -- `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 11 on-disk entries,
      recounted directly rather than assumed. Prior notes preserved below
      for history.

      Incremented to 10 during WF02-REQ372-20260921 Step Final's SECOND
      rebase onto main (main advanced again, with DOC-UPDATER's own
      ISS-0773 SCOPE-CHANGE done-event, WF03-ISS0772-20260921,
      2026-09-21T13:14:36Z -- permanent-lockout zero-grant role-claims-sync
      fix, between the first rebase/push and the PR's mergeability check):
      v21.yaml itself auto-merged cleanly, so the real on-disk count is the
      authority here, not either side's own prior note -- `grep -c
      "^  - req:" docs/status/requirement_status.v21.yaml` = 10 on-disk
      entries, recounted directly rather than assumed. Prior notes preserved
      below for history.

      Incremented to 4 by DOC-UPDATER's own ISS-0769 SCOPE-CHANGE done-event
      append (WF03-ISS0769-20260921, 2026-09-21T04:30:37Z -- bearer-token
      leakage into crash logs, plus the node-wide logger-filter-crash bug
      found along the way) -- `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 4 on-disk entries. 256
      lines / 14793 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 8 by DOC-UPDATER (WF02-REQ372-20260921 Step 6), after
      appending the REQ-372 `done` event. Recounted, not assumed:
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 8.
      Prior note for context: incremented to 7 by ORCH (WF02-REQ372-20260921),
      fixing a bookkeeping omission this same run introduced: Step 00
      appended a REQ-372 `started` event directly to
      docs/status/requirement_status.v21.yaml but never updated this index
      entry's own `entries:` count at the time, caught by TEST-RUNNER's
      Step 4 run of test/docs/requirement_status_invariants_test.exs
      (assertion A9).
      on-disk entries, recounted directly rather than assumed. 310 lines /
      18053 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Reconciled during this run's (WF03-ISS0768-20260921) Step Final rebase:
      three concurrent DOC-UPDATER appends landed on volume 21 independently
      -- ISS-0769 (bearer-token leakage into crash logs, plus a node-wide
      logger-filter-crash bug found along the way, 2026-09-21T04:30:37Z),
      ISS-0770 (secret_key_base missing from the redaction key list,
      2026-09-21T05:07:24Z), and ISS-0768 (RoleRegistry tenant-schema-prefix
      fix, confirmed not a regression, broken since REQ-020 introduction,
      2026-09-21T05:58:34Z) -- and a plain rebase conflict on this index
      entry composed all three rather than picking one. `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 6 on-disk entries. 298 lines
      / 17496 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 6 by DOC-UPDATER's own REQ-397 done-event append
      (WF02-REQ397-20260921, 2026-09-21T09:29:10Z -- GET /api/v1/promotions
      tenant-data-scoped list route), written against this same volume's
      then-5-entry state before the ISS-0768 reconciliation above landed
      concurrently -- `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 6 on-disk entries in that
      worktree at that time. 294 lines / 16870 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Reconciled during REQ-397's own Step Final rebase onto fresh
      origin/main (2026-09-21, after WF03-ISS0768's reconciliation above had
      already landed on origin/main): this index entry and
      requirement_status.v21.yaml both conflicted (both sides' own local
      history had independently claimed 6 as the successor to a common
      5-entry ancestor). Composed to preserve all entries from both sides --
      the REQ-397 done-event was appended after the already-reconciled
      ISS-0769/ISS-0770/ISS-0768 trio, not in place of any of them.
      `grep -c "^  - req:" docs/status/requirement_status.v21.yaml` = 7
      on-disk entries, confirmed against the real post-rebase file. 320
      lines / 18481 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Opened by DOC-UPDATER's own ISS-0743 close-out, successor to volume 20
      (closed at 19 entries / 1227 frozen-prefix lines, over the 1200-line
      roll ceiling -- the rebase reconciliation of ISS-0741's and ISS-0743's
      concurrently-written entries crossed it). Created with volume 20's
      header text verbatim (volume number, opening date and predecessor path
      updated) and an empty `history:` list, per this index's
      `roll_rule.on_exceed` procedure.

      Incremented to 1 by DOC-UPDATER's own REQ-371 done-event append
      (WF02-REQ371-20260919, 2026-09-20T11:45:52Z -- operator-facing
      rollback/withdrawal screen for definitions) -- `grep -c "^  - req:"
      docs/status/requirement_status.v21.yaml` = 1 on-disk entry. 190 lines /
      10972 bytes, well under the 1200-line/120000-byte roll ceiling, so no
      roll triggered.

      Incremented to 5 by DOC-UPDATER's own ISS-0770 done-event append
      (WF03-ISS0770-20260921, 2026-09-21T05:07:24Z -- secret_key_base added
      to Letflow.Secrets.Redaction's sensitive-key list), appended after
      rebasing onto origin/main's concurrently-landed ISS-0769 entry (PR
      #1668) -- `grep -c "^  - req:" docs/status/requirement_status.v21.yaml`
      = 5 on-disk entries. 272 lines / 15587 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

  - volume: 22
    path: docs/status/requirement_status.v22.yaml
    status: current
    opened: 2026-09-22
    entries: 28
    note: >
      Incremented to 28 by DOC-UPDATER's own REQ-403 done-event append
      (feature/WF02-REQ403-20260925, 2026-09-25T16:55:44Z -- module
      install route gated by :ModulesManage and GET /api/v1/me/modules
      gated by the new core :MyModulesRead permission, S11's fourth
      requirement). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 28 on-disk entries,
      recounted directly rather than assumed. 1016 lines / 62676 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 27 by DOC-UPDATER's own REQ-402 done-event append
      (feature/WF02-REQ402-20260925, 2026-09-25T14:59:41Z -- tenant-scoped
      tenant_modules table, Letflow.Modules.TenantModule schema and
      Letflow.Modules.Installs install/list context, S11's third
      requirement). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 27 on-disk entries,
      recounted directly rather than assumed. 936 lines / 57647 bytes, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 26 by DOC-UPDATER's own REQ-405 doc-correction append
      (WF02-REQ405-20260925, 2026-09-25T03:39:10Z -- corrected §1.2 of
      lib/letflow/design/req405-check-boundaries.md: tree-character format
      description for parse_xref_output, space-indented fallback, annotation
      stripping). `grep -c "^  - req:" docs/status/requirement_status.v22.yaml`
      = 26 on-disk entries. Prior notes preserved below verbatim.

      Incremented to 25 by DOC-UPDATER's own REQ-401 done-event append
      (feature/WF02-REQ401-20260925, 2026-09-25T03:06:46Z -- catalog-sourced
      module permissions/role_grants in Letflow.Api.Authorization, plus the
      new core :ModulesManage permission, S11's second requirement). `grep -c
      "^  - req:" docs/status/requirement_status.v22.yaml` = 25 on-disk
      entries, recounted directly rather than assumed. 856 lines / 52437
      bytes, well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 24 by DOC-UPDATER's own ISS-0772 SCOPE-CHANGE done-event
      append (fix/ISS-0772-logfilter-metadata, 2026-09-25T00:40:32Z --
      LogFilter metadata-only redaction fix + two-stage doc correction,
      merged as e2298ea9/PR #1816; also updated ISS-0826, commit 4a6016ba,
      to record its backlog had grown since its own last count). `grep -c
      "^  - req:" docs/status/requirement_status.v22.yaml` = 24 on-disk
      entries, recounted directly rather than assumed. 798 lines / 48644
      bytes, well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 23 by DOC-UPDATER's own ISS-0817 SCOPE-CHANGE done-event
      append (fix/ISS-0817-attachment-viewer-flake, 2026-09-24T23:41:31Z --
      D1 blob-URL/iframe assertion race in AttachmentViewerPage.test.tsx,
      fixed by polling for the actual iframe instead of the container div).
      `grep -c "^  - req:" docs/status/requirement_status.v22.yaml` = 23
      on-disk entries, recounted directly rather than assumed (this count
      had drifted to 22 on disk vs. a stale `entries: 20` recorded here after
      WF03-ISS0765-20260924's own close-out commit (e8cfd614) appended two
      SCOPE-CHANGE events without updating this field -- corrected forward
      here rather than left to compound). 750 lines / 45442 bytes, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 20 by DOC-UPDATER's own REQ-400 done-event append
      (WF02-REQ400-20260924, 2026-09-24T17:24:56Z -- first requirement of
      S11 modular-platform architecture: Letflow.Modules.Module behaviour
      + Letflow.Modules.Catalog). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 20 on-disk entries,
      recounted directly rather than assumed. 663 lines / 39988 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 19 by DOC-UPDATER's own REQ-395 done-event append
      (WF02-REQ395-20260924, 2026-09-24T06:05:00Z -- CONDITIONAL_PASS: seed
      script scripts/seed_swiftroute_definition.sh merged, 5 structural tests
      pass, live QA execution blocked by ISS-0739+QA credentials, UAT re-run
      deferred). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 19 on-disk entries,
      recounted directly rather than assumed. Also includes REQ-392 done
      (WF02-REQ392-20260924, 2026-09-24T05:19:25Z) and REQ-395 started
      merged with main before this entry. Well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 16 by DOC-UPDATER's own REQ-389 done-event append
      (WF02-REQ389-20260923, 2026-09-24T01:36:56Z -- content-type allowlist
      enforcement for instance-attachment uploads, plus REVIEWER's separately
      filed+merged side-finding ISS-0788). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 16 on-disk entries,
      recounted directly rather than assumed. 533 lines / 31469 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 15 by DOC-UPDATER's own REQ-399 done-event append
      (WF02-REQ399-20260923, 2026-09-23T22:23:29Z -- case-detail dependency-
      version/provenance panel consuming REQ-080's already-shipped
      GET /:id/pins route, plus a new telemetry test as this cycle's real
      backend contribution). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 15 on-disk entries,
      recounted directly rather than assumed. 499 lines / 29280 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 14 by DOC-UPDATER's own REQ-396 done-event append
      (WF02-REQ396-20260923, 2026-09-23T20:18:19Z -- HUMAN_TASK
      escalation_timer_duration schema + SCH-04 escalation wiring).
      `grep -c "^  - req:" docs/status/requirement_status.v22.yaml` = 14
      on-disk entries (after rebase: 13 prior + REQ-396 done). Well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 13 by ELIXIR-DEV's WF02-REQ396-20260923 Step 00 started
      event (2026-09-23T18:37:05Z). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 13 on-disk entries after
      rebase (REQ-394 done + REQ-398 done + REQ-396 started), recounted
      directly rather than assumed. Well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered. Prior notes preserved below verbatim.

      Incremented to 12 by DOC-UPDATER's REQ-398 done-event append
      (WF02-REQ398-20260923, 2026-09-23T20:05:56Z -- frontend promotion-
      review list/queue page + nav entry). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 12 on-disk entries,
      recounted directly rather than assumed. 453 lines / 26762 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 11 by DOC-UPDATER's rebase of
      WF02-REQ394-20260923 onto origin/main (Step 6/7, resolving a merge
      conflict in this index and in requirement_status.v22.yaml caused by
      REQ-393's concurrent merge to main landing its own started/done
      events in this same volume before REQ-394's done-event append was
      pushed) -- adds REQ-394's done event
      (2026-09-23T17:54:56Z) after REQ-393's done event
      (2026-09-23T17:10:09Z). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 11 on-disk entries,
      recounted directly rather than assumed. 416 lines / 24335 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 10 by FRONTEND-DEV's WF02-REQ393-20260923 Step Final
      rebase: composed REQ-393 started (2026-09-23T10:15:15Z), REQ-391 done
      (2026-09-23T11:39:43Z), and REQ-393 done (2026-09-23T17:10:09Z)
      chronologically. `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 10 on-disk entries,
      recounted directly rather than assumed. Well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 9 by FRONTEND-DEV's WF02-REQ393-20260923 Step Final
      rebase: composed REQ-393 started (2026-09-23T10:15:15Z) chronologically
      before REQ-391 done (2026-09-23T11:39:43Z). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 9 on-disk entries,
      recounted directly rather than assumed. Well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.
      Prior notes preserved below verbatim.

      Incremented to 8 by DOC-UPDATER's own REQ-391 done-event append
      (WF02-REQ391-20260923 Step 6, including the REQ-390 merge-
      reconciliation cycle). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 8 on-disk entries,
      recounted directly rather than assumed. 366 lines / 21490 bytes,
      well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 5 by DOC-UPDATER's own REQ-387 done-event append

      Incremented to 4 by DOC-UPDATER's own merge of origin/main into
      feature/WF02-REQ388-20260923 (Step 6/7, resolving a merge conflict in
      this index caused by REQ-386's concurrent merge to main landing its
      own two events -- started and done -- in this same volume before
      REQ-388's done-event append was pushed). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 4 on-disk entries,
      recounted directly rather than assumed: REQ-386 started, REQ-386
      done, ISS-0784 SCOPE-CHANGE done, REQ-388 done. 269 lines / 15691
      bytes, well under the 1200-line/120000-byte roll ceiling, so no roll
      triggered. Prior notes preserved below verbatim.

      Incremented to 3 during WF02-REQ386-20260923's own Step Final rebase
      onto origin/main (second conflict, Step 6's own done-event append):
      composed REQ-386's done event (2026-09-23T00:53:45Z) chronologically
      before ISS-0784's SCOPE-CHANGE done event (2026-09-23T00:57:17Z).
      `grep -c "^  - req:" docs/status/requirement_status.v22.yaml` = 3
      on-disk entries after composition, recounted directly rather than
      assumed. Prior notes preserved below verbatim.

      Incremented to 2 during WF02-REQ386-20260923's own Step Final rebase
      onto origin/main: composed REQ-386's started event (2026-09-22T22:51:01Z)
      chronologically before ISS-0784's SCOPE-CHANGE done event
      (2026-09-23T00:57:17Z). `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 2 on-disk entries after
      composition, recounted directly rather than assumed. Well under the
      1200-line/120000-byte roll ceiling, no roll triggered. Prior note
      preserved below verbatim.

      Incremented to 1 by DOC-UPDATER's own ISS-0784 SCOPE-CHANGE done-event
      append (WF03-ISS0784-20260922's Step 6, composed during this run's own
      rebase onto fresh origin/main after REQ-383's done-event append had
      already rolled volume 21 to this volume from under it). `grep -c
      "^  - req:" docs/status/requirement_status.v22.yaml` = 1 on-disk entry.
      211 lines / 12411 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered. Prior note preserved below verbatim.

      Opened by DOC-UPDATER's own REQ-383 done-event append rolling volume
      21 (WF02-REQ383-20260923's Step Final rebase onto origin/main), per
      this index's `roll_rule.on_exceed` procedure. `entries:` will be
      incremented in place as real events land -- this file currently
      contains only the header and an empty `history:` list.

      Incremented to 2 by DOC-UPDATER's own REQ-386 done-event append
      (WF02-REQ386-20260923, 2026-09-23T00:53:45Z -- time-limited signed
      URLs for instance-attachment byte content) -- `grep -c "^  - req:"
      docs/status/requirement_status.v22.yaml` = 2 on-disk entries. 204
      lines / 11845 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

  - volume: 20
    path: docs/status/requirement_status.v20.yaml
    status: closed
    opened: 2026-09-18
    closed: 2026-09-20
    entries: 19
    lines: 1262
    bytes_working_tree: 79810
    frozen_prefix_lines: 1227
    frozen_prefix_sha256: "3bab1026ad2739e54fd6ff58104a75d6260642e24017e192eb15b599506d318f"
    note: >
      Closed by DOC-UPDATER's own ISS-0743 SCOPE-CHANGE done-event append,
      composed during feature/WF03-ISS0739-20260920's rebase onto fresh
      origin/main (which had independently gained ISS-0741's own append) --
      `grep -c "^  - req:" docs/status/requirement_status.v20.yaml` = 19
      on-disk entries after the rebase, 1227 lines -- over the 1,200-line
      roll ceiling (bytes, 77521, stayed under the 120000-byte ceiling; only
      the line ceiling was crossed), confirmed via `wc -l` on the
      working-tree file. Real rebase conflict on this file, resolved by
      keeping BOTH entries (origin's ISS-0741 entry first, this branch's
      ISS-0743 entry second) -- append-only means preserving both.

      Incremented to 19 by DOC-UPDATER's own rebase of
      feature/WF03-ISS0739-20260920 (PR #1618, ISS-0743 close-out) onto
      fresh origin/main: origin/main had independently gained ISS-0741's own
      DOC-UPDATER append (2026-09-20T07:14:07Z -- InstanceBoardPage
      start-dialog click-freeze fix, bumping this volume's on-disk count to
      18 without its own index note paragraph) since this branch's own
      ISS-0743 append (below) was first written against volume 20 at 18
      entries. The rebase produced a real conflict in
      docs/status/requirement_status.v20.yaml (both sides appended after the
      same tail entry); resolved by keeping BOTH entries, in file order by
      when each was written (origin's 07:14:07Z entry first, this branch's
      own 08:10:54Z entry second) -- append-only means preserving both,
      never dropping one to make the counts match a smaller number.
      `grep -c "^  - req:" docs/status/requirement_status.v20.yaml` = 19
      on-disk entries, recounted after the rebase, not assumed from either
      side's own pre-rebase note. 1227 lines / 77521 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 18 by DOC-UPDATER's own ISS-0743 SCOPE-CHANGE done-event
      append (WF03-ISS0743-20260920, 2026-09-20T08:10:54Z --
      Letflow.Definitions.search_paginated/3 exception-hardening fix), before
      the rebase above composed it with origin's own concurrent append -- at
      that point in this branch's own history, `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 18 on-disk entries. 1169
      lines / 73899 bytes, well under the 1200-line/120000-byte roll ceiling,
      so no roll triggered.

      Incremented to 17 by DOC-UPDATER's own rebase of
      feature/WF03-ISS0737-20260920 onto fresh origin/main: origin/main had
      independently gained ORCH's own SCOPE-CHANGE done-event append
      (2026-09-20T04:07:37Z -- attachment-cross-tenant-probe (PW-09) GUI
      review, REQ-386/387/388 filed) since this branch's own append (below)
      was first written against volume 20 at 15 entries. The rebase produced
      a real conflict in docs/status/requirement_status.v20.yaml (both sides
      appended after the same tail entry); resolved by keeping BOTH entries,
      in file order by when each was written (origin's 04:07:37Z entry
      first, this branch's own 05:02:49Z entry second) -- append-only means
      preserving both, never dropping one to make the counts match a smaller
      number. `grep -c "^  - req:" docs/status/requirement_status.v20.yaml`
      = 17 on-disk entries, recounted after the rebase, not assumed from
      either side's own pre-rebase note. 1109 lines / 70215 bytes, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 16 by DOC-UPDATER's own ISS-0738 SCOPE-CHANGE done-event
      append (WF03-ISS0737-20260920, 2026-09-20T05:02:49Z --
      DefinitionListPage def-name click main-thread-freeze fix), before the
      rebase above composed it with origin's own concurrent append -- at
      that point in this branch's own history, `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 16 on-disk entries.

      Incremented to 15 during a merge of two independent concurrent
      appends, reconciled by ORCH: DOC-UPDATER's ISS-0729 SCOPE-CHANGE
      done-event append (WF03-ISS0729-20260920, 2026-09-20T02:40:21Z --
      ISS-0729 DefinitionListPage search-freeze fix), and ORCH's own
      SCOPE-CHANGE done-event append (2026-09-20T02:35:00Z -- REQ-382/
      REQ-383 filed, hybrid partially-built tenant-branding-applied
      scenario: read/store side done, admin write path/contrast
      validation/activity-log recording missing). Both entries are
      present on disk (DOC-UPDATER's 02:40 entry precedes ORCH's 02:35
      entry in file order, since each was appended independently on its
      own branch before this merge reconciled them -- append-only means
      preserving both, not re-sorting by timestamp) --
      `grep -c "^  - req:" docs/status/requirement_status.v20.yaml` = 15
      on-disk entries. 1000 lines / 63302 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Prior: Incremented to 13 by ORCH's own SCOPE-CHANGE done-event append
      (2026-09-20T02:10:24Z -- REQ-379/REQ-380/REQ-381 filed, unbuilt
      solution-pack update-conflict-resolution feature found during
      test/fixtures/uat/scenarios/platform/
      template-update-conflict-resolution.yaml review), on top of the prior
      increment to 12 below -- 847 lines / 54294 bytes at that time, well
      under the 1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 12 by ORCH's own SCOPE-CHANGE done-event append
      (2026-09-20T01:20:44Z -- REQ-376/REQ-377 filed, partition-based
      event-history retirement gap found during test/fixtures/uat/scenarios/
      platform/partition-retention-drop.yaml review), on top of the prior
      increment to 11 below -- `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 12 on-disk entries. 786
      lines / 50187 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 11 by ORCH's own SCOPE-CHANGE done-event append
      (2026-09-20T01:02:22Z -- REQ-374/REQ-375 filed, platform-wide tenant-
      migration fanout gap found during test/fixtures/uat/scenarios/platform/
      migration-partial-failure-resume.yaml review), on top of the prior
      increment to 10 by ORCH's own SCOPE-CHANGE done-event append
      (2026-09-20T00:00:00Z -- REQ-371 filed, operator-facing rollback GUI
      gap found during test/fixtures/uat/scenarios/platform/
      definition-promotion-rollback.yaml review), landing on top of a
      concurrent sibling-session append (below) resolved together during
      this same merge -- `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 11 on-disk entries. 716
      lines / 45611 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 9 by DOC-UPDATER's own ISS-0728 SCOPE-CHANGE done-event
      append (WF03-ISS0728-20260919, 2026-09-19T22:39:24Z -- ISS-0728
      CANDIDATE-role exam-list nav-discoverability fix) -- `grep -c
      "^  - req:" docs/status/requirement_status.v20.yaml` = 9 on-disk
      entries. 620 lines / 38642 bytes, well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered.

      Incremented to 7 by DOC-UPDATER's own ISS-0726 SCOPE-CHANGE done-event
      append (WF03-ISS0726-20260919, 2026-09-19T21:15:34Z -- ISS-0726
      hard-navigation-to-/exam OIDC redirect-path-restore fix) -- `grep -c
      "^  - req:" docs/status/requirement_status.v20.yaml` = 7 on-disk
      entries. 539 lines / 33603 bytes, well under the 1200-line/120000-byte
      roll ceiling, so no roll triggered.

      Incremented to 6 by DOC-UPDATER's own ISS-0722 SCOPE-CHANGE done-event
      append (WF03-ISS0722-20260919, 2026-09-19T07:21:51Z -- ISS-0722
      resolved, manual-mode exam question materialization fix) --
      `grep -c "^  - req:" docs/status/requirement_status.v20.yaml` = 6
      on-disk entries. 470 lines / 29103 bytes, well under the
      1200-line/120000-byte roll ceiling, so no roll triggered.

      Incremented to 5 by DOC-UPDATER's own REQ-369 done-event append
      (WF02-REQ369-AC6AC7-final3-20260919, 2026-09-19T05:39:57Z -- REQ-369
      close-out, AC6/AC7 finally satisfied) -- `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 5 on-disk entries. 432
      lines / 27069 bytes, well under the 1200-line/120000-byte roll
      ceiling, so no roll triggered.

      Incremented to 4 by ORCH's own REQ-369 blocked-event append
      (2026-09-19T02:49:52Z -- AC6/AC7 re-verification surfaced a new live
      BLOCKER, ISS-0719, filed and routed) -- `grep -c "^  - req:"
      docs/status/requirement_status.v20.yaml` = 4 on-disk entries; well
      under the 1200-line/120000-byte roll ceiling, no roll triggered.

      Prior note (1 entry): Incremented to 1 by DOC-UPDATER's own ISS-0716
      SCOPE-CHANGE done-event append (WF03-ISS0716-20260918 -- fixed
      ColumnPromotionTest AC1's concurrent-tenant-commit flake) --
      `grep -c "^  - req:" docs/status/requirement_status.v20.yaml` = 1
      on-disk entry; 232 lines / 13672 bytes, well under the
      1200-line/120000-byte roll ceiling, no roll triggered. This branch's
      own PR hit a real race with a sibling session's independent roll of
      this same volume (see this entry's own note in v20.yaml for the full
      account) -- rebuilt from a fresh origin/main checkout rather than
      hand-reconciling two divergent "closed volume 19" states.

      Prior note (0 entries): Opened by ORCH's own ISS-0690 blocked-event
      close-out, successor to volume 19 (closed at 23 entries / 1212
      frozen-prefix lines, over the 1200-line roll ceiling). Created with
      volume 19's header text verbatim (volume number, opening date and
      predecessor path updated) and an empty `history:` list, per this
      index's `roll_rule.on_exceed` procedure.


known_anomalies:
  # Entries that do not conform to the documented vocabulary and are LEFT AS
  # WRITTEN, because correcting a past entry is precisely what the append-only
  # rule forbids. Declared here so they are discoverable without a survey, and so
  # the invariant test can distinguish "known, tolerated" from "new drift".
  # The test asserts this list matches the anomalies actually on disk EXACTLY:
  # a new undocumented value fails, and a silent normalisation of one of these
  # ALSO fails.
  - path: docs/status/requirement_status.yaml
    line: 3490
    req: REQ-031
    field: event
    value: SCOPE-CHANGE
    should_have_been: "req: SCOPE-CHANGE with event: done"
    cause: docs/agents/protocols/TASK_QUEUE.md:133 stated the convention in the wrong field
  - path: docs/status/requirement_status.yaml
    line: 3577
    req: REQ-042
    field: event
    value: SCOPE-CHANGE
    should_have_been: "req: SCOPE-CHANGE with event: done"
    cause: docs/agents/protocols/TASK_QUEUE.md:133 stated the convention in the wrong field
  - path: docs/status/requirement_status.yaml
    line: 3744
    req: REQ-038
    field: event
    value: SCOPE-CHANGE
    should_have_been: "req: SCOPE-CHANGE with event: done"
    cause: docs/agents/protocols/TASK_QUEUE.md:133 stated the convention in the wrong field

known_shape_anomalies:
  # Entries whose FIELD SET or FIELD NAMES do not conform to the five-field entry
  # shape documented in the current volume's header, and which are LEFT AS
  # WRITTEN for the same reason as `known_anomalies:` above: correcting a past
  # entry is precisely what the append-only rule forbids.
  #
  # This is a SEPARATE key from `known_anomalies:` deliberately. That key declares
  # VOCABULARY violations -- an entry that HAS a field whose VALUE is not in the
  # documented list -- and is keyed {path, line, field, value}. It cannot express
  # these: a MISSING field has no line and no value to cite, and a MISNAMED
  # field's value is perfectly legal. Assertion A5 asserts set equality over
  # vocabulary violations in BOTH directions, so filing a shape defect under
  # `known_anomalies:` would turn A5 red. The two sets are disjoint by
  # construction and are asserted separately: A5 over `known_anomalies:`,
  # A4b over `known_shape_anomalies:`.
  #
  # `entry_line` is the entry's own `- req:` line. It is a stable address because
  # volume 1 is frozen and pinned by `frozen_prefix_sha256` above.
  #
  # A RECORD MAY ONLY CITE A CLOSED, DIGEST-PINNED VOLUME. `path:` must name a
  # volume whose `volumes:` entry above has `status: closed` AND a
  # `frozen_prefix_sha256:`. A4b asserts this. A shape defect in the CURRENT
  # volume must be FIXED in the working tree before it is committed -- it can
  # never be declared here. This is what keeps the list bounded: a closed
  # volume's defect set can never grow, so this list can never grow either
  # except when a volume is closed with a defect already in it.
  #
  # `kind:` is one of: missing_field | misnamed_field | extra_field | field_order.
  # Only the first two occur today. A `misnamed_field` record accounts for BOTH
  # the absence of `field:` AND the presence of `found_as:` -- it is ONE
  # violation, not two.
  #
  # Assertion A4b asserts this list equals the shape violations actually on disk
  # EXACTLY, in both directions: a new shape defect fails (drift detected), and a
  # silent "correction" of one of these fails too (a past entry was rewritten).
  # These three were found by A4 on its first ever run, 2026-08-21, under
  # ISS-0119. They had been on disk, undetected, since 2026-08-19.
  - path: docs/status/requirement_status.yaml
    entry_line: 4803
    req: REQ-053
    event: started
    kind: missing_field
    field: note
    found_as: null
    found_line: null
    should_have_been: "note: <short free-text>, as the fifth and last field"
    cause: >
      Appended with four fields instead of five. No mechanical check on entry
      shape existed at the time -- ISS-0119's assertion A4 is the first one, and
      it found this on its first run.
  - path: docs/status/requirement_status.yaml
    entry_line: 4896
    req: REQ-056
    event: started
    kind: misnamed_field
    field: at
    found_as: timestamp
    found_line: 4899
    should_have_been: 'at: "2026-08-19T02:40:35Z"'
    cause: >
      The timestamp field was written as `timestamp:` instead of `at:`. The VALUE
      is correct and is valid ISO-8601; only the field NAME is wrong. `at:` is
      the documented name and is used by the other 181 entries in this volume.
  - path: docs/status/requirement_status.yaml
    entry_line: 5050
    req: REQ-054
    event: started
    kind: missing_field
    field: note
    found_as: null
    found_line: null
    should_have_been: "note: <short free-text>, as the fifth and last field"
    cause: >
      Same as line 4803: appended with four fields instead of five, in the same
      period and by the same appending pattern.
