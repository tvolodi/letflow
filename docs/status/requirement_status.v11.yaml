# Letflow — Requirement run history — VOLUME 11 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v10.yaml (volume 10, closed,
#   1,210 frozen-prefix lines / 1,240 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v11.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v11.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v11.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v11.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-07T14:11:58Z"
    note: >
      ISS-0524 resolved via WF03-ISS0524-20260907 (queue task 524, GH#1055).
      Originally-claimed regression (wasm_hang discovery finding zero tagged
      tests after ISS-0418) was NOT reproducible -- ISSUE-FIXER confirmed all
      5 real @tag :wasm_hang sites intact and discovery working correctly;
      judged a one-off transient/environmental flake. Treated as a scoped
      diagnosability improvement instead of a behavior fix: discover_wasm_hang_tests/0
      now retries the dry-run discovery subprocess exactly once on an empty
      first result before hard-failing; the hard-fail message uses a new
      header-anchored diagnostic window (find_discovery_window/2, mirroring
      extract_test_list_block/1's own header/terminator matching) instead of
      a flat line tail -- CODE-DESIGN-VALIDATOR FAILed an early flat-40-line-tail
      design for silently dropping the real header (live-measured 232-line
      header-to-terminator span), reworked to an explicit 300-line bound with
      an explicit not-complete flag on overflow and a distinct more-severe
      signal when no header is found at all. Zero-tolerance-after-retry
      policy preserved. Pipeline: ISSUE-FIXER -> CODE-DESIGNER (1 rework) ->
      CODE-DESIGN-VALIDATOR (1 re-check, PASS) -> ELIXIR-DEV -> REVIEWER
      (PASS; SECURITY-REVIEWER out of scope, no tenant-data path) ->
      TEST-RUNNER (main suite 3422/3424 passed, 2 pre-existing unrelated
      rustc-absent failures; wasm_hang stage logic independently confirmed
      correct via direct replication since the gated local task couldn't
      reach it past the unrelated toolchain gap). Implementation commit
      b775b2d4. ORCH will merge and release queue task 524 status:done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-07T16:07:19Z"
    note: >
      ISS-0525 resolved via WF03-ISS0525-20260907 (queue task 525, GH#1060).
      ORCH's own finding while merging ISS-0524 (PR #1059): 2 test failures
      in Letflow.SandboxPoolTest (RT-9, RT-2) on real GitHub Actions CI,
      unrelated to that PR's diff. ISSUE-FIXER ruled out the originally-
      hypothesized cross-CI-job/cross-partition collision and found the real
      cause: RT-9 deliberately starts a real schema-provisioning
      Task.Supervisor.async_nolink/3 worker and only waited for it to START,
      never to FINISH; ExUnit's LIFO on_exit ordering meant its own cleanup
      raced the still-live orphaned worker's schema drop, sometimes losing
      and leaving a schema behind that corrupted RT-2's unrelated baseline
      assertion. A new manifestation of a gap ISS-0224 had explicitly named
      and declined to fix at the time, not a regression of ISS-0227 or
      ISS-0292. Fixed test-only: RT-9 now monitors the worker's real pid and
      assert_receive's its :DOWN before the test returns, closing the race
      at its source rather than stacking more retry/timeout arithmetic onto
      the cleanup helpers (explicitly avoided per ISS-0292's own history of
      3 prior FAILs for that shape of fix). No production code changed, no
      claim-release added (each test's pool is a private unregistered
      GenServer, no cross-test leak possible). Verification: 24 repeated-seed
      local runs (24/24), full test/letflow/ directory (3153/3153), REVIEWER's
      independent 5 more seeded runs (5/5), TEST-RUNNER's final gate showing
      zero SandboxPoolTest failures across all 8 partitions. Pipeline:
      ISSUE-FIXER -> CODE-DESIGNER (0 reworks) -> CODE-DESIGN-VALIDATOR
      (PASS first pass) -> ELIXIR-DEV -> REVIEWER (PASS; SECURITY-REVIEWER
      out of scope) -> TEST-RUNNER. Implementation commit ae3906dd. ORCH
      will merge and release queue task 525 status:done.
  - req: SCOPE-CHANGE
    event: revised
    agent: ORCH
    at: "2026-09-07T17:41:24Z"
    note: >
      ISS-0393 (queue task 393, GH#773) closed as not-applicable-as-scoped
      via WF03-ISS0393-20260907. The user provided R-Co's real GitHub
      remote (https://github.com/tvolodi/R-Co, commit d19bfec4a9ec49234534
      6bfccd8e389bd9cd92db), resolving the prior Windows-filesystem-path
      unreachability, but ISS-0393's own premise -- that R-Co's real
      tests/simulation/scenarios/*.yaml corpus is a verbatim drop-in
      replacement for REQ-206/207/208's synthetic scenario fixtures --
      does not hold. ELIXIR-DEV diffed all 11 real/synthetic file pairs
      and found the real files are narrative UAT scripts (prose fields,
      pipeline_test: keys pointing at Playwright specs for UAT-RUNNER),
      incompatible with Letflow.Simulation.Runner.run/1's params/args-driven
      machine-executable format (ScenarioFixture.load!/1's single
      build_outcome/1 clause requires verification.args, absent from every
      real file) -- made zero file changes and correctly escalated rather
      than inventing content to force a fit. ORCH presented the finding to
      the user, who chose to keep the existing synthetic fixtures as-is
      and split the real R-Co narrative content into a correctly-scoped
      follow-up. Filed ISS-0526 (queue task 526, GH#1062) targeting
      UAT-RUNNER's domain instead. REQ-206/207/208's design docs corrected
      (append-only) to remove the implied expectation of a straightforward
      future port. REVIEWER independently re-verified the core technical
      claim against real code (scenario_fixture.ex:97-100) and real R-Co
      content (spot-checked swiftroute-shipment-attach-delivery-note.yaml
      directly against the cited commit sha) -- PASS. Docs-only change,
      mix compile --warnings-as-errors clean, mix test
      test/letflow/simulation/ 27 passed (confirms true no-op on code).
      ORCH will merge and release queue task 393 status:done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-07T18:17:02Z"
    note: >
      ISS-0526 resolved via WF03-ISS0526-20260907 (queue task 526, GH#1062),
      the correctly-scoped successor to ISS-0393. Ported R-Co's 11 named
      tenant-business narrative UAT scenario files (4 SwiftRoute, 4 Vortex,
      3 Meridian) verbatim into test/fixtures/uat/scenarios/<company>/*.yaml,
      a new destination distinct from test/fixtures/simulation/'s existing
      synthetic Runner-executed fixtures (confirmed completely untouched).
      Each ported file carries a provenance header (URL+commit sha, no local
      path) matching ISS-0388's established convention. pipeline_test: kept
      verbatim in all 3 files carrying it; the 2 whose referenced Playwright
      spec doesn't exist yet got an added NOTE (ISS-0526) comment rather than
      silent stripping. .claude/agents/uat-runner.md and
      docs/agents/workflows/WF-05_uat_run.md updated to resolve their own
      previously-explicit "location TBD" language and correctly contrast
      this narrative format against Letflow.Simulation.Runner's unrelated
      machine-executable fixtures. Pipeline: CODE-DESIGNER (PASS first pass)
      -> CODE-DESIGN-VALIDATOR (PASS first pass) -> ELIXIR-DEV -> REVIEWER
      (PASS; SECURITY-REVIEWER out of scope) -> TEST-RUNNER (3431/3433
      passed, 2 pre-existing unrelated rustc-absent failures, zero
      SandboxPoolTest failures confirming ISS-0525's already-merged fix
      holds). Implementation commit 65c4618b. The 18-file platform/ corpus
      explicitly kept out of scope (per REQ-210's own already-recorded
      decision) and filed as follow-up ISS-0527 (queue task 527, GH#1064).
      ORCH will merge and release queue task 526 status:done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-07T18:51:59Z"
    note: >
      ISS-0527 resolved via WF03-ISS0527-20260907 (queue task 527, GH#1064),
      the follow-up ISS-0526 recommended for R-Co's platform-operator UAT
      corpus. Ported all 18 R-Co tests/simulation/scenarios/platform/*.yaml
      files verbatim into test/fixtures/uat/scenarios/platform/*.yaml,
      reusing ISS-0526's already-validated design pattern (destination
      convention, provenance header, pipeline_test: verbatim-plus-NOTE
      handling) without a fresh CODE-DESIGNER/CODE-DESIGN-VALIDATOR pass.
      Corrected the issue's own "~7" pipeline_test: estimate to the real
      count of 12, none resolving to an existing spec in either R-Co's or
      letflow's web tree, each annotated. Two files flagged for
      SECURITY-REVIEWER per the issue's own acceptance criteria:
      attachment-cross-tenant-probe.yaml confirmed a sound UAT target
      already matching Letflow's implemented INV-1 cross-tenant-404
      mechanism; sandbox-cross-tenant-probe.yaml found to target R-Co's own
      unbuilt runtime-agent subsystem with narrative terminology that could
      be mis-mapped onto the semantically-unrelated Letflow.SandboxPool
      module by a future UAT-RUNNER invocation -- a clarifying NOTE was
      applied directly (non-blocking finding, mechanical fix) disclaiming
      the relationship and marking the scenario BLOCKED/UNBUILT_FEATURE.
      .claude/agents/uat-runner.md and docs/agents/workflows/WF-05_uat_run.md
      updated to reflect the platform/ corpus now being present (no longer
      "deferred") plus a security-severity note on the two probe scenarios.
      test/fixtures/simulation/ and ISS-0526's previously-ported
      swiftroute/vortex/meridian directories confirmed completely untouched
      throughout. Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER (PASS, applied
      fix) -> REVIEWER (PASS) -> TEST-RUNNER (3431/3433 passed, 2
      pre-existing unrelated rustc-absent failures, zero SandboxPoolTest
      failures confirming ISS-0525's fix continues to hold). Implementation
      commits ec21d779, 1f48b630. Does not re-decide REQ-210's original
      platform-corpus scoping rationale. ORCH will merge and release queue
      task 527 status:done. This closes the R-Co UAT-scenario-porting
      thread opened by ISS-0393 (S7 backlog).
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-07T19:35:36Z"
    note: >
      ISS-0391 (queue task 391, GH#770) closed as resolved-via-duplicate,
      per its own pre-recorded duplicate_note instructing whichever of
      ISS-0391/ISS-0393 got picked up first to close the other. ISS-0393
      was picked up first this session; by the time R-Co became reachable,
      ISS-0393's own verbatim-port premise (and therefore ISS-0391's
      identical request) was found false and closed as not-applicable-as-
      scoped, with the real content instead ported via ISS-0526/ISS-0527
      into test/fixtures/uat/scenarios/. Direct-action doc-only change per
      ORCHESTRATOR.md SS10's sizing rule (single file, no code/test-behavior
      change) -- branch/PR/merge still followed GIT_MERGE.md's procedure.
      Merged as PR #1067. This closes the last open item in the R-Co
      UAT-scenario-porting thread.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-08T08:40:30Z"
    note: >
      ISS-0535 resolved via WF03-ISS0535-20260908 (queue task 535, GH#1078).
      Environment-only fix, no application code change. Root cause: two stacked
      environmental issues -- (1) letflow-2-postgres-1 (shared Postgres, port
      5472) had reached max_connections=100 due to letflow-2's idle test pool
      connections (~70-80 across letflow_test2/3/4), blocking mix ecto.create
      with FATAL 53300 too_many_connections; (2) the original
      versions_missing=[20260907020001] symptom was the stale-tenant_template
      class already documented in docs/anti-patterns.md, already cleaned up on
      this host by ISS-0399's own run (tenant_template.schema_migrations already
      contains 20260907020001 -- confirmed direct read). @tenant_scoped_migration_manifest
      in lib/letflow/tenant_provisioning.ex already includes 20260907020001 as its
      last entry -- no manifest registration gap existed. Fix applied: ALTER SYSTEM
      SET max_connections = 300 in letflow-2-postgres-1 followed by container
      restart; setting is persisted via postgresql.auto.conf inside the container
      volume. Verified: mix test test/letflow/plugs/admission_pipeline_test.exs
      passes 8/8 post-fix. No handoff subagent chain (no code change). ORCH will
      merge and release queue task 535 status:done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-08T09:52:26Z"
    note: >
      ISS-0534 resolved via WF03-ISS0534-20260908 (queue task 534, GH#1077),
      found by a sibling session's live-browser audit (ADHOC-ENGINE-AUDIT-
      20260908). Fixed lib/letflow/plugs/auth_pipeline.ex's handle_auth_error/2:
      the {:error, {:provision, _reason}} clause discarded its error reason
      entirely, so a 500 "user provisioning failed" was undiagnosable from
      server output. Added one Logger.error call (via inspect/1) logging the
      reason before the unchanged 500 response -- trace_id auto-attached by
      Letflow.Obs.Logger, no manual threading needed. ISSUE-FIXER traced
      every reachable reason shape (an open union including :external_identity_collision,
      Ecto.Changeset.t(), a plug-local :invalid_tenant_id atom) confirming a
      naive changeset-only fix would crash on the atom cases. SECURITY-REVIEWER
      confirmed via User.jit_changeset/2 that no reachable shape can carry a
      raw credential (password_hash is a fixed sentinel, never caller attrs)
      -- INV-4 PASS. Explicitly did not reclassify the 500 status code
      (would silently reverse req021-auth-plug-pipeline.md's settled OQ-9
      decision) or add an unrelated Keycloak-volume-reset doc note. Pipeline:
      ISSUE-FIXER -> ELIXIR-DEV -> SECURITY-REVIEWER (PASS) -> REVIEWER (PASS)
      -> TEST-RUNNER. TEST-RUNNER's first full-suite attempt surfaced a much
      larger pre-existing environmental problem on THIS host (letflow-1-postgres-1,
      a distinct host from ISS-0535's own letflow-2/letflow-3 connection-exhaustion
      finding above): 745 failures from 25 stale letflow_test* Postgres
      databases predating ISS-0399's migration, a second recurrence of the
      stale-tenant_template class WF03-ISS0399-20260907 already hit and
      documented in anti-patterns.md. ORCH obtained explicit user approval
      (auto-mode classifier blocks DROP DATABASE) to drop the 25 stale test
      databases (never letflow_dev), confirmed via clean re-run: 3448/3450
      passed, only the known unrelated rustc-absent CheckToolchainTest
      failures remaining, zero AuthPipelineTest regressions. Filed ISS-0535
      (queue task 535, GH#1078) for this class before discovering the sibling's
      own independent, differently-rooted fix for the same GH issue had
      already landed (composed both root causes into ISS-0535's own record
      on rebase rather than overwriting either); appended a recurrence note
      to anti-patterns.md's existing entry rather than duplicating it.
      Implementation commit 76f3bf5c. ORCH will merge and release queue task
      534 status:done.
