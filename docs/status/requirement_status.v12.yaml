# Letflow — Requirement run history — VOLUME 12 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v11.yaml (volume 11, closed,
#   1,212 frozen-prefix lines / 1,238 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v12.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v12.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v12.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v12.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: REQ-285
    event: done
    agent: ORCH
    at: "2026-09-09T07:51:58Z"
    note: >
      REQ-285 (queue task 555, GH#1128) implemented via
      WF02-REQ285-20260909: adopts react-intl as web/'s i18n layer
      (decision record docs/migration/decisions/0021-web-i18n-library.md,
      chosen over i18next -- most .toLocale*() call sites are module-scope
      helper functions, not JSX-inline, so react-intl's imperative
      createIntl()/IntlShape API reaches them without restructuring call
      signatures; i18next has no native date-formatter of its own,
      leaving it with zero load-bearing usage until a future translation
      requirement -- the same "partial subsystem, no producer" shape
      this project's own decisions already reject elsewhere). Re-verified
      REQ-127's 2026-08-22 survey rather than inheriting it: counts
      drifted from 25/27/20 to 25 grep-matched lines / 26 actual
      invocations / 19 files (a UsersPage.tsx consolidation). All 26
      converted, 0 left bare (26=26). The hardcoded 'en-US' site (now at
      UsersPage.tsx:91 after the consolidation) no longer hardcodes a
      locale. New fallback chain (web/src/i18n/sessionLocale.ex): tenant
      default_locale > browser preference > hardcoded "en" (not
      "en-US"), 10 curated PLATFORM_SUPPORTED_LOCALES, 5 testable cases
      (exceeds AC3's minimum 3). A genuine, honestly-flagged gap: no
      current requirement wires a tenant's default_locale to the web SPA
      (REQ-281's endpoint excludes locale data by design; REQ-282's
      mobile endpoint doesn't reach the web SPA; REQ-283 is CSS/branding
      only) -- shipped with tenantDefaultLocale defaulting to null in
      production (browser-preference and hardcoded-fallback tiers only,
      in practice) but the mechanism is fully testable today via an
      injectable tenant-default value, documented as an open dependency
      for a future requirement rather than a silent workaround. Design
      needed 1 rework cycle: CODE-DESIGN-VALIDATOR caught a real
      self-contradicting factual error (a claim that the frontend
      TenantConfig interface already gained a branding key "since
      REQ-281," when REQ-281 only changed the backend response shape --
      the design's own later section correctly attributed frontend
      consumption to REQ-283's still-pending scope). Pipeline:
      CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (1 rework, re-check PASS) ->
      FRONTEND-DEV -> REVIEWER (PASS -- independently re-ran the
      completeness grep for all three toLocale* variants across web/src/
      and confirmed zero remaining hits, not just trusting the reported
      26=26 claim; confirmed react-intl's imperative API is genuinely
      used at module scope as the decision record's own reasoning
      requires, no divergence toward hooks-everywhere that would have
      undermined it) -> TEST-RUNNER (backend mix compile confirmed zero
      impact; frontend gates independently re-run: 310/310 unit tests,
      48/48 guard tests). SECURITY-REVIEWER out of scope (pure frontend,
      no tenant-data path). Policy doc (docs/frontend/frontend-requirements.md)
      states explicitly the app ships English-only strings by design and
      translating UI strings is a separate later requirement, per AC7.
      ORCH will merge and release queue task 555 status:done.

  - req: REQ-286
    event: done
    agent: ORCH
    at: "2026-09-09T08:28:43Z"
    note: >
      REQ-286 (queue task 556, GH#1129) implemented via
      WF02-REQ286-20260909: exposes REQ-273's write-once tasks.form_schema
      column as a 14th key in lib/letflow/routers/tasks.ex's
      task_detail_map/3 -- response-shaping on a tenant data path, under
      a security gate against INV-1/INV-2/INV-5. Mechanism: a single
      Map.put reading task.form_schema directly off the already-fetched,
      already-scoped struct -- no new query/join. Deliberately scoped to
      task_detail_map/3 only, not task_list_item_map/2 (no AC exercises
      list/inbox routes). Version-pinning (AC3) proven automatic rather
      than built: grepped every changeset touching Letflow.Engine.Task
      and confirmed only insert_changeset/2 ever casts :form_schema --
      complete_changeset/2 and assignment_changeset/2 don't, and no
      fourth changeset or raw Repo.update exists anywhere in the repo --
      so reading the column back IS reading the pinned value, with
      nothing to build. CODE-DESIGNER additionally found and fixed a
      second stale moduledoc reference (a comment above
      task_list_item_map/2) the requirement's own AC5 didn't name but
      said the same false thing. SECURITY-REVIEWER named all three
      invariants explicitly per AC4: INV-1 (tenant isolation) traced
      end-to-end -- Authorize plug derives prefix server-side from
      auth_context.tenant_id, never caller input; tasks has no tenant_id
      column (schema-per-tenant, decision 0006 D2) -- confirming
      form_schema can never cross tenant boundaries; INV-2 (field
      authorization) confirmed via the same already-scoped struct, no
      new fetch path; INV-5 (no oracle) confirmed the not-found branch is
      untouched, the new Map.put only runs on the success path.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no rework
      -- independently re-verified the version-pinning-is-automatic
      proof and the full cross-tenant leak trace against real source) ->
      ELIXIR-DEV -> SECURITY-REVIEWER (PASS) -> REVIEWER (PASS --
      independently reproduced 56/56, confirmed both moduledoc rewrites
      read consistently with each other) -> TEST-RUNNER (full suite
      3544/3546 passed, 2 pre-existing rustc-absent failures unrelated).
      Merge-time bookkeeping: flipping REQ-286 to done expired two
      sibling blocked-by deferrals (REQ-284 and REQ-292, both scoped to
      REQ-286). REQ-284's OTHER dependency (REQ-273) was also already
      done, leaving it with no live blocker at all -- registered it
      properly in the queue (task 560, GH#1145) rather than inventing a
      fake re-scope target. REQ-292 still has two live dependencies
      (REQ-290, REQ-291) -- re-scoped its deferral to REQ-291, not
      REQ-290, specifically to avoid creating a blocked-by cycle (REQ-290's
      own pre-existing deferral is separately scoped to REQ-292 itself,
      as a workaround for REQ-289 being absent from this file -- caught
      by mix letflow.check_deferral_staleness's own cycle detector on
      the first attempt, fixed before it ever left this machine). ORCH
      will merge and release queue task 556 status:done.

  - req: REQ-284
    event: done
    agent: ORCH
    at: "2026-09-09T09:16:18Z"
    note: >
      REQ-284 (queue task 560, GH#1145) implemented via
      WF02-REQ284-20260909: a closed x-ui.widget vocabulary (5 widgets --
      rich-text-lite, masked-input, searchable-select, rating, slider)
      documented in docs/frontend/x-ui-widget-vocabulary.md and
      registered into web/src/components/forms/fieldRegistry.ts (was an
      empty Map). Deliberately small set: each covers an interaction
      design-system.md section 7.6's built-in type-to-input table
      structurally cannot express, each needs zero backend dependency
      (a file-upload/remote-search widget was excluded for exactly that
      reason), and none touch scripting/visibility logic (deferred, not
      rejected, to decision 0020 D1a's separate track). CODE-DESIGNER
      surfaced a real gap during design: web/src/components/forms/
      DynamicFormRenderer.tsx does NOT call FieldFactory/fieldRegistry
      today -- it has its own duplicate inline switch -- so these
      widgets are not yet reachable from the real task-completion UI.
      CODE-DESIGN-VALIDATOR independently confirmed this is real (traced
      the actual imports/call graph) but correctly judged it a
      clearly-flagged, scope-compliant limitation rather than a FAIL:
      none of REQ-284's 8 ACs require that wiring, and the requirement's
      own scope fence anticipated a pipeline-boundary question. Also
      required and delivered: a keying-mechanism fix (FieldFactory.tsx
      switched from looking up fieldRegistry by JSON-Schema type to a
      new, additive TaskFormField.xUiWidget field, matching decision
      0020 D3's actual "keyed by an x-ui.widget name" wording) --
      confirmed additive/minimal by both CODE-DESIGN-VALIDATOR and
      REVIEWER, not a bigger pipeline change than the scope fence
      permits. REVIEWER read all 5 widget implementations directly
      (not just the reported grep) specifically to rule out an
      injection risk in rich-text-lite (the classic "rich text" trap) --
      confirmed it's a plain textarea whose toolbar only splices literal
      markdown-lite token strings into a plain string, never parses or
      renders anything as HTML; no eval/new Function/dangerouslySetInnerHTML/
      innerHTML anywhere in any of the 5 widgets. Pipeline: CODE-DESIGNER
      -> CODE-DESIGN-VALIDATOR (PASS, no rework) -> FRONTEND-DEV ->
      REVIEWER (PASS -- independently read all 5 widgets for injection
      risk, confirmed ARIA contracts against actual rendered DOM not
      just test assertions) -> TEST-RUNNER (backend mix compile
      confirmed zero impact; frontend gates independently re-run:
      320/320 unit tests, 48/48 guard tests). SECURITY-REVIEWER out of
      scope (pure frontend, no tenant-data path). Merge-time bookkeeping:
      flipping REQ-284 to done expired two sibling blocked-by deferrals
      (REQ-291, REQ-293). REQ-291 had no live blocker left at all (its
      other dependency, REQ-288, was already done) -- registered it
      properly in the queue (task 561, GH#1147) rather than a fake
      re-scope. REQ-293 still has two live dependencies (REQ-290,
      REQ-292) -- re-scoped to REQ-290, verified no cycle. Also updated
      test/mix/tasks/letflow_check_deferral_staleness_test.exs's
      T-LIVE-DEFERRED-COUNT assertions (9 -> 8, and the exact id list)
      to match the real, re-derived post-merge state -- caught locally
      before pushing this time, having been caught by CI itself on the
      REQ-286 merge two turns ago. ORCH will merge and release queue
      task 560 status:done.
