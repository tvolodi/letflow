# Letflow — Requirement run history — VOLUME 12 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v11.yaml (volume 11, closed,
#   1,212 frozen-prefix lines / 1,238 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v12.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v12.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v12.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v12.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: REQ-285
    event: done
    agent: ORCH
    at: "2026-09-09T07:51:58Z"
    note: >
      REQ-285 (queue task 555, GH#1128) implemented via
      WF02-REQ285-20260909: adopts react-intl as web/'s i18n layer
      (decision record docs/migration/decisions/0021-web-i18n-library.md,
      chosen over i18next -- most .toLocale*() call sites are module-scope
      helper functions, not JSX-inline, so react-intl's imperative
      createIntl()/IntlShape API reaches them without restructuring call
      signatures; i18next has no native date-formatter of its own,
      leaving it with zero load-bearing usage until a future translation
      requirement -- the same "partial subsystem, no producer" shape
      this project's own decisions already reject elsewhere). Re-verified
      REQ-127's 2026-08-22 survey rather than inheriting it: counts
      drifted from 25/27/20 to 25 grep-matched lines / 26 actual
      invocations / 19 files (a UsersPage.tsx consolidation). All 26
      converted, 0 left bare (26=26). The hardcoded 'en-US' site (now at
      UsersPage.tsx:91 after the consolidation) no longer hardcodes a
      locale. New fallback chain (web/src/i18n/sessionLocale.ex): tenant
      default_locale > browser preference > hardcoded "en" (not
      "en-US"), 10 curated PLATFORM_SUPPORTED_LOCALES, 5 testable cases
      (exceeds AC3's minimum 3). A genuine, honestly-flagged gap: no
      current requirement wires a tenant's default_locale to the web SPA
      (REQ-281's endpoint excludes locale data by design; REQ-282's
      mobile endpoint doesn't reach the web SPA; REQ-283 is CSS/branding
      only) -- shipped with tenantDefaultLocale defaulting to null in
      production (browser-preference and hardcoded-fallback tiers only,
      in practice) but the mechanism is fully testable today via an
      injectable tenant-default value, documented as an open dependency
      for a future requirement rather than a silent workaround. Design
      needed 1 rework cycle: CODE-DESIGN-VALIDATOR caught a real
      self-contradicting factual error (a claim that the frontend
      TenantConfig interface already gained a branding key "since
      REQ-281," when REQ-281 only changed the backend response shape --
      the design's own later section correctly attributed frontend
      consumption to REQ-283's still-pending scope). Pipeline:
      CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (1 rework, re-check PASS) ->
      FRONTEND-DEV -> REVIEWER (PASS -- independently re-ran the
      completeness grep for all three toLocale* variants across web/src/
      and confirmed zero remaining hits, not just trusting the reported
      26=26 claim; confirmed react-intl's imperative API is genuinely
      used at module scope as the decision record's own reasoning
      requires, no divergence toward hooks-everywhere that would have
      undermined it) -> TEST-RUNNER (backend mix compile confirmed zero
      impact; frontend gates independently re-run: 310/310 unit tests,
      48/48 guard tests). SECURITY-REVIEWER out of scope (pure frontend,
      no tenant-data path). Policy doc (docs/frontend/frontend-requirements.md)
      states explicitly the app ships English-only strings by design and
      translating UI strings is a separate later requirement, per AC7.
      ORCH will merge and release queue task 555 status:done.

  - req: REQ-286
    event: done
    agent: ORCH
    at: "2026-09-09T08:28:43Z"
    note: >
      REQ-286 (queue task 556, GH#1129) implemented via
      WF02-REQ286-20260909: exposes REQ-273's write-once tasks.form_schema
      column as a 14th key in lib/letflow/routers/tasks.ex's
      task_detail_map/3 -- response-shaping on a tenant data path, under
      a security gate against INV-1/INV-2/INV-5. Mechanism: a single
      Map.put reading task.form_schema directly off the already-fetched,
      already-scoped struct -- no new query/join. Deliberately scoped to
      task_detail_map/3 only, not task_list_item_map/2 (no AC exercises
      list/inbox routes). Version-pinning (AC3) proven automatic rather
      than built: grepped every changeset touching Letflow.Engine.Task
      and confirmed only insert_changeset/2 ever casts :form_schema --
      complete_changeset/2 and assignment_changeset/2 don't, and no
      fourth changeset or raw Repo.update exists anywhere in the repo --
      so reading the column back IS reading the pinned value, with
      nothing to build. CODE-DESIGNER additionally found and fixed a
      second stale moduledoc reference (a comment above
      task_list_item_map/2) the requirement's own AC5 didn't name but
      said the same false thing. SECURITY-REVIEWER named all three
      invariants explicitly per AC4: INV-1 (tenant isolation) traced
      end-to-end -- Authorize plug derives prefix server-side from
      auth_context.tenant_id, never caller input; tasks has no tenant_id
      column (schema-per-tenant, decision 0006 D2) -- confirming
      form_schema can never cross tenant boundaries; INV-2 (field
      authorization) confirmed via the same already-scoped struct, no
      new fetch path; INV-5 (no oracle) confirmed the not-found branch is
      untouched, the new Map.put only runs on the success path.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no rework
      -- independently re-verified the version-pinning-is-automatic
      proof and the full cross-tenant leak trace against real source) ->
      ELIXIR-DEV -> SECURITY-REVIEWER (PASS) -> REVIEWER (PASS --
      independently reproduced 56/56, confirmed both moduledoc rewrites
      read consistently with each other) -> TEST-RUNNER (full suite
      3544/3546 passed, 2 pre-existing rustc-absent failures unrelated).
      Merge-time bookkeeping: flipping REQ-286 to done expired two
      sibling blocked-by deferrals (REQ-284 and REQ-292, both scoped to
      REQ-286). REQ-284's OTHER dependency (REQ-273) was also already
      done, leaving it with no live blocker at all -- registered it
      properly in the queue (task 560, GH#1145) rather than inventing a
      fake re-scope target. REQ-292 still has two live dependencies
      (REQ-290, REQ-291) -- re-scoped its deferral to REQ-291, not
      REQ-290, specifically to avoid creating a blocked-by cycle (REQ-290's
      own pre-existing deferral is separately scoped to REQ-292 itself,
      as a workaround for REQ-289 being absent from this file -- caught
      by mix letflow.check_deferral_staleness's own cycle detector on
      the first attempt, fixed before it ever left this machine). ORCH
      will merge and release queue task 556 status:done.

  - req: REQ-284
    event: done
    agent: ORCH
    at: "2026-09-09T09:16:18Z"
    note: >
      REQ-284 (queue task 560, GH#1145) implemented via
      WF02-REQ284-20260909: a closed x-ui.widget vocabulary (5 widgets --
      rich-text-lite, masked-input, searchable-select, rating, slider)
      documented in docs/frontend/x-ui-widget-vocabulary.md and
      registered into web/src/components/forms/fieldRegistry.ts (was an
      empty Map). Deliberately small set: each covers an interaction
      design-system.md section 7.6's built-in type-to-input table
      structurally cannot express, each needs zero backend dependency
      (a file-upload/remote-search widget was excluded for exactly that
      reason), and none touch scripting/visibility logic (deferred, not
      rejected, to decision 0020 D1a's separate track). CODE-DESIGNER
      surfaced a real gap during design: web/src/components/forms/
      DynamicFormRenderer.tsx does NOT call FieldFactory/fieldRegistry
      today -- it has its own duplicate inline switch -- so these
      widgets are not yet reachable from the real task-completion UI.
      CODE-DESIGN-VALIDATOR independently confirmed this is real (traced
      the actual imports/call graph) but correctly judged it a
      clearly-flagged, scope-compliant limitation rather than a FAIL:
      none of REQ-284's 8 ACs require that wiring, and the requirement's
      own scope fence anticipated a pipeline-boundary question. Also
      required and delivered: a keying-mechanism fix (FieldFactory.tsx
      switched from looking up fieldRegistry by JSON-Schema type to a
      new, additive TaskFormField.xUiWidget field, matching decision
      0020 D3's actual "keyed by an x-ui.widget name" wording) --
      confirmed additive/minimal by both CODE-DESIGN-VALIDATOR and
      REVIEWER, not a bigger pipeline change than the scope fence
      permits. REVIEWER read all 5 widget implementations directly
      (not just the reported grep) specifically to rule out an
      injection risk in rich-text-lite (the classic "rich text" trap) --
      confirmed it's a plain textarea whose toolbar only splices literal
      markdown-lite token strings into a plain string, never parses or
      renders anything as HTML; no eval/new Function/dangerouslySetInnerHTML/
      innerHTML anywhere in any of the 5 widgets. Pipeline: CODE-DESIGNER
      -> CODE-DESIGN-VALIDATOR (PASS, no rework) -> FRONTEND-DEV ->
      REVIEWER (PASS -- independently read all 5 widgets for injection
      risk, confirmed ARIA contracts against actual rendered DOM not
      just test assertions) -> TEST-RUNNER (backend mix compile
      confirmed zero impact; frontend gates independently re-run:
      320/320 unit tests, 48/48 guard tests). SECURITY-REVIEWER out of
      scope (pure frontend, no tenant-data path). Merge-time bookkeeping:
      flipping REQ-284 to done expired two sibling blocked-by deferrals
      (REQ-291, REQ-293). REQ-291 had no live blocker left at all (its
      other dependency, REQ-288, was already done) -- registered it
      properly in the queue (task 561, GH#1147) rather than a fake
      re-scope. REQ-293 still has two live dependencies (REQ-290,
      REQ-292) -- re-scoped to REQ-290, verified no cycle. Also updated
      test/mix/tasks/letflow_check_deferral_staleness_test.exs's
      T-LIVE-DEFERRED-COUNT assertions (9 -> 8, and the exact id list)
      to match the real, re-derived post-merge state -- caught locally
      before pushing this time, having been caught by CI itself on the
      REQ-286 merge two turns ago. ORCH will merge and release queue
      task 560 status:done.

  - req: REQ-277
    event: started
    agent: ORCH
    at: "2026-09-09T03:00:25Z"
    note: >
      WF02-REQ277-20260909 started (queue task 551, GH#1124). Migrate
      web/src/pages/definitions/ and web/src/pages/dlq/ onto the
      design-system primitives (DataTable/Button/StatusBadge/PaginationControls)
      and off colour literals, per decision 0020 D2b, 2 of 3. Second of three
      sibling page-migration requirements. docs/requirements.yaml entry was
      missing until this run's Step 00 -- same registration-gap class as
      REQ-272/275/287. ORCH added the entry (status: in_progress) before
      proceeding to Step 1 (CODE-DESIGNER). Recorded here (volume 12) rather
      than volume 11 despite the original 03:00:25Z timestamp: this event's
      home commit (a7c0396d) was authored on feature/WF02-REQ277-20260909
      back when volume 11 was still current and open, but that branch stalled
      before Step Final and volume 11 closed (frozen, DO NOT APPEND) under
      REQ-282's merge in the interim. Rebasing this branch onto current main
      at Step 00b (2026-09-09T12:00:54Z) surfaced the conflict; per the
      closed volume's own header this append routes to whichever volume the
      index names current at append time, which is this one.

  - req: REQ-277
    event: done
    agent: DOC-UPDATER
    at: "2026-09-09T03:52:24Z"
    note: >
      WF02-REQ277-20260909 done (queue task 551, GH#1124). Migrated
      web/src/pages/definitions/ and web/src/pages/dlq/ onto the design-system
      primitives (DataTable/Button/StatusBadge/PaginationControls), eliminating
      201 literal-colour occurrences across 4 files in favor of var(--token-name)
      from web/src/styles/tokens.css, per decision 0020 D2b, 2 of 3. Hand-rolled
      tables, buttons, status pills, and pagination controls in both directories
      replaced by the matching REQ-272/274/275/287 primitives; JSON displays each
      resolved to a named disposition (JsonDiffView/JsonEditor/hand-rolled with
      reason). DlqPage's pagination was adapted from its prior cursor-based
      scheme to PaginationControls' page-based API. Pipeline: CODE-DESIGNER
      (2 rework rounds) -> CODE-DESIGN-VALIDATOR -> FRONTEND-DEV ->
      SECURITY-REVIEWER -> REVIEWER -> TEST-DESIGNER -> TEST-DESIGN-VALIDATOR ->
      TEST-RUNNER -> RELEASE-VALIDATOR (independently re-confirmed all 7
      acceptance criteria, including re-reading the DlqPage pagination
      adaptation code directly, PASS). One incidental issue filed, not fixed
      here: ISS-0559 (GH#1135, Button title/pressed-state gap).
      web/tests/guards/forbidlist.ts confirmed unmodified. docs/requirements.yaml
      flipped in_progress -> done. README.md checked for definitions/dlq page
      or design-system-primitive references (none found at this level of
      detail) and left unchanged. This is the second of three sibling
      page-migration requirements (0020 D2b) -- REQ-276 and REQ-278 remain
      (both since landed on main: see this same volume's REQ-284/285/286
      entries and volume 11's REQ-278/281/282 entries -- REQ-277 is now the
      last of the three to land). Recorded here (volume 12) for the same
      reason the started-event above was: this event's home commit
      (7f459464) was authored while volume 11 was still current, but volume
      11 closed before this branch reached Step Final; redirected to the
      current volume at rebase time (WF02-REQ277-20260909 Step 00b,
      2026-09-09T12:03:52Z). Next: route to FRONTEND-DEV for Step Final
      (git merge) -- this rebase/re-verify (Step 00b) is that continuation.
  - req: REQ-291
    event: done
    agent: ORCH
    at: "2026-09-09T12:53:41Z"
    note: >
      REQ-291 (queue task 561, GH#1147) implemented via
      WF02-REQ291-20260909: extends REQ-284's x-ui vocabulary doc with
      visible_when, computed and cross-field validation logic keys
      (0020 D1a step 8, part 2 of 2), validated entirely at definition
      time -- no evaluator (that is REQ-292 Elixir/REQ-293 TypeScript/
      REQ-294 Dart, all explicitly out of scope here). New leaf module
      Letflow.Definitions.FormSchemaExpressions and a new CHK-20 in
      Graph.validate_node_attributes/1, mirroring CHK-18's
      check_sub_process_interface/1 pattern exactly; 4 new additive
      Violation.code() atoms. Semantics pinned so REQ-292/293/294 cannot
      silently diverge: no "variables." prefix (bare sibling top-level
      property names only, any dotted path out of scope); a computed
      field's absent/null input always evaluates to nil (a single stated
      value, not a range); both a hidden field's value and a computed
      field's value ARE submitted on submit (per 0020 D1a constraint 3 --
      the server re-validates/recomputes, never stores on trust);
      computed-field reference cycles of any length rejected at
      definition time via DFS with an on-stack set (self-reference,
      2-node and 3-node cycles all covered by separate tests, plus a
      diamond-DAG negative test proving no false positive). Security
      boundary stated in the vocabulary doc's own words, citing 0020
      D1a: client-side hiding is a UX affordance only, never access
      control; a field whose visibility is security-relevant must be
      omitted from the schema server-side. Pipeline: CODE-DESIGNER ->
      CODE-DESIGN-VALIDATOR (PASS, no rework) -> ELIXIR-DEV ->
      REVIEWER (PASS -- independently hand-traced the DFS cycle
      detector for the on-stack-vs-visited bug class, confirmed absent;
      independently re-ran mix format/compile/test). SECURITY-REVIEWER
      out of scope (definition-time validation only, no tenant-data
      response-shaping, no API route, no migration). Both hard scope
      fences confirmed via git diff: lib/letflow/engine/expr.ex
      untouched, no file under web/ touched. Full suite (re-run
      independently by ORCH in a clean, uncontended state after
      REVIEWER's own run hit database contention from a concurrent
      test process): 3582/3584 passed, 2 pre-existing rustc-absent
      CheckToolchainTest failures unrelated to this branch. Merge-time
      bookkeeping: flipping REQ-291 to done expired REQ-292's
      blocked-by: REQ-291 deferral. REQ-292's only remaining live
      dependency is REQ-290, but REQ-290's own deferral was scoped to
      REQ-292 as a workaround for its true dependency, REQ-289, being
      entirely absent from docs/requirements.yaml (a genuine
      registration gap, same class as REQ-272/275/287 -- REQ-289 is
      queue task 544/GH#1100 with real implementation work sitting
      unmerged on origin/feature/WF02-REQ289-20260909, which predates
      several requirements merged to main since and needs a rebase).
      Registered REQ-289 as a `pending` entry (matching reality --
      unmerged, not done) so REQ-290 could point directly at its real
      blocker, freeing REQ-292 to point at REQ-290 without forming a
      cycle: new chain REQ-292 -> REQ-290 -> REQ-289 (linear, acyclic).
      Updated test/mix/tasks/letflow_check_deferral_staleness_test.exs's
      T-LIVE-DEFERRED-COUNT comment to document the re-derivation (count
      and id set both happened to stay unchanged at 8 -- only internal
      scope targets moved), confirmed live via the standard
      MIX_ENV=test mix run --no-start -e audit call before pushing.
      ORCH will merge and release queue task 561 status:done.
  - req: REQ-279
    event: done
    agent: ORCH
    at: "2026-09-09T15:39:05Z"
    note: >
      REQ-279 (queue task 562, GH#1151) implemented via
      WF02-REQ279-20260909: deletes the literal-colour guard's
      web/src/pages/ exemption -- the completion test for decision 0020
      D2, now that REQ-276/277/278 have all migrated their pages off
      literal colours. Exactly one allowedPaths entry and its two-line
      comment removed from web/tests/guards/forbidlist.ts; no other
      line touched. Re-verified independently by REVIEWER (not trusted
      from FRONTEND-DEV's self-report): npm run guards passed cleanly
      with the exemption gone (48/48 tests, including the source-scan
      that now runs unexempted against web/src/pages/) -- no literal-
      colour violation was masked by the migrations, so no page or
      component file needed touching (which would have violated AC4's
      scope fence). npm run type-check, npm run lint and npm test all
      independently re-run clean (323/323 tests). Pipeline: FRONTEND-DEV
      -> REVIEWER (PASS, independently re-ran all four npm commands in
      the foreground rather than trusting a background/async report).
      TEST-DESIGNER/TEST-DESIGN-VALIDATOR skipped -- existing guard
      tests already exercise this change directly and REVIEWER
      independently confirmed coverage. SECURITY-REVIEWER out of scope
      (test-guard config only, no tenant-data path). Merge-time
      bookkeeping: flipping REQ-279 to done expired REQ-283's
      blocked-by: REQ-279 deferral. REQ-283's other dependency, REQ-281,
      was already done -- no live blocker left at all, so registered
      REQ-283 properly in the queue (task 563, GH#1154) rather than
      inventing a re-scope target, the same pattern used for REQ-284
      and REQ-291 earlier this session. Updated
      test/mix/tasks/letflow_check_deferral_staleness_test.exs's
      T-LIVE-DEFERRED-COUNT assertions (7 -> 6, and the exact id list)
      before pushing. ORCH will merge and release queue task 562
      status:done.
  - req: REQ-283
    event: done
    agent: ORCH
    at: "2026-09-09T16:19:09Z"
    note: >
      REQ-283 (queue task 563, GH#1154) implemented via
      WF02-REQ283-20260909: applies tenant branding as CSS
      custom-property overrides on tokens.css in the SPA (0020 D1c,
      theming half). New BrandingProvider mounted once at app root in
      main.tsx, reading REQ-281's branding block through the existing
      fetchTenantConfig cache. Closed one-key allowlist
      (brand_colors.primary -> --color-brand-600), matching
      Letflow.Identity.Tenant's own @brand_colors_allowed_keys ~w(primary)
      server-side closure -- applyBranding.ts iterates the allowlist's
      own keys, never the tenant-supplied object's keys, so no
      tenant-controlled property name can ever reach the DOM. Logo
      rendered via a plain <img src>, alt sourced only from appName,
      never a markup-interpreting sink. All three fallback cases
      (branding absent, a key missing, endpoint failure outright)
      collapse to tokens.css's existing platform defaults via
      ?./?? chains, matching tenantConfig.ts's established
      catch-all-never-rejects pattern. Hard-coded "Letflow" product
      name in AppShell.tsx's sidebar replaced with branding's appName.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no
      rework, independently verified the allowlist-closure and
      logo-safety claims against actual server-side source) ->
      FRONTEND-DEV -> SECURITY-REVIEWER (PASS -- independently
      confirmed allowlist iterates its own keys not the tenant
      object's, alt sourced only from appName, CSS values set via
      element.style.setProperty (never string-concatenated into raw
      CSS/style text), zero eval/dangerouslySetInnerHTML/innerHTML,
      all fallback branches terminate safely) -> REVIEWER (1 rework
      cycle: AC1's computed-CSS-value test initially only exercised
      applyBrandingColors() directly, never rendered the real
      BrandingProvider tree, so a real bug in the provider's field-path
      wiring would have gone undetected by any existing test.
      FRONTEND-DEV added one render-level test through the real
      component tree with a genuine fail-first proof -- REVIEWER
      independently reproduced that fail-first break itself rather
      than trusting the report, confirmed the test fails on the broken
      path and passes once reverted -- re-check PASS). TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR skipped: 337 tests (53 files) already map
      every acceptance criterion with distinct, non-tautological
      assertions, independently confirmed by both SECURITY-REVIEWER and
      REVIEWER re-running the full suite themselves. No dependents
      deferred against REQ-283 -- no cascade to fix this time.
  - req: REQ-289
    event: started
    agent: ORCH
    at: "2026-09-09T16:33:54Z"
    note: >
      User directly named GH#1100 (REQ-289) for implementation. Queue
      task 544 found locked by a dead prior run (branch
      feature/WF02-REQ289-20260909, last commit ~16h old, zero
      handoffs/ files, no PR ever opened) -- force-released and
      re-locked as orch-req289 per TASK_QUEUE.md's dead-host recovery
      path. That prior run's own commit (dc031e61) contains substantial
      real implementation (39-entry corpus, 174-line test file, design
      doc, decision-record addendum) -- rebased cleanly onto current
      main (one conflict in decision 0020, composed not discarded) and
      resumed rather than restarted, per HANDOFF_PROTOCOL.md 4.1's
      broader recovery shape. Full investigation and one genuine
      acceptance-criteria gap found (AC3's null literal kind untracked)
      in handoffs/WF02-REQ289-20260909/step-00-git-setup.json.

  - req: REQ-295
    event: done
    agent: ORCH
    at: "2026-09-09T17:26:12Z"
    note: >
      REQ-295 (queue task 564, GH#1158) implemented via
      WF02-REQ295-20260909: answers decision 0023's DDL-execution open
      question with a new decision record,
      docs/migration/decisions/0024-entity-promotion-ddl-execution.md,
      plus a companion design artefact
      (lib/letflow/design/req295-entity-promotion-ddl-execution.md) for
      REQ-296 onward to build against. Design-only -- no lib/ code, no
      migration. Registered into docs/requirements.yaml at this run's
      Step 00 (same registration-gap class as REQ-272/275/287/289 --
      existed in the queue but not the file).
      Q1 (what runs the DDL): Letflow.TenantProvisioning, extended with
      new ColumnPromotion functions -- not its existing manifest
      mechanism (wrong shape for N already-live tenant schemas with
      real data, versus N freshly-provisioned empty ones), not a
      dedicated migrator (would reimplement TenantProvisioning's
      already-reviewed identifier-injection safety argument a second
      time), not the promotion path itself (a cross-tenant reach INV-1
      forbids).
      Q2 (partial failure): per-tenant, not atomic -- new global table
      entity_column_promotions, states pending -> ddl_applied ->
      backfilling -> backfilled -> active, plus ddl_failed and an
      independent query_eligible flag.
      Q3 (backfill): replay through rebuild_projection/2, not an
      inline UPDATE -- dual-write into both the new column and
      field_values during the backfill window, with query_eligible
      gating :typed_column dispatch so a not-yet-backfilled row is
      provably unreachable via the new column path (:json_field is the
      only possible dispatch until backfilled -> active).
      Q4 (rollback): never a drop/narrow (0023's additive-only rule
      respected throughout) -- immediate correction is
      query_eligible := false (fail closed, excludes from the query
      allowlist), the actual fix ships as a corrective re-backfill or a
      brand-new promotion under a new name.
      entity_record_latest recommendation: retire, not migrate -- no
      deployment holds entity records and every entity type gets its
      own per-type table under 0023 regardless of promotion.
      0023's "Open question" section amended to point at 0024 by path
      -- verified via a diff scoped to only that section, nothing else
      in 0023 changed.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (1 rework cycle:
      a missing file:line citation for allowlist.ex's typed_columns/0,
      alongside two correctly-cited functions -- fixed, re-check PASS)
      -> SECURITY-REVIEWER (1 rework cycle: a real cross-tenant
      DDL-execution risk -- the design doc's mutating functions took
      tenant_id and promotion_id as two independently-sourced,
      uncross-checked parameters, so a caller could in principle pair
      one tenant's tenant_id with another tenant's promotion_id and run
      DDL against the wrong schema. CODE-DESIGNER fixed by dropping
      tenant_id as an independent parameter entirely, deriving it from
      the loaded ColumnPromotion row instead -- re-check PASS,
      independently re-traced by SECURITY-REVIEWER) -> REVIEWER (PASS
      -- confirmed extending TenantProvisioning, a plain unsupervised
      module arbitrating concurrency via pg_advisory_xact_lock, needs
      no new supervision-tree review since it inherits REQ-045's
      already-settled process-vs-row precedent rather than making a new
      choice; fixed one trivial stale-arity doc nit directly rather
      than routing back for something that small). Both required gates
      (0023's own closing sentence: "needs its own design and its own
      SECURITY-REVIEWER and REVIEWER gates") are recorded in 0024's own
      distinct sign-off sections. No dependents deferred against
      REQ-295 -- no cascade to fix.
  - req: REQ-289
    event: done
    agent: DOC-UPDATER
    at: "2026-09-09T17:33:49Z"
    note: >
      REQ-289 (queue task 544, GH#1100) implemented via
      WF02-REQ289-20260909: exports a 40-entry language-neutral
      conformance corpus (priv/expr_conformance/corpus.json) derived
      from test/letflow/engine/expr_test.exs, covering all 6 comparison
      operators, and/or/not, all 5 literal kinds (number, string,
      boolean, null, and the null-asymmetry case added to close AC3's
      gap), all 5 arithmetic operators, unary negation, dotted variable
      paths, and all 8 builtins named by
      Letflow.Engine.Expr.builtin_function_names/0 -- the coverage test
      is driven by that function's return value, not a hand-copied
      list. Includes the three divergence-prone semantics record 0020
      D1a calls out by name: ASCII-only lower/upper, trim's 4-character
      ASCII whitespace set, and all 3 infinity markers (:infinity,
      :neg_infinity, :nan) from float division. Distinguishes parse
      failures from eval failures with dedicated entries of each, and a
      test asserting they are not interchangeable. Companion schema doc
      at lib/letflow/design/req289-expr-conformance-corpus.md documents
      both the JSON encodings chosen for the infinity markers and for
      failure outcomes, and the corpus location's rationale, recorded
      in docs/migration/decisions/0020-frontend-architecture.md's
      amended D1a Implementation record (REVIEWER-signed). Recovery of
      a genuinely prior, real implementation
      (origin/feature/WF02-REQ289-20260909, commit dc031e61 / 0fbfb9e9
      after rebase) that had died before any bookkeeping landed --
      ORCH's Step 00 force-released the dead queue lock, rebased
      cleanly onto current main, and found one real acceptance-criteria
      gap (AC3's null literal kind untracked): closed by commit
      c7b00ce7, adding corpus entry expr-040 (transcribing
      expr_test.exs's null-asymmetry comparison case) and retagging
      expr-039 with lit:null, per a CODE-DESIGN-VALIDATOR-approved
      design addendum. test/fixtures/simulation/differential_corpus.json,
      its REQ-209 test, and lib/letflow/engine/expr.ex confirmed
      unmodified by git diff, per the requirement's own hard scope
      fence (EXP-102). RELEASE-VALIDATOR independently re-derived and
      PASSED all 10 acceptance criteria (not trusted from the recovered
      branch's own prior reports). REQ-290 (CI drift guard + version
      marker) and REQ-292 (Elixir evaluator wiring) both depend on this
      corpus and were previously scoped around its absence from
      docs/requirements.yaml -- see REQ-290's own `depends_on:
      [REQ-289]` and its blocked-by deferral comment, both of which now
      have a real, done blocker rather than a placeholder; re-scoping
      either deferral, or registering REQ-290 in the queue, is left to
      ORCH's own routing decision, not resolved here. docs/requirements.yaml
      flipped in_progress -> done.
  - req: REQ-295
    event: started
    agent: ORCH
    at: "2026-09-09T18:03:52Z"
    note: >
      REQ-295..REQ-302 filed and registered as one batch (WF-01) for
      docs/migration/decisions/0023-entity-storage-hybrid.md, the entity
      storage decision from commit dceaa87a: per-entity-type tables with
      a hybrid promoted-column/blob shape, replacing the single shared
      entity_record_latest JSONB table REQ-225..231 shipped. Merged as
      afe964bf (PR #1166). Queue task ids 564-571, GH#1158-#1165;
      docs/requirements.yaml impl_order carries the mapping. This event
      is filed against REQ-295 as the batch's lead entry; all eight
      entries are `pending` and none has been worked.
      SHAPE, and the reason it is not a flat list: 0023 names one open
      question -- how a promotion's DDL executes per tenant in a
      humanless pipeline -- and declares it blocking ("No implementation
      requirement may be filed against this record until it is
      answered"). The batch is therefore gated. REQ-295 (0024, the
      DDL-execution answer) and REQ-302 (0025, ON DELETE policy and
      localized-text plain-text-vs-tsvector) are design roots with no
      dependencies; REQ-296..301 sit behind the roots each actually
      needs. Verified by transitive closure rather than asserted:
      REQ-298 reaches {295,296,297,302}, REQ-301 reaches
      {295,296,297,299,302}. The queue enforces it independently --
      every implementation task returned eligible:false with blocked_by
      naming exactly its blockers.
      REQ-VALIDATOR gated the batch FAIL twice before passing, both
      substantive. Round 1: six entries missing decision 0022's
      mandatory bucket declaration, and REQ-298/REQ-301 each left a
      consequential design decision (FK ON DELETE semantics; tsvector
      vs. plain-text columns) to be made by an implementing agent
      mid-run; it also repaired a YAML corruption where the first
      append had been spliced into the middle of REQ-294, truncating
      it. Round 2: the fix had relocated those two decisions into
      REQ-295 rather than resolving them, making REQ-295 itself
      oversized -- sub-questions 1-4 compose with each other, ON DELETE
      and tsvector are independent of them and of each other. Resolved
      by splitting them into REQ-302 as a parallel root. Supporting
      textual point, accepted: 0023's "its own SECURITY-REVIEWER and
      REVIEWER gates" sentence is scoped to the DDL-execution question
      alone, so it never required those two answers to share one record
      or one review pass. REQ-295 and REQ-302 write separate records so
      two parallel CODE-DESIGNER sessions never write the same file;
      editing 0023's own "Open question" section is REQ-295's alone.
      TWO CI GUARDS TRIPPED, both corpus-coupled, both test-data-only
      fixes with detector logic unchanged. (1) T-LIVE-ACTIVITY: filing
      the first-ever S10 entries made S10 present-but-inactive, the
      same transition S5/S6/S7 each made on expansion, since `pending`
      does not confer activity. Re-derived with
      CheckDeferralStaleness.audit/1 as that test's own comment
      instructs rather than guessed: active unchanged, inactive [] ->
      ["S10"], 0 violations. (2) Req207VortexTest AC4 Signal 2: five of
      the new titles match its word-bounded entity/entities pattern.
      Signal 3 -- the real gate -- was re-verified live before
      extending the allowlist: no lib/letflow/entities.ex, no
      entity_query.ex, no entity router module, and both
      Letflow.Routers.{Entities,EntityQuery} still in router.ex's
      reserved/unbuilt table, so BLOCKED_ON_DEPENDENCY genuinely still
      holds. Every other test reading the live corpus was swept in the
      same pass rather than discovered serially in CI (240 passed).
      SCOPE NOTE: S10 gap 13 (SolutionPack's pack_document carries no
      entity_definitions or form-schema section) is deliberately NOT
      filed here. It was found by the same architecture review but is
      not a consequence of 0023, and it blocks bucket A more
      fundamentally than gaps 10-11 do -- it remains unowned.
      CONCURRENCY: a sibling session merged REQ-289 (#1157) mid-run,
      requiring a rebase; the guards were re-derived and re-run against
      the merged corpus rather than assumed to survive it. Queue task
      564 (REQ-295) was claimed by letflow-orch within a minute of
      registration and is left locked -- that is the queue working as
      designed for multi-host coordination, not a stale lock.
  - req: REQ-302
    event: done
    agent: ORCH
    at: "2026-09-09T19:55:43Z"
    note: >
      REQ-302 (queue task 565, GH#1159) implemented via
      WF02-REQ302-20260909: answers two independent column-semantics
      questions for entity storage, split out from REQ-295 after
      REQ-VALIDATOR's second sizing gate -- new decision record
      docs/migration/decisions/0025-promoted-fk-ondelete-and-localized-text-search-strategy.md,
      deliberately its own file (not shared with REQ-295's 0024) since
      the two run as parallel CODE-DESIGNER sessions with no ordering
      dependency. Design-only, no lib/ code; docs/migration/decisions/0023-entity-storage-hybrid.md
      and 0024 both confirmed untouched (0023's "Open question" amendment
      is REQ-295's alone).
      Sub-question 1 (ON DELETE for promoted FK columns): ON DELETE
      RESTRICT (Ecto on_delete: :restrict). Reconciled against
      Letflow.Entities.Records.delete_record/2's actual soft-delete
      semantics (records.ex:199-219, upsert_record_latest/3's
      :update/:delete clause at :289-298 -- UPDATE ... SET deleted =
      true, never a SQL DELETE) -- RESTRICT is inert on the normal
      application path and only fires against a genuine out-of-band
      hard DELETE, where it fails loudly (a Postgres constraint
      violation) rather than silently losing tenant data. CASCADE
      rejected (irreversible cross-table data loss), SET NULL rejected
      (forces nullable FK columns, silent detachment), NO ACTION
      rejected (strictly weaker than RESTRICT, no cyclic-FK scenario
      needing deferrability). Matches existing on_delete: :restrict
      precedent (req043/req054/req211), though REVIEWER flagged one
      counter-example (a migration's own on_delete: :nothing on an
      identical-shape FK, already tracked as an open inconsistency
      under ISS-0225) as a non-blocking citation-completeness nit --
      doesn't affect RESTRICT's correctness on its own merits.
      Sub-question 2 (localized-text strategy): per-field,
      definition-level choice via a new search_strategy attribute
      (:plain default, :fulltext opt-in) -- not a single global
      mechanism. Plain generated columns are the default since that
      matches the only existing full-text precedent
      (Letflow.Definitions.search/2, definitions.ex:79-88, explicitly
      ILIKE-only, zero new indexes); confirmed via grep that no
      tsvector/GIN usage exists anywhere else in lib/. tsvector kept as
      an explicit per-field opt-in for BilimBaga's localized
      question-bank search, the exact case 0023 itself names as
      justifying it.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (1 rework cycle:
      a records.ex citation overshot its actual clause by 5 lines
      (289-303 claimed vs. 289-298 real), into an unrelated function --
      fixed, re-check PASS) -> SECURITY-REVIEWER (PASS -- independently
      confirmed the soft-delete-only application path from source,
      traced RESTRICT's actual Postgres failure mode (a loud,
      auditable constraint violation), confirmed no cross-tenant FK
      risk) -> REVIEWER (PASS -- confirmed the on_delete: :restrict
      precedent is real though not perfectly uniform (the ISS-0225 nit
      above), confirmed no accidental coupling between the two
      independent sub-questions, confirmed 0023/0024 both genuinely
      untouched). Both gates named by the requirement's own initiative
      (0023's own gate sentence doesn't literally cover column
      semantics) recorded in 0025's own distinct sign-off sections. No
      dependents' deferral hatches reference REQ-302 -- REQ-298/REQ-301
      remain unstartable regardless, pending REQ-296/297/299.
  - req: REQ-303
    event: started
    agent: ORCH
    at: "2026-09-09T20:03:55Z"
    note: >
      REQ-303..REQ-306 filed and registered as one batch (WF-01) for
      S10 gap 13 -- "a pack format that can carry entity definitions."
      Merged as 9b443129 (PR #1176). Queue task ids 574-577,
      GH#1172-#1175; docs/requirements.yaml impl_order carries the
      mapping. Filed against REQ-303 as the batch's lead entry; all four
      entries are `pending` and none has been worked.
      THE GAP: Letflow.Definitions.SolutionPack's pack_document
      (lib/letflow/definitions/solution_pack.ex:186-195) has four
      content sections -- definitions, service_catalog_entries (always
      [], rejected on install), variable_schemas, manifest.required_roles
      (read-only) -- and none can carry an entity definition. Decision
      0022 defines bucket A as capability delivered as definitions
      installed via SolutionPack.install/3, so bucket A's own delivery
      vehicle cannot carry part of what bucket A consists of. Chain is
      REQ-303 (decision record 0026: schema versioning, install
      semantics, name collisions, the interim-rejection stance, INV-1
      scoping) -> REQ-304 (export side) -> REQ-305 (install side) ->
      REQ-306 (tests).
      THE GAP ROW OVERSTATES THE DEFECT, recorded here because the stage
      file still carries the original framing. Gap 13 asserts the pack
      format has "no form-schema section" and treats that as half the
      problem. It does not survive contact with the code: form_schema is
      not a standalone artefact but an optional attribute on a HUMAN_TASK
      node inside a process definition's graph
      (definitions/graph.ex:852-870,
      definitions/form_schema_expressions.ex:34-40, resolved at
      activation in routers/tasks.ex:87-107). pack_definition/1 embeds
      definition.graph verbatim (solution_pack.ex:377-385) and
      parse_definition/1 round-trips an arbitrary graph map
      (solution_pack.ex:454-470), so form schemas ALREADY travel through
      the existing definitions section with zero code change. The real
      gap is entity definitions only -- roughly half what the stage file
      asserts. Found by REQ-ANALYST, confirmed independently by
      REQ-VALIDATOR reading all four modules, and spot-checked by ORCH.
      REQ-303 AC5 requires the decision record to state this with
      file:line citations rather than silently narrowing scope. The
      stage-10 gap-13 row itself is NOT corrected here -- that is
      DOC-UPDATER's territory and REQ-303 will produce the authoritative
      correction.
      INDEPENDENT OF THE REQ-295..302 TRACK. Gap 13 was found by the
      same architecture review that produced decision 0023 but is not a
      consequence of it: 0023 is entity STORAGE shape, gap 13 is pack
      DELIVERY sections -- different module, different subsystem.
      Verified rather than asserted: 0023's text never mentions
      entity_definitions, and no depends_on edge crosses between the two
      batches, so they can run in parallel rather than being wrongly
      serialized.
      REQ-VALIDATOR PASS on the first gate round (unlike the REQ-295..302
      batch, which took two). It confirmed the form-schema finding
      independently, checked that all five of REQ-303's design questions
      are genuinely open rather than already settled by existing code (a
      padded design requirement being a real defect), and confirmed
      REQ-303's questions are compositional facets of one artefact rather
      than the independent-questions bundling that forced REQ-302 to be
      split out of REQ-295. One grounding defect it found was fixed
      before merge: an acceptance criterion cited decision records 0024
      and 0025, which did not exist at drafting time -- they are
      REQ-295's and REQ-302's own deliverables on the track this batch is
      deliberately independent of, so depending on them would have
      reintroduced the coupling the batch exists without.
      CI: passed all four checks on the first attempt. The
      req207_vortex_test allowlist was extended for REQ-304's title
      PROACTIVELY rather than after CI caught it -- the REQ-295..302
      batch tripped that same guard, so it was checked before pushing.
      Signal 3, the real gate, was re-verified live again: still no
      lib/letflow/entities.ex, no lib/letflow/entity_query.ex, no entity
      router module, and Letflow.Routers.Entities present only as a
      reserved/unbuilt row in router.ex.
      HISTORY-ORDERING ARTIFACT, noted rather than corrected: this
      volume's "REQ-295 done" (17:26:12Z, appended by the concurrent
      session that completed REQ-295 and produced decision record 0024)
      precedes this volume's "REQ-295 started" (18:03:52Z, appended by
      this session, describing the FILING of the REQ-295..302 batch).
      Both are accurate about what their own session did; the inversion
      is a real consequence of two sessions working the same requirement
      id concurrently, one filing it and one executing it. The
      append-only rule forbids reordering or editing either, and
      test/docs/requirement_status_invariants_test.exs does not assert
      chronological ordering (20 passed with the inversion present), so
      nothing is corrected -- it is recorded here so a later reader does
      not mistake it for a clock error or a corrupted append.
  - req: REQ-296
    event: done
    agent: ORCH
    at: "2026-09-09T23:08:01Z"
    note: >
      REQ-296 (queue task 566, GH#1160) implemented via
      WF02-REQ296-20260909: DDL-generation half of decision 0023 --
      Letflow.Entities.Definition.DDL.generate_table_ddl/2, a pure
      function taking a Definition.t() plus a table_name and returning
      raw SQL CREATE TABLE text, matching REQ-295's raw-SQL execution
      mechanism (confirmed against 0024's own text, not assumed).
      Structural columns match entity_record_latest (minus entity_type,
      since each new table is single-type by construction). Two
      independent promotion triggers -- FK-field membership (promoted
      regardless of queried) and queried: true (promoted regardless of
      FK membership); a field satisfying neither stays only inside
      field_values jsonb. All 7 non-json field_type() values map to a
      concrete Postgres type; :json fields are never promoted via a
      defence-in-depth filter independent of the trigger check (beyond
      Validator's already-enforced Rule 3). Both table_name and every
      column identifier are validated against an anchored allowlist
      regex before interpolation (not a blocklist), enum values are
      quote-doubled before embedding in CHECK constraints, and decimal
      precision/scale are integer-guarded with a safe numeric fallback
      -- no SQL-injection vector found by SECURITY-REVIEWER's
      independent trace of every value that reaches the generated SQL
      string. Extension point for REQ-301's future locale-generated-
      column case stated in the moduledoc, not implemented. Pipeline:
      CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no rework) ->
      ELIXIR-DEV -> SECURITY-REVIEWER (PASS -- independently confirmed
      the identifier-validation regex, enum escaping and decimal
      guards, ran the real ephemeral-Postgres test itself; flagged two
      non-blocking test-coverage gaps) -> REVIEWER (PASS -- closed
      SECURITY-REVIEWER's two flagged gaps directly rather than routing
      back to ELIXIR-DEV for something that small, bringing the suite
      to 24 tests; independently re-ran the full suite solo after
      diagnosing an earlier concurrent-mix-test-invocation flake in
      ServiceCatalogTest, confirmed only the 2 pre-existing rustc-absent
      failures remain). TEST-DESIGNER/TEST-DESIGN-VALIDATOR skipped --
      24 tests already map every acceptance criterion with real,
      non-tautological assertions including a genuine ephemeral-Postgres
      round-trip, independently confirmed by both SECURITY-REVIEWER and
      REVIEWER. No blocked-by deferral hatches reference REQ-296 --
      REQ-297/298/301 (its real dependents) remain unstartable
      regardless via the queue's own depends_on tracking.

  - req: REQ-299
    event: started
    agent: ORCH
    at: "2026-09-10T01:15:02Z"
    note: >
      WF02-REQ299-20260910 started (queue task 568, GH#1162). Makes
      Letflow.Entities.Query.Allowlist.typed_columns/0 per-entity-type,
      reading REQ-296's promoted-column enumeration off Definition.t()
      rather than a second hand-maintained list, per decision 0023's
      consequences section. Shadowing precedence rule (typed column
      always wins over a same-named JSONB key) is unchanged. Branch:
      feature/WF02-REQ299-20260910.

  - req: REQ-297
    event: done
    agent: ORCH
    at: "2026-09-10T01:36:48Z"
    note: >
      REQ-297 (queue task 567, GH#1161) implemented via
      WF02-REQ297-20260909: the promotion executor -- the first code
      to actually run entity-storage DDL against live tenant schemas,
      implementing REQ-295's designed ColumnPromotion mechanism using
      REQ-296's DDL generator. Nine functions on
      Letflow.TenantProvisioning (register/run/run_for_all/retry/
      backfill/activate/suspend, plus two query-eligibility checks),
      tenant enumeration via list_registrations/0, a new global
      entity_column_promotions table (per-tenant, not cross-tenant,
      atomicity, per 0024's actual decision), backfill via a real
      rebuild_projection/2 replay (dual-write during the window), and
      table-creation-on-first-use -- a genuine, correctly-flagged gap
      (0024/0025/REQ-296 all disclaimed owning entity-table lifecycle;
      CODE-DESIGN-VALIDATOR independently confirmed this via REQ-296's
      own moduledoc explicitly assigning the job to REQ-297/298) rather
      than a silent architectural re-decision.
      Additive-only enforced by a real information_schema.columns
      check before every ALTER TABLE ADD COLUMN -- a type conflict is
      rejected with a typed error and marks the row ddl_failed, never
      coerced. Dual-write landed as a NEW, separate Multi.run step in
      Letflow.Entities.Records (upsert_record_latest/3's own two
      existing clauses left byte-for-byte untouched -- a deliberately
      more conservative shape than 0024's literal "amendment" wording,
      flagged as such in the code). Backfill invocation of
      rebuild_projection/2 proven genuine via a real :telemetry
      attachment on Ecto's own query event, not inferred from a
      correct end value.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no
      rework -- independently judged the table-lifecycle scoping call
      a legitimate gap-fill, not scope overreach, since it's the
      natural consequence of already-decided work rather than a new
      cross-cutting architectural choice) -> ELIXIR-DEV -> 
      SECURITY-REVIEWER (PASS -- the highest-stakes review in this
      batch, exhaustively traced cross-tenant DDL-execution risk, SQL
      injection, additive-only enforcement, entity_column_promotions
      access scoping, and the entity_type_not_found edge case ELIXIR-DEV
      itself flagged, confirming it's a narrow, correctly-distinguishable
      condition rather than a masked failure) -> REVIEWER (PASS --
      independently re-derived every check, found a THIRD full-suite
      failure beyond the 2 known rustc-absent ones, a SandboxPoolTest
      timing flake, and confirmed it pre-existing/unrelated via an
      isolated 23/23 re-run rather than accepting the "2 pre-existing"
      framing at face value). TEST-DESIGNER/TEST-DESIGN-VALIDATOR
      skipped -- 17 tests already map every AC with real, non-inferred
      assertions (direct Postgres queries, genuine telemetry-based
      invocation proof, genuine type-conflict rejection), independently
      confirmed by both SECURITY-REVIEWER and REVIEWER. No blocked-by
      deferral hatches reference REQ-297 -- REQ-298/301 remain
      unstartable regardless via the queue's own depends_on tracking.

  - req: REQ-299
    event: done
    agent: DOC-UPDATER
    at: "2026-09-10T04:07:42Z"
    note: >
      REQ-299 (queue task 568, GH#1162) implemented via
      WF02-REQ299-20260910: Letflow.Entities.Query.Allowlist.typed_columns/2,
      a new per-entity-type promoted-column accessor sharing REQ-296's
      DDL.promoted_columns/1 enumeration -- typed_columns/0 (the fixed
      7-entry structural table) and load/2 left byte-for-byte unchanged.
      Real mid-run scope conflict: implementing the original AC4 wording
      literally (wiring load/2 to the new per-entity-type set) broke
      compiler.ex's and cursor.ex's column-atom resolution for the
      mainline non-FK promoted-field case -- 15 test failures. REVIEWER
      approved deferring the load/2 wiring to REQ-300, which already
      depends_on: [REQ-298, REQ-299] and now carries an explicit
      acceptance criterion for it; REQ-VALIDATOR and ORCH amended
      REQ-299's AC4 wording and REQ-300's acceptance criteria to close
      the gap REQ-300 inherits (both amendments already committed on
      this branch, commit dfda9606, earlier in this run). Pipeline:
      CODE-DESIGNER (3 iterations) -> CODE-DESIGN-VALIDATOR -> REVIEWER
      (scope sign-off on the AC4 deferral) -> REQ-VALIDATOR (AC4/REQ-300
      text fix) -> ELIXIR-DEV (2 attempts) -> SECURITY-REVIEWER (PASS) ->
      REVIEWER (idiom PASS) -> TEST-DESIGNER -> TEST-DESIGN-VALIDATOR
      (PASS) -> TEST-RUNNER (PASS, 3620/3620) -> RELEASE-VALIDATOR (PASS
      after one formatting fix; full suite 3620/3620, mix letflow.check
      clean). docs/requirements.yaml flipped in_progress -> done.
      README.md checked -- no documented current-behavior change (pure
      internal query-layer accessor, no user-facing effect) -- left
      unchanged. docs/migration/decisions/0023-entity-storage-hybrid.md
      confirmed needing no edit, per REVIEWER's own prior sign-off that
      0023's claim doesn't need amendment.

  - req: REQ-301
    event: started
    agent: ORCH
    at: "2026-09-10T04:35:04Z"
    note: >
      WF02-REQ301-20260910 started (queue task 570, GH#1164). Adds a
      :localized_text Definition.field_type() (blob-stored, keyed by
      locale), a per-definition supported-locale declaration, a Validator
      carve-out letting it be queried: true (unlike :json), a generated-
      column-per-locale case in REQ-296's DDL generator, REQ-297 executor
      wiring, and REQ-299 allowlist exposure, per 0023's re-scoping of S10
      gap 11. Plain-text-vs-tsvector mechanism is REQ-302's decision, not
      this requirement's to make. Flagged at Step 00: REQ-301's AC5
      ("filtering/sorting on a locale's generated column works end-to-end
      through Allowlist + Compiler") may hit the exact same
      compiler.ex/cursor.ex column-atom-resolution gap WF02-REQ299-20260910
      found and deferred to REQ-300 -- REQ-300 is still pending and is not
      in REQ-301's depends_on. CODE-DESIGNER instructed to check this at
      design time rather than discover it after an implementation attempt.
      Branch: feature/WF02-REQ301-20260910.

  - req: REQ-298
    event: done
    agent: ORCH
    at: "2026-09-10T05:33:45Z"
    note: >
      REQ-298 (queue task 569, GH#1163) implemented via
      WF02-REQ298-20260910: activates constraint_def as real Postgres
      unique indexes and fk_def.references_entity as real referential
      integrity (REFERENCES ... ON DELETE RESTRICT, per REQ-302's
      exact decision), extending REQ-296's DDL generator and REQ-297's
      promotion executor -- no new per-tenant-DDL-execution code path.
      FK REFERENCES rides the same ALTER TABLE ADD COLUMN statement
      (single column, syntactically valid); a multi-column
      constraint_def unique index needs a follow-up ALTER TABLE ADD
      CONSTRAINT, handled by a new ConstraintActivation retrofit
      mechanism firing once all of a constraint's fields exist as
      real columns, under the SAME per-tenant pg_advisory_xact_lock
      serialization run_column_promotion/1 already uses (so a
      constraint activation and a column promotion against the same
      tenant schema never interleave). Many-to-many (a question_tags-
      shaped worked example, two fk_def entries plus one constraint_def
      of type: :unique over both) verified with no join-table special
      case in either the generator or the executor; no array-of-
      references anti-pattern anywhere in the diff.
      A genuine implementation necessity surfaced mid-build: FK-promoted
      columns' Postgres type must be forced to uuid (not the ordinary
      :string -> text mapping) since Postgres requires matching
      equality-operator types for a REFERENCES constraint against a
      uuid record_id target -- flagged explicitly rather than silently
      decided, and this in turn surfaced a REAL, genuinely
      production-crashing gap SECURITY-REVIEWER caught on first pass:
      the new uuid pg_type wasn't handled by cast_promoted_value/2 or
      the two placeholder builders in Letflow.Entities.Records and
      Letflow.Entities.Record.Projector, so any real dual-write or
      backfill against an FK-promoted column would crash with a
      Postgrex encode error -- masked by the original test suite's own
      helper manually applying the missing cast instead of exercising
      the real production path. Fixed at all three sites plus two new
      tests genuinely exercising Records.create_record/2's dual-write
      and the real backfill/replay path, both verified via git stash
      to fail pre-fix and pass post-fix.
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS, no rework
      -- independently assessed the new ConstraintActivation state
      machine as proportional and race-free, not a disproportionate
      second competing mechanism) -> ELIXIR-DEV -> SECURITY-REVIEWER
      (1 rework cycle: the production-crashing uuid-placeholder bug
      above, caught on first pass with full trace-through of the
      write paths; re-check PASS with an independently-reproduced
      fail-first proof and a from-scratch full-suite re-run) ->
      REVIEWER (PASS -- confirmed the placeholder generalization is
      genuinely additive with zero behavioral change to any
      non-FK-promoted write path, confirmed ConstraintActivation needs
      no dedicated supervision review per REQ-045's precedent, cited
      SECURITY-REVIEWER's already-fresh full-suite evidence rather
      than re-running a third 25+-minute suite unnecessarily).
      TEST-DESIGNER/TEST-DESIGN-VALIDATOR skipped -- 44 tests across
      both touched test files already map every acceptance criterion
      with real Postgres-verified assertions, independently confirmed
      by both SECURITY-REVIEWER and REVIEWER. No blocked-by deferral
      hatches reference REQ-298 -- REQ-300 (its real dependent) remains
      unstartable regardless via the queue's own depends_on tracking.
