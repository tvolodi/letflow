# Letflow — Requirement run history — VOLUME 14 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v13.yaml (volume 13, closed,
#   1,201 frozen-prefix lines / 1,230 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v14.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v14.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v14.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v14.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-11T03:39:58Z"
    note: >
      WF03-ISS0594-20260911 started (queue task 594, GH#1222,
      docs/issues/ISS-0594.yaml). test/letflow/service_catalog_test.exs:588's
      "SolutionPack's moduledoc names REQ-192 as the owner" test is a bare
      substring check (module_source =~ "REQ-192") that REQ-307's own
      moduledoc correction (2026-09-11) has semantically inverted: the
      corrected text now says the service_catalog_entries install policy
      is UNOWNED, but the test still passes only because the string
      "REQ-192" still happens to appear. A latent false-green in the
      dangerous direction -- removing that string entirely (making the
      moduledoc MORE correct) would FAIL this test. test/specs/REQ-191.md
      lines 179-182 carry the same stale wording. Fix direction: assert
      the actual current property (policy documented as unowned, hard-fail
      retained) rather than a requirement-id substring. Branch:
      fix/ISS0594-20260911.
      NOTE: this entry was originally appended to (the now-closed) volume
      13, then composed here instead by this run's own Step Final rebase,
      since volume 13 was independently closed by fix/ISS-0595-20260911
      before this branch's own Step Final ran -- "one run's started/done
      land one in each volume across a roll boundary," per this index's
      own established pattern.

  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-09-11T04:19:50Z"
    note: >
      WF03-ISS0594-20260911 done (queue task 594, GH#1222). Root cause:
      test/letflow/service_catalog_test.exs's AC10 test had a
      semantically-inverted bare substring assertion
      (module_source =~ "REQ-192") that kept passing after REQ-307
      corrected solution_pack.ex's moduledoc/comment to say the
      service_catalog_entries policy is UNOWNED rather than deferred to
      REQ-192 -- a test that would FAIL if a future edit made the
      moduledoc MORE correct by removing the "REQ-192" substring
      entirely. Fix (CODE-DESIGNER design, TEST-DESIGNER
      implementation): renamed the describe block/test and swapped the
      assertion to module_source =~ "UNOWNED"; corrected
      test/specs/REQ-191.md's matching stale wording. No lib/ change.
      mix test test/letflow/service_catalog_test.exs: 33/33 passed;
      mix compile --warnings-as-errors and mix format
      --check-formatted clean. SECURITY-REVIEWER skipped (test/spec-doc
      -only, no tenant-data path, confirmed by CODE-DESIGN-VALIDATOR and
      REVIEWER). REVIEWER PASSED, independently confirmed via git
      log/git show that the new assertion is a genuine pre/post-REQ-307
      discriminator. Branch: fix/ISS0594-20260911.
      NOTE: this entry was originally appended to (the now-closed) volume
      13, then composed here instead, for the same roll-boundary reason
      this run's own started-event above already states.

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-11T04:57:55Z"
    note: >
      WF03-ISS0591-ISS0592-20260911 started (queue tasks 591/592,
      GH#1217/GH#1218, docs/issues/ISS-0591.yaml, docs/issues/ISS-0592.yaml).
      Handling both together, per their own filed sequencing recommendation
      -- both edit .github/workflows/ci.yml's on:/jobs structure and both
      must preserve the same required status-check contexts ("Backend gate
      (mix letflow.check)", "Frontend gate (npm run check)", decision 0018).
      ISS-0591: bare on: [push, pull_request] with no concurrency: group
      runs every job twice per commit (measured: 14 runs for 7 commits on
      a real branch), doubling CI cost and test-DB contention (a likely
      contributor to ISS-0590's SandboxPoolTest flake). ISS-0592: no
      paths:/paths-ignore: filter means a docs-only PR still runs the full
      ~8-9min backend gate against main's strict branch protection,
      repeatedly losing the up-to-date-merge race under concurrent agent
      sessions (observed directly this session, multiple PRs this
      run rebased 2-3+ times). CAUTION (both issues' own text): a
      path-filtered/skipped job reports its required context as MISSING,
      not passing, which blocks merges PERMANENTLY -- getting this wrong
      is strictly worse than today's slow-but-working state. Needs the
      standard required-checks-with-path-filters skip-shim pattern, not a
      bare paths: filter. Branch: fix/ISS0592-20260911.

  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-09-11T05:48:27Z"
    note: >
      WF03-ISS0591-ISS0592-20260911 done (queue tasks 591/592, GH#1217/
      GH#1218, PR #1229). ISS-0591 fix: added a `concurrency:` group on
      .github/workflows/ci.yml keyed
      `${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}`
      with `cancel-in-progress: true`, and narrowed `push:` to
      `branches: [main]` so PR-branch pushes are covered only by the
      `pull_request:` event -- eliminates the doubled-run-per-commit
      pattern (measured 14 runs / 7 commits pre-fix). ISS-0592 fix: added a
      new non-required `changes` job ("Detect changed paths") that computes
      backend-/frontend-relevant path changes, with the existing `backend`/
      `frontend` jobs gated by a fail-open condition
      (`!cancelled()` and the relevant `changes` output `!= 'false'`) so
      they still always run and report their required context (never
      MISSING) but skip the expensive work when only non-code paths
      changed -- avoids the bare-paths:-filter trap (a skipped job reports
      MISSING and blocks merges permanently) both issues' own filings
      warned against. Design passed CODE-DESIGN-VALIDATOR across 3 rounds;
      ELIXIR-DEV's implementation verified with 6 real dry-run scenarios;
      REVIEWER independently re-verified the evidence live; TEST-RUNNER
      confirmed PR #1229 up to date with main with all three required
      checks green (Backend gate, Detect changed paths, Frontend gate) on
      head SHA ea588692. Both docs/issues/ISS-0591.yaml and
      docs/issues/ISS-0592.yaml set to status: resolved. GitHub mirror
      issues #1217/#1218 closed with evidence comments citing PR #1229.
      Branch: fix/ISS0592-20260911 (not merged by this step -- ORCH's Step
      Final does the merge next).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T06:01:30Z"
    run: WF03-ISS0591-ISS0592-20260911
    note: >
      Step Final. PR #1229 (fix/ISS0592-20260911 -> main) confirmed CI green
      on head SHA 849519af (Backend gate 7m30s, Detect changed paths 6s,
      Frontend gate 1m0s) and mergeStateStatus CLEAN, then squash-merged
      (commit e6b97ab5) with branch deleted. Verified post-merge:
      `gh api repos/tvolodi/letflow/branches/main/protection --jq
      '.required_status_checks.contexts'` still returns exactly
      ["Backend gate (mix letflow.check)","Frontend gate (npm run check)"]
      -- unchanged, confirming the fix did not alter what main requires.
      Queue tasks 591 and 592 locked and released with status: done
      (agent_id tvolodi-orch); both now open=false/status=done. This closes
      out ISS-0591 and ISS-0592.

  - req: REQ-308
    event: started
    agent: ORCH
    at: "2026-09-11T06:01:45Z"
    run: WF02-REQ308-20260911
    note: >
      Claimed via get_next_task (queue task 587, GH#1208). Design-only
      requirement (S10 gap 1): HTTP surface for entity definitions/records
      including the query DSL over HTTP, per 0022 rule-1 vertical-neutral
      wording. Branch: design/REQ308-20260911. Dispatching CODE-DESIGNER
      next, gated by CODE-DESIGN-VALIDATOR and SECURITY-REVIEWER (tenant-data
      path per the requirement's own acceptance criteria).

  - req: REQ-308
    event: done
    agent: DOC-UPDATER
    at: "2026-09-11T06:14:37Z"
    run: WF02-REQ308-20260911
    note: >
      Design-only requirement closed. Artefact:
      lib/letflow/design/req308-entity-http-surface.md -- specifies the HTTP
      surface for Letflow.Entities.{Definitions,Records} and the query DSL
      (S10 gap 1), per 0022 rule-1 vertical-neutral wording. Passed
      CODE-DESIGN-VALIDATOR and SECURITY-REVIEWER (verdict recorded in the
      artefact's own §11, PASS on INV-1/INV-2/INV-5/INV-6/INV-7/INV-8;
      INV-3/INV-4/INV-9 not applicable). Key decisions: one router,
      Letflow.Routers.Entities at /entities, not two (§2 -- the deferred
      table's two module names are an R-Co-filename artefact, not two URL
      prefixes); four new permission atoms
      (:EntitiesDefinitionsRead/:EntitiesDefinitionsWrite/:EntitiesRecordsWrite/
      :EntitiesQuery), deliberately not shared with the existing
      Definitions{Read,Write,Create} atoms (§3); the query DSL transported
      as a POST /entities/query body, not a GET query string, since
      query_request() is unboundedly nested (§4); compile/2's and
      paginate/5's error unions mapped to concrete HTTP statuses; INV-1/2/5/7
      each addressed by name with the concrete mechanism in §5. No
      docs/migration/decisions/ record filed -- §10 reasons this is
      ordinary route-table/permission-matrix design work, not a load-bearing
      cross-cutting architectural decision, contrasting explicitly with 0024
      (REQ-295's record). This requirement implemented no lib/ code, mounted
      no route, and touched no test -- the design artefact is the entire
      deliverable, per REQ-308's own scope fence. `git diff --name-only
      main...HEAD` confirms only lib/letflow/design/req308-entity-http-surface.md
      plus status-bookkeeping files (docs/requirements.yaml,
      docs/status/requirement_status.index.yaml,
      docs/status/requirement_status.v14.yaml) touched -- no
      docs/migration/decisions/ record, matching §10's own conclusion that
      none was warranted. Commits: 24ee128f (Step 00), 7e257217
      (CODE-DESIGNER), 439be0b1 (SECURITY-REVIEWER PASS). Branch:
      design/REQ308-20260911.
