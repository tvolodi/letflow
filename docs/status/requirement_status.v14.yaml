# Letflow — Requirement run history — VOLUME 14 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v13.yaml (volume 13, closed,
#   1,201 frozen-prefix lines / 1,230 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v14.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v14.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v14.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v14.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-11T03:39:58Z"
    note: >
      WF03-ISS0594-20260911 started (queue task 594, GH#1222,
      docs/issues/ISS-0594.yaml). test/letflow/service_catalog_test.exs:588's
      "SolutionPack's moduledoc names REQ-192 as the owner" test is a bare
      substring check (module_source =~ "REQ-192") that REQ-307's own
      moduledoc correction (2026-09-11) has semantically inverted: the
      corrected text now says the service_catalog_entries install policy
      is UNOWNED, but the test still passes only because the string
      "REQ-192" still happens to appear. A latent false-green in the
      dangerous direction -- removing that string entirely (making the
      moduledoc MORE correct) would FAIL this test. test/specs/REQ-191.md
      lines 179-182 carry the same stale wording. Fix direction: assert
      the actual current property (policy documented as unowned, hard-fail
      retained) rather than a requirement-id substring. Branch:
      fix/ISS0594-20260911.
      NOTE: this entry was originally appended to (the now-closed) volume
      13, then composed here instead by this run's own Step Final rebase,
      since volume 13 was independently closed by fix/ISS-0595-20260911
      before this branch's own Step Final ran -- "one run's started/done
      land one in each volume across a roll boundary," per this index's
      own established pattern.

  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-09-11T04:19:50Z"
    note: >
      WF03-ISS0594-20260911 done (queue task 594, GH#1222). Root cause:
      test/letflow/service_catalog_test.exs's AC10 test had a
      semantically-inverted bare substring assertion
      (module_source =~ "REQ-192") that kept passing after REQ-307
      corrected solution_pack.ex's moduledoc/comment to say the
      service_catalog_entries policy is UNOWNED rather than deferred to
      REQ-192 -- a test that would FAIL if a future edit made the
      moduledoc MORE correct by removing the "REQ-192" substring
      entirely. Fix (CODE-DESIGNER design, TEST-DESIGNER
      implementation): renamed the describe block/test and swapped the
      assertion to module_source =~ "UNOWNED"; corrected
      test/specs/REQ-191.md's matching stale wording. No lib/ change.
      mix test test/letflow/service_catalog_test.exs: 33/33 passed;
      mix compile --warnings-as-errors and mix format
      --check-formatted clean. SECURITY-REVIEWER skipped (test/spec-doc
      -only, no tenant-data path, confirmed by CODE-DESIGN-VALIDATOR and
      REVIEWER). REVIEWER PASSED, independently confirmed via git
      log/git show that the new assertion is a genuine pre/post-REQ-307
      discriminator. Branch: fix/ISS0594-20260911.
      NOTE: this entry was originally appended to (the now-closed) volume
      13, then composed here instead, for the same roll-boundary reason
      this run's own started-event above already states.

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-11T04:57:55Z"
    note: >
      WF03-ISS0591-ISS0592-20260911 started (queue tasks 591/592,
      GH#1217/GH#1218, docs/issues/ISS-0591.yaml, docs/issues/ISS-0592.yaml).
      Handling both together, per their own filed sequencing recommendation
      -- both edit .github/workflows/ci.yml's on:/jobs structure and both
      must preserve the same required status-check contexts ("Backend gate
      (mix letflow.check)", "Frontend gate (npm run check)", decision 0018).
      ISS-0591: bare on: [push, pull_request] with no concurrency: group
      runs every job twice per commit (measured: 14 runs for 7 commits on
      a real branch), doubling CI cost and test-DB contention (a likely
      contributor to ISS-0590's SandboxPoolTest flake). ISS-0592: no
      paths:/paths-ignore: filter means a docs-only PR still runs the full
      ~8-9min backend gate against main's strict branch protection,
      repeatedly losing the up-to-date-merge race under concurrent agent
      sessions (observed directly this session, multiple PRs this
      run rebased 2-3+ times). CAUTION (both issues' own text): a
      path-filtered/skipped job reports its required context as MISSING,
      not passing, which blocks merges PERMANENTLY -- getting this wrong
      is strictly worse than today's slow-but-working state. Needs the
      standard required-checks-with-path-filters skip-shim pattern, not a
      bare paths: filter. Branch: fix/ISS0592-20260911.

  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-09-11T05:48:27Z"
    note: >
      WF03-ISS0591-ISS0592-20260911 done (queue tasks 591/592, GH#1217/
      GH#1218, PR #1229). ISS-0591 fix: added a `concurrency:` group on
      .github/workflows/ci.yml keyed
      `${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}`
      with `cancel-in-progress: true`, and narrowed `push:` to
      `branches: [main]` so PR-branch pushes are covered only by the
      `pull_request:` event -- eliminates the doubled-run-per-commit
      pattern (measured 14 runs / 7 commits pre-fix). ISS-0592 fix: added a
      new non-required `changes` job ("Detect changed paths") that computes
      backend-/frontend-relevant path changes, with the existing `backend`/
      `frontend` jobs gated by a fail-open condition
      (`!cancelled()` and the relevant `changes` output `!= 'false'`) so
      they still always run and report their required context (never
      MISSING) but skip the expensive work when only non-code paths
      changed -- avoids the bare-paths:-filter trap (a skipped job reports
      MISSING and blocks merges permanently) both issues' own filings
      warned against. Design passed CODE-DESIGN-VALIDATOR across 3 rounds;
      ELIXIR-DEV's implementation verified with 6 real dry-run scenarios;
      REVIEWER independently re-verified the evidence live; TEST-RUNNER
      confirmed PR #1229 up to date with main with all three required
      checks green (Backend gate, Detect changed paths, Frontend gate) on
      head SHA ea588692. Both docs/issues/ISS-0591.yaml and
      docs/issues/ISS-0592.yaml set to status: resolved. GitHub mirror
      issues #1217/#1218 closed with evidence comments citing PR #1229.
      Branch: fix/ISS0592-20260911 (not merged by this step -- ORCH's Step
      Final does the merge next).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T06:01:30Z"
    note: >
      Run WF03-ISS0591-ISS0592-20260911, Step Final. PR #1229
      (fix/ISS0592-20260911 -> main) confirmed CI green
      on head SHA 849519af (Backend gate 7m30s, Detect changed paths 6s,
      Frontend gate 1m0s) and mergeStateStatus CLEAN, then squash-merged
      (commit e6b97ab5) with branch deleted. Verified post-merge:
      `gh api repos/tvolodi/letflow/branches/main/protection --jq
      '.required_status_checks.contexts'` still returns exactly
      ["Backend gate (mix letflow.check)","Frontend gate (npm run check)"]
      -- unchanged, confirming the fix did not alter what main requires.
      Queue tasks 591 and 592 locked and released with status: done
      (agent_id tvolodi-orch); both now open=false/status=done. This closes
      out ISS-0591 and ISS-0592.

  - req: REQ-308
    event: started
    agent: ORCH
    at: "2026-09-11T06:01:45Z"
    note: >
      Run WF02-REQ308-20260911. Claimed via get_next_task (queue task 587,
      GH#1208). Design-only
      requirement (S10 gap 1): HTTP surface for entity definitions/records
      including the query DSL over HTTP, per 0022 rule-1 vertical-neutral
      wording. Branch: design/REQ308-20260911. Dispatching CODE-DESIGNER
      next, gated by CODE-DESIGN-VALIDATOR and SECURITY-REVIEWER (tenant-data
      path per the requirement's own acceptance criteria).

  - req: REQ-308
    event: done
    agent: DOC-UPDATER
    at: "2026-09-11T06:14:37Z"
    note: >
      Run WF02-REQ308-20260911. Design-only requirement closed. Artefact:
      lib/letflow/design/req308-entity-http-surface.md -- specifies the HTTP
      surface for Letflow.Entities.{Definitions,Records} and the query DSL
      (S10 gap 1), per 0022 rule-1 vertical-neutral wording. Passed
      CODE-DESIGN-VALIDATOR and SECURITY-REVIEWER (verdict recorded in the
      artefact's own §11, PASS on INV-1/INV-2/INV-5/INV-6/INV-7/INV-8;
      INV-3/INV-4/INV-9 not applicable). Key decisions: one router,
      Letflow.Routers.Entities at /entities, not two (§2 -- the deferred
      table's two module names are an R-Co-filename artefact, not two URL
      prefixes); four new permission atoms
      (:EntitiesDefinitionsRead/:EntitiesDefinitionsWrite/:EntitiesRecordsWrite/
      :EntitiesQuery), deliberately not shared with the existing
      Definitions{Read,Write,Create} atoms (§3); the query DSL transported
      as a POST /entities/query body, not a GET query string, since
      query_request() is unboundedly nested (§4); compile/2's and
      paginate/5's error unions mapped to concrete HTTP statuses; INV-1/2/5/7
      each addressed by name with the concrete mechanism in §5. No
      docs/migration/decisions/ record filed -- §10 reasons this is
      ordinary route-table/permission-matrix design work, not a load-bearing
      cross-cutting architectural decision, contrasting explicitly with 0024
      (REQ-295's record). This requirement implemented no lib/ code, mounted
      no route, and touched no test -- the design artefact is the entire
      deliverable, per REQ-308's own scope fence. `git diff --name-only
      main...HEAD` confirms only lib/letflow/design/req308-entity-http-surface.md
      plus status-bookkeeping files (docs/requirements.yaml,
      docs/status/requirement_status.index.yaml,
      docs/status/requirement_status.v14.yaml) touched -- no
      docs/migration/decisions/ record, matching §10's own conclusion that
      none was warranted. Commits: 24ee128f (Step 00), 7e257217
      (CODE-DESIGNER), 439be0b1 (SECURITY-REVIEWER PASS). Branch:
      design/REQ308-20260911.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T06:39:05Z"
    note: >
      ISS-0593 (GH#1219, queue task 593) -- main's live branch protection
      had enforce_admins: true, contradicting decision
      0018-branch-protection-posture.md section (d)'s explicit rejection
      of that setting ("No, enforce_admins: true is not appropriate --
      reject it"), meaning 0018's documented --admin override path for a
      genuine CI-infrastructure break did not currently exist. Not a
      code fix -- a live GitHub repo security-setting change, so
      confirmed with the user before acting rather than treating it as
      pre-authorized humanless-pipeline work. User chose to restore the
      recorded posture. Confirmed check-run names unchanged
      ("Backend gate (mix letflow.check)", "Frontend gate (npm run
      check)") per 0018's own Step 1, including after the just-merged
      ISS-0591/0592 CI dedup/path-filter change. Applied 0018's own
      exact recorded configuration via gh api --method PUT
      .../branches/main/protection (enforce_admins: false, same required
      status checks, required_pull_request_reviews: null,
      allow_force_pushes: false, allow_deletions: false). Verified live
      via a fresh GET matching every field. No claim about how the
      live setting diverged in the first place -- outside this issue's
      scope, and the decision record itself needed no amendment since
      it already correctly describes the intended posture.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T08:01:40Z"
    note: >
      ISS-0599 (GH#1240, queue task 599) -- letflow-queue tasks 596/597/598
      (REQ-309/310/311) were all registered with depends_on: [] due to a
      shell-pipeline JSON-key parsing bug in the registration script, even
      though docs/requirements.yaml correctly records the real chain
      (REQ-310 depends_on [REQ-309], REQ-311 depends_on [REQ-310]).
      letflow-queue exposes no update endpoint for depends_on, so the
      documented fix directions were (a) rely on requirements.yaml and
      accept the exposure, (b) cancel+re-register with new ids, or (c) add
      an update endpoint (its own decision). At pickup, the residual risk
      the issue named had ALREADY materialized live: task 596 was locked
      by orch-session-7ca7f9f6, and task 597 had ALREADY been claimed by
      tvolodi-orch (a session other than the one executing 596) before
      596 finished. Left 597's active lock untouched (not a fix target);
      locked and immediately released task 598 with status: "blocked",
      closing the remaining get_next_task exposure window. Option (b) not
      attempted -- 597 genuinely in progress under a live lock, cancelling
      would be disruptive not corrective. docs/requirements.yaml's own
      depends_on chain remains authoritative.
  - req: REQ-309
    event: done
    agent: ORCH
    at: "2026-09-11T09:05:00Z"
    note: >
      Four entity permission atoms, ten endpoint_policy_key/2 clauses and
      role_allows?/2 matrix arms added to Letflow.Api.Authorization, per
      REQ-308 design section 3. Landed ahead of its consuming router by
      design -- authz_* macros take the policy key as a compile-time
      literal and the enforcement test fails any route resolving to
      :Unknown, so the vocabulary must exist first. SECURITY-REVIEWER
      PASS, REVIEWER PASS; both independently re-derived the 95-pair
      regression grid's honesty and each ran its own mutation to confirm
      it bites. Merged as PR #1242.
  - req: REQ-310
    event: done
    agent: ORCH
    at: "2026-09-11T10:00:06Z"
    note: >
      Letflow.Routers.Entities created with nine definition and record
      routes, mounted at /entities, and both deferred-routes rows retired
      from router.ex. POST /entities/query deliberately absent -- REQ-311
      appends it. SECURITY-REVIEWER PASS on the first pass (INV-1, INV-2,
      INV-5, INV-6, INV-7, INV-8 apply; verified the prefix shape at all
      nine call sites and that idempotency keys are namespaced per tenant
      schema). REVIEWER returned FAIL on round 1 with two blocking
      defects, both found by issuing real requests rather than reading
      code: activate_definition declared rationale optional while the
      delegate requires it non-empty, so a legal request returned an
      opaque 422; and build_definition_document/1's blanket rescue
      degraded real validation errors into a detail-free 422, with which
      error a caller received depending on whether their typo coincided
      with an atom already loaded in the BEAM. Both fixed in rework round
      1, which also found a third instance the review missed
      (whitespace-only rationale passed FieldConstraint's exact-""
      check). REVIEWER PASS on re-gate after re-deriving test
      non-vacuity itself with three independent reverts.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T10:00:06Z"
    note: >
      Two tests re-derived rather than silenced after REQ-310 falsified
      their dispositions. req207_vortex_test.exs's armed tripwire fired
      exactly as its own 2026-09-11 UPDATE block predicted: the scenario
      is still blocked but for a narrower true reason (the record query
      route is REQ-311's, not "no HTTP surface exists"), its Signal 3 had
      become a tautology asserting flat paths that would never exist, and
      the tripwire is re-armed against REQ-311 with four killed mutants.
      router_test.exs's REQ-070 AC5 hardcoded 11 deferred routes; REQ-310
      retired two, and the block grew from 2 assertions to 5 rather than
      merely decrementing a count. Correction recorded in both: only
      Letflow.Routers.Entities graduated -- Letflow.Routers.EntityQuery
      never existed as a module and was retired by merge per design
      section 2.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T11:22:11Z"
    note: >
      ISS-0600 (GH#1245, queue task 600) -- FieldGrants.redact_item/2
      matched %Latest{} only, but Compiler's {:per_type_table, _} query
      branch emits a plain entity_row() map, causing redact_page/2 to
      raise FunctionClauseError on any non-join query against a promoted
      entity type. Pre-existing since REQ-296-298 added per-type tables
      after FieldGrants was built (REQ-231); latent until REQ-311's
      still-in-progress POST /entities/query became the first real
      caller. REQ-311's own router-side workaround (not yet on main) was
      confirmed security-equivalent by that branch's own SECURITY-REVIEWER;
      REVIEWER there made filing this issue a condition of accepting it.
      Pipeline: CODE-DESIGNER (PASS first try; a research fork briefly
      exceeded its read-only brief and wrote a competing design doc,
      caught and deleted by CODE-DESIGNER itself before reporting back)
      -> CODE-DESIGN-VALIDATOR PASS first try (independently verified the
      Elixir clause-ordering semantics claim: a %Latest{} struct also
      structurally matches a bare map pattern, confirming the new clause
      correctly placed second) -> ELIXIR-DEV (fail-first-proven: reproduced
      the exact FunctionClauseError with the fix removed, 13/13 passing
      restored) -> SECURITY-REVIEWER PASS (confirmed no divergent
      redaction logic, no extra field exposed; investigated a recurring
      SandboxPoolTest RT-6 failure in the full-suite run and determined
      it's a genuine residual flake in ISS-0590's own earlier fix under
      heavier full-suite contention, not a regression from this diff --
      recommended a distinct follow-up issue) -> REVIEWER PASS
      (independently re-ran the full suite; RT-6 didn't reproduce that
      run, consistent with it being a flake). REQ-311's router-side
      workaround removal explicitly deferred to whoever merges that
      branch. TEST-DESIGNER/TEST-DESIGN-VALIDATOR skipped per this
      session's established precedent.
  - req: REQ-311
    event: done
    agent: ORCH
    at: "2026-09-11T11:36:14Z"
    note: >
      POST /entities/query added to Letflow.Routers.Entities, wiring both
      field-redaction branches. This closes S10 gap 1: the entity
      subsystem, built since REQ-225 but unreachable over HTTP, is now
      fully served. SECURITY-REVIEWER PASS and REVIEWER PASS.
      INCIDENT: the first implementation passed both gates and was then
      DESTROYED by an unrelated subagent running git checkout on the
      still-uncommitted file while mutation-testing a different file.
      Recovery was attempted and proven impossible -- no dangling blob,
      no stash, and the compiled .beam had already been rebuilt from the
      reverted source (confirmed by decompiling its abstract_code:
      redact_plain_page and load_restriction_sets both absent). The test
      suite survived, so the work was checkpointed immediately as
      d8e12145 and the handler REBUILT against the design spec plus its
      own 932 committed test lines -- a green target, not a redesign.
      Both gates then re-reviewed the rebuilt code from scratch rather
      than carrying the prior PASS forward, since a pass on destroyed
      code is not evidence about new code. Lesson recorded: commit
      before mutating, and never git checkout a file holding uncommitted
      work.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T11:36:14Z"
    note: >
      Two further tripwires re-derived rather than silenced, both
      falsified by REQ-311 itself. req207_vortex_test.exs fired for the
      SECOND time exactly as armed; the disposition is still blocked but
      the blocker has moved out of lib/ entirely and changed owner from
      S10 to S8 -- the simulation Runner defers every :gui step
      unconditionally. Its new tripwire asserts on OBSERVED Runner
      behaviour rather than source text, because mutation proved a text
      match would stay green through the exact S8 change it must catch.
      query_cursor_field_grants_test.exs carried REQ-231's AC4 refuting
      any router referencing Query.Cursor/Query.FieldGrants -- which
      REQ-311's handler does by design. AC4 was a SCOPE constraint on
      REQ-231's own commits ("deferred, not forbidden"), not an
      architectural ban, so it was inverted to "entities.ex is the ONLY
      such router", keeping the load-bearing half (no second,
      unredacted query path) and still able to fail.
      ISS-0600 filed (GH#1245): FieldGrants.redact_page/2 raises
      FunctionClauseError on the plain-map rows a promoted per-type
      table produces. Pre-existing since REQ-296/297/298 postdated
      REQ-231; REQ-311 is its first caller. Worked around router-side
      with a shape adapter both gates confirmed security-equivalent and
      fail-loud.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T12:24:53Z"
    note: >
      ISS-0601 (GH#1248, queue task 601) -- test/letflow/sandbox_pool_test.exs's
      RT-6 (the same death-path-c test ISS-0590 fixed earlier the same
      day) flaked once under full-suite load, even though ISS-0590's own
      fix genuinely ran, losing the race under heavier Postgres/DDL
      contention than whatever run originally validated it. Filed by
      ORCH per SECURITY-REVIEWER's recommendation during ISS-0600's own
      review. Pipeline: CODE-DESIGNER (PASS first try; traced the
      crash/cleanup path end-to-end confirming no lib/ defect, chose
      raising the timeout over a new completion signal as the smallest
      fix for a MINOR flake, and surfaced a load-bearing secondary
      finding: doubling wait_until_schema_dropped/2's deadline would push
      RT-6's combined internal wait past ExUnit's own 60s default
      per-test timeout, requiring @tag timeout: :infinity on both
      affected tests) -> CODE-DESIGN-VALIDATOR PASS first try
      (independently re-derived every load-bearing claim from source,
      including the ExUnit-timeout arithmetic) -> ELIXIR-DEV (honestly
      reported the flake did not reproduce in this run's full suite,
      correctly not oversold as proof) -> REVIEWER PASS (independently
      re-verified the crash/cleanup trace, confirmed no decision-record
      conflict with ISS-0590, independently re-ran the full suite).
      SECURITY-REVIEWER/TEST-DESIGNER/TEST-DESIGN-VALIDATOR correctly
      skipped (test-file-only, no tenant-data path, no new coverage --
      only timing-parameter adjustments to existing tests).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-11T13:56:56Z"
    note: >
      Run WF01-S10gaps-2-3-12-20260911. Filed and registered the next
      batch of S10 gap requirements after the gap-1 chain (REQ-308..311)
      closed and the queue drained: REQ-312 (aggregation/reporting
      queries, S10 gap 2), REQ-313 (attachments on entity records, S10
      gap 3), REQ-314 (bulk import/export of entity records, S10 gap
      12). All three design-only, depends_on [REQ-311]. REQ-ANALYST
      drafted (commit c261ad24), REQ-VALIDATOR PASSed all three
      (testability, consistency with decisions 0022/0023, premises
      verified against real code, 0022 rule-1 vocabulary neutrality),
      PR #1252 merged (commit ce403c7d). Registered in letflow-queue as
      tasks 602/603/604, each with depends_on: [598] (REQ-311's queue
      id) correctly resolved -- not repeating ISS-0599's empty-edge
      mistake. impl_order written back into docs/requirements.yaml.
      docs/migration/stage-10-bilimbaga-vertical.md's gap table updated
      to show gaps 2/3/12 as design-owned but not yet closed.
  - req: REQ-313
    event: started
    agent: ORCH
    at: "2026-09-11T14:13:24Z"
    note: >
      Run WF02-REQ313-20260911. Claimed via get_next_task (queue task
      603, GH#1254). Design-only requirement (S10 gap 3): attachments
      on an entity record. Branch: design/REQ313-20260911. Dispatching
      CODE-DESIGNER next, gated by CODE-DESIGN-VALIDATOR and
      SECURITY-REVIEWER (tenant-data path).

  - req: REQ-312
    event: done
    agent: ORCH
    at: "2026-09-11T14:19:36Z"
    note: >
      REQ-312 (queue task 602, GH#1253) implemented via
      feature/WF02-REQ312-20260911: S10 gap 2, a design-only requirement
      for an aggregation/reporting query capability
      (count/sum/avg/min/max/group_by) over Letflow.Entities.Query.
      Compiler's existing infrastructure, extending Letflow.Routers.
      Entities' now-landed POST /entities/query surface with a new
      sibling route, POST /entities/query/aggregate, and a new
      compile_aggregate/2 alongside (not replacing) compile/2. Reuses
      Allowlist.load/2 + resolve_field/2 for group_by/aggregate-target
      resolution rather than a second allowlist. New :EntitiesAggregate
      permission atom. Design artefact:
      lib/letflow/design/req312-query-aggregation.md. Pipeline:
      CODE-DESIGNER (2 rework cycles: first a self-contradictory
      typed_columns/1 arity citation -- that function doesn't exist,
      only /0 and /2 do -- plus a misquote of metrics_exposition.ex;
      then a genuine security gap SECURITY-REVIEWER found) ->
      CODE-DESIGN-VALIDATOR PASS -> SECURITY-REVIEWER FAIL then PASS
      (found that the design's INV-2 mechanism -- reject aggregate-
      target/group_by fields the caller can't see -- explicitly
      exempted filters fields, which combined with count/group_by turns
      a pre-existing weak single-bit filter oracle into an exact
      per-group count, letting a caller reconstruct a restricted
      field's tenant-wide distribution in a handful of requests; fixed
      by extending the pre-compilation restriction check to filters
      fields too, scoped only to the new aggregate route, leaving the
      existing plain query route untouched; verdict recorded directly
      in the design artefact per this requirement's own AC) ->
      CODE-DESIGN-VALIDATOR final narrow re-check PASS. Rule 1 (0022)
      vocabulary self-check and scope (only the one design file
      touched) both verified. No lib/ implementation, no route mounted,
      no migration, no test, matching this requirement's own scope
      fence. docs/requirements.yaml flipped pending -> done.

  - req: REQ-313
    event: done
    agent: DOC-UPDATER
    at: "2026-09-11T14:32:29Z"
    note: >
      Run WF02-REQ313-20260911. Design-only requirement closed. Artefact:
      lib/letflow/design/req313-entity-record-attachments.md -- specifies
      attachments on an entity record (S10 gap 3). Passed
      CODE-DESIGN-VALIDATOR after 4 rework rounds and SECURITY-REVIEWER
      (verdict recorded in the artefact's own §10, PASS on
      INV-1/INV-2/INV-5/INV-6/INV-7/INV-8; INV-3/INV-4/INV-9 not
      applicable). Notable rework findings: round 2/3 caught a false
      premise in an earlier draft's schema/FK reasoning (a claim that
      entity_record_latest's (entity_type, record_id) pair had "no unique
      index stated in its own schema module" was false -- a real composite
      unique index exists, re-verified directly against
      lib/letflow/entities/record/latest.ex and its backing migration),
      which flipped the FK decision from a soft/app-level reference to a
      real DB-level composite FK; a later round then caught a missed code
      path (Letflow.Entities.Record.Projector.write_snapshots/3's
      delete-then-reinsert backfill path, which a plain immediate FK would
      crash on), resolved by making the composite FK
      DEFERRABLE INITIALLY DEFERRED rather than dropping back to a soft
      reference. Key decisions: new entity_record_attachments table (not
      an extension of instance_attachments), keyed by (entity_type,
      record_id) with a DEFERRABLE composite FK to entity_record_latest;
      new Letflow.Repository.EntityAttachments context module mirroring
      Letflow.Repository.Attachments' upload/2, list/2, get/2,
      get_content/2, delete/2 shape; four new routes nested under
      /entities/records/:entity_type/:record_id/attachments on the
      existing Letflow.Routers.Entities (not a new router module); two new
      permission atoms, :EntitiesAttachmentsManage/:EntitiesAttachmentsRead
      (not reused from :AttachmentsManage/:AttachmentsRead or
      :EntitiesRecordsWrite), with a concrete default role-grants table
      (PLATFORM_ADMIN both, PROCESS_DESIGNER Read only, PROCESS_OPERATOR
      both, TASK_WORKER Read only, AGENT_RUNNER none) flagged for REVIEWER
      sign-off as a judgment call, not silently decided; INV-a/INV-b
      (content_type untrusted, byte_size independently measured) inherited
      verbatim from Letflow.Repository.Attachments. No
      docs/migration/decisions/ record filed -- §8 reasons this is
      ordinary route-table/permission-matrix/schema design work following
      existing precedent (0003/0022/0023), not a new cross-cutting
      architectural decision. This requirement implemented no lib/ code,
      mounted no route, created no migration, and touched no test -- the
      design artefact is the entire deliverable, per REQ-313's own scope
      fence. `git diff --name-only main...HEAD` confirms only
      lib/letflow/design/req313-entity-record-attachments.md plus
      status-bookkeeping files (docs/requirements.yaml,
      docs/status/requirement_status.index.yaml,
      docs/status/requirement_status.v14.yaml) touched -- no
      docs/migration/decisions/ record, matching §8's own conclusion that
      none was warranted. Also updated
      docs/migration/stage-10-bilimbaga-vertical.md's gap table row 3 to
      reflect the design is now closed (implementation not yet filed).
      Commits: 4d9dc387 (Step 00), d21b6aea (CODE-DESIGNER), 75b72172
      (CODE-DESIGNER rework, FK premise fix + OQ-2 role grants), d34bb30f
      (SECURITY-REVIEWER PASS). Branch: design/REQ313-20260911.
