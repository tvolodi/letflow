# Letflow — Requirement run history — VOLUME 15 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v14.yaml (volume 14, closed,
#   1,213 frozen-prefix lines / 1,240 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v15.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v15.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v15.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v15.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-12T06:05:23Z"
    note: >
      ISS-0616 (queue task 616, GH#1289): get_next_task silently
      ignoring an unsupported dry_run parameter and taking a real lock
      -- self-filed by a prior ORCH run after hitting exactly this,
      with recovery already taken (released the fictitious
      orch-probe lock, claimed properly). Direct-action fix per
      ORCHESTRATOR.md sizing rule (one file, docs-only, no new
      function/spec, no migration, no supervision code, no tenant-
      data path, no test-asserted behaviour change): added a
      paragraph to docs/agents/protocols/TASK_QUEUE.md's existing
      "Reachability checks must not have side effects" section
      stating explicitly that no dry_run mode exists, that
      letflow-queue silently ignores unknown query parameters rather
      than rejecting them with 400, and cross-referencing REQ-222
      (target repo tvolodi/letflow-queue) as the eventual real fix
      (a genuine read-only eligibility listing) rather than inventing
      a client-side simulation of one. Fix directions (a) and (b) from
      the issue are out of scope for this repo (letflow-queue is a
      separate repo); this closes fix direction (c), the
      near-free-and-prevents-recurrence one. Flipped ISS-0616's
      status to resolved and closed GH#1289 with an evidence comment.
  - req: REQ-323
    event: done
    agent: ORCH
    at: "2026-09-12T06:14:03Z"
    note: >
      S10 gap 6 designed -- a pattern for exposing a read-only resource to
      an unauthenticated caller, the last unowned bucket-B gap and the one
      deliberate hole in Letflow's auth pipeline. Design-only; produced
      lib/letflow/design/req323-unauthenticated-read-pattern.md and
      decision record 0028. SECURITY-REVIEWER PASS, REVIEWER PASS after one
      rework round.
      The crux: with no token there is no auth_context, so INV-1's usual
      mechanism (Api.Context.scoped_repo_opts/1) is unavailable. The design
      resolves tenant via a 256-bit CSPRNG capability handle stored as
      SHA-256 in a global registry, whose row's tenant_id then feeds the
      SAME TenantProvisioning.schema_name_for_tenant/1 derivation -- only
      the source of tenant_id differs, the storage mechanism is unchanged.
      Anti-enumeration rests on a named property (drawn-and-stored, never
      derived; no collection or index route exists) rather than an
      assurance. INV-5 is held on both halves including timing: exactly one
      round-trip before any refusal, with a syntactic short-circuit on a
      malformed handle explicitly forbidden because it is precisely the
      signal INV-5 names.
      Two judgement calls worth recording. Mounting TenantStatus was
      REJECTED as worse than useless -- with no auth context it is a silent
      no-op (tenant_status.ex:71-81) that would look like protection; the
      design folds tenants.status into the existing round-trip and returns
      404, never that plug's 403, which would disclose two facts at once.
      And assign_trace_id/1 was rejected because it reflects a
      caller-supplied x-trace-id verbatim with no validation, which is a
      surface a world-reachable endpoint should not have.
      REVIEWER's round-1 FAIL caught the design justifying its own
      structure by citing decision 0024 as precedent for a short record
      with a long design -- 0024 is 616 lines to REQ-295's 205, the exact
      reverse. Re-checking that, CODE-DESIGNER found and corrected a fourth
      inherited claim of its own (ClaimMappingConfig is a runtime
      Application.fetch_env!/2, not a compile-time read). Both gates
      verified every source quotation independently; no inaccurate claim
      survived.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-12T06:14:03Z"
    note: >
      ISS-0616 filed (GH#1289): get_next_task silently ignores an
      unsupported dry_run parameter and takes a real lock. ORCH's own
      read-only availability probe locked task 615 (REQ-323) to the
      fictitious agent id "orch-probe" for 57 minutes, discovered only when
      the same ORCH hit HTTP 409 claiming REQ-323 for real -- blocked by
      itself. Two defects: there is no read-only way to ask whether the
      queue has eligible work (REQ-222 may already cover this, and the
      record cross-references it rather than duplicating), and unknown
      query parameters are ignored rather than rejected, which is what
      turned a wrong assumption into a stuck lock. Recovered by releasing
      under the probe's own agent id. No work lost.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-12T12:31:00Z"
    note: >
      ISS-0617 (queue task 617, GH#1294, discovered during a P2
      feasibility probe) -- Letflow.Entities.Record.Validator.
      field_subschema/1 was missing a clause for the :localized_text
      field type (REQ-301 landed its DDL and query halves but never
      the write-path validator), so any record write against a
      :localized_text field raised FunctionClauseError instead of a
      clean validation error, an INV-8-shaped defect blocking S10 P2's
      exit condition. Fixed via fix/ISS-0617-20260912: added the
      missing clause matching ddl.ex's storage shape
      (field_values["<name>"]["<locale>"]), with additionalProperties:
      false set explicitly at the nested level (confirmed not
      inherited from the outer schema) and partial-locale writes
      allowed (every per-locale generated column is nullable: true in
      ddl.ex). Fail-first verified: reverting the clause reproduces the
      crash on 7 of 17 tests. Cross-checked no other field type shares
      this half-landed shape.

      Pipeline: ISSUE-FIXER (root-cause diagnosis: confirmed exact
      field shape, storage shape, additionalProperties scoping, and
      that no other field type is affected) -> ELIXIR-DEV (7 new
      tests) -> SECURITY-REVIEWER PASS (INV-8: confirmed no crash path
      remains including the degenerate empty-locales case, safely
      excluded upstream by Definition.Validator's Rule 10; INV-1:
      confirmed the accepted locale set comes from the tenant's own
      already-validated definition, never from caller-supplied write
      payload) -> REVIEWER PASS (independently verified the DDL-shape
      match and the nullable-columns justification for the partial-
      locale decision, independently ran the full suite: only the
      known pre-existing rust-toolchain and tenant-provisioning-race
      failures, none attributable to this diff). TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR skipped per this project's established
      precedent -- both hard-gate reviewers independently verified the
      7-test coverage as comprehensive. docs/issues/ISS-0617.yaml
      flipped to status: resolved.
  - req: REQ-324
    event: started
    agent: ORCH
    at: "2026-09-12T17:15:28Z"
    note: >
      Run WF02-REQ324-20260912. Claimed via get_next_task (queue task
      618, GH#1297). Lifts Letflow.Entities.Definition.Validator's
      Rule 9 (self-referential-FK rejection) per S10 gap 14, per
      lib/letflow/design/req225-entity-definition-schema-validation.md's
      own numbered open item 4, which anticipated this as follow-up
      work. Must settle ON DELETE semantics for the self-edge (per
      decision 0025), cycle prevention, the DDL generator's
      self-reference handling (empirically verified against real
      Postgres), and the query compiler's self-join path. Branch:
      feature/WF02-REQ324-20260912. Dispatching ELIXIR-DEV next,
      gated by REVIEWER (not a tenant-facing new route, but touches
      the validator/DDL/compiler core, so SECURITY-REVIEWER's scope
      test will decide whether it applies).
  - req: REQ-325
    event: done
    agent: ORCH
    at: "2026-09-12T18:06:01Z"
    note: >
      REQ-325 (queue task 619, GH#1298, no depends_on) implemented via
      feature/WF02-REQ325-20260912: a decision record settling S10
      gaps 16 and 17 -- the third and fourth recurrence of "a new
      packable-content-type wants to extend pack_document's closed
      struct type" (after decision 0026's entity_definitions and
      decision 0027's rejection of service_catalog_entries). Answers:
      (1) a pack does NOT carry scripted rules -- unlike 0027's
      precedent, no existing out-of-band route exists today for
      scripts, but the NO holds even more conservatively (closes the
      surface entirely, nothing to eventually replace); (2) a pack
      does NOT seed roles -- the read-only role-mapping checklist
      stays permanent, POST /roles confirmed as the direct analogue of
      0027's POST /service-catalog grounding, established by evidence
      (full grep -rn upsert_role lib/ output quoted and every .ex hit
      walked); (3) audit question does not arise, both prior answers
      NO; (4) pack_document stays a closed struct type; (5) corrected
      pack-phase deliverables stated for the stage file's owner to
      apply later. Bucket A's meaning under decision 0022 is
      unchanged. No design artefact produced -- both sub-question-1
      answers NO, none warranted.

      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR FAIL then PASS
      (first pass caught a dangling forward-reference to a promised
      but never-written "Caller walk" evidence subsection; CODE-
      DESIGNER added the real evidence, CODE-DESIGN-VALIDATOR
      independently re-ran the grep and confirmed the walk) ->
      SECURITY-REVIEWER PASS naming pack-supplied executable content
      by name per this requirement's explicit acceptance criterion ->
      REVIEWER PASS, distinct from SECURITY-REVIEWER's, on decision-
      record consistency (confirmed no silent re-decision of
      0022/0026/0027, independently re-verified the upsert_role caller
      walk a third time). Scope fence held -- only the one decision-
      record file plus docs/requirements.yaml status bookkeeping; no
      lib/letflow/definitions/, lib/letflow/identity/,
      lib/letflow/engine/, migrations, test/, or the stage file
      touched. docs/requirements.yaml flipped pending -> done.
  - req: REQ-326
    event: done
    agent: ORCH
    at: "2026-09-12T19:38:08Z"
    note: >
      REQ-326 (queue task 620, GH#1301, no depends_on) implemented via
      feature/WF02-REQ326-20260912: authored five bucket-A entity-
      definition documents under priv/packs/bilimbaga/entity_
      definitions/ (category, tag, question, answer_option,
      question_tag) plus priv/packs/bilimbaga/README.md and a
      validator test -- the first pack content authored in S10,
      unblocking P2's exit condition. Ports the source's two per-locale
      translation tables into their parent entity's :localized_text
      field (REQ-301's mechanism); question_tag authored as a join
      entity type per docs/anti-patterns.md's array-of-references
      entry and REQ-300's join-entity shape; answer_option kept as a
      separate entity type so is_correct is a first-class promoted
      field for FieldGrants redaction (REQ-231). Every indexes/
      foreign_keys/constraints entry carries a name following the
      <prefix>_<entity>_<columns> convention. Deliberately omitted, per
      document: id/created_at/updated_at (record subsystem supplies
      these); created_by on question (event stream's actor_id answers
      authorship instead, since an entity fk_def cannot cross into
      identity.users); parent_id on category and question
      (Definition.Validator Rule 9 rejects self-referential fk_def
      today, REQ-324 pending lifts it, no workaround substituted).

      Pipeline: REQ-ANALYST (owner per the requirement's own rationale
      -- content documents in an already-designed format, no interface
      or @spec left to design) -> REVIEWER PASS (independently verified
      every field/type/citation against the requirement's detailed
      specification field by field, confirmed Rule 2's shared fk+
      constraint uniqueness-scope claim by reading the validator
      directly, independently ran the new test: 18 passed). No
      SECURITY-REVIEWER gate -- static pack content with no route, no
      migration, no live data path; REQ-328 owns the actual install
      wiring. 0022's rule 1 does not apply -- bucket A, naming domain
      vocabulary is the deliverable, not a violation. Scope fence held
      -- only priv/packs/, the one new test file, and docs/
      requirements.yaml status bookkeeping; no lib/letflow/, web/,
      priv/repo/migrations/, or docs/migration/ touched.

      SEPARATE FINDING, not part of this requirement's scope: mix
      letflow.check's full run surfaced a pre-existing,
      REQ-326-unrelated failure in Req207VortexTest -- its hardcoded
      REQ-id allowlist has not been reconciled for REQ-324's entry
      (filed via a separate concurrent WF-01 session, title contains
      "entity"). Confirmed via `git diff main --stat` (empty on this
      branch) that the failure predates and is independent of REQ-326;
      to be filed as its own issue (note: TEST-RUNNER already fixed
      this tripwire as part of REQ-324's own PR #1306, triaging
      REQ-324/325/326/327/328 across two rounds -- no separate issue
      needed). docs/requirements.yaml flipped pending -> done.

  - req: REQ-324
    event: done
    agent: DOC-UPDATER
    at: "2026-09-12T19:54:00Z"
    note: >
      Close-out for run WF02-REQ324-20260912 (queue task 618, GH#1297),
      whose implementation, REVIEWER PASS and TEST-RUNNER green-CI
      confirmation had already merged to main (PR #1306, commit
      6d7face1) without the close-out step running. Lifted
      Letflow.Entities.Definition.Validator's Rule 9
      (self-referential-FK rejection) per S10 gap 14, following
      lib/letflow/design/req225-entity-definition-schema-validation.md's
      own anticipated-follow-up note (numbered open item 4). Four
      empirical findings: (1) decision 0025 already covers ON DELETE
      for the self-edge unconditionally, no code change needed; (2)
      cycle prevention is NOT enforced anywhere in the platform -- a
      stated, reasoned position, not an oversight; (3) the DDL
      generator already handles self-references correctly, verified
      against a real Postgres; (4) the query compiler's self-join path
      was found genuinely BROKEN (`ERROR 42702 ambiguous_column`) and
      fixed by qualifying `hop_on_dynamic/1`'s fragments to the binding
      alias, with a follow-on gap (filter/sort dynamics still
      unqualified for a self-join) correctly identified and left for a
      future requirement rather than silently folded in. REVIEWER PASS
      -- thorough, independently re-verified all four findings against
      real code and decision text. TEST-RUNNER confirmed green CI on
      PR #1306 (merged, commit 6d7face1), including triaging
      REQ-324/325/326/327/328 into the Req207VortexTest tripwire across
      two rounds as main moved. docs/requirements.yaml flipped
      pending -> done. Added gap 14 (self-referential entity
      relationships) as a new row to
      docs/migration/stage-10-bilimbaga-vertical.md's gap table --
      that table only listed thirteen gaps and had no row for gap 14 at
      all, even though REQ-324's own description cited "S10 gap 14" as
      its motivation; the new row is marked closed, citing REQ-324 and
      summarizing the four findings above. Closed GitHub mirror issue
      #1297 with an evidence comment citing PR #1306 and its CI
      results. Only status-bookkeeping and this one stage-doc file
      touched in this branch -- confirmed via
      `git diff --name-only main...HEAD`; the implementation itself was
      already merged separately and is untouched here.
  - req: REQ-328
    event: blocked
    agent: ORCH
    at: "2026-09-12T20:22:06Z"
    note: >
      Claimed letflow-queue task 622 via get_next_task, dispatched to
      ELIXIR-DEV on branch feature/WF02-REQ328-20260913. ELIXIR-DEV
      correctly stopped before writing anything: REQ-328 depends_on
      [REQ-326, REQ-327] (docs/requirements.yaml line ~22430's own
      impl_order comment: "taking 622 first fails loudly, since its
      pack document embeds the ten definition documents 620 and 621
      author"), and REQ-327 (queue task 621, exam-configuration entity
      definitions) is still status: pending -- only REQ-326's five
      entity_definitions/*.json files exist on disk, none of
      REQ-327's five exam-configuration ones. Confirmed via
      get_next_task/orch-probe: task 621 is not offered (locked by
      another host, presumably a sibling session already working
      REQ-327), so it is in flight, not merely unclaimed. No code
      changes made -- ELIXIR-DEV's git status was clean. Released
      queue task 622 back to status: open (not "blocked", since
      release-blocked is a permanent exclusion per TASK_QUEUE.md and
      this is a temporary ordering gap, not a permanent defect).
      Discarded the local feature/WF02-REQ328-20260913 branch (never
      pushed, contained only a since-superseded started-event commit,
      no implementation work). Deferring REQ-328 until REQ-327 reaches
      done; will re-poll the queue rather than re-claim 622 again in
      the meantime.
  - req: REQ-327
    event: done
    agent: ORCH
    at: "2026-09-12T20:22:08Z"
    note: >
      The FIRST actual pack content S10 has produced. Five
      exam-configuration entity-definition documents authored under
      priv/packs/bilimbaga/entity_definitions/ -- exam, exam_section,
      exam_question_rule, exam_question_rule_tag, exam_manual_question --
      plus README-constraints.md and a validator test. All five pass
      Letflow.Entities.Definition.Validator.validate/1; 48 tests pass,
      re-run independently by ORCH.
      Context: S10 had produced 31 requirements and ZERO pack content,
      ten of them design-or-decision-record only. The user called out
      that the pipeline was designing requirements to design
      requirements rather than expressing the domain. This is the
      correction, and it is bucket A -- 0022 rule 1 binds bucket B only,
      so naming exams and questions here is the deliverable.
      REVIEWER PASS with an unusually thorough gate: all 33 source
      columns accounted for (carried or dropped with a stated reason,
      none silently missing), enum values verbatim including order,
      DECIMAL precisions matched, and nullability checked across all 26
      carried fields -- a check nobody asked for. It also diffed the
      test's mirror of solution_pack.ex's private
      atomize_definition_json/1 character-by-character and confirmed all
      seven whitelists identical, which is what makes the 48 assertions
      meaningful rather than self-referential.
      Three modelling calls held: tag_ids JSONB is remodelled as a join
      entity (the array-of-references shape is anti-patterns.md:2775) and
      declared as a remodelling rather than a transcription; the nine
      CHECK constraints have no storage-layer equivalent and the loss is
      stated bluntly rather than presented as an equivalent relocation;
      exam_assignment is NOT authored because assignee_id is polymorphic
      across user/department/all, recorded as an open question with three
      candidate shapes and no preference expressed.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-12T20:22:08Z"
    note: >
      ISS-0623 filed (GH#1308): entity-definition `indexes` are inert
      metadata. ddl.ex has zero occurrences of CREATE INDEX and zero
      references to :indexes, so a declared index is validated by Rule 4,
      persisted in the definition document, and then silently dropped --
      while constraints and FKs DO emit. The DDL generator handles two of
      the three declarative structures and skips the third.
      Found by REVIEWER while gating REQ-327 and verified independently
      by ORCH. Severity MAJOR: nothing crashes and nothing leaks, but a
      first-class part of the entity-definition schema does nothing, the
      gap is invisible to the author, and every future pack inherits it.
      REQ-327's five indexes, ported from the source system's own
      measured CREATE INDEX statements, are currently documentation.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-12T20:26:16Z"
    note: >
      Claimed letflow-queue task 623 (WF-03 issue ISS-0623, GitHub
      #1308): lib/letflow/entities/definition/ddl.ex validates
      index_def entries (Rule 4, index_shape_violations/1) but never
      emits CREATE INDEX -- a declared index installs cleanly and does
      nothing, discovered by REVIEWER while gating REQ-327's five
      exam-configuration indexes. Dispatching ISSUE-FIXER for
      root-cause diagnosis per WF-03, though the queue description
      already contains a strong root-cause analysis from REVIEWER;
      ISSUE-FIXER will confirm/extend it and route to CODE-DESIGNER +
      ELIXIR-DEV for the actual fix (emit CREATE INDEX alongside table
      creation, check for cross-entity index-name collision within a
      tenant schema, add a pg_indexes-based test, audit whether any
      other declarative Definition.t() structure is similarly
      validated-but-never-emitted).
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-12T22:56:11Z"
    note: >
      ISS-0624 (queue task 624, GH#1311, discovered during
      WF02-REQ328-20260913) -- Letflow.Entities.Records's private
      field_document/1, which rebuilds a field_def() map from the
      persisted definition_json on the real create_record/2 write
      path, dropped `locales` (and every other optional field_def()
      key except name/type/required/enum_values). This meant ISS-0617's
      own :localized_text clause in Record.Validator.field_subschema/1
      could never match end-to-end, so every :localized_text write
      still raised FunctionClauseError -- the exact crash ISS-0617 was
      filed to fix, reached via a different call path ISS-0617's own
      tests never exercised (they called build_record_schema/1
      directly, never create_record/2). Three of the ten entity types
      REQ-326/REQ-327 authored (category, question, exam) could not
      have records written at all until this fix -- discovered live
      when REQ-328 became the first requirement to exercise the write
      path end to end.

      Fixed via fix/ISS-0624-20260912: audited field_document/1 against
      the full field_def() optional-key set and added every gap found
      -- locales, search_strategy, decimal_precision, decimal_scale,
      queried -- using safe Map.get/2,3 defaults throughout.
      decimal_precision/decimal_scale were also dropped but, verified
      via fail-first testing, not actually crash-causing on this path;
      added anyway for converter completeness, matching the two
      sibling converters (Query.Allowlist.field_document/1,
      TenantProvisioning.field_from_persisted/1) that already carry
      them correctly. default deliberately left out, matching both
      siblings -- no consumer reads it. Added 3 new end-to-end tests
      through the real create_record/2, closing the exact
      test-coverage gap that let ISS-0617 ship incomplete.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS (INV-8:
      independently reproduced the crash via revert/restore, confirmed
      neither sibling converter shares this defect; INV-1: confirmed
      the fix only widens what's read from the tenant's own
      already-persisted definition) -> REVIEWER PASS (confirmed design
      conformance against Definition.field_def() and
      field_subschema/1's actual needs, independently ran the full
      suite: only known pre-existing/environmental failures, plus one
      self-documented ISS-0624 canary test in
      test/letflow/packs/bilimbaga_pack_install_test.exs now failing
      because the fix works -- flagged as its own non-blocking
      follow-up, not a regression). TEST-DESIGNER/TEST-DESIGN-VALIDATOR
      skipped per established precedent -- both hard-gate reviewers
      independently verified the 3-test end-to-end coverage as
      comprehensive. docs/issues/ISS-0624.yaml flipped to status:
      resolved.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-12T23:39:02Z"
    note: >
      Close-out for ISS-0623 (queue task 623, GH#1308), on branch
      issue/ISS-0623-20260913 (commit f169e369). Added
      DDL.index_create_statements/2, emitting one `CREATE [UNIQUE] INDEX`
      per declared index_def, wired into
      TenantProvisioning.create_and_populate_entity_table/3 alongside the
      existing column/constraint/FK DDL. Design in
      lib/letflow/design/iss0623-entity-index-ddl-emission.md settled
      scope: every declared index is always emitted, including one
      overlapping a unique constraint, with no dedup; cross-entity-type
      index-name collision within a tenant schema is a documented
      follow-on rather than a defect fixed here (it now surfaces loudly
      as a real Postgres error instead of silently doing nothing);
      retrofitting indexes onto an already-promoted entity type is
      likewise a named follow-on; :localized_text-typed fields are
      defensively rejected from index_def since their storage shape has
      no single column to index. Two supporting fixes found and made
      during implementation: document_from_persisted/1 was dropping the
      :indexes key entirely (would have made the whole fix a no-op
      against real persisted definitions), and
      mark_ddl_failed_and_return/3 / run_column_promotion/1 crashed on
      transaction handling against a genuinely-aborted connection.
      Verified against real Postgres pg_indexes, not just generated DDL
      text. Pipeline: SECURITY-REVIEWER PASS, REVIEWER PASS,
      TEST-DESIGN-VALIDATOR PASS (69/69 targeted tests), TEST-RUNNER
      re-verification after rebase onto main: full suite 4053 passed,
      exit 0. docs/issues/ISS-0623.yaml flipped to status: resolved.
  - req: REQ-328
    event: revised
    agent: ORCH
    at: "2026-09-12T23:50:31Z"
    note: >
      Addendum to WF03-ISS0624-20260912 (queue task 624), on the same
      branch fix/ISS-0624-20260912: test/letflow/packs/
      bilimbaga_pack_install_test.exs (REQ-328's install-proof test,
      already merged) carried a self-documented canary asserting
      ISS-0624's crash as a workaround, with a comment naming exactly
      what to re-enable once the bug was fixed. Now that ISS-0624 is
      fixed on this same branch, completed REQ-328's originally-
      deferred acceptance criteria per that comment's own instructions:
      real category/question/exam record writes with their
      :localized_text fields (name/stem/title) round-tripping all
      three of kk/ru/en; a new test proving question's own FK to
      category bites (distinct from the pre-existing question_tag->tag
      proof); a real exam_question_rule referencing a real exam; and
      the many-to-many join rewired to assert it returns the actual
      tagged question rather than a placeholder is_list(rows) check.
      Pre-existing orphan-FK-violation tests preserved unchanged. Test-
      only, no production code touched, matching REQ-328's own scope
      fence. SECURITY-REVIEWER PASS (INV-1, INV-8), REVIEWER PASS
      (confirmed conformance against REQ-328's actual acceptance
      criteria, independently ran the full suite: only known
      pre-existing/environmental failures). No status-field change to
      REQ-328 itself -- it was already done; this only completes its
      test's deferred coverage.
  - req: REQ-328
    event: done
    agent: ORCH
    at: "2026-09-13T02:57:45Z"
    note: >
      Final close-out for REQ-328 (queue task 622, GH#1303) --
      correcting ORCH's own earlier oversight. PR #1312
      (feat/WF02-REQ328-20260913) correctly left this requirement
      `status: pending`, blocked on ISS-0624, having found live during
      implementation that Records.field_document/1 dropped locales
      and every :localized_text write (category, question, exam)
      raised FunctionClauseError -- an honest escalation per the
      requirement's own finding clause, not a workaround. The prior
      WF03-ISS0624-20260912 SCOPE-CHANGE done-event entry in this
      volume incorrectly stated "no status-field change to REQ-328
      itself -- it was already done"; that assumption was wrong. This
      entry corrects it.

      With ISS-0624 now fixed and its own addendum completing REQ-328's
      originally-deferred test coverage (both already recorded in this
      volume), all of REQ-328's acceptance criteria are independently
      re-verified as satisfied on main: `mix test
      test/letflow/packs/bilimbaga_pack_install_test.exs` -> 5 passed.
      A real install_result map was produced and quoted in the test's
      own IO.puts output (ten installed_entity_definitions entries,
      each status: "installed" -- the hardcoded string, distinct from
      the persisted EntityDefinition row's :inactive atom); the
      pre-activation create_record/2 call proven to return
      {:error, {:definition_not_found, _}}; role_mapping_checklist
      reports every declared role bound: false (correct, per decision
      0029's permanent advisory-only stance); real category/question/
      exam/tag/question_tag/exam_question_rule records written and
      read back; the question->category FK and question_tag->tag FK
      both proven to reject with a real Postgres foreign_key_violation;
      the question.stem :localized_text write round-trips kk/ru/en;
      the through many-to-many join returns the actual tagged
      question. No production module touched (git diff --name-only
      confirmed empty against lib/letflow/definitions/solution_pack.ex,
      lib/letflow/entities/**, and priv/repo/migrations/ throughout
      both the original PR and the addendum). docs/requirements.yaml
      flipped pending -> done.
  - req: REQ-329
    event: started
    agent: ORCH
    at: "2026-09-13T03:42:20Z"
    note: >
      Claimed letflow-queue task 625 (S10 P3, bucket A). Author five
      live-session entity definitions (session, session_question,
      session_answer, session_event, session_question_score) under
      priv/packs/bilimbaga/entity_definitions/, porting BilimBaga's
      014/015/016 migrations and model.go structs, following REQ-326/
      REQ-327's established conventions (no id/timestamps/created_by,
      composite natural keys as unique constraints not primary keys,
      relocated CHECK constraints in README-constraints.md). Notably:
      session_event is a deliberate addition (event_type field) beyond
      its tab_switch_events source, which the source itself validates
      then discards; and all five ON DELETE divergences (Letflow's
      fixed RESTRICT vs. the source's CASCADE on most FKs) must be
      tabulated. No pack installation, no Elixir -- that is a later
      requirement (REQ-334 per this entry's own description).
      Dispatching to ELIXIR-DEV given the real validator-execution
      work involved, despite the nominal REQ-ANALYST owner field,
      matching the REQ-326/327/328 precedent.
  - req: REQ-330
    event: done
    agent: ORCH
    at: "2026-09-13T04:23:39Z"
    note: >
      REQ-330 (queue task 626, GH#1317, no depends_on) implemented via
      feature/WF02-REQ330-20260913: design record classifying S10 P3's
      five live-exam-session behaviours -- deadline enforcement (SPLIT:
      storage bucket A via session.expires_at, enforcement bucket C;
      TIMER-node hypothesis tested and fails, Scheduler.Timer.
      arm_changeset/2 requires :instance_id, decision 0022 confirms the
      exam session is not a process instance); auto-submission of
      expired sessions (bucket B -- Letflow.Scheduler.Poller already
      the right shape, expressible without naming the exam domain,
      REQ-331's premise stands); autosave (bucket C, measured not
      asserted -- EntityTypeInstance's one-row-per-entity-TYPE and
      EventStore's lock("FOR UPDATE") on instance_sequence quoted,
      concluded within capacity at ~33 writes/sec exam-realistic
      scale, but SECURITY-REVIEWER flagged the lack of server-side
      rate limiting as an unaddressed gap for REQ-332); per-question
      scoring (bucket C on a newly-found basis -- decision 0022's "Lua
      grading rules ship in the pack" claim is NOT realized today, no
      node-dispatch path in transition.ex's eight node_type values
      reaches Letflow.Engine.Lua.Executor, recorded as a finding
      rather than silently patched into 0022's own text); anti-cheat
      signal capture (SPLIT: storage bucket A via session_event entity
      records chosen over raw EventStore events or an aggregate
      counter, policy bucket C, answering the stage file's own open
      question). Proposes 4 modules under lib/letflow/exam/ (Session,
      QuestionSetResolver, Scoring, AntiCheat), each with a rule-2
      justification table row. No implementation code, no file under
      lib/letflow/exam/ created.

      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR FAIL then PASS
      (first pass caught a factual node-type miscount carried into
      both documents and a missing FR-BB source citation for deadline
      enforcement; both fixed and reconfirmed) -> SECURITY-REVIEWER
      PASS (confirmed tenant-isolation via prefix parameters on every
      proposed @spec, confirmed the Lua-dispatch gap is a genuine
      absent path not a half-wired mechanism, flagged the autosave
      capacity gap and the anti-cheat debounce's unimplemented-but-real
      status as follow-ups for REQ-332/REQ-333) -> REVIEWER PASS
      (confirmed no silent re-decision of 0022, confirmed the stage
      file's anti-cheat open question is genuinely answered not
      restated, independently re-verified both corrected defects,
      independently re-examined QuestionSetResolver's bucket-C
      justification specifically rather than rubber-stamping it).
      Scope fence held -- only the two design-record files plus
      docs/requirements.yaml status bookkeeping. docs/requirements.yaml
      flipped pending -> done.
  - req: REQ-329
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T04:58:22Z"
    note: >
      Close-out for run WF02-REQ329-20260913 (queue task 625, GH#1316),
      branch feature/WF02-REQ329-20260913 (commit 98decbd8). Authored
      five new entity-definition documents under
      priv/packs/bilimbaga/entity_definitions/ -- session,
      session_question, session_answer, session_event,
      session_question_score -- porting BilimBaga's migrations
      014/015/016. All five independently pass the real
      Letflow.Entities.Definition.Validator.validate/1. Recorded in
      README-constraints.md: the ON-DELETE divergence table (8 real
      FKs, 3 matching and 5 diverging against Letflow's fixed RESTRICT)
      and the TIMESTAMPTZ/expires_at invariant. session_event's
      deliberate event_type addition beyond its tab_switch_events
      source is honestly labeled as an addition, not silently ported.

      REVIEWER independently re-verified every claim against the
      actual BilimBaga source files (the three migrations plus
      service.go/repository.go) and confirmed a genuine discrepancy in
      the requirement's own acceptance-criteria text (title says
      "seven FKs," body enumerates eight) was correctly flagged rather
      than silently reconciled either way. Test coverage extended in
      the existing test/letflow/packs/bilimbaga_entity_definitions_test.exs
      (one narrow rework round fixed a false-coverage claim in that
      file's own moduledoc, per TEST-DESIGN-VALIDATOR). Gated PASS by
      REVIEWER and (after that one rework round) TEST-DESIGN-VALIDATOR.

      TEST-RUNNER verified a full green suite (4120/4120 passed) plus
      a clean `mix letflow.check` on this branch. ORCH separately found
      and fixed a live, currently-necessary tripwire triage in
      test/letflow/simulation/req207_vortex_test.exs for both REQ-329
      and the concurrently-filed REQ-331 -- both admitted to the
      unconditional allowed_ids tier, since neither touches the entity
      HTTP query surface.

      Pushed the branch, opened the PR, waited for CI green, confirmed
      mergeStateStatus CLEAN, squash-merged, deleted the branch, closed
      GitHub mirror issue #1316 with an evidence comment citing the PR
      and TEST-RUNNER's results. docs/requirements.yaml flipped
      pending -> done. Local main synced to the merge commit.
  - req: REQ-331
    event: blocked
    agent: ORCH
    at: "2026-09-13T05:10:57Z"
    note: >
      Claimed letflow-queue task 627 via get_next_task, twice (queue
      does not track depends_on, so it kept re-offering 627 once
      released). REQ-331 is explicitly "GATED ON REQ-330" per its own
      description -- REQ-330 (task 626, design record deciding which
      P3 behaviours are bucket B vs. bucket C) is still status: pending
      in docs/requirements.yaml, and confirmed locked by a different
      agent when probed directly (POST /tasks/626/lock -> "task is
      locked by a different agent"), so REQ-330 is in flight elsewhere,
      not merely unclaimed. No code changes made. Released queue task
      627 back to status: open (not "blocked" -- temporary ordering
      gap, not a permanent defect, matching the REQ-328/REQ-327
      precedent). Deferring REQ-331 until REQ-330 reaches done; will
      re-poll the queue rather than re-claim 627 in the meantime.
  - req: REQ-331
    event: started
    agent: ORCH
    at: "2026-09-13T05:39:39Z"
    note: >
      Re-claimed letflow-queue task 627 -- REQ-330 (design record) is
      now status: done, merged via PR #1323 (commit afcea13b).
      Confirmed REQ-330's own verdict on this behaviour BEFORE
      dispatching, per this requirement's own acceptance criterion:
      lib/letflow/design/req330-exam-live-session.md section 2, "Verdict:
      bucket B -- CONFIRMED, not overturned." Build an eighth per-tenant
      sweep on the existing lib/letflow/scheduler/poller.ex (no new
      GenServer/supervisor child/ticker), matching the existing seven
      sweeps' conventions (fresh config read per tick,
      Admission.global_cap/0 read live, per-tenant failure isolation via
      try/rescue), with FOR UPDATE SKIP LOCKED concurrency, per-record
      transaction isolation, and the completion timestamp set to the
      deadline value (not sweep time) -- porting BilimBaga's
      autojob.go/FR-BB310 behaviour without any of the six forbidden
      domain words (exam, session, question, candidate, submit, grade).
      INV-1 applies; SECURITY-REVIEWER gates it. Dispatching to
      ELIXIR-DEV.
  - req: REQ-332
    event: started
    agent: ORCH
    at: "2026-09-13T05:50:35Z"
    note: >
      Claimed letflow-queue task 628 (GH#1319, S10 P3, bucket C). Build
      the live-session runtime under lib/letflow/exam/ -- start,
      autosave, submit, score -- limited to exactly the three modules
      REQ-330's rule-2 table authorises for this requirement:
      Letflow.Exam.Session, Letflow.Exam.QuestionSetResolver,
      Letflow.Exam.Scoring (Letflow.Exam.AntiCheat is REQ-333's scope).
      No migration, no Ecto schema -- all five session entity types
      (REQ-329) read/written exclusively through Letflow.Entities.Records
      and Letflow.Entities.Query. Autosave write path is whatever
      REQ-330 concluded (confirmed bucket C, ordinary path, no fast
      path) -- REQ-332's own conditional resolves to the "ordinary
      path" branch, not the "different write path" branch. No
      gen_statem/GenServer/process-per-session (REQ-045/decision 0022
      non-negotiable). Dispatching to ELIXIR-DEV.
  - req: REQ-331
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T06:46:53Z"
    note: >
      Close-out for run WF02-REQ331-20260913 (queue task 627, GH#1318),
      branch feature/WF02-REQ331-20260913 (commit 20b1c5e3). REQ-330's
      verdict quoted per REQ-331's own acceptance criterion:
      lib/letflow/design/req330-exam-live-session.md section 2, "Verdict:
      bucket B -- CONFIRMED, not overturned" -- REQ-331 was built exactly
      as filed, not re-scoped.

      New module lib/letflow/scheduler/record_deadline_sweep.ex (plain
      functional module, not a process) added as an eighth per-tenant
      sweep inside the existing lib/letflow/scheduler/poller.ex GenServer's
      tick pipeline -- no new GenServer/supervisor child/ticker, matching
      REQ-330's reuse-Poller verdict and the existing seven sweeps'
      conventions (fresh config read per tick, live
      Admission.global_cap/0 read inline, per-tenant try/rescue
      isolation). Config-driven rule declaration
      (Application.get_env(:letflow, :deadline_sweep, ...),
      platform-operator/deploy-time only, no HTTP route or tenant-facing
      path reaches it, no new permission atom needed) -- reasoning stated
      explicitly in the moduledoc per its own acceptance criterion.

      Real Postgres FOR UPDATE SKIP LOCKED concurrency (empirically
      validated by ELIXIR-DEV: removing the lock clause caused 5/5
      failures in a race test, restoring it passed 3/3), per-record
      transaction isolation (a genuine unmocked schema-evolution
      validation failure proven not to block sibling records), exact
      completion-timestamp-equals-deadline assertion (not a range), and
      an explicit INV-1 cross-tenant isolation test -- all four
      independently re-verified by SECURITY-REVIEWER and REVIEWER, not
      merely trusted from ELIXIR-DEV's report. Zero forbidden domain
      vocabulary (exam/session/question/candidate/submit/grade) in any
      new artefact, per a mechanical grep re-run independently three
      times (ELIXIR-DEV, SECURITY-REVIEWER, REVIEWER) -- one sanctioned
      bare-path FR-BB310 citation is the only exception, exactly per the
      requirement's own escape clause.

      Also: the Req207VortexTest tripwire triage for REQ-331 was already
      handled in an earlier, separate PR (#1325, commit 67b861ad) during
      the same session, before REQ-330 unblocked this requirement -- no
      new tripwire work needed here.

      Gated PASS by SECURITY-REVIEWER (INV-1 tenant isolation confirmed),
      REVIEWER (idiom/convention/decision-record consistency confirmed),
      and TEST-DESIGN-VALIDATOR (full acceptance-criteria-to-test mapping
      confirmed). TEST-RUNNER verified a full green suite (4124/4124
      passed) plus a clean `mix letflow.check` on this branch.
      docs/requirements.yaml flipped pending -> done.
  - req: REQ-333
    event: blocked
    agent: ORCH
    at: "2026-09-13T06:59:26Z"
    note: >
      Claimed letflow-queue task 629 via get_next_task. Checked
      REQ-333's own enforced "ambiguity escape" first: REQ-330's design
      doc (lib/letflow/design/req330-exam-live-session.md section 5)
      DOES specifically name the storage shape (entity-record events
      via the session_event entity REQ-329 defines, written through
      Letflow.Entities.Records, per decision 0030 Finding 2) and the
      write-amplification mitigation (a configured debounce window on
      session_event.occurred_at) -- so the ambiguity escape does NOT
      apply; REQ-333 is not blocked on that count. However, REQ-333's
      own acceptance criteria hard-require the 'submit' branch to
      "auto-submit AND SCORE the session" -- scoring is REQ-332's
      deliverable (lib/letflow/exam/ does not exist yet on disk;
      confirmed via `ls`), and REQ-332 (queue task 628) is `status:
      pending` and locked by a different agent (confirmed via direct
      POST /tasks/628/lock probe), so it is in flight elsewhere, not
      merely unclaimed. This is a real dependency the queue's own
      depends_on: [] field does not capture. No code changes made.
      Released queue task 629 back to status: open. Deferring REQ-333
      until REQ-332 reaches done; will re-poll the queue rather than
      re-claim 629 in the meantime.
  - req: REQ-332
    event: done
    agent: ORCH
    at: "2026-09-13T07:02:32Z"
    note: >
      REQ-332 (queue task 628, GH#1319) implemented via
      feature/WF02-REQ332-20260913 (commits 47c88222/7a7216f1/2d35a29b).
      Built exactly the three modules REQ-330's rule-2 table authorised:
      lib/letflow/exam/session.ex (~894 lines), question_set_resolver.ex
      (109 lines), scoring.ex (246 lines) -- 1,249 total lines, matching
      the module-set REQ-330 authorised exactly (Letflow.Exam.AntiCheat
      left unbuilt, REQ-333's scope). No migration, no Ecto schema --
      all reads/writes go through Letflow.Entities.Records/Query against
      REQ-329's five session entity types. Autosave uses the ordinary
      write path per REQ-330's confirmed bucket-C conclusion. No
      gen_statem/GenServer/Registry anywhere in lib/letflow/exam/
      (grep confirmed empty by both ELIXIR-DEV and REVIEWER
      independently).

      Pipeline: ELIXIR-DEV implemented with 45 new tests (seeded
      question-set reproducibility, session-snapshot isolation, all six
      eligibility checks -- five real, one (:not_assigned) an honestly
      documented no-op pending an exam_assignment entity that does not
      exist yet -- server-side deadline enforcement, answer-shape
      validation, scoring arithmetic for every rule including the
      no-correct-option error case and the short-text
      grading_pending path, submit idempotency and a genuine
      concurrent-submit row-lock test) -> SECURITY-REVIEWER PASS
      (independently ran the suite, verified INV-1 tenant isolation via
      two real cross-tenant/cross-candidate tests, verified server-side
      deadline enforcement and ownership-before-write ordering,
      confirmed the submit-lock test proves a genuine lock not a lucky
      interleaving; flagged the :not_assigned gap and an unlocked
      concurrent create/3 race as non-blocking follow-ups) ->
      REVIEWER FAIL then PASS (first pass found the concurrent create/3
      race SECURITY-REVIEWER had flagged as a follow-up was in fact
      undocumented anywhere, unlike the honestly-recorded :not_assigned
      finding -- required either a real fix or explicit documentation;
      ELIXIR-DEV chose a real fix, wrapping create/3 in a transaction
      with a new lock_exam_row/2 FOR-UPDATE lock mirroring submit/3's
      existing lock_session_row/2, plus a new concurrent-create test;
      REVIEWER's re-check empirically disabled the lock, confirmed the
      new test fails exactly as predicted, restored it, confirmed green,
      then signed off lib/letflow/design/req330-exam-live-session.md's
      Section 7 table for all three modules, flagging the resulting
      exam-level (not candidate+exam-level) lock granularity as a
      non-blocking scale follow-up) -> TEST-RUNNER independently
      confirmed a full mix letflow.check run clean except the two
      pre-existing, environment-only rust-toolchain failures (report:
      test/reports/report-20260913-WF02-REQ332-20260913.yaml).

      Two non-blocking follow-ups carried forward for a later
      requirement/REQ-ANALYST: (1) the :not_assigned eligibility check
      is a documented no-op until an exam_assignment entity is
      authored -- an unassigned candidate can currently start/complete
      a session for any active in-window exam in their own tenant; (2)
      create/3's exam-row lock serialises ALL candidates starting the
      same exam concurrently, a possible throughput bottleneck at scale
      once a (candidate_id, exam_id)-scoped lock target exists.

      docs/requirements.yaml flipped pending -> done.
  - req: REQ-333
    event: started
    agent: ORCH
    at: "2026-09-13T08:00:09Z"
    note: >
      Claimed letflow-queue task 629 (GH#1320, S10 P3, bucket C policy +
      bucket A storage). Checked REQ-333's own enforced ambiguity
      escape first: REQ-330's design doc section 5 DOES name the
      storage shape specifically enough to build without interpretation
      -- session_event entity records (REQ-329), written through
      Letflow.Entities.Records, chosen over raw EventStore events or an
      aggregate counter (decision 0030 Finding 2) -- and DOES specify
      the write-amplification mitigation: record_signal/4 reads the
      session's most recent session_event.occurred_at via an ordinary
      Letflow.Entities.Query read and rejects a signal inside a
      configured debounce window. Ambiguity escape does NOT apply;
      proceeding, not blocking. Build Letflow.Exam.AntiCheat (the
      fourth module in REQ-330's rule-2 table, PENDING sign-off) --
      validates exactly three signal types (tab_switch/blur/
      fullscreen_exit), derives action_taken from the exam's
      on_tab_switch config (never from caller input, per service.go
      AC-10 -- the security-relevant property), branches log/warn/
      submit (submit triggers Letflow.Exam.Session.submit/3), and
      enforces the debounce window. No HTTP route, no new permission
      atom. Dispatching to ELIXIR-DEV.
  - req: REQ-334
    event: blocked
    agent: ORCH
    at: "2026-09-13T08:29:39Z"
    note: >
      Claimed letflow-queue task 630 via get_next_task -- REQ-332 just
      landed (merged, PR #1328), which unblocked REQ-333 (queue task
      629), but 629 is now locked by a different agent (confirmed via
      direct POST /tasks/629/lock probe), in flight elsewhere. REQ-334
      itself hard-requires "one row per module actually created by
      REQ-332 AND REQ-333" in its bucket-C inventory update (acceptance
      criterion 1) -- REQ-333 is still status: pending, so the module
      list this requirement must inventory is incomplete. Attempting it
      now would either omit REQ-333's modules (failing the requirement's
      own completeness criterion) or require redoing the work once
      REQ-333 lands. No code/doc changes made. Released queue task 630
      back to status: open. Deferring REQ-334 until REQ-333 reaches
      done; will re-poll the queue rather than re-claim 630 in the
      meantime.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-13T09:29:24Z"
    note: >
      Claimed letflow-queue task 631 (WF-03 issue ISS-0631, GitHub
      #1330): lib/letflow/exam/session.ex:911's session_view/1 calls
      String.to_existing_atom on the persisted "in_progress" status
      string, but :in_progress appears nowhere in lib/ as an executed
      atom literal (only inside @type specs), so Session.create/3's
      success or ArgumentError crash depends on incidental BEAM atom-
      table load order rather than the code's own structure. Found
      live by ELIXIR-DEV while writing REQ-333's tests, in an isolated
      parallel test partition where nothing else had interned the
      atom yet; worked around only in the test file, not fixed in
      production code (out of REQ-333's scope -- session.ex belongs to
      REQ-332). Fix direction already clear and precedented: replace
      with an explicit status_atom/1-style mapper, matching this same
      module's own existing idiom for exactly this problem
      (question_type_atom/1 line 918, polarity_atom/1 line 924).
      Dispatching ISSUE-FIXER to confirm/extend the diagnosis (and
      audit outcome_from_session/1's similar-shaped atoms per the
      issue's own suggestion) before routing to CODE-DESIGNER/
      ELIXIR-DEV for the actual fix.
  - req: REQ-333
    event: done
    agent: ORCH
    at: "2026-09-13T09:51:21Z"
    note: >
      REQ-333 (queue task 629, GH#1320) implemented via
      feature/WF02-REQ333-20260913 (commits 3c3a9712/953d6adf). Built
      Letflow.Exam.AntiCheat (309 lines) -- the fourth and final module
      REQ-330's rule-2 table authorised. Storage per decision 0030
      Finding 2's verbatim conclusion: session_event entity records
      (REQ-329) via Letflow.Entities.Records, not raw EventStore
      events or an aggregate counter. Ambiguity escape checked first
      and did NOT apply -- REQ-330 named the storage shape and the
      debounce mitigation specifically enough to build without
      interpretation.

      record_signal/4 accepts exactly three signal types
      (tab_switch/blur/fullscreen_exit); action_taken is derived
      SOLELY from the exam's on_tab_switch config -- no action_taken
      parameter exists anywhere in the public function signature, the
      strongest form of service.go AC-10's guarantee (verified by
      SECURITY-REVIEWER tracing the code, not just reading the test);
      ownership/in-progress/deadline guards run before any write;
      log/warn/submit branches all tested end-to-end including submit
      actually transitioning the session's real stored status; the
      debounce mitigation (reads the session's most recent
      session_event.occurred_at via an ordinary Letflow.Entities.Query
      read, rejects inside a configured window) is proven by a
      repeated-signal test, not asserted. No HTTP route, no new
      permission atom, no migration. grep for
      gen_statem/GenServer/Registry across all four lib/letflow/exam/
      modules returns nothing.

      Pipeline: ELIXIR-DEV (60 tests total across test/letflow/exam/)
      -> SECURITY-REVIEWER PASS (independently traced the AC-10
      guarantee to its strongest form, verified INV-1 via a real
      cross-tenant test, flagged the debounce check-then-insert as
      non-atomic under concurrent requests from one session -- bounded
      amplification, non-blocking) -> REVIEWER PASS (confirmed
      idiomatic consistency with session.ex's sibling modules,
      confirmed no cheap idiomatic fix exists for the debounce race
      without inventing new entity-subsystem machinery, agreed
      non-blocking; signed off design doc Section 7's fourth and final
      row) -> TEST-RUNNER independently confirmed a full mix
      letflow.check run clean except the two pre-existing,
      environment-only rust-toolchain failures.

      ELIXIR-DEV also surfaced a genuine pre-existing bug in REQ-332's
      already-merged session.ex (String.to_existing_atom on a status
      atom never used as an executed literal elsewhere -- a
      load-order-dependent ArgumentError risk) while writing this
      requirement's tests. Correctly left session.ex untouched (out of
      REQ-333's authorised scope) and filed it as ISS-0631 (queue task
      631, GH#1330) instead of silently working around it in
      production code; SECURITY-REVIEWER and REVIEWER both confirmed
      AntiCheat's own submit path does not exercise the fragile code
      path.

      With this requirement done, all four rows in REQ-330's rule-2
      table (Session, QuestionSetResolver, Scoring, AntiCheat) carry a
      REVIEWER PASS sign-off -- P3's stage-file exit condition ("each
      module carries its REVIEWER bucket-C sign-off") is met.
      docs/requirements.yaml flipped pending -> done.
  - req: REQ-334
    event: started
    agent: ORCH
    at: "2026-09-13T10:08:38Z"
    note: >
      Claimed letflow-queue task 630 (GH#1321, S10, stage-10 bucket-C
      inventory bookkeeping -- not a bucket deliverable itself).
      Populate docs/migration/stage-10-bilimbaga-vertical.md's
      bucket-C inventory table with one row per module actually under
      lib/letflow/exam/ (Session, QuestionSetResolver, Scoring,
      AntiCheat), copying REQ-330's rule-2 sentences verbatim and
      pointing at each module's real REVIEWER sign-off; report total
      module/line count with a quoted command; record what LEFT bucket
      C (REQ-329's five entity definitions to bucket A, REQ-331's
      deadline sweep to bucket B) against P3's phases-table row
      predicting bucket C; close the anti-cheat open question with a
      pointer to REQ-330's decision record (annotated, not deleted);
      record certificate issuance as NOT delivered and why; state
      whether REQ-330 overturned any REQ-ANALYST starting verdict;
      leave decision 0022 itself unmodified. Also flip REQ-329 through
      REQ-333's status entries in docs/requirements.yaml (already done
      individually at each requirement's own close-out -- this task
      verifies and, if any drifted, corrects) and append their
      status-history events (already appended individually -- this
      task's own acceptance criteria require the close-out to quote
      them, not necessarily append new ones for already-recorded
      events). Dispatching to DOC-UPDATER.
  - req: REQ-334
    event: done
    agent: ORCH
    at: "2026-09-13T10:13:16Z"
    note: >
      REQ-334 (queue task 630, GH#1321) implemented via
      feature/WF02-REQ334-20260913 (commit 3001a018), docs-only, by
      DOC-UPDATER. docs/migration/stage-10-bilimbaga-vertical.md's
      bucket-C inventory table replaced "(none yet)" with one row per
      module actually under lib/letflow/exam/ (Session,
      QuestionSetResolver, Scoring, AntiCheat), rule-2 sentences copied
      verbatim from lib/letflow/design/req330-exam-live-session.md
      section 7, REVIEWER sign-off column pointing at REQ-332's and
      REQ-333's real dated PASS entries in that same table. Measured
      against the real tree and quoted: `ls lib/letflow/exam/` (4
      files) and `wc -l lib/letflow/exam/*.ex` (309+109+246+941 = 1605
      total lines), dated 2026-09-13.

      Recorded what left bucket C: REQ-329's five session entities to
      bucket A, REQ-331's deadline sweep to bucket B -- P3's
      phases-table row predicted bucket C but P3 as executed also
      produced bucket-A and bucket-B work, held for four of six
      behaviours REQ-330 analyzed. Anti-cheat's open question marked
      ANSWERED with a pointer to decision 0030, original question text
      kept and annotated, matching decision 0027's precedent style.
      Certificate issuance recorded as NOT delivered by P3 and why
      (needs PDF/QR rendering, mix.exs carries no such dependency,
      gaps 4/5 mechanism-settled by decision 0027 but capabilities
      open and unowned). Overturned-verdicts finding: none of REQ-330's
      five bucket verdicts reversed REQ-ANALYST's starting position
      (design doc section 6's own explicit statement), but decision
      0030's Finding 1 (the Lua-grading-rules gap against decision
      0022's own bucket table) is named as a real, non-reversal finding
      worth keeping visible. decision 0022 confirmed untouched (`git
      diff --name-only` quoted by DOC-UPDATER, shows only the stage
      file). REQ-329 through REQ-333 confirmed already status: done in
      docs/requirements.yaml with no drift found; their five
      status-history done-events already present in this volume are
      quoted in DOC-UPDATER's own report rather than re-appended.
      docs/requirements.yaml flipped pending -> done for REQ-334
      itself. This closes out S10 P3.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-13T12:31:22Z"
    note: >
      ISS-0631 (queue task 631, GH#1330) fixed via
      issue/ISS-0631-20260913 (commit 5007186e), PR #1333. Added
      session_status_atom/1 to lib/letflow/exam/session.ex, an
      explicit closed-clause mapper for the four valid session
      statuses (in_progress/submitted/auto_submitted/grading_pending),
      matching this same module's own existing idiom
      (question_type_atom/1, polarity_atom/1). Replaces both
      String.to_existing_atom/1 call sites the issue named
      (session_view/1 at line 911 and outcome_from_session/1 at line
      865), removing the BEAM atom-table load-order dependency and
      turning a corrupted persisted status into a FunctionClauseError
      instead of the previous opaque ArgumentError.

      Session interrupted mid-pipeline by a power outage after the
      design gate (commits 2a496434/2fe3f87e) but before commit;
      ELIXIR-DEV's implementation and TEST-DESIGNER's tests survived
      uncommitted in the working tree and were verified intact on
      resume: `mix compile` clean, `mix test
      test/letflow/exam/session_test.exs` 30/30 passing (4 new --
      happy path, auto-submit path, corrupted-status raises the
      specific FunctionClauseError type, and a structural test
      grepping session.ex's own source for the absence of
      to_existing_atom/to_atom).

      Merging PR #1333 hit a real conflict with main (REQ-333/REQ-334
      landed first, both appending to this same v15.yaml tail and its
      index note) -- resolved by composing both sides in `at:`
      timestamp order (SCOPE-CHANGE started 09:29:24Z, before REQ-333
      done 09:51:21Z) with no reordering needed, same pattern as this
      volume's earlier REQ-333/REQ-334 conflict resolutions. No
      overlap with REQ-333/REQ-334's own changes: REQ-333 built
      AntiCheat (a different module) and explicitly left session.ex
      untouched, filing this issue rather than fixing it in scope.
      docs/requirements.yaml carries no entry for ISS-0631 (issue
      tracked in docs/issues/ISS-0631.yaml, not requirements.yaml);
      no requirement status flip applies here.
# ─────────────────────────────────────────────────────────────────────────────
# VOLUME 15 — CLOSED 2026-09-13. DO NOT APPEND TO THIS FILE.
# This volume holds the complete requirement run history from 2026-09-12 through
# 2026-09-13 (32 entries, 1245 frozen-prefix lines / 1278 total lines with this
# closure footer). It is frozen: every line above this
# comment is preserved byte-for-byte. Closed at issue/ISS-0631-20260913's own
# ISS-0631 done-event append, which brought this file to 1245 lines -- over its
# declared roll_rule ceiling (max_lines: 1200) -- confirmed via `wc -l` on the
# working-tree file per this volume's own "WHEN THIS VOLUME IS FULL" procedure.
# Reads of this volume are TARGETED reads only -- a line range or a pattern
# match, never a whole-file read. Use whichever your shell has:
#   Read tool with offset/limit (works everywhere), or
#   Bash:       grep -n PATTERN FILE   /   sed -n 'START,ENDp' FILE
#   PowerShell: Select-String -Path FILE -Pattern PATTERN
#               (Get-Content FILE)[START..END]
#
# NEW ENTRIES GO TO THE CURRENT VOLUME. Find it in:
#   docs/status/requirement_status.index.yaml
# which, as of this closure, names:
#   docs/status/requirement_status.v16.yaml
#
# Why this file was closed: ORCH's own ISS-0631 done-event append (appended
# while resolving this PR's merge conflict with main's already-merged
# REQ-333/REQ-334 appends to this same file) brought the on-disk count to 32
# entries across 1245 lines -- over the roll ceiling. No entry above this
# footer was moved, edited or deleted.
#
# KNOWN ANOMALIES IN THIS VOLUME: this volume's tail (REQ-333 started through
# REQ-334 done, plus this SCOPE-CHANGE done entry) was composed across a real
# three-way merge conflict between issue/ISS-0631-20260913 and origin/main
# (REQ-333/REQ-334 landed on main first). Resolved by ordering the conflicting
# entries by their own `at:` timestamps with no reordering needed -- same
# pattern as this volume's own earlier REQ-333/REQ-334 conflict resolutions
# recorded in the index's volume-15 note history.
