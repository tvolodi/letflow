# Letflow — Requirement run history — VOLUME 16 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v15.yaml (volume 15, closed,
#   1,245 frozen-prefix lines / 1,278 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v16.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v16.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v16.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v16.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: REQ-335
    event: started
    agent: ORCH
    at: "2026-09-13T13:45:15Z"
    note: >
      Claimed letflow-queue task 632 (GH#1336, S10 P4, bucket C route
      surface). Owner ELIXIR-DEV, deliberately deviating from P4's
      FRONTEND-DEV default per the requirement's own owner-rationale
      paragraph: the entire deliverable is a new lib/letflow/routers/
      module plus new lib/letflow/api/authorization.ex permission
      atoms plus tenant-prefix-scoped calls into lib/letflow/exam/ --
      none of it touches web/. Build Letflow.Routers.ExamSessions (or
      a clearer name ELIXIR-DEV settles on), mounted at the normal
      authenticated /api/v1 forward, NOT decision 0028's
      unauthenticated capability-handle pattern (a candidate sitting
      an exam is an authenticated tenant user). Covers exactly session
      start/autosave/submit/anti-cheat-signal plus a session-state
      read, delegating to REQ-332's Letflow.Exam.Session and REQ-333's
      Letflow.Exam.AntiCheat -- no new runtime logic. No CODE-DESIGNER
      gate (matches REQ-310/311's precedent, route surface over an
      already-designed shape) UNLESS the is_correct/answer-key
      redaction open question this requirement flags turns out
      genuinely undecided, in which case ELIXIR-DEV must stop and
      escalate to CODE-DESIGNER rather than guess. Explicitly excludes
      the manual grading-queue admin workflow, adaptive selection, and
      cross-session history views -- none of their runtimes exist.
      Dispatching to ELIXIR-DEV.
  - req: REQ-335
    event: done
    agent: ORCH
    at: "2026-09-13T15:37:08Z"
    note: >
      REQ-335 (queue task 632, GH#1336) implemented via
      feature/WF02-REQ335-20260913 (commits 262ed233/bd814b11). Built
      Letflow.Routers.ExamSessions, the first HTTP route surface for
      the candidate-facing exam session API, delegating to REQ-332's
      Letflow.Exam.Session and REQ-333's Letflow.Exam.AntiCheat: POST
      /exam-sessions (start), GET /exam-sessions/:id (state read, new
      get_session_state_for_user/3), PUT
      /exam-sessions/:id/answers/:question_id (autosave -- path
      corrected from the requirement's own inferred POST to the real
      PUT, verified against backend/internal/sessions/handler.go's
      actual routing registration), POST /exam-sessions/:id/submit,
      POST /exam-sessions/:id/events (anti-cheat signal). Mounted at
      the normal authenticated /api/v1 forward, not decision 0028's
      unauthenticated pattern. Five new permission atoms
      (ExamSessionStart/Read/Save/Submit/ReportEvent) minted following
      the :Entities* precedent, granted to TASK_WORKER (this
      codebase's only non-privileged tenant-user role).

      The is_correct/answer-key redaction open question this
      requirement explicitly flagged was resolved as hand-assembly,
      not FieldGrants: no single Query.Compiler page spans the four
      joined entity types this read composes, and the pack has no
      entity_field_restrictions row for is_correct today, so a
      FieldGrants-based route would currently leak it outright.
      SECURITY-REVIEWER independently traced every field back to its
      source and confirmed the redaction is structurally safe (named
      map-literal keys, no passthrough of any entity's full
      field_values, so a future schema field cannot silently leak
      through) rather than merely incidentally safe.

      Pipeline: ELIXIR-DEV (comprehensive route/authorization/
      ownership/redaction tests) -> SECURITY-REVIEWER PASS
      (independently verified INV-1 tenant isolation, the 404-not-403
      ownership property, the redaction mechanism by tracing the code
      rather than accepting the reasoning, permission-atom wiring, and
      spot-checked the SaveAnswer path correction against the
      reference source directly; surfaced a genuine pre-existing,
      independent finding -- TASK_WORKER's existing EntitiesQuery
      grant might already expose is_correct via the generic
      entities-query route -- filed separately as ISS-0647 rather than
      expanding this requirement's scope) -> REVIEWER PASS (confirmed
      router idiom matches Letflow.Routers.Entities' precedent,
      confirmed no expansion of REQ-332's/REQ-333's own scope fences,
      gave an explicit standing sign-off that ELIXIR-DEV -- not
      FRONTEND-DEV, P4's stated default -- is the correct owner here
      since the entire deliverable is lib/letflow/routers/ +
      lib/letflow/api/authorization.ex + tenant-scoped lib/letflow/exam/
      calls with nothing under web/) -> TEST-RUNNER found one real,
      fixable defect (an unused default arg on a private test helper
      tripping the ISS-0069-class compiler-warning gate, unrelated to
      REQ-335's actual behavior -- all 4199 real tests passed both
      times TEST-RUNNER ran the suite) -- fixed directly, re-verified
      clean.

      Also fixed, on this branch, a live CI-blocking tripwire in
      test/letflow/simulation/req207_vortex_test.exs: a batch of six
      new S10 P4 requirements (REQ-336, REQ-338-340, REQ-342, REQ-343)
      landed on main mid-run, all FRONTEND-DEV client-side work or
      DOC-UPDATER bookkeeping, none touching the entities HTTP route
      surface -- admitted to the unconditional allowlist,
      REVIEWER-confirmed.

      docs/requirements.yaml flipped pending -> done.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-13T15:49:56Z"
    note: >
      Claimed letflow-queue task 647 (ISS-0647, GH#1352) -- self-filed
      during REQ-335's SECURITY-REVIEWER pass. Fact-finding first, per
      the issue's own acceptance criteria: empirically prove whether a
      TASK_WORKER-scoped POST /entities/query against question/
      answer_option currently returns is_correct (a pre-existing
      permission grant predating REQ-335, not introduced by it). If
      reachable, apply a fix (FieldGrants/entity_field_restrictions row
      or a scope narrowing) with a regression test; if not reachable,
      state why and quote the guard. Dispatching to ELIXIR-DEV given
      the real code-tracing/test-writing work involved.
  - req: REQ-336
    event: blocked
    agent: ORCH
    at: "2026-09-13T16:13:04Z"
    note: >
      Claimed letflow-queue task 633 cleanly (no sibling collision) and
      dispatched FRONTEND-DEV, which built the full admin-CRUD engine
      (web/src/api/entities.ts, EntityRecordForm.tsx, TagListPage.tsx,
      i18n catalog) against the tag pilot entity, 474/474 web/ tests
      passing. REVIEWER then found a genuine BLOCKER-severity backend gap
      while checking REQ-336's own duplicate-tag acceptance criterion:
      pack-installed entity types' declared unique constraints
      (tag.json's uq_tag_name included) are never actually enforced --
      column promotion, the only mechanism that materializes a
      constraint into real DDL, is never triggered at pack-install time.
      Empirically proven (a throwaway test created two "tag" records
      with an identical name in the same tenant; both succeeded with
      distinct record_ids, no error). Filed as ISS-0648 (queue task 648,
      GH#1354, BLOCKER). REQ-336's own AC4 ("creating a tag with a
      duplicate name surfaces the API's real 409/422") cannot be
      genuinely verified until ISS-0648 is fixed -- the frontend's
      error-shape handling is correct but never actually exercised
      against a real failure today. Released queue task 633 back to
      status: open rather than sitting on it. REQ-336's implementation
      is preserved uncommitted in the working tree (not discarded) --
      resuming this run once ISS-0648 lands rather than redoing the
      frontend work. REQ-340/REQ-342/REQ-343 inherit the same gap and
      are blocked in turn. Proceeding to REQ-338 (no dependency on
      REQ-336) while ISS-0648 is queued for its own fix cycle.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-13T18:30:49Z"
    note: >
      ISS-0647 (queue task 647, GH#1352) resolved via
      fix/ISS-0647-20260913 (commits 80bca0f1/9b12c537/cd2120a1).
      Fact-finding confirmed the leak was live, not theoretical: a
      real HTTP-stack test against real Postgres showed a
      TASK_WORKER-scoped POST /entities/query returning
      question.explanation and answer_option.is_correct/
      likert_weight/likert_polarity in clear. The FieldGrants
      redaction mechanism already existed and was already wired into
      the generic query routes -- only the entity_field_restrictions
      DATA was missing.

      First fix attempt (80bca0f1) seeded the restriction rows only
      from test/support/exam_fixtures.ex -- a test helper.
      SECURITY-REVIEWER correctly FAILed this, tracing the real
      production path (POST /solution-packs/install ->
      SolutionPack.install/3) and finding zero references to the
      seeding function there, meaning a real tenant installing the
      pack today got zero protection -- the leak this issue was filed
      to close was still live in reality despite a passing test suite.
      SECURITY-REVIEWER also found ELIXIR-DEV's "kept generic per
      decisions 0026/0027/0029" scope rationale did not hold up: those
      decisions govern the pack-document's closed section-key struct,
      not the installer function's internal behavior.

      Reworked (9b12c537): seed_pack_specific_field_restrictions/2
      added directly inside SolutionPack.run_install/5's own
      transaction, using the same prefix(opts) helper every other
      step uses, matched via MapSet.subset?/2 of
      Bilimbaga.answer_key_fields/0's entity types against the
      install's actual entity-type set -- the one bilimbaga-aware
      branch in an otherwise generic installer. Verified
      FieldGrants.load_restrictions/3 reads entity_field_restrictions
      by entity_type string alone (no join to entity_definitions'
      status column) before relying on that claim, confirming
      install-time seeding is meaningful even though entity
      definitions stay :inactive per 0026. New end-to-end test
      installs the real committed priv/packs/bilimbaga/pack.json via
      SolutionPack.install/3 and never calls the seeding function
      directly, then proves redaction through a real
      POST /entities/query call.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER FAIL then PASS (first
      pass found the test-fixture-only gap; second pass independently
      traced the real call chain and confirmed the rework genuinely
      closes it) -> REVIEWER PASS (confirmed the decision-record
      reading, confirmed no pack-hook/callback mechanism already
      exists to prefer over the inline branch, confirmed the test is
      non-tautological; flagged one non-blocking nit -- a
      future-maintainer comment for the entity-type-name-matching
      edge case once a second pack exists -- added directly by ORCH).
      Full mix test: 4207-4209/4209 passed depending on run, only
      pre-existing rust-toolchain-absence failures and one confirmed
      parallel-execution-only flake.

      docs/issues/ISS-0647.yaml flipped open -> resolved. Fixed a
      real, live, previously-undetected answer-key data leak
      reachable by any TASK_WORKER-scoped tenant user, not only exam
      candidates -- broader exposure than REQ-335's own scope, closed
      before shipping.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-13T19:22:16Z"
    note: >
      Claimed letflow-queue task 652 (ISS-0652, GH#1360, S3) --
      centralization fix. Query.Compiler.select_entity_row/1 returns
      record_id as a raw 16-byte UUID binary for a per-type-table row,
      contradicting its own entity_row() @type promise of String.t().
      Two independent call sites (lib/letflow/routers/entities.ex's
      normalise_record_id/1, lib/letflow/exam/session.ex's
      normalize_record_id/1) already copy-pasted the identical local
      fix rather than centralizing it -- same root-cause defect class
      as ISS-0649/ISS-0651. Fix: cast record_id to a string at the
      source in select_entity_row/1 itself, remove both now-redundant
      local helpers, survey for any other undiscovered copy-pasted
      workaround. Dispatching to ELIXIR-DEV.
  - req: REQ-338
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T19:27:01Z"
    note: >
      REQ-338 (hand-written candidate exam-taking UI -- start screen,
      in-progress question flow with autosave and server-reanchored
      countdown, submit, result -- wired to REQ-335's exam-session
      routes and REQ-333's anti-cheat signal route) implemented on
      feature/WF02-REQ338-20260914, superseding the abandoned
      feature/WF02-REQ338-20260913 branch (commit 78e0146e, left
      unmerged -- its web/src/api/exam.ts, web/src/api/queryKeys.ts and
      web/src/pages/exam/ExamListPage.tsx were an earlier, CI-failing
      attempt, since fully rewritten). Delivered: web/src/api/exam.ts
      (startSession/saveAnswer/submitSession/getSessionState/
      reportEvent wrapping REQ-335's real routes), web/src/types/exam.ts,
      web/src/utils/examErrors.ts, web/src/hooks/useAntiCheatSignals.ts
      (visibilitychange/blur/fullscreenchange -> REQ-333's
      tab_switch/blur/fullscreen_exit, torn down on unmount/leaving the
      in-progress screen), web/src/i18n/examMessages.ts +
      ExamIntlProvider.tsx (kk/ru/en, no hard-coded English, grep-proven),
      web/src/pages/exam/ExamListPage.tsx and ExamSessionPage.tsx, plus
      their test suites and the exam.ts/i18n-grep test suites, and the
      additive exam-only hunks in web/src/router.tsx (two new routes)
      and web/src/api/queryKeys.ts (the `exam` key group, staged
      separately from REQ-336's own `entities` key group via `git add
      -p`, verified via `git diff --cached` before commit).

      Ambiguity escape (this requirement's own mandatory close-out
      statement): chose OPTION (a) -- list every exam whose status is
      active, matching check_assigned/3's actual current no-op
      behaviour honestly, with the list screen's own copy stating
      PROMINENTLY that this is provisional pending an assignment
      decision record. Basis, quoted verbatim from
      lib/letflow/exam/session.ex's moduledoc "FINDING" section: "the
      'assignment' eligibility check cannot be truthfully implemented
      ... `check_assigned/3` below is a documented no-op -- every
      candidate is currently treated as assigned. `:not_assigned`
      remains declared in `eligibility_error()` for interface-shape
      fidelity with the design doc, but is unreachable in this
      implementation." The provisional-pending-decision-record copy
      rendered in ExamListPage is asserted present by its own test.

      ISS-0650 (MAJOR, queue task 650, GH-1356) discovered and correctly
      scoped OUT of this requirement while implementing it: REQ-332's
      Session.autosave_answer/4 and upsert_answer/6 have no
      text_answer field anywhere in the write path, so a short-text
      question's answer can never be captured or graded regardless of
      what a client sends. Confirmed not a frontend gap --
      ExamSessionPage correctly shows a "not yet supported" note for
      short-text questions rather than offering a text input that would
      silently fail to save. Filed as its own backend issue rather than
      worked around here; REQ-338's own acceptance criteria for
      grading_pending / short-text handling are satisfied against the
      backend's actual (incomplete) capability, not a client-side
      shim.

      REQ-336 coupling defect found and fully fixed across two rounds
      during this close-out. REQ-336 (generic admin-CRUD engine) and
      REQ-338 share two files (web/src/router.tsx, web/src/api/
      queryKeys.ts) plus additions inside web/src/types/api.ts and
      web/src/api/client.ts that belong to REQ-336 alone -- both
      requirements' uncommitted work sat in the same working tree after
      REQ-336 was blocked on ISS-0648 (pack-install unique-constraint
      gap) and preserved rather than discarded. First close-out attempt
      committed REQ-338's own new files plus the router.tsx/queryKeys.ts
      hunks but MISSED that REQ-336 had also added shared-file type/
      queryKey scaffolding beyond the `entities` key group and a
      `putWithHeaders` client helper -- a partial fix that would have
      left REQ-336's own additions half-committed under REQ-338's
      commit. Reworked: re-diffed the full working tree against main,
      confirmed via `git diff --cached` that ONLY REQ-338's own new
      files and the exam-only hunks in router.tsx/queryKeys.ts were
      staged, then independently verified the result by stripping every
      REQ-336 file (entities.ts, components/entities/, pages/entities/,
      entityFieldToFormField.ts, entitiesMessages.ts,
      EntitiesIntlProvider.tsx, entities-i18n-grep.test.ts,
      api/__tests__/entities.test.ts) AND REQ-336's own hunks in the two
      shared files, and confirming REQ-338 builds and its own test
      suite (`npm run check`) passes standalone against main with
      REQ-336 entirely absent. REQ-336's own uncommitted work (including
      its shared-file hunks) was left untouched in the working tree
      throughout, to resume once ISS-0648 lands.

      Reviewed twice by REVIEWER (once for the implementation, once for
      the router.tsx/queryKeys.ts decoupling fix) -- both PASS.
      Independently re-confirmed PASS by REVIEWER after a branch-name
      collision recovery (the abandoned feature/WF02-REQ338-20260913
      name from the earlier buggy attempt, superseded here by
      feature/WF02-REQ338-20260914 since ISS-0648's unrelated fix had
      been sharing the same working directory in the interim and has
      since fully merged, confirmed clean on main). TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR/TEST-RUNNER completed; `npm run check`
      inside web/ reported 502+48 tests passing. RELEASE-VALIDATOR
      independently confirmed every acceptance criterion satisfied.

      docs/requirements.yaml flipped pending -> done.
  - req: REQ-336
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T20:55:45Z"
    note: >
      REQ-336 (generic admin-CRUD screen engine in web/, S10 P4 bucket C
      client, proven against the tag pilot entity) completed after
      resuming from the ISS-0648 block recorded above. Full pipeline:
      FRONTEND-DEV implementation (web/src/api/entities.ts,
      web/src/utils/entityFieldToFormField.ts,
      web/src/components/entities/, web/src/pages/entities/,
      web/src/i18n/entitiesMessages.ts,
      web/src/i18n/EntitiesIntlProvider.tsx, plus the entities-only
      hunks in web/src/api/client.ts (putWithHeaders, for If-Match
      optimistic-concurrency updates), web/src/types/api.ts and
      web/src/api/queryKeys.ts's `entities` key group) -> blocked on a
      real BLOCKER-severity backend gap found while checking this
      requirement's own AC4 (pack-installed unique constraints, tag's
      uq_tag_name included, were never enforced because column
      promotion never ran at pack-install time) -> ISS-0648 fixed and
      merged separately, unblocking this requirement -> resumed, and a
      SECOND backend gap surfaced in the same AC4 path: even after
      ISS-0648's fix, a unique-violation write was still surfacing to
      the client as a bare 500 instead of a real 409/422, because
      lib/letflow/entities/records.ex had no translation for Postgres's
      unique_violation error class. ELIXIR-DEV root-caused and fixed
      this in lib/letflow/entities/records.ex, with new end-to-end
      coverage in test/letflow/routers/entities_test.exs and
      test/letflow/entities/iss0648_column_promotion_trigger_test.exs
      proving a real duplicate-tag POST against a real pack-installed
      constraint now returns the documented error shape rather than a
      500. Pipeline continued: SECURITY-REVIEWER PASS, REVIEWER PASS,
      TEST-DESIGNER (no coverage gaps found), TEST-DESIGN-VALIDATOR
      PASS, TEST-RUNNER (full `mix letflow.check`: 4221/4221; full
      `npm run check` inside web/: 550/550), RELEASE-VALIDATOR PASS
      (independently re-verified every acceptance criterion).

      AC8 close-out ("no BilimBaga file copied"): confirmed by naming
      the actual web/ design-system components the generic engine
      composes rather than any copied BilimBaga-specific screen code --
      PageLayout, Button, DataTable, PaginationControls, ConfirmDialog,
      and QueryStateBoundary.

      docs/requirements.yaml flipped pending -> done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-13T21:16:59Z"
    note: >
      ISS-0652 (queue task 652, GH#1360) resolved via
      fix/ISS-0652-20260913 (commit 1e5bff6f). Centralized the fix:
      wrapped record_id in type(field(...), Ecto.UUID) at all three
      select sites in Letflow.Entities.Query.Compiler
      (select_entity_row/1, primary_entity_row_select_dynamic/0,
      joined_entity_row_select_dynamic/2) -- the same idiom the module
      already uses elsewhere for schemaless-binding type casts
      (json_cast_dynamic/2), confirmed by REVIEWER as matching
      existing precedent rather than introducing a new one. Removed
      the now-redundant normalise_record_id/1 helper in
      lib/letflow/routers/entities.ex.

      Confirmed (grep + git log -S) that the normalize_record_id/1
      helper the issue's own filing named in lib/letflow/exam/session.ex
      never actually existed in this checkout -- a discrepancy in the
      issue's own text, not a code defect; session.ex's record_id
      reads were already fixed for free by the centralized Compiler
      fix. Survey for other undiscovered copy-pasted workarounds found
      none in production code, only two test files manually decoding
      the raw binary, both updated.

      SECURITY-REVIEWER found and confirmed a genuine incidental fix:
      the same defect had silently broken
      Letflow.Exam.Session.build_option_states/4's record_id-keyed
      Map.has_key?/2 lookup against caller-supplied string
      option_ids -- would have surfaced once answer_option was ever a
      promoted per-type table. This fix repairs that for free.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS (fast-pass gate,
      confirmed no tenant-scoping code touched, confirmed the removed
      helper is behavior-equivalent, confirmed the regression test is
      genuine by reverting the fix and reproducing 2 failures) ->
      REVIEWER PASS (confirmed idiom, confirmed the issue-text
      discrepancy, independently re-ran the workaround survey) ->
      TEST-RUNNER confirmed a full mix letflow.check run clean except
      three pre-existing, environment-only failures.

      docs/issues/ISS-0652.yaml flipped open -> resolved (a sibling
      session filed the docs record for this issue independently,
      concurrently with this fix; composed at this Step Final rebase
      rather than overwritten -- see that file's own history).
  - req: REQ-340
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T21:32:34Z"
    note: >
      REQ-340 (the two mechanically-driven widgets -- enum select,
      unique-composite constraint error surfacing -- for the admin-CRUD
      engine's field registry) completed as a reuse-and-verify job, no
      new production code needed. FRONTEND-DEV's investigation ran the
      requirement's own mandated check first: `grep -rl "enum\|:enum"
      web/src/components/forms` found the enum widget already lives in
      FieldFactory.renderFormField's builtin switch (fieldType ===
      'select' && fieldDef.enum), and REQ-336's existing
      entityFieldToFormField.ts already bridges an entity :enum field
      onto that same rendering path -- so no new fieldRegistry.ts or
      widgets/ code was required. Two new test files supply the proof
      the acceptance criteria demand: EntityRecordForm.enumWidget.test.tsx
      (renders the shared widget against real enum_values from
      question.difficulty, exam.show_answers and
      exam_question_rule.mode, drawn from the actual committed
      definition documents under priv/packs/bilimbaga/entity_definitions/,
      and proves changing a mocked definition's enum_values changes the
      rendered options) and EntityRecordForm.compositeUnique.test.tsx
      (proves REQ-336's existing composite-unique error-display path
      correctly surfaces question_tag's and exam_section's real
      composite constraint violations, with no new client-side
      validation logic added).

      Pipeline: FRONTEND-DEV investigation -> REVIEWER PASS ->
      TEST-DESIGNER (confirmatory, no coverage gaps found) ->
      TEST-DESIGN-VALIDATOR PASS -> TEST-RUNNER (`npm run check`:
      508+48 tests passing) -> RELEASE-VALIDATOR PASS (independently
      re-verified all 8 acceptance criteria against real code and the
      actual committed entity-definition JSON).

      Corrected entity count restated per this requirement's own
      close-out obligation: nine remaining, not eight -- REQ-326's five
      (category, question, answer_option, question_tag, and tag itself
      already piloted by REQ-336) plus REQ-327's five (exam,
      exam_section, exam_question_rule, exam_question_rule_tag,
      exam_manual_question), minus REQ-336's tag pilot, leaves nine:
      category, question, answer_option, question_tag, exam,
      exam_section, exam_question_rule, exam_question_rule_tag,
      exam_manual_question.

      docs/requirements.yaml flipped pending -> done.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-13T21:57:13Z"
    note: >
      Claimed letflow-queue task 651 (ISS-0651, GH#1359, S3) --
      same defect family as ISS-0652 but in
      Letflow.Entities.Query.Cursor. build_next_cursor/2 and
      read_sort_value/2 pattern-match %Letflow.Entities.Record.Latest{}
      specifically, no bare-map fallback for Compiler.entity_row()'s
      plain-map shape -- unlike field_grants.ex's redact_item/2 and
      routers/entities.ex's query_item_map/1, which REQ-300/ISS-0600
      already generalized for both shapes. Reachable via
      Cursor.paginate/5 against the generic keyset-paginated
      entity-query route. Not a one-line relax like ISS-0648's fv/2
      fix: entity_row() has no :id field the way %Latest{} does
      (record_id is the analog), so this needs an explicit design
      decision on what the cursor's id/sort-value components read from
      a promoted-table row, confirmed against the real field keyset
      pagination needs for monotonic stability -- then generalized the
      same way the two existing precedent functions were, not a third
      pattern. Dispatching to ELIXIR-DEV with instructions to document
      the design decision as part of the fix, escalating to
      CODE-DESIGNER only if genuinely undecidable.
  - req: REQ-342
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T22:14:10Z"
    note: >
      REQ-342 (the localized_text and foreign-key-reference widgets for
      the admin-CRUD engine's field registry, proven against real field
      shapes from all nine remaining BilimBaga entity types via targeted
      component tests -- no entity screen, nav, or routing wiring, that
      being REQ-343's job) completed. Two genuinely new widgets built:
      localized_text (a per-locale tab/stacked-field group posting one
      {"kk":...,"ru":...,"en":...} value, proven against question.stem,
      category.name and exam_section.title) and a fk-reference extension
      to the existing searchableSelect.tsx (a searchable select resolving
      a human-readable label via a real POST /entities/query call rather
      than the raw record_id, proven against question.category_id ->
      category, exam_section.exam_id -> exam, and
      exam_question_rule_tag.tag_id -> tag). New files:
      web/src/components/entities/entityWidgetRegistry.ts,
      web/src/components/forms/widgets/localizedText.tsx, plus their test
      suites (EntityRecordForm.fkReference.test.tsx,
      EntityRecordForm.localizedText.test.tsx, entityWidgetRegistry.test.ts,
      localizedTextAndFkReference.i18nGrep.test.ts). Modified:
      EntityRecordForm.tsx, searchableSelect.tsx, entitiesMessages.ts,
      forms.ts, entityFieldToFormField.ts.

      FRONTEND-DEV found and fixed a real react-hook-form bug during
      development: the fk-reference widget had register/ref and onBlur
      wired directly onto the visible combobox input, so a blur event
      (e.g. focus moving to the submit button) overwrote the selected
      record_id with the displayed label text, corrupting the field's
      real value. Fixed by making a hidden input the sole RHF-registered
      element holding the real record_id, with the visible combobox
      driving it via setValue rather than being registered itself. This
      fix carries genuine mutation-tested regression coverage:
      TEST-DESIGNER reverted the fix, confirmed the exact record_id/label
      corruption reproduced and the regression test failed, then
      re-applied the fix and confirmed clean.

      Pipeline: FRONTEND-DEV implementation -> REVIEWER PASS ->
      TEST-DESIGNER (mutation-tested the blur-bug regression coverage for
      real, as above) -> TEST-DESIGN-VALIDATOR PASS -> TEST-RUNNER
      (`npm run check`: 519+48 tests passing) -> RELEASE-VALIDATOR PASS
      (independently re-verified all acceptance criteria against real
      code and the committed entity-definition JSON, and independently
      traced the blur-bug fix mechanics rather than trusting the report).

      docs/requirements.yaml flipped pending -> done.
  - req: REQ-343
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T22:53:16Z"
    note: >
      REQ-343 (wire full list/create/edit/delete screens, nav and
      role-gated routing for all nine remaining BilimBaga entity types
      onto REQ-336's admin-CRUD engine, using REQ-340's/REQ-342's
      widgets) completed -- the FINAL piece of S10 P4. This closes out
      ALL of S10 P4: REQ-335, REQ-336, REQ-338, REQ-340, REQ-342 and
      REQ-343 are now all done.

      FRONTEND-DEV built one generic component, EntityCrudPage.tsx,
      reused across all nine remaining entity types (category,
      question, answer_option, question_tag, exam, exam_section,
      exam_question_rule, exam_question_rule_tag,
      exam_manual_question) rather than nine duplicated per-entity
      pages -- new web/src/config/bilimbagaEntities.ts supplies the
      per-entity configuration the shared component reads. New nav
      shell: web/src/pages/admin/bilimbaga/BilimBagaAdminPage.tsx and
      BilimBagaEntityRoute.tsx, wired into web/src/router.tsx and
      web/src/components/layout/AppShell.tsx.

      All three requirement-mandated gaps were recorded, each with
      real test coverage rather than a comment: (1) answer_option's
      is_correct field-grant -- the admin form displays and allows
      setting is_correct, close-out states this is the authoring
      surface (question-bank editor), distinct from REQ-335's/
      REQ-338's candidate-facing redaction question; (2)
      category.parent_id / question.parent_id -- neither field exists
      on the entity definition today (REQ-326 deliberately did not
      author them), so no screen/field/workaround was added; proven by
      web/src/__tests__/bilimbaga-parent-id-exam-assignment-grep.test.ts
      grepping the new web/ files for parent_id and finding no
      rendered form field or mocked entity; (3) exam_assignment -- no
      entity type exists to build a screen for (REQ-327 deliberately
      did not author it, assignee_id being polymorphic); the exam-admin
      section's own UI carries a one-line visible note pointing at
      lib/letflow/exam/session.ex's FINDING section and REQ-327's
      README-constraints.md, asserted present by the same grep test's
      companion rendering test.

      Nav role-gating verified against lib/letflow/api/authorization.ex's
      real permission matrix: PLATFORM_ADMIN and PROCESS_OPERATOR hold
      :EntitiesRecordsWrite and see the new admin nav entry;
      PROCESS_DESIGNER and TASK_WORKER do not hold it and do not see
      the entry, proven by web/src/components/layout/__tests__/
      AppShell.bilimbaga.test.tsx against the real permission atoms
      rather than a mocked role list.

      REVIEWER PASS with one narrow i18n fix applied and re-verified:
      a hard-coded Yes/No boolean-formatting string in the generic
      form/list rendering was caught, routed through the react-intl
      catalog (web/src/i18n/entitiesMessages.ts, kk/ru/en), and
      re-verified clean by REVIEWER on the fixed commit.

      Pipeline: FRONTEND-DEV implementation -> REVIEWER PASS (i18n fix
      applied and re-verified) -> TEST-DESIGNER (added the missing grep
      test for the parent_id/exam_assignment gap,
      web/src/__tests__/bilimbaga-parent-id-exam-assignment-grep.test.ts)
      -> TEST-DESIGN-VALIDATOR PASS -> TEST-RUNNER (`npm run check`:
      547+48 tests passing) -> RELEASE-VALIDATOR PASS (independently
      re-verified every acceptance criterion against real code,
      including the role-gating permissions in
      lib/letflow/api/authorization.ex).

      No file or component copied from
      c:\Users\tvolo\dev\ai-dala\BilimBaga\frontend\ -- built entirely
      on web/'s own design-system components and REQ-336's/REQ-340's/
      REQ-342's engine and widgets.

      docs/requirements.yaml flipped pending -> done.
  - req: REQ-339
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T23:01:45Z"
    note: >
      REQ-339 (populate the stage-10 bucket-C inventory with P4's
      web/src/pages/exam/ modules, correct the P4 phase-table exit
      condition, and record the exam_assignment gap as a standing open
      question) completed -- pure documentation, no code change. THIS
      CLOSES OUT ALL OF S10 P4: REQ-335, REQ-336, REQ-338, REQ-339,
      REQ-340, REQ-342 and REQ-343 are now all done.

      ONE (inventory): added two rows to docs/migration/
      stage-10-bilimbaga-vertical.md's bucket-C inventory table --
      web/src/pages/exam/ExamListPage.tsx and ExamSessionPage.tsx --
      sourced from REQ-338's own done-event close-out (this volume,
      above) rather than invented, since REQ-338 had no CODE-DESIGNER
      gate and so no design-doc section 7 table to copy from.

      TWO (measure): `ls web/src/pages/exam/` shows ExamListPage.tsx,
      ExamSessionPage.tsx, __tests__/; `wc -l web/src/pages/exam/*.tsx
      web/src/api/exam.ts` = 139 + 358 + 124 = 621 total (497 lines
      across the two page modules, 124 in the API client), measured
      2026-09-14. Both commands and their real output are quoted in the
      stage file.

      THREE (P4 exit condition): marked MET as of 2026-09-14 against
      REQ-336's/REQ-340's/REQ-342's/REQ-343's/REQ-338's actual
      close-outs, each independently naming its web/ design-system
      components and stating no BilimBaga frontend file was copied
      (quoted in the stage file). Cited REQ-340/REQ-342/REQ-343 wherever
      the stage file previously named REQ-337/REQ-341, with the retire-
      and-split history stated accurately (REQ-337 -> REQ-340/REQ-341;
      REQ-341 -> REQ-342/REQ-343, both REQ-VALIDATOR FAILs for bundling
      widget-type work with wiring).

      FOUR (exam_assignment gap): added a new, standalone entry to the
      stage file's "Open questions" section (not merged into an existing
      one) naming REQ-327, REQ-332, REQ-343 and REQ-338 as the four
      places this gap surfaced, quoting session.ex's FINDING verbatim,
      and stating plainly this requirement does NOT resolve it -- a
      decision-record requirement is a follow-on, not this entry's job.

      decisions/0022-bilimbaga-vertical.md was NOT touched --
      `git diff --name-only` confirmed to show only
      docs/migration/stage-10-bilimbaga-vertical.md, docs/requirements.yaml
      and this run-history append.

      REQ-335, REQ-336, REQ-338, REQ-340, REQ-342 and REQ-343 were
      verified already `status: done` in docs/requirements.yaml (no
      drift found, none rewritten) and their own status-history
      done-events were confirmed already present in this same volume
      (requirement_status.v16.yaml) rather than re-appended: REQ-335 done
      2026-09-13T15:37:08Z, REQ-338 done 2026-09-13T19:27:01Z, REQ-336
      done 2026-09-13T20:55:45Z, REQ-340 done 2026-09-13T21:32:34Z,
      REQ-342 done 2026-09-13T22:14:10Z, REQ-343 done
      2026-09-13T22:53:16Z.

      docs/requirements.yaml flipped pending -> done for REQ-339 only.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-13T23:51:02Z"
    note: >
      ISS-0651 (queue task 651, GH#1359) resolved via
      fix/ISS-0651-20260913 (commit 08a859d0). build_next_cursor/2 had
      embedded row.id (entity_record_latest's surrogate PK) as the
      cursor tiebreak, but a promoted per-type table's
      Compiler.entity_row() has no :id field at all -- and the query-
      composition side (paginate/5's order_by, build_resume_terms/2's
      id_term dynamic) also referenced the physical id column, so a
      row-reading-only fix would have embedded record_id in the cursor
      while SQL still filtered on id, a silently wrong resume point
      rather than a crash. Switched the tiebreak uniformly to
      record_id on both sides. SECURITY-REVIEWER independently
      verified the uniqueness reasoning against actual migration/DDL
      source (not the PR's own claim): entity_record_latest's genuine
      UNIQUE(entity_type, record_id) constraint (with entity_type
      always filtered) and every promoted per-type table's genuine
      table-wide UNIQUE(record_id) constraint. New read_record_id/1
      helper and read_sort_value/2's :json_field clause follow the
      exact two-clause struct-then-bare-map idiom already established
      by field_grants.ex's redact_item/2 and routers/entities.ex's
      query_item_map/1 -- no third pattern.

      A related but distinct defect (resolved_sort_term/2, a different
      function, raises KeyError sorting by a promoted business column)
      was correctly scoped out and filed separately as ISS-0653 (queue
      task 653, GH#1370) rather than folded in or silently avoided --
      the regression test's routing around it via the :json_field path
      is documented in the test itself.

      New regression test proves keyset pagination across three real
      pages against a promoted per-type table: distinct/non-
      overlapping record_ids, correct ordering, next_cursor == nil
      only on the final page.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS (verified the
      uniqueness claims against actual schema source, confirmed no
      tenant-scoping predicate touched) -> REVIEWER PASS (confirmed
      idiom match, confirmed the moduledoc documents the invariant a
      future reader must preserve, confirmed the ISS-0653 scope
      decision) -> TEST-RUNNER confirmed a full mix letflow.check run
      clean except the pre-existing rust-toolchain-absence failures.

      docs/issues/ISS-0651.yaml flipped open -> resolved.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-14T00:11:35Z"
    note: >
      Claimed letflow-queue task 653 (ISS-0653, GH#1370, S3) -- self-
      filed while resolving ISS-0651. Third and final function in this
      same defect family in Letflow.Entities.Query.Cursor:
      resolved_sort_term/2's :typed_column clause resolves a sort
      column atom via Allowlist.typed_columns/0, which only knows the
      fixed structural-7 columns plus entity_record_latest's own typed
      columns -- not a promoted entity type's own promoted business
      columns. Sorting a keyset-paginated query by a genuinely
      promoted business column raises KeyError. ISS-0651's own
      regression test had to route around this via the :json_field
      path to avoid tripping it. Fix: generalize resolved_sort_term/2
      to resolve a promoted business column's real typed column,
      following the same struct-vs-plain-map/promoted-vs-structural
      precedent ISS-0651/ISS-0652 already established. Dispatching to
      ELIXIR-DEV.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T02:06:46Z"
    note: >
      ISS-0653 (queue task 653, GH#1370) resolved via
      fix/ISS-0653-20260914 (commit 8ecaf4e3) -- the third and final
      fix in this defect family (after ISS-0651, ISS-0652), all in
      Letflow.Entities.Query.Cursor. resolved_sort_term/2's
      :typed_column clause previously did Map.fetch!(Allowlist.
      typed_columns(), name) unconditionally, raising KeyError for a
      genuinely promoted business column. Generalized by mirroring an
      ALREADY-REVIEWED-AND-MERGED pattern in a different module,
      Compiler.build_order_by/3 -- no new design decision needed, since
      Compiler had already solved this exact same-shaped problem
      twice. read_sort_value/2's :typed_column clause gets the same
      dispatch, falling through to a new read_promoted_column_value/2
      helper matching FieldGrants.redact_item/2's precedent.

      SECURITY-REVIEWER verified fragment("?", literal(^name)) is
      Ecto's own safe identifier-quoting macro, traced name's
      provenance to a server-built allowlist map (never raw caller
      input), confirmed no tenant-scoping predicate touched. REVIEWER
      confirmed the idiom matches Compiler's precedent exactly and
      grepped the whole module confirming no fourth instance of this
      defect class remains -- this closes the entire ISS-0651/0652/
      0653 defect family in Cursor.

      New regression test proves sorting by a promoted business
      column's real typed/generated-column form (not the :json_field
      fallback) works correctly across three real pages, with
      source: :typed_column explicitly asserted to confirm the
      promoted-column path is genuinely exercised.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS -> REVIEWER PASS
      -> TEST-RUNNER confirmed a full mix letflow.check run clean
      except the pre-existing rust-toolchain-absence failures.

      docs/issues/ISS-0653.yaml flipped open -> resolved.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-14T02:20:53Z"
    note: >
      Claimed letflow-queue task 650 (ISS-0650, GH#1356, S10, MAJOR) --
      self-filed by FRONTEND-DEV during REQ-338. Letflow.Exam.Session.
      autosave_answer/4's answer_attrs and upsert_answer/6 have no
      text_answer field at all -- a short_text exam question can never
      be answered regardless of what a client sends, even though
      saved_answer's own type (REQ-335) already declares text_answer,
      session_answer.json (REQ-329) already declares the field in its
      schema, and REQ-332's Scoring module already marks a short_text
      answer pending_manual for human grading with nothing for a human
      grader to actually grade. Fix direction: extend answer_attrs to
      accept an optional text_answer, branch check_answer_shape/2's
      :short_text case to validate it, extend upsert_answer/6 to write
      it, confirm submit/3/Scoring's pending_manual path reads the
      real value, then wire REQ-338's ExamSessionPage.tsx's "not yet
      supported" placeholder to a real text input through the now-
      complete autosave path. Backend first (ELIXIR-DEV), then
      frontend (FRONTEND-DEV) once the write path is real.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T03:53:37Z"
    note: >
      ISS-0650 (queue task 650, GH#1356, MAJOR) resolved via
      fix/ISS-0650-20260914 (commits 0332bf8f, 696d902e) -- fixed end
      to end, backend then frontend. Backend: check_answer_shape/2 ->
      /3 validates text_answer (nil/string for :short_text, hard
      rejection for every other type); upsert_answer/6 -> /7 writes it
      when present, omits the key when absent (matches
      session_answer.json's required:false-but-not-nullable schema
      and correctly clears a prior answer via full-replacement write
      semantics); Scoring.score_question/2's :short_text clause now
      threads the real text into pending_manual instead of discarding
      it. A companion fix beyond the issue's own listed scope was
      required and applied: lib/letflow/routers/exam_sessions.ex's PUT
      route/@autosave_schema had no text_answer field at all, so no
      real HTTP client could ever have reached the backend fix --
      REVIEWER confirmed this was a genuine scope correction, not
      creep. Frontend: ExamSessionPage.tsx's static placeholder
      replaced with a real controlled textarea (restore-on-load,
      autosave-on-blur with a no-op guard, i18n'd in en/ru/kk).

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS (traced
      text_answer through validation with no bypass, verified the
      omit-vs-nil distinction against real full-replacement semantics,
      confirmed cross-type rejection is real and tested; flagged an
      unbounded string-length as non-blocking) -> REVIEWER PASS
      (backend: agreed deferring the length cap is correct given
      existing precedent; confirmed the router companion fix was
      genuine) -> FRONTEND-DEV -> REVIEWER PASS (frontend: confirmed
      type/shape fidelity against the real backend schema, confirmed
      the no-op guard's baseline, confirmed i18n discipline; found and
      reported a genuine narrow edge case -- an anti-cheat forced-
      submit can discard an uncommitted textarea edit that never
      reached blur -- filed separately as ISS-0654 rather than
      blocking) -> TEST-RUNNER confirmed both mix letflow.check and
      npm run check clean except pre-existing environment-only/
      probabilistic flakes, none touching exam or router paths.

      docs/issues/ISS-0650.yaml flipped open -> resolved. A short-text
      exam question can now actually be answered end to end, closing
      a real functional gap that made REQ-332's pending_manual grading
      path for that question type entirely unreachable.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-14T04:05:04Z"
    note: >
      Claimed letflow-queue task 654 (ISS-0654, GH#1373, S10) -- self-
      filed by REVIEWER during ISS-0650's frontend review. Pure
      frontend fix: ExamSessionPage.tsx's short_text question type
      stores an in-progress edit in local textDraft state, only saved
      to the backend on the textarea's onBlur. useAntiCheatSignals'
      forced-submit outcome can flip phase straight to 'result' from a
      signal callback with no blur ever firing, silently discarding an
      uncommitted edit. Fix: flush the draft (save it) before or as
      part of the forced submission, without introducing a network
      race between the flush-save and the submit call. Dispatching to
      FRONTEND-DEV.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T04:13:57Z"
    note: >
      ISS-0654 (queue task 654, GH#1373) resolved via
      fix/ISS-0654-20260914 (commit c5eac5cc). useAntiCheatSignals.ts's
      report/1 gained an optional flushBeforeReport callback, awaited
      before examApi.reportEvent is called -- a flush-save always
      completes strictly before the anti-cheat report that may carry
      a forced submit, no race. Read via a flushRef (kept in sync by a
      separate effect) rather than the listener-attach effect's own
      deps, so the anti-cheat listeners aren't torn down/reattached
      every keystroke. ExamSessionPage.tsx extracted the existing
      no-op-guarded blur-save check into a shared maybeSaveDraft/2
      helper used by both the pre-existing blur handler and the new
      flush callback.

      New test proves genuine save-before-report ordering (not a
      timing coincidence): mocks a real macrotask-delayed save against
      a synchronous report call and asserts the call-order array --
      REVIEWER independently traced that removing the await would
      flip this order. A second test proves the no-op guard fires zero
      spurious saves when the draft already matches the saved value.

      Pipeline: FRONTEND-DEV -> REVIEWER PASS (independently traced
      the flushRef pattern for stale-closure safety, traced the
      race-proof test's actual failure mode on revert, confirmed the
      unconditional-flush-before-every-report scope is structurally
      correct since the client cannot know a report's outcome in
      advance; ran the full npm run check suite live -- 554 tests + 48
      guard tests passed, clean production build).

      docs/issues/ISS-0654.yaml flipped open -> resolved.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-14T04:23:00Z"
    note: >
      Claimed letflow-queue task 649 (ISS-0649, GH#1355, S3) -- self-filed
      by CODE-DESIGNER during ISS-0648's design pass.
      TenantProvisioning.column_promotions_in_flight/2's status filter
      excludes "active", so Records.dual_write_promoted_columns/3 stops
      writing to a promoted entity type's per-entity-type table entirely
      once its ColumnPromotion reaches active -- contradicting decision
      0024 section 3 step 3's stated intent of a single write continuing
      after cutover, not zero. Consequence if unfixed: any promotion
      driven to active (e.g. future REQ-299/300 work) silently regresses
      that entity type's UNIQUE constraint enforcement for every write
      after cutover, reintroducing ISS-0648's own bug at a later
      lifecycle stage. Tenant-data-path change -- full pipeline
      (ELIXIR-DEV -> SECURITY-REVIEWER -> REVIEWER -> TEST-RUNNER)
      dispatched.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T05:25:00Z"
    note: >
      ISS-0649 (queue task 649, GH#1355) resolved via
      fix/ISS-0649-20260914. column_promotions_in_flight/2 renamed to
      column_promotions_for_table_write/2, status filter expanded to
      include "active" alongside the existing ddl_applied/backfilling/
      backfilled -- Records.dual_write_promoted_columns/3's single call
      site now keeps writing full rows to the promoted per-entity-type
      table forever after cutover.

      Confirmed against decision 0024 section 3 step 3 (independently
      re-read by both SECURITY-REVIEWER and REVIEWER): active only
      stops dual-writing the attribute into entity_record_latest's own
      field_values blob, not the per-entity-type table's own row, which
      is documented elsewhere as unconditional. The separate
      column_promotion_dual_write?/3 (field_values blob dual-write) and
      query_eligible (REQ-299/300's read-path :typed_column dispatch)
      are both untouched and confirmed independent.

      New test drives a real ColumnPromotion through register ->
      run_column_promotion -> backfill_column_promotion ->
      activate_column_promotion to active, then proves via a real
      Records.create_record/2 call that a post-activation write still
      lands in the per-entity-type table and a duplicate value against
      the unique-constrained column still fails, with a raw-SQL
      row-count check confirming no phantom row.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER PASS (INV-1/INV-7/INV-8
      confirmed, independently re-ran affected tests plus the full mix
      letflow.check: 627/629, only pre-existing rustc-absent
      toolchain-check failures) -> REVIEWER PASS (confirmed rename
      clarity and scope minimality via git diff --stat, independently
      re-read decision 0024 and the iss0648 design doc's own
      pre-registered ISS-0649 fix direction, both matching the diff) ->
      TEST-RUNNER ran the full 8-way-partitioned mix letflow.check
      (~4234 tests+properties): 4232 passed, 2 failures, both the same
      pre-existing rustc-absent CheckToolchainTest failures, unrelated
      to this diff.

      docs/issues/ISS-0649.yaml flipped open -> resolved.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-14T06:47:07Z"
    note: >
      Claimed letflow-queue task 655 (ISS-0655, GH#1377, S10). MINOR
      frontend-only gap: web/src/config/bilimbagaEntities.ts's
      BILIMBAGA_ENTITY_TYPES lists only nine entity types, missing
      "tag" -- a real, installed pack entity type authored by REQ-326.
      Consequence: /admin/bilimbaga/tag 404s despite tag records
      existing and being writable. Root cause: tag was REQ-336's pilot
      entity for the generic admin-CRUD engine; REQ-343 generalized the
      pilot into entity-agnostic EntityCrudPage and wired the other
      nine types to it but dropped tag instead of carrying it across.
      No docs/issues/ISS-0655.yaml record existed yet (queue task only)
      -- filing it as part of this close-out. Frontend-only, no
      tenant-data-path implications -- FRONTEND-DEV -> REVIEWER
      pipeline dispatched (SECURITY-REVIEWER skipped, matches ISS-0654
      precedent).
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T07:18:00Z"
    note: >
      ISS-0655 (queue task 655, GH#1377) resolved via
      fix/ISS-0655-20260914 (commit 4277f9a4). Added
      { entityType: 'tag' } to BILIMBAGA_ENTITY_TYPES, corrected the
      stale header-comment count. TagListPage.tsx (the superseded
      hand-built pilot screen) and its test deleted after confirming no
      remaining functional references. i18n catalog updated: added
      entities.entityType.tag, removed orphaned entities.tag.list.*/
      .field.name/.delete.* keys, kept .form.submitError/.conflictError
      since EntityRecordForm.tsx consumes those generically for every
      entity type. New regression tests at two layers
      (bilimbagaEntities.test.ts reading the real pack entity
      definitions off disk; BilimBagaEntityRoute.test.tsx mounting the
      route per type) -- would have caught the original bug. Two other
      tests' stale hard-coded toHaveLength(9) assertions fixed to 10.

      Pipeline: FRONTEND-DEV -> REVIEWER PASS (confirmed minimal scope,
      11 files +211/-532; independently grepped and confirmed
      TagListPage.tsx's deletion was correct not premature; confirmed
      i18n key hygiene; confirmed the new tests' admin-manageable-vs-
      runtime-state split is a defensible architectural rule; ran the
      full npm run check live -- type-check clean, lint clean, 567
      tests + 48 guard tests passed, production build succeeded).
      SECURITY-REVIEWER correctly skipped -- MINOR frontend-only fix,
      no tenant-data-path implications.

      docs/issues/ISS-0655.yaml filed and flipped straight to resolved
      (no prior open record existed).
  - req: REQ-344
    event: done
    agent: ORCH
    at: "2026-09-14T08:05:00Z"
    note: >
      Claimed letflow-queue task 656 after get_next_task fell through to
      the requirement backlog (no eligible issue-type task at the time).
      Dispatched to FRONTEND-DEV, which produced
      docs/testing/REQ-344-bilimbaga-parity-triage.md classifying all 19
      BilimBaga Playwright spec files (168 test() blocks, independently
      re-verified by ORCH via the same three grep methods before
      merging: line-anchored, raw-occurrence, word-boundary, all 168)
      against Letflow's real routes/data-testid hooks -- PORTABLE-NOW,
      PORTABLE-AFTER-<named REQ>, or NO-COUNTERPART, per-test where a
      file spans categories. Corrected
      docs/migration/stage-10-bilimbaga-vertical.md's P5 row from an
      unreproducible 286 to the measured 168. Re-derived the
      tag-reachability finding live against current main (post-ISS-0655,
      merged immediately prior in this same session): TagListPage.tsx no
      longer exists, /admin/bilimbaga/tag is reachable via
      EntityCrudPage -- ORCH independently confirmed this by grep before
      merging. No code touched; `git diff --name-only` verified limited
      to the two docs files. Merged via PR #1386
      (feat/REQ-344-20260914), CI green (backend + frontend gates).
      letflow-queue task 656 released status: done.

