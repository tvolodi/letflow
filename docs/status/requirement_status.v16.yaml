# Letflow — Requirement run history — VOLUME 16 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v15.yaml (volume 15, closed,
#   1,245 frozen-prefix lines / 1,278 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v16.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v16.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v16.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v16.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: REQ-335
    event: started
    agent: ORCH
    at: "2026-09-13T13:45:15Z"
    note: >
      Claimed letflow-queue task 632 (GH#1336, S10 P4, bucket C route
      surface). Owner ELIXIR-DEV, deliberately deviating from P4's
      FRONTEND-DEV default per the requirement's own owner-rationale
      paragraph: the entire deliverable is a new lib/letflow/routers/
      module plus new lib/letflow/api/authorization.ex permission
      atoms plus tenant-prefix-scoped calls into lib/letflow/exam/ --
      none of it touches web/. Build Letflow.Routers.ExamSessions (or
      a clearer name ELIXIR-DEV settles on), mounted at the normal
      authenticated /api/v1 forward, NOT decision 0028's
      unauthenticated capability-handle pattern (a candidate sitting
      an exam is an authenticated tenant user). Covers exactly session
      start/autosave/submit/anti-cheat-signal plus a session-state
      read, delegating to REQ-332's Letflow.Exam.Session and REQ-333's
      Letflow.Exam.AntiCheat -- no new runtime logic. No CODE-DESIGNER
      gate (matches REQ-310/311's precedent, route surface over an
      already-designed shape) UNLESS the is_correct/answer-key
      redaction open question this requirement flags turns out
      genuinely undecided, in which case ELIXIR-DEV must stop and
      escalate to CODE-DESIGNER rather than guess. Explicitly excludes
      the manual grading-queue admin workflow, adaptive selection, and
      cross-session history views -- none of their runtimes exist.
      Dispatching to ELIXIR-DEV.
  - req: REQ-335
    event: done
    agent: ORCH
    at: "2026-09-13T15:37:08Z"
    note: >
      REQ-335 (queue task 632, GH#1336) implemented via
      feature/WF02-REQ335-20260913 (commits 262ed233/bd814b11). Built
      Letflow.Routers.ExamSessions, the first HTTP route surface for
      the candidate-facing exam session API, delegating to REQ-332's
      Letflow.Exam.Session and REQ-333's Letflow.Exam.AntiCheat: POST
      /exam-sessions (start), GET /exam-sessions/:id (state read, new
      get_session_state_for_user/3), PUT
      /exam-sessions/:id/answers/:question_id (autosave -- path
      corrected from the requirement's own inferred POST to the real
      PUT, verified against backend/internal/sessions/handler.go's
      actual routing registration), POST /exam-sessions/:id/submit,
      POST /exam-sessions/:id/events (anti-cheat signal). Mounted at
      the normal authenticated /api/v1 forward, not decision 0028's
      unauthenticated pattern. Five new permission atoms
      (ExamSessionStart/Read/Save/Submit/ReportEvent) minted following
      the :Entities* precedent, granted to TASK_WORKER (this
      codebase's only non-privileged tenant-user role).

      The is_correct/answer-key redaction open question this
      requirement explicitly flagged was resolved as hand-assembly,
      not FieldGrants: no single Query.Compiler page spans the four
      joined entity types this read composes, and the pack has no
      entity_field_restrictions row for is_correct today, so a
      FieldGrants-based route would currently leak it outright.
      SECURITY-REVIEWER independently traced every field back to its
      source and confirmed the redaction is structurally safe (named
      map-literal keys, no passthrough of any entity's full
      field_values, so a future schema field cannot silently leak
      through) rather than merely incidentally safe.

      Pipeline: ELIXIR-DEV (comprehensive route/authorization/
      ownership/redaction tests) -> SECURITY-REVIEWER PASS
      (independently verified INV-1 tenant isolation, the 404-not-403
      ownership property, the redaction mechanism by tracing the code
      rather than accepting the reasoning, permission-atom wiring, and
      spot-checked the SaveAnswer path correction against the
      reference source directly; surfaced a genuine pre-existing,
      independent finding -- TASK_WORKER's existing EntitiesQuery
      grant might already expose is_correct via the generic
      entities-query route -- filed separately as ISS-0647 rather than
      expanding this requirement's scope) -> REVIEWER PASS (confirmed
      router idiom matches Letflow.Routers.Entities' precedent,
      confirmed no expansion of REQ-332's/REQ-333's own scope fences,
      gave an explicit standing sign-off that ELIXIR-DEV -- not
      FRONTEND-DEV, P4's stated default -- is the correct owner here
      since the entire deliverable is lib/letflow/routers/ +
      lib/letflow/api/authorization.ex + tenant-scoped lib/letflow/exam/
      calls with nothing under web/) -> TEST-RUNNER found one real,
      fixable defect (an unused default arg on a private test helper
      tripping the ISS-0069-class compiler-warning gate, unrelated to
      REQ-335's actual behavior -- all 4199 real tests passed both
      times TEST-RUNNER ran the suite) -- fixed directly, re-verified
      clean.

      Also fixed, on this branch, a live CI-blocking tripwire in
      test/letflow/simulation/req207_vortex_test.exs: a batch of six
      new S10 P4 requirements (REQ-336, REQ-338-340, REQ-342, REQ-343)
      landed on main mid-run, all FRONTEND-DEV client-side work or
      DOC-UPDATER bookkeeping, none touching the entities HTTP route
      surface -- admitted to the unconditional allowlist,
      REVIEWER-confirmed.

      docs/requirements.yaml flipped pending -> done.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-13T15:49:56Z"
    note: >
      Claimed letflow-queue task 647 (ISS-0647, GH#1352) -- self-filed
      during REQ-335's SECURITY-REVIEWER pass. Fact-finding first, per
      the issue's own acceptance criteria: empirically prove whether a
      TASK_WORKER-scoped POST /entities/query against question/
      answer_option currently returns is_correct (a pre-existing
      permission grant predating REQ-335, not introduced by it). If
      reachable, apply a fix (FieldGrants/entity_field_restrictions row
      or a scope narrowing) with a regression test; if not reachable,
      state why and quote the guard. Dispatching to ELIXIR-DEV given
      the real code-tracing/test-writing work involved.
  - req: REQ-336
    event: blocked
    agent: ORCH
    at: "2026-09-13T16:13:04Z"
    note: >
      Claimed letflow-queue task 633 cleanly (no sibling collision) and
      dispatched FRONTEND-DEV, which built the full admin-CRUD engine
      (web/src/api/entities.ts, EntityRecordForm.tsx, TagListPage.tsx,
      i18n catalog) against the tag pilot entity, 474/474 web/ tests
      passing. REVIEWER then found a genuine BLOCKER-severity backend gap
      while checking REQ-336's own duplicate-tag acceptance criterion:
      pack-installed entity types' declared unique constraints
      (tag.json's uq_tag_name included) are never actually enforced --
      column promotion, the only mechanism that materializes a
      constraint into real DDL, is never triggered at pack-install time.
      Empirically proven (a throwaway test created two "tag" records
      with an identical name in the same tenant; both succeeded with
      distinct record_ids, no error). Filed as ISS-0648 (queue task 648,
      GH#1354, BLOCKER). REQ-336's own AC4 ("creating a tag with a
      duplicate name surfaces the API's real 409/422") cannot be
      genuinely verified until ISS-0648 is fixed -- the frontend's
      error-shape handling is correct but never actually exercised
      against a real failure today. Released queue task 633 back to
      status: open rather than sitting on it. REQ-336's implementation
      is preserved uncommitted in the working tree (not discarded) --
      resuming this run once ISS-0648 lands rather than redoing the
      frontend work. REQ-340/REQ-342/REQ-343 inherit the same gap and
      are blocked in turn. Proceeding to REQ-338 (no dependency on
      REQ-336) while ISS-0648 is queued for its own fix cycle.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-13T18:30:49Z"
    note: >
      ISS-0647 (queue task 647, GH#1352) resolved via
      fix/ISS-0647-20260913 (commits 80bca0f1/9b12c537/cd2120a1).
      Fact-finding confirmed the leak was live, not theoretical: a
      real HTTP-stack test against real Postgres showed a
      TASK_WORKER-scoped POST /entities/query returning
      question.explanation and answer_option.is_correct/
      likert_weight/likert_polarity in clear. The FieldGrants
      redaction mechanism already existed and was already wired into
      the generic query routes -- only the entity_field_restrictions
      DATA was missing.

      First fix attempt (80bca0f1) seeded the restriction rows only
      from test/support/exam_fixtures.ex -- a test helper.
      SECURITY-REVIEWER correctly FAILed this, tracing the real
      production path (POST /solution-packs/install ->
      SolutionPack.install/3) and finding zero references to the
      seeding function there, meaning a real tenant installing the
      pack today got zero protection -- the leak this issue was filed
      to close was still live in reality despite a passing test suite.
      SECURITY-REVIEWER also found ELIXIR-DEV's "kept generic per
      decisions 0026/0027/0029" scope rationale did not hold up: those
      decisions govern the pack-document's closed section-key struct,
      not the installer function's internal behavior.

      Reworked (9b12c537): seed_pack_specific_field_restrictions/2
      added directly inside SolutionPack.run_install/5's own
      transaction, using the same prefix(opts) helper every other
      step uses, matched via MapSet.subset?/2 of
      Bilimbaga.answer_key_fields/0's entity types against the
      install's actual entity-type set -- the one bilimbaga-aware
      branch in an otherwise generic installer. Verified
      FieldGrants.load_restrictions/3 reads entity_field_restrictions
      by entity_type string alone (no join to entity_definitions'
      status column) before relying on that claim, confirming
      install-time seeding is meaningful even though entity
      definitions stay :inactive per 0026. New end-to-end test
      installs the real committed priv/packs/bilimbaga/pack.json via
      SolutionPack.install/3 and never calls the seeding function
      directly, then proves redaction through a real
      POST /entities/query call.

      Pipeline: ELIXIR-DEV -> SECURITY-REVIEWER FAIL then PASS (first
      pass found the test-fixture-only gap; second pass independently
      traced the real call chain and confirmed the rework genuinely
      closes it) -> REVIEWER PASS (confirmed the decision-record
      reading, confirmed no pack-hook/callback mechanism already
      exists to prefer over the inline branch, confirmed the test is
      non-tautological; flagged one non-blocking nit -- a
      future-maintainer comment for the entity-type-name-matching
      edge case once a second pack exists -- added directly by ORCH).
      Full mix test: 4207-4209/4209 passed depending on run, only
      pre-existing rust-toolchain-absence failures and one confirmed
      parallel-execution-only flake.

      docs/issues/ISS-0647.yaml flipped open -> resolved. Fixed a
      real, live, previously-undetected answer-key data leak
      reachable by any TASK_WORKER-scoped tenant user, not only exam
      candidates -- broader exposure than REQ-335's own scope, closed
      before shipping.
  - req: REQ-338
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T19:27:01Z"
    note: >
      REQ-338 (hand-written candidate exam-taking UI -- start screen,
      in-progress question flow with autosave and server-reanchored
      countdown, submit, result -- wired to REQ-335's exam-session
      routes and REQ-333's anti-cheat signal route) implemented on
      feature/WF02-REQ338-20260914, superseding the abandoned
      feature/WF02-REQ338-20260913 branch (commit 78e0146e, left
      unmerged -- its web/src/api/exam.ts, web/src/api/queryKeys.ts and
      web/src/pages/exam/ExamListPage.tsx were an earlier, CI-failing
      attempt, since fully rewritten). Delivered: web/src/api/exam.ts
      (startSession/saveAnswer/submitSession/getSessionState/
      reportEvent wrapping REQ-335's real routes), web/src/types/exam.ts,
      web/src/utils/examErrors.ts, web/src/hooks/useAntiCheatSignals.ts
      (visibilitychange/blur/fullscreenchange -> REQ-333's
      tab_switch/blur/fullscreen_exit, torn down on unmount/leaving the
      in-progress screen), web/src/i18n/examMessages.ts +
      ExamIntlProvider.tsx (kk/ru/en, no hard-coded English, grep-proven),
      web/src/pages/exam/ExamListPage.tsx and ExamSessionPage.tsx, plus
      their test suites and the exam.ts/i18n-grep test suites, and the
      additive exam-only hunks in web/src/router.tsx (two new routes)
      and web/src/api/queryKeys.ts (the `exam` key group, staged
      separately from REQ-336's own `entities` key group via `git add
      -p`, verified via `git diff --cached` before commit).

      Ambiguity escape (this requirement's own mandatory close-out
      statement): chose OPTION (a) -- list every exam whose status is
      active, matching check_assigned/3's actual current no-op
      behaviour honestly, with the list screen's own copy stating
      PROMINENTLY that this is provisional pending an assignment
      decision record. Basis, quoted verbatim from
      lib/letflow/exam/session.ex's moduledoc "FINDING" section: "the
      'assignment' eligibility check cannot be truthfully implemented
      ... `check_assigned/3` below is a documented no-op -- every
      candidate is currently treated as assigned. `:not_assigned`
      remains declared in `eligibility_error()` for interface-shape
      fidelity with the design doc, but is unreachable in this
      implementation." The provisional-pending-decision-record copy
      rendered in ExamListPage is asserted present by its own test.

      ISS-0650 (MAJOR, queue task 650, GH-1356) discovered and correctly
      scoped OUT of this requirement while implementing it: REQ-332's
      Session.autosave_answer/4 and upsert_answer/6 have no
      text_answer field anywhere in the write path, so a short-text
      question's answer can never be captured or graded regardless of
      what a client sends. Confirmed not a frontend gap --
      ExamSessionPage correctly shows a "not yet supported" note for
      short-text questions rather than offering a text input that would
      silently fail to save. Filed as its own backend issue rather than
      worked around here; REQ-338's own acceptance criteria for
      grading_pending / short-text handling are satisfied against the
      backend's actual (incomplete) capability, not a client-side
      shim.

      REQ-336 coupling defect found and fully fixed across two rounds
      during this close-out. REQ-336 (generic admin-CRUD engine) and
      REQ-338 share two files (web/src/router.tsx, web/src/api/
      queryKeys.ts) plus additions inside web/src/types/api.ts and
      web/src/api/client.ts that belong to REQ-336 alone -- both
      requirements' uncommitted work sat in the same working tree after
      REQ-336 was blocked on ISS-0648 (pack-install unique-constraint
      gap) and preserved rather than discarded. First close-out attempt
      committed REQ-338's own new files plus the router.tsx/queryKeys.ts
      hunks but MISSED that REQ-336 had also added shared-file type/
      queryKey scaffolding beyond the `entities` key group and a
      `putWithHeaders` client helper -- a partial fix that would have
      left REQ-336's own additions half-committed under REQ-338's
      commit. Reworked: re-diffed the full working tree against main,
      confirmed via `git diff --cached` that ONLY REQ-338's own new
      files and the exam-only hunks in router.tsx/queryKeys.ts were
      staged, then independently verified the result by stripping every
      REQ-336 file (entities.ts, components/entities/, pages/entities/,
      entityFieldToFormField.ts, entitiesMessages.ts,
      EntitiesIntlProvider.tsx, entities-i18n-grep.test.ts,
      api/__tests__/entities.test.ts) AND REQ-336's own hunks in the two
      shared files, and confirming REQ-338 builds and its own test
      suite (`npm run check`) passes standalone against main with
      REQ-336 entirely absent. REQ-336's own uncommitted work (including
      its shared-file hunks) was left untouched in the working tree
      throughout, to resume once ISS-0648 lands.

      Reviewed twice by REVIEWER (once for the implementation, once for
      the router.tsx/queryKeys.ts decoupling fix) -- both PASS.
      Independently re-confirmed PASS by REVIEWER after a branch-name
      collision recovery (the abandoned feature/WF02-REQ338-20260913
      name from the earlier buggy attempt, superseded here by
      feature/WF02-REQ338-20260914 since ISS-0648's unrelated fix had
      been sharing the same working directory in the interim and has
      since fully merged, confirmed clean on main). TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR/TEST-RUNNER completed; `npm run check`
      inside web/ reported 502+48 tests passing. RELEASE-VALIDATOR
      independently confirmed every acceptance criterion satisfied.

      docs/requirements.yaml flipped pending -> done.
  - req: REQ-336
    event: done
    agent: DOC-UPDATER
    at: "2026-09-13T20:55:45Z"
    note: >
      REQ-336 (generic admin-CRUD screen engine in web/, S10 P4 bucket C
      client, proven against the tag pilot entity) completed after
      resuming from the ISS-0648 block recorded above. Full pipeline:
      FRONTEND-DEV implementation (web/src/api/entities.ts,
      web/src/utils/entityFieldToFormField.ts,
      web/src/components/entities/, web/src/pages/entities/,
      web/src/i18n/entitiesMessages.ts,
      web/src/i18n/EntitiesIntlProvider.tsx, plus the entities-only
      hunks in web/src/api/client.ts (putWithHeaders, for If-Match
      optimistic-concurrency updates), web/src/types/api.ts and
      web/src/api/queryKeys.ts's `entities` key group) -> blocked on a
      real BLOCKER-severity backend gap found while checking this
      requirement's own AC4 (pack-installed unique constraints, tag's
      uq_tag_name included, were never enforced because column
      promotion never ran at pack-install time) -> ISS-0648 fixed and
      merged separately, unblocking this requirement -> resumed, and a
      SECOND backend gap surfaced in the same AC4 path: even after
      ISS-0648's fix, a unique-violation write was still surfacing to
      the client as a bare 500 instead of a real 409/422, because
      lib/letflow/entities/records.ex had no translation for Postgres's
      unique_violation error class. ELIXIR-DEV root-caused and fixed
      this in lib/letflow/entities/records.ex, with new end-to-end
      coverage in test/letflow/routers/entities_test.exs and
      test/letflow/entities/iss0648_column_promotion_trigger_test.exs
      proving a real duplicate-tag POST against a real pack-installed
      constraint now returns the documented error shape rather than a
      500. Pipeline continued: SECURITY-REVIEWER PASS, REVIEWER PASS,
      TEST-DESIGNER (no coverage gaps found), TEST-DESIGN-VALIDATOR
      PASS, TEST-RUNNER (full `mix letflow.check`: 4221/4221; full
      `npm run check` inside web/: 550/550), RELEASE-VALIDATOR PASS
      (independently re-verified every acceptance criterion).

      AC8 close-out ("no BilimBaga file copied"): confirmed by naming
      the actual web/ design-system components the generic engine
      composes rather than any copied BilimBaga-specific screen code --
      PageLayout, Button, DataTable, PaginationControls, ConfirmDialog,
      and QueryStateBoundary.

      docs/requirements.yaml flipped pending -> done.
