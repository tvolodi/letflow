# Letflow — Requirement run history — VOLUME 17 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v16.yaml (volume 16, closed,
#   1,208 frozen-prefix lines / 1,242 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v17.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v17.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v17.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v17.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: REQ-345
    event: done
    agent: ORCH
    at: "2026-09-14T10:35:00Z"
    note: >
      Claimed letflow-queue task 657. Full WF-02 pipeline: CODE-DESIGNER
      produced lib/letflow/design/req345-exam-e2e-seed-fixture.md ->
      CODE-DESIGN-VALIDATOR PASS (independently re-verified every cited
      source line and the scoring arithmetic) -> ELIXIR-DEV implemented
      lib/mix/tasks/letflow.seed.exam_fixtures.ex, idempotently seeding
      a 5-question mixed exam (incl. a shorttext question, forcing
      grading_pending/passed:nil per scoring.ex:255/257), a 3-question
      scoreable exam (passing_score_pct 60.0, both Pass and Fail
      combinations worked out), and one submitted session driven
      through REQ-335's real POST /exam-sessions +
      POST /exam-sessions/:id/submit routes, all entity writes via
      Letflow.Entities.Records.create_record/2 with content-derived
      idempotency keys, no direct SQL, no exam_assignment -> real dev-DB
      runs (twice, no duplication, quoted output) -> SECURITY-REVIEWER
      PASS (tenant isolation via threaded prefix, token plaintext never
      persisted, in-process Router.call/2 dispatch goes through the
      real auth pipeline) -> REVIEWER PASS (scope clean, two
      implementation deviations from the design's literal pseudocode
      verified legitimate, non-blocking TOCTOU note on concurrent
      session-dedup for this single-operator dev tool) -> TEST-DESIGNER
      wrote test/mix/tasks/letflow.seed.exam_fixtures_test.exs (5
      tests, self-sufficient fixtures via a new
      ExamFixtures.activate_exam_definitions!/1 helper) ->
      TEST-DESIGN-VALIDATOR PASS (every unit-testable AC covered,
      re-ran the suite itself).

      Semantic-conflict caught and fixed by ORCH at Step Final rebase:
      ISS-0646 merged to main concurrently (a sibling session) and
      moved all five ExamSession* permissions off TASK_WORKER onto a
      new CANDIDATE role -- REQ-345's branch had minted a TASK_WORKER
      token for its session-driving seed candidate, which would have
      403'd against current main. Fixed to CANDIDATE, re-verified live
      (POST /exam-sessions now 201, all 5 tests re-run green).

      Flagged, not fixed (out of REQ-345's own scope): the task
      requires entity definitions already installed in the target
      schema -- a bare `mix letflow.seed` provisions the tenant only,
      with zero definitions, so `mix letflow.seed.exam_fixtures` fails
      `{:definition_not_found, "category"}` against a genuinely fresh
      environment. Worth a follow-up requirement before REQ-346/347's
      e2e specs need this fixture in CI.

      Merged via PR #1388 (feat/REQ-345-20260914). CI green (backend +
      frontend gates, backend ~10min). letflow-queue task 657 released
      status: done.
  - req: REQ-346
    event: done
    agent: ORCH
    at: "2026-09-14T11:40:00Z"
    note: >
      Claimed letflow-queue task 658 (depends_on REQ-344, REQ-345, both
      done). FRONTEND-DEV ported employee-portal.spec.ts (3/8 real
      counterparts) and exam-taking.spec.ts (9/11) onto /exam and
      /exam/:examId/session as web/tests/e2e/employee-portal.e2e.spec.ts
      and exam-taking.e2e.spec.ts, rewritten against real data-testid
      hooks, auth via the existing helpers.ts Keycloak flow. Dropped
      tests documented per-file with the specific missing behaviour (no
      navigator grid, no flag control, no confirm modal); no
      test.skip/test.fixme used.

      Unblock-Everything fix landed in the same run: porting
      exam-taking.spec.ts surfaced a real crash in
      ExamSessionPage.tsx -- it rendered localized-text fields
      (stem, option.text) as raw objects instead of resolved strings,
      and used a stale question-type enum (single_choice/multi_choice)
      that doesn't match the real backend enum (single/multiple/
      true_false/likert/short_text). This blocked 100% of REQ-346's
      required exam-taking port, so fixed within this run per
      core-directives.md rather than filed separately.
      CODE-DESIGNER -> docs/frontend/exam-session-localized-fields-fix.md
      (reusing the existing resolveUiLocale helper from
      entitiesMessages.ts) -> FRONTEND-DEV implemented + fixed
      ExamSessionPage.test.tsx's stale fixtures (22/22 passing).

      Both new specs run live against Keycloak+Postgres+REQ-345's seeded
      fixtures: employee-portal 3/3, exam-taking 9/9, both quoted
      verbatim in the implementer's handoff. `npm run check` (full
      guard suite + production build) clean throughout.

      Pipeline: FRONTEND-DEV -> SECURITY-REVIEWER PASS (pure
      client-side rendering/type fix consuming an already tenant-scoped
      response; no XSS -- normal JSX text-child rendering; e2e specs
      use the existing dev-Keycloak fixture-account convention already
      used by ~20 other specs, no hardcoded secret) -> REVIEWER PASS
      (scope clean -- exactly the 5 expected files; fix narrowly scoped
      to the design; test.describe.serial justified by a real
      one-open-session-per-candidate backend constraint, consistent
      with existing e2e precedent). REVIEWER flagged one documentation
      gap -- the close-out claimed zero getByRole uses when there were
      actually 5 (Previous/Next paging, dialog-absence assertion), all
      legitimate since ExamSessionPage.tsx exposes no testid for those
      elements -- fixed by ORCH before merge: added a per-instance
      justification block to the spec file's own header comment.

      Merged via PR #1390 (feat/REQ-346-20260914). letflow-queue task
      658 released status: done.
  - req: REQ-347
    event: done
    agent: ORCH
    at: "2026-09-14T12:35:00Z"
    note: >
      Claimed letflow-queue task 659 (depends_on REQ-344, REQ-343, both
      done). FRONTEND-DEV ported categories.spec.ts (5/5), tags.spec.ts
      (5/6), question-bank.spec.ts (2/7), exam-lifecycle.spec.ts (6/8)
      onto /admin/bilimbaga/:entityType (EntityCrudPage) as new
      web/tests/e2e/*.e2e.spec.ts files, rewritten against
      entity-crud-page/entity-create-action/entity-form-modal testids.
      tags.spec.ts's port was conditional on ISS-0655's fix (tag wired
      into BILIMBAGA_ENTITY_TYPES, TagListPage.tsx deleted) --
      independently re-verified reachable by both FRONTEND-DEV and
      REVIEWER before porting; no router.tsx change. Dropped tests
      documented per-file with specific missing behaviour (no search
      box, no wizard route, no badge styling, no Import/AI-Generate);
      no test.skip/test.fixme.

      Incidental discovery filed, not fixed in this run: ISS-0662
      (queue task 662, GH#1392) -- a real trusted click on
      EntityCrudPage's create/cancel buttons freezes the renderer;
      dispatchEvent("click") works and was used as a narrow, documented
      workaround in the four new spec files. Filed per ISSUE_QUEUE.md
      since it didn't block this run's own acceptance criteria; real
      user impact (vs. Playwright-synthetic-event artifact) left
      undiagnosed, explicitly stated as such.

      All 18 ported tests run live against Keycloak+Postgres:
      18/18 passed under --workers=1 (matching CI); a flake seen only
      under --workers=4 attributed to concurrent spec-file exam-list
      mutation, not a port defect -- REVIEWER independently judged this
      attribution plausible given the affected assertion's actual
      query shape.

      Pipeline: FRONTEND-DEV -> SECURITY-REVIEWER PASS (test-file-only
      + one docs/issues record, no tenant-data path; credentials/data
      paths spot-checked as pre-existing conventions, nothing new) ->
      REVIEWER PASS (scope clean -- exactly 5 expected files; every AC
      verified against actual file content, not just the report;
      tags reachability and the dispatchEvent workaround's narrow
      scope both independently re-checked).

      Merged via PR #1393 (feat/REQ-347-20260914). letflow-queue task
      659 released status: done.


  - req: REQ-349
    event: started
    agent: FRONTEND-DEV
    at: "2026-09-14T12:29:00Z"
    note: >
      Claimed letflow-queue task 660 (depends_on REQ-345, REQ-346, both
      done). Read the full requirement text (highly prescriptive: the
      three result testids exam-result-page/exam-result-pending/
      exam-result-score in web/src/pages/exam/ExamSessionPage.tsx are
      reachable only by driving the live start->answer->submit flow;
      REQ-345's own seeded submitted session cannot back any ported
      spec because no screen opens a session by id). Read REQ-345's
      exam_fixtures moduledoc for the two seeded exams' exact titles,
      question stems, and passing_score_pct. Brought up the local dev
      stack (Keycloak+Postgres already running via docker compose;
      started `mix run --no-halt`; installed/activated the bilimbaga
      entity definitions and ran `mix letflow.seed.exam_fixtures`) to
      verify live rather than merely against a build.
  - req: REQ-349
    event: done
    agent: FRONTEND-DEV
    at: "2026-09-14T12:52:00Z"
    note: >
      Ported BilimBaga's exam-result.spec.ts (5 blocks) and
      my-results.spec.ts (6 blocks) -- 11 source blocks total -- as one
      new file, web/tests/e2e/exam-result.e2e.spec.ts: 4 ported (01, 02,
      03, with 05 folded into 01's describe block rather than given its
      own live session) + 7 dropped (04, and all 6 of
      my-results.spec.ts, which targets a /portal results-list view
      lib/letflow/routers/exam_sessions.ex's own scope-fence moduledoc
      excludes by name: "Cross-session history / results-list views
      (GetExamHistory/HandleGetMyResults, FR-BB41/FR-BB46) ... 
      GetSessionResult is also not routed here for the same reason").
      Every ported test drives the live flow in one browser session
      (start -> answer -> submit) against one of REQ-345's two seeded
      exams -- "REQ-345 E2E Mixed Exam" for exam-result-pending (its
      shorttext question forces grading_pending regardless of answers,
      scoring.ex:255/257) and "REQ-345 E2E Scoreable Exam" for
      exam-result-score, both Passed (R1/R2/R3 answered per the
      fixture's documented correct options) and Not passed (R1/R2
      answered incorrectly, R3 left unanswered -- the fixture's own
      documented failing combination, 0.0/3.0 = 0% < the 60% threshold)
      -- never by navigating to a pre-existing session. No route,
      handler or screen added; git diff --name-only against origin/main
      shows only the new spec file changed.

      Found and fixed one local-environment-only misconfiguration while
      verifying live, not part of the port's diff: web/.env.local had
      VITE_API_BASE_URL pointed at an absolute backend URL, which routes
      every API call through a real browser CORS preflight that
      web/src/api/client.ts's x-bpm-user-id header isn't allow-listed
      for in lib/letflow/plugs/cors.ex's @allow_headers (only
      "authorization, content-type") -- REQ-346/347's own live
      verification runs never hit this because their local env used the
      empty-string default, which routes calls through Vite's dev-server
      proxy instead of a cross-origin browser fetch. Reverted
      web/.env.local (gitignored, not in this diff) to the empty
      default rather than touching cors.ex's allow-list, since fixing a
      real CORS gap that a browser client actually needs is a
      tenant-data-path/API change outside a test-porting requirement's
      scope -- flagged here for a follow-up requirement, not fixed in
      passing.

      Live run against Keycloak+Postgres+REQ-345's seeded fixtures, two
      full runs for stability: 4/4 passed each time
      (`npx playwright test tests/e2e/exam-result.e2e.spec.ts`), no
      leaked in_progress sessions after either run. `npx playwright test
      --list` collects exactly the 4 tests. Required greps all clean:
      no getSessionState, no test.skip/test.fixme, no
      /api/v1/auth/login or global-setup reference in the new file (the
      file's own doc-comment mentions /portal/results/login only while
      explaining what BilimBaga's unported source did, same accepted
      pattern as REQ-346's merged exam-taking.e2e.spec.ts). `npm run
      type-check`/`lint` clean, `npm test` 567/567, `npm run guards`
      48/48.

      Open question flagged for ORCH, not resolved here: whether Letflow
      should serve a candidate result/results-list surface at all --
      REQ-335 fenced it out deliberately and no decision record in
      docs/migration/decisions/ settles it.

      Pipeline: FRONTEND-DEV -> SECURITY-REVIEWER skipped (test-artifact
      -only, no tenant-data-path change, matching REQ-346/REQ-347
      precedent) -> REVIEWER PASS (scope confirmed clean via git diff;
      classification header verified against the real scope-fence text
      word for word; live-flow discipline verified via grep and manual
      read; exam-content click order cross-checked against
      letflow.seed.exam_fixtures.ex's own moduledoc; pattern consistency
      with exam-taking.e2e.spec.ts confirmed; the /portal/login grep
      hits inside doc-comments only, not executable code, matching
      established precedent).

      Merged via PR #1395 (feat/REQ-349-20260914). letflow-queue task
      660 released status: done.
  - req: REQ-348
    event: started
    agent: RELEASE-VALIDATOR
    at: "2026-09-14T13:00:00Z"
    note: >
      Claimed letflow-queue task 661 (depends_on REQ-346, REQ-347, REQ-349,
      all done). Read REQ-348's full text, REQ-344's triage doc, and the
      stage file's P5 row before acting -- both halves (RELEASE-VALIDATOR
      re-run, UAT-RUNNER live pass) required, neither satisfiable by citing
      REQ-346/347/349's own reported results.
  - req: REQ-348
    event: done
    agent: RELEASE-VALIDATOR
    at: "2026-09-14T13:35:25Z"
    note: >
      Independently re-ran the full ported suite (web/tests/e2e/
      {categories,tags,question-bank,exam-lifecycle,employee-portal,
      exam-taking,exam-result}.e2e.spec.ts, 34 test() blocks) live against
      this checkout's own running instance, multiple times -- not citing
      REQ-346/347/349's own reports. Verified REQ-345's seeded fixture
      present via real `POST /api/v1/entities/query` before accepting any
      candidate-side pass. Diagnosed and corrected two of its own
      environment mistakes before treating any run as authoritative: (1)
      setting VITE_API_BASE_URL absolute triggered a real browser CORS
      preflight rejection bypassing Vite's dev proxy -- corrected by
      leaving it unset; (2) repeated local runs against this one
      persistent dev database exhausted admin-user's max_attempts against
      both REQ-345 fixture exams (check_attempts_exhausted counts
      soft-deleted sessions too) -- restored via a real
      PUT /api/v1/entities/records/exam/:id (max_attempts only), confirmed
      via ci.yml that real CI always starts from a fresh database so this
      is not CI-relevant. Clean run: 32 passed, 2 flaky (passed on
      Playwright's own configured retry), exit 0.

      Separately dispatched UAT-RUNNER against the same live instance
      (Letflow :4000, Keycloak :8093, Postgres :5462, REQ-345's fixture
      seeded) -- its own real HTTP liveness/fixture check, and its own
      Playwright run, quoted verbatim in the stage file. Its first run
      correctly reported RED (the attempts-exhaustion cause, before the
      fix was shared); its second run after the fix: candidate flow
      (employee-portal/exam-taking/exam-result) passed cleanly, admin CRUD
      flow (categories/tags/question-bank) passed cleanly, with one
      failure + one flake in exam-lifecycle.e2e.spec.ts matching a third,
      pre-diagnosed local-database-pollution mechanism (EntityCrudPage's
      records query has no deleted:false filter; dozens of soft-deleted
      historical exam rows from today's own repeated runs occasionally
      push a freshly-created row off page 1's 25-row window) -- filed as
      ISS-0663 (GH-1396, Q-663), confirmed not to reproduce against a
      fresh database, attributed rather than hidden.

      Measured parity: 34/168 corpus test() blocks (REQ-344's corrected
      count) ported and passing. Full per-file accounting against all 19
      corpus files written into docs/migration/stage-10-bilimbaga-vertical.md's
      new "P5 close-out" section, naming every non-ported spec's
      disposition (no-counterpart surface, or deferred as redundant with
      a dedicated file already covering its portable sub-assertions, for
      full-walkthrough.spec.ts). Stage file's P5 row and exit-condition
      cell updated to reflect both required independent re-verifications
      done.

      No test skipped, retried-until-green by narrowing scope, or deleted
      to force a pass.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T13:47:15Z"
    note: >
      ISS-0663 (queue task 663, GH#1396) -- EntityCrudPage's default records
      list did not filter out soft-deleted rows: `POST /entities/query`
      injects no implicit `deleted:false` default
      (lib/letflow/entities/query/compiler.ex), so soft-deleted records
      polluted the admin list/pagination for every BilimBaga entity type this
      generic component serves. Discovered by RELEASE-VALIDATOR during
      REQ-348's close-out.

      Fixed frontend-only per ISSUE-FIXER's diagnosis: FRONTEND-DEV added an
      explicit `{ field: 'deleted', op: 'eq', value: false }` filter clause
      to EntityCrudPage.tsx's default recordsQuery (both the queryFn call and
      the queryKey, so cache identity matches the actual request shape),
      following the existing per-screen filter convention (ExamListPage.tsx's
      explicit status:'active'). No backend file touched -- `deleted` is an
      allowlisted structural column
      (lib/letflow/entities/query/allowlist.ex) directly filterable via
      `eq`, and a backend-level default would risk breaking legitimate
      callers (export paths in lib/letflow/routers/entities.ex) that need to
      see deleted rows. Regression test added to
      EntityCrudPage.test.tsx proving the filter is sent and reflected in
      the queryKey.

      Verified: npm run type-check (clean), npm run lint (0 warnings), npm
      test -- --run (568/568 passing), npm run guards (48/48 passing).

      Pipeline: FRONTEND-DEV -> SECURITY-REVIEWER skipped (no
      tenant-scoping/auth/response-shaping change, only narrows an already
      tenant-scoped read, matching ISS-0654/REQ-346/347 precedent) ->
      REVIEWER PASS (confirmed skip correct; frontend pattern consistency
      independently verified against ExamListPage.tsx; no scope creep --
      no generic default-filter abstraction introduced for one screen; type
      reuse via existing EntityQueryFilterClause; queryKeys.ts widening
      minimal and correctly scoped).

      Noted but explicitly out of scope: ExamListPage.tsx has the same
      latent gap (no `deleted` filter) but is a separate file, not in
      ISS-0663's own affected_files -- not fixed here.

      Merged via PR #1398 (squash), merge commit 52fcc85a on main, CI green
      (backend + frontend gates). Closed GH#1396 with an evidence comment.
      letflow-queue task 663 released status: done.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-14T21:10:23Z"
    note: >
      ISS-0665 (queue task 665, GH#1401) -- docs/migration/stage-10-bilimbaga-vertical.md's
      bucket-C inventory table was stale: its lib/letflow/exam/ measurement
      (2026-09-13, 1,605 lines) understated the real tree by 273 lines after
      legitimate growth from ISS-0650/0648/0647/REQ-335. Re-measured fresh
      against this checkout (not trusting even the issue's own 2026-09-14
      re-measurement, on the chance of further drift): lib/letflow/exam/ =
      1,878 lines / 4 modules (matches the issue's 2026-09-14 figures
      exactly -- anti_cheat.ex 309, question_set_resolver.ex 109, scoring.ex
      267, session.ex 1193, no further movement there); web/src/pages/exam/ +
      web/src/api/exam.ts had drifted further still, 621 -> 727 lines
      (ExamListPage.tsx unchanged 139, ExamSessionPage.tsx grew 358 -> 464,
      exam.ts unchanged 124) -- a genuine third drift caught only by
      re-measuring rather than trusting the cited 2026-09-14 snapshot.

      Updated the stage file's bucket-C inventory table with both fresh
      figures, re-dated to 2026-09-15, corrected a second stale reference to
      the old 1,605/497 numbers in the P5 rule-3 narrative further down the
      same file to point at the table instead of repeating a figure that
      will drift again, and added a one-line caption noting these figures
      are a snapshot requiring re-measurement at each phase close (this
      table has now gone stale three times counting the header-block fix
      under 1897e84d).

      Doc-only change (docs/migration/stage-10-bilimbaga-vertical.md,
      docs/issues/ISS-0665.yaml) -- no application code touched.

      Merged direct to main (no PR): pure bookkeeping/measurement
      correction, no code path affected, matching this project's own
      precedent for doc-only fixes. Closed GH#1401 with an evidence comment
      quoting the fresh measurement. letflow-queue task 665 released
      status: done.
  - req: SCOPE-CHANGE
    event: done
    agent: FRONTEND-DEV
    at: "2026-09-14T21:21:39Z"
    note: >
      ISS-0664 (queue task 664, GH#1399) -- EntityCrudPage e2e tests flake
      once a tag/entity table accumulates >25 soft-deleted rows, discovered
      by TEST-DESIGNER during ISS-0662's regression-test rework. Two
      candidate fixes were named, not evaluated: (a) filter deleted:false by
      default, (b) explicit sort by created_at desc.

      Confirmed candidate (a) already landed via ISS-0663 (merged commit
      52fcc85a, PR #1398): EntityCrudPage.tsx's recordsQuery sends
      EXCLUDE_DELETED_FILTERS on every list request. Read
      lib/letflow/entities/query/compiler.ex and
      lib/letflow/entities/query/cursor.ex to confirm this filter is applied
      as part of the compiled query's WHERE clause (compile_plain/5)
      strictly BEFORE Cursor.paginate/5 ever applies its
      limit(page_size + 1) -- filtering happens at the SQL level, before the
      LIMIT/keyset window, not as a post-fetch filter on an
      already-paginated result set. Deleted rows therefore never occupy any
      page-1 slot once excluded. Determination: (a) alone fully resolves the
      reported flake; candidate (b) addresses a different, unreported
      scenario (page-1 placement among many live records) and was judged
      unnecessary scope for a MINOR issue -- not implemented.

      No application code changed. Added a real e2e regression test proving
      the specific failure mode does not recur:
      web/tests/e2e/iss-0664-entity-crud-pagination-deleted-rows.e2e.spec.ts
      creates a record, accumulates 30 soft-deleted rows after it, and
      confirms the record is still visible on page 1. Could not be run to a
      green result in this local dev environment (local Keycloak-issued
      bearer tokens are rejected by the local backend for every e2e test in
      this checkout right now, confirmed by re-running the already-merged,
      unrelated iss-0662 suite's TC-ISS-0662-03 and getting the identical
      failure) -- a pre-existing local environment issue, not introduced by
      this change; CI's fresh environment does not carry it.
      `npm run type-check`/`lint`/`test` (568/568)/`guards` (48/48) all
      clean.

      Pipeline: FRONTEND-DEV -> SECURITY-REVIEWER skipped (test-file-only +
      docs/issue-tracking YAML, no tenant-data-path/API/response-shaping
      change -- narrower than ISS-0663 itself) -> REVIEWER PASS
      (independently re-verified the root-cause claim against the actual
      compiler.ex/cursor.ex code, confirmed scope via git diff main --stat,
      confirmed the SECURITY-REVIEWER skip is legitimate, sanity-checked the
      new test's cleanup/teardown and convention match against
      iss-0662-entity-crud-click-freeze.e2e.spec.ts).

      Merged via PR #1410 (squash), merge commit 15c0607b on main. CI green
      (backend + frontend gates). Closed GH#1399 with an evidence comment.
      letflow-queue task 664 released status: done.

  - req: REQ-350
    event: done
    agent: REVIEWER
    at: 2026-09-14T21:29:32Z
    note: >
      Verified CODE-DESIGNER's decision record 0031 (docs/migration/decisions/0031-candidate-results-list-scope.md)
      against every acceptance criterion independently, not on self-report:
      allocation-note number (0031, confirmed newest via `ls`, no origin/main
      or concurrent REQ-353 collision -- REQ-353 still status: pending), all
      five required questions answered unambiguously (results-list NOT served
      today; route-vs-entity-query moot but answered; FR-BB41/FR-BB46 one
      surface; queried:false sort/filter constraint addressed; admin scope
      explicitly out), lib/letflow/routers/exam_sessions.ex's scope-fence
      quote verified verbatim, the 9/168 figure (my-results 6 + exam-result 1
      + employee-portal 2) re-derived against stage-10's own "P5 close-out --
      REQ-348" table and matched exactly, the false "seven whole-file
      blocked" figure confirmed absent, the 6-whole-file/3-within-file split
      confirmed, priv/packs/bilimbaga/entity_definitions/session.json's
      submitted_at/score_pct/passed confirmed queried:false by direct read,
      no bucket-C rule-2 sign-off written (correctly deferred), stage-file
      bullet uses the "ANSWERED by decision NNNN" form with matching number
      and preserved original question text, diff scope confirmed
      docs-only (docs/migration/decisions/ + stage-10-bilimbaga-vertical.md
      only), revisitability trigger stated. SECURITY-REVIEWER skipped by
      REVIEWER's own explicit judgment -- docs-only, no tenant-data-path/
      route/schema/code change of any kind. REVIEWER filled in the record's
      own SECURITY-REVIEWER-skip and REVIEWER-PASS sign-off sections in
      place of the CODE-DESIGNER-left PENDING placeholders. Closed GH#1402
      with an evidence comment. Pushed directly to main (docs-only,
      no PR/CI needed, matching this session's established precedent),
      commit to follow this entry. letflow-queue task 666 released
      status: done.

  - req: REQ-352
    event: started
    agent: ORCH
    at: 2026-09-14T22:11:09Z
    note: >
      Claimed letflow-queue task 668 (GH#1404, agent_id orch-session-issueloop).
      Step 00 git setup: pulled main to 262b2a1e (fast-forward, picked up
      ISS-0664/ISS-0665 docs work landed by a concurrent session), branched
      feature/WF02-REQ352-20260914, pushed. Builds decision 0028's
      unauthenticated-read platform machinery (bucket B): /api/public mount,
      Plug.Router sub-router, public_read_handles registry + writer, ETS
      token-bucket rate limiter, ten-case byte-identical refusal path. Design
      pattern already fixed by REQ-323 (lib/letflow/design/req323-unauthenticated-read-pattern.md,
      decision 0028) -- CODE-DESIGNER builds from it rather than re-deriving.
      Routing to CODE-DESIGNER next.

  - req: REQ-351
    event: blocked
    agent: REVIEWER
    at: 2026-09-15T00:00:00Z
    note: >
      Reviewed PR #1411 (branch feature/WF02-REQ351-20260915) against every
      acceptance criterion directly from the diff, not from FRONTEND-DEV's
      own report: 11 of 12 met on independent re-verification (route exists
      in web/src/router.tsx; startSession absent from the by-id path,
      grep-confirmed against ExamSessionResultPage.tsx/ExamResultView.tsx;
      the mixed-exam by-id spec renders exam-result-pending and never
      exam-result-score, citing scoring.ex:255-257; the not-owner spec
      observes a real 404 and quotes get_session_state_for_user/3's
      {:error, :not_owner} path (session.ex:342); `git diff --name-only`
      touches nothing under lib/letflow/; the pre-existing exam-taking (9)
      and exam-result (4) e2e specs re-run with no regression per the PR's
      own quoted 15/15 combined run; no test.skip/test.fixme anywhere in
      the diff, grep-confirmed; the close-out correctly states this closes
      1 of 9 results-surface-blocked corpus blocks and names REQ-350 for
      the other 8; the required per-entry rule-2 sign-off was correctly
      requested rather than self-certified; the URL-PROVISIONAL statement
      names REQ-350 by id; the stage-10 bucket-C inventory was re-measured
      with real ls/wc -l output (2 modules/603 lines -> 4 modules/812
      lines, web/src/pages/exam/ total 936 lines with the API client).

      REVIEWER's own rule-2 adjudication (docs/migration/stage-10-bilimbaga-vertical.md,
      "REVIEWER sign-off" section, 2026-09-15 entry): bucket C confirmed for
      both new modules (ExamResultView.tsx, ExamSessionResultPage.tsx) --
      the honest bucket-B question REQ-351 posed has a real answer, but the
      component's state machine is keyed to ExamSubmissionOutcome's
      exam-specific shape and session-status vocabulary, which a generic
      entity-record detail view could not express. PASS on rule 2 for both
      (pure extraction reuse, no speculative plumbing). No supervision/OTP
      concern -- frontend-only change.

      The twelfth criterion -- a spec proving exam-result-page and
      exam-result-score render from a LOADED session for REQ-345's
      shorttext-free exam -- is genuinely unmet, not merely partially
      built: no such scenario exists anywhere in the diff, and FRONTEND-DEV
      correctly said so rather than fabricating a pass. Root cause,
      verified directly: Letflow.Exam.Session's private session_view/1
      (lib/letflow/exam/session.ex:1151-1160) never maps score_pct/passed
      into GET /exam-sessions/:id's response, even though
      outcome_from_session/1 (session.ex:1087-1098) proves both are already
      persisted on the record once submitted. REVIEWER judged that
      extending session_view/1 to carry those fields is NOT a minor,
      same-PR completion -- lib/letflow/routers/exam_sessions.ex's own
      moduledoc states verbatim that "a *result* view is a sixth behaviour
      ... not a subset of the *state* read this module does implement",
      and adding score/passed to the existing state-read route would
      deliver exactly that reserved behaviour through a side door. This is
      Option B: a real product/scope decision, not a quick ELIXIR-DEV
      fix-up. Filed ISS-0674 (Q-674, GH-1412) with the full fix-direction
      options rather than looping FRONTEND-DEV or ELIXIR-DEV against a gap
      neither can close within its own scope fence.

      PR #1411 merged as-is despite the open criterion -- the two shipped
      modules are real, regression-free, and close 1 of 9
      results-surface-blocked corpus blocks; there is no reason to hold
      working, correctly-scoped code hostage to a separate decision. Merge
      commit on main to follow this entry. GH#1403 left OPEN with a comment
      recording the partial-completion verdict and a pointer to ISS-0674
      (GH#1412); it is not closed because REQ-351's own acceptance criteria
      are not fully met. Requirement status set to `blocked` in
      docs/requirements.yaml (schema has no `partial` value; `blocked` is
      the closest honest read -- a genuine, stable stop not resolvable by
      more work inside this requirement, only by ISS-0674's own
      resolution). letflow-queue task 667 released status: blocked.
  - req: SCOPE-CHANGE
    event: done
    agent: ELIXIR-DEV
    at: "2026-09-14T23:30:49Z"
    note: >
      ISS-0674 (Q-674, GH-1412) resolved -- GET /exam-sessions/:id never
      returned score_pct/passed, blocking REQ-351's twelfth acceptance
      criterion. REVIEWER independently judged CODE-DESIGNER's design
      (lib/letflow/design/iss0674-session-view-score-fields.md) does NOT
      cross REQ-335's router scope fence: extending session_view/1 with
      score_pct/passed is a legitimate completion of GET
      /exam-sessions/:id's already-authorized read, not the reserved
      "sixth behaviour" result view.

      ELIXIR-DEV implemented: lib/letflow/exam/session.ex (session_view/1,
      @type session_view, new shared passed_for_status/2 helper also now
      used by outcome_from_session/1), lib/letflow/routers/exam_sessions.ex
      (session_view_json/1). One correction to the design doc applied
      during implementation: `passed` must be nil for :in_progress too
      (not just :grading_pending), since the DB persists `false` as a
      NOT-NULL placeholder at session creation rather than an absent
      value -- documented inline. Verified: `mix compile` clean, `mix test
      test/letflow/exam/session_test.exs
      test/letflow/routers/exam_sessions_test.exs --seed 0` = 52 passed, 0
      failed, run twice independently (ELIXIR-DEV and REVIEWER).

      SECURITY-REVIEWER spot-check: PASS -- no new Repo/query/join,
      ownership guard and INV-5 unchanged, no cross-candidate/tenant leak,
      no answer-key redaction regression, diff scope limited to expected
      files.

      FRONTEND-DEV wired web/src/pages/exam/ExamSessionResultPage.tsx's
      score_unavailable branch to render real scores via the existing
      ExamResultView/exam-result-score path, updated
      web/src/types/exam.ts's ExamSessionView type, updated the ISS-0674
      gap comment, and updated
      web/src/pages/exam/__tests__/ExamSessionPage.test.tsx's mock. Added
      a new e2e scenario to web/tests/e2e/exam-result-by-id.e2e.spec.ts
      covering the scoreable-exam by-id score render. Verified: `npm run
      type-check` clean, `npm run lint` clean (--max-warnings 0), `npm
      test` 82 files/568 tests passed, `npm run guards` 4 files/48 tests
      passed, and the Playwright e2e spec re-run against a real
      backend+Keycloak: 3 passed (8.0s) -- all three REQ-351 by-id
      scenarios (scoreable score render, pending, not-owner) now pass.

      Closed GH#1412 with an evidence comment (gh issue comment + gh issue
      close, real command output captured). letflow-queue task 674 was NOT
      released via the queue's release_lock endpoint by this run -- that
      call was not made; if task 674 is still held, a subsequent run must
      release it per docs/agents/protocols/TASK_QUEUE.md.
  - req: REQ-351
    event: done
    agent: REVIEWER
    at: "2026-09-14T23:30:49Z"
    note: >
      ISS-0674's resolution (same commit, entry immediately above) closes
      REQ-351's twelfth and final acceptance criterion -- the
      scoreable-exam exam-result-score scenario now renders a real score
      from a loaded by-id session. All 12 acceptance criteria independently
      re-verified met: the 11 already confirmed at the 2026-09-15 `blocked`
      entry above stand unchanged (re-run, still green), and the twelfth is
      now met per the ISS-0674 evidence chain (session_view/1 extended,
      52/52 backend tests passing, SECURITY-REVIEWER PASS, frontend wired
      and e2e-verified 3/3 passing against a real backend+Keycloak).

      Requirement status flipped from `blocked` to `done` in
      docs/requirements.yaml in the same run. GH#1403 closed with an
      evidence comment pointing at ISS-0674's resolution (gh issue comment
      + gh issue close, real command output captured). letflow-queue task
      667 was NOT released via the queue's release_lock endpoint by this
      run -- that call was not made; if task 667 is still held, a
      subsequent run must release it per
      docs/agents/protocols/TASK_QUEUE.md.
  - req: REQ-353
    event: done
    agent: DOC-UPDATER
    at: "2026-09-14T23:43:26Z"
    note: >
      Closed out REQ-353 (letflow-queue task 669, GH#1405). Confirmed
      docs/migration/decisions/0032-bucket-rule-2-scope-amendment.md
      carries a filled-in REVIEWER sign-off (PASS, 2026-09-15) rather
      than the CODE-DESIGNER-left PENDING placeholder: all 12 acceptance
      criteria independently re-verified by REVIEWER (0022 confirmed
      byte-identical via `git diff --name-only main --
      docs/migration/decisions/0022-bilimbaga-vertical.md`; all five
      clauses re-checked against source wording; class-wide sign-off
      decline confirmed verbatim; the stage file's dangling "hard
      constraint" back-references at lines 331/605 re-derived as
      absent and reported as ISS-0675 rather than fixed in-scope;
      SECURITY-REVIEWER skip justified). Flipped
      docs/requirements.yaml's REQ-353 status pending -> done. Did not
      re-review REVIEWER's verdict, per this run's own instruction that
      it is final. Closed GH#1405 with an evidence comment summarizing
      REVIEWER's verdict (decision 0032 filed amending 0022's bucket
      rule 2 scope, all 12 ACs met, 0022 left byte-identical, dangling
      stage-file references reported as ISS-0675 rather than fixed
      in-scope). letflow-queue task 669 released status: done under
      agent_id tvolodi-orch.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-14T23:46:24Z"
    note: >
      ISS-0675 (queue task 675, GH#1413) resolved -- docs/migration/stage-10-bilimbaga-vertical.md
      contained two back-references to "this file's own hard constraint"
      (lines 331, 605) that pointed at text never actually written anywhere
      in the file or docs/ tree, discovered by CODE-DESIGNER while authoring
      decision 0032. Re-derived the dangling state independently before
      touching anything: `grep -rn -i "hard constraint" docs/` returns only
      these two back-references, no statement of the constraint; the
      surrounding passages (lines ~320-332, ~595-606) contain no such rule
      under any name.

      Read decision 0030's actual "Consequences" section and REVIEWER's
      actual 2026-09-13 sign-off text before rewriting, so the replacement
      references are accurate rather than plausible-sounding. Rewrote both
      pointers to cite the real precedent: decision 0030's "Consequences"
      bullet ("`0022`'s bucket table is not edited by this record") and
      REVIEWER's 2026-09-13 sign-off on it ("a disagreement with standing
      text becomes a decision-record finding, not a quiet edit"). Kept both
      surrounding sentences' meaning intact -- only the dangling pointer's
      target changed, not the surrounding argument.

      docs/issues/ISS-0675.yaml flipped open -> resolved with a resolution
      note. Docs-only change (docs/migration/stage-10-bilimbaga-vertical.md,
      docs/issues/ISS-0675.yaml) -- no application code touched. Fetched
      origin/main first and confirmed no divergence/conflicts on the hot
      files (requirement_status.v17.yaml, requirements.yaml, the stage file
      itself) before pushing direct to main, matching this session's
      established precedent for pure docs fixes. Closed GH#1413 with an
      evidence comment quoting the before/after text at both locations.
      letflow-queue task 675 released status: done under agent_id
      tvolodi-orch.
  - req: REQ-354
    event: done
    agent: DOC-UPDATER
    at: "2026-09-14T23:56:15Z"
    note: >
      Closed out REQ-354 (letflow-queue task 670, GH#1406). Confirmed
      docs/migration/decisions/0033-pdf-qr-rendering-dependencies.md
      carries a filled-in REVIEWER sign-off (PASS, 2026-09-15) rather
      than a PENDING placeholder: PDF rendering choice `pdf` (option P1)
      over `chromic_pdf` (option P2) -- both real, named, licensed
      candidates evaluated with release date/maintenance/pure-Elixir-vs-
      native classification/transitive impact; QR encoding choice
      `eqrcode` (option Q1) over `qr_code` (option Q2) on the same basis.
      Both chosen libraries are pure-Elixir, MIT-licensed, zero
      transitive footprint. No-new-dependency alternatives (hand-rolled
      PDF bytes; shelling out to an external binary for either
      capability) evaluated and rejected on stated grounds, not omitted.
      Renderer runs in-process (no service_catalog entry needed) as a
      direct consequence of both libraries being pure Elixir with no
      external binary/NIF. Decision-0022 rule-1 vocabulary check re-run
      by REVIEWER and confirmed clean (one hit, the grep pattern's own
      literal text). Did not re-review REVIEWER's verdict, per this
      run's own instruction that it is final. Flipped
      docs/requirements.yaml's REQ-354 status pending -> done. Closed
      GH#1406 with an evidence comment summarizing the decision.
      letflow-queue task 670 released status: done under agent_id
      tvolodi-orch.
