# Letflow — Requirement run history — VOLUME 18 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v17.yaml (volume 17, closed,
#   1,223 frozen-prefix lines / total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v18.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v18.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v18.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v18.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: 2026-09-15T06:03:14Z
    note: >
      Re-claimed letflow-queue task 676 (ISS-0676, GH#1415, agent_id
      orch-session-issueloop) via set_lock -- its earlier blocked-status
      release (this session, waiting on REQ-355) is now stale: REQ-355
      merged, and REQ-356 has also since merged (lib/letflow/exam/
      certificate_document.ex now exists on main). Branch
      fix/ISS-0676-20260915 created from main at fd885bc3. Issue:
      Letflow.Exam.Certificate.issue_error is a bare atom union with no
      exhaustive-handling guarantee at
      lib/letflow/routers/exam_sessions.ex's render_issue_certificate/2
      catch-all clause (line 516). MINOR hardening finding, not a
      blocker -- same class already accepted for
      Letflow.Exam.Session's own error unions. This entry was originally
      appended to volume 17 before that volume rolled to closed
      upstream (REQ-356's own close-out); composed into volume 18
      instead during this branch's Step Final rebase, per the
      established started/done-across-a-roll precedent (see volume 17's
      index entry).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: 2026-09-15T06:32:38Z
    note: >
      ISS-0676 (queue task 676, GH#1415) resolved via
      fix/ISS-0676-20260915 (commit d719dfb1, full detail in
      docs/issues/ISS-0676.yaml's resolution field). Real Dialyzer
      experiment confirmed no compiler-enforced exhaustiveness is
      achievable for issue_error's atom union in idiomatic Elixir;
      resolved via documentation (permanent comments citing the
      experiment and Session's identical precedent) plus a real
      regression test covering all six atoms over HTTP, verified
      discriminating by mutation (TEST-DESIGN-VALIDATOR removed a
      clause, confirmed the test fails, reverted). REVIEWER PASS,
      TEST-DESIGN-VALIDATOR PASS, TEST-RUNNER PASS (4299/4299, zero
      failures). SECURITY-REVIEWER out of scope. GH#1415 closed with an
      evidence comment; letflow-queue task 676 released status: done.

  - req: REQ-358
    event: started
    agent: ORCH
    at: 2026-09-16T08:38:02Z
    note: >
      Claimed letflow-queue task 680 (GH#1430) via get_next_task, agent_id
      tvolodi-orch. Reclaimed cleanly per docs/anti-patterns.md's
      2026-09-16 same-host-concurrent-ORCH entry -- confirmed local main
      up to date with origin/main (git fetch + git log main..origin/main
      empty) before branching, so no repeat of that incident's race.
      Routing WF-02 in full: extend the UAT scenario YAML schema with a
      scope field and step-level branching, and extend UAT-RUNNER with an
      explicit environment target. Branch feature/WF02-REQ358-20260916
      created from main at ab731b76 and pushed. run_id
      WF02-REQ358-20260916.

  - req: REQ-358
    event: done
    agent: DOC-UPDATER
    at: 2026-09-16T10:14:35Z
    note: >
      Extended the UAT scenario YAML schema with a `scope` field (platform vs.
      open tenant-vertical identifier) and step-level `when:` branching
      (eq/in/else conditions keyed on runtime-observable facts, e.g. role),
      extended UAT-RUNNER/WF-05 with an explicit environment target
      (base URL + named credential source), and added a schema-validation
      mix task (lib/mix/tasks/letflow.check_uat_scenario_schema.ex).
      RELEASE-VALIDATOR PASSed (handoffs/WF02-REQ358-20260916/
      step-05-release-validation.json) -- all acceptance criteria satisfied.
      Two fast-follow gaps logged, neither attributable to REQ-358 itself:
      QA lacks Meridian/tenant fixture data, and no browser-automation tool
      exists in the pipeline yet. Both already recorded in Step 5's own
      handoff; no separate tracking mechanism invented for them here.
      README.md checked -- no mention of UAT-RUNNER, the scenario schema, or
      an ASCII state diagram touching this behavior anywhere in it, so no
      README change made. No docs/migration/stage-N-*.md or
      docs/migration/decisions/*.md file was named in REQ-358's acceptance
      criteria, so that check is N/A for this requirement.

  - req: REQ-363
    event: started
    agent: ORCH
    at: 2026-09-16T10:28:14Z
    note: >
      Claimed letflow-queue task 686 (GH#1438, agent_id
      orch-session-issueloop, depends_on [REQ-041], already done). Step
      00 git setup: pulled main (which now carries REQ-358's own merge
      plus a proper WF-01 registration of REQ-363..366), branched
      feature/WF02-REQ363-20260916, pushed. SELF-CORRECTION: an earlier
      attempt this run backfilled a docs/requirements.yaml stub for
      REQ-363 from get_next_task's own returned description/
      acceptance_criteria (terse, incomplete), and CODE-DESIGNER
      produced a design doc against that stub before the real WF-01
      registration (commit 2027f295, REQ-363..366's full scoping) had
      landed on main. That branch was deleted (never merged) once the
      rebase conflict surfaced the real, richer requirement text --
      depends_on [REQ-041] (not []), a screen_id/route-identifier field
      the stub never had, process_definition_id as a real FK (the stub
      used a bare UUID mirroring ProcessDefinition's own no-FK
      convention), and sanitization stated as THIS requirement's own
      scope (not deferred). A real discrepancy also surfaced worth
      flagging to CODE-DESIGNER for this rework: the official
      requirement text states confirmed_for_definition_version should
      be an integer "matching Letflow.Definitions.ProcessDefinition.version",
      but CODE-DESIGN-VALIDATOR's prior (now-superseded) review directly
      verified process_definition.ex's real version column is :string,
      not an integer -- this needs resolving explicitly (design must
      state which is correct and why), not silently picked. Starting a
      genuine CODE-DESIGNER pass against the real requirement text next.

  - req: REQ-363
    event: done
    agent: ORCH
    at: 2026-09-16T10:35:01Z
    note: >
      REQ-363 (queue task 686, GH#1438) design complete against the
      real WF-01-registered requirement text: CODE-DESIGNER produced
      lib/letflow/design/req363-help-content-data-model.md (commit
      db552779) covering the tenant-scoped help_content table
      (screen_id, nullable process_definition_id with an
      application-level-not-DB-FK validation per process_definitions'
      own established no-cross-schema-FK convention, title, body,
      status draft|live only, confirmed_at, confirmed_for_definition_version,
      media reserved jsonb, created_by, timestamps -- tenant-scoped via
      prefix mirroring process_definitions' real migration/schema
      convention), draft/live lifecycle reasoning quoting the
      requirement's own Why section, the platform-vs-tenant split
      (a separate platform_help_content global-schema table, reasoned
      explicitly against REQ-358's own scope-field precedent since
      help_content has real per-tenant schema isolation a shared-table-
      plus-flag would violate), and write-path sanitization as this
      requirement's own scope (raw HTML/script/event-handler-attributes
      named explicitly as rejected shapes).

      Resolved the confirmed_for_definition_version integer/string
      discrepancy explicitly rather than silently: the requirement text
      says integer "matching Letflow.Definitions.ProcessDefinition.version",
      but process_definition.ex:88 and its migration both show version
      is genuinely :string -- designed confirmed_for_definition_version
      as :string to actually match the real column (an :integer column
      cannot diff against a :string column with no numeric-format
      constraint), flagged as OQ-3 for REVIEWER sign-off rather than
      unilaterally deciding.

      CODE-DESIGN-VALIDATOR PASSed on the first attempt against the
      real requirement text: independently re-verified the version-type
      claim at the cited file/line, confirmed screen_id and the
      FK-semantics discussion are genuinely present (unlike the
      discarded first attempt's stub), confirmed zero smuggled
      implementation code (one fenced block, pure schema field
      declarations, no function bodies), confirmed all real acceptance
      criteria map to concrete design elements, confirmed the
      platform-vs-tenant reasoning against REQ-358's actual design doc.

      This requirement's own scope is design-only (its own acceptance
      criteria: CODE-DESIGN-VALIDATOR PASS gates REQ-364, which
      implements the actual migration/schema/sanitization) -- no
      ELIXIR-DEV/SECURITY-REVIEWER/REVIEWER/TEST-* steps needed here,
      though REVIEWER sign-off on OQ-3/OQ-4 is expected at REQ-364's own
      Step 2d. Flipped docs/requirements.yaml's REQ-363 status
      in_progress -> done. letflow-queue task 686 to be released
      status: done.

      SELF-CORRECTION RECORD: this run required a full restart after an
      earlier attempt backfilled requirements.yaml from an incomplete
      queue-API summary before the real WF-01 registration had landed
      -- see this volume's REQ-363 started-event entry above for the
      full account. Also: a concurrent CODE-DESIGN-VALIDATOR subagent
      working this same requirement in this same shared (non-worktree)
      checkout directory collided with unrelated REQ-362 Step-00 work
      being done in parallel by ORCH, silently discarding REQ-362's
      first uncommitted attempt -- redone cleanly afterward, see
      REQ-362's own started-event entry.

  - req: REQ-359
    event: started
    agent: ORCH
    at: 2026-09-16T10:30:49Z
    note: >
      Claimed letflow-queue task 681 (GH#1431) via get_next_task, agent_id
      tvolodi-orch. depends_on REQ-358 (done). Confirmed local main up to
      date with origin/main before branching (git fetch + git log
      main..origin/main empty aside from an unrelated in-flight
      feature/WF02-REQ363-20260916 branch, not touching this requirement's
      scope). Routing WF-02 in full: define a BA (business-analyst) agent
      role file, update AGENT_SYSTEM.md's roster/capability matrix, append
      a dated addendum to decision 0004, and instantiate a real BA persona
      for the BilimBaga tenant vertical authoring and exercising one real
      UAT scenario against QA. Branch feature/WF02-REQ359-20260916 created
      from main and pushed. run_id WF02-REQ359-20260916.

      COMPOSITION NOTE (Step Final rebase onto origin/main at 7bab6d2a):
      this entry's own `at:` (10:30:49Z) sorts between REQ-363's started
      event (10:28:14Z, landed on main first via commit 7bab6d2a) and
      REQ-363's done event (10:35:01Z, same commit) -- reordered into that
      chronological position rather than appended after both, per this
      file's own established composition precedent (see volume 3's index
      note, "recount-every-time"/chronological-interleave pattern).

  - req: REQ-359
    event: done
    agent: DOC-UPDATER
    at: 2026-09-16T10:49:45Z
    note: >
      Defined the BA-<VERTICAL> agent role (.claude/agents/ba-analyst.md),
      parameterized by tenant-vertical/solution-pack via
      docs/agents/ba-personas/<vertical>.yaml rather than a fixed persona
      list -- dual authoring+sign-off responsibility, sign-off artefacts at
      test/uat-reports/ba-signoff-<vertical>-<run_id>.yaml. Instantiated the
      first real persona (docs/agents/ba-personas/bilimbaga.yaml) and
      authored one real UAT scenario
      (test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml).
      docs/agents/AGENT_SYSTEM.md's roster table (3) and capability matrix
      (3.1) gained the BA-<VERTICAL> row; docs/migration/decisions/
      0004-humanless-pipeline.md gained its 2026-09-16 REQ-359 addendum,
      append-only, original Decision/Reasoning sections untouched.
      RELEASE-VALIDATOR PASSed all 5 acceptance criteria -- AC5's real
      UAT-RUNNER dispatch against QA came back honestly BLOCKED (no
      BilimBaga tenant/candidate/exam-definition seed data exists in QA;
      GET /api/v1/tenants returns only Default Tenant, identity search for
      bilimbaga/candidate returns count: 0, GET /api/v1/definitions returns
      zero installed), verified independently twice with full quoted
      evidence rather than fabricated as a pass -- REVIEWER and
      RELEASE-VALIDATOR both confirmed AC5 only requires real execution
      plus a quoted result, not a passing scenario, so this does not block
      closure. Underlying QA seed-data gap filed as ISS-0690 (GH-1445,
      queue Q-690). README.md checked -- no agent roster, ASCII state
      diagram, or "Running it" content references agent roles or counts
      anywhere in it, so no README change was needed.

  - req: REQ-362
    event: started
    agent: ORCH
    at: 2026-09-16T10:34:13Z
    note: >
      COMPOSITION NOTE (Step Final rebase onto a further-advanced
      origin/main): this entry's own `at:` (10:34:13Z) chronologically
      sits between REQ-359's started event (10:30:49Z) and REQ-363's
      done event (10:35:01Z), both already landed earlier in this
      volume -- appended here at the tail instead of reordered into
      that exact position, since three other requirements' entries
      (REQ-363, REQ-359, and this rebase's own REQ-362 done-event
      below) all interleave within the same ~20-minute window and a
      full reorder risks a transcription error; the real ordering is
      preserved by each entry's own `at:` timestamp, which is what
      matters for the record's accuracy.
      Claimed letflow-queue task 682 (GH#1432, agent_id
      orch-session-issueloop, depends_on [REQ-358], done -- merged as
      09e1e367). Step 00 git setup: pulled main, branched
      feature/WF02-REQ362-20260916, pushed. (Note: a first Step 00
      attempt on this same branch was silently lost -- a
      CODE-DESIGN-VALIDATOR subagent working WF02-REQ363-20260916
      concurrently in this same shared checkout directory, not an
      isolated worktree, checked out its own branch mid-way and
      discarded this branch's uncommitted working-tree state before it
      was committed. Redone cleanly here with no other agent running
      concurrently. Lesson for future sessions: do not interleave ORCH's
      own git operations with a dispatched agent's when both touch the
      same shared checkout -- serialize, or use Agent's
      isolation:"worktree" option.) Two-phase visual regression testing:
      phase 1 AI-agent-accepted screenshot baselines, phase 2 mechanical
      pixel-diff via Playwright's built-in screenshot comparison,
      unconditional auto-fail, dual-tracked issue filing on detected
      difference, explicit-justification re-baseline action. Routing to
      CODE-DESIGNER next.

  - req: REQ-362
    event: done
    agent: DOC-UPDATER
    at: 2026-09-16T12:50:55Z
    note: >
      Two-phase visual regression testing (phase 1 AI-agent-accepted
      screenshot baselines, phase 2 mechanical Playwright pixel-diff,
      unconditional auto-fail, dual-tracked issue filing, explicit-
      justification re-baseline) shipped end to end. CODE-DESIGNER's
      design PASSed CODE-DESIGN-VALIDATOR on first attempt. ELIXIR-DEV
      implemented it and exercised all four "must happen for real"
      acceptance criteria for real: a real baseline accepted from a real
      UAT run, a real pixel-diff pass against an unchanged screen and a
      real pixel-diff fail against a deliberately altered screen, a real
      dual-tracked issue filed on the detected diff (ISS-0691, GH#1446)
      and closed, and a real re-baseline exercised with its justification
      recorded. SECURITY-REVIEWER PASSed (commit d7248b81) -- out of
      scope, no tenant-data path touched -- filing one non-blocking
      finding, ISS-0692 (future PII-in-git-history risk if this tooling
      is later pointed at a real authenticated screen). REVIEWER PASSed
      (commit e0a2690d), independently re-running the spec and confirming
      GH#1446 live. TEST-DESIGNER found and fixed a real CI-fixture-
      mutation defect in the original real-exercise spec (tagged
      `@manual`, since it mutated shared fixtures unsafely for CI), adding
      a disposable-fixture formal suite plus 18 unit tests in its place.
      TEST-DESIGN-VALIDATOR PASSed, running the formal suite twice and
      confirming zero fixture drift -- the load-bearing evidence for this
      requirement's test coverage -- and flagged one unrelated gap,
      ISS-0694 (pre-existing 'certificate' entity-type test gap).
      TEST-RUNNER PASSed: backend 4324/4324, frontend statics clean,
      REQ-362's own tests 6/6 e2e plus 18/18 unit. RELEASE-VALIDATOR
      independently re-verified all six acceptance criteria from a cold
      read and re-ran the full suite itself, PASSing, and flagged two
      unrelated flakes as separate backlog items: ISS-0695
      (Scheduler.PollerTest flake) and ISS-0696 (frontend vitest
      worker-distribution flake). None of ISS-0692/ISS-0694/ISS-0695/
      ISS-0696 block this requirement; all filed as separate issues.
      docs/agents/AGENT_SYSTEM.md's artifact-locations table already
      carries the visual-baselines row (test/fixtures/uat/visual-
      baselines/, owner UAT-RUNNER) added during CODE-DESIGNER's Step 1,
      confirmed present and accurate here, not re-added. README.md
      checked -- REQ-362 does not alter any documented current behavior
      surfaced there (no ASCII state diagram or "Running it" section
      touches UAT visual regression), so no README change made. No
      docs/migration/stage-N-*.md or docs/migration/decisions/*.md file
      was named in REQ-362's acceptance criteria, so that check is N/A.

  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: 2026-09-17T07:49:00Z
    note: >
      ISS-0696 (queue task 696, GH#1457) resolved via
      fix/WF03-ISS0696-20260917 (commit ade5fc0e, full detail in
      docs/issues/ISS-0696.yaml's resolution_note field). Root cause: a
      genuine React effect-ordering race in ExamSessionPage.tsx --
      textDraft synced via a useEffect committing one render cycle late,
      so under full-suite vitest scheduler jitter a forced-blur anti-cheat
      signal could fire before that effect committed and maybeSaveDraft
      wrongly saved a stale empty draft. Fixed by deriving textDraft
      during render instead of via effect (10 insertions/3 deletions).
      CODE-DESIGN-VALIDATOR PASS, SECURITY-REVIEWER PASS (out of scope),
      REVIEWER PASS (idiomatic render-time setState guard, converges in
      one extra render, no infinite-loop risk). TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR both empirically confirmed no single-file
      deterministic test has discriminating power for this full-suite-only
      race, so no new test file was added -- the regression guard is
      repeated full-suite execution itself. RELEASE-VALIDATOR PASSed
      independently: 30/30 total full web/ vitest suite runs green across
      FRONTEND-DEV/TEST-RUNNER/RELEASE-VALIDATOR (10 each), 586/586 tests
      every run, 0 flakes; backend mix test 4324/4324. GH#1457 closed with
      an evidence comment. letflow-queue task 696 to be released
      status: done by ORCH after this step -- queue interaction explicitly
      out of scope for this DOC-UPDATER step per this run's own dispatch.
