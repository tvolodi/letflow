# Letflow — Requirement run history — VOLUME 2 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.yaml (volume 1, closed, frozen,
# 5,766 lines — targeted reads only, a whole-file read of it is refused).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v2.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v2.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v2.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v2.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: REQ-113
    event: started
    agent: ORCH
    at: 2026-08-21T05:52:25Z
    note: >
      Picking up queue task 188 (GH#376), named directly by the project owner. set_lock
      claimed it under agent_id ai-dala-orch (200, locked; was open/unlocked). REQ-113
      was already drafted and REQ-VALIDATOR-passed on origin/main via run
      WF01-TESTPARALLEL-20260821 (handoffs/WF01-TESTPARALLEL-20260821/) before this
      session started, along with its dependent follow-on REQ-114 (wiring the script
      into TEST-RUNNER/RELEASE-VALIDATOR, out of scope for this run). Routing through
      WF-02 (Requirement Implementation) per the project owner's explicit instruction to
      use the full producer/validator pipeline. Branch feature/WF02-REQ113-20260821
      created off origin/main and pushed (Step 00). Status flipped pending -> in_progress
      here.
  - req: REQ-113
    event: done
    agent: DOC-UPDATER
    at: 2026-08-21T07:29:31Z
    note: >
      Implemented scripts/test_parallel.sh, wrapping Elixir's native `mix test
      --partitions N` (N from an env override or `nproc`, never hardcoded): one
      `MIX_ENV=test mix compile` up front to avoid the concurrent-compile race, N
      background `MIX_TEST_PARTITION=<i> mix test --partitions N` processes against the
      shared Postgres instance, then a wait and an aggregation step that parses and sums
      each partition's own reported pass/fail/property counts rather than trusting exit
      status alone, exiting non-zero if any partition failed. Mid-run rework: the
      original parsing regex was written against an assumed ExUnit summary shape
      ("N tests, M failures") that does not match this toolchain's real
      `ExUnit.CLIFormatter` output -- actual lines are "Result: N[/M] passed (...)" plus
      a separate "Failed: N tests" line when failures exist. CODE-DESIGNER reworked the
      design (376bb81, df5b2a9) and ELIXIR-DEV reworked the script (dc93092, cf28a46) to
      match the real output shape; RELEASE-VALIDATOR's rerun
      (handoffs/WF02-REQ113-20260821/step-05-release-validator-rerun.json,
      test/reports/report-20260821-WF02-REQ113-20260821-rerun.yaml) then PASSed all six
      acceptance criteria at commit 6c55a33, including live verification of the
      aggregation logic against both an injected failure and real non-injected
      per-partition failures. Incidentally surfaced during that rerun and forwarded, not
      fixed here: at N=8 (this host's real nproc) the script's own per-partition
      Postgres pool (pool_size = schedulers_online()*2 = 16) times 8 partitions plus
      Mix's build-directory lock serializing partition startup triggers Postgres
      "too_many_connections" -- a real environment-capacity finding, not a REQ-113
      defect (no AC requires collision-freedom at arbitrary N, and TEST_PARALLEL_N
      already provides a safe override), filed as ISS-0194 (letflow-queue task 194,
      GH#382). README.md's "Running it" section updated to mention
      scripts/test_parallel.sh alongside the existing scripts/timed_test.sh entry; no
      docs/migration/stage-N-*.md or docs/migration/decisions/*.md file was named by
      this requirement's acceptance criteria, so step 4 of WF-02 Step 6 is N/A here.
  - req: REQ-113
    event: done
    agent: ORCH
    at: 2026-08-21T07:56:20Z
    note: >
      Step Final (git merge) resumed and completed after the prior session's BLOCKED
      log (2026-08-21T07:34:29Z, gh unauthenticated). gh CLI now authenticated
      (user tvolodi, repo+workflow scopes). Branch had drifted since the BLOCKED log
      (origin/main moved to b816a64), so re-fetched and re-rebased clean onto current
      main (0 conflicts, 16 commits), force-with-lease pushed (tip ee352a5).
      mix compile --warnings-as-errors: 0 warnings. mix test: 1274 passed, 0 failed.
      Opened PR #384, merged via `gh pr merge --squash --delete-branch`
      (main fast-forwarded b816a64 -> 8570dc7). Local cleanup verified: on main,
      log shows the squash commit, clean tree, single worktree, remote branch
      confirmed deleted. This is the event that makes REQ-113 actually present on
      main -- the earlier DOC-UPDATER `done` event above reflected only the feature
      branch. Queue task 188 released as done via letflow-queue, closing GH#376
      (confirmed via `gh issue view 376`: state CLOSED).
  - req: REQ-114
    event: started
    agent: ORCH
    at: 2026-08-21T08:00:20Z
    note: >
      Named directly by project owner (letflow-queue task 189, GH#377), locked under
      agent_id ai-dala-orch. Dependency REQ-113 confirmed done on main (8570dc7).
      Branch feature/WF02-REQ114-20260821 created and pushed from origin/main. Routing
      through full WF-02 producer/validator pipeline per instruction.
  - req: REQ-114
    event: done
    agent: DOC-UPDATER
    at: 2026-08-21T08:17:23Z
    note: >
      Replaced plain `mix test` full-suite invocations with REQ-113's
      scripts/test_parallel.sh across the 7 edit sites identified in scope:
      docs/agents/workflows/WF-02_requirement_implementation.md's Step 4
      (TEST-RUNNER) and Step 5 (RELEASE-VALIDATOR), WF-04_full_test_run.md's
      overview diagram and Step 1 (TEST-RUNNER), and
      .claude/agents/test-runner.md and .claude/agents/release-validator.md's
      own literal invocations found during the audit. WF-03_issue_resolving.md
      confirmed (not assumed) to need no edit of its own: its "Steps 2-4"
      section mirrors WF-02's Step 1/1b/2a-2d/3/3b/4 by reference rather than
      restating a `mix test` invocation. Process gap this run hit and fixed:
      RELEASE-VALIDATOR's Step 5 first pass correctly FAILed on missing
      handoff records and an unconfirmed docs/migration/decisions/0008 REVIEWER
      sign-off; both were backfilled and REVIEWER's confirmation properly
      recorded (decisions/0008's "REVIEWER sign-off — 2026-08-21" section)
      before Step 5 was re-run and PASSed
      (handoffs/WF02-REQ114-20260821/step-05-release-validator-rerun.json).
      REVIEWER's Step 2d confirmed both of decisions/0008's Option A picks:
      the stage tag question was out of scope for that gate (not evaluated),
      and DOC-UPDATER's remit widening to cover docs/agents/workflows/*.md
      edits was confirmed, not overridden. README.md's "Running it" section
      updated to remove the stale "not yet wired into TEST-RUNNER/
      RELEASE-VALIDATOR (REQ-114)" caveat now that the wiring is done. No
      docs/migration/stage-N-*.md file was named by this requirement's
      acceptance criteria. test/reports/ not applicable: this is a docs-only
      requirement and TEST-RUNNER/TEST-DESIGN-VALIDATOR were skipped by
      Step 3's scope test.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-08-21T09:03:51Z"
    note: >
      ISS-0119 / GH#373 resolved by run WF03-ISS0119-20260821, and this is the first
      entry in volume 2. The defect was that this history's own append-only safeguard
      had become unexecutable: docs/status/requirement_status.yaml crossed the Read
      tool's 262,144-byte limit on 2026-08-18, so for ~2.5 days every agent was told to
      "read the file in full before appending" while that read was refused, and each
      improvised a different partial read instead. The fix bounds the file rather than
      restating the rule. Volume 1 was CLOSED IN PLACE at 5,766 lines / 182 entries /
      361,376 bytes and digest-pinned by frozen_prefix_sha256 -- not split and not
      renamed, because ~103 historical run records under handoffs/ and test/reports/
      cite that path and iss-0078-pin-rebind-provenance.md:367 cites line 5158 by
      number, so freezing in place keeps every citation valid. Volume 2
      (docs/status/requirement_status.v2.yaml) was opened as the current volume, and
      docs/status/requirement_status.index.yaml was created as THE entry point: agents
      now resolve the current volume through the index instead of assuming a filename.
      A roll rule caps every current volume at 1,200 lines / 120,000 bytes so a full
      read stays possible by construction, which is the whole point -- a safeguard that
      requires seeing the schema cannot survive a file too large to show it. The
      append-only rule itself gained its FIRST mechanical enforcement:
      test/docs/requirement_status_invariants_test.exs, 13 tests, whose assertion A6
      fails if closed volume 1's frozen prefix changes by a single byte. Previously the
      rule was enforced only by agents' eyes, and had already been broken once by a
      from-scratch rewrite. Two further findings came out of the work rather than being
      designed in. Assertion A4 found THREE previously-unknown malformed entries in
      volume 1 on its first ever run -- lines 4803 (REQ-053) and 5050 (REQ-054) missing
      a note field entirely, and 4896 (REQ-056) writing "timestamp:" where "at:"
      belongs -- on disk undetected since 2026-08-19; they are declared under the
      index's new known_shape_anomalies key and LEFT AS WRITTEN, since correcting a past
      entry is exactly what this file forbids. And the new header now STATES the
      conventions that were previously discoverable only by surveying 182 entries: both
      legal req values (with "req: ISS-NNNN" called out as illegal and zero-precedent),
      all six event values (volume 1's header listed three while six were in use), and
      the five-field entry shape. Verification: full suite run on branch and main, two
      runs each -- the branch adds no failure main does not already have, and the 13 new
      invariant tests pass in full-suite context on both branch runs. Residual work is
      carried by ISS-0193, not lost: this run fixed the readability defect, not the
      downstream citations. Anti-patterns entries added by the run: "An instruction
      whose mechanism has silently become unexecutable" and "Presuming an in-progress
      artefact dead and writing that presumption into a handoff as fact".
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-08-21T09:52:36Z"
    note: >
      ISS-0201 / GH#392 resolved by run WF03-ISS0201-20260821: the three MINOR wording
      riders the REVIEWER gate raised against HANDOFF_PROTOCOL.md section 2 on the
      ISS-0198 run, all three APPLIED (none declined), all in that one file. (1) The
      "exactly two OPTIONAL fields" sentence is now scoped to the FIELDS of the schema
      block rather than the MEMBERS of an object inside it, pointing at section 4.1(b) as
      the authority on not_agent_attested's members and its optional seventh,
      backfill_note; backfill_note was deliberately NOT added to the fence, since the
      exception admitting it is spent and named one file, so putting it in the general
      schema block would invite a second file to acquire it -- the paragraph instead says
      that a linter deriving that member set from the fence alone and rejecting the one
      authorised file has mis-read the sentence. (2) The requirement_text/source_text seam
      is settled by FIELD, not by file: a value under a REQ-ID key is that requirement's
      description and nothing else, and any other text out of docs/requirements.yaml
      (acceptance_criteria, depends_on, owner, raw entry YAML) goes in source_text keyed
      "docs/requirements.yaml". (3) The rider that mattered -- a check-shaped clause after
      the inline-versus-artifacts_in table making it mandatory, not advisory, that
      dropping a restatement obliges adding the dropped source's path to artifacts_in,
      written as a per-source check a weak model executes ("for every source whose text I
      removed, shortened, or replaced with a mention -- including one now named only by an
      ID -- is that source's relative path present, verbatim, in context.artifacts_in?
      Add the path, or put the text back. There is no third option") and closing on the
      exact failure the ISS-0198 AC5 re-draft produced: count the sources, not the edits.
      The JSON schema fence was NOT changed, so no handoff file's validity moved; verified
      rather than asserted -- json.loads over the fence returns OK with 18 top-level keys,
      and a re-parse of every *.json under handoffs/ except registry.json gives 604
      parseable, 0 unparseable. core-directives.md and ORCHESTRATOR.md were left untouched
      (git status clean on both); their pre-existing pointer lines still carry neither the
      rule nor any threshold, which is the property ISS-0198 established and this run had
      to avoid re-widening. No length rule and no target length was added: section 2's
      justification rests on redundancy, and none of the three riders was a reason to
      reopen that.
  - req: REQ-067
    event: started
    agent: ORCH
    at: 2026-08-21T14:54:57Z
    note: >
      Picking up queue task 134 (GH#274) via get_next_task. Dependency REQ-066 confirmed
      done. Ports R-Co's src/api/pagination.zig (406 lines, API-06) as
      Letflow.Api.Pagination -- cursor encode/decode with prefix/expiry validation,
      page-size validation, and the PageResponse(T) envelope. R-Co's own
      validatePageSize/1 rejects (PageSizeTooLarge on 0 and >200), never clamps --
      confirmed by reading pagination.zig directly rather than assuming. Security-load-
      bearing: the decoded cursor carries no tenant/schema field by construction (INV-1),
      tenant scoping comes exclusively from REQ-072's resolved request context. Branch
      feature/WF02-REQ067-20260821 created off origin/main and pushed (Step 00). Status
      flipped pending -> in_progress here.
<<<<<<< HEAD
  - req: REQ-068
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-21T15:34:10Z"
    note: >
      Ported validation.zig/content_type.zig/validate.zig as Letflow.Api.Validation,
      Letflow.Plugs.ContentType, Letflow.Plugs.SafeJsonParser. Added a real :uuid type
      check and enum-membership constraint (R-Co's own pattern-match validator was a
      documented no-op, ported intent not the stub). 105 real tests, all passing;
      adversarial nesting-depth check empirically verified at ~990KB near the 1MiB body
      cap. No route wires these plugs in yet -- infrastructure for REQ-069 onward. Self-
      gated (single agent performing every WF-02 role) since this execution context
      cannot spawn subagents.
  - req: REQ-069
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-21T15:46:31Z"
    note: >
      Ported authorization.zig as Letflow.Api.Authorization (5 Roles, 14 Permissions,
      3-variant AccessDecisionKind, R-Co's exact names as atoms). AllowWithRowFilter's
      scope is structurally derived only from AccessContext.user_id -- evaluate_access/2
      has no parameter a caller-supplied filter could reach (INV-2). roles_from_strings/1
      safely converts the untrusted [String.t()] AuthPipeline actually populates
      conn.assigns[:auth_context][:roles] with (not R-Co's enum directly) into the closed
      role set, dropping unrecognized strings rather than crashing or defaulting to allow;
      avoids String.to_existing_atom/1 (unbounded-atom-creation vector). 24+1 tests
      covering all 9 acceptance criteria, all passing (153/153 across test/letflow/api/ +
      test/letflow/plugs/, nothing else broken); AC9 runs the real AuthPipeline plug via a
      unique-realm tenant (deliberately not the shared bpm-default seed, to avoid
      ISS-0108's live collision class) rather than a hand-built assigns map. Adversarial
      security self-review: case/whitespace role-string variants, 100k garbage strings,
      duplicate/order-independence, nil input -- all safe, no bypass found. Moduledoc
      states the 403-vs-404/INV-5 boundary and names AGENT_RUNNER/DlqOperate/
      WebhooksManage as ported-but-unreachable. Self-gated (single agent performing every
      WF-02 role) since this execution context cannot spawn subagents.
  - req: REQ-067
    event: done
    agent: DOC-UPDATER
    at: 2026-08-21T15:46:51Z
    note: >
      Shipped Letflow.Api.Pagination (lib/letflow/api/pagination.ex): cursor
      encode/decode with prefix and expiry validation, page-size validation
      (reject-not-clamp on 0/201/non-numeric, per R-Co's own validatePageSize/1),
      and the PageResponse(T) envelope every S4 list endpoint will delegate to.
      RELEASE-VALIDATOR (step-05) independently confirmed all six acceptance
      criteria directly against the shipped code and test/letflow/api/pagination_test.exs:
      constants (MAX_PAGE_SIZE 200/DEFAULT_PAGE_SIZE 50/MIN_PAGE_SIZE 1/expiry
      86_400_000_000us via named module attributes), reject-not-clamp on the
      0/201/non-numeric fence-posts, cross-endpoint prefix rejection, expiry
      rejection via a constructed old cursor, INV-1 tenant isolation (Cursor
      defstruct is exactly [:inner], no tenant field, plus a cross-tenant replay
      test), and pagination completeness across 3 pages of 25 fixture rows.
      RELEASE-VALIDATOR's overall verdict was PARTIAL, but that reflects only
      unrelated pre-existing full-suite noise -- 3 Letflow.TenantSchemaReaperTest
      failures independently re-derived as a sibling-partition interaction with
      the ISS-0110 guard, already filed as ISS-0217/GH#441, not caused by this
      branch and not touching this module (its own 38 tests: 0 failures). No
      docs/migration/stage-N-*.md or docs/migration/decisions/*.md file was named
      by REQ-067's acceptance criteria, so that step of WF-02 Step 6 is N/A here.
      README.md checked (grep for "pagination"): no hit, confirming no
      externally-visible HTTP surface yet (no route wired up until REQ-072-onward)
      -- no README update needed.
  - req: REQ-084
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-21T16:19:00Z"
    note: >
      OpenAPI spec strategy decision record (OQ-2) -- docs/migration/decisions/
      0010-openapi-spec-strategy.md decides DEFER to S6: no spec hand-built, no
      open_api_spex adopted, none served in S4. R-Co's src/api/openapi/ (7
      modules, 992 lines) plus routes/openapi.zig (158) reverified directly
      against the live tree, matching the requirement's own figures exactly.
      open_api_spex ruled out as downstream of decisions/0001-web-framework.md's
      REQ-065 addendum (Plug/Bandit stands): the library's Phoenix-macro-driven
      value proposition doesn't transfer to a Plug/Bandit router, so adopting it
      would carry a Phoenix-shaped dependency for a fraction of its value.
      Reasoning: no consumer exists yet (web/ grepped directly, S8 owns
      integration), a hand-built spec not derived from the real route table
      would drift the way R-Co's own path_registry.zig exists to prevent, and no
      Ecto-to-JSON-Schema equivalent to schema_registry.zig exists in lib/ today
      (checked directly). Ownership handed to S6 by name, no requirement ID
      invented, matching REQ-068's identical treatment of its own deferred
      scope. Self-gated REVIEWER sign-off recorded in
      docs/migration/stage-4-api-surface.md (no separate role split available in
      this execution context, stated plainly there); git diff --stat confirmed
      docs-only.
