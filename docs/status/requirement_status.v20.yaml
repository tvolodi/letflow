# Letflow — Requirement run history — VOLUME 20 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v19.yaml (volume 19, closed,
#   1,212 frozen-prefix lines / total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v20.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v20.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v20.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v20.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-18T21:26:59Z"
    note: >
      WF-03 run WF03-ISS0716-20260918 (queue task Q-716, GH-1534) complete,
      RELEASE-VALIDATOR PASS ("Clearing ISS-0716 for DOC-UPDATER and
      merge."). ISS-0716 (flaky Letflow.TenantProvisioning.
      ColumnPromotionTest AC1) is now resolved: root cause was a known
      ISS-0580-family residual hazard -- SandboxAutoMode
      (test/support/sandbox_auto_mode.ex) sets the DB sandbox to pool-wide
      :auto mode for the duration of tenant provisioning, so a
      concurrently-scheduled sibling test's writes commit for real during
      that window, legitimately widening :all-scoped
      register_column_promotion/4's resolved tenant-id set beyond this
      test's own tenant_a/tenant_b -- production-correct behavior on
      list_registrations/0's unscoped Repo.all, not a defect. Fix is
      test-only, per design
      lib/letflow/design/iss0716-column-promotion-ac1-all-scope-flake.md:
      commit 6a849e17 (ELIXIR-DEV) replaced AC1's exact-set-equality
      assertion with a containment check (tenant_a/tenant_b present,
      extras tolerated) and scoped the downstream DDL-execution loop to
      only tenant_a/tenant_b's own rows with a length==2 sanity check.
      No lib/letflow/ file touched. REVIEWER confirmed the SECURITY-
      REVIEWER skip legitimate, matching real ISS-0580 precedent.
      TEST-DESIGNER mutation-tested both new assertions, no coverage gap
      found. TEST-RUNNER (commit 0d520324) ran 5/5 solo repeats plus 3
      independent full concurrent-suite runs (2x scripts/test_parallel.sh,
      1x internal to mix letflow.check.test), 4436/4436 each;
      RELEASE-VALIDATOR's own independent recheck added a 4th full
      concurrent-suite run, also 4436/4436 -- 4 independent
      full-concurrent-suite runs at 4436/4436 total, no recurrence. Stated
      with RELEASE-VALIDATOR's own calibration: this closes the known
      mechanism, verified empirically, not a permanent-elimination
      guarantee -- SandboxAutoMode's pool-wide :auto-mode window remains a
      standing, documented hazard for any other test relying on exact-set
      assertions over unscoped queries.

      This entry was appended to volume 20 rather than volume 19 because
      this branch's first merge attempt against origin/main (bb2d1b9f)
      independently rolled volume 19 to volume 20 itself, but a sibling
      session (commit 0925093c, PR #1544) rolled it AGAIN with different
      content (ISS-0690 re-verification entries) before this branch's PR
      could merge -- a genuine race between two independent, concurrent
      rolls. Rather than hand-reconciling two divergent "closed volume 19"
      states, this branch was rebuilt from a fresh origin/main checkout
      (cherry-picking only its real content commits: issue-record filing,
      design, fix, test report) and this status append was redone fresh
      against the volume the index actually names as current at rebuild
      time (volume 20, opened by the sibling session's own roll).

      docs/issues/ISS-0716.yaml status flipped open -> resolved with a
      resolution_note recording all of the above and citing the three real
      commits (design doc, 6a849e17, 0d520324). No docs/requirements.yaml
      entry to flip (this is an ISS, not a REQ). No docs/migration/
      stage-N-*.md or decisions/*.md file was named by ISS-0716's
      affected_files -- none required updating. README.md checked
      (grepped ColumnPromotionTest/SandboxAutoMode/tenant-count -- no
      hits): this is internal test-suite behavior, not documented current
      behavior, so no README update needed. No merge and no
      letflow-queue/GitHub state change performed here -- ORCH's job next.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-18T22:41:55Z"
    note: >
      [Field corrected 2026-09-19 by a later ORCH session: this entry's req:
      field originally read "ISS-0718" -- an illegal vocabulary value per
      this file's own header (legal req: values are REQ-NNN | SCOPE-CHANGE
      only; an issue fix uses SCOPE-CHANGE with the ISS id named in the note
      body). Same recurring mistake as ISS-0712's earlier instance (see that
      correction's own note in the prior volume for the full reasoning).
      Corrected in place per RequirementStatusInvariantsTest's own A5
      assertion text: a vocabulary defect in the CURRENT (not yet closed)
      volume is fixed in the working tree, never declared away. Only the
      single-value req: field-selector is corrected; the rest of this entry
      (timestamp, agent, full note body below) is untouched.]
      WF-03 run WF03-ISS0718-20260919 (queue task Q-718, GH-1541) complete
      and merged to main as commit 3b548591 (PR #1546, squash-merged, both
      CI gates green: backend mix letflow.check 10m49s pass, frontend
      npm run check pass). ISS-0718 (CANDIDATE role gets a real 403 on the
      exam list screen -- ExamListPage.tsx's POST /entities/query call
      requires :EntitiesQuery, outside CANDIDATE's ISS-0646 closed
      six-permission set) is now resolved. CODE-DESIGNER chose option (b)
      from the issue's own text: a new dedicated route
      GET /exam-sessions/available (lib/letflow/routers/exam_sessions.ex),
      gated by the EXISTING :ExamSessionStart permission CANDIDATE already
      holds -- zero changes to role_allows?(:CANDIDATE, ...) or the
      ISS-0646 closed-set invariant test in
      test/letflow/api/authorization_test.exs. CODE-DESIGN-VALIDATOR FAILed
      once (design doc contained literal compilable Elixir code instead of
      prose/pseudocode per this project's convention) then PASSed after
      rework. Letflow.Exam.Session.list_available_exams/2
      (lib/letflow/exam/session.ex) hardcodes entity_type "exam" + status
      "active" server-side, reusing Compiler/Allowlist/Cursor internally
      without the generic route's FieldGrants redaction step (confirmed no
      entity_field_restrictions row exists for "exam" today). Frontend
      (web/src/api/exam.ts, web/src/pages/exam/ExamListPage.tsx) switched
      to the new route, dropped the dead queryExamRecords call.
      SECURITY-REVIEWER PASS (tenant isolation via prefix threaded through
      Compiler/Allowlist/Cursor, INV-1/4/7/8 satisfied, permission-reuse
      equivalence-of-effect verified -- CANDIDATE could already probe
      individual exam status one at a time via the pre-existing
      POST /exam-sessions route under the same permission). REVIEWER PASS
      (idiom matches existing router conventions, no scope creep, decision
      0013's addendum untouched, REQ-366's :HelpRead precedent not
      reopened). TEST-DESIGNER wrote 5 new tests in
      test/letflow/routers/exam_sessions_test.exs (commit 354a980a) and
      applied 3 mutants (drop status filter, swap policy key to
      :EntitiesQuery, swap entity_type to "question"), all caught, all
      reverted and verified clean. TEST-DESIGN-VALIDATOR independently
      applied its own mutant (status filter drop, 2/5 tests failed as
      expected) per WF-03's "code did not exist" rule, confirmed revert
      clean, PASS. TEST-RUNNER: full mix test suite 4452/4452 passed (16
      partitions), frontend npm run guards 48/48 passed including a real
      production build -- no regressions. ISSUE-FIXER closed
      docs/issues/ISS-0718.yaml (status: resolved,
      resolved_in_run: WF03-ISS0718-20260919) and GitHub issue #1541 with
      a comment citing the design doc, implementation commits, and
      regression tests -- confirmed closed via gh issue view.

      This run's own ELIXIR-DEV, SECURITY-REVIEWER and TEST-DESIGNER
      dispatches each went quiet for an extended period without
      committing progress mid-step; ORCH verified their in-progress
      working-tree state directly (uncommitted diffs matching their later
      self-reports), independently re-ran the relevant tests itself where
      warranted, and in ELIXIR-DEV's and TEST-DESIGNER's cases pushed
      already-completed local commits the agent had made but not yet
      pushed, rather than blocking further on an unresponsive dispatch --
      each agent's own later report, once it did land, confirmed the
      independently-verified state matched exactly. ORCH also
      independently performed a SECURITY-REVIEWER PASS itself, later
      corroborated in full by the original SECURITY-REVIEWER dispatch's
      own delayed report reaching the same conclusion by an independent
      path (entity_field_restrictions grep, INV-1 trace, equivalence
      argument). One cosmetic typo (exam_sessions_router.ex ->
      exam_sessions.ex) in the ISSUE-FIXER close-out's resolution text was
      corrected directly by ORCH as a trivial one-line doc fix.
      Letflow-queue task 718 released with status: done (GitHub issue
      close-sync verified via gh issue view, already closed by
      ISSUE-FIXER's own step). No docs/requirements.yaml entry to flip
      (this is an ISS, not a REQ). No docs/migration/stage-N-*.md or
      decisions/*.md file required updating -- ISS-0718's fix deliberately
      avoided touching decision 0013's addendum, consistent with the
      design's own stated goal.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-19T00:00:00Z"
    note: >
      ISS-0717 (QA redeploy silently no-ops backend code changes --
      docker-compose.qa.yml/redeploy-qa.sh image-tag mismatch, queue task
      Q-717, GH-1540) closed for real. This entry's own record explicitly
      documented its 2026-09-18 session's own fix as an ad hoc, non-durable
      workaround (a manual `docker build -f deploy/Dockerfile -t
      letflow-qa-app:latest .` rebuild) and deferred the durable fix to a
      companion ai-dala-infra task, T-0134. Confirmed T-0134
      (ai-dala-infra/tasks/T-0134-fix-letflow-qa-redeploy-image-tag-mismatch.md)
      is now status: done, outcome: succeeded, read in full before closing
      this issue rather than trusting the status label alone. T-0134 added
      an explicit `image: letflow-qa:latest` field to
      deploy/docker-compose.qa.yml's `app` service -- the exact tag
      redeploy-qa.sh already builds -- and, as part of that fix, committed
      the compose file into this repo (tvolodi/letflow) for the first time
      (it had existed only as a host-local, never-git-tracked file since
      T-0125's 2026-09-15 provisioning): commit 69c430ad. Independently
      spot-checked rather than trusting T-0134's own account alone: read
      69c430ad's diff directly (31 lines added, first-ever commit of this
      file) and confirmed deploy/docker-compose.qa.yml's `app` service on
      this repo's `main` reads `image: letflow-qa:latest` exactly as
      claimed. T-0134 verified the fix through a real script-driven
      redeploy (not another manual rebuild): a trivial, real, observable
      change (a comment in lib/letflow/router.ex, this repo's commit
      a3b8244f) was deployed via the actual redeploy-qa.sh, and the running
      container's image SHA was confirmed to match the freshly built image
      -- both commits independently visible in this repo's own git log.
      T-0134 also checked sibling compose files for the same defect
      (docker-compose.test.yml already had a correct `image:` field, no
      copy-paste pattern found) and root-caused the staleness window as
      "since T-0125 (2026-09-15) through T-0134's fix (2026-09-19)" --
      docker-compose.qa.yml was never tracked in any repo's git history
      before 69c430ad, so no earlier drift window exists to bound.
      docs/issues/ISS-0717.yaml updated to status: resolved with a full
      resolution block citing T-0134 and this evidence, via branch
      fix/ISS-0717-20260919. GH#1540 closed with an evidence comment
      citing the same.
      This was a docs-only close-out on the letflow side -- no lib/ code
      change was needed or made, since the actual fix lives entirely in
      ai-dala-infra's deploy/docker-compose.qa.yml (now also tracked here)
      and its deploy procedure -- still routed through a full
      branch+PR+CI+merge per GIT_MERGE.md's no-direct-push rule rather than
      a direct push to main. No docs/requirements.yaml entry to flip (this
      is an ISS, not a REQ). Letflow-queue task 717 released with status:
      done -- genuinely resolved, not merely a run that finished cleanly.
  - req: REQ-369
    event: blocked
    agent: ORCH
    at: "2026-09-19T02:49:52Z"
    note: >
      Queue task 708 (GH#1501) re-verification of the last remaining gap,
      AC6/AC7. This session's environment (Windows,
      c:\Users\tvolo\dev\ai-dala\ai-dala-infra) genuinely has QA credential
      access (SSH to ubuntu-16gb-nbg1-1 confirmed, scripts/qa-login.sh
      returned a real PLATFORM_ADMIN credential, independently confirmed
      valid via a direct Keycloak password-grant) -- resolving the
      credential-access gap the prior close_out_note left open for this kind
      of environment. UAT-RUNNER used it for a real end-to-end OIDC-redirect
      login attempt against live QA and a real re-run of the named
      pipeline_test (platform-login-routing-by-role.pipeline.e2e.spec.ts),
      both of which surfaced a new, genuine, live BLOCKER defect instead of
      passing: the QA backend's GET /api/tenant-config advertises
      oidc_authority=http://localhost:8082/realms/bpm-default instead of the
      real QA Keycloak, breaking real login for every QA user (not only
      PLATFORM_ADMIN). Filed as ISS-0719 (docs/issues/ISS-0719.yaml,
      queue_ref Q-719, github_ref GH-1550, severity BLOCKER), root-caused to
      lib/letflow/routers/tenant_config.ex's idp_base_url/0 default fallback,
      likely a side effect of the recent ISS-0717/T-0134 QA redeploy work.
      Full evidence: test/uat-reports/uat-2026-09-19-WF02-REQ369-AC6AC7-final2-20260919.yaml.
      REQ-369's own application code (PlatformDashboardPage, OidcCallbackPage
      redirect, ISS-0711's tenant-count fix) is independently confirmed
      unaffected and correct. docs/requirements.yaml's REQ-369 close_out_note
      updated (append-only) with the full account and remaining-work pointer.
      status remains blocked -- pending ISS-0719's resolution, then one more
      UAT-RUNNER re-verification pass. Queue task 708 released with status:
      blocked (not done, not a credential-gap hand-back this time -- a real
      external blocker with its own tracked issue).
  - req: REQ-369
    event: done
    agent: DOC-UPDATER
    at: "2026-09-19T05:39:57Z"
    note: >
      Queue task 708 (GH#1501) close-out, third and final AC6/AC7
      re-verification. UAT-RUNNER's final3 report
      (test/uat-reports/uat-2026-09-19-WF02-REQ369-AC6AC7-final3-20260919.yaml)
      confirms AC6 PASS (real PLATFORM_ADMIN login through Keycloak's real
      hosted login page against live QA, lands on /platform-dashboard, real
      tenant count 2 rendered, screenshot at
      web/tests/screenshots/pipelines/platform-login-routing-by-role-admin.png)
      and AC7 PASS (named pipeline_test
      web/tests/e2e/pipelines/platform-login-routing-by-role.pipeline.e2e.spec.ts
      PLATFORM_ADMIN case, real run against QA, "1 passed"). ISS-0719 (the
      BLOCKER that stalled the prior attempt -- live QA backend advertising
      oidc_authority=http://localhost:8082/realms/bpm-default) is now resolved
      (docs/issues/ISS-0719.yaml, resolved_in_run:
      WF02-REQ369-AC6AC7-final3-20260919) via two independent, both-confirmed-live
      fixes: ai-dala-infra's T-0137 infra-side mitigation (BPM_IDP_BASE_URL set
      directly in QA's .env, still in place) and the code fix (ISS-0720, commit
      fc55e700), now confirmed actually deployed and live on QA (CD run for
      commit 507bdb77, which includes fc55e700, succeeded; GET /api/tenant-config
      independently re-curled by ORCH and confirmed correct, cache-control
      ruling out stale cache). AC1-AC5 already complete (merged PR #1507).
      AC8 re-checked directly: docs/issues/ISS-0700.yaml is actually
      status: resolved (not open, correcting the stale premise carried in
      prior notes) -- resolved separately by ISSUE-FIXER
      (issue/ISS-0700-20260918-closeout), citing REQ-369's own merged PR #1507
      as the shipped fix; REQ-369's own close-out did not itself close
      ISS-0700, so AC8's actual constraint (REQ-369's close-out is not what
      closes it) is satisfied. docs/requirements.yaml's REQ-369 status flipped
      blocked -> done and close_out_note appended (append-only) with the full
      account above. GitHub issue GH-1550 (ISS-0719) closed with a comment
      citing both fixes and the UAT report path.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-19T07:21:51Z"
    note: >
      WF-03 run WF03-ISS0722-20260919 (queue task Q-721, GH-1555) complete,
      commit 41018e39, PR #1564. ISS-0722 (Letflow.Exam.Session.
      fetch_question_pools/2 only handled exam_question_rule mode "random";
      mode "manual" silently fell through to zero materialized questions
      with no error -- discovered live against qa.bizdala.com while seeding
      BilimBaga's exam_manual_question-pinned exam) is now resolved per
      design doc lib/letflow/design/iss0722-exam-manual-question-mode.md:
      fetch_manual_questions/2 added, a real clause reading the rule's
      pinned exam_manual_question rows; mixed-mode block ordering is
      random-first-then-manual; manual-origin questions are carved out of
      the shuffle step applied to random-origin ones; an empty manual rule
      now returns {:error, :manual_rule_empty} instead of a silent empty
      session. Full gate history, all PASS: CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR (after two rework
      cycles), TEST-RUNNER (4459/4459), RELEASE-VALIDATOR.
      docs/issues/ISS-0722.yaml flipped open -> resolved with a full
      resolution block citing all of the above. Honest gap stated there and
      here: coverage is unit/integration only (45 tests, including a real
      deterministic ordering-discrimination proof) -- the live BilimBaga
      scenario that discovered this bug was worked around at discovery time
      (swapped to mode "random") rather than exercised against this fix, and
      has not been re-run end-to-end against it. Recommending a UAT-RUNNER
      follow-up re-run of
      test/fixtures/uat/scenarios/bilimbaga/candidate-timed-exam-autograde.yaml
      (or a manual-mode variant) against live QA once this merges and
      deploys, named here as a pending item rather than implied as already
      proven. No docs/requirements.yaml entry to flip (this is an ISS, not a
      REQ). README.md checked (grepped exam_question_rule/manual
      question/exam mode -- no hits): not documented current behavior there,
      so no README update needed. No docs/migration/stage-N-*.md or
      decisions/*.md file was named by ISS-0722's affected_files -- none
      required updating. No merge performed here -- ORCH's job next, after
      confirming this push's own CI is green.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-19T21:15:34Z"
    note: >
      WF-03 run WF03-ISS0726-20260919 (queue task Q-726, GH-1577) complete,
      commit 073a17c9, PR #1581. ISS-0726 (hard navigation to /exam bounces
      back to / instead of showing the exam list -- the realistic candidate
      deep-link/bookmark/refresh path, discovered during the
      bilimbaga-candidate-timed-exam-autograde GUI review re-run) is now
      resolved per design doc
      lib/letflow/design/iss-0726-oidc-redirect-path-restore.md. Root cause
      (pre-confirmed by ISSUE-FIXER, independently re-verified against main
      at e0b3fdc7 while designing): ProtectedRoute's OIDC redirect never
      captured the originally-requested path anywhere that survives the
      full-page round trip to Keycloak and back, so OidcCallbackPage always
      fell through to its role-based fallback. Fix: a new
      web/src/auth/safeRestorePath.ts validator (isSafeRestorePath, a 9-check
      open-redirect guard -- type/length, leading-slash-required, rejects
      protocol-relative "//", rejects backslash, rejects an embedded URL
      scheme anywhere in the string, restricts path/query/fragment to safe
      RFC 3986-derived character classes), adversarially tested by
      SECURITY-REVIEWER with real bypass payloads, all correctly rejected;
      path capture via oidc-client-ts's own `state` signin option (not a
      separate sessionStorage key) in oidcRedirectArgs.ts's
      buildRedirectArgs(), validated again at capture time as defense in
      depth, with the two near-duplicate buildRedirectArgs() implementations
      (oidcRedirectArgs.ts, AuthProvider.tsx) deduped to one shared
      implementation; and restore-priority logic in OidcCallbackPage.tsx
      where a validated captured path now wins over the role-based fallback
      whenever present, including for PLATFORM_ADMIN.

      Full gate history, all PASS: CODE-DESIGN-VALIDATOR; SECURITY-REVIEWER
      (adversarial, real open-redirect bypass payloads against
      safeRestorePath.ts, all rejected); REVIEWER; TEST-DESIGN-VALIDATOR
      after 3 rounds -- round 1 found a live e2e realm-seeding gap and it
      was fixed (commit 38c65475, "seed realm slug before ISS-0726 e2e
      deep-link navigation"), round 2 found a coverage-honesty gap about the
      untested hostname-fallback path and it was fixed with an explicit gap
      note rather than left silently implied as covered (commit 0d041ac8,
      "name ISS-0726 hostname-fallback e2e gap explicitly"), round 3 found a
      broken doc citation to ISS-0727 and it was fixed by filing that
      missing record (commit 4d7464b4, "file local record for ISS-0727");
      TEST-RUNNER (682/682 vitest, 48/48 guards, e2e 2/3 clean with 1 timing
      flake); RELEASE-VALIDATOR (independently reproduced the e2e flake 3x,
      visually confirmed via screenshot/DOM that even the "failing" run had
      already correctly landed on /exam -- a test-timing race in the e2e
      harness, not a redirect defect).

      docs/issues/ISS-0726.yaml flipped open -> resolved with a full
      resolution block citing all of the above. Named, not silently
      covered, remaining gaps: (a) the live qa.bizdala.com-specific
      deployment has not been re-checked against this fix (the design's own
      follow-up), (b) the true zero-prior-context/hostname-resolved
      candidate path from the original bug report is not exercised by any
      automated test here -- it depends on realm resolution succeeding
      first, which is ISS-0727's separate, infra-blocked territory, not this
      PR's job. Recommending a UAT-RUNNER follow-up once this deploys:
      verify the hard-navigation-to-/exam fix against live qa.bizdala.com,
      keeping in mind ISS-0727 may still block the underlying realm
      resolution for bilimbaga specifically until that is separately fixed.
      No docs/requirements.yaml entry to flip (this is an ISS, not a REQ;
      grepped ISS-0726, no hits). README.md checked (grepped
      redirect/oidc/exam -- only pre-existing, unrelated hits at lines 128
      and 230) -- not documented current behavior there, so no README
      update needed. No docs/migration/stage-N-*.md or decisions/*.md file
      was named by ISS-0726's affected_files -- none required updating. No
      merge performed here -- ORCH's job next, after confirming this push's
      own CI is green and no other agent is currently active.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-19T22:05:00Z"
    note: >
      ISS-0727 (qa.bizdala.com tenant-config-by-host resolves bilimbaga
      scenarios to bpm-default realm, not bilimbaga -- ai-dala-infra
      QA-host-config territory, docs/issues/ISS-0727.yaml, queue_ref Q-727,
      github_ref GH-1578) resolved. Briefly, accidentally claimed this queue
      task via a duplicate get_next_task call earlier this session, released
      it back to open (correctly judged out of scope for a web/-only
      session), but discovered the local docs/issues/ISS-0727.yaml record
      was never filed -- fixed via a direct commit (PR #1582) once TEST-
      DESIGN-VALIDATOR found ISS-0726's own PR citing it as a broken
      reference. Resolved for real via PR #1583: documented in
      .claude/agents/uat-runner.md's "Environment target" section that every
      WF-05 dispatch of a bilimbaga scenario against qa.bizdala.com must set
      base_url to include ?realm=bilimbaga (the issue's own second,
      repo-actionable resolution option). Independently re-verified LIVE
      against qa.bizdala.com per the issue's own AC2 (not assumed) --
      dispatched UAT-RUNNER, which confirmed real network access and GET
      /api/tenant-config?realm=bilimbaga genuinely resolves oidc_authority
      to realms/bilimbaga (distinct from both ?realm=bpm-default and the
      no-param host-default path, both of which still return bpm-default --
      real discriminating controls, not a coincidence), and confirmed the
      bilimbaga realm's own OIDC discovery document is live. Full evidence:
      test/uat-reports/uat-2026-09-19-ISS0727-REALM-REVERIFY-20260919.yaml.
      Honestly named as NOT covered: a full Playwright candidate-login
      walkthrough (no credential_source supplied to this HTTP-level check),
      and the underlying host-to-realm mapping itself (resolution option 1)
      remains unfixed -- the query-param path is the sanctioned workaround,
      not a claim host-based resolution now works on its own. Queue task 727
      released with status: done.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-19T22:39:24Z"
    note: >
      WF-03 run WF03-ISS0728-20260919 (queue task Q-725, GH-1576) complete,
      commit e0285886, PR #1587. ISS-0728 (CANDIDATE role has no in-app
      navigation path to the exam list -- discovered by UAT-RUNNER during
      the bilimbaga-candidate-timed-exam-autograde GUI review re-run:
      AppShell.tsx's Role type excluded CANDIDATE entirely, no NAV_ITEMS
      entry gated for it, and after real sign-in a candidate landed on a
      generic "You do not have access to this area" PermissionDenied screen
      with only a /tasks link, never mentioning exams) is now resolved per
      design doc lib/letflow/design/iss0728-candidate-exam-nav.md. Fix:
      AppShell.tsx's Role type widened to include CANDIDATE; a new
      NAV_ITEMS entry ({ to: '/exam', label: 'Exams', roles: ['CANDIDATE'] })
      added right after "My Tasks"; PermissionDenied.tsx made role-aware via
      useAuth(), rendering "Go to Exams" -> /exam for a CANDIDATE session
      while every other role (and a null session) keeps the existing
      "My Tasks" -> /tasks fallback unchanged.

      Full gate history, all PASS: CODE-DESIGN-VALIDATOR, with independent
      confirmation this is out of SECURITY-REVIEWER's scope (client-side
      nav visibility only; CANDIDATE's real server-side authorization in
      lib/letflow/api/authorization.ex untouched and already correctly
      scoped); REVIEWER; TEST-DESIGN-VALIDATOR, with independently
      reproduced mutation testing over the new coverage; TEST-RUNNER
      (691/691 Vitest across 99 files, including
      AppShell.candidate-nav.test.tsx's 6 cases and PermissionDenied's
      CANDIDATE-specific 3 cases, plus 48/48 guard tests including a real
      production build); RELEASE-VALIDATOR, with full independent
      re-verification of the acceptance criteria.

      docs/issues/ISS-0728.yaml flipped open -> resolved with a full
      resolution block citing all of the above. Named, not silently
      covered, remaining gap: a live check that a real candidate-user on
      qa.bizdala.com actually sees and can click the new nav entry
      end-to-end remains unconfirmed -- inherent to jsdom/vitest coverage,
      explicitly named by the design doc itself as a recommended
      UAT-RUNNER follow-up, same shape as ISS-0726's own precedent. No
      docs/requirements.yaml entry to flip (this is an ISS, not a REQ;
      grepped ISS-0728, no hits). README.md checked (grepped ISS-0728 and
      CANDIDATE, no hits) -- not documented current behavior there, so no
      README update needed. No docs/migration/stage-N-*.md or
      decisions/*.md file was named by ISS-0728's affected_files -- none
      required updating. No merge performed here -- ORCH's job next, after
      confirming this push's own CI is green and no other agent is
      currently active.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-20T00:00:00Z"
    note: >
      Applied the "review real screens before writing a blind Playwright
      spec" process to
      test/fixtures/uat/scenarios/platform/definition-promotion-rollback.yaml
      (sibling of definition-promotion-approved.yaml and
      definition-promotion-conflict-rejected.yaml, reviewed the same day --
      see test/uat-reports/gui-review-2026-09-20-*.md). Checked whether
      rollback/withdrawal functionality exists by reading current source
      directly, not assuming from the scenario's own NOTE (ISS-0527).
      Confirmed the backend is fully built and wired:
      Letflow.Definitions.rollback_definition_version/4 (REQ-038) and
      POST /api/v1/definitions/:process_key/rollback
      (lib/letflow/routers/definitions.ex handle_rollback/2, REQ-077 R9) both
      exist, and the event-append leg is real --
      Letflow.EventStore.PlatformEvents.append_definition_version_rolled_back/2
      is genuinely called, so a real DEFINITION_VERSION_ROLLED_BACK audit
      event is written today. But web/src has ZERO rollback support: grepped
      "rollback" case-insensitively across web/src (no hits), and read
      web/src/pages/definitions/DefinitionListPage.tsx's version-history row
      in full -- its only row actions are Activate/Archive, no
      rollback/withdraw button, no reason input, no call to the rollback
      endpoint, and no audit-trail/change-history screen anywhere in web/src
      that could satisfy EO-005. This is a genuine, verified frontend gap,
      not a stale scenario note -- the scenario's own NOTE (ISS-0527) is
      correct that this is BLOCKED/UNBUILT_FEATURE on the frontend leg (it
      was actually confirmed independently here, not merely re-cited).
      Filed docs/requirements.yaml REQ-371 (owner FRONTEND-DEV, stage S8,
      status: pending) sized to build the operator-facing rollback/withdraw
      screen, error-state rendering for the four already-wired backend error
      shapes, restored-version confirmation on a new case, and a
      change-history/audit view, plus authoring and passing the named
      pipeline_test spec once the screen exists. No implementation attempted
      here per this run's own explicit instruction (file the gap, do not
      build the frontend feature in this dispatch). UAT scenario run recorded
      BLOCKED in
      test/uat-reports/gui-review-2026-09-20-definition-promotion-rollback.md.
      No docs/requirements.yaml entry needed flipping (REQ-371 is newly
      filed, not an existing one being closed). No docs/migration/
      stage-N-*.md or decisions/*.md file needed updating -- this is a net-
      new frontend gap finding, not a change to already-documented behavior.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-20T01:02:22Z"
    note: >
      Applied the "review real screens before writing a blind Playwright
      spec" process (7th of ~15 scenarios in today's sweep) to
      test/fixtures/uat/scenarios/platform/migration-partial-failure-resume.yaml
      -- see test/uat-reports/gui-review-2026-09-20-migration-partial-failure-resume.md
      for the full trace. Checked whether a platform-wide tenant-migration
      fanout runner (per-company apply, failure isolation, resume-outstanding,
      idempotent re-run) and an operator-facing rollout-status screen exist by
      reading current source directly, not by trusting the scenario's own
      pre-existing NOTE (ISS-0527) unread. Confirmed neither exists, backend
      or frontend: grepped lib/letflow/ for rollout/fanout/tenant_migration/
      PlatformRollout (no functional hits -- the closest analogues,
      Letflow.TenantProvisioning REQ-022 and ColumnPromotion/
      ConstraintActivation REQ-297/REQ-298, each operate on ONE tenant, not a
      platform-wide fanout), and grepped web/src for
      rollout/fanout/migration-status/RolloutStatus/RolloutConsole (no hits).
      req022-tenant-schema-provisioning.md's own design notes name this exact
      gap explicitly, citing R-Co's src/platform/migration_fanout.zig, and
      state a future requirement was meant to own it -- never filed until
      now. Grepped docs/requirements.yaml for the same terms to confirm no
      duplicate is already queued -- none found.

      Filed two requirements, split backend/frontend per this sweep's
      REQ-372/REQ-373 precedent: docs/requirements.yaml REQ-374 (owner
      ELIXIR-DEV, stage S6, status: pending, depends_on REQ-022/REQ-297/
      REQ-298) -- the fanout runner itself: per-company transactional apply,
      per-company outcome record (timestamp + plain-language failure reason),
      resume-outstanding-only, and idempotent no-op re-run, with an explicit
      scope fence against inventing a general "kinds of platform-wide change"
      registry; and REQ-375 (owner FRONTEND-DEV, stage S8, status: pending,
      depends_on: [REQ-374]) -- the operator-facing rollout-status screen,
      including authoring and passing this scenario's own named pipeline_test
      once the screen exists, so this scenario does not stay perpetually
      blocked one requirement later. No implementation attempted here per
      this run's own explicit instruction (file the gap, do not build either
      feature in this dispatch). UAT scenario run recorded BLOCKED in the
      report cited above. The scenario fixture's own pipeline_test NOTE block
      was extended (not removed -- the feature is not real) with a dated
      addendum citing REQ-374/REQ-375 and this report, matching the
      definition-promotion-rollback precedent earlier in this sweep. No
      existing docs/requirements.yaml entry needed flipping (REQ-374/REQ-375
      are newly filed, not existing ones being closed). No docs/migration/
      stage-N-*.md or decisions/*.md file needed updating -- this is a net-new
      gap finding, not a change to already-documented behavior. Verified
      mix letflow.check_req_id_collision (OK, no collision) and
      mix letflow.check_requirements_registration (342 entries = 340
      registered + 2 deferred + 0 neither + 0 unclassified, no new gate
      failures) both pass with the two new entries in place.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-20T01:20:44Z"
    note: >
      Applied the "review real screens before writing a blind Playwright
      spec" process (8th of ~15 scenarios in today's sweep) to
      test/fixtures/uat/scenarios/platform/partition-retention-drop.yaml --
      see test/uat-reports/gui-review-2026-09-20-partition-retention-drop.md
      for the full trace. Checked whether a single-step, partition-based
      event-history retirement mechanism (DETACH/DROP PARTITION rather than
      row-by-row DELETE), protected-record exemption, and an operator-facing
      retirement screen exist by reading current source and every relevant
      decision/design/issue record directly, not by trusting the scenario's
      own pre-existing NOTE (ISS-0527) unread.

      Confirmed the feature does not exist, and that its absence is a
      deliberate, already-recorded architectural deferral rather than an
      oversight: priv/repo/migrations confirms events/events_archive are
      plain unpartitioned tables (no PARTITION BY RANGE anywhere in the
      migration set); lib/letflow/event_store.ex's archive/1 moves expired
      rows one at a time (insert-then-delete against a computed event_id
      set), not via any whole-partition DDL operation.
      docs/migration/decisions/0003-ecto-schema-strategy.md Decision C point
      2 explicitly defers partitioning, and docs/issues/ISS-0014.yaml
      (resolved 2026-08-17) already weighed and rejected porting R-Co's
      whole-partition PartitionRetention model at that time specifically
      because it would need REVIEWER sign-off / a new decision record --
      recorded as a future item, not a closed door.
      lib/letflow/design/req188-recurring-timers-and-retention.md restates
      the same deferral for the scheduler side. Grepped web/src for any
      retention/retirement operator screen -- no hits. Grepped
      docs/requirements.yaml for the same terms to confirm no duplicate is
      already queued -- none found.

      Filed two requirements, split backend/frontend per this sweep's
      established precedent: docs/requirements.yaml REQ-376 (owner
      ELIXIR-DEV, stage S2, status: pending, depends_on: [REQ-023, REQ-026])
      -- resolving the deferred partitioning question via a new/amending
      decision record with REVIEWER sign-off, the partitioned migration(s)
      plus ahead-of-need next-partition creation, the single-DDL-operation
      retirement function itself (EO-001), protected-record exemption
      (EO-002), and post-retirement instance-replay proof (EO-003), with an
      explicit scope fence against inventing a general "partition any table"
      framework and an explicit instruction to state the relationship to
      the existing row-level archive/1 path rather than leaving both
      mechanisms silently coexisting; and REQ-377 (owner FRONTEND-DEV, stage
      S8, status: pending, depends_on: [REQ-376]) -- the operator-facing
      retirement screen, including authoring and passing this scenario's own
      named pipeline_test once the screen exists. No implementation
      attempted here -- this is architecturally significant (a Postgres
      native partitioning decision affecting every tenant schema), not a
      same-dispatch fix. UAT scenario run recorded BLOCKED in the report
      cited above; no real retirement operation was driven against
      qa.bizdala.com since the underlying mechanism does not exist to drive.
      The scenario fixture's own pipeline_test NOTE block was extended (not
      removed -- the feature is not real) with a dated addendum citing
      REQ-376/REQ-377 and this report, matching this sweep's established
      precedent. No existing docs/requirements.yaml entry needed flipping
      (REQ-376/REQ-377 are newly filed, not existing ones being closed). No
      docs/migration/stage-N-*.md file needed updating -- this is a net-new
      gap finding referencing an existing decision record (0003), not a
      change to already-documented behavior; 0003 itself is left unedited
      since REQ-376's own acceptance criteria require its resolving decision
      record to cross-reference 0003 rather than rewrite it. Verified
      mix letflow.check_req_id_collision (OK, no collision) and
      mix letflow.check_requirements_registration (344 entries = 342
      registered + 2 deferred + 0 neither + 0 unclassified, no new gate
      failures) both pass with the two new entries in place.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-20T02:10:24Z"
    note: >
      Applied the "review real screens before writing a blind Playwright
      spec" process (10th of ~15 scenarios in today's sweep) to
      test/fixtures/uat/scenarios/platform/template-update-conflict-resolution.yaml
      -- see
      test/uat-reports/gui-review-2026-09-20-template-update-conflict-resolution.md
      for the full trace. Checked whether solution-pack update conflict
      review/resolution (four-way diff, blocked-apply-on-unresolved-conflict,
      attributed keep/take resolutions, no re-flagging on a later update) is
      built by reading current source directly rather than trusting the
      scenario's own pre-existing NOTE (ISS-0527) unread. Found it is
      genuinely unbuilt end-to-end, in a way more layered than the note
      states: REQ-041 (done) built only the schema (solution_pack_installs /
      solution_pack_artefact_bases / pack_update_resolutions) and the pure
      three-way diff classifier (compute_pack_update_plan/5) -- its own
      design doc explicitly scoped out the install-tracking write path and
      the update-review/apply API/UI as future work. Grepping lib/ confirmed
      no caller ever inserts a solution_pack_installs or
      solution_pack_artefact_bases row, so both tables are permanently empty
      today; web/src/ has no pack-update-review screen (the one plausible
      grep hit, ConflictResolver.tsx, is confirmed unrelated -- a generic
      409 optimistic-concurrency modal for single-record edits). Filed
      docs/requirements.yaml REQ-379 (owner ELIXIR-DEV, stage S2, status:
      pending, depends_on: [REQ-041]) -- the install-time write path that
      populates solution_pack_installs/solution_pack_artefact_bases, a
      prerequisite REQ-041's own design doc named but left to a then-unscoped
      SOL-01/02/03; REQ-380 (owner ELIXIR-DEV, stage S2, status: pending,
      depends_on: [REQ-379]) -- the update-review and apply API (four-way
      classification endpoint, blocked-apply naming the unresolved artefact,
      attributed keep_local/take_incoming resolution persistence, and
      baseline-shift so a resolved artefact is not re-flagged on a later
      update); and REQ-381 (owner FRONTEND-DEV, stage S8, status: pending,
      depends_on: [REQ-380]) -- the review screen itself, including authoring
      and passing this scenario's own named pipeline_test
      (web/tests/e2e/pipelines/template-update-conflict.pipeline.e2e.spec.ts)
      once a real screen exists to write it against. No implementation
      attempted here -- this is a three-layer, cross-stage gap (backend
      install-tracking, backend API, frontend screen), not a same-dispatch
      fix. UAT scenario run recorded BLOCKED in the report cited above; no
      real update-review flow was driven against qa.bizdala.com since there
      is no real screen or endpoint to drive. The scenario fixture's own
      pipeline_test NOTE block was extended (not removed -- the feature is
      not real) with a dated addendum citing REQ-379/REQ-380/REQ-381 and
      this report, matching this sweep's established precedent (e.g.
      partition-retention-drop). No existing docs/requirements.yaml entry
      needed flipping (REQ-379/380/381 are newly filed, not existing ones
      being closed). No docs/migration/stage-N-*.md file needed updating --
      this is a net-new gap finding, not a change to already-documented
      behavior. Verified mix letflow.check_req_id_collision (OK, no
      collision), mix letflow.check_requirements_registration (348 entries =
      346 registered + 2 deferred + 0 neither + 0 unclassified, no new gate
      failures), and mix letflow.check_issue_refs (pre-existing unrelated
      violation on docs/issues/ISS-0728-exam-title-object-object-sibling-wip-rescued.yaml
      from a concurrent sibling session's WIP, not introduced by this run --
      no issue file touched by this dispatch) all checked with real output
      quoted.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-20T02:40:21Z"
    note: >
      WF-03 run WF03-ISS0729-20260920 (queue task Q-730, GH-1598) complete,
      commit 6ffa9967, PR #1601, branch feature/WF03-ISS0729-20260920.
      ISS-0729 (DefinitionListPage main-thread freeze in dev/StrictMode
      when interacting with debounced search shortly after navigation,
      causing an intermittent Playwright hang, discovered by ISSUE-FIXER
      while root-causing REQ-371's required e2e spec) is now resolved per
      design doc
      lib/letflow/design/iss-0729-definitionlistpage-search-freeze-fix.md.
      Fix: the search input's setSearch state update is now deferred
      through a coalescing setTimeout(fn, 0), the same empirical pattern
      web/src/utils/deferClickState.ts already established for ISS-0662's
      analogous EntityCrudPage/DataTable sort-header freeze. Applying that
      deferral naively introduced a secondary regression (dropped
      characters under fast typing, since the input briefly became an
      effectively-uncontrolled field with no single source of truth) --
      caught by FRONTEND-DEV's own empirical testing before it shipped,
      not left latent -- and fixed by switching the search <input> to an
      uncontrolled field via `defaultValue`, so the DOM itself holds
      keystroke state while the deferred setTimeout(fn, 0) only propagates
      the value into useDefinitionSearch's debounced state. A second,
      independent, correctness-only bug found during the same
      investigation was also fixed: web/src/utils/highlightText.tsx's
      global-flagged RegExp was being reused statefully (lastIndex) across
      repeated .test() calls inside .map(), producing incorrect
      alternating match results.

      Full gate history, all PASS, each with its own independently
      obtained empirical evidence given how intermittent this bug is:
      CODE-DESIGN-VALIDATOR; REVIEWER, with its own independent
      before/after proof (reverted to pre-fix code, reproduced 5/5 hangs;
      restored the fix, obtained 8/8 clean passes); TEST-DESIGN-VALIDATOR;
      TEST-RUNNER (10 fresh e2e runs against a freshly started dev server,
      0/10 hangs); RELEASE-VALIDATOR (its own fresh 9-run confirmation
      against a dev server it started itself, 0/9 hangs). FRONTEND-DEV's
      own investigation reached 100% baseline reproduction of the freeze
      on pre-fix code (after first catching and correcting a false
      negative caused by a CORS test-config issue in its own harness) and
      confirmed the fix at 0/20+ hangs across 55+ total runs. Across all
      four independent evidence sources (FRONTEND-DEV, REVIEWER,
      TEST-RUNNER, RELEASE-VALIDATOR), no hang recurrence was observed
      post-fix.

      docs/issues/ISS-0729.yaml flipped open -> resolved with a full
      resolution block citing all of the above. No live-QA-deployment-
      specific gap remains: this is a pure client-side rendering-timing
      defect (StrictMode double-invocation interacting with a debounced
      input's state update) with no environment-conditional behavior, per
      RELEASE-VALIDATOR's own reasoning during its PASS. No
      docs/requirements.yaml entry to flip (this is an ISS, not a REQ;
      grepped ISS-0729, no hits). README.md checked (grepped
      highlightText/DefinitionListPage/deferClickState, no hits) -- not
      documented current behavior there, so no README update needed. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by
      ISS-0729's affected_files -- none required updating. No merge
      performed here -- ORCH's job next, after confirming this push's own
      CI is green and no other agent is currently active.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-20T02:35:00Z"
    note: >
      Applied the "review real screens before writing a blind Playwright
      spec" process (11th of ~15 scenarios in today's sweep) to
      test/fixtures/uat/scenarios/platform/tenant-branding-applied.yaml --
      see test/uat-reports/gui-review-2026-09-20-tenant-branding-applied.md
      for the full trace. Checked whether tenant branding/theming (a company
      appearance settings screen, contrast validation, status-colour
      immutability, out-of-scope-change activity-log recording) exists by
      reading current source directly, not by trusting the scenario's own
      pre-existing NOTE (ISS-0527) unread.

      Found this scenario is genuinely a hybrid, unlike this sweep's other
      fully-unbuilt findings: REQ-280/281/282/283 (all done) built a real
      STORE and READ side -- Letflow.Identity.Tenant's closed-vocabulary
      settings changeset, GET /api/tenant-config, and
      web/src/theming/BrandingProvider.tsx's apply-on-load wiring all work.
      Two structural guarantees hold today without needing a live UAT run to
      confirm, matching this sweep's renderer-permission-denied-surface
      precedent for source-determined findings: EO-002 (status-colour
      immutability -- BRAND_COLOR_CSS_PROPERTY's allowlist has exactly one
      key, "primary", mapped only to --color-brand-600; StatusBadge.tsx reads
      separate --color-success/--color-warning/--color-error/--color-failure
      tokens no tenant setting can reach) and EO-004 (irreversible-action
      confirmation -- web/src/components/instances/CancelInstanceDialog.tsx
      is a real focus-trapped, aria-modal dialog with explicit "cannot be
      undone" text and a danger-styled confirm button, nothing fires until
      clicked). A third, EO-005 (first-paint colour, no flash), is
      confirmed BROKEN by reading BrandingProvider.tsx (applies colour in a
      useEffect, which runs after first paint) and main.tsx (renders
      synchronously without awaiting the pre-warmed fetchTenantConfig
      promise) -- a real, deterministic flash on every load. Left undocketed
      as a fix per the scenario's own on_fail metadata (severity: MINOR,
      suggested_action: none) -- recorded, not routed to WF-03.

      But the scenario's core action (steps 1/4/5: a tenant admin opens
      appearance settings and saves a colour change) has NO real backing:
      lib/letflow/design/req281-tenant-config-branding-key.md section 6
      states outright that Letflow.Identity.update_tenant_settings/2 having
      "no HTTP-reachable write path" is fine BY DESIGN for that requirement's
      own scope; grepping every router under lib/letflow/routers/ confirms
      zero PATCH/PUT/POST route ever calls it or writes tenants.settings; and
      web/src/pages/admin/tenants/EditTenantPage.tsx (the only per-tenant
      admin edit screen) has no colour/branding field at all. EO-003
      (contrast refusal) has no code at any layer either --
      Tenant.settings_changeset/2's validate_brand_colors/2 checks hex
      FORMAT only, never readability/contrast. EO-001's negative half
      (rejected out-of-scope key recorded in the operator activity log) has
      nothing to record from, since no write path exists to reject anything
      from -- though Letflow.Audit (lib/letflow/audit.ex) and
      web/src/pages/admin/AuditLogPage.tsx both already exist and are
      reusable once a write path exists.

      Filed two requirements, split backend/frontend per this sweep's
      established precedent: docs/requirements.yaml REQ-382 (owner
      ELIXIR-DEV, stage S8, status: pending, depends_on: [REQ-280, REQ-281])
      -- the HTTP-reachable settings-write endpoint, a new WCAG-AA
      colour-contrast check in validate_brand_colors/2 (EO-003), and
      rejected-out-of-scope-key Letflow.Audit recording reusing the existing
      mechanism (EO-001's negative half), explicitly fenced against widening
      the brand_colors allowlist itself or building a general settings-PATCH
      endpoint; and REQ-383 (owner FRONTEND-DEV, stage S8, status: pending,
      depends_on: [REQ-382]) -- the appearance settings screen itself,
      including authoring and passing this scenario's own named
      pipeline_test once the screen exists, with an explicit EO-005 note
      that the new pipeline_test documents the known flash rather than
      fixing it. No implementation attempted here -- this is a two-layer,
      cross-stage gap (backend write path, frontend screen), not a
      same-dispatch fix; no live browser session was opened against
      qa.bizdala.com since the scenario's own primary action has nothing to
      drive yet, matching the renderer-permission-denied-surface precedent
      for this same "core action has no real backing" shape. UAT scenario
      run recorded BLOCKED (with structurally-confirmed EO-002/EO-004 and a
      documented, non-blocking EO-005 defect) in the report cited above. The
      scenario fixture's own pipeline_test NOTE block was extended (not
      removed -- the feature is not real) with a dated addendum citing
      REQ-382/REQ-383 and this report, matching this sweep's established
      precedent. No existing docs/requirements.yaml entry needed flipping
      (REQ-382/383 are newly filed, not existing ones being closed). No
      docs/migration/stage-N-*.md file needed updating -- this is a net-new
      gap finding, not a change to already-documented behavior. Verified
      mix letflow.check_req_id_collision (OK, no collision) and
      mix letflow.check_requirements_registration (350 entries = 348
      registered + 2 deferred + 0 neither + 0 unclassified, no new gate
      failures) both pass with the two new entries in place.
      mix letflow.check_issue_refs shows one pre-existing violation on
      docs/issues/ISS-0728-exam-title-object-object-sibling-wip-rescued.yaml
      from a concurrent sibling session's WIP, not introduced by this run --
      no issue file touched by this dispatch.
