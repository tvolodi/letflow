# Letflow — Requirement run history — VOLUME 21 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v20.yaml (volume 20, closed,
#   1,227 frozen-prefix lines / 1,262 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v21.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v21.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v21.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v21.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: REQ-371
    event: done
    agent: DOC-UPDATER
    at: "2026-09-20T11:45:52Z"
    note: >
      Operator-facing rollback/withdrawal screen for released process
      definitions (web/src) shipped on feature/WF02-REQ371-20260919 (PR
      #1593), this run's longest-running work item -- six fix cycles before
      going green: ISS-0729, a rollback-spec row-locator scoping bug,
      ISS-0737, ISS-0738, ISS-0741, ISS-0743, plus two final in-branch fixes
      (a Playwright sandbox `.fill()`/`.pressSequentially()` hang workaround
      in the rollback e2e spec, and a pre-existing InstanceDetailPage.tsx bug
      reading nonexistent instance fields on the 'Definition' row). Passed
      two independent REVIEWER gates (idiom/scope, all 7 acceptance criteria
      individually confirmed satisfied each time) and RELEASE-VALIDATOR's
      final independent re-verification: 4469/4469 backend tests,
      726/726 frontend tests, 4x e2e pipeline runs (3 clean, 1 unrelated
      cold-start flake), all 7 ACs re-derived and confirmed, clean trivial
      merge-check against origin/main.
  - req: REQ-378
    event: started
    agent: ORCH
    at: "2026-09-20T19:09:00Z"
    note: >
      ISS-0736/REQ-378 (OIDC session role/status revocation has no live
      enforcement effect) picked up via WF-03 on feature/WF03-ISS0736-20260921,
      queue task 738, mandatory SECURITY-REVIEWER gate.
  - req: REQ-378
    event: done
    agent: ORCH
    at: "2026-09-20T22:51:25Z"
    note: >
      Tenant-admin role/status revocation now has live per-request
      enforcement for OIDC sessions: AuthPipeline.authenticate_oidc/2 gained
      a live Letflow.Identity.list_effective_role_names/2 check plus a
      users.status inactive short-circuit, replacing exclusive reliance on
      JWT claims. A one-time role_claims_synced_at marker seeds
      group_members from JWT claims at first login only (never re-fires,
      so revocation can't be silently resurrected by a token refresh).
      GroupsPage.tsx/api/identity.ts's broken member-removal wiring
      (wrong endpoint, discarded user_id) fixed as part of AC2's GUI path.
      Went through 3 rounds of design rework (GUI-path verification gap,
      JIT-provisioning seeding gap, pre-existing-test-contract gap) and 2
      rounds of implementation rework (an identity_migration.ex copy_users/2
      column-scope regression) before all gates passed. SECURITY-REVIEWER
      PASSED twice (original mechanism + delta), REVIEWER PASSED twice.
      Full suite green: 4472/4472 backend, 728/728 frontend. Design:
      lib/letflow/design/req378-oidc-live-revocation-check.md. Regression
      tests: test/letflow/plugs/iss0736_oidc_live_revocation_test.exs,
      web/src/api/__tests__/identity.removeMembers.test.ts. ISS-0736
      resolved, GH-1648 closed. Two follow-up issues filed: ISS-0764 (MINOR,
      tenant_status.ex raw-role-check hygiene) and ISS-0765 (MAJOR,
      groupsApi sibling-endpoint broken-prefix bug). Merged via PR (Step
      Final) on feature/WF03-ISS0736-20260921.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T04:30:37Z"
    note: >
      ISS-0769 (bearer-token leakage into crash logs) resolved on
      feature/WF03-ISS0769-20260921 (PR #1668). Fixed
      Letflow.Secrets.Redaction.redact_map/1's list handling to also redact
      a sensitive-keyed {key, value} 2-tuple (closing the gap where
      Bandit.Pipeline.run/5's own crash-logging catch clause put a raw conn
      -- including conn.req_headers's Authorization bearer token -- into
      Logger.error/2 metadata on any unrescued 500). Along the way found and
      fixed a more severe, previously-unknown second bug in the same
      function: redact_map/1 raised Protocol.UndefinedError on any struct
      value (e.g. %Plug.Conn{} itself), and because this module runs as a
      :logger primary filter, that uncaught exception permanently removed
      the filter node-wide ({removed_failing_filter,
      letflow_secrets_redaction}), silently disabling redaction for every
      subsequent log event until restart -- fixed by normalizing structs to
      plain maps (Map.from_struct/1) before the recursive walk.
      Letflow.Obs.Logger.redact_sensitive/1 now delegates to
      Letflow.Secrets.Redaction.redact_map/1 instead of a separate
      hand-copied sensitive-key list. Design:
      lib/letflow/design/iss0769-crash-log-header-redaction.md. Fail-then-pass
      independently reproduced by ELIXIR-DEV, SECURITY-REVIEWER, and
      RELEASE-VALIDATOR alike (both the raw-token leak and the node-wide
      filter crash). Gates: SECURITY-REVIEWER PASSED, REVIEWER PASSED,
      TEST-RUNNER PASSED (4498/4498 full suite), RELEASE-VALIDATOR PASSED
      (independent re-run, clean auto-merge check against unmerged PR
      #1669). Non-blocking follow-up ISS-0770 (secret_key_base gap) filed
      from this investigation.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T05:07:24Z"
    note: >
      ISS-0770 resolved (secret_key_base missing from
      Letflow.Secrets.Redaction's @sensitive_exact_keys list, found by
      SECURITY-REVIEWER while independently re-verifying ISS-0769 on
      PR #1668) via PR #1669 on feature/WF03-ISS0770-20260921: added
      secret_key_base to @sensitive_exact_keys plus a regression test.
      REVIEWER, TEST-RUNNER (4132/4132 broader run), and
      RELEASE-VALIDATOR (independent re-verification: 8/8 targeted,
      472/474 mix letflow.check.test, 2 failures confirmed unrelated
      pre-existing flake ISS-0766) all PASSED independently. queue_ref
      was null (filed directly, not via register_task), so no queue
      task to release.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T05:58:34Z"
    note: >
      ISS-0768 (Letflow.Identity.RoleRegistry never threaded tenant-schema
      prefix -- POST /roles always 500s, relation groups/tenant_role does
      not exist) resolved on feature/WF03-ISS0768-20260921. RoleRegistry's
      list_roles/upsert_role/do_upsert_role/insert_or_update_role now thread
      opts :: [prefix: String.t()] through every Repo call, matching the
      established Keyword.fetch!(opts, :prefix)-bound-once convention;
      routers/identity.ex's GET/POST /roles handlers now pass
      conn.assigns.scoped_opts through. REGRESSION FINDING: NOT a
      regression -- broken since the module's introduction (commit
      a1c4ab9b7d61c852c612ed03a51a64cbf52d1371, REQ-020); ELIXIR-DEV's
      git log -p --follow -S"prefix" search returned zero hits across the
      file's entire 2-commit history, independently corroborated by
      SECURITY-REVIEWER's own re-run of the same command. Gated through
      CODE-DESIGN-VALIDATOR PASS, SECURITY-REVIEWER PASS, REVIEWER PASS
      (one rework each for design-doc scope and an opts[:prefix]-vs-bound-
      variable idiom fix), TEST-DESIGNER's new cross-tenant-isolation
      regression test, TEST-DESIGN-VALIDATOR PASS, and TEST-RUNNER PASS:
      role_registry_test.exs 15/15 passed standalone including the new
      ISS-0768 test by name; authoritative full-suite run 4484/4493 passed,
      the 9 failures explicitly assessed as shared-Postgres-host connection
      contention among concurrent sibling sessions, not regressions.
  - req: REQ-372
    event: started
    agent: ORCH
    at: "2026-09-21T06:54:20Z"
    note: >
      WF02-REQ372-20260921 begun (letflow-queue task 729, GH#1592, locked
      under agent_id ORCH-letflow-c5). Backend semantic decision-rule
      validation pass (field-existence + type-compatibility checking) for
      EXCLUSIVE_GATEWAY edge conditions, re-run in full at the definition's
      release/promotion submission path. depends_on REQ-109/REQ-288/REQ-291
      all confirmed done. Step 00 (git setup) PASSED on
      feature/WF02-REQ372-20260921.
  - req: REQ-397
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T09:29:10Z"
    note: >
      GET /api/v1/promotions tenant-data-scoped, paginated, filterable
      (status/def_id) list route shipped on feature/WF02-REQ397-20260921
      (PR #1672), so a reviewer can discover pending/rejected/etc. reviews
      without an out-of-band reviewId. lib/letflow/routers/promotions.ex
      gained the new route plus Letflow.Definitions.PromotionReviewStore
      list_reviews/2, schema-scoped via prefix: same as every other call in
      that module, same :Unknown/PLATFORM_ADMIN-only authorization as R1-R8.
      One real ISS-0069-class fix cycle along the way (dead default-arg on
      req397_sample_plan/2 in the test fixtures). Gates: CODE-DESIGN-VALIDATOR
      PASSED twice (pagination-shape and filter-rejection reworks),
      SECURITY-REVIEWER PASSED (tenant-data-scoped list route per
      security-invariants.md, same class as REQ-394), REVIEWER PASSED,
      TEST-DESIGN-VALIDATOR PASSED, TEST-RUNNER/RELEASE-VALIDATOR PASSED
      twice (full suite 4515/4515 both rounds). Design:
      lib/letflow/design/req397-promotion-review-list-route.md. REQ-398 (the
      frontend list page depending on this endpoint) remains pending,
      unblocked by this merge.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T10:23:22Z"
    note: >
      ISS-0767 (NonSkippableApprovalGate.tsx did not surface a
      rehearsal-required blocked state once apply_review/4 gained its
      assertion-run gate, ISS-0732 follow-up) resolved on
      feature/WF03-ISS0767-20260921 (PR #1679). Added
      classifyApplyRehearsalError (exact-string-match on the RFC 9457
      `detail` string) and one RehearsalGateError UI block per reason,
      covering all four distinct apply_review/4 blocked states
      (:assertion_run_missing, :assertion_run_digest_mismatch,
      :assertion_run_in_progress, :assertion_run_failed), inserted ahead
      of the pre-existing bare status===409 digest-mismatch fallback in
      handleApply's catch chain -- a critical handler-ordering fix, since
      all five 409 cases share identical status/type/title and are
      disambiguated only by `detail`. Confirms the CORRECTION already on
      record in ISS-0767.yaml: the issue's original filing guessed a
      single, wrongly-named error reason; the actual shipped shape is
      four reasons. Gates: CODE-DESIGN-VALIDATOR PASSED,
      REVIEWER PASSED (empirically reproduced the ordering bug
      before/after the fix), TEST-RUNNER PASSED (739/739 full suite +
      48/48 targeted), RELEASE-VALIDATOR PASSED (independent full
      re-check plus byte-verification of the four `detail` strings
      against lib/letflow/definitions/promotion.ex). All four gates
      passed independently, no rework cycles. Design:
      lib/letflow/design/iss0767-non-skippable-gate-rehearsal-blocked-state.md.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T13:14:36Z"
    note: >
      ISS-0773 (sync_role_claims_from_token/3 silently wrote zero role grants on
      a zero-grant resolve_group_ids_for_role_names/2 result, permanently
      stamping role_claims_synced_at and closing off the function's own
      designed retry path -- QA bpm-default/bilimbaga admin-users 403'd on
      every authenticated route despite valid tokens) resolved on
      feature/WF03-ISS0772-20260921 (PR #1680). Fix: a Logger.warning on
      zero-grant resolution plus conditional Multi.update marker-stamping, so
      a zero-grant sync leaves the marker nil and naturally retries on next
      login instead of permanently locking the user out. Evidence:
      RELEASE-VALIDATOR's own fresh fail-then-pass reproduction, 59/59
      identity_test.exs, full `mix letflow.check.test` clean, REQ-378's
      revocation-permanence invariant independently re-derived and confirmed
      preserved (marker only ever becomes MORE retryable, never grants
      unearned access). Gates: CODE-DESIGN-VALIDATOR PASSED twice (second pass
      after fixing a real design-doc-provenance gap -- a design doc that
      existed only in a since-discarded worktree, caught by SECURITY-REVIEWER,
      fixed by properly committing it), SECURITY-REVIEWER PASSED twice,
      REVIEWER PASSED, TEST-DESIGN-VALIDATOR PASSED, TEST-RUNNER PASSED,
      RELEASE-VALIDATOR PASSED. Design:
      lib/letflow/design/iss-0773-role-claims-sync-lockout-fix.md.
  - req: REQ-372
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T13:28:21Z"
    note: >
      Backend semantic decision-rule validation shipped: new
      Letflow.Definitions.SemanticValidation module performs field-existence
      checking (referenced variable name absent from the definition's
      declared VariableSchema fields, with a Levenshtein-based nearest-
      field-name suggestion) and type-compatibility checking (comparison
      between two operands whose declared VariableSchema types are not
      comparable, e.g. numeric/money vs. text/string) for
      EXCLUSIVE_GATEWAY edge conditions. Wired into
      Letflow.Definitions.activate/2 and validate_definition_graph/2 so
      the pass re-runs in full (not from a cached result) at the
      definition's actual release/promotion submission path. Design
      amendment (design doc section 2.5) exempts definitions with zero
      declared VariableSchema fields from the check, to avoid retroactively
      breaking pre-existing definitions that never registered one.
      HUMAN_TASK routing/assignment-by-field checking is explicitly
      out of scope for this requirement (recorded in the module's
      moduledoc) -- there is no existing mechanism for a human-task
      assignment rule to reference a process field, so that case has no
      code path to reach. RELEASE-VALIDATOR independently confirmed all
      10 acceptance criteria PASS: mix letflow.check clean exit 0
      (lint_handoffs, check_async_sandbox_reachability, format,
      compile --warnings-as-errors, full test suite 16/16 partitions with
      0 ISS-0069 warnings, wasm_hang isolated tests, lua_wallclock_race
      all OK). lib/letflow/engine/expr.ex unmodified; no web/ file
      touched. This event covers WF-02 Step 6 (docs only) -- Step Final
      (git merge) is routed separately per docs/agents/workflows/
      WF-02_requirement_implementation.md to the Step 00 agent
      (ELIXIR-DEV).
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T16:23:31Z"
    note: >
      ISS-0774 (process-definition HUMAN_TASK routing role names, e.g.
      role-ops-manager, were never recognized by Authorization.role_from_string/1's
      closed platform-role atom set -- a user whose only tenant_role was a
      process-routing name had their entire roles list reduced to [] by
      roles_from_strings/1 and failed every platform API permission check
      indefinitely) resolved on feature/WF03-ISS0774-20260921 (tip d3a38c4a).
      Fix: tenant_role gains a kind column (:platform_role | :process_routing_role,
      deterministically backfilled from Authorization.roles/0's six literals), and
      Letflow.Identity.list_effective_role_names/2 is filtered to kind ==
      :platform_role before feeding roles_from_strings/1. All 4 ACs met (explicit
      decision with rejected alternatives named; process-routing-role-only user
      gets an explicit actionable 403 signal, covered by tests T3/T4; SwiftRoute
      UAT scenario unblocked; mix letflow.check clean). Regression found and fixed
      mid-pipeline (commit d3a38c4a): IdentityMigration.copy_tenant_roles/2's bare
      `select: tr` broke against the legacy public.tenant_role table (no kind
      column there, Postgrex 42703) -- fixed the same way its copy_groups/2 and
      copy_users/2 siblings already handle this schema-drift hazard, restricting
      the select and classifying kind in Elixir per-row before insert;
      identity_migration_test.exs 12/12 (was failing before), 207/207 across the
      identity/role_registry/tasks/identity-router suites, run twice. Separately,
      REVIEWER independently re-verified SECURITY-REVIEWER's finding that the
      design's own OQ-1 premise (claim that OIDC tokens can only ever claim one of
      the six platform-role strings) was factually wrong; confirmed pre-existing,
      zero diff hits in this PR, correctly filed as its own issue rather than
      folded in or left undocumented -- ISS-0775 (MINOR, queue task 773, GH-1684).
      Gates, all PASSED: CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (x2 -- second
      pass a targeted re-check on the regression fix), REVIEWER (x2, same),
      TEST-DESIGN-VALIDATOR, TEST-RUNNER (x2, final 4539/4539 passed 0 failures),
      RELEASE-VALIDATOR (independently re-verified all 4 ACs plus the regression
      fix plus ISS-0775's correct filing/scoping -- full PASS). Design:
      lib/letflow/design/iss-0774-role-domain-authorization.md.
  - req: REQ-373
    event: started
    agent: ORCH
    at: "2026-09-21T17:09:10Z"
    note: >
      WF02-REQ373-20260921 begun (letflow-queue task 732, GH#1620, locked
      under agent_id ORCH-letflow-c5). Backend version-status lifecycle
      (publish/retire) for service_catalog entries, wired to a real
      Letflow.Engine.PinResolver.Lookup implementation replacing
      default_lookup/0 in Engine.create/2's pin_lookup/2 helper for
      catalog_entry references. depends_on REQ-059/REQ-060/REQ-191/REQ-192
      all confirmed done (pre-verified before this run started). Step 00
      (git setup) PASSED on feature/WF02-REQ373-20260921, branched off
      origin/main at 7b4b4467.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T18:20:11Z"
    note: >
      ISS-0775 (MINOR, GH-1684, queue task 773 -- Letflow.Identity.
      resolve_group_ids_for_role_names/2's write path, called from
      sync_role_claims_from_token/3, matched a claimed OIDC role-name string
      against tenant_role.name with no kind predicate, so a token claiming a
      string that happened to match an existing :process_routing_role-kind
      row's name got an unintended group_members row written for it) resolved
      on feature/WF03-ISS0775-20260921 (tip a1997387). Design decision:
      CODE-DESIGNER chose candidate (a) -- restrict the write path to kind ==
      :platform_role rows only -- over the issue's own tentative preference for
      candidate (b) (restrict to :process_routing_role rows), because (b) would
      have severed REQ-378's entire OIDC JIT-provisioning mechanism (the only
      domain list_effective_role_names/2 has read since ISS-0774) to guard a
      "legitimate IdP-federated routing-role claim" use case with zero evidence
      anywhere in the codebase that it exists; every real process-routing-role
      binding is provisioned exclusively via RoleRegistry.upsert_role/4 +
      add_group_member/3, never a token claim, so (a) satisfies that concern
      vacuously while (b) would not have. Fix: added `and t.kind ==
      :platform_role` to resolve_group_ids_for_role_names/2's where clause
      (lib/letflow/identity.ex) -- a routing-kind name collision now resolves
      to no group_id, same shape as today's existing no-match case. Tests T1-T3
      added to test/letflow/identity_test.exs (platform-kind claim still
      writes; routing-kind claim writes nothing and the existing zero-
      resolution warning still fires; mixed claim list writes only the
      platform-kind membership). Gates, all PASSED: CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR, TEST-RUNNER (full
      suite 4566/4566 passed, 0 failures, plus isolated wasm_hang and
      lua_wallclock_race subchecks clean -- test/reports/
      report-20260921-WF03-ISS0775-20260921.yaml), RELEASE-VALIDATOR
      (independently re-verified the design decision, the fix, T1-T3, and
      separately confirmed non-regression of REQ-378's OIDC live-revocation
      mechanism and of ISS-0773's fix -- full PASS). Design:
      lib/letflow/design/iss-0775-oidc-role-sync-kind-scoping.md.
  - req: REQ-374
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T20:40:11Z"
    note: >
      Platform-wide tenant-migration fanout runner (backend) shipped on
      feature/WF02-REQ374-20260921 (tip 9acdcb51), letflow-queue task 733,
      GH-1621, stage S6. New Letflow.Platform.MigrationRollout applies an
      arbitrary platform-defined change to every active company's own tenant
      schema in turn, transactionally per company (one company's failure
      neither rolls back nor blocks any other), persists a per-company
      SUCCEEDED/FAILED outcome (completion timestamp, plain-language failure
      reason derived from the real error), supports resuming a rollout
      against only not-yet-succeeded companies, and is idempotent on re-run
      once every company already holds the change. Routes in
      lib/letflow/routers/platform_migrations.ex gated behind a new
      explicitly-named platform-admin-only permission wired through
      lib/letflow/plugs/api_pipeline.ex and lib/letflow/api/authorization.ex.
      Design: lib/letflow/design/req374-tenant-migration-fanout-runner.md.
      Went through two rounds of genuine design fixes (an apply_outstanding/1
      crash-recovery gap, and stating the suspended-status unreachability
      explicitly in the status enum) before CODE-DESIGN-VALIDATOR PASSED, and
      one round of implementation fix after REVIEWER caught a real
      silent-swallow bug (a registration conflict was being silently dropped
      instead of raising on an unreachable status) plus applied a
      reason-string quality fix to describe_failure_reason/1 (now handles
      Ecto.Changeset) directly. TEST-DESIGN-VALIDATOR independently
      reproduced fail-first mutation evidence for the new coverage in
      test/letflow/platform/migration_rollout_test.exs and
      test/letflow/routers/platform_migrations_test.exs. RELEASE-VALIDATOR
      independently re-verified all 7 acceptance criteria, including AC-7
      ("mix letflow.check passes"): two independent mix letflow.check.test
      runs both hit exactly the pre-existing, already-tracked ISS-0766 flake
      (test/letflow/identity_test.exs's REQ-019 AC1 tests, unrelated to any
      file this requirement touches) and nothing else, matching the
      established project precedent for treating this specific flake as
      non-blocking recorded at
      docs/status/requirement_status.v21.yaml:267-270 (ISS-0770's
      RELEASE-VALIDATOR entry, same flake). REQ-375 (the operator-facing
      rollout-status screen depending on this endpoint) remains pending,
      unblocked by this merge.
  - req: REQ-373
    event: done
    agent: DOC-UPDATER
    at: "2026-09-21T20:58:11Z"
    note: >
      Backend version-status lifecycle (publish/retire) for service_catalog
      entries shipped on feature/WF02-REQ373-20260921. Schema decision
      recorded explicitly in the moduledoc: an in-place `status`
      Ecto.Enum ([:ACTIVE, :RETIRED]) column added directly to
      service_catalog, plus a new sibling archive table,
      service_catalog_versions, capturing prior versions -- NOT a
      composite-key-versioned service_catalog table. New publish/3 and
      retire/1 operations on Letflow.ServiceCatalog. New
      Letflow.ServiceCatalog.PinLookup module implements a real
      Letflow.Engine.PinResolver.Lookup, wired into Engine.create/2's
      pin_lookup/2 helper in place of PinResolver.default_lookup/0 for
      catalog_entry references, so a real case can now actually record a
      resolved catalog_entry pin. Two new routes gated behind the
      already-shipped :AdminServicesManage permission (no new permission
      name invented). PLC-01/module_ref catalog versioning explicitly
      named still out of scope, citing pin_resolver.ex's own existing
      SCOPE GAP language. One rework cycle at the design gate (a
      status-casing self-contradiction in the moduledoc), one rework
      cycle at implementation (archive_changeset/1's over-broad
      validate_required on fields that are nullable on an archived row,
      found by TEST-DESIGNER), one rework cycle at the test-design gate
      (a service_catalog_versions test-cleanup leak, found by
      TEST-DESIGN-VALIDATOR). All gates ultimately PASSED:
      CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (x2), REVIEWER (x2),
      TEST-DESIGN-VALIDATOR (x2), TEST-RUNNER, RELEASE-VALIDATOR.
      RELEASE-VALIDATOR's independent full-suite re-run: 4585/4588 (3
      pre-existing unrelated failures, structurally cleared), plus a
      real AC8-blocking handoff-lint defect RELEASE-VALIDATOR found and
      fixed along the way. Follow-on FRONTEND-DEV requirement REQ-399
      filed (case-detail dependency-version/provenance screen, depends
      on this requirement), per the REQ-291/REQ-288 and REQ-398/REQ-397
      precedent pattern -- names its own required new backend route,
      GET /api/v1/instances/{id}/pins (PIN-04 AC4's own SCOPE note),
      as in-scope for REQ-399 itself since nothing else would build it.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T00:46:45Z"
    note: >
      ISS-0777 (BLOCKER, queue task 776, GH-1696 -- Letflow.Platform.
      MigrationRollout.start_rollout/3 crashed with an unhandled ArgumentError
      (bare 500) when entity_type contained a hyphen or any character rejected
      by DDL.valid_identifier?/1, no request-time format validation existing
      anywhere on the write path, blocking REQ-375's own AC5) resolved on
      feature/WF03-ISS0777-20260921 (tip 4193e277). Design: two-tier
      identifier-format validation -- a regex `pattern` constraint on
      entity_type/attribute in platform_migrations.ex's @start_schema (via
      the existing Letflow.Api.Validation mechanism) as the first tier, plus
      a second check inside Letflow.TenantProvisioning.
      register_column_promotion/4 itself as defense-in-depth so every caller
      of that function, not just this router, is protected from the same
      crash path -- CODE-DESIGN-VALIDATOR's first pass required this second
      crash-path be made explicit before approving. ELIXIR-DEV implemented
      both tiers, returning a clean 422-class error instead of ever reaching
      checked_table_name/1's raise, and corrected the stale "already
      validated" comment at that call site. Test-validity issue found and
      corrected: TEST-DESIGNER's first regression tests (Tests A/B/C)
      asserted the 422 response but did not provision an active tenant
      before invoking the router, so the request never actually reached the
      code path that used to crash -- the tests could pass even with the bug
      still present. TEST-DESIGN-VALIDATOR caught this and required rework;
      the fix (commit a880edb8, "Tests A/B/C now provision a real active
      tenant") was independently re-verified twice with genuine
      fail-then-pass evidence, once by TEST-DESIGN-VALIDATOR against the
      reworked tests and again by RELEASE-VALIDATOR re-running the full
      suite independently. Gates, all PASSED: CODE-DESIGN-VALIDATOR (one
      rework round), SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR (one
      rework round, the test-validity fix above), TEST-RUNNER,
      RELEASE-VALIDATOR (final independent full-suite run: 4591/4593 passed,
      the only 2 non-passing being the pre-existing documented ISS-0766
      flake, unrelated). Design:
      lib/letflow/design/iss-0777-entity-type-identifier-validation.md.
  - req: REQ-375
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T01:46:38Z"
    note: >
      Operator-facing rollout-status screen (web/src) shipped on
      feature/WF02-REQ375-20260921 (tip 04467459), letflow-queue task 735,
      GH-1623, stage S8, depends_on REQ-374 (done). Every implementation/
      review gate PASSED on the first pipeline pass (CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR, TEST-RUNNER), but
      real live UAT testing against a genuinely running instance
      subsequently found FOUR real defects that unit-level and design-time
      review had not caught -- the lesson worth stating plainly for future
      readers: passing gates and passing unit tests do not by themselves
      prove a screen works end-to-end against real data. (1) ISS-0777
      (BLOCKER): MigrationRollout.start_rollout/3 crashed with a bare 500 on
      an entity_type containing a hyphen -- no request-time identifier-
      format validation existed anywhere on the write path this screen's
      AC5 depends on. Fixed and merged separately as its own issue
      (feature/WF03-ISS0777-20260921, tip 4193e277) with two-tier validation
      (router-level pattern constraint plus defense-in-depth inside
      TenantProvisioning.register_column_promotion/4); this branch was
      blocked on that landing first. (2) The e2e spec's own fixture used a
      hyphenated entity_type identifier, which the new (correct) validation
      then rejected -- fixed by switching the fixture to an underscore-
      joined identifier (`pl_rollout_${fixtureId}`, commit 4013642f). (3)
      The e2e spec provisioned an active EntityDefinition for only one of
      the three test companies, so the other two healthy companies
      legitimately 404/not-found'd against a real backend that correctly
      required one -- fixed by adding a real
      createActiveEntityDefinition provisioning step (db-exec.ts) for both
      remaining healthy companies (commit 04467459). (4) A completed_at/
      em-dash display assumption baked into both a unit-test fixture and
      the e2e spec turned out to not match confirmed-correct production
      behavior -- fixed in both test artifacts (commit 29a72762), not in
      production code, once RELEASE-VALIDATOR/UAT-RUNNER confirmed the
      production display logic was already right. All four fixes verified
      by UAT-RUNNER's own live e2e re-runs (culminating in a genuine
      all-7-steps full pass, test/uat-reports/
      uat-2026-09-22-WF02-REQ375-e2e-live-20260922b.yaml), then
      RELEASE-VALIDATOR independently re-confirmed PASS with its own
      separate, fresh, live e2e rollout run against the same running
      instance -- a second real pass distinct from UAT-RUNNER's, not a
      re-read of UAT-RUNNER's report. All 5 acceptance criteria (per-
      company outcome/reason rows matching EO-003, real (no-mock) resume
      control gated on outstanding companies, EO-005 no-op re-run display,
      PLATFORM_ADMIN-only screen reachability, and the e2e pipeline spec
      itself passing) confirmed done.
  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-09-22T03:22:13Z"
    note: >
      ISS-0780 (MINOR, queue task Q-779, GH-1704 -- lib/letflow/plugs/cors.ex's
      @allow_headers omitted x-bpm-user-id, blocking every genuinely
      cross-origin authenticated request at the browser's CORS preflight;
      discovered incidentally by UAT-RUNNER during REQ-375's live e2e run)
      resolved on feature/WF03-ISS0780-fix-20260922. Fix (commit 3d1bb946):
      added x-bpm-user-id, if-match and idempotency-key to @allow_headers,
      gated PASS by SECURITY-REVIEWER and REVIEWER (handoffs
      step-03b-security-reviewer.json, step-03c-reviewer.json). Regression
      test added at test/letflow/plugs/cors_test.exs (commit d71d761e),
      shown fail-then-pass against pre-fix/post-fix code, gated PASS by
      TEST-DESIGN-VALIDATOR (step-04b-test-design-validator.json) and
      independently confirmed by TEST-RUNNER's full-suite run: 4619/4619
      passed (commit 0cfcb71d). GitHub mirror GH-1704 closed with a
      resolved-comment citing this run-id and the regression test path.
      All handoffs under handoffs/WF03-ISS0780-20260922/.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T06:35:11Z"
    note: >
      ISS-0778 (MAJOR, queue task 777, GH-1697 -- REQ-378's DB-backed
      platform-role sync had no seeding path, so a freshly-provisioned
      tenant had no route to a working PLATFORM_ADMIN grant without manual
      tenant_role/group_members bootstrap, discovered incidentally by
      UAT-RUNNER during REQ-375's live e2e run) resolved on
      feature/WF03-ISS0778-20260922 (tip babecb0f). Design decision: seed
      platform-role group bindings for all six Authorization.roles/0
      literals (kind: :platform_role) via new
      RoleRegistry.seed_default_platform_role_groups/1, wired directly into
      Letflow.TenantOnboarding.provision_and_migrate/1 as a step between
      migration replay and activation -- option (a) from the issue's own
      fix_direction, over a separate mix letflow.seed-only step, because it
      unifies routers/tenants.ex's provision_and_respond/2 and
      mix.tasks.letflow.seed.ex's provision_and_replay/2 onto the same
      shared onboarding path instead of leaving two independent call sites
      to each reproduce the same gap. A seeding failure surfaces as
      {:error, {:role_seeding_failed, reason}}, leaving the tenant at
      :migrating, recoverable via the existing recover_provisioning/1 path.
      No migration, no schema change. 4-tier regression suite (identity_test.exs,
      role_registry_test.exs, tenant_onboarding_test.exs, plus a full live
      e2e HTTP test proving OIDC login to a PLATFORM_ADMIN-gated endpoint
      with zero manual bootstrap), with mutation-testing evidence for two
      mutants (dropped seeding step: 88/91 passed; wrong kind tag: 86/91
      passed) documented in test/specs/ISS-0778.md. Gates, all PASSED:
      CODE-DESIGN-VALIDATOR (independent source verification),
      SECURITY-REVIEWER (privilege correctness, idempotency/race-safety of
      get_or_create_group_by_name/2, tenant isolation -- deep Ecto-internals
      trace), REVIEWER (idiom, scope), TEST-DESIGN-VALIDATOR (independent
      mutation reproduction), TEST-RUNNER, and RELEASE-VALIDATOR
      (independent full re-derivation of all acceptance criteria; also
      independently caught and confirmed the fix for a real
      mix format --check-formatted gap in two test files that every earlier
      gate had missed, commit babecb0f, no semantic change). Design:
      lib/letflow/design/iss-0778-platform-role-seeding.md.
  - req: REQ-379
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T09:30:31Z"
    note: >
      Solution-pack install-time write path shipped on
      feature/WF02-REQ379-20260922 (tip 803d55e3), letflow-queue task 739,
      GH-1627, stage S2, depends_on REQ-041 (done). CORRECTION TO THE
      REQUIREMENT'S OWN PREMISE: REQ-379's own description in this file and
      the GUI-review report it was filed from
      (test/uat-reports/gui-review-2026-09-20-template-update-conflict-resolution.md)
      both claimed "no caller anywhere ever inserts a SolutionPackInstall
      row" -- this was factually stale by the time of the 2026-09-20 GUI
      review that repeated it: Letflow.Definitions.SolutionPack.install/3
      already inserted that row, landed under REQ-078 (commit 92fd7a6e,
      2026-08-22), a month before the review. CODE-DESIGN-VALIDATOR
      independently verified this finding against source and history before
      the design gate passed. The real, narrow gap this requirement closed
      was solution_pack_artefact_bases never being written at install time;
      AC1 (the solution_pack_installs row) was already-existing REQ-078
      behavior that this branch added regression-test coverage for, not new
      functionality. What actually shipped: install/3 now also snapshots
      every delivered artefact into solution_pack_artefact_bases as that
      artefact's base_content/base_version at install time, and a
      re-install of the same version does not overwrite an existing base
      snapshot for an artefact the tenant has since locally adapted.
      compute_pack_update_plan/5 (REQ-041) verified end-to-end against real
      rows produced by this write path, covering all four classifications
      (unchanged/clean_update/local_only/conflict). Gates, all PASSED:
      CODE-DESIGN-VALIDATOR (independently verified the AC1-already-exists
      finding above), SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR
      (caught and confirmed the fix for a subtle AC2 test-discriminating-
      power gap via real mutation testing, commit f8f3bd28), TEST-RUNNER
      (4630/4630 full suite), RELEASE-VALIDATOR (independent full
      re-derivation of all 5 acceptance criteria, including the stale-
      premise correction). Design:
      lib/letflow/design/req379-solution-pack-install-write-path.md.
      REQ-380 (update-review/apply API) and REQ-381 (frontend screen),
      both depending on this requirement, remain pending, unblocked by
      this merge.
  - req: REQ-380
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T11:22:04Z"
    note: >
      Solution-pack update-review/apply API shipped on
      feature/WF02-REQ380-20260922 (tip 1f436807), letflow-queue task 740,
      GH-1628, stage S2, depends_on REQ-379 (done). Three routes/operations
      added under lib/letflow/routers/solution_packs.ex: (1) update-review
      endpoint returning each artefact under exactly one of
      unchanged/safe-to-update/local-only/both-sides-conflict, matching
      compute_pack_update_plan/5's existing classification (EO-001); (2)
      apply endpoint that refuses to apply and names the specific
      unresolved artefact when a both-sides-conflict artefact has no
      matching pack_update_resolutions row (EO-002), otherwise applies --
      untouched artefacts left as-is (EO-004), take_incoming artefacts
      updated to incoming content, keep_local artefacts left unchanged
      with a pack_update_resolutions row recording resolved_by/resolved_at
      (EO-003); (3) a second update-review call after a prior apply
      compares against the tenant's now-current state, not the original
      install, so a previously keep_local-resolved artefact is not
      re-flagged as a fresh conflict (EO-005). All gates PASSED:
      CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (deep
      canonicalization-corruption-risk trace), REVIEWER (fixed a real
      design-doc contradiction directly, plus a clean targeted re-check on
      a route-policy allowlist fix), TEST-DESIGN-VALIDATOR (real mutation
      testing, plus surfaced/confirmed a genuine follow-up finding),
      TEST-RUNNER (4639/4641, only known ISS-0766 flake), RELEASE-VALIDATOR
      (independent full re-derivation of all 6 acceptance criteria).
      Non-blocking follow-up ISS-0781 (MINOR, GH-1714, queue task 780)
      filed and already resolved separately: apply_entry/6's :conflict
      clause honors a later call's own submitted resolution for the
      applied action even when it differs from the immutable, already-
      persisted pack_update_resolutions attribution row, so the audit
      trail can end up describing a decision that no longer matches the
      tenant's actual current content state -- found by TEST-DESIGNER
      during this run's own test-design gate, independently confirmed by
      TEST-DESIGN-VALIDATOR, does not violate any REQ-380 acceptance
      criterion (none covers the two-call scenario), tracked and merged as
      its own issue. REQ-381 (the review screen depending on this
      requirement) remains pending, unblocked by this merge.
  - req: REQ-376
    event: started
    agent: ELIXIR-DEV
    at: "2026-09-22T05:07:11Z"
    note: >
      WF-02 run WF02-REQ376-20260922 started (letflow-queue task 736,
      GH#1624). Branch feature/WF02-REQ376-20260922 created and pushed
      from origin/main (bf0b677b). Scope: Postgres-native monthly
      PARTITION BY RANGE (created_at) for events/events_archive, a
      single-DDL whole-partition retirement mechanism, protected-record
      (keep_forever) exemption, and proof of post-retirement replay
      (EO-001/EO-002/EO-003). Requires a decision record under
      docs/migration/decisions/ cross-referencing 0003-ecto-schema-
      strategy.md Decision C and docs/issues/ISS-0014.yaml, with REVIEWER
      sign-off. Backend half; REQ-377 (frontend, already filed,
      impl_order 737) is the follow-on operator screen.
  - req: REQ-376
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T12:06:47Z"
    note: >
      Postgres-native monthly PARTITION BY RANGE (created_at) partitioning
      for events/events_archive shipped on feature/WF02-REQ376-20260922,
      per decision docs/migration/decisions/
      0037-event-store-partitioning-and-whole-partition-retirement.md
      (amends 0003 Decision C point 2, cross-references ISS-0014, REVIEWER
      sign-off recorded 2026-09-22). Whole-month DETACH+ATTACH retirement
      replaces row-by-row DELETE for expired history, proven single-DDL
      and non-blocking to concurrent writes (EO-001); keep_forever-policy
      records are provably identical in count before/after retirement
      (EO-002); an instance with history in an already-retired month still
      replays complete, gap-free history afterward (EO-003); next month's
      partition is pre-created ahead of need with a real idempotent
      lookahead sweep (AC5), no write path ever waits on partition
      creation; the pre-existing Letflow.EventStore.archive/1 row-level
      move path is explicitly stated to coexist, not be retired/replaced.
      RELEASE-VALIDATOR independently re-verified all 6 acceptance
      criteria against the real code/tests/decision record (not against
      prior self-reports), re-ran the REQ-376-specific test suites
      directly against Postgres, and spot-checked the EO-001/EO-002/EO-003
      test bodies line-by-line to confirm genuine mechanism proofs (real
      captured SQL / concurrent-write / cross-table count / cross-month
      replay assertions), not return-value-only checks. Went through 2
      design reworks, 3 implementation reworks, 2 test-design reworks, 2
      test-runner rounds and 2 release-validator rounds before all gates
      passed -- the final release-validator round (PASS,
      step-05-release-validator-final.json) re-confirmed a fresh
      mix letflow.lint_handoffs 0-violation run after the prior FAIL
      (step-05-release-validator.json) found and ELIXIR-DEV/TEST-RUNNER
      corrected two handoff-schema violations in this run's own artifacts
      (a completed_at/started_at ordering inversion and a non-schema
      metadata key); full rework history preserved in
      handoffs/WF02-REQ376-20260922/, not restated further here.
      TEST-RUNNER's final full-suite run: 4632/4632 passed. Design:
      lib/letflow/design/req376-partition-event-retirement.md. REQ-377
      (the operator-facing retirement screen depending on this backend)
      remains pending, unblocked by this merge.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T13:05:00Z"
    note: >
      ISS-0781 (MINOR, GH-1714, queue task 780 -- REQ-380's apply_entry/6
      :conflict clause let a later call's own submitted resolution govern
      the applied action even when it differed from the immutable,
      already-persisted pack_update_resolutions attribution row, making
      the audit trail potentially misleading about what state change
      actually occurred) resolved on feature/WF03-ISS0781-20260922 (tip
      0e701d60). CODE-DESIGNER chose candidate (a) from the issue's own
      fix_direction: the applied action now always honors the persisted
      resolution row (true first-decision-wins semantics), consistent
      with the row's own on_conflict: :nothing immutability. Design:
      lib/letflow/design/iss-0781-attribution-applied-action-consistency.md.
      Implementation (commit f48e64ab): find_submitted_resolution/2
      deleted outright; apply_entry/6's :conflict clause now calls
      apply_from_persisted_resolution/5 unconditionally instead of
      checking the current call's own resolutions argument first --
      insert_submitted_resolutions/6 (step 3) always runs earlier in the
      same transaction, so a first-ever submission is still visible here
      via read-your-writes and applies correctly; only a second, differing
      call for an already-persisted artefact now has its applied action
      match the row instead of diverging. Regression test extended,
      test/letflow/routers/solution_packs_update_test.exs:433-546 (now
      439-663), asserting both halves: the row stays unchanged AND the
      base advance matches the ORIGINAL persisted resolution, not the
      second call's differing submission; a second test added for the
      :merged resolution kind. Two mid-pipeline self-caught corrections:
      design doc S4.2's unreachable reverse-ordering test scenario fixed
      by REVIEWER (commit 27e958f7); test/specs/REQ-380.md's stale
      find_submitted_resolution/2 references fixed by TEST-DESIGNER
      (commit 0e701d60). Gates, all PASSED: CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR,
      TEST-RUNNER, RELEASE-VALIDATOR (independent re-verification,
      full-suite run 4640/4642 passed, the only 2 failures being the
      pre-existing, already-tracked ISS-0766 flake in identity_test.exs,
      unrelated to this branch, confirmed via isolated rerun 62/62
      passed). docs/issues/ISS-0781.yaml flipped open -> resolved. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by
      ISS-0781's acceptance criteria. Merged via PR (Step Final routed to
      ORCH per WF-03).
  - req: REQ-381
    event: done
    agent: DOC-UPDATER
    at: "2026-09-22T16:08:43Z"
    note: >
      Solution-pack update review screen (web/src) shipped on
      feature/WF02-REQ381-20260922 (tip a12f179f), letflow-queue task 741,
      GH-1629, stage S8, depends_on REQ-380 (done). Third and last of the
      three requirements filed from
      test/uat-reports/gui-review-2026-09-20-template-update-conflict-resolution.md
      (REQ-379 install-time write path, REQ-380 update-review/apply API,
      REQ-381 this screen); all three now done, closing that GUI-review
      finding's chain in full. Screen reachable from the company's pack
      screen, wired to REQ-380's real endpoints (no mock data), showing the
      four-way classified update review (EO-001), a
      keep-local/take-incoming decision per both-sides-conflict artefact,
      blocked-apply with the endpoint's own named-artefact message while any
      conflict is undecided (EO-002), post-apply attribution (who/when)
      visible for kept artefacts (EO-003) with untouched artefacts left
      unchanged (EO-004), and no re-presentation of an already-resolved
      artefact as a fresh conflict on a later update offer (EO-005). Per the
      requirement's own acceptance criteria, the scenario fixture's stale
      NOTE (ISS-0527) was removed from
      test/fixtures/uat/scenarios/platform/template-update-conflict-resolution.yaml
      and web/tests/e2e/pipelines/template-update-conflict.pipeline.e2e.spec.ts
      was authored against the real screen. One real spec-authoring bug
      found and fixed mid-pipeline by UAT-RUNNER's own diagnostic run
      (test/uat-reports/uat-2026-09-22-WF02-REQ381-AC6-20260922.yaml,
      BLOCKED_ON_SPEC_BUG): the spec's graphFor() fixture helper built a
      HUMAN_TASK node with attributes: null, tripping a real, pre-existing,
      unrelated graph-validator rule (lib/letflow/definitions/graph.ex
      CHK-09) requiring a non-empty role attribute -- not a REQ-381
      implementation defect; UAT-RUNNER's own diagnostic local edit proved
      the real implementation passes all six EO-001..EO-005 pl.step stages
      end-to-end, then the fix (a non-blank role string on the fixture's
      HUMAN_TASK node) was committed for real (commit 6f9ccfff). Gates, all
      PASSED: CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER (frontend 792/792 unit +
      48/48 guard tests green, type-check/lint/build clean; backend main
      suite 1244/1246, the 2 failures being the pre-disclosed ISS-0766
      identity_test.exs flake, independently confirmed via isolated
      rerun 62/62, unrelated to REQ-381's changes), RELEASE-VALIDATOR
      (final independent re-verification: live e2e re-run PASS, full
      backend suite PASS modulo the same known ISS-0766 flake, frontend
      suite PASS, scope confirmed clean, mergeable against main). Design:
      lib/letflow/design/req381-solution-pack-update-review-screen.md.
