# Letflow — Requirement run history — VOLUME 22 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v21.yaml (volume 21, closed,
#   1,235 frozen-prefix lines / 1,271 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v22.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v22.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v22.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v22.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.




history:
  - req: REQ-386
    event: started
    agent: ELIXIR-DEV
    at: 2026-09-22T22:51:01Z
    note: >
      WF02-REQ386-20260923 started -- time-limited signed URLs for
      instance-attachment byte content (letflow-queue task 746, GH#1634).
      Branch feature/WF02-REQ386-20260923 created and pushed (Step 00 PASS).
      Depends on REQ-211/REQ-212 (both done) -- extends
      fetch_scoped_attachment_content/3's not-found-folding coverage rather
      than replacing it.
  - req: REQ-386
    event: done
    agent: DOC-UPDATER
    at: 2026-09-23T00:53:45Z
    note: >
      WF02-REQ386-20260923 Step 6 -- all gates passed (design 1b, security 2c,
      idiom 2d, test-design 3b PASS; test run 4744/4746 with 2 pre-existing
      ISS-0766 failures; release validation Step 5 PASS, independently
      re-verified all 6 acceptance criteria). Ships
      POST /instances/:id/attachments/:attachment_id/link (issues a
      300-second HMAC-signed link via lib/letflow/repository/attachment_links.ex)
      and a token-gated content-serving path that folds expired/invalid/
      cross-tenant tokens to the same attachment_link_expired response,
      preserving REQ-211/212's existing not-found-folding untouched. Status
      flipped in docs/requirements.yaml. README.md not touched -- it documents
      no attachment API route surface at any granularity (grepped for
      "attachment": zero hits), so there is nothing there to keep in sync.
      docs/migration/stage-6-operational-cross-cutting.md not touched either:
      its REQ-211/212 note records the introduction of a new subsystem
      (closing GH#769/ISS-0390); REQ-386 extends that already-recorded
      subsystem rather than adding a new one, so no new provenance note is
      warranted for consistency with that doc's own pattern.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T00:57:17Z"
    note: >
      ISS-0784 (MINOR, queue task 783, GH-1731 -- no operator-facing signal
      when task activation rolls back on {:invalid_form_schema, ...}, a gap
      two independent design passes, REQ-273 OQ-2 and REQ-385 OQ-1, had each
      separately flagged and never acted on) resolved on
      feature/WF03-ISS0784-20260922 (tip 129a5095). ORIGINAL FIX: a new
      record_task_activation_rejection_audit/5 helper records the rejection
      via Letflow.Audit at all four call sites that can hit the rollback --
      Engine.create/2, Engine.complete_task/3, the timer-fired advance path,
      and the service-task-outcome advance path. SIGNIFICANT MID-CYCLE
      CORRECTION, worth flagging prominently: TEST-DESIGNER proved directly
      against Postgres that sites 3/4 (persist_timer_fired_advance/7,
      do_persist_service_task_advance/10) wrote zero audit_entries rows on a
      real rejection -- the audit write was nested inside the caller's own
      already-open outer transaction (Scheduler.fire_timer/2,
      Engine.advance_after_service_task_outcome/4), so it silently rolled
      back along with the failed attempt, completely defeating the feature
      at exactly those two sites. Fixed (commit 6c716637) by moving the
      audit write to the two outer callers (Scheduler.attempt_fire/2,
      ServiceTaskDispatcher.call_advance_after_service_task_outcome/3),
      issued strictly after their own Repo.transaction/1 returns, in its own
      true independent transaction; record_task_activation_rejection_audit/5
      exposed (was defp) so both callers can reach it; a new site-4
      regression test (service_task_wiring_test.exs) proves the fix
      generalizes beyond TEST-DESIGNER's original site-3 test
      (timer_wiring_test.exs). Gates: CODE-DESIGN-VALIDATOR (one rework
      round -- caught and required a fix for a false "no new UI" claim in
      the original design), ELIXIR-DEV, TEST-DESIGNER (found and documented
      the transaction-boundary defect above with real fail-then-pass
      Postgres evidence), RELEASE-VALIDATOR (final verdict PASS/mergeable,
      independently traced and confirmed the transaction-boundary fix
      sound). Design:
      lib/letflow/design/iss0784-task-activation-rollback-audit-signal.md.
      No docs/migration/stage-N-*.md or decisions/*.md file was named by
      this issue's acceptance criteria. Merged via PR on
      feature/WF03-ISS0784-20260922.
  - req: REQ-388
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T03:28:33Z"
    note: >
      Audit-log entry for a denied cross-tenant or otherwise-failed instance-
      attachment access attempt, filed from the GUI-review UAT run
      (test/uat-reports/gui-review-2026-09-20-attachment-cross-tenant-probe.md,
      third of three requirements from that finding -- REQ-386/REQ-387 remain
      pending). Adds an `Letflow.Audit` write on the denied branches
      (cross-tenant and cross-instance-same-tenant) of
      `fetch_scoped_attachment_content/3` in
      `lib/letflow/routers/instances.ex`, without altering the existing
      byte-identical `Response.not_found(conn)` shape (EO-001) -- the audit
      write fires strictly after the response tuple is decided, matching this
      codebase's existing audit-write idiom. Design:
      lib/letflow/design/req388-attachment-access-denial-audit.md. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria; web/src/pages/admin/AuditLogPage.tsx
      needed no frontend change per its own AC, confirmed by the diff (no
      web/ files touched). Full pipeline history -- CODE-DESIGNER,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER (found and fixed a
      design-doc arity mismatch), TEST-RUNNER, RELEASE-VALIDATOR (final
      verdict PASS, mergeable, architecturally sound) -- on
      feature/WF02-REQ388-20260923 (tip f4a9b9c9), merged via PR.
  - req: REQ-387
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T07:25:03Z"
    note: >
      WF02-REQ387-20260923 Step 6 -- RELEASE-VALIDATOR final verdict PASS,
      mergeable, fresh independent live e2e re-run confirmed. Ships the
      frontend document-viewer for instance attachments: a typed API client
      (web/src/api/attachments.ts), an upload/list widget
      (web/src/components/instances/AttachmentPanel.tsx via
      web/src/hooks/useAttachments.ts), and
      web/src/pages/instances/AttachmentViewerPage.tsx routing to
      byte-identical AttachmentNotFoundScreen.tsx for foreign-tenant and
      never-issued references, a distinct AttachmentLinkExpiredScreen.tsx
      with a fresh-link affordance, and AttachmentSuccessView.tsx/
      AttachmentErrorScreen.tsx for the remaining cases. Calls REQ-386's
      signed-link endpoint. Permanent Playwright regression spec
      web/tests/e2e/pipelines/attachment-cross-tenant.pipeline.e2e.spec.ts
      authored and passing against a real running instance -- an earlier
      run in this same cycle (commit 7a643088) found and fixed the spec
      never provisioning Keycloak, then UAT-RUNNER's live re-run confirmed
      PASS. The stale ISS-0527 NOTE in
      test/fixtures/uat/scenarios/platform/attachment-cross-tenant-probe.yaml
      is removed (confirmed via diff against origin/main), since REQ-386 and
      this requirement have both now shipped and the feature it warned about
      is real. This completes the 3-part attachment-cross-tenant chain filed
      from the same 2026-09-20 GUI-review finding
      (test/uat-reports/gui-review-2026-09-20-attachment-cross-tenant-probe.md)
      -- REQ-386 (backend signed-link, done) and REQ-388 (audit logging,
      done) both independently confirmed done in docs/requirements.yaml
      before writing this entry. Status flipped in docs/requirements.yaml.
      README.md not touched -- greped for "attachment": zero hits, same as
      REQ-386's own note recorded; no documented current-behavior surface
      to keep in sync. No docs/migration/stage-N-*.md or decisions/*.md
      file was named by this requirement's acceptance criteria. Merged via
      PR on feature/WF02-REQ387-20260923 (tip 88346cd6).
  - req: REQ-390
    event: started
    agent: ELIXIR-DEV
    at: 2026-09-23T08:05:32Z
    note: "WF02-REQ390-20260923 started"
  - req: REQ-390
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T09:35:54Z"
    note: >
      WF02-REQ390-20260923 done -- per-tenant storage quota tracking implemented.
      Ships `Letflow.Repository.Quota` context module with
      `total_used_bytes/2`, `quota_exceeded?/3`, and `record_upload/3` /
      `record_deletion/3` for atomic tenant-scoped byte-counter maintenance.
      Storage tracked via a new `tenant_storage_usage` table (migration
      20260923_add_tenant_storage_usage). Quota enforcement wired into
      `Letflow.Repository.Attachments.upload/2` so uploads exceeding the
      per-tenant ceiling are rejected before writing to storage. REST
      endpoint `GET /tenants/:id/storage-usage` exposes the current usage
      figure. README.md: zero `quota` hits -- no documented current-behavior
      surface to keep in sync, not touched. docs/migration/stage-6-operational-
      cross-cutting.md: zero `quota` hits -- no update required.
      Full pipeline (CODE-DESIGNER, SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER,
      TEST-RUNNER, RELEASE-VALIDATOR) completed on
      feature/WF02-REQ390-20260923, merged via PR.
  - req: REQ-393
    event: started
    agent: FRONTEND-DEV
    at: "2026-09-23T10:15:15Z"
    note: "WF02-REQ393-20260923 started"
  - req: REQ-391
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T11:39:43Z"
    note: >
      WF02-REQ391-20260923 Step 6 -- includes a real merge-reconciliation
      cycle with concurrently-landed REQ-390 (both requirements independently
      modified lib/letflow/repository/attachments.ex; DOC-UPDATER flagged the
      conflict rather than forcing it, ELIXIR-DEV reconciled it in merge
      commit 91374d40, and SECURITY-REVIEWER/REVIEWER/TEST-RUNNER/
      RELEASE-VALIDATOR all re-verified the reconciled code -- final verdict
      PASS, mergeable, confirmed "Already up to date" against current
      origin/main). Ships instance-attachment attach/remove history events
      and approval-record attachment attribution: `upload/2` and `delete/2`
      (the latter gaining a new `deleted_by` 3rd positional argument) each
      record a history/timeline-visible event via the existing
      `Letflow.EventStore.InstanceProjection` mechanism naming the action,
      file_name, and actor; `Letflow.Engine.complete_task/3`'s
      TASK_COMPLETED path snapshots the instance's currently-attached
      attachment file_names onto the decision record (snapshot, not a live
      reference -- justified in the design doc: a later removal must not
      retroactively change what an approval record shows a document
      reviewed). Design: lib/letflow/design/
      req391-attachment-history-approval-attribution.md. Full pipeline --
      CODE-DESIGNER, SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER,
      TEST-RUNNER (4765 test+property executions, only failure the
      pre-existing known flake ISS-0766/identity_test.exs), RELEASE-VALIDATOR
      (final verdict PASS, mergeable) -- on
      feature/WF02-REQ391-20260923 (tip db13cda7), merged via PR #1639
      (GH-1639). Status flipped in docs/requirements.yaml. REQ-390 confirmed
      already `status: done` on origin/main before writing this entry
      (merged earlier as PR #1749, a3156299). README.md not touched --
      grepped for "attachment": zero hits, same pattern as REQ-386/387/388's
      own notes recorded; no documented current-behavior surface to keep in
      sync. No docs/migration/stage-N-*.md or decisions/*.md file was named
      by this requirement's acceptance criteria.

  - req: REQ-393
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T17:10:09Z"
    note: "WF02-REQ393-20260923 done -- generic entity-list browse screen (EntityListBrowserPage)"
  - req: REQ-394
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T17:54:56Z"
    note: >
      WF02-REQ394-20260923 Step 6 -- all gates passed (CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER PASSed the design/implementation;
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, RELEASE-VALIDATOR PASSed the
      implementation; final verdict MERGEABLE, no blocking defects). Ships a
      new per-(user, entity_type) authorization layer,
      `Letflow.Entities.TypeAccess`, backed by two new tenant-scoped tables
      (`entity_type_restrictions`, `user_entity_type_grants`, migrations
      20260923010001/20260923010002). `POST /entities/query` and the three
      entity-definition-read routes now fold a caller lacking per-type
      access into the exact same response as a genuinely empty/nonexistent
      type -- 200 `{"items": [], "next_cursor": null}` for the query route,
      and the equivalent not-found-shaped response for the definition-read
      routes -- closing the existence-oracle gap named in this requirement's
      description. This is a deliberate, tested behavior change from
      REQ-311's prior `{:error, :entity_type_not_found}` -> plain 404
      mapping for a query against a type absent from the tenant entirely:
      that case is now folded into the same indistinguishable-empty-type
      response rather than a distinguishable 404, and is documented and
      covered directly in the design doc and test suite (test/specs/REQ-394.md,
      test/letflow/entities/type_access_test.exs,
      test/letflow/routers/entities_test.exs). The existing coarse
      `:EntitiesQuery`/`:EntitiesDefinitionsRead` route-level permissions are
      unchanged and still required to reach the query engine at all -- this
      is an additional, finer-grained check layered on top. Design:
      lib/letflow/design/req394-per-entity-type-authorization.md (status
      line corrected from its stale "pending CODE-DESIGN-VALIDATOR" wording
      to reflect the full passed pipeline, per RELEASE-VALIDATOR's non-
      blocking flag). No docs/migration/stage-N-*.md or decisions/*.md file
      was named by this requirement's acceptance criteria. README.md not
      touched -- grepped for entity_type/EntitiesQuery/Entities-route
      surface: zero hits, no documented current-behavior surface to keep in
      sync. Status flipped in docs/requirements.yaml. Full pipeline on
      feature/WF02-REQ394-20260923 (85514518 design, bd77a129
      implementation, 252fbfe9 tests), merged via PR.
  - req: REQ-398
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T20:05:56Z"
    note: >
      WF02-REQ398-20260923 Step 6 -- all gates passed (CODE-DESIGN-VALIDATOR,
      REVIEWER, TEST-DESIGN-VALIDATOR, TEST-RUNNER, RELEASE-VALIDATOR; final
      verdict MERGEABLE, no blocking defects). Ships the frontend promotion-
      review list/queue page: a typed list client in web/src/api/promotions.ts
      and corresponding hook in web/src/hooks/usePromotions.ts calling
      REQ-397's GET /api/v1/promotions endpoint; a new routed list page
      rendering status, def_type/def_id, requested_by and timestamps with a
      status filter, each row linking to the existing
      definitions/:id/promotions/:reviewId detail route; a new nav entry in
      AppShell.tsx's NAV_ITEMS gated by the same client-side PLATFORM_ADMIN
      check PromotionReviewPage.tsx already uses. The design doc explicitly
      states this SUPERSEDES ISS-0730 design doc section 1's no-nav-entry
      decision, made at a time no list endpoint existed -- now that REQ-397
      ships that endpoint, the stated rationale no longer holds; this is a
      deliberate, documented reversal, not a silent re-decision. ISS-0734's
      fix_direction piece 2 (durable recording of submit-time conflict
      refusals / ConflictRejectionAlert.tsx reshaping) remains explicitly
      OUT OF SCOPE and open, cross-referenced in the design doc so a future
      reader does not mistake this page's absence of refused-submission rows
      for a bug this requirement was meant to fix. SECURITY-REVIEWER was
      judged and confirmed not needed for this change -- pure frontend,
      mirrors PromotionReviewPage.tsx's existing auth pattern, consumes an
      already-security-reviewed endpoint (REQ-397). A real guard violation
      (hardcoded tenant slug in a new test fixture) was found mid-cycle and
      fixed (commit 17b8fcd3). Design: lib/letflow/design/
      req398-promotion-review-list-page.md. No docs/migration/stage-N-*.md
      or decisions/*.md file was named by this requirement's acceptance
      criteria. README.md not touched -- grepped for "promotion": zero hits,
      no documented current-behavior surface to keep in sync. Status flipped
      in docs/requirements.yaml. Full pipeline on
      feature/WF02-REQ398-20260923 (54d8e6a6 design, 17a5292c implementation,
      a2a0b878 tests, 17b8fcd3 guard fix), merged via PR.
  - req: REQ-396
    event: started
    agent: ELIXIR-DEV
    at: "2026-09-23T18:37:05Z"
    note: "WF02-REQ396-20260923 started"
  - req: REQ-396
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T20:18:19Z"
    note: >
      WF02-REQ396-20260923 done -- HUMAN_TASK escalation_timer_duration added
      to the graph schema and SCH-04's escalation half wired (timer fire ->
      original task non-actionable, new task assigned to escalation role),
      closing the deferral REQ-185/REQ-188 already named. HUMAN_TASK escalation
      remains a HUMAN_TASK (CHK-21, engine wiring) per the release-validator
      PASS report. README.md not touched -- zero "escalation" hits; no
      documented current-behavior surface to keep in sync.
  - req: REQ-399
    event: done
    agent: DOC-UPDATER
    at: "2026-09-23T22:23:29Z"
    note: >
      WF02-REQ399-20260923 Step 6 -- full pipeline passed (CODE-DESIGN-
      VALIDATOR, SECURITY-REVIEWER dispatched directly by ORCH as a
      standalone subagent PASS, REVIEWER, TEST-DESIGN-VALIDATOR, TEST-RUNNER,
      RELEASE-VALIDATOR verdict MERGEABLE, all 8 ACs independently confirmed,
      backend suite 4783/4783 clean on a second uncontaminated run). Ships
      a case-detail dependency-version/provenance panel on the instance
      detail page, covering all four PinResolver source values
      (:resolved/:override/:inherited/:rebound) with distinct human-readable
      labels, plus loading/error/empty QueryStateBoundary states. NOTABLE:
      this cycle's real backend contribution was NOT a new route --
      GET /api/v1/instances/{id}/pins already shipped under REQ-080; this
      cycle's backend work (feca389b) is a new telemetry test proving that
      route issues zero additional catalog/module reads (AC2), confirming
      Letflow.Engine.PinResolver.reconstruct_effective_pins/2's existing
      contract holds. Frontend: 171cf1e8 (panel) and 6c573e6a (AC4/AC5/AC6
      test coverage). Design: 2183c0d0. Status flipped in
      docs/requirements.yaml. README.md not touched -- no "pin"-related
      documented current-behavior surface to keep in sync. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria. SECURITY-REVIEWER's PASS has no
      separate handoffs/*.json artifact (dispatched directly, not through
      the formal workflow script) -- its findings are recorded in full in
      this cycle's PR body instead. Merged via PR on
      feature/WF02-REQ399-20260923.
  - req: REQ-389
    event: done
    agent: DOC-UPDATER
    at: "2026-09-24T01:36:56Z"
    note: >
      WF02-REQ389-20260923 Step 6 -- full pipeline passed (CODE-DESIGN-VALIDATOR,
      REVIEWER -- which also found and separately filed+merged ISS-0788, a
      pre-existing unrelated defect, not blocking this requirement --
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, RELEASE-VALIDATOR verdict MERGEABLE,
      all 5 acceptance criteria independently confirmed). Ships a module-level
      content-type allowlist checked as the first guard in
      `Letflow.Repository.Attachments.upload/2` -- before the size ceiling,
      the malware scan, and any `repository_artifacts`/`instance_attachments`
      write -- returning a new `{:error, :content_type_not_allowed}` tag on a
      rejected declared content_type. The route layer
      (`lib/letflow/routers/instances.ex`) renders this as a distinct 415
      response naming the rejected type and the allowed set in plain
      language, separate from the existing `:file_too_large`/`:infected`
      bodies. A test confirms a rejected upload leaves neither a
      `repository_artifacts` nor an `instance_attachments` row behind, and a
      regression test confirms `application/pdf` (REQ-211/212's own fixture)
      continues to be accepted. Design doc
      (lib/letflow/design/req389-attachment-content-type-allowlist.md)
      documents explicitly why this new check against the caller-declared
      content_type does not contradict INV-a's existing no-MIME-sniffing
      statement -- INV-a's scope (no byte-content sniffing) is unchanged;
      this adds a coarser check against the declared string only. Commits:
      4973aa90 (design), 27e004d3 (implementation), 8ad89658 (tests). Status
      flipped in docs/requirements.yaml. README.md not touched -- grepped for
      "attachment"/"content-type": no documented current-behavior surface
      describing the attachment upload API to keep in sync (same pattern as
      REQ-386/387/388/390/391's own notes). No docs/migration/stage-N-*.md
      or decisions/*.md file was named by this requirement's acceptance
      criteria. Merged via PR on feature/WF02-REQ389-20260923.
  - req: REQ-392
    event: done
    agent: DOC-UPDATER
    at: "2026-09-24T05:19:25Z"
    note: >
      WF02-REQ392-20260924 Step 6 -- full pipeline passed (CODE-DESIGN-VALIDATOR
      2 rounds, SECURITY-REVIEWER conditional-pass fixed, REVIEWER,
      TEST-DESIGN-VALIDATOR independently reproduced fail-then-pass mutation
      evidence, TEST-RUNNER full backend 4802/4802 and full frontend 874/874
      clean, RELEASE-VALIDATOR verdict MERGEABLE with all 7 ACs independently
      confirmed). This is the fourth and final piece of the REQ-387/389/390/
      391/392 attachment-feature chain -- closes it. Ships a "remove" action
      on REQ-387's attachment widget (DELETE /instances/:id/attachments/:id,
      list updates without a full reload); distinct, readable 415
      (content-type-not-allowed, REQ-389) and 409 (quota-exceeded, REQ-390)
      rejection messages each naming the specific limit hit; a storage-usage
      figure near the widget backed by a new `storage_summary/1` context
      function and `GET /instances/storage-usage` route, verified to update
      correctly across an accepted upload and a removal; instance
      history/timeline coverage of REQ-391's attach/remove events with actor
      attribution; the approval/decision screen rendering the reviewed
      attachment's file_name; and a real (non-stub) Playwright spec at
      web/tests/e2e/pipelines/shipment-attach-delivery-note.pipeline.e2e.spec.ts
      covering all five scenario steps, plus removal of the stale ISS-0526
      NOTE in test/fixtures/uat/scenarios/swiftroute/shipment-attach-delivery-
      note.yaml now that the full chain has shipped (confirmed via diff and a
      direct grep for "NOTE"/"ISS-0526" against this branch: zero hits).
      Commits: 63c5c55b+9639d274 (design), 0f852c51 (backend route/context
      fn), 0fbca6b9 (frontend UI), 90f5e683 (security fix --
      endpoint_policy_key clause for the new storage-usage route), 55d1f6d7
      (tests), bc9f615f (e2e-spec fixture fix -- a missing HUMAN_TASK `role`
      attribute, found live by UAT-RUNNER).

      AC6 -- HONEST CHARACTERIZATION, deliberately not a claimed clean pass:
      UAT-RUNNER stood up a real live backend+Keycloak stack and ran the spec
      multiple times. It found and (via TEST-DESIGNER) fixed one genuine
      spec-fixture bug (the missing HUMAN_TASK role attribute, commit
      bc9f615f). After that fix, a full live run still could not complete --
      it hung intermittently at upload-button clicks, always the click
      immediately following a prior upload interaction on the same page,
      never a fresh first click. UAT-RUNNER's root-cause work: (1) proved the
      backend responds correctly and fast via direct curl; (2) proved the
      frontend button is genuinely clickable via a raw DOM `element.click()`
      call, which succeeds instantly where Playwright's synthetic
      `locator.click()` hangs; (3) proved via `document.elementFromPoint()`
      hit-testing at the exact failure moment that no overlay/toast/z-index
      issue exists -- the button is the topmost, fully clickable element with
      no CSS/DOM obstruction. Conclusion, accepted by ORCH as well-evidenced:
      Playwright/Chromium CDP input-dispatch flakiness specific to this
      shared, resource-contended host (confirmed low free memory, high load
      from concurrent multi-agent sessions), not a defect in REQ-392's
      frontend, backend, or test code -- one earlier run (run4) passed
      through this exact structural point before better diagnostics were
      added, supporting intermittent host flakiness over an always-
      reproducing defect. The spec is real, non-stub, structurally verified,
      and its core interactions were independently proven correct via direct
      backend+DOM testing, but a full live Playwright run did not complete in
      this environment -- this is NOT a claimed clean e2e pass, and is worth
      a clean re-run once host contention is lower. Per RELEASE-VALIDATOR's
      own judgment and this project's convention, UAT-RUNNER e2e verification
      is separable from the merge-blocking static gates (design/security/
      idiom/test-design/test-run), so this did not block the MERGEABLE
      verdict. Status flipped in docs/requirements.yaml. README.md not
      touched -- grepped for "attachment": zero hits, same pattern as
      REQ-386/387/388/389/390/391's own notes recorded; no documented
      current-behavior surface to keep in sync. No docs/migration/stage-N-*.md
      or decisions/*.md file was named by this requirement's acceptance
      criteria. REQ-387/389/390/391 all confirmed already `status: done` in
      docs/requirements.yaml before writing this entry.
  - req: REQ-395
    event: started
    agent: ORCH
    at: "2026-09-24T03:52:29Z"
    note: WF-02 started, queue task 755
  - req: REQ-395
    event: done
    agent: DOC-UPDATER
    at: "2026-09-24T06:05:00Z"
    note: "CONDITIONAL_PASS -- seed script scripts/seed_swiftroute_definition.sh merged, 5 structural tests pass, ACs 1-3 pending ISS-0739+QA credentials, UAT re-run deferred to after ISS-0739"
  - req: REQ-400
    event: done
    agent: DOC-UPDATER
    at: "2026-09-24T17:24:56Z"
    note: >
      WF02-REQ400-20260924 Step 6 -- first requirement of S11 (modular-
      platform architecture). RELEASE-VALIDATOR independently re-verified
      all 5 acceptance criteria PASS against real fresh test/grep/compile
      output. AC5 (`mix letflow.check` passes) initially showed the one
      known pre-existing S11-newly-registered T-LIVE-ACTIVITY deferral-
      staleness failure -- confirmed to be entirely outside REQ-400's own
      diff (`git diff origin/main...HEAD --stat` touches only
      config/config.exs, config/test.exs, lib/letflow/modules/{catalog,
      module}.ex, test/letflow/modules/*, test/support/modules/fixture/*,
      and the design/spec docs -- zero overlap with the T-LIVE-ACTIVITY
      test file) and already fixed on main via commit 4263be60 (PR #1794);
      REQ-400's branch had simply forked one commit earlier. Rebased
      feature/WF02-REQ400-20260924 onto latest origin/main (clean, no
      conflicts -- disjoint files as expected) to pull in 4263be60, then
      re-ran `mix letflow.check` fresh in the rebased worktree end-to-end:
      genuinely clean, `combined: 4820 tests, 6 properties, 0 failures
      (4826/4826 passed)`, `mix letflow.check.test: OK -- no test
      failures, no ISS-0069 warnings (8 partitions)`, and the isolated
      `:lua_wallclock_race` re-check also passed clean -- overall exit 0,
      not merely "expected to pass." Ships `Letflow.Modules.Module` (a
      behaviour with required `manifest/0` and optional `router/0`/
      `on_install/2`, per 0039 D4) and `Letflow.Modules.Catalog` (a plain
      module, not a process, per 0039's REVIEWER note on REQ-045 --
      confirmed via `git grep -n "GenServer\|Agent.start\|Supervisor" --
      lib/letflow/modules/` returning zero hits), reading the module list
      from `config :letflow, :modules` via `Application.compile_env/3`;
      manifest validation covering all six bad-manifest cases named in
      the acceptance criteria; and a test-only fixture module
      `Letflow.Modules.Fixture` under test/support/modules/fixture/,
      registered only in config/test.exs. `config/config.exs` registers
      `[]` in this P1 slice -- no real module exists until REQ-408. Design:
      lib/letflow/design/req400-module-behaviour-catalog.md. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria (0039 itself, the decision record
      this requirement implements, was not touched -- it needed no update,
      confirmed via diff). README.md not touched -- grepped for "module":
      only pre-existing unrelated hits (mix app description, moduledoc
      cross-references, @moduletag), no documented current-behavior
      surface describing a modules subsystem to keep in sync; the modules
      list stays `[]` in this slice so there is nothing operator-visible
      yet. Status flipped in docs/requirements.yaml. Full pipeline --
      CODE-DESIGNER (one rework round per CODE-DESIGN-VALIDATOR), ELIXIR-
      DEV, TEST-DESIGNER, RELEASE-VALIDATOR (final verdict PASS) -- on
      feature/WF02-REQ400-20260924 (efce259b implementation, 9fb81d75 fix,
      04f6df09 tests, rebased onto origin/main at d28c3cf6 to pick up
      4263be60/PR #1794 and c7632ecf before this entry).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-25T06:40:00Z"
    note: >
      WF03-ISS0765-20260924 -- ISS-0765 (MAJOR) resolved and merged, PR #1808
      squash-merged as dec06da7. groupsApi's six remaining calls corrected off
      the dead /api/v1/admin/groups/ prefix to /api/v1/identity/groups; .get
      and .update DELETED rather than repointed, because neither exists at any
      prefix (absent from __authz_routes__/0, and identity.ex has no
      get_group/2 or update_group/3) -- the issue's own fix_direction was not
      satisfiable as written for those two. .members retyped to the real
      {items, next_cursor, count} envelope, which had to land WITH the prefix
      or the fix would have converted a silent 404 into a render-time
      TypeError. .addMembers narrowed to .addMember sending {user_id},
      mirroring ISS-0736. .removeMembers byte-unchanged. Full pipeline:
      ISSUE-FIXER, CODE-DESIGNER (one rework -- CODE-DESIGN-VALIDATOR FAILed
      the first design on a matrix row that could not fail pre-fix),
      FRONTEND-DEV, SECURITY-REVIEWER PASS, REVIEWER PASS, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR PASS (independently rebuilt mutant M-1a from the
      design's own table and reproduced all five tsc diagnostics byte-for-
      byte). CI 3/3 green after two rounds of handoff-lint fixes. Queue task
      810 released done. End-to-end evidence followed post-merge via
      ai-dala-orch's live UAT under ISS-0791.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-25T06:45:00Z"
    note: >
      Queue reconciliation and issue-backlog registration, same session. Found
      seven open docs/issues/ records that had never been through
      register_task and were therefore invisible to get_next_task on every
      host; registered four (ISS-0764/0765/0772/0745 -> tasks 807-810) and
      deliberately held three back so issue-type LIFO priority would not
      starve the S11 roadmap. The session then filed and registered nine
      further findings surfaced by ISS-0765's own pipeline: ISS-0811 through
      ISS-0819 (tasks 811-819, GH #1799-1809). Net effect on the shared queue:
      0 eligible tasks at session start (everything either locked by
      ai-dala-orch on REQ-400 or transitively blocked behind it) -> 13
      eligible. Also reconciled a cross-host duplicate: ISS-0818 and ISS-0791
      were one queue task (Q-818/GH-1807) with two on-disk records, authored
      concurrently on two hosts; ISS-0791 kept as the record holding the UAT
      evidence, ISS-0818 turned into a tombstone with its refs stripped, and
      the collision class recorded in docs/anti-patterns.md as distinct from
      the already-documented filename-collision class (the filenames differ,
      so nothing collides at the git level and the existing guard cannot see
      it).
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-24T23:41:31Z"
    note: >
      ISS-0817 (MINOR, queue task 817, GH-1806 -- web/ test flake in
      web/src/pages/instances/__tests__/AttachmentViewerPage.test.tsx's D1
      blob-URL/iframe assertion, a genuine low-probability race, not a
      one-off flake) resolved on fix/ISS-0817-attachment-viewer-flake
      (commit d5ba6aab post-rebase). ISSUE-FIXER diagnosed the root cause
      with 40 total repro attempts (0 failures on old code) and fully
      explained the mechanism by reading both effects: the container div
      commits to the DOM before the inner useEffect sets objectUrl and
      mounts the iframe, so an unlucky scheduling window let the assertion
      race against a not-yet-mounted iframe. FRONTEND-DEV fixed it by making
      the waitFor poll for the actual iframe's presence instead of just the
      container div, and independently proved the fix closes the real race
      via a temporary induced-delay stress test (old assertion shape fails
      deterministically under a forced 50ms delay, new shape passes 5/5
      under the same delay; delay then reverted, confirmed via
      `git diff --stat` that only the test file remains changed). REVIEWER
      PASSed cleanly (diff exactly one file, root-cause fix not a band-aid,
      no scope creep, independently re-ran `npm test` 890/890 and the fixed
      test file in isolation 8/8 clean). TEST-RUNNER PASSed (full backend
      4829/4832, 3 failures independently root-caused to pre-existing tracked
      unrelated flakes ISS-0766/ISS-0426; full frontend 890/890 clean; fixed
      test file run 10x isolated, 10/10 clean). DISCLOSED DEVIATION:
      RELEASE-VALIDATOR confirmed AC1 (58+ total clean runs, a real stated
      count) and AC2 (root cause genuinely explained) but stopped responding
      productively partway through AC3/AC4 (~49 minutes in, well past a
      reasonable window; a status nudge went unanswered). DOC-UPDATER
      confirmed via `ps aux` that no test process was actually running at
      that point and proceeded to close this issue on REVIEWER's and
      TEST-RUNNER's own independent, thorough confirmations of the identical
      scope, rather than wait further -- recorded honestly per this
      project's established norm of disclosing deviations. Status flipped to
      `resolved` in docs/issues/ISS-0817.yaml. No docs/migration/stage-N-*.md
      or decisions/*.md file was named by this issue's fix_direction. Merged
      via PR on fix/ISS-0817-attachment-viewer-flake.
  - req: SCOPE-CHANGE
    event: done
    agent: DOC-UPDATER
    at: "2026-09-25T00:40:32Z"
    note: >
      ISS-0772 (MINOR, queue task 807, GH-1795 -- Letflow.Secrets.LogFilter
      only redacted Logger metadata, never message-interpolated content, but
      the ISS-0771 design doc and MigrationReplayBoot's own moduledoc
      falsely claimed full coverage) resolved and merged, PR #1816 merged
      as e2298ea9. Fixed per suggested_fix option (b): converted the three
      Logger calls in migration_replay_boot.ex to pass tenant_id/
      schema_name/reason as Logger metadata instead of string-interpolating
      them, so LogFilter's existing metadata-only redaction genuinely
      covers this call site. Corrected both docs' overclaim in two stages:
      an initial pass, then a narrower follow-up fix after SECURITY-REVIEWER
      caught that the first correction was itself still a partial overclaim.
      Added 2 regression tests confirming the converted calls pass values as
      metadata, not interpolated into the message. Validation chain, all
      independently confirmed: SECURITY-REVIEWER (CONDITIONAL-PASS, one
      doc-overclaim finding, fixed in the same run), REVIEWER (PASS),
      TEST-DESIGN-VALIDATOR (PASS, real mutation-reproduction on 2 mutants),
      TEST-RUNNER (full suite 4835/4837, the 2 failures independently
      root-caused to a genuinely new, unrelated pre-existing bug filed
      separately as ISS-0827 -- test/letflow/router_test.exs's stale
      REQ-070 AC5 deferred-route count), RELEASE-VALIDATOR (PASS on all 3
      acceptance criteria, own fresh mutation-reproduction). DOC-UPDATER
      rebased the branch onto origin/main and ran `mix letflow.check`
      fresh: the only failure was `letflow.lint_handoffs`, 33 new
      violations under handoffs/WF03-BATCH-*-20260925/,
      handoffs/WF03-ISS0806-20260925/, and handoffs/WF03-ISS0808-20260925/
      -- wider than the 11-violation, WF03-ISS0808-only count ISS-0826's
      own resolution had recorded, since concurrent sessions kept landing
      new handoffs and the original ISS-0826 fix commit had missed each
      BATCH-* run's own step-05-issue-fixer.json/step-release-validator.json.
      Confirmed same class/root cause as ISS-0826 (no test failures, no
      compile errors, nothing touching this branch's own files) and updated
      ISS-0826 (commit 4a6016ba, pushed directly to main) to record the
      growth and clarify its `resolved` means "as investigated at the
      time," not "count is zero" -- an expected, self-refilling condition
      until concurrent sessions finish cleanly or adopt ISS-0819's
      single-file lint mode. ORCH then admin-merged PR #1816 after
      independently confirming CI's sole failure was exactly that same
      known lint_handoffs-only backlog (35 violations at merge time, same
      class). Status flipped to `resolved` in docs/issues/ISS-0772.yaml. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      issue's suggested_fix. README.md not touched -- no documented
      current-behavior surface describing LogFilter/MigrationReplayBoot
      logging to keep in sync. Queue task 807 released done.
  - req: REQ-401
    event: done
    agent: DOC-UPDATER
    at: "2026-09-25T03:06:46Z"
    note: >
      WF02-REQ401-20260925 Step 6 -- second requirement of S11 (modular-
      platform architecture), implementing 0039 D4 ("Permissions and who
      holds them") and the permission half of D5 (`:ModulesManage`). Full
      validation chain, all independently confirmed: SECURITY-REVIEWER PASS
      (rigorous hand-traced authorization answer-preservation proof),
      REVIEWER PASS (idiom/scope/decision-record), TEST-DESIGN-VALIDATOR
      PASS (3 real mutation-reproduction proofs), RELEASE-VALIDATOR PASS
      on all 8 acceptance criteria with rigorous evidence including live-
      fail probes for AC4/AC5 (temporarily emptied the fixture's
      route_policies, confirmed the enforcement tests genuinely fail,
      reverted, reconfirmed pass). Ships `Letflow.Api.Authorization.
      permissions/0` returning core's `@permissions` followed by every
      Catalog module's permissions via `Letflow.Modules.Catalog`;
      `role_allows?/2` falling back to the Catalog after every existing
      core clause's result is preserved unchanged (`PLATFORM_ADMIN`'s
      existing catch-all `true` at authorization.ex:1005 stays and
      therefore also covers module permissions, per 0039 REVIEWER R2); a
      new core permission `:ModulesManage` granted to `PLATFORM_ADMIN`
      only, same pattern as `:TenantsManage`; and `endpoint_policy_key/2`
      falling back, for `/modules/<id>/<rest>` paths, to the matching
      module's manifest `route_policies` entry. The ISS-0646 CANDIDATE
      test (authorization_test.exs:1830) still asserts exactly its
      original six exam-permission atoms unchanged -- the exam atoms stay
      in core in this requirement, moving only in REQ-408 (P2). DOC-UPDATER
      rebased feature/WF02-REQ401-20260925 onto latest origin/main (clean,
      1 commit behind, docs-only commit, no conflicts) and re-ran the
      diff-related suite fresh in the rebased worktree:
      `mix test test/letflow/api/authorization_test.exs
      test/letflow/api/authorization_enforcement_test.exs
      test/letflow/modules/catalog_test.exs` -> 143 passed, 0 failures.
      Host load was low at check time (loadavg ~1.25), so this fresh
      targeted run was taken in addition to, not instead of, RELEASE-
      VALIDATOR's own prior 143/143-twice confirmation; a full
      `mix letflow.check.test` was contention-poisoned earlier in the day
      per RELEASE-VALIDATOR's own report (host load spiked to 185,
      DBConnection timeouts in all 8 partitions) with zero failures
      touching lib/letflow/api/authorization.ex, lib/letflow/modules/
      catalog.ex, or their test files -- the only failures matched
      already-filed, unrelated issues (ISS-0827's stale router_test.exs
      count, ISS-0826's self-refilling handoff-lint backlog). Design:
      lib/letflow/design/req401-catalog-sourced-permissions.md. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria (0039 itself, the decision record
      this requirement implements, was not touched by the implementation --
      confirmed via diff -- and needed none, since it already documented
      D4/D5 as forward-looking design). README.md not touched -- grepped
      for "ModulesManage"/"module permission"/"role_grants": zero hits
      outside docs/migration/decisions/0039-platform-module-solution-
      layering.md, no documented current-behavior surface to keep in sync.
      Status flipped in docs/requirements.yaml. Full pipeline --
      CODE-DESIGNER, SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER,
      RELEASE-VALIDATOR -- on feature/WF02-REQ401-20260925, rebased onto
      origin/main (2a97b3f3) before this entry.

  - req: REQ-405
    event: doc-correction
    timestamp: 2026-09-25T03:39:10Z
    agent: DOC-UPDATER
    workflow: WF02-REQ405-20260925
    note: >
      REVIEWER flagged an inaccuracy in lib/letflow/design/req405-check-boundaries.md
      §1.2: the parse_xref_output Format contract prose incorrectly stated "Lines
      indented by one or more spaces are target paths", when the actual format emitted
      by `mix xref graph --format plain` uses tree characters (`|-- ` / ``-- ``),
      not space-indentation. The implementation in
      lib/mix/tasks/letflow.check_boundaries.ex correctly handles both tree characters
      (primary) and space-indented lines (compatibility fallback), but the design doc
      only described the fallback. Corrected §1.2 to: describe the tree-character
      format as primary with a sample block; note the space-indented fallback
      explicitly; confirm that `(compile)`/`(runtime)` suffix annotations are stripped.
      No status flip (REQ-405 was already done before this correction). No README.md
      change needed. One file touched: lib/letflow/design/req405-check-boundaries.md.
  - req: REQ-402
    event: done
    agent: DOC-UPDATER
    at: "2026-09-25T14:59:41Z"
    note: >
      WF02-REQ402-20260925 Step 6 -- third requirement of S11 (modular-
      platform architecture), implementing the persistence/install half of
      0039 D5. Full validation chain, all independently confirmed:
      CODE-DESIGN-VALIDATOR FAILed once (uncommitted design doc + real code
      disguised as pseudocode) then re-PASSed after fix; SECURITY-REVIEWER
      PASS (INV-1 cross-schema isolation verified, install/list/opts contract
      is prefix-only, no tenant_id/schema parameter, `git grep`-confirmed);
      REVIEWER PASS (idiom matches SolutionPack.install/3's existing
      transaction shape, third fixture module FixtureFailingInstall
      design-authorized, not scope creep); TEST-DESIGN-VALIDATOR PASS (4
      independent mutations reproduced fail-then-pass: dropped prefix,
      depends_on bypass, on_install rollback bypass, tenant_id param leak);
      TEST-RUNNER first pass showed 124 failures, root-caused to a since-fixed
      host-wide orphaned-docker-stacks RAM leak unrelated to this branch (see
      docs/anti-patterns.md's "git worktree remove never tears down the
      worktree's own docker-compose stack" entry), clean re-run 4875/4885
      passed, remaining 10 failures all confirmed pre-existing/unrelated;
      RELEASE-VALIDATOR PASS, independently re-ran and verified all 8
      acceptance criteria itself rather than trusting reports -- AC1-AC7
      directly re-executed and passing, AC8 (`mix letflow.check`) fails only
      on the pre-existing, already-documented ISS-0826 handoff-lint backlog
      (58 violations, confined to handoffs/ paths this branch's diff does not
      touch, confirmed via `git diff main...HEAD --stat`); the one open item,
      test/letflow/identity_test.exs REQ-019 AC1, was confirmed to be the
      known ISS-0766 flake by reproducing the identical failure on a clean
      main checkout with zero REQ-402 code present. Ships a tenant-scoped
      `tenant_modules` table (migration
      priv/repo/migrations/20260925000001_create_tenant_modules.exs, added to
      `Letflow.TenantProvisioning.tenant_scoped_migrations/0`, and to
      `test/support/tenant_fixture.ex`'s `@expected_tenant_tables`
      oracle-rot guard), `Letflow.Modules.TenantModule` (Ecto schema), and
      `Letflow.Modules.Installs` with `install/3`/`list_installed/1`
      (prefix-only tenant identification per INV-1; one `Repo.transaction`
      rejecting an unknown module id, an already-installed module, or an
      unmet `depends_on`; installs the manifest's `pack` through the existing
      `Letflow.Definitions.SolutionPack.install/3` when non-nil; calls
      `on_install/2` if exported; any step failing rolls back every write).
      Two new test-only fixture modules,
      `Letflow.Modules.FixtureDependent` and
      `Letflow.Modules.FixtureFailingInstall`, registered only in
      `config/test.exs`. DOC-UPDATER rebased feature/WF02-REQ402-20260925
      onto latest origin/main (one commit behind, a docs-only anti-patterns
      commit, clean rebase, no conflicts) before this entry. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria (0039 itself, the decision record
      this requirement implements, was not touched -- confirmed via diff --
      and needed no update, since it already documented D5 as forward-
      looking design). README.md not touched -- grepped for
      "tenant_modules"/"modules install"/"ModulesManage"/"tenant-scoped
      module": zero hits, no documented current-behavior surface to keep in
      sync, same pattern as REQ-400/REQ-401's own notes recorded. Status
      flipped in docs/requirements.yaml. Full pipeline -- CODE-DESIGNER,
      CODE-DESIGN-VALIDATOR, ELIXIR-DEV, SECURITY-REVIEWER, REVIEWER,
      TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER, RELEASE-VALIDATOR --
      on feature/WF02-REQ402-20260925 (0d13aece design, 58932ec0
      implementation after rebase onto 93f02a90).
  - req: REQ-403
    event: done
    agent: DOC-UPDATER
    at: "2026-09-25T16:55:44Z"
    note: >
      WF02-REQ403-20260925 Step 6 -- fourth requirement of S11 (modular-
      platform architecture), implementing the HTTP half of 0039 D5 for
      install and list. Full validation chain, all independently
      confirmed: CODE-DESIGN-VALIDATOR PASS (all 11 ACs traced, no
      embedded code -- DOC-UPDATER fixed a trivial AC10/AC11 table-
      numbering nit directly after the PASS, commit 2db7da72);
      SECURITY-REVIEWER PASS (INV-1 server-derived prefix confirmed on
      both routes -- neither `install/3` nor `list_installed/1` accepts
      a caller-supplied tenant_id/tenant/prefix, INV-2 response
      allowlist exactly module_id+version on the list route, the
      existing platform-admin gate on install is unweakened, and the
      new `:MyModulesRead` role-agnostic grant clause is structurally
      incapable of fail-opening any other permission); REVIEWER PASS
      (idiom matches the TenantSettings router precedent exactly, zero
      scope creep -- touches exactly 7 non-test files, D5 decision-
      record consistency confirmed against
      docs/migration/decisions/0039-platform-module-solution-layering.md,
      the ISS-0646 CANDIDATE test in authorization_test.exs correctly
      asserts exactly seven permissions via strict equality); TEST-
      DESIGN-VALIDATOR PASS (4 independent mutations reproduced
      fail-then-pass: prefix-spoofing bypass via body params, deletion
      of the `:MyModulesRead` grant clause, a response-field leak
      -- extra `installed_at` key on the list route -- and an
      error-status mapping change from 409 to 201 on re-install, each
      correctly caught by its corresponding AC test); TEST-RUNNER (full
      suite 4890/4898, all 8 failures independently classified
      pre-existing/unrelated: 3 RequirementStatusInvariantsTest, 1
      ISS-0831-class CheckIssueRefsTest, 1 ISS-0819-class
      LintHandoffsTest, 2 ISS-0827-class RouterTest stale deferred-route
      count, and 1 newly-identified-but-unrelated CheckBoundariesTaskTest
      AC2 bug confirmed identical on a clean main checkout and in
      REQ-405's own test, unrelated to this branch's diff; AC11
      `mix letflow.check` fails only on the pre-existing ISS-0826-class
      handoff-lint backlog, 58 violations, zero REQ-403 references);
      RELEASE-VALIDATOR PASS, independently re-ran everything itself
      rather than trusting reports (128/128 targeted tests, full suite
      reproducing the identical 8 known-unrelated failures on two
      separate runs), confirmed AC1-AC11 all hold against the real diff,
      and committed the test report file
      (test/reports/report-20260925-WF02-REQ403-20260925.yaml, tip
      6707f441). Ships `POST /api/v1/tenant/modules` (new
      `lib/letflow/routers/tenant_modules.ex`, forwarded from
      `lib/letflow/plugs/api_pipeline.ex`, gated by `:ModulesManage`,
      calling `Letflow.Modules.Installs.install/3` with a
      server-derived `prefix` from `conn.assigns.scoped_opts` only --
      201/404/409/422 per the requirement's response contract) and a new
      core permission `:MyModulesRead` in
      `lib/letflow/api/authorization.ex` granted to every role through
      one role-agnostic clause placed before all per-role clauses,
      gating a new `GET /api/v1/me/modules` route in
      `lib/letflow/routers/me.ex` that reads
      `Letflow.Modules.Installs.list_installed/1` (server-derived prefix
      only) and returns exactly `{module_id, version}` per entry. No
      `GET /api/v1/me` root route added; `:MembershipsRead` grants
      unchanged (confirmed via diff, per the requirement's own
      acceptance criteria). Branch feature/WF02-REQ403-20260925 was
      already ahead of origin/main by these 4 commits with no newer
      commits on origin/main to rebase onto at Step 6 time (checked via
      `git fetch origin && git log --oneline HEAD..origin/main`: empty).
      Design: lib/letflow/design/req403-module-install-route.md. No
      docs/migration/stage-N-*.md or decisions/*.md file was named by
      this requirement's acceptance criteria (0039 itself, the decision
      record this requirement implements, was not touched -- confirmed
      via diff -- and needed no update, since it already documented D5
      as forward-looking design). README.md not touched -- grepped for
      "module"/"me/modules"/"me/memberships": only pre-existing
      unrelated hits, no documented current-behavior surface describing
      a modules or /me API to keep in sync, same pattern as
      REQ-400/401/402's own notes recorded. Status flipped in
      docs/requirements.yaml. Full pipeline -- CODE-DESIGNER,
      CODE-DESIGN-VALIDATOR, ELIXIR-DEV, SECURITY-REVIEWER, REVIEWER,
      TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER,
      RELEASE-VALIDATOR -- on feature/WF02-REQ403-20260925 (66261e87
      design, 2db7da72 design fix, 7109d456 implementation, 6707f441
      test report).
  - req: REQ-404
    event: done
    agent: DOC-UPDATER
    at: "2026-09-25T18:56:16Z"
    note: >
      WF02-REQ404-20260925 Step 6 -- fifth requirement of S11 (modular-
      platform architecture), implementing 0039 D4's router-dispatch
      half and D5's 404-before-403 rule. SECURITY-REVIEWER FOUND AND
      REQUIRED A FIX FOR A REAL BLOCKER, worth flagging prominently: a
      genuine INV-5 timing side-channel in the first implementation --
      an unknown module id short-circuited with 0 DB round trips while a
      known-but-uninstalled module id cost 1 (the `Installs.installed?/2`
      lookup), leaking exactly the "does this module exist at all" vs
      "have you installed it" distinction D5 exists to hide, observable
      to any caller via response timing even though both cases returned
      byte-identical 404 bodies. Fixed via a design amendment (req404
      design doc §9) plus a code patch (commit 027493c9) moving
      `Installs.installed?/2` to run unconditionally as the first
      statement of `resolve/2`, before the module-id-exists branch, so
      both paths now cost the same DB round trip regardless of outcome.
      SECURITY-REVIEWER re-verified PASS after the fix, tracing all 4
      code paths (unknown id, known-uninstalled, known-installed-no-
      router, known-installed-with-router) itself against the patched
      code. REVIEWER PASS (idiom, zero scope creep, D4/D5 decision-
      record consistency, the new `required_permission/1` fallback
      clause in `lib/letflow/api/authorization.ex` correctly placed
      after existing core clauses). TEST-DESIGN-VALIDATOR PASS -- 4
      independent mutations reproduced fail-then-pass, including
      reintroducing the EXACT original INV-5 bug, caught by a new
      telemetry-based regression test asserting equal DB query counts
      across both 404 paths with concrete query-count evidence.
      TEST-RUNNER: full suite 4896/4904, all 8 failures independently
      classified pre-existing/unrelated (2 ISS-0827-class RouterTest
      stale deferred-route count, 1 ISS-0832-class
      CheckBoundariesTaskTest AC2 partition-env bug, 1 ISS-0831-class
      CheckIssueRefsTest, 3 newly-identified-but-unrelated
      RequirementStatusInvariantsTest failures tracing to REQ-405's own
      malformed status-volume entry -- see this same volume's
      `event: doc-correction` REQ-405 entry above, a pre-existing known
      anomaly, not introduced by this branch -- and 1
      newly-identified-but-unrelated failure from a BOM-corrupted
      handoffs/registry.json; none touch
      lib/letflow/routers/modules.ex, lib/letflow/plugs/api_pipeline.ex,
      lib/letflow/modules/installs.ex, lib/letflow/api/authorization.ex,
      or test/support/modules/fixture/router.ex). RELEASE-VALIDATOR
      PASS -- independently re-derived all 6 acceptance criteria,
      re-traced the INV-5 fix itself by reading the actual code and
      watching live telemetry output rather than trusting the report,
      re-ran the full suite and reproduced the identical 4896/4904
      result, and confirmed the test report file
      (test/reports/report-20260925-WF02-REQ404-20260925.yaml) is
      committed. One flagged, non-blocking caveat: AC6's literal
      wording ("mix letflow.check passes") is technically false only
      because of the pre-existing, unrelated ISS-0826-class
      handoff-lint backlog (58 violations, zero REQ-404 references) --
      same precedent as REQ-400/401/402/403's own merges, not a REQ-404
      defect. Ships `lib/letflow/routers/modules.ex`
      (`Letflow.Routers.Modules`, a plain Plug dispatcher forwarded from
      `lib/letflow/plugs/api_pipeline.ex` under `/modules`), gating
      every `/api/v1/modules/<id>/<rest>` request on
      `Letflow.Modules.Installs.installed?/2` (server-derived `prefix`
      from `conn.assigns.scoped_opts`, INV-1) before any permission
      check runs, so an uninstalled or nonexistent module id yields 404
      for every role including PLATFORM_ADMIN, never 403; installed
      modules dispatch `<rest>` to the module's own `router/0` Plug,
      whose `authz_*` routes enforce permissions as today via the new
      `required_permission/1` fallback clause in
      `lib/letflow/api/authorization.ex`. `test/support/modules/fixture/
      router.ex` converted to an `AuthorizedRouter` to prove the
      dispatch end-to-end. Branch feature/WF02-REQ404-20260925 confirmed
      NOT behind origin/main at Step 6 time (`git fetch origin &&
      git log --oneline HEAD..origin/main`: empty) -- no rebase needed.
      Design: lib/letflow/design/req404-module-router-mount.md (one
      CODE-DESIGN-VALIDATOR rework round pre-implementation, false
      precedent citation removed; §9 INV-5 timing-parity amendment
      added post-SECURITY-REVIEWER finding). No docs/migration/
      stage-N-*.md or decisions/*.md file was named by this
      requirement's acceptance criteria (0039 itself, the decision
      record this requirement implements, was not touched -- confirmed
      via diff -- and needed no update, since it already documented
      D4/D5 as forward-looking design). README.md not touched --
      grepped for "module": only pre-existing unrelated hits (mix app
      description, moduledoc cross-references), no documented
      current-behavior surface describing a modules-router API to keep
      in sync, same pattern as REQ-400/401/402/403's own notes
      recorded. Status flipped in docs/requirements.yaml. Full pipeline
      -- CODE-DESIGNER, CODE-DESIGN-VALIDATOR, ELIXIR-DEV,
      SECURITY-REVIEWER (found+required the INV-5 fix above),
      REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER,
      RELEASE-VALIDATOR -- on feature/WF02-REQ404-20260925 (f7d99ec6
      design, 6ec93834 design fix, 31fc03d6 implementation, 02eb13e9
      INV-5 design amendment, 027493c9 INV-5 code fix, e7dbd36a test
      report).

  - req: REQ-409
    status: done
    timestamp: 2026-09-26T00:10:00Z
    run_id: WF02-REQ409-20260926
    agent: ELIXIR-DEV
    summary: >
      Tenant-scoped migration 20260926010001 backfills one tenant_modules row
      (module_id: "exam", version: "0.1.0") for every tenant whose
      solution_pack_installs table has a pack_id = 'bilimbaga-question-bank' row.
      Uses ON CONFLICT (module_id) DO NOTHING for idempotency. Tenant UUID
      derived from schema name via tenant_id_for_schema_name/1; no SQL
      string interpolation of tenant-controlled data (INV-7). Added to
      tenant_scoped_migrations/0 after 20260925000001. 4/4 tests pass (tenant
      with pack gets exam row, tenant without gets nothing, different pack gets
      nothing, idempotency confirmed). SECURITY-REVIEWER PASS (all INV-1..INV-8
      satisfied). Status flipped in docs/requirements.yaml. Commit: 64e76394.
  - req: REQ-410
    status: done
    timestamp: 2026-09-26T01:20:00Z
    run_id: WF02-REQ410-20260926
    agent: ELIXIR-DEV
    summary: >
      Moved lib/letflow/exam/*.ex -> lib/letflow/modules/exam/ (namespace
      Letflow.Exam.* -> Letflow.Modules.Exam.*), lib/letflow/routers/exam_sessions.ex
      -> lib/letflow/modules/exam/router.ex (Letflow.Routers.ExamSessions ->
      Letflow.Modules.Exam.Router). Moved test files. Updated exam.ex router/0 to
      return Letflow.Modules.Exam.Router. Removed forward('/exam-sessions', ...) from
      api_pipeline.ex. Updated all live callers (enforcement test, exam_fixtures.ex,
      seed task, UAT scenarios). Exam routes now served at
      /api/v1/modules/exam/exam-sessions/*. All 7 AC pass: old paths gone, new
      HTTP-level tests pass (404 without row, 200 with), authorization_enforcement
      passes with new paths, 147 moved tests pass. mix letflow.check_boundaries 0
      violations. SECURITY-REVIEWER PASS. Commit: 24eab9a5.
  - req: REQ-412
    status: done
    timestamp: 2026-09-26T02:00:00Z
    run_id: WF02-REQ412-20260926
    agent: FRONTEND-DEV
    summary: >
      Moved web/src/pages/exam/ and web/src/pages/admin/bilimbaga/ into
      web/src/modules/exam/ (git mv). Moved exam.ts, types/exam.ts,
      examMessages.ts, useAntiCheatSignals.ts. Created modules/exam/index.ts
      with ModuleDefinition (5 routes, 2 nav items). Created ModuleGuard.tsx
      for 404-gate at route level. Updated all /exam-sessions URLs to
      /modules/exam/exam-sessions. Removed exam routes/nav from router.tsx and
      AppShell.tsx. Updated e2e tests. npm run check: 930 tests pass, 0
      boundary violations. All 5 ACs pass. Commit: be729822.
  - req: REQ-411
    status: done
    timestamp: 2026-09-26T02:30:00Z
    run_id: WF02-REQ411-20260926
    agent: ELIXIR-DEV
    summary: >
      priv/packs/bilimbaga/ moved to priv/modules/exam/ (git mv). Exam
      manifest pack field set to 'modules/exam/pack.json'. on_install/2
      seeds 4 answer-key entity_field_restrictions rows. Deleted
      lib/letflow/packs/bilimbaga.ex; removed seed_pack_specific_field_
      restrictions/2 and Bilimbaga alias from solution_pack.ex. Added
      module_pack_ids/0 to catalog.ex. Added HTTP 409 module_owned_pack
      check to solution_packs.ex router before any write. All 8 ACs
      pass. SECURITY-REVIEWER PASS. Commits: be729822 (changes bundled
      into REQ-412) + 5732e5aa (format fixes).
  - req: REQ-413
    status: done
    timestamp: 2026-09-26T03:00:00Z
    run_id: WF02-REQ413-20260926
    agent: ELIXIR-DEV
    summary: >
      P2 close-out. Removed ExamSession*/ExamCertificateIssue dead code from
      authorization.ex (type unions, @permissions, endpoint_policy_key clauses,
      required_permission clauses, CANDIDATE core clause replaced with false).
      Updated authorization_test.exs: @core_permissions now 34, @pre_req401 now
      32. Updated .claude/agents/ba-analyst.md, product-owner.md, and
      docs/agents/ba-personas/bilimbaga.yaml URL strings. Updated stage-11 module
      inventory exam row (3530 lines, 2026-09-26). AC1: 0 grep hits. AC2:
      check_boundaries 901 edges 0 violations. AC3: s11_measurements_test passes,
      role_from_string CANDIDATE count=1. AC4: all 4 old paths absent. AC5: mix
      letflow.check passes, npm run check passes. AC6: Playwright deferred (no
      running stack). AC7: inventory updated. Commit: 74f106ff.
  - req: REQ-414
    status: done
    timestamp: 2026-09-26T04:00:00Z
    run_id: WF02-REQ414-20260926
    agent: ELIXIR-DEV
    summary: >
      Added Installs.put_settings/3 validating settings against manifest
      settings_schema (uses existing JsonSchema.validate/2, no new dep).
      Added TenantModule.settings_changeset/2. Added PUT /api/v1/tenant/
      modules/:module_id/settings gated by :ModulesManage returning 200/
      422/404. Added endpoint_policy_key clause. 15 tests pass: 200 for
      valid body, 422 for type error, 422 for extra key (additionalProperties),
      403 for 5 roles, 404 uninstalled, two-tenant isolation. nil-schema
      accepts only empty map. SECURITY-REVIEWER PASS (INV-1..INV-5 verified).
      No new hex dep. Commit: 73e7102e.
  - req: REQ-415
    status: done
    timestamp: 2026-09-26T05:10:00Z
    run_id: WF02-REQ415-20260926
    agent: ELIXIR-DEV
    summary: >
      Created priv/solutions/bilimbaga.json (exam module at 0.1.0).
      Created lib/letflow/modules/solutions.ex (5th D3 core file) with
      load/1 (path-traversal prevention, version validation) and install/3
      (single transaction, topological ordering, pre-check for already-
      installed). Created POST /api/v1/tenant/solutions (201/403/404/409).
      17 new tests all pass: topo-order, atomicity, load validation, role
      gate, two-tenant isolation, no migration created (D6). SECURITY-
      REVIEWER PASS (all INV-1..INV-5 verified). Commit: be133e54.
  - req: REQ-416
    status: done
    timestamp: 2026-09-26T06:00:00Z
    run_id: WF02-REQ416-20260926
    agent: ELIXIR-DEV
    summary: >
      Created bilimbaga_solution_e2e_test.exs: 7-step integration test
      through real routes, no mocks. Step 1: fresh tenant provision.
      Step 2: POST /tenant/solutions -> 201. Step 3: GET /me/modules lists
      exam; entity defs and on_install/2 field restrictions verified.
      Step 4: exam data seeded via ExamFixtures (no SQL). Step 5: CANDIDATE
      full exam path (list/start/save/submit/score/certificate) all pass,
      passed: true. Step 6: TASK_WORKER query answer_option -> answer-key
      fields redacted. Step 7: second tenant without solution -> 404 for
      all 6 roles. All 6 ACs pass. mix letflow.check passes. Updated
      stage-11-modular-platform.md status to P1-P3 done. Commit: 29513e28.
      S11 (REQ-400..416) is fully complete.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: 2026-09-26T12:03:14Z
    note: >
      Queue/yaml reconciliation per ISS-0848 step 1. Released as done
      (queue status was open/blocked-mismatched, yaml already done):
      task 252 (REQ-122), 256 (REQ-123), 734 (REQ-373, was queue-blocked),
      794 (REQ-405, force-released from a stale orch-main lock), 797-805
      (REQ-408..REQ-416), 816 (ISS-0816, force-released from a stale
      orch-main-20260925 lock). EXCEPTION per ISS-0836: tasks 795 (REQ-406)
      and 796 (REQ-407) released as blocked, not done -- their yaml
      status: done was never RELEASE-VALIDATOR-verified per ISS-0836's own
      findings (task 795 had been mistakenly released done by ORCH in a
      prior turn before the ISS-0836 collision was fully understood;
      corrected here to blocked for consistency with 796). GET /tasks
      re-pull after all releases confirms every listed id now matches
      this exception pattern. Also flagging: this volume's REQ-409..REQ-416
      entries use req/status/timestamp/run_id/agent/summary fields, not
      the documented req/event/agent/at/note schema
      test/support/status_history.ex actually parses -- likely the same
      root cause as ISS-0833 (malformed REQ-405 entry) but far more
      widespread; noted in ISS-0833 as a follow-up rather than fixed here
      (out of scope for ISS-0848).
# VOLUME 22 — CLOSED 2026-09-26. DO NOT APPEND TO THIS FILE.
# This volume holds the complete requirement run history from 2026-09-22 through
# 2026-09-26 (38 entries, 1259 frozen-prefix lines before this closure footer).
# It is frozen: every line above this comment is preserved byte-for-byte. Closed
# by ORCH's own SCOPE-CHANGE (ISS-0848 queue/yaml reconciliation) append, which
# brought this file to 1259 lines -- over the declared roll_rule ceiling
# (1200 lines / 120000 bytes). No entry above this footer was moved, edited or
# deleted.
#
# Successor:
#   docs/status/requirement_status.v23.yaml
#
# which, as of this closure, names:
#   docs/status/requirement_status.v23.yaml
#
# KNOWN ANOMALIES IN THIS VOLUME: the "doc-correction" event value at line 859
# (undeclared vocabulary, pre-existing, see ISS-0833) and the
# req/status/timestamp/run_id/agent/summary shape used by the REQ-409..REQ-416
# entries instead of the documented req/event/agent/at/note schema (also
# pre-existing, see ISS-0833's 2026-09-26 addendum) -- neither introduced by
# this closure, both flagged for a future fix rather than corrected here.
