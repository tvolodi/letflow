# Letflow — Requirement run history — VOLUME 3 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v2.yaml (volume 2, closed,
#   1,429 frozen-prefix lines / 1,464 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v3.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v3.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-08-22T09:30:45Z"
    note: >
      WF03-ISS0263-20260822 (queue task 263, GH#518) done. Rolled
      docs/status/requirement_status.v2.yaml (1407 lines / 92601 bytes / 43
      entries, over its declared roll_rule ceiling of max_lines: 1200) per
      its own "WHEN THIS VOLUME IS FULL" procedure: appended this run's
      started-event to v2.yaml (bringing it to 44 entries / 1429 frozen-prefix
      lines), appended v2.yaml's closure footer (modeled on volume 1's
      precedent), set volume 2 status: closed / frozen_prefix_sha256 in the
      index, created this file (requirement_status.v3.yaml) with volume 2's
      header verbatim (path references updated) and an empty history, and
      set volume 3 current in the index. Reconciliation: 43 entries in
      v2.yaml before this run touched it; 44 in the closed v2.yaml after
      (43 pre-existing + this run's own started-event); this done-event is
      entry 1 of the new current volume 3 -- so all 43 pre-existing entries
      plus both of this run's own events (44 total in v2, 1 in v3) are
      accounted for, none rewritten/reordered/deleted (`git diff --numstat`
      on v2.yaml showed insertions-only throughout).
      `mix test test/docs/requirement_status_invariants_test.exs`: 19/20
      before the fix (A3 failing: measured 1407 > max_lines 1200), 20/20
      after. Full suite: 1717/1718 passed; the 1 failure
      (Mix.Tasks.Letflow.CheckDeferralStalenessTest, S8/S9 staleness
      assertion) is structurally unrelated -- `git status --porcelain`
      shows only docs/status/*.yaml changed by this run, and neither that
      test file nor its module/docs/requirements.yaml dependency is among
      them -- filed as a finding for ORCH to register (ISS-0263 itself
      stays scoped to the roll). New current volume (v3.yaml) is 168 lines /
      10032 bytes, well under ceiling.
  - req: REQ-128
    event: started
    agent: ELIXIR-DEV
    at: "2026-08-22T09:26:00Z"
    note: >
      WF02-REQ128-20260822. Add Keycloak to the dev stack: docker-compose
      service, five-role realm import (decisions/0013), real local issuer
      in config/dev.exs/test.exs. Appended here (volume 3), not volume 2
      (chronologically current when this run's own started-event first
      landed): ISS-0263's roll (commit b6cf798, merged during this run)
      closed and froze volume 2 byte-for-byte, so this event lands in
      whatever volume is current now, per that volume's own rule -- same
      as ISS-0263's own started/done pair landed one in each volume across
      its roll boundary.
  - req: REQ-128
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-22T10:04:09Z"
    note: >
      Implemented per the CODE-DESIGN-VALIDATOR-approved design
      (lib/letflow/design/req128-keycloak-dev-stack.md): keycloak service
      added to docker-compose.yml (host port via LETFLOW_KEYCLOAK_PORT/
      config/keycloak_port.exs, default 8082 -- avoids R-Co's 8081/5432/
      5433, verified by reading R-Co's own docker-compose.yml directly),
      priv/keycloak/realms/bpm-default.json defining all five
      Letflow.Api.Authorization roles per decisions/0013 (operator-user
      seeded with PROCESS_OPERATOR, not R-Co's PLATFORM_ADMIN), a
      realm-roles protocol mapper projecting a flat top-level roles claim,
      no nginx gateway. config/dev.exs and config/test.exs point :oidc at
      the real local issuer; config/prod.exs deliberately unchanged
      (git diff 96ac42f..HEAD -- config/prod.exs empty) -- no real
      production issuer exists yet. Realm kept named bpm-default, NOT
      renamed, after implementation-time discovery that it is a pinned
      domain invariant (Letflow.Identity.Tenant's @default_tenant_slug and
      create_changeset/3 pinning, plus dozens of existing tests) --
      documented as a correction in the design doc rather than silently
      diverging from the validated design. A second commit added
      :allow_unsafe_http (default false, set true only in dev.exs/test.exs)
      threaded into Oidcc.ProviderConfiguration.Worker's
      provider_configuration_opts, required because oidcc rejects a
      non-https userinfo_endpoint by default and the local Keycloak has no
      TLS termination -- config/prod.exs does not set the key, so a real
      deployed issuer keeps oidcc's safe https-only validation.
      SECURITY-REVIEWER PASSed (INV-4 applies/satisfied; INV-1/2/3/5/6/7/8
      not applicable -- no schema/migration/API-route/SQL touched).
      REVIEWER PASSed (idiom, the bpm-default correction judged correct
      not scope creep, no ownership violation, decision-record
      consistency). TEST-DESIGNER's first pass concluded zero new test
      code was needed; TEST-DESIGN-VALIDATOR FAILed that (the
      allow_unsafe_http default-false branch in application.ex had no
      regression guard); TEST-DESIGNER added
      test/letflow/application_test.exs's new test reading config/prod.exs
      directly via Config.Reader.read!/2 and asserting no :allow_unsafe_http
      key; TEST-DESIGN-VALIDATOR re-ran PASS (verified by mutating
      config/prod.exs, confirming the test fails, then reverting). Full
      suite: 1717/1719 passed (2 pre-existing, unrelated failures --
      RequirementStatusInvariantsTest A3, now fixed on main by ISS-0263's
      roll and no longer expected to fail post-rebase; and
      CheckDeferralStalenessTest's S8/S9 staleness check against
      docs/requirements.yaml, still open, unrelated -- neither touches
      OIDC/Keycloak/application.ex). All nine acceptance criteria verified
      live against the running Keycloak container: healthcheck healthy,
      discovery document fetched (issuer matches), five roles read back
      via Admin REST API, operator-user confirmed PROCESS_OPERATOR only,
      decoded token payload shows flat top-level roles claim,
      Oidcc.ProviderConfiguration.Worker reached ready state (no retry)
      via a throwaway mix run script. RELEASE-VALIDATOR independently
      re-derived all nine criteria and the test attribution from scratch
      (not trusting this run's own reports) -- PASS. Status flipped
      pending -> done here.
  - req: REQ-118
    event: started
    agent: ELIXIR-DEV
    at: "2026-08-22T10:09:04Z"
    note: >
      WF02-REQ118-20260822 (queue task 235) started. CORS support for the
      browser-origin SPA -- allowed-origin list from config (mirroring
      :problems_base_uri's Application.compile_env pattern), no wildcard
      default, tenant-data-path change so SECURITY-REVIEWER is a hard gate.
  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-08-22T10:13:48Z"
    note: >
      WF03-ISS0264-20260822 (queue task 264, GH#520) done. Fixed
      test/mix/tasks/letflow_check_deferral_staleness_test.exs's
      T-LIVE-ACTIVITY test, whose hardcoded active/inactive stage literal
      went stale once S8/S9 became genuinely active tonight (real
      REQ-115..139 requirements registered, some done/in_progress).
      Detector logic (lib/mix/tasks/letflow.check_deferral_staleness.ex) and
      docs/requirements.yaml both unchanged -- test-data-only fix, per
      CODE-DESIGNER's design (lib/letflow/design/iss0264-deferral-staleness-test-literal-update.md,
      CODE-DESIGN-VALIDATOR PASS after one rework round correcting a
      mis-cited mutant in the design's own supporting narrative). Chose to
      keep the test pinned (not converted to the unpinned/derived T-ROSTER
      pattern from ISS-0231), because ISS-0258's own design already ruled
      this test a genuine MS2/MS3 discriminator, unlike T-ROSTER where
      pinned and unpinned forms were equally discriminating. REVIEWER PASS
      (scope: only the test file + design doc changed; SECURITY-REVIEWER out
      of scope, no tenant-data path). TEST-DESIGNER/TEST-DESIGN-VALIDATOR
      confirmed the existing test itself is the fail-then-pass regression
      proof (pre-fix 56/57 with this test the sole failure, reproduced twice
      independently; post-fix 57/57, confirmed three times). Full suite via
      scripts/test_parallel.sh: 1360/1718 passed, 358 failures, all
      structurally attributed to shared-host contention (~15 concurrent
      sibling worktrees tonight) and not this branch -- git diff shows only
      the two files above changed, and a standalone re-run of one heavily
      affected file (registry_test.exs, 93 of the 358 failures) passed
      28/28 clean.
  - req: REQ-118
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-22T10:29:11Z"
    note: >
      WF02-REQ118-20260822 (queue task 235) done. Added Letflow.Plugs.Cors,
      mounted in Letflow.Router ahead of both the /api/tenant-config and
      /api/v1 forwards. Allowed origins from config
      (:letflow, :cors_allowed_origins), read via Application.get_env/3 at
      request time (deliberately not compile_env, unlike :problems_base_uri
      -- origins differ per deploy env, no rebuild needed to change them),
      default ["http://localhost:5173", "http://127.0.0.1:4173"] (no
      wildcard); prod (config/runtime.exs) sources CORS_ALLOWED_ORIGINS,
      fails closed to [] when unset. No Access-Control-Allow-Credentials
      ever emitted (SPA's bearer token is held in-memory, sent via
      Authorization header, never cookies -- confirmed by grep of
      web/src/api/client.ts, independently re-checked by
      SECURITY-REVIEWER and RELEASE-VALIDATOR). Preflight (OPTIONS)
      implemented (Authorization is not CORS-safelisted, so the browser
      preflights every authenticated call) -- 204 short-circuit with the
      full preflight header set. Unlisted origin gets no CORS headers
      (rejection is header-omission, browser-enforced, not a server 403).
      Pipeline: CODE-DESIGNER -> CODE-DESIGN-VALIDATOR (PASS) ->
      implementation -> SECURITY-REVIEWER (PASS, all INV-1..INV-8 assessed)
      -> REVIEWER (PASS) -> TEST-DESIGNER (18 tests, all router-dispatched
      via Letflow.Router.call/2) -> TEST-DESIGN-VALIDATOR (PASS) ->
      TEST-RUNNER (1733/1734 full suite; lone failure is pre-existing
      unrelated S8/S9 stage-activation drift,
      Mix.Tasks.Letflow.CheckDeferralStalenessTest, tracked earlier in this
      volume) -> RELEASE-VALIDATOR (PASS, independently re-derived). No
      wildcard default anywhere; unlisted-origin rejection covered by a
      test asserting header absence (not a status code). No changes under
      web/ (git diff --stat -- web/ empty throughout). No FAIL-then-fix
      cycles this run -- every gate passed on first attempt.
  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-08-22T10:35:22Z"
    note: >
      WF03-ISS0265-20260822 (queue task 265, GH#523) done. Fixed the H1/H3
      handoff-lint violation on
      handoffs/WF03-ISS0260-20260822/step-04d-test-design-validator-recheck.json
      (introduced by commit dff2270/PR #515, first surfaced during
      WF03-ISS0262-20260822 but structurally unrelated to it). Chose to
      reshape the file to the standard handoff envelope rather than
      grandfather it, since it was a one-off deviation -- sibling files
      step-04b/04c/04e in the same run already used the standard schema.
      Every original field/value was preserved unchanged, only re-nested
      under `result`. `mix letflow.lint_handoffs`: FAIL -- 2 new violations
      before, OK -- 0 new violations (811 files, 25 grandfathered) after.
      Scoped test test/mix/tasks/letflow.lint_handoffs_test.exs: 17/20
      passed both before and after (confirmed via git stash), same 3
      pre-existing H6 floor-commit failures unrelated to this change (diff
      touches only the one handoffs/ JSON file, never lib/ or test/).
  - req: REQ-120
    event: started
    agent: CODE-DESIGNER
    at: "2026-08-22T11:20:23Z"
    note: >
      WF02-REQ120-20260822 (queue task 239) started. Settling the
      design-token question -- one source or a recorded decision not to
      have one -- among the three unreconciled positions (design-system.md
      tokens.css model, design-tokens/letflow.tokens.json, inline hex
      literals).
  - req: REQ-120
    event: done
    agent: CODE-DESIGNER
    at: "2026-08-22T11:20:23Z"
    note: >
      WF02-REQ120-20260822 done. Chose design-system.md's palette as the
      one source of truth (lib/letflow/design/req120-design-token-source.md).
      design-tokens/letflow.tokens.json rejected (smaller, no neutral
      scale, conflicting hex values with design-system.md, wired to
      nothing per grep). Inline hex literals rejected as no design system
      at all. literal-colour guard pattern's target change specified
      (drop semicolon-lookahead, keep tokens.css allowedPaths exemption)
      -- not implemented here. Follow-on requirement REQ-141 drafted
      (impl_order: UNREGISTERED) to build tokens.css and migrate
      web/src/components/'s 47 files / 496 hex-literal occurrences (real
      grep count, re-verified by CODE-DESIGN-VALIDATOR independently).
      docs/frontend/design-system.md and web/README.md corrected to match
      the decision. CODE-DESIGN-VALIDATOR: PASS, all 5 acceptance
      criteria verified independently, no implementation code found, no
      file under web/src/components/ modified. SECURITY-REVIEWER out of
      scope (no tenant-data path -- docs/design artefact only, git diff
      confirms only docs/frontend/design-system.md, docs/requirements.yaml,
      web/README.md, and the new lib/letflow/design/ file touched). No
      executable surface produced (docs-only) -- TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR/TEST-RUNNER skipped per WF-02's scope test,
      verified instead by `git diff --stat` confirming zero changes under
      web/src/components/.
