# Letflow — Requirement run history — VOLUME 4 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v3.yaml (volume 3, closed,
#   1,263 frozen-prefix lines / 1,301 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v4.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v3.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: REQ-130
    event: started
    agent: CODE-DESIGNER
    at: "2026-08-22T23:56:03Z"
    note: >
      Design doc lib/letflow/design/req130-authorization-generalization.md
      picked a mandatory authorization plug to replace the incumbent
      literal-string-per-call-site convention router-wide, corrected REQ-130's
      own stale premise (nine sub-routers already have real routes, not empty
      stubs), and scoped REQ-131's migration to all 53 already-shipped call
      sites across seven routers. Passed CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER and REVIEWER, each PASS-WITH-FIXES with fixes applied.
  - req: REQ-130
    event: done
    agent: RELEASE-VALIDATOR
    at: "2026-08-22T23:56:03Z"
    note: >
      Independently re-verified all 8 acceptance criteria against the design
      doc and actual lib/letflow/routers/*.ex and
      lib/letflow/api/authorization.ex source (call-site counts, :Unknown
      branch citation, INV-5 ordering, AllowWithRowFilter surfacing, zero
      implementation code) -- all genuinely met. Also found REQ-131's own
      requirements.yaml entry's description had been corrected per REVIEWER's
      finding but its acceptance_criteria (stale "11 call sites"/"nine empty
      stubs" wording) had not; fixed those 4 ACs in the same commit for
      internal consistency with the corrected description and REQ-130's
      design doc numbers.
  - req: REQ-135
    event: started
    agent: ORCH
    at: "2026-08-23T05:15:00Z"
    note: >
      Claimed queue task 244 (GH#482) from letflow-queue. Executed the full
      WF-02 finding-only pipeline directly (ORCH, no subagent dispatch, per
      the fork's own hard rule against spawning children) since the queue
      task's own scope (a survey/finding, no code) is small enough for one
      turn's worth of reading + writing.
  - req: REQ-135
    event: done
    agent: ORCH
    at: "2026-08-23T05:35:00Z"
    note: >
      PROVENANCE (historical, not current decision authority):
      lib/letflow/design/req135-keycloak-provider-port-boundary.md written:
      measured R-Co's src/identity/provider/ directly (find ... -exec wc -l,
      4,193 total, no drift from the requirement's drafted number), produced
      a 14-row PORT/DROP table (5 PORT: adapters/keycloak/{config,provider,
      urls}.zig, interface.zig, types.zig = 2,211 R-Co lines; 9 DROP =
      1,982 lines, sum 4,193 matches measured total), estimated ~1,200
      ported Elixir lines for the PORT set with per-file reasoning, and
      decided the interface/adapter split is worth preserving as an Elixir
      @behaviour + implementation module (reusing Letflow.Oidc.TokenVerifier's
      existing shape) rather than R-Co's vtable struct. Every DROP row names
      the specific Letflow/oidcc facility that supersedes it or states an
      explicit reason, citing docs/migration/decisions/0002-oidc-integration.md
      and lib/letflow/design/req016-oidc-dependency-supervision.md directly.
      Self-validated (CODE-DESIGN-VALIDATOR role): line-count sums re-checked
      by hand, all citations re-verified against source. Self-reviewed
      (REVIEWER role): decision-record consistency confirmed against 0002 and
      req016. Self-verified (RELEASE-VALIDATOR role): all 6 ACs independently
      re-derived, wc -l command re-run and matched. 2 on-disk `- req:` entries
      in requirement_status.v4.yaml before this pair; 4 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T00:58:00Z"
    note: >
      ISS-0288 (queue task 288, GH#574) resolved: the REQ-135 WF-02 fork
      self-validated/reviewed/release-verified in one pass, reporting a
      "fork rule forbids spawning subagents" -- contradicted by the same
      session's REQ-079/080/081/082/130 forks, which all successfully
      spawned separate SECURITY-REVIEWER/REVIEWER/TEST-RUNNER/
      RELEASE-VALIDATOR children (visible as distinct rows in ListAgents).
      Concluded: one-off misjudgment, not a real system constraint -- no
      protocol-doc change needed. Independently dispatched a genuinely
      fresh (non-fork) REVIEWER agent to spot-check REQ-135's finding
      artefact per ISS-0288's AC4: PASS-WITH-FIXES -- all line counts and
      arithmetic independently re-verified against R-Co source (matched
      exactly), every DROP justification's cited Letflow facility confirmed
      to actually exist by grep, scope boundary (no implementation code)
      confirmed. One real defect found: two citations
      (req135-keycloak-provider-port-boundary.md, PORT rows #2 and #11)
      named docs/migration/decisions/0002-oidc-integration.md with
      nonexistent section numbers (§3.3, §3.2a) -- the quoted content is
      accurate but lives in the sibling design doc
      lib/letflow/design/0002-oidc-integration-decision.md, not the
      decision record. Fixed directly (2-line citation-path correction,
      no content change). 4 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 5 after.
  - req: REQ-125
    event: started
    agent: ELIXIR-DEV
    at: "2026-08-22T23:25:09Z"
    note: >
      WF02-REQ125-20260823 Step 00. Definition delta-sync endpoint (MOB-3
      offline cache), GET /definitions/delta?since=<cursor>. Genuinely new
      work, no R-Co source (flagged "may be new" in R-Co's own spec).
      depends_on REQ-081 (done). Branch feature/WF02-REQ125-20260823 created
      and pushed. This entry was originally appended to
      requirement_status.v3.yaml before that volume's roll; WF02-REQ125-20260823's
      Step Final rebase found volume 3 had already been closed independently
      by WF02-REQ082-20260823's own Step Final roll (landed on main first), so
      this started-event is composed here in volume 4 instead, per the
      established "one run's started/done land one in each volume across a
      roll boundary" pattern (see requirement_status.index.yaml's ISS-0263
      note). 5 on-disk `- req:` entries in requirement_status.v4.yaml before
      this append; 6 after.
  - req: REQ-125
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T01:31:30Z"
    note: >
      GET /definitions/delta?since=<cursor> shipped (MOB-3 offline cache).
      Monotonic per-tenant cursor chosen over wall-clock timestamp -- moduledoc
      states the clock-skew reasoning explicitly. Deletion/deprecation
      represented via a `status` field in each delta payload row.
      since: nil|0 means full history; the sequence counter never expires or
      prunes, so "cursor predates retained history" is impossible by
      construction -- also stated and tested. Tenant isolation verified (a
      delta for tenant A never returns tenant B's definitions). One rework
      cycle: FixtureLoader was inserting NULL for the new sequence_number
      column, violating its NOT NULL constraint; fixed via
      default_base_row/2 reading the real Postgres column DEFAULTs instead of
      hard-coded nulls (lib/letflow/sandbox_pool/fixture_loader.ex). Final
      full-suite run: 1916/1918 (2 pre-existing, unrelated
      EngineConcurrencyTest flakes). Migrations:
      priv/repo/migrations/20260823000003_add_sequence_number_to_process_definitions.exs,
      priv/repo/migrations/20260823000004_create_definition_sequence.exs.
      Design doc: lib/letflow/design/req125-definitions-delta-sync.md. Gates
      passed: CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (x2), REVIEWER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER (retry after rework),
      RELEASE-VALIDATOR -- all PASS. 6 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 7 after.
  - req: REQ-117
    event: blocked
    agent: ORCH
    at: "2026-08-23T02:37:01Z"
    note: >
      Claimed via get_next_task (queue task id 246, depends_on: [242/REQ-116,
      done] -- queue considered it eligible). Not dispatched to any workflow:
      REQ-117's own text requires reading back "the authority and client-id
      REQ-133 set" and verifying it against decision-0002, but REQ-133
      (S4, impl_order 254) is itself still status: pending in
      requirements.yaml, and its own dependency REQ-131 (impl_order 248) is
      also pending (blocked further behind impl_order 243). Confirmed live:
      web/src/auth/OidcManager.ts and web/src/auth/tenantConfig.ts both still
      default to http://localhost:8081/realms/bpm-default (R-Co's port),
      while docker-compose.yml/config/dev.exs put Letflow's own Keycloak on
      port ${LETFLOW_KEYCLOAK_PORT:-8082} -- REQ-133 has not yet applied its
      fix, so REQ-117's AC1 (real login producing an accepted token) cannot
      succeed against current defaults, and AC2 has nothing "REQ-133 set" to
      read back yet. (ISS-0275, the Keycloak audience-mapper 401, is
      separately already resolved -- not the blocker here.) This is an
      undeclared dependency: REQ-117's queue depends_on lists only 242, not
      REQ-133's queue task, so get_next_task will keep surfacing 246 as
      eligible until REQ-133 (and transitively REQ-131) land. Released task
      246 with status: "blocked" (not left open) specifically to prevent
      get_next_task re-claiming the same not-yet-workable task on the next
      /loop iteration; it stays ineligible until re-locked deliberately once
      REQ-133 is done. No files changed. 7 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 8 after.
  - req: REQ-121
    event: started
    agent: ORCH
    at: "2026-08-23T02:41:00Z"
    note: >
      Claimed via get_next_task (queue task id 247, github_issue_number 485,
      depends_on: [241/REQ-115, done]). No duplicate found under docs/issues/
      for GH#485 -- genuine new work. WF-02 Step 00 (git setup) complete via
      FRONTEND-DEV: branch feature/WF02-REQ121-20260823 created and pushed.
      Fails ORCH's direct-action sizing check (docs/agents/ORCHESTRATOR.md
      Section 10, check 1: touches more than one file) so routed through the
      full workflow rather than acted on directly. Pre-Step-1 investigation:
      web/src/pages/admin/users/UsersPage.tsx confirmed genuinely unreferenced
      (grep over web/ for the path and for relative-import forms found
      nothing); its only component import, CreateUserDialog, is also
      unreferenced anywhere else. DeactivateUserDialog is NOT dead -- it is
      imported by web/src/pages/admin/UserDetailPage.tsx, but that importer
      is itself an orphan: grep found no reference to UserDetailPage anywhere
      in web/src, including router.tsx (both /admin/users and
      /admin/users/:id route to the live UsersPage.tsx, not UserDetailPage).
      This is a third dead file outside REQ-121's stated scope (only the two
      UsersPage.tsx files and web/src/components/admin/users/ were named) --
      flagged for CODE-DESIGNER/FRONTEND-DEV to resolve on evidence, not
      silently folded into this deletion. 8 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 9 after.
  - req: REQ-121
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T02:56:04Z"
    note: >
      WF02-REQ121-20260823 complete. Deleted
      web/src/pages/admin/users/UsersPage.tsx (confirmed unreferenced anywhere
      in web/) and web/src/components/admin/users/CreateUserDialog.tsx (its
      only importer was the deleted page). Kept
      web/src/components/admin/users/DeactivateUserDialog.tsx -- imported by
      web/src/pages/admin/UserDetailPage.tsx, which is itself unreferenced by
      router.tsx (a third orphan file, out of REQ-121's named scope, flagged
      as a follow-up finding rather than deleted here). All 5 acceptance
      criteria independently verified at RELEASE-VALIDATOR (Step 5): zero
      code references confirmed via grep (only a stale README.md prose
      mention remained), frontend gates all passed (type-check, lint,
      178/178 tests, 48/48 guards), DeactivateUserDialog/UserDetailPage/live
      UsersPage.tsx/router.tsx confirmed untouched. Design doc:
      lib/letflow/design/req121-remove-dead-userspage.md. Gates passed:
      CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (out of scope, no tenant-data
      path), REVIEWER, TEST-DESIGNER (out of scope, no executable surface --
      3b/4 skipped), RELEASE-VALIDATOR -- all PASS, no rework needed. 9
      on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 10 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T03:43:00Z"
    note: >
      WF03-ISS0289-20260823 complete, merged to main via PR #578 (commit
      6e5e3ee). ISS-0289 was REQ-121's own follow-up finding: registered via
      register_task (queue task 289, GH#577) at the start of this run.
      ISSUE-FIXER diagnosed root cause as a router wiring gap, not dead code:
      UsersPage.tsx carried a redundant inline detail/edit branch with NO
      PLATFORM_ADMIN role gate at admin/users/:id, while the fully-built
      UserDetailPage.tsx (real role gate, DeactivateUserDialog, group-
      membership editing) sat unwired. CODE-DESIGNER/CODE-DESIGN-VALIDATOR
      chose Option 1 (adopt UserDetailPage.tsx as sole detail/edit view,
      route param renamed :id -> :userId, UsersPage.tsx's inline branch
      removed). FRONTEND-DEV implemented; SECURITY-REVIEWER confirmed this is
      a net ADD of access control (the old inline branch had zero role check
      -- any authenticated user could previously edit/deactivate any user).
      REVIEWER passed on idiom/scope. TEST-DESIGNER wrote a route-level
      fail-then-pass regression test (web/src/__tests__/router.iss-0289.test.tsx)
      targeting the routing/wiring boundary, independently reproduced by
      TEST-DESIGN-VALIDATOR via a safe robocopy-based worktree (TEST-DESIGNER's
      own run hit and self-corrected a git-worktree-junction node_modules
      deletion, logged to docs/anti-patterns.md). TEST-RUNNER confirmed full
      suite green (180 unit tests, 48 guard tests, real vite build). Issue
      closed resolved (docs/issues/ISS-0289.yaml), GitHub issue #577 closed
      with evidence comment. Queue task 289 released done. All changes
      independently verified on main by ORCH post-merge (router.tsx,
      UsersPage.tsx, UserDetailPage.tsx, test file, issue record all
      confirmed present/correct) before writing this entry. 10 on-disk
      `- req:` entries in requirement_status.v4.yaml before this append; 11
      after.
  - req: REQ-131
    event: started
    agent: ORCH
    at: "2026-08-23T03:47:13Z"
    note: >
      Claimed via letflow-queue get_next_task (queue task id 248, GH#486).
      Queue API returned this requirement's STALE 2026-08-22 description
      text (nine empty sub-routers, identity.ex 11 call sites); using
      docs/requirements.yaml's corrected 2026-08-23 text instead (7 already-
      wired routers, 53 call sites, promotion.ex the one genuine remaining
      stub, metrics.ex deliberately ungated) as source of truth for the
      handoff, per ORCH's sole role reading that file to scope work. No
      duplicate found under docs/issues/ for GH#486. depends_on REQ-130,
      status done (design doc lib/letflow/design/req130-authorization-
      generalization.md, already passed CODE-DESIGN-VALIDATOR/
      SECURITY-REVIEWER/REVIEWER under REQ-130's own run). WF-02 Step 00
      complete: branch feature/WF02-REQ131-20260823 created off up-to-date
      main and pushed. Given REQ-130's design doc already IS the design
      artefact for this exact implementation (and already gate-passed),
      Step 1/1b (CODE-DESIGNER/CODE-DESIGN-VALIDATOR) are not re-run as a
      separate round for REQ-131 -- proceeding directly to Step 2a
      (ELIXIR-DEV) referencing that design doc. 11 on-disk `- req:` entries
      in requirement_status.v4.yaml before this append; 12 after.
  - req: REQ-131
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T05:07:19Z"
    note: >
      REQ-130's authorization mechanism (Letflow.Plugs.Authorize +
      Letflow.Api.AuthorizedRouter) implemented and made the single mandatory
      authorization path router-wide. Migrated all 53 already-shipped call
      sites across seven routers (identity 16, audit 1, definitions 13,
      instances 7, onboarding 3, tenants 6, tasks 7) onto the plug, deleting
      the old per-router with_authorized_scope/4 and with_authorization/4
      helpers entirely. Closed 3 previously-silent authorization gaps by
      reusing existing policy-key atoms: instances.ex's rebind-pins/
      reconstruct routes, solution_packs.ex's export/install routes, and
      definitions.ex's /validate route. lib/letflow/api/authorization.ex
      confirmed unchanged by every gate. Test coverage added:
      test/letflow/api/authorization_enforcement_test.exs,
      test/letflow/plugs/authorize_test.exs, extended
      test/letflow/routers/identity_test.exs, test/specs/REQ-131.md. Gates
      passed: SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR, TEST-RUNNER
      (full suite 1982/1985, 3 unrelated pre-existing bookkeeping-invariant
      failures), RELEASE-VALIDATOR -- all PASS, RELEASE-VALIDATOR
      independently confirmed all 10 acceptance criteria genuinely met.
      docs/migration/stage-4-api-surface.md's "Identity infrastructure and
      authorization" section updated to reflect Letflow.Api.Authorization is
      no longer unreachable. 12 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 13 after.
  - req: REQ-137
    event: started
    agent: ORCH
    at: "2026-08-23T05:14:28Z"
    note: "WF02-REQ137-20260823 Step 00 (git setup) PASS; design-only requirement, no implementation code -- realm provisioning design + sized implementation plan, constrains REQ-076's boundary"
  - req: REQ-137
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T05:25:58Z"
    note: >
      Design-only requirement. lib/letflow/design/req137-realm-provisioning.md
      shipped: realm creation designed on top of REQ-135's PORT/DROP table
      (cited by path), realm name committed to tenant.idp_realm_id at
      tenant-creation time (no write-back needed, REQ-019's immutability
      respected), idempotent create_tenant_realm/1 primitive with a live
      Keycloak existence guard as the R-Co realm_guard analog. Partial-failure
      path designed in both directions with no compensating deletes in
      either (extends the existing TenantProvisioning/ISS-0230 no-rollback
      policy) -- retry-safe via the same existence guard. Composition with
      TenantProvisioning/TenantOnboarding stated: three primitives, none
      calls another (REQ-022 no-implicit-chaining extended, not violated),
      TenantOnboarding.provision_and_migrate/1 named as the sole explicit
      sequencer. REQ-076 boundary stated: calls provision_and_migrate/1
      unchanged plus supplies idp_realm_id/oidc_mode at creation, must not
      call RealmProvisioning/ProviderAdmin directly or build its own retry
      machinery. Sized implementation plan: 4 numbered entries (150+300+180+50
      = 680 lines), each with line count, named R-Co source ports or Letflow
      modules, and a one-agent-turn justification. No implementation code.
      RELEASE-VALIDATOR independently re-verified all 6 acceptance criteria
      PASS. 13 on-disk `- req:` entries in requirement_status.v4.yaml before
      this append; 14 after.
  - req: REQ-136
    event: started
    agent: ORCH
    at: "2026-08-23T02:45:00Z"
    note: >
      REQ-136 (GitHub Actions CI running the backend gate) started.
  - req: REQ-136
    event: done
    agent: ORCH
    at: "2026-08-23T06:10:00Z"
    note: >
      REQ-136 shipped: .github/workflows/ci.yml added with jobs.backend
      running mix letflow.check against a postgres:16 service matching
      docker-compose.yml, triggered on push and pull_request. Job keyed
      as a named sibling-ready jobs.backend so REQ-138's future frontend
      job can append without restructuring. docs/agents/protocols/
      GIT_MERGE.md's stale "no CI exists" precondition corrected
      (REVIEWER flag). First real CI run (32613777269) surfaced 3
      pre-existing environment-sensitive test defects unrelated to this
      requirement's own diff -- fixed directly per coordinator decision
      (widened SandboxPoolTest's vacuity floor 200ms->100ms and
      EngineConcurrencyTest's timing multiplier 30x->50x with measured
      real-CI rationale in each; fixed AuditIssueClosuresTest's fake-gh
      fixture, which was Windows-only (gh.bat, ;-joined PATH) and
      silently failed to isolate on Linux CI producing 177 fetch errors
      -- now branches on :os.type() for both the script and PATH
      separator). Real CI run 32619149244 confirmed green after the
      fixes. AC4 demonstrated for real: a deliberate mix-format
      violation pushed and shown red on CI (run 32621650147, actual
      log line quoted), then reverted and confirmed green again (run
      32621691722). RELEASE-VALIDATOR independently re-verified all 6
      acceptance criteria PASS, including re-running gh run list/view
      itself and a local test rerun (47/47 passed) of the three fixed
      files. 15 on-disk `- req:` entries in requirement_status.v4.yaml
      before this pair; 17 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T07:00:00Z"
    note: >
      ISS-0290 (queue task 290, GH#581) resolved directly (docs-only, no
      code): handoffs/WF03-ISS0289-20260823/step-final-git-merge.json had
      created_at/started_at (03:45:00Z) copy-paste-typo'd to AFTER
      completed_at (03:44:30Z), tripping lint_handoffs H2. Surfaced by
      REQ-136's first post-CI full-suite run, unrelated to REQ-136's own
      diff. Corrected created_at/started_at to 03:41:30Z, consistent with
      the merge evidence already embedded in the same file (mergedAt
      2026-08-23T03:42:17Z, recovery completing by 03:44:30Z). Checked all
      9 other handoffs in the WF03-ISS0289-20260823 run for similar
      timestamp-ordering defects: none found. mix letflow.lint_handoffs
      reconfirmed clean (0 new violations, 25 pre-existing grandfathered).
      17 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 18 after.
  - req: REQ-132
    event: started
    agent: ORCH
    at: "2026-08-23T05:33:42Z"
    note: >
      Claimed via letflow-queue get_next_task (queue task id 253, GH#491).
      depends_on REQ-131 and REQ-083, both status done. No duplicate found
      under docs/issues/ for GH#491 (a coincidental "491" substring match in
      ISS-0227.yaml was a commit-hash fragment, not this issue). Applies
      Letflow.Api.Authorization's AllowWithRowFilter kind (returned but not
      yet enforced after REQ-131) on the task read path, per REQ-132's own
      text: task_scope {:own_user_and_groups, user_id} is GROUPS-INCLUSIVE,
      row scope must come from the AccessDecision tuple alone (never a
      request-supplied assignee_id). WF-02 Step 00 complete via ELIXIR-DEV:
      branch feature/WF02-REQ132-20260823 created off up-to-date main and
      pushed. 18 on-disk `- req:` entries in requirement_status.v4.yaml
      before this append; 19 after (renumbered during Step Final rebase
      onto REQ-136/ISS-0290, which landed on main after this branch was
      created; original note said 14/15 against this branch's stale base).
  - req: REQ-132
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T06:50:05Z"
    note: >
      Row-scoping mechanism found ALREADY IMPLEMENTED on main (landed
      incidentally under REQ-083/130/131) -- zero lib/ or migration diff this
      run. This run produced: lib/letflow/design/req132-row-scoping.md
      (design doc citing and confirming the existing mechanism, passed
      CODE-DESIGN-VALIDATOR after one rework round removing embedded code
      blocks), test/specs/REQ-132.md, and new coverage in
      test/letflow/routers/tasks_test.exs (5 new describe blocks, AC1/AC2/
      AC3/AC5 for GET /tasks specifically -- previously only GET
      /tasks/inbox had this coverage; AC4 documented as code-reading
      evidence, not a runnable test). Full suite: 1984 tests / 5 properties,
      2 pre-existing unrelated failures (ISS-0289 vocabulary-drift
      assertions in test/docs/requirement_status_invariants_test.exs,
      confirmed structurally pre-existing/off-branch by RELEASE-VALIDATOR).
      Gates passed: CODE-DESIGN-VALIDATOR (1 rework round),
      SECURITY-REVIEWER (INV-1/2/6/7/8 all PASS), REVIEWER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER (1 rework round for an unrelated
      index-consistency bookkeeping fix), RELEASE-VALIDATOR -- all
      ultimately PASS, all 5 acceptance criteria independently confirmed.
      No docs/migration/stage-N-*.md or docs/migration/decisions/ file
      named in REQ-132's acceptance criteria; none updated. README.md not
      updated -- no documented user-facing behavior changed, the mechanism
      was already live; this closed a compliance/test-coverage gap only.
      19 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 20 after (renumbered during Step Final rebase onto
      REQ-136/ISS-0290; original note said 15/16 against this branch's
      stale base).
  - req: REQ-138
    event: started
    agent: ORCH
    at: "2026-08-23T07:03:30Z"
    note: >
      WF02-REQ138-20260823 begun -- add frontend gate job (npm ci && npm
      run check) to .github/workflows/ci.yml, alongside REQ-136's backend
      job.
  - req: REQ-138
    event: done
    agent: ORCH
    at: "2026-08-23T07:40:00Z"
    note: >
      REQ-138 shipped: jobs.frontend added to .github/workflows/ci.yml
      (backend job untouched, pure sibling addition per AC2). Also fixed a
      genuine pre-existing bug found via the first real CI run: unquoted
      glob patterns in web/package.json's test/test:watch/test:coverage
      exclude flags (--exclude tests/e2e/**) worked by accident on Windows
      (npm's cmd.exe shell doesn't glob-expand) but broke on Linux CI
      (bash expands the glob before vitest sees it, so vitest loaded
      Playwright e2e specs directly and crashed on test.describe()) --
      same class of Windows-vs-Linux bug as REQ-136's own gh-fixture fix.
      Quoted the patterns; check script itself left untouched (AC5).
      Two unrelated CI-runner-noise flakes hit this branch's runs
      (Letflow.EngineConcurrencyTest AC1 at 80.15x, filed as ISS-0291 since
      REQ-136 already widened its multiplier once; Letflow.SandboxPoolTest
      RT-8/RT-9 with a real Postgres query_canceled, filed as ISS-0292) --
      both confirmed unrelated via git diff --stat (REQ-138's own diff
      touches only .github/workflows/ci.yml and web/package.json, zero
      Elixir), neither blocked this merge, both re-ran green on retry.
      AC4 demonstrated for real: a deliberate TS2322 type error in
      web/src/utils/canvas/interfaceValidation.ts turned the frontend job
      red (CI run 32625889148, quoted log line: "error TS2322: Type
      'string' is not assignable to type 'number'"), reverted cleanly, and
      the revert-confirmation run (32626122080) went green. Composed here
      after REQ-132's done-event (both this run's events at 07:03:30Z/
      07:40:00Z sort chronologically after REQ-132's done at 06:50:05Z, so
      no reordering needed). 20 on-disk `- req:` entries in
      requirement_status.v4.yaml before this pair; 22 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T09:05:00Z"
    note: >
      ISS-0291 (queue task 291, GH#584) resolved: Letflow.EngineConcurrencyTest
      AC1's "no global serialization" check had flaked 3 times (ISS-0260
      locally, then twice on GitHub Actions CI even after two threshold
      widenings 30x->50x) because it compared batch time to a single baseline
      via a ratio -- inherently sensitive to unbounded shared-runner CPU
      noise. Replaced with a direct interval-overlap measurement: each of
      the 100 concurrent completions records its own [start, end] window via
      System.monotonic_time/0, and a new max_overlap_depth/1 interval-sweep
      asserts genuine overlap occurred (depth > 1) -- immune to host
      speed/noise, since a global serializer can never produce overlapping
      windows regardless of speed, and genuine per-row locking always can.
      Removed the now-dead ac1_timing_multiplier/0 machinery and its 7-test
      regression suite. Caught and fixed one real bug in the fix itself
      (interval-sweep tiebreak was backwards, would have produced a false
      positive at an exact-tie boundary) via self-review after an
      accidental subagent dispatch mid-review (fork rule violation, self-
      corrected). Also caught and fixed a second self-introduced regression:
      deleting the baseline measurement left complete_attrs/1's default
      argument unused (same warning class as ISS-0069/ISS-0284), flagged by
      ORCH and fixed before merge. Verified: mix format clean, 3/3 local
      runs green, full mix letflow.check clean (1982 tests), real CI green
      on both backend and frontend gates (PR #588, merge e49948a). 22
      on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 23 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T09:38:00Z"
    note: >
      ISS-0294 (queue task 294, GH#590) resolved directly, no code change
      (per its own AC4 -- process/prompt-clarity investigation only). Root
      cause: ORCH's own ad-hoc fork-dispatch prompts this session combined
      "dispatch each pipeline role exactly once, in strict sequence" with
      "do not self-review" in wording that a fork could plausibly
      over-generalize into "don't spawn subagents at all" -- the same
      belief surfaced independently by both the REQ-135 fork (ISS-0288)
      and the ISS-0291 fork. Since this prompt text is generated per-dispatch
      rather than stored in a repo file, the fix is behavioral: saved a
      persistent memory (fork-dispatch-wording-clarity, cross-session) that
      states both halves explicitly and separately in every future fork
      dispatch -- "you ARE expected to spawn Agent children for each
      pipeline role" stated apart from "exactly once, not double-dispatch."
      23 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 24 after.
  - req: REQ-139
    event: done
    agent: ORCH
    at: "2026-08-23T09:45:00Z"
    note: >
      Handled directly (docs-only, single file, well within ORCH's own
      sizing rule -- docs/agents/protocols/GIT_MERGE.md). Corrected the
      Precondition section's stale "REQ-138 (frontend gate, pending as of
      this writing)" premise (REQ-138 has since landed) to name both
      REQ-136 and REQ-138 as landed and describe both the backend and
      frontend CI jobs. Corrected the sole remaining "REQ-013" mention
      (line 268, "before REQ-013 lands") to reflect REQ-013's local gate
      and REQ-136/REQ-138's CI all being landed. Verified via grep: no
      "still `pending`" phrase applied to REQ-013 remains anywhere in the
      file. Did not touch the "What 'reported green' means here" paragraph
      or the standing-failures attribution rule, per this requirement's own
      AC5. 24 on-disk `- req:` entries in requirement_status.v4.yaml before
      this append; 25 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-23T10:37:20Z"
    note: >
      ISS-0295 (queue task 295, GH#591) resolved: web/tests/e2e/*.ts hardcoded
      a stale Keycloak port (8081) and client_id (bpm-platform-api) across 33
      files, with no shared constant/override mechanism -- run WF03-ISS0295-20260823
      routed the full WF-03 pipeline (ISSUE-FIXER diagnosis, CODE-DESIGNER +
      CODE-DESIGN-VALIDATOR, FRONTEND-DEV implementation, SECURITY-REVIEWER
      [out of scope, test-only/non-secret config], REVIEWER, TEST-DESIGNER +
      TEST-DESIGN-VALIDATOR [independently reproduced fail-then-pass twice],
      TEST-RUNNER [type-check/lint/184 unit tests/48 guards/228 e2e-discovery
      all green]) -- all PASS, no rework needed. Consolidated into two
      exported constants in web/tests/e2e/helpers.ts: BPM_IDP_BASE_URL
      (default rebased 8081->8082) and BPM_IDP_CLIENT_ID (new, default
      letflow-web, previously had NO override mechanism at all). Merged to
      main via PR #592, commit 91749ee. Diagnosis-time discrepancy recorded
      in docs/issues/ISS-0295.yaml: the issue's own filing text claimed
      REQ-133 had already corrected the SPA's own OIDC defaults
      (web/src/auth/OidcManager.ts, tenantConfig.ts) -- that is FALSE, REQ-133
      remains `pending` on main with no merged work, both before and after
      this fix. This issue's fix is scoped ONLY to the e2e test suite; it
      does not substitute for REQ-133's separate, still-open SPA-side work.
      Post-merge sanity check (ORCH, independent of the merge agent's own
      report): `git log origin/main` and `gh pr view 592` both confirm MERGED
      at commit 91749ee7706c0c509fab6896483cf4e886aef03b; `grep -rn
      "8081\|bpm-platform-api" web/tests/e2e/` on main returns zero matches.
      Queue task 295 released with status done, GH#591 closed with resolution
      comment citing docs/issues/ISS-0295.yaml. 25 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 26 after.
  - req: REQ-134
    event: started
    agent: ORCH
    at: "2026-08-23T10:43:56Z"
    note: >
      WF02-REQ134-20260823 Step 00 -- exercise a real Keycloak-issued token
      through Letflow.Plugs.AuthPipeline in a tagged, excluded-by-default
      integration path. depends_on REQ-128/REQ-131 both done. Branch
      feature/WF02-REQ134-20260823 pushed.
  - req: REQ-134
    event: done
    agent: DOC-UPDATER
    at: "2026-08-23T13:24:48Z"
    note: >
      Real-Keycloak token integration path shipped:
      test/letflow/integration/keycloak_auth_pipeline_test.exs drives a
      genuine token through Letflow.Plugs.AuthPipeline end to end, tagged
      :keycloak and excluded by default via test/test_helper.exs. This run
      went through 3 rework cycles before RELEASE-VALIDATOR: AC3's
      fixture-reset logic twice (count-before/after JIT-provisioning
      assertions) and one AC2 assertion-mismatch fix (tampered-signature
      401 body shape). RELEASE-VALIDATOR independently ran the live-Keycloak
      suite for the 3rd time overall (after TEST-DESIGN-VALIDATOR and
      TEST-RUNNER) -- 3/3 passed, reproduced independently each time -- and
      additionally re-derived AC4 under the condition it is actually about:
      earlier gates had only inferred "passes with Keycloak down" from the
      exclusion mechanism being config-static, so RELEASE-VALIDATOR ran
      `docker compose stop keycloak`, confirmed it stopped, then ran plain
      `mix test` end to end (1986 passed, 3 excluded, 0 failures), restarting
      Keycloak afterward -- genuine "Keycloak actually stopped" evidence no
      earlier gate had gathered. All 6 acceptance criteria independently
      confirmed. Also fixed a MINOR issue RELEASE-VALIDATOR carried forward
      from REVIEWER: lib/letflow/design/req134-real-keycloak-token-integration.md
      S3.1 described the design-phase {:skip, message} setup_all mechanism;
      corrected to describe the raise-based mechanism actually shipped
      (ExUnit 1.20.3 does not support dynamic runtime skip from setup_all).
      README.md's "Running it" section given a one-line pointer to the new
      excluded-by-default suite and its exact run command. No
      docs/migration/stage-N-*.md or docs/migration/decisions/ file named
      in REQ-134's acceptance criteria; confirmed none needed updating. 27
      on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 28 after.
  - req: REQ-133
    event: blocked
    agent: FRONTEND-DEV
    at: "2026-08-23T13:50:00Z"
    note: >
      Config fix (web/src/auth/OidcManager.ts, tenantConfig.ts,
      web/.env.example, lib/letflow/routers/tenant_config.ex -- the latter
      not in REQ-133's own named scope but discovered to carry the identical
      stale R-Co defaults, completely overriding the SPA-side fix without
      it) landed via PR #593, REVIEWER PASS, curl-verified. AC7
      (type-check/lint/test) and AC8 (route inventory: REQ-073..085 all
      done except REQ-077 blocked) completed for real. AC1-6 (live browser
      login proof) could not be completed despite multiple hours of
      genuine attempts -- reproducible Playwright/Chromium automation
      flakiness in this environment, confirmed NOT an app defect via curl
      (backend and Keycloak both respond correctly for the fixed values)
      and one real browser network log showing the correct URL requested.
      Two real environment issues found and documented for the next
      attempt: Vite dev server IPv6-only binding, and React 18 StrictMode
      double-firing signinRedirect() in dev mode (use a production preview
      build instead). Follow-up filed: ISS-0296 (queue task 296, GH#594).
      28 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 29 after.
  - req: REQ-140
    event: started
    agent: ORCH
    at: "2026-08-23T14:00:00Z"
    note: >
      WF02-REQ140-20260823. Full pipeline: CODE-DESIGNER wrote
      lib/letflow/design/req140-platform-event-append-path.md, resolving
      the requirement's own open question (module placement) with
      reasoning. CODE-DESIGN-VALIDATOR PASS. ELIXIR-DEV implemented
      EventStore.append_platform_event/2 (append/2 left byte-unchanged) and
      Letflow.EventStore.PlatformEvents (three deterministic adapters).
  - req: REQ-140
    event: done
    agent: ORCH
    at: "2026-08-23T15:35:00Z"
    note: >
      SECURITY-REVIEWER PASS (INV-1 tenant isolation confirmed -- platform
      sentinel check happens before the M1 guard-skip, all writes remain
      prefix-scoped). REVIEWER PASS (idiom, scope, decision-record
      consistency clean). TEST-DESIGNER wrote
      test/letflow/event_store/platform_events_test.exs (new) and extended
      test/letflow/tenant_provisioning_event_seed_test.exs. TEST-RUNNER:
      platform_events_test.exs 9/9, tenant_provisioning_event_seed_test.exs
      9/9, full event_store regression guard 175/175, all passed. A
      broader 16-way-parallel full-suite run surfaced 3 pre-existing,
      unrelated flakes (confirmed via git diff not touching this branch)
      -- registered separately as ISS-0297, not blocking. RELEASE-VALIDATOR
      independently re-derived and PASSed all 13 acceptance criteria
      against real re-run tests (184/184 combined). Unblocks REQ-077
      (promotion routes) -- REQ-077 remains status: blocked pending its own
      follow-up run to wire these appenders in, not implemented here (out
      of REQ-140's scope). This run's own base predated REQ-134's landing on
      main, so its own pre-rebase recount (27) was stale; recounted directly
      from `grep -c "^  - req:"` on the composed post-rebase file = 31,
      per this note's own recount-every-time rule, rather than carrying
      forward either side's independently-computed number.
  - req: REQ-122
    event: done
    agent: ORCH
    at: "2026-08-24T02:35:00Z"
    note: >
      Inventory of all 37 e2e spec files produced at
      docs/testing/REQ-122-e2e-inventory.md. RUNNABLE-NOW set: 1 spec
      (sh05-06.shell.e2e.spec.ts — 3/3 PASSED, no Keycloak, no backend
      required). BLOCKED-ON-INFRA: 36 specs — 35 require Keycloak
      bpm-default realm + seeded users; 1 (oidcf2-subdomain) requires
      Letflow backend at :4000 for TC01/TC02 (TC03/TC04 pass). No spec
      file under web/tests/e2e/ was modified (git diff confirmed clean).
      Infrastructure needed to repeat: Playwright + Chromium + Vite dev
      server only (for sh05-06); Letflow + Keycloak + Postgres + seeded
      users for all others.
  - req: REQ-141
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T03:10:00Z"
    note: >
      Built web/src/styles/tokens.css from the design-system.md palette and
      imported it once from web/src/main.tsx so the app loads the token source
      before routing. No web/src/components/ files were modified, the legacy
      design-tokens/letflow.tokens.json file stayed untouched, and the guard
      patterns were left alone per this requirement's narrow scope. Verified in
      web with `npm run type-check && npm run lint && npm test && npm run guards`,
      which passed with 32 test files / 184 tests green and 48 guard tests green;
      a production `vite build` also succeeded. 32 on-disk `- req:` entries
      in requirement_status.v4.yaml before this append; 33 after.
  - req: REQ-142
    event: started
    agent: ORCH
    at: "2026-08-24T03:19:00Z"
    note: >
      WF02-REQ142-20260824 Step 00. Canvas hex-literal to tokens.css migration
      (15 files, ~118 occurrences per 2026-08-22 count). depends_on REQ-141
      (done). Branch feature/WF02-REQ142-20260824 created and pushed. Queue task
      268 claimed (GH#530). 33 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append; 34 after.
  - req: REQ-142
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T03:45:02Z"
    note: >
      Migrated all 118 inline hex literals across 15 files in
      web/src/components/canvas/ to CSS custom properties from tokens.css.
      No fallback hex values remain (grep confirms 0 hits). ProcessCanvas.tsx's
      <Background color> prop (previously "#ccc") replaced with
      var(--color-neutral-400) — @xyflow/react passes the color value through
      a CSS custom property internally, so var() syntax is valid. No test
      files modified. No files outside canvas/ touched. All gates passed:
      CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (out of scope — no tenant-data
      path), REVIEWER, TEST-DESIGNER (no new tests needed -- pure style
      substitution, busywork gate avoided), RELEASE-VALIDATOR (independently
      verified all 4 ACs). Final: npm run type-check, lint, test (184/184),
      guards (48/48) all pass.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-24T04:54:12Z"
    note: >
      ISS-0330 resolved (WF03-ISS0330-20260824, PR #634, 5b57ed9). Two
      pre-existing invariant test failures fixed: A4b (REQ-122 done entry in
      requirement_status.v4.yaml was missing required `agent:` field, added
      `agent: ORCH` matching the direct-push attribution pattern) and A9
      (requirement_status.yaml/volume 1 index declared entries: 182 but had
      185 on disk, corrected in the index). Both failures traced to sibling
      session commit 70632a3 (direct main push bypassing CI). All 20
      invariants tests now pass. Queue task 330 released done, GH#633 closed.
      35 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 36 after.
  - req: REQ-143
    event: started
    agent: ORCH
    at: "2026-08-24T05:09:08Z"
    note: >
      WF02-REQ143-20260824 Step 00. promotions/ and instances/ hex-literal to
      tokens.css migration (12 files, 193 occurrences per 2026-08-22 count;
      re-verified at start: 162 hits, 57 distinct hex values -- many Tailwind/
      Mantine colors not in current palette, design-step addendum likely needed).
      depends_on REQ-141 (done). Branch feature/WF02-REQ143-20260824 created
      and pushed. Queue task 269 claimed (GH#531). 36 on-disk `- req:` entries
      in requirement_status.v4.yaml before this append; 37 after.
  - req: REQ-143
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T05:44:22Z"
    note: >
      Migrated 162 hex literals across 12 files in web/src/components/promotions/
      and instances/ to CSS custom properties. Design-step addendum: 13 new
      tokens added to tokens.css and design-system.md (--color-success-tint,
      --color-error-tint, --color-failure, and 10 --color-avatar-* tokens for
      ActorAvatar.tsx's avatar palette). All 55 distinct hex values mapped;
      zero grep hits remain. All gates passed: CODE-DESIGN-VALIDATOR (PASS,
      confirmed --color-failure token is semantically distinct from
      --color-error-dark), SECURITY-REVIEWER (out of scope), REVIEWER (1 rework:
      prerequisite commit was not yet pushed; corrected), RELEASE-VALIDATOR
      (independently verified all 4 ACs -- grep 0, no test files modified,
      npm run type-check/lint/test(184/184)/guards(48/48) all pass).
      37 on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 38 after.
  - req: REQ-144
    event: started
    agent: ORCH
    at: "2026-08-24T06:03:23Z"
    note: >
      WF02-REQ144-20260824 Step 00. ui/+forms/+webhooks/ hex-literal migration
      (12 files, 133 occurrences per 2026-08-22 count; re-verified: 118 hits,
      33 distinct values -- many Tailwind status/alert colors not in palette,
      design-step addendum expected). depends_on REQ-141 (done). Branch
      feature/WF02-REQ144-20260824 created. Queue task 270 (GH#532).
      38 on-disk entries before this append; 39 after.
  - req: REQ-144
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T06:37:27Z"
    note: >
      Migrated 118 hex literals across 12 files in ui/+forms/+webhooks/ to
      CSS custom properties. Design-step addendum: 4 new tokens added to
      tokens.css/design-system.md (--color-warning-tint/border/text +
      --color-error-border). AC-3 rework: ConflictResolver.12.test.tsx:120
      asserted literal #dc2626 which jsdom cannot resolve from a CSS var --
      updated regex to also accept var(--interactive-danger), authorized as
      fixing an over-specified test assertion. All 4 ACs verified: grep 0,
      12 target files + test fix, 184/184 tests, 48/48 guards. CI 4/4 green.
      39 on-disk entries before this append; 40 after.
  - req: REQ-077
    event: revised
    agent: ORCH
    at: "2026-08-24T07:04:06Z"
    note: >
      Status flipped blocked -> pending. REQ-077's original blocker was the
      absence of a production platform-event appender (REVIEWER ruling at
      WF02-REQ077-20260822). REQ-140 (platform event append path) is now
      done (merged 2026-08-23, PR #597). Both depends_on satisfied:
      REQ-072 (done) and REQ-140 (done). Queue task id=144 returns 404 on
      the live service (likely never registered or expired), so
      re-registration is needed before the next run claims it. GH#284
      (the issue mirror) remains open correctly -- REQ-077 is pending, not
      done. 40 on-disk entries before this append; 41 after.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-24T07:05:49Z"
    note: >
      REQ-077 queue reconciliation. Old queue task 144 expired (404). Re-
      registered as queue task 331 (GH#637). docs/requirements.yaml updated:
      impl_order 144->331, status blocked->pending. GH#284 commented and
      closed; track at GH#637. 41 on-disk entries before this append; 42 after.
  - req: REQ-145
    event: started
    agent: ORCH
    at: "2026-08-24T07:33:40Z"
    note: >
      WF02-REQ145-20260824 Step 00. layout/+definitions/+admin/ hex-literal
      migration (8 files, 52 occurrences per 2026-08-22 count; re-verified:
      45 hits, 28 distinct values -- Tailwind sky/blue and yellow/amber
      palette additions likely needed). depends_on REQ-141 (done). Branch
      feature/WF02-REQ145-20260824 created. Queue task 271 (GH#533).
      42 on-disk entries before this append; 43 after.
  - req: REQ-147
    event: done
    agent: ELIXIR-DEV
    at: "2026-08-24T10:22:38Z"
    note: >
      PROVENANCE (historical, not current decision authority):
      Renamed pack_update_resolutions.resolution Ecto.Enum:
      :keep_theirs->:keep_local, :custom->:merged. Migration
      20260824143417_rename_resolution_enum_values.exs (defensive; zero rows
      existed). @type and doc prose updated. Test string literals updated.
      req041 design doc OQ-6 marked resolved with REQ-147 + R-Co
      pack_update.zig:25-29 citations. CODE-DESIGNER skipped (mechanical
      rename). SECURITY-REVIEWER PASS, REVIEWER PASS, TEST-DESIGNER (scope:
      no new tests needed), RELEASE-VALIDATOR PASS (5/5 ACs; 5 pre-existing
      concurrency flakes in full suite unrelated to this branch). 47 on-disk
      entries before this append; 48 after.
  - req: REQ-146
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T09:19:04Z"
    note: >
      Tightened literal-colour guard (dropped semicolon-lookahead, catches JS/TSX
      inline style literals). Deleted design-tokens/letflow.tokens.json. Resolved
      web/README.md drift entries. Offender fixture updated to JS inline-style case
      (fail-first/pass-after verified). Exposed and fixed 22 additional rgba/hex
      violations in components/ and utils/ (shadow tokens, overlay tokens added to
      tokens.css). allowedPaths: web/src/pages/ (pending migration), vendor+index
      bundles (tokens.css compilation). vite.config.ts manualChunks vendor split.
      All gates: 184/184 tests, 48/48 guards (source+bundle). CI 4/4 green.
      45 on-disk entries before this append; 46 after.
  - req: REQ-147
    event: started
    agent: ORCH
    at: "2026-08-24T09:31:33Z"
    note: >
      WF02-REQ147-20260824 Step 00. Mechanical S2 enum rename:
      :keep_theirs->:keep_local, :custom->:merged in
      pack_update_resolution.ex + defensive migration + test update +
      design doc correction. CODE-DESIGNER skipped (rename confirmed
      mechanical per requirement description). Branch created. Queue task
      282 (GH#556). 46 on-disk entries before this append; 47 after.
  - req: REQ-145
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-24T08:00:39Z"
    note: >
      Migrated 45 hex literals across 7 files in layout/+definitions/+admin/
      to CSS custom properties. Design-step addendum: 6 prerequisite changes
      (5 new tokens + --surface-sidebar corrected from var(--color-neutral-900)
      to #1e293b). CODE-DESIGN-VALIDATOR took 3 rework cycles (count stale in
      summary, OQ-3 stale in §7, §1.2 stale count). SECURITY-REVIEWER (out of
      scope), REVIEWER (PASS), TEST-DESIGNER (no new tests), RELEASE-VALIDATOR
      (all 4 ACs verified: grep 0, 7 target files, no test files modified,
      184/184 tests, 48/48 guards). 43 on-disk entries before this append;
      44 after.
  - req: REQ-146
    event: started
    agent: ORCH
    at: "2026-08-24T08:14:12Z"
    note: >
      WF02-REQ146-20260824 Step 00. Final REQ-141 split requirement: delete
      design-tokens/letflow.tokens.json, tighten literal-colour guard to catch
      JS/TSX inline style literals, update web/README.md drift entries.
      Pre-check: zero hex hits across web/src/components/ (AC-5 verified).
      Branch feature/WF02-REQ146-20260824 created. Queue task 272 (GH#534).
      44 on-disk entries before this append; 45 after.
  - req: REQ-148
    event: started
    agent: ORCH
    at: "2026-08-24T10:34:44Z"
    note: >
      WF02-REQ148-20260824 Step 00. S5 Lua adoption spike: add {:lua, "~> 1.0"}
      to mix.exs, run deps.get/compile, write empirical OQ-2 spike design doc.
      Branch feature/WF02-REQ148-20260824 created. Queue task 300 (GH#600).
      SECURITY-REVIEWER and REVIEWER both hard gates per decision 0014.
      48 on-disk entries before this append; 49 after.
  - req: REQ-148
    event: done
    agent: CODE-DESIGNER
    at: "2026-08-24T10:49:31Z"
    note: >
      {:lua, "~> 1.0"} added to mix.exs (lua 1.0.2 fetched, 63+121 files
      compiled, 0 warnings). req148-lua-runtime-spike.md written: OQ-2
      (a)-(d) all answered empirically -- budget exhaustion catchable, host
      errors catchable, instruction_count accessible as internal struct
      field (yes-with-mechanism), sandbox deny-set confirmed for 7 functions.
      _VERSION=Lua 5.3, 3/2=1.5, 3//2=1. LUA-01 restatement complete.
      Escape hatch NOT exercised. SECURITY-REVIEWER PASS (forward condition:
      INV-3 re-evaluated when execution path wired). REVIEWER PASS (instruction
      count field: yes-with-mechanism, not contradiction). TEST-DESIGNER (no
      executable surface). RELEASE-VALIDATOR PASS (8/8 ACs). 49 on-disk
      entries before this append; 50 after.
  - req: REQ-077
    event: started
    agent: ORCH
    at: "2026-08-26T02:55:42Z"
    note: >
      PROVENANCE (historical, not current decision authority):
      WF02-REQ077-20260826 Step 00. Six promotion-pipeline route modules
      (promotion_review.zig, promotions.zig, promotion_read.zig,
      promotion_assertion.zig, promotion.zig, definition_rollback.zig).
      depends_on REQ-072 (done) and REQ-140 (done, unblocked platform event
      appender) both satisfied. Design artefact
      lib/letflow/design/req077-promotion-pipeline-routes.md already written
      and CODE-DESIGN-VALIDATOR PASS (WF02-REQ077-20260822) -- Step 1/1b
      skipped, starting at Step 2a. Branch feature/WF02-REQ077-20260826
      created and pushed. Queue task 331 (GH#637), locked by orch-ai-dala.
      50 on-disk entries before this append; 51 after.
  - req: REQ-077
    event: done
    agent: DOC-UPDATER
    at: "2026-08-26T04:11:08Z"
    note: >
      PROVENANCE (historical, not current decision authority):
      WF02-REQ077-20260826 complete. Six R-Co promotion-pipeline route modules
      (promotion_review.zig, promotions.zig, promotion_read.zig,
      promotion_assertion.zig, promotion.zig, definition_rollback.zig) ported as
      ten routes across three routers: lib/letflow/routers/promotions.ex,
      definitions.ex, and tenants.ex. Design artefact
      lib/letflow/design/req077-promotion-pipeline-routes.md already validated
      2026-08-22 (CODE-DESIGN-VALIDATOR PASS); Step 1/1b skipped, implementation
      started directly at Step 2a. Two rework iterations during this run:
      SECURITY-REVIEWER caught an INV-8 nil-crash, and TEST-RUNNER caught an
      iso8601/DateTime crash -- both fixed and re-verified. Full suite:
      2027/2028 (one pre-existing, unrelated failure tracked as ISS-0333).
      RELEASE-VALIDATOR independently re-verified all six acceptance criteria
      PASS. Also corrected this design doc's stale "IS BLOCKED" banner (lines
      ~10-31), which had gone stale once REQ-140 (platform-event-appender)
      shipped 2026-08-23 -- flagged by both ELIXIR-DEV and
      TEST-DESIGN-VALIDATOR during this run but left unfixed until now. 51
      on-disk `- req:` entries in requirement_status.v4.yaml before this
      append; 52 after.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-26T06:05:09Z"
    note: >
      WF03-ISS0332-20260826 complete. DEFINITION_PROMOTED schema_version 2
      backfill routine for pre-existing tenants. New module
      Letflow.TenantProvisioning.Backfill (lib/letflow/tenant_provisioning/
      backfill.ex) and Mix task mix letflow.backfill_event_type_versions
      (lib/mix/tasks/letflow.backfill_event_type_versions.ex). Pre-existing
      tenants with DEFINITION_PROMOTED at schema_version 1 (post-REQ-077
      state) can now be upgraded to v2 to unblock R10
      (Promotion.promote_active_definition/5, ENV-03 review-less promote
      path). All pipeline gates PASS: SECURITY-REVIEWER, REVIEWER,
      TEST-DESIGN-VALIDATOR (with mutation testing), TEST-RUNNER (2032
      tests, 0 failures). Issue file docs/issues/ISS-0332.yaml set to
      resolved. GH#642 closed. Queue task 332 released as done. 52
      on-disk entries before this append; 53 after. (Vocab-corrected from
      `req: ISS-0332` to `req: SCOPE-CHANGE` by a later direct-to-main
      commit, fc4d240 -- that commit's diff also trimmed this note's body;
      restored here during WF02-REQ151-20260826's Step Final rebase since
      shortening a past entry's content is exactly what this file's
      append-only rule forbids, same principle as the volume-1 anomalies
      this index already documents as left-as-written rather than
      "cleaned up".)

  - req: REQ-151
    event: started
    agent: ORCH
    at: "2026-08-26T09:21:20Z"
    note: >
      WF02-REQ151-20260826 started (queue task 337, GH#650, stage S5).
      Step 00 git-setup PASS (ELIXIR-DEV): branch feature/WF02-REQ151-20260826
      created and pushed. 53 on-disk entries before this append; 54 after.

  - req: REQ-149
    event: done
    agent: DOC-UPDATER
    at: "2026-08-26T10:20:00Z"
    note: >
      WF02-REQ149-20260826 complete. Design-only requirement settling OQ-1
      (Lua memory limit mechanism). RECOMMENDED MECHANISM: :max_heap_size
      with kill: true. Satisfies LUA-09 terminate clause; graceful-failure
      clause not achievable with pure-BEAM Lua VM. No decision 0014 addendum
      needed. Deliverable: lib/letflow/design/req149-lua-memory-limit.md.
      Queue task 335. 54 on-disk entries before this append; 55 after.

  - req: REQ-151
    event: done
    agent: DOC-UPDATER
    at: "2026-08-26T10:44:18Z"
    note: >
      WF02-REQ151-20260826 complete. Letflow.Engine.Lua.Sandbox implemented
      as the single sandbox-construction function every script execution
      obtains its Lua state from, with an explicit 28-entry deny-set
      (io.*/os.*/package/load/loadfile/require/dofile/loadstring/debug, each
      with a per-entry reason comment) enumerated against Lua 5.3's real
      stdlib rather than 5.1's. {:lua, "~> 1.0"} re-added to mix.exs/mix.lock
      to support this. Moduledoc restates LUA-03 (loadstring absent from
      5.3; jit/ffi/bit vacuous rather than satisfied; SBX-1's open-then-prune
      ordering does not transfer as a mechanism) and LUA-04 (no bytecode
      loader exists, so only-source-text holds structurally with no
      rejection path to test) rather than claiming their literal text is
      satisfied, and records the coroutine decision explicitly with its
      reason. REQ-058's AC5 guardrail (which had required a placeholder
      restatement of LUA-03/LUA-04 pending this requirement) retired with
      REVIEWER sign-off confirming consistency with decision 0014. 30 new
      tests added (sandbox_test.exs, lua_script_audit_test.exs). Full suite:
      2060/2062 (2 pre-existing unrelated failures, filed as ISS-0339 and
      ISS-0340, both independently re-confirmed pre-existing/off-branch by
      RELEASE-VALIDATOR via a fresh worktree at merge-base). RELEASE-VALIDATOR
      independently re-verified all 7 acceptance criteria PASS. No
      docs/migration/stage-N-*.md or docs/migration/decisions/ file named in
      REQ-151's acceptance criteria; none required updating -- confirmed per
      the Step 05 handoff's own note. 53 on-disk `- req:` entries in
      requirement_status.v4.yaml before this append (as independently
      computed against this run's own pre-rebase base); 56 after once
      composed into the true chain during WF02-REQ151-20260826's Step Final
      rebase, which interleaves this entry after REQ-149's done-event (55
      on-disk before this specific append in the real combined history; 56
      after) -- same "independently appended against a stale base, recounted
      at merge time" shape as WF02-REQ125-20260823's note above for volume 3.

# ─────────────────────────────────────────────────────────────────────────────
# VOLUME 4 — CLOSED 2026-08-26. DO NOT APPEND TO THIS FILE.
#
# This volume holds the complete requirement run history from 2026-08-23 through
# 2026-08-26 (56 entries, lines 1-1203). It is frozen: every line above this
# comment is preserved byte-for-byte. Closed under WF02-REQ151-20260826's Step
# Final rebase because composing that run's own REQ-151 entries with main's
# concurrently-landed ISS-0332/REQ-151-started/REQ-149-done chain brought this
# file to 1203 lines -- over its declared roll_rule ceiling (max_lines: 1200) --
# confirmed failing test/docs/requirement_status_invariants_test.exs's assertion
# A3 ("the current volume is within the roll-rule ceilings"). Reads of this
# volume are TARGETED reads only -- a line range or a pattern match, never a
# whole-file read. Use whichever your shell has:
#   Read tool with offset/limit (works everywhere), or
#   Bash:       grep -n PATTERN FILE   /   sed -n 'START,ENDp' FILE
#   PowerShell: Select-String -Path FILE -Pattern PATTERN
#               (Get-Content FILE)[START..END]
#
# NEW ENTRIES GO TO THE CURRENT VOLUME. Find it in:
#   docs/status/requirement_status.index.yaml
# which, as of this closure, names:
#   docs/status/requirement_status.v5.yaml
#
# Why this file was closed: WF02-REQ151-20260826's Step Final rebase composed
# main's ISS-0332/REQ-151-started/REQ-149-done chain (landed independently)
# with this run's own REQ-151-done append, bringing the on-disk count to 56
# across 1203 lines -- over the roll ceiling. No entry above this footer was
# moved, edited or deleted.
#
# KNOWN ANOMALIES IN THIS VOLUME: none declared beyond what volumes 1-3 already
# declare against themselves.
# ─────────────────────────────────────────────────────────────────────────────
