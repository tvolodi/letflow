# Letflow — Requirement run history — VOLUME 5 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v4.yaml (volume 4, closed,
#   1,203 frozen-prefix lines / 1,236 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v5.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v5.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: REQ-155
    event: started
    agent: ORCH
    at: "2026-08-28T07:13:00Z"
    note: >
      WF02-REQ155-20260827 started (queue task 307, GH#607, stage S5).
      Step 00 git-setup PASS (ELIXIR-DEV): branch
      feature/WF02-REQ155-20260827 created and pushed. Execution runs under
      Letflow.Engine.PluginTaskSupervisor with Task.yield/2 and
      Task.shutdown(task, :brutal_kill) on timeout. 12 on-disk entries before
      this append; 13 after.

  - req: REQ-155
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T07:13:20Z"
    note: >
      WF02-REQ155-20260827 complete. Host-enforced Lua wall-clock timeout added
      in Letflow.Engine.Lua.Executor by running each execution in a supervised
      Task.Supervisor async_nolink task, awaiting with Task.yield/2, and
      hard-killing on timeout with Task.shutdown(task, :brutal_kill). The outer
      host timeout survives even when the script pcall-catches its own
      :max_instructions budget exhaustion. Timeout surfaces as
      {:error, {:timeout_exceeded, timeout_ms}} and is distinctly matched from
      REQ-154's {:error, {:budget_exceeded, limit}}. The moduledoc now states
      the layer-2 restatement and the host-enforced requirement, and the task
      uses Letflow.Engine.PluginTaskSupervisor as the existing supervised task
      boundary. mix compile --warnings-as-errors and mix test
      test/letflow/engine/lua/executor_test.exs both passed (Result: 17 passed).
      Status flipped in_progress -> done in docs/requirements.yaml.

  - req: REQ-133
    event: done
    agent: FRONTEND-DEV
    at: "2026-08-26T21:00:00Z"
    note: >
      WF03-ISS0296-20260826 complete. Live browser login proof: PLATFORM_ADMIN
      (admin-user) and TASK_WORKER (worker-user) both complete OIDC
      redirect/callback flow via real Keycloak port 8093, Chromium headless.
      PLATFORM_ADMIN nav: Instances, My Tasks, Definitions, DLQ, Webhooks,
      Users, Groups, Tokens, Audit, Health, Metrics, Register Tenant, Tenants,
      Services. TASK_WORKER nav: My Tasks only. Authenticated API call
      GET /api/v1/identity/users with admin bearer token returns 200 with real
      user list (3 users). TASK_WORKER bearer token against same route returns
      403 RFC-9457 problem document. localStorage and sessionStorage empty
      after OIDC login (InMemoryWebStorage, FNFR-06 compliant). All 9
      Playwright tests pass (oidcf-login: 5/5, req133-ac2-ac5: 4/4).
      type-check 0 errors, lint 0 warnings, unit tests 184/184, guards 48/48.
      Two incidental fixes: (1) tenant_config.ex derives IDP URL from
      Application :oidc issuer config so per-workspace keycloak port overrides
      propagate without BPM_IDP_BASE_URL env var; (2) stale admin-user
      external_id in dev DB updated to match current Keycloak sub UUID.
      ISS-0296 closed.

  - req: REQ-150
    event: started
    agent: ORCH
    at: "2026-08-26T18:32:00Z"
    note: >
      WF02-REQ150-20260826 started (queue task 336, GH#649, stage S5).
      Step 00 git-setup PASS (ELIXIR-DEV): branch feature/WF02-REQ150-20260826
      created and pushed. Fresh restart of a run a prior session had begun
      and abandoned (stale branch never registered/merged) -- deleted and
      restarted clean before this dispatch.

  - req: REQ-150
    event: done
    agent: DOC-UPDATER
    at: "2026-08-26T19:45:10Z"
    note: >
      WF02-REQ150-20260826 complete. Design-only requirement settling OQ-6 (Lua
      5.3 integer/float marshalling across Letflow.Engine.VariableMerge and JSONB
      variable storage). lib/letflow/design/req150-lua-number-marshalling.md
      produced, with normative rule in §2 (cited as REQ-150 §2.n by LUA-11's
      REQ-159/REQ-160), owning module/function named in §3, and WASM-12 parity
      binding recorded in §4. Passed design, CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER, REVIEWER, TEST-DESIGNER scope-test, and
      RELEASE-VALIDATOR gates. No lib/letflow/engine/ change, confirmed. Decision
      record docs/migration/decisions/0014-scripting-plugin-runtime-strategy.md's
      OQ-6 updated to point at the settled design. Status flipped pending -> done
      in docs/requirements.yaml.

  - req: REQ-152
    event: started
    agent: ORCH
    at: "2026-08-26T20:19:00Z"
    note: >
      WF02-REQ152-20260826 started (queue task 304, GH#604, stage S5). Step 00
      git-setup PASS (ELIXIR-DEV): branch feature/WF02-REQ152-20260826 created
      and pushed. Claimed via get_next_task after ORCH force-released expired
      queue tasks 301/302/303 (REQ-149/150/151, all actually done, orphaned as
      depends_on targets by a prior queue reset that re-registered them as
      335/336/337 without repointing downstream edges) which had silently
      blocked this task. SECURITY-REVIEWER hard gate applies -- modifies the
      Lua sandbox boundary.

  - req: REQ-152
    event: done
    agent: DOC-UPDATER
    at: "2026-08-26T21:03:23Z"
    note: >
      WF02-REQ152-20260826 complete. Deny all four ambient Lua time sources
      (os.time, os.date, os.clock, os.difftime) from inside a script and
      implement platform.now() returning Letflow's authoritative time as an
      injectable ISO 8601 UTC string, per LUA-14 restated. Combined with
      REQ-151's general enumeration, all eleven Lua 5.3 os functions are
      proven unreachable by test. Default denials (os.execute, load) confirmed
      still held after the sandbox change, guarding the :sandboxed-replaces-
      defaults trap. platform.now documented as ungated by design per
      R-Co/src/lua/host_api/mod.zig's capability matrix. Passed design,
      security-reviewer (hard gate, sandbox boundary), reviewer, test-designer,
      test-design-validator, test-runner (2092/2092 passing), and
      release-validator (independent re-verification). Status flipped
      in_progress -> done in docs/requirements.yaml.

  - req: REQ-153
    event: started
    agent: ORCH
    at: "2026-08-27T08:45:00Z"
    note: >
      WF02-REQ153-20260827 started (queue task 305, GH#605, stage S5).
      Step 00 git-setup PENDING (ELIXIR-DEV dispatched): branch
      feature/WF02-REQ153-20260827 to be created and pushed. 5 on-disk
      entries before this append; 6 after.

  - req: REQ-153
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T04:43:57Z"
    note: >
      WF02-REQ153-20260827 complete. Per-invocation Lua state isolation and
      concrete LuaScriptAudit.Executor implementation (LUA-02). Module
      Letflow.Engine.Lua.Executor at lib/letflow/engine/lua/executor.ex:
      implements @behaviour Letflow.Engine.LuaScriptAudit.Executor, calls
      Sandbox.new/0 per invocation (no state reuse), runs Lua source via
      Lua.eval!/2, returns {:ok, %{manifest_hash: sha256_hex}}. script_ref
      concrete shape settled as binary source text. INV-LSA-1 and INV-LSA-2
      confirmed held with real executor via execute_script_for_audit/6 tests.
      11/11 executor tests pass. 2100/2108 full suite (8 pre-existing, none
      attributable to this branch). Status flipped in_progress -> done.
  - req: REQ-154
    event: started
    agent: ORCH
    at: "2026-08-27T06:32:50Z"
    note: >
      WF02-REQ154-20260827 started (queue task 306, GH#606, stage S5).
      Step 00 git-setup PENDING (ELIXIR-DEV dispatched): branch
      feature/WF02-REQ154-20260827 to be created and pushed. 7 on-disk
      entries before this append; 8 after.

  - req: REQ-157
    event: started
    agent: ORCH
    at: "2026-08-27T06:59:00Z"
    note: >
      WF02-REQ157-20260827 started (queue task 309, GH#609, stage S5).
      Step 00 git-setup PASS (ELIXIR-DEV): branch
      feature/WF02-REQ157-20260827 created and pushed. Dispatching Step 1
      to CODE-DESIGNER next. 8 on-disk entries before this append; 9 after.

  - req: REQ-154
    event: done
    agent: TEST-RUNNER
    at: "2026-08-27T13:42:00Z"
    note: >
      WF02-REQ154-20260827 Step 04 TEST-RUNNER complete. mix compile
      --warnings-as-errors exit 0. Full suite: 2110/2112 passed. Two failures:
      (1) SandboxTest AC1 Windows grep pre-existing known failure, not
      attributable to REQ-154. (2) LintHandoffsTest H6 NEW: security-reviewer
      handoff result.summary was a map; fixed lint_handoffs.ex check_advisory/2
      to coerce non-binary summary to empty string before String.length/1. After
      fix only pre-existing failure remains. executor_test.exs: 15/15 pass.
      AC-1 PASS, AC-2 PASS, AC-3 PASS, AC-4 PASS. Verdict: PASS.

  - req: REQ-154
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T14:10:00Z"
    note: >
      WF02-REQ154-20260827 complete. In-band instruction budget for Lua script
      execution (LUA-08 layer 1 of 2). execute_with_manifest/3 added to
      Letflow.Engine.Lua.Executor with configurable :max_instructions opt;
      execute_with_manifest/2 delegates via default_budget/0 (Application config
      :lua_max_instructions). Budget exhaustion returns {:error, {:budget_exceeded,
      limit}}. Sandbox.new/1 updated to pass :max_instructions to Lua.new/1.
      ISS-0350 fix: is_binary guard and @impl restored on execute_with_manifest/2.
      Moduledoc states LUA-08 layer-1 restatement and INV-2/INV-4 non-port
      explanation. 4 new AC tests (AC-1..4) + 11 inherited REQ-153 tests all pass
      (15/15). Status flipped in_progress -> done.

  - req: REQ-157
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T10:34:27Z"
    note: >
      WF02-REQ157-20260827 complete. Capability model and capability check at
      every gated host-function call site (LUA-05, LUA-06). Platform table
      exposes exactly the eight functions of R-Co's host_api/mod.zig matrix
      (call_service, read_variable, write_variable, log, emit_event,
      get_instance_state, now, fail) and no others, enumerated from inside a
      script. Capability-denial tests present for all six gated functions,
      each asserting a structured Lua error naming function, capability
      required, and capabilities granted. call_service's grant confirmed
      parameterised by service id (service:call:alpha does not authorise
      call_service('beta')). now and fail confirmed callable with an empty
      capability set. Moduledoc reproduces the R-Co capability matrix
      including the now/fail ungated-by-design rationale. Passed design,
      CODE-DESIGN-VALIDATOR (2 rework iterations to remove literal Elixir
      code from the design doc), SECURITY-REVIEWER (hard gate, authorization
      boundary for tenant-supplied scripts), REVIEWER, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, and RELEASE-VALIDATOR
      (independent re-verification, Step 5 PASS). Status flipped
      pending -> done in docs/requirements.yaml.
      CORRECTION to this run's own earlier draft of this note: it originally
      claimed no `started` event had ever been logged for REQ-157 in this
      volume. That was wrong -- the started event above (agent: ORCH, at:
      "2026-08-27T06:59:00Z") landed on main from this same run while this
      branch was in flight and only became visible on rebase; there was no
      process gap after all.

  - req: REQ-155
    event: started
    agent: ORCH
    at: "2026-08-27T11:07:00Z"
    note: >
      WF02-REQ155-20260827 started (queue task 307, GH#607, stage S5).
      Claimed via get_next_task after REQ-157's completion. Out-of-band
      host-enforced wall-clock kill for Lua script execution -- layer 2 of
      the two-layer pair (LUA-10 restated), depends on REQ-154 (done).
      Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ155-20260827 created from main (7b6fcff) and pushed.
      Dispatching Step 1 to CODE-DESIGNER next. 12 on-disk entries before
      this append; 13 after.

  - req: REQ-155
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T13:44:03Z"
    note: >
      WF02-REQ155-20260827 complete. Out-of-band host-enforced wall-clock kill
      for Lua script execution -- layer 2 of the two-layer pair (LUA-10
      restated). Script execution runs under a supervised task with a
      CONFIGURABLE wall-clock timeout enforced from outside the process
      (Process.exit(pid, :kill) on timeout), proven to still terminate a
      script that has pcalled its own REQ-154 instruction-budget exhaustion
      and kept looping, with the dead process/no-child asserted post-timeout
      and the timeout error structurally distinguishable from REQ-154's
      in-band budget error. Passed design, SECURITY-REVIEWER (hard gate),
      REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER, and
      RELEASE-VALIDATOR (Step 5 recheck, PASS). This run needed 2 rework
      iterations, each of which found and fixed a real bug rather than a
      cosmetic one: (a) config/test.exs's lua_wallclock_timeout_ms was too
      tight at 200ms under parallel test-suite load, causing an intermittent
      spurious failure in an unrelated test -- an intermediate 1500ms was
      tried and also found insufficient before it was raised to 5000ms;
      (b) two flaky tests in executor_test.exs were hardened/root-cause-fixed
      -- AC-5's fixed-sleep in-flight-child check was replaced with a
      poll/retry loop, and a long-standing "execute_with_manifest/2 is
      exported" function_exported?/3 race was root-cause-fixed via
      Code.ensure_loaded/1 rather than papered over. Separately, ISS-0340 (a
      previously "resolved" flaky test) was found recurring during this run's
      original Step 4 and has been reopened -- see docs/issues/ISS-0340.yaml
      (status: reopened), not fixed as part of this requirement. Status
      flipped pending -> done in docs/requirements.yaml.

  - req: REQ-156
    event: started
    agent: ORCH
    at: "2026-08-27T13:58:30Z"
    note: >
      WF02-REQ156-20260827 started (queue task 308, GH#608, stage S5).
      Claimed via get_next_task after REQ-155's completion. Configurable
      memory limit for Lua script execution, per REQ-149's decided
      mechanism (LUA-09 restated), depends on REQ-149 (done) and REQ-155
      (done). Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ156-20260827 created from main (3dbbdb2) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 14 on-disk
      entries before this append; 15 after.

  - req: REQ-156
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T14:58:20Z"
    note: >
      WF02-REQ156-20260827 complete. Configurable memory limit for Lua script
      execution via :max_heap_size (LUA-09 restated), per REQ-149's decided
      mechanism. Design needed 1 rework iteration -- a single literal-Elixir-
      code violation in a table cell; substance was approved first pass,
      including a source-verified finding (confirmed against real Elixir
      1.20.3 source, task/supervisor.ex and task/supervised.ex) that
      Task.Supervisor.async_nolink/2 structurally cannot carry a
      max_heap_size spawn_opt, so the memory-limited path spawns directly via
      :erlang.spawn_opt/2 instead. TEST-DESIGNER found and closed a real
      mutation-detectable coverage gap: a caller-issued-kill-on-timeout
      branch, distinct from the BEAM heap-kill branch, was previously
      untested -- independently re-verified by TEST-DESIGN-VALIDATOR
      reproducing the same mutation from scratch. Design, security, idiom,
      test-design, test-run, and release-validation all passed with only that
      one design rework and one test-design gap-closure -- no implementation
      rework was needed at all (unlike sibling REQ-155, which needed 2
      implementation rework rounds). Status flipped pending -> done in
      docs/requirements.yaml.

  - req: REQ-158
    event: started
    agent: ORCH
    at: "2026-08-27T15:15:00Z"
    note: >
      WF02-REQ158-20260827 started (queue task 310, GH#610, stage S5).
      Claimed via get_next_task after REQ-156's completion. Capability
      manifest validation on script load, wired to the existing
      LuaScriptAudit hash path (LUA-07), depends on REQ-157 (done).
      Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ158-20260827 created from main (76b089d) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 16 on-disk
      entries before this append; 17 after.

  - req: REQ-158
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T16:35:39Z"
    note: >
      WF02-REQ158-20260827 complete. Manifest representation (ported from
      R-Co/src/lua/manifest.zig, capability set + identity fields, with
      dropped fields named/justified in the moduledoc), a load-time
      validate_at_load/3 entry point that rejects a modified manifest before
      any script text executes, and a manifest hash that feeds
      LuaScriptAudit's existing INV-LSA-2 mismatch check (LUA-07) rather than
      reimplementing or bypassing it. SECURITY-REVIEWER found and ELIXIR-DEV
      fixed a REAL hash-collision BLOCKER: under the original raw-separator
      byte layout, two different capability sets could hash identically via
      delimiter injection (e.g. ["a\nb","c"] vs ["a","b","c"]) -- fixed with
      length-prefixed framing (a 32-bit length prefix per field) and
      independently re-verified structurally collision-free by
      SECURITY-REVIEWER's recheck. TEST-DESIGNER separately found and closed
      a real mutation-detectable test gap: every prior test compared the hash
      function only against itself on different inputs, never against an
      independently-reconstructed expected value, so a systematic framing
      weakening (e.g. shrinking to a 16-bit length prefix) would have slipped
      through completely undetected -- TEST-DESIGN-VALIDATOR independently
      reproduced that same mutation from scratch to confirm the fix closed
      the gap. AC2 (rejection before execution) is satisfied with a disclosed
      caller-discipline scope gap: validate_at_load/3 has no production
      caller yet, mirroring REQ-058's own accepted "no caller yet" precedent,
      examined and accepted as non-blocking by SECURITY-REVIEWER twice,
      deferred to REQ-159/160/161. RELEASE-VALIDATOR's independent re-run hit
      an unrelated recurrence of the already-tracked ISS-0340 flake,
      structurally cleared via isolation testing. Status flipped pending ->
      done in docs/requirements.yaml.

  - req: REQ-159
    event: started
    agent: ORCH
    at: "2026-08-27T16:52:00Z"
    note: >
      WF02-REQ159-20260827 started (queue task 311, GH#611, stage S5).
      Claimed via get_next_task after REQ-158's completion. Lua host
      API 1/2 -- read path -- platform.read_variable, get_instance_state,
      log (LUA-11 read half, LUA-13), depends on REQ-158 and REQ-150
      (both done). Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ159-20260827 created from main (f79d828) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 18 on-disk
      entries before this append; 19 after.

  - req: REQ-159
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T18:08:14Z"
    note: >
      WF02-REQ159-20260827 complete. First requirement to implement real
      platform.* host-function bodies: platform.read_variable, platform.
      get_instance_state, platform.log (LUA-11 read half, LUA-13), using the
      new Letflow.Engine.LuaNumberMarshalling module settled by REQ-150. Step
      1b CODE-DESIGN-VALIDATOR found and required fixing a real design-stage
      authorization gap: get_instance_state's original design, via an
      unsupported invented "parent reading child's state" scenario, allowed
      reading ANY instance the tenant owned -- fixed to restrict reads to a
      script's own instance only, returning :forbidden uniformly for anything
      else, real instance or nonexistent. TEST-DESIGNER separately found and
      closed a real mutation-detectable test gap via active mutation testing:
      the existing tests verified the CORRECT RETURN VALUE of the self-
      instance check but not the actual security property -- that no
      Repo/database call happens at all on a mismatch -- closed with a
      :telemetry.attach/4-based test proving zero Repo calls occur, and
      independently re-verified by TEST-DESIGN-VALIDATOR reproducing the exact
      mutation from scratch, then spot-checked again at the source level by
      RELEASE-VALIDATOR. SECURITY-REVIEWER additionally confirmed no timing
      side-channel exists between a same-tenant-sibling-instance rejection and
      a nonexistent-instance rejection: both take the identical zero-Repo-call
      code path. ELIXIR-DEV made two well-justified implementation deviations
      from the design's literal text -- run_stub's real arity, forced by
      Lua.encode!/2's actual needs, and log's explicit Lua.decode!/2 step,
      since the design's assumption that table args arrive pre-decoded was
      factually wrong -- both confirmed by REVIEWER as forced by the real
      tv-labs/lua library API rather than shortcuts, and documented via a
      design-doc addendum TEST-DESIGNER added. Passed design (with the Step 1b
      rework above), CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (hard gate),
      REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER, and
      RELEASE-VALIDATOR (Step 5 PASS). Status flipped pending -> done in
      docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 20 on-disk entries.

  - req: REQ-160
    event: started
    agent: ORCH
    at: "2026-08-27T18:25:30Z"
    note: >
      WF02-REQ160-20260827 started (queue task 312, GH#612, stage S5).
      Claimed via get_next_task after REQ-159's completion. Lua host
      API 2/2 -- write path -- platform.write_variable (staged and
      atomic across every failure arm: raised script error, REQ-154's
      instruction budget, REQ-155's wall-clock kill, REQ-156's memory
      limit), call_service, emit_event (LUA-11 write half, LUA-12).
      Depends on REQ-159, REQ-158, REQ-157 (all done). The
      transactional-staging-surviving-a-brutal-process-kill requirement
      is the architecturally hardest Lua host-function work this
      session. Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ160-20260827 created from main (44ba8a2) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 20 on-disk
      entries before this append; 21 after.

  - req: REQ-160
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T20:17:26Z"
    note: >
      WF02-REQ160-20260827 complete. Lua host API 2/2 -- write path --
      platform.write_variable staged-and-atomic across every S5 failure arm
      (raised script error, REQ-154's instruction budget, REQ-155's
      wall-clock kill, REQ-156's memory limit), call_service, emit_event
      (LUA-11 write half, LUA-12). This run went through one design rework
      (Step 1b CODE-DESIGN-VALIDATOR rejected the original design for a
      kill-race invariant gap -- fixed by adding INV-write-discard-on-race,
      ensuring the staging buffer's only escape hatch is a normal per-process
      return, never read by platform.ex itself on a killed process) and one
      test-design rework (missing kill-arm and atomicity tests, closed with
      dedicated tests for each of the four failure arms plus a
      VariableMerge.merge/3 atomicity test). RELEASE-VALIDATOR (Step 5)
      independently re-derived all 8 acceptance criteria against real code
      and freshly-run tests: `mix test test/letflow/engine/lua/platform_test.exs`
      -- 90 tests, 0 failures; `mix compile --warnings-as-errors` -- exit 0,
      no warnings. Passed design (with Step 1b rework), CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER (hard gate), REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR
      (with rework), TEST-RUNNER, and RELEASE-VALIDATOR (Step 5 PASS). Status
      flipped pending -> done in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 21 on-disk entries before this
      append; 22 after.

  - req: REQ-161
    event: started
    agent: ORCH
    at: "2026-08-27T20:30:47Z"
    note: >
      WF02-REQ161-20260827 started (queue task 313, GH#613, stage S5).
      Claimed via get_next_task after REQ-160's completion. platform.fail
      terminates by a mechanism the script cannot intercept (LUA-15
      restated) -- decision 0014's watchlist item: an ordinary raised
      Lua error would let a script pcall its own platform.fail call and
      continue running, defeating the requirement entirely. Depends on
      REQ-160 (done). SECURITY-REVIEWER hard gate. Step 00 git-setup
      PASS (ORCH, direct): branch feature/WF02-REQ161-20260827 created
      from main (4ad5347) and pushed. Dispatching Step 1 to
      CODE-DESIGNER next. 22 on-disk entries before this append; 23
      after.

  - req: REQ-161
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T22:28:33Z"
    note: >
      WF02-REQ161-20260827 complete. platform.fail now terminates by a
      mechanism the script cannot intercept (LUA-15 restated), replacing the
      old raise-based stub -- the exact pcall-catchable hazard LUA-15's
      restatement exists to fix -- with an exit/1-based uninterceptable
      termination mechanism. CODE-DESIGN-VALIDATOR verified the exit/1
      approach against the real vendored deps/lua source rather than assumed
      behaviour, and RELEASE-VALIDATOR empirically re-confirmed it by running
      an actual ad hoc script and observing the raw crash/exit term, not just
      a passing test. One small REVIEWER rework round: 2 stale do_fail/1
      moduledoc arity references, fixed directly by ORCH as a
      sizing-rule-eligible mechanical text fix. Passed design,
      CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER (hard gate), REVIEWER (with
      the one small rework above), TEST-DESIGNER, TEST-DESIGN-VALIDATOR,
      TEST-RUNNER, and RELEASE-VALIDATOR (Step 5 PASS, empirical
      re-confirmation). Status flipped pending -> done in
      docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 23 on-disk entries before
      this append; 24 after.

  - req: REQ-162
    event: started
    agent: ORCH
    at: "2026-08-27T22:40:25Z"
    note: >
      WF02-REQ162-20260827 started (queue task 314, GH#614, stage S5).
      Claimed via get_next_task after REQ-161's completion. Uncaught
      Lua errors captured as structured SCRIPT_ERROR events (LUA-16
      restated) -- stack trace, capability state at failure (from
      REQ-157's real capability set, not re-derived), and an
      instruction-count field whose retrievability must be resolved
      empirically per REQ-148 spike answer (c), never zero-filled if
      not retrievable. Must distinguish SCRIPT_ERROR from four other
      arms by pattern match: REQ-161's SCRIPT_FAILED, REQ-154's budget
      error, REQ-155's timeout, REQ-156's memory limit. The literal
      "division by zero" acceptance text is a Lua 5.1 assumption --
      in 5.3, 1/0 yields inf (no raise); must substitute 1//0 or 1%0
      and document why. Depends on REQ-161, REQ-157 (both done).
      SECURITY-REVIEWER hard gate (error reports are a data-disclosure
      surface). Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ162-20260827 created from main (3c74cee) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 24 on-disk
      entries before this append; 25 after.

  - req: REQ-162
    event: done
    agent: DOC-UPDATER
    at: "2026-08-27T23:54:00Z"
    note: >
      WF02-REQ162-20260827 complete. Uncaught Lua errors are now captured
      host-side as structured SCRIPT_ERROR events (LUA-16 restated),
      distinguishable by pattern match from SCRIPT_FAILED (REQ-161),
      REQ-154's budget error, REQ-155's wall-clock timeout, and REQ-156's
      memory-limit error. This is the final requirement in the S5
      REQ-153..REQ-162 Lua host-function chain: all 8 platform.*
      capability-matrix rows are now real, and the full uncaught-error/
      deliberate-failure/kill taxonomy (SCRIPT_FAILED, SCRIPT_ERROR,
      budget-exceeded, wallclock-timeout, memory-limit) is complete and
      pattern-match-distinguishable end to end. Every gate (SECURITY-REVIEWER
      hard gate included, given error reports are a data-disclosure surface)
      passed clean on the first attempt -- no rework rounds. RELEASE-VALIDATOR
      did an empirical verification rather than a code read alone: it ran a
      real 1//0 script via `mix run` and observed the actual
      instruction_count field first-hand. 25 on-disk entries before this
      append; 26 after.

  - req: REQ-163
    event: started
    agent: ORCH
    at: "2026-08-28T00:10:32Z"
    note: >
      WF02-REQ163-20260828 started (queue task 315, GH#615, stage S5).
      Claimed via get_next_task after REQ-162's completion. First of
      two decision requirements (with REQ-164) unblocking S5's WASM
      half, per decision 0014's "Ownership of execution": settle OQ-4
      -- core modules vs. the component model for the WASM ABI.
      Decision artefact only, no mix.exs change, no lib/letflow/engine/
      edit. Must ground the choice in what the wasmex version Letflow
      would adopt actually supports (read the dependency/its published
      API, not inferred from an option name), restate WASM-02's
      four-export ABI into the chosen mode's concrete contract, name
      the exact import surface WASM-07's filesystem-denial test must
      target, and state the value-representation consequence for
      REQ-150's number-marshalling rule (WASM-12 parity). Lighter
      pipeline than a code requirement: CODE-DESIGNER -> CODE-DESIGN-
      VALIDATOR -> REVIEWER -> RELEASE-VALIDATOR -> DOC-UPDATER;
      ELIXIR-DEV/SECURITY-REVIEWER/TEST-* skipped, no code exists to
      implement or test. Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ163-20260828 created from main (6c8152f) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 26 on-disk
      entries before this append; 27 after.

  - req: REQ-163
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T00:26:15Z"
    note: >
      WF02-REQ163-20260828 complete. Decision-only requirement (no code, no
      mix.exs or lib/letflow/engine/ change) settling OQ-4: WASM core modules
      chosen over the component model for the WASM ABI, grounded in real
      wasmex v0.15.1 published documentation rather than inferred from the
      :wasm_component_model option's existence. Independently re-verified via
      live WebFetch by three separate gates -- CODE-DESIGN-VALIDATOR,
      REVIEWER, and RELEASE-VALIDATOR each independently confirmed the key
      wasmex claims against the real published docs, not just each other's
      say-so. lib/letflow/design/req163-wasm-abi-choice.md restates WASM-02's
      four-export ABI into the core-module contract, names the exact import
      surface WASM-07's filesystem-denial test must target, and states the
      value-representation consequence for REQ-150's number-marshalling rule
      (WASM-12 parity). This unblocks REQ-166 (execution), REQ-167
      (capability denial), and REQ-171/172 (host API parity) to build
      against a concrete ABI rather than R-Co's internally-inconsistent,
      never-implemented specification prose. Lighter pipeline than a code
      requirement (CODE-DESIGNER -> CODE-DESIGN-VALIDATOR -> REVIEWER ->
      RELEASE-VALIDATOR -> DOC-UPDATER, no ELIXIR-DEV/SECURITY-REVIEWER/
      TEST-*), passed every gate on first attempt. Status flipped pending ->
      done in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 27 on-disk entries before
      this append; 28 after.

  - req: REQ-164
    event: started
    agent: ORCH
    at: "2026-08-28T00:38:40Z"
    note: >
      WF02-REQ164-20260828 started (queue task 316, GH#616, stage S5).
      Claimed via get_next_task after REQ-163's completion. Second of
      two decision requirements unblocking S5's WASM half: settle OQ-3
      -- whether Letflow hosts WASM guest compilation at all
      (WASM-03/04/05). Decision 0014's own evidence already leans NO:
      R-Co/src/wasm/ is stubs end to end and a search for '*.wasm'
      across the entire R-Co tree returns zero files -- there is no
      working pipeline to port and no artifact corpus to stay
      compatible with. If NO, WASM-03/04/05 are DECLINED (not
      deferred) and REQ-175 must be CANCELLED with a status-history
      event, not left pending. If YES, the artefact must supply
      REQ-175's language-agnostic restatement (artifact-hash-keyed
      caching, not source-hash, since Letflow has no Zig toolchain).
      Decision-only requirement, no code. Lighter pipeline: CODE-
      DESIGNER -> CODE-DESIGN-VALIDATOR -> REVIEWER -> RELEASE-
      VALIDATOR -> DOC-UPDATER; no ELIXIR-DEV/SECURITY-REVIEWER/TEST-*.
      Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ164-20260828 created from main (fadb756) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 28 on-disk
      entries before this append; 29 after.

  - req: REQ-164
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T00:50:32Z"
    note: >
      WF02-REQ164-20260828 complete. Decision-only requirement (no code, no
      mix.exs or lib/letflow/engine/ change) settling OQ-3: NO, Letflow does
      not host WASM guest compilation. Letflow's WASM plugin pipeline accepts
      only pre-compiled .wasm artifacts submitted by the caller; Letflow never
      invokes a Zig or any other compiler as part of plugin intake,
      registration, or execution. Consequently WASM-03 (Source Compilation
      Job), WASM-04 (Compile Caching), and WASM-05 (Build Reproducibility) are
      DECLINED rather than deferred -- R-Co/src/wasm/ is stubs end to end and
      a search for '*.wasm' across the entire R-Co tree returns zero files,
      so nothing working exists to inherit. lib/letflow/design/
      req164-wasm-compilation-hosting.md records the decision, names the
      S6 operational inputs the decision needed, and states that REQ-175 (the
      conditional requirement gated on a YES) must be cancelled rather than
      left pending. Independently re-verified by CODE-DESIGN-VALIDATOR,
      REVIEWER, and RELEASE-VALIDATOR (three separate gates) before this
      step. Status flipped pending -> done in docs/requirements.yaml.
      `grep -c "^  - req:" docs/status/requirement_status.v5.yaml` = 29
      on-disk entries before this append; 30 after.

  - req: REQ-175
    event: cancelled
    agent: DOC-UPDATER
    at: "2026-08-28T00:50:32Z"
    note: >
      WF02-REQ164-20260828 Step 6. REQ-164 decided NO on OQ-3 -- Letflow does
      not host WASM guest compilation; WASM-03/04/05 declined; this
      conditional requirement is cancelled per its own text and REQ-164's
      acceptance criteria. See lib/letflow/design/
      req164-wasm-compilation-hosting.md's Decision section, which records
      this cancellation as its own required consequence. Status flipped
      pending -> cancelled in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 30 on-disk entries before
      this append; 31 after.

  - req: REQ-165
    event: started
    agent: ORCH
    at: "2026-08-28T01:12:40Z"
    note: >
      WF02-REQ165-20260828 started (queue task 317, GH#617, stage S5).
      Claimed via get_next_task after REQ-164's completion. FIRST
      requirement of S5's WASM half with real code (REQ-163/164 were
      decision-only) -- adopt wasmex (Wasmtime via a Rust NIF) behind
      the mandatory process boundary decision 0014 requires, with a
      pinned Rust toolchain in CI (OQ-7), restating WASM-01's mechanism
      clause honestly (Rust NIF, not the C API; runtime-loaded shared
      library, not a statically linked platform binary) while its
      acceptance criterion (no external Wasm runtime dependency at
      deploy time) is met as literally worded. Must disclose the
      residual risk decision 0014(a) records: a Wasmtime/NIF crash
      takes down the whole BEAM node, uncovered by the process
      boundary. Letflow's first native-code build dependency --
      SECURITY-REVIEWER hard gate, REVIEWER sign-off explicitly named.
      Environment pre-check: no Rust toolchain (cargo/rustc) currently
      installed -- ELIXIR-DEV must install one and report honestly if
      environment/network restrictions block a successful native
      build. Full WF-02 pipeline (not the lighter decision-only path
      REQ-163/164 used). Step 00 git-setup PASS (ORCH, direct): branch
      feature/WF02-REQ165-20260828 created from main (4601218) and
      pushed. Dispatching Step 1 to CODE-DESIGNER next. 31 on-disk
      entries before this append; 32 after.

  - req: REQ-123
    event: started
    agent: ORCH
    at: "2026-08-28T02:34:22Z"
    note: >
      WF02-REQ123-20260828 started (stage S8). Requirement-scoped
      decision-record task: produce a cutover-strategy decision grounded in
      actual S7 evidence (big-bang vs gradual/dual-running) and state Zig
      backend deprecation as a condition, not a date. Baseline evidence on
      pickup: docs/migration/stage-7-simulation-uat-parity.md still reads
      "Status: not started" with no expanded S7 requirements and no REVIEWER
      sign-off, so this run must decide whether that signal is sufficient
      rather than invent a preference. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 34 on-disk entries before
      this append; 35 after.

  - req: REQ-123
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T02:34:22Z"
    note: >
      WF02-REQ123-20260828 complete. Added
      docs/migration/decisions/0015-cutover-strategy.md with an explicit
      decision: S7 signal is currently insufficient to choose big-bang or
      gradual/dual-running, so cutover selection is deferred until parity
      evidence exists. The record cites concrete evidence by name from
      docs/migration/stage-7-simulation-uat-parity.md and
      docs/migration/stage-8-frontend-cutover.md, states Zig backend
      deprecation as conditions (not a date), and names the specific missing
      evidence required to settle the strategy. Updated
      docs/migration/stage-8-frontend-cutover.md Decisions section to link the
      record and remove the open-question wording. Status flipped pending ->
      done in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 35 on-disk entries before
      this append; 36 after.

  - req: REQ-165
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T02:54:59Z"
    note: >
      WF02-REQ165-20260828 complete (RELEASE-VALIDATOR PASS, commit f518c8a).
      Adopted wasmex (Wasmtime via a Rust NIF) behind the mandatory process
      boundary decision 0014 requires -- Letflow's FIRST native-code build
      dependency and the first WASM-half requirement with real code (REQ-163/
      164 were decision-only). Every gate passed clean on the first attempt:
      design, both hard gates (SECURITY-REVIEWER and REVIEWER, the latter
      explicitly named by decision 0014 as this requirement's sign-off),
      mutation-tested test design, full-suite run, and release validation.
      REVIEWER independently rebuilt the entire Rust toolchain from scratch to
      verify the native build rather than trusting a report. RELEASE-VALIDATOR
      empirically confirmed the process boundary via an ad hoc script, not
      just a code read. TEST-DESIGNER's mutation testing found and closed 2
      real coverage gaps: a GenServer leak-detection gap, and zero prior
      coverage of the new toolchain-check F9 failure mode. This branch was
      rebased mid-run onto concurrent work from another session (REQ-155/
      REQ-123-related commits, plus ORCH's own ISS-0287 reopen) with no
      conflicts. Unblocks REQ-166 (the full plugin ABI, building on REQ-163's
      core-modules decision). Status flipped pending -> done in
      docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 36 on-disk entries before this
      append; 37 after.

  - req: REQ-166
    event: started
    agent: ORCH
    at: "2026-08-28T03:31:50Z"
    note: >
      WF02-REQ166-20260828 started (queue task 318, GH#618, stage S5).
      Claimed via get_next_task after REQ-165's completion. WASM
      module ABI validation and rejection of non-conforming modules
      AT REGISTRATION, not first invocation (WASM-02). Implements the
      export contract REQ-163 chose (core modules) -- must cite
      REQ-163's artefact, not re-derive the mapping. Rejection must
      name the specific missing export in a structured error, and
      registration must be a distinct step from invocation so a
      rejected module is never invocable. SECURITY-REVIEWER hard gate.
      Depends on REQ-165 (done, provides the process boundary/handler
      pattern this builds on). Step 00 git-setup PASS (ORCH, direct):
      branch feature/WF02-REQ166-20260828 created from main (53ce710)
      and pushed. Dispatching Step 1 to CODE-DESIGNER next. 37 on-disk
      entries before this append; 38 after.

  - req: REQ-166
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T05:01:13Z"
    note: >
      WF02-REQ166-20260828 complete (RELEASE-VALIDATOR Step 5 PASS, commit
      637f631). WASM module ABI validation implemented in
      lib/letflow/engine/wasm/module_registry.ex: a module missing execute
      (or any of init/deinit/get_capabilities/alloc/memory) is rejected at
      REGISTRATION with a structured {:error, {:invalid_abi, defects}} error
      naming the missing export, never at first invocation -- registration is
      a distinct two-stage gate (export-shape check, then a real
      Wasmex.start_link/1 instantiation attempt) so a rejected module can
      never produce a registered_module() value and is never invocable. A
      conforming module registers successfully and is proven actually
      invocable, not merely export-shaped. Moduledoc cites REQ-163's
      core-modules artefact (req163-wasm-abi-choice.md sections 3.1/3.2/4) by
      name and states the WASM-02 restatement plus scope boundary (no
      capability sandboxing/resource limits/host API/versioning -- those are
      REQ-167/169/170/171/172/173). mix test test/letflow/engine/wasm/: 30
      passed, 0 failures. mix compile --force --warnings-as-errors: 0
      warnings, exit 0. Full parallel suite: 2318/2318 passed. ISS-0351 (a
      Task.shutdown(:brutal_kill) native-thread-cleanup edge case) filed as
      narrow/MINOR/out-of-scope and confirmed non-blocking by
      RELEASE-VALIDATOR -- deferred to whichever future requirement first
      wires a tenant-facing registration path. No scope creep: touches only
      module_registry.ex (new) plus a 14-line sibling Task.Supervisor child
      in lib/letflow/application.ex. Passed design, CODE-DESIGN-VALIDATOR,
      SECURITY-REVIEWER (hard gate), REVIEWER, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, and RELEASE-VALIDATOR (Step 5
      independent re-verification). Status flipped pending -> done in
      docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 38 on-disk entries before
      this append; 39 after.
  - req: REQ-167
    event: started
    agent: ORCH
    at: "2026-08-28T05:13:32Z"
    note: >
      WF02-REQ167-20260828 started (queue task 319, GH#619, stage S5).
      Claimed via get_next_task after REQ-166's completion. WASM import
      whitelist derived from the module manifest (WASM-06) plus WASM-07's
      restated no-filesystem-by-default guarantee, asserted against
      REQ-163's named core-modules import surface rather than the
      component-model-specific wasi:filesystem/types literal. Enforcement
      point is instantiation failure, not a present-but-erroring import.
      This is the sandbox boundary for guest code -- SECURITY-REVIEWER hard
      gate. Depends on REQ-166 (done). Builds on
      Letflow.Engine.Wasm.ModuleRegistry (REQ-166) and PluginHandler
      (REQ-165).

  - req: REQ-167
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T06:15:38Z"
    note: >
      WF02-REQ167-20260828 complete (RELEASE-VALIDATOR Step 5 PASS, commit
      78d4101). WASM import whitelist built FROM the module manifest's
      declared capabilities (WASM-06) in
      lib/letflow/engine/wasm/capability_gate.ex: a module declaring var:read
      only cannot import platform_call_service, instantiation fails
      structurally ({:instantiation_denied, {:unresolved_import, ...}}) with
      no calling-process crash, and the import table is inspected
      structurally (Map.has_key?/refute) rather than via invoke-and-catch.
      WASM-07's no-filesystem-by-default guarantee is restated and tested
      against REQ-163's named core-modules import surface
      (wasi_snapshot_preview1::path_open) rather than the component-model-
      specific wasi:filesystem/types literal that would produce a false
      pass under core modules -- moduledoc explains this substitution
      explicitly, restates WASM-07 citing req163-wasm-abi-choice.md, and
      states plainly that no filesystem-grant path exists today. mix test
      test/letflow/engine/wasm/: 48 passed, 0 failures. mix compile
      --warnings-as-errors --force: clean, 0 warnings. Scope confirmed via
      git diff --stat main...HEAD: only capability_gate.ex (new),
      req167-wasm-import-whitelist.md (new design doc), 4 new
      priv/wasm_fixtures/req167_*.wat fixtures, capability_gate_test.exs
      (new), test/specs/REQ-167.md (new), and a 12-line
      CapabilityGateTaskSupervisor addition to application.ex --
      module_registry.ex and plugin_handler.ex both confirmed untouched
      (empty targeted diff). Passed design, SECURITY-REVIEWER (hard gate,
      sandbox boundary for guest code), REVIEWER, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, and RELEASE-VALIDATOR (Step 5
      independent re-verification of all 7 real acceptance criteria against
      shipped code and freshly re-run test/compile output). No REQ-167
      reference found under docs/migration/ needing a companion update.
      Status flipped pending -> done in docs/requirements.yaml. `grep -c
      "^  - req:" docs/status/requirement_status.v5.yaml` = 40 on-disk
      entries before this append; 41 after.
  - req: REQ-168
    event: started
    agent: ORCH
    at: "2026-08-28T06:37:33Z"
    note: >
      WF02-REQ168-20260828 started (queue task 320, GH#620, stage S5).
      Claimed via get_next_task after REQ-167's completion. WASM linear
      memory isolation and host-side validation of every guest
      pointer/length pair (WASM-08) -- the entry on decision 0014's
      satisfiable list with the highest cost if wrong, since under
      wasmex/Wasmtime an unvalidated guest pointer dereference is a
      BEAM-node-level fault, not a request-level one. A single validation
      function every future host function (REQ-171/172) will consume,
      written once here before those requirements exist. SECURITY-REVIEWER
      hard gate. Depends on REQ-166 (done).

  - req: REQ-168
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T07:51:17Z"
    note: >
      WF02-REQ168-20260828 complete (RELEASE-VALIDATOR Step 5 PASS, commit
      69f021e). WASM-08 linear memory isolation and host-side validation of
      every guest pointer/length pair implemented in
      lib/letflow/engine/wasm/memory_guard.ex: check_bounds/3 is the sole
      arithmetic core, and read/4 and write/4 are the only functions that
      ever call Wasmex.Memory.*, confirmed by
      `grep -rn 'Wasmex.Memory\.' lib/ --include='*.ex'` -- all 10 hits
      confined to memory_guard.ex. A malformed pointer/length pair from a
      guest yields a structured {:error, {:invalid_pointer, ...}} outcome,
      with the BEAM node and its supervisor confirmed alive afterward, never
      an exception/exit/native fault. Tests cover offset-beyond-size,
      length-past-end, and offset+length overflow, each rejected
      pre-dereference via check_bounds/3 alone for the extreme-magnitude
      cases (no live Wasmex call ever uses a magnitude >= 2^40, per the live
      SIGABRT safety constraint independently re-verified by 3 prior gates
      plus RELEASE-VALIDATOR). Per-instance isolation proven with two real
      Wasmex instances: a write in one is not observable in the other.
      Moduledoc states the boundary honestly per REQ-165: bounds host-side
      dereferences only, does NOT bound a fault inside Wasmtime's own native
      code, citing req165-wasmex-process-boundary.md sec 7.2 by name and
      quoting its "does not raise, exit, or trap in the ordinary BEAM sense"
      text verbatim. mix test test/letflow/engine/wasm/: 66 passed, 0
      failures (2 pre-existing, unrelated module_registry_test.exs warnings).
      mix compile --warnings-as-errors: exit 0, clean. Scope confirmed via
      git diff --stat main...HEAD: 14 files, all additions, no deletions --
      module_registry.ex, capability_gate.ex, plugin_handler.ex, and
      application.ex all confirmed untouched. No REQ-168 reference found
      under docs/migration/ needing a companion update. Passed design,
      SECURITY-REVIEWER (hard gate), REVIEWER, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, and RELEASE-VALIDATOR (Step 5
      independent re-verification of all 6 real acceptance criteria against
      shipped code and freshly re-run test/compile output). Status flipped
      pending -> done in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 42 on-disk entries before
      this append; 43 after.
  - req: REQ-169
    event: started
    agent: ORCH
    at: "2026-08-28T08:04:32Z"
    note: >
      WF02-REQ169-20260828 started (queue task 321, GH#621, stage S5).
      Claimed via get_next_task after REQ-168's completion. WASM fuel
      budget (WASM-09) and linear-memory cap (WASM-10) per invocation,
      paired since both are direct configuration of the same wasmex
      objects (Wasmex.EngineConfig's :consume_fuel, default FALSE --
      note the default: a config that forgets to enable it produces an
      unlimited guest with no error anywhere -- and Wasmex.StoreLimits'
      memory_size/table_elements). Decision 0014's evidence for both was
      READ from documentation, not run -- if either doesn't behave as
      documented, that's a finding to record, not work around silently.
      Fuel must be per-invocation (reset between calls, not per-store).
      SECURITY-REVIEWER hard gate. Depends on REQ-168 (done).

  - req: REQ-169
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T09:15:56Z"
    note: >
      WF02-REQ169-20260828 complete (RELEASE-VALIDATOR Step 5 PASS, commit
      57148c8). WASM fuel budget (WASM-09) and linear-memory cap (WASM-10) per
      invocation implemented in lib/letflow/engine/wasm/resource_limits.ex:
      build_store/1 enables Wasmex.EngineConfig's :consume_fuel: true
      explicitly (default is false), arm_fuel/2 resets a configurable
      per-invocation fuel budget between calls (not per-store), and
      StoreLimits' memory_size caps linear memory per instance, both
      configurable rather than hardcoded. Live-verified WASM-10 divergence:
      memory.grow beyond the configured cap does NOT trap as decision 0014
      had assumed from documentation alone -- it returns an ordinary
      {:ok, [-1]} success -- but real memory size is provably unchanged
      (memory_grew_within_cap?/3), so the underlying security property still
      holds even though the literal "traps cleanly" wording does not.
      Disclosed, not worked around silently: moduledoc states the divergence
      by name, and REVIEWER's Step 2d review corrected
      docs/migration/decisions/0014-scripting-plugin-runtime-strategy.md to
      move WASM-10 from the "satisfiable substantially as worded" list into
      the "NOT satisfiable as literally worded" section and add it to the
      REQ-VALIDATOR watchlist -- independently cross-checked by
      RELEASE-VALIDATOR against the shipped moduledoc and confirmed a
      genuine, accurate correction, not scope creep. Fuel exhaustion,
      ordinary traps, and REQ-170's future wall-clock timeout are
      distinguishable by pattern match via classify_call_result/1. mix test
      test/letflow/engine/wasm/: 78 passed, 0 failures (2 pre-existing
      module_registry_test.exs warnings, unrelated). mix compile
      --warnings-as-errors --force: clean recompile of 134 files, 0 warnings.
      Scope confirmed via git diff --stat main...HEAD: only
      resource_limits.ex (new), its test file (new), 3 new
      priv/wasm_fixtures/req169_*.wat fixtures, the design doc and test spec
      (new), 8 handoff JSONs, and the decision-0014 correction above --
      module_registry.ex, capability_gate.ex, plugin_handler.ex,
      memory_guard.ex, and application.ex all confirmed byte-identical to
      main. No other REQ-169 reference found under docs/migration/ needing a
      companion update. Passed design, SECURITY-REVIEWER (hard gate),
      REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER, and
      RELEASE-VALIDATOR (Step 5 independent re-verification of all 8 real
      acceptance criteria against shipped code and freshly re-run
      test/compile output). Status flipped pending -> done in
      docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 44 on-disk entries before
      this append; 45 after.
  - req: REQ-170
    event: started
    agent: ORCH
    at: "2026-08-28T09:27:07Z"
    note: >
      WF02-REQ170-20260828 started (queue task 322, GH#622, stage S5).
      Claimed via get_next_task after REQ-169's completion. WASM
      per-invocation wall-clock timeout enforced across the process
      boundary (WASM-11) -- the requirement that cashes in decision
      0014's mandatory process boundary as an actual guarantee. Two
      layers to assert: wasmex's own documented interrupt-and-keep-Store
      behavior (read from docs, not run -- this requirement verifies it
      live) and REQ-165's supervised-task boundary independently
      terminating the invocation regardless of whether the first
      mechanism cooperates. OQ-5 (scheduler safety under real load) is
      explicitly NOT this requirement's deliverable -- any scheduler-stall
      evidence found while building this must be filed as an issue
      against OQ-5, not silently absorbed. SECURITY-REVIEWER hard gate.
      Depends on REQ-169 (done).

  - req: REQ-170
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T10:55:29Z"
    note: >
      WF02-REQ170-20260828 complete (RELEASE-VALIDATOR Step 5 PASS). WASM
      per-invocation wall-clock timeout enforced across the process boundary
      (WASM-11) implemented in lib/letflow/engine/wasm/call_timeout.ex (new)
      and lib/letflow/engine/wasm/plugin_handler.ex (node_config timeout_ms
      threaded into Wasmex.call_function/4). Two layers asserted: wasmex's
      documented interrupt-and-keep-Store claim was run live and found NOT to
      hold (Store permanently wedged after a timeout, GenServer.call exit) --
      disclosed in call_timeout.ex's moduledoc verbatim rather than silently
      absorbed, mirroring decision 0014's WASM-10 precedent; and REQ-165's
      outer supervised-task boundary independently terminates the invocation
      well before a longer inner wasmex timeout could fire, confirmed by
      asserting the dispatched task process is dead. Timeout is configurable
      (node_config['timeout_ms']), not hardcoded, with a mutation-closing test
      proving it isn't silently dropped to wasmex's own 5000ms default.
      CallTimeout.classify/1 distinguishes the timeout error from REQ-169's
      fuel/memory errors by pattern match. Moduledoc states OQ-5 (scheduler
      safety under real load) remains unsettled and names what would settle
      it (a load spike plus S6 thresholds), filing the new
      worker-pool-exhaustion (TOKIO_RUNTIME) evidence as an issue rather than
      absorbing it as a tuning detail. mix test test/letflow/engine/wasm/: 98
      passed, 0 failures. mix compile --force --warnings-as-errors: clean
      recompile of 135 files, 0 warnings. Full parallel suite (8 partitions):
      2386/2386 passed. Scope confirmed via git diff --stat main...HEAD:
      only call_timeout.ex (new), plugin_handler.ex (+31/-11), and
      docs/migration/decisions/0014-scripting-plugin-runtime-strategy.md
      (93-line correction, made by REVIEWER's Step 2d pass and independently
      re-verified as accurate, not overreach, by RELEASE-VALIDATOR) --
      module_registry.ex, capability_gate.ex, memory_guard.ex,
      resource_limits.ex, plugin_interface.ex, and application.ex all
      confirmed untouched. No other REQ-170 reference found under
      docs/migration/ needing a companion update. Passed design,
      SECURITY-REVIEWER (hard gate), REVIEWER, TEST-DESIGNER,
      TEST-DESIGN-VALIDATOR, TEST-RUNNER, and RELEASE-VALIDATOR (Step 5
      independent re-verification of all 7 real acceptance criteria against
      shipped code and freshly re-run test/compile output). Status flipped
      pending -> done in docs/requirements.yaml. `grep -c "^  - req:"
      docs/status/requirement_status.v5.yaml` = 46 on-disk entries before
      this append; 47 after.

# ─────────────────────────────────────────────────────────────────────────────
# VOLUME 5 — CLOSED 2026-08-28. DO NOT APPEND TO THIS FILE.
#
# This volume holds the complete requirement run history from 2026-08-26 through
# 2026-08-28 (47 entries, lines 1-1204). It is frozen: every line above this
# comment is preserved byte-for-byte. Closed under WF02-REQ170-20260828's Step 6
# DOC-UPDATER append, which brought this file to 1204 lines -- over its declared
# roll_rule ceiling (max_lines: 1200) -- confirmed via `wc -l` per this volume's
# own "WHEN THIS VOLUME IS FULL" procedure. Reads of this volume are TARGETED
# reads only -- a line range or a pattern match, never a whole-file read. Use
# whichever your shell has:
#   Read tool with offset/limit (works everywhere), or
#   Bash:       grep -n PATTERN FILE   /   sed -n 'START,ENDp' FILE
#   PowerShell: Select-String -Path FILE -Pattern PATTERN
#               (Get-Content FILE)[START..END]
#
# NEW ENTRIES GO TO THE CURRENT VOLUME. Find it in:
#   docs/status/requirement_status.index.yaml
# which, as of this closure, names:
#   docs/status/requirement_status.v6.yaml
#
# Why this file was closed: appending REQ-170's done-event (WF02-REQ170-20260828
# Step 6) brought the on-disk count to 47 entries across 1204 lines -- over the
# roll ceiling. No entry above this footer was moved, edited or deleted.
#
# KNOWN ANOMALIES IN THIS VOLUME: none declared beyond what volumes 1-3 already
# declare against themselves.
# ─────────────────────────────────────────────────────────────────────────────
