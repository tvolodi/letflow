# Letflow — Requirement run history — VOLUME 6 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v5.yaml (volume 5, closed,
#   1,204 frozen-prefix lines / 1,233 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v6.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v6.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: REQ-171
    event: started
    agent: ORCH
    at: "2026-08-28T11:53:04Z"
    note: >
      WF02-REQ171-20260828 started (queue task 323, GH#623, stage S5).
      Claimed via get_next_task after REQ-170's completion. WASM host API
      1/2 -- read path (read_variable, log, now, uuid) at parity with
      Lua's REQ-159/REQ-152 -- the first real wiring of ModuleRegistry
      (REQ-166), CapabilityGate (REQ-167), MemoryGuard (REQ-168), and
      ResourceLimits (REQ-169) into an actual host function dispatch.
      Direction of conformance settled by decision 0014 (4): Lua's host
      API is the definition WASM conforms to -- this requirement does not
      modify REQ-159's Lua implementation. uuid has no Lua counterpart in
      R-Co's eight-function matrix -- must be decided explicitly (WASM-side
      addition vs. Lua-side gap), not quietly implemented as parity.
      Number marshalling must cite REQ-150's rule, no second rule.
      Capability gating comes from REQ-167's whitelist, no second model.
      write_variable/call_service/fail are REQ-172's. SECURITY-REVIEWER
      hard gate. Depends on REQ-170 (done), REQ-159 (done), REQ-152 (done).
  - req: REQ-171
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T13:20:10Z"
    note: >
      WF02-REQ171-20260828 done (queue task 323, GH#623, stage S5). WASM
      host API 1/2 -- read path (read_variable, log, now, uuid) at parity
      with Lua's REQ-159/REQ-152 shipped in lib/letflow/engine/wasm/host_api.ex,
      with capability gating wired via lib/letflow/engine/wasm/capability_gate.ex
      (REQ-167's whitelist, no second model). RELEASE-VALIDATOR independently
      re-verified all 9 acceptance criteria: mix test test/letflow/engine/wasm/
      -- 119 passed, 0 failures; mix compile --warnings-as-errors --force --
      exit 0, zero warnings. Scope check confirmed module_registry.ex,
      memory_guard.ex, resource_limits.ex, plugin_handler.ex, call_timeout.ex,
      plugin_interface.ex, and lib/letflow/engine/lua/ untouched. uuid decision
      documented in host_api.ex moduledoc as a WASM-side addition with no Lua
      counterpart, not presented as parity. Unblocks REQ-172 (write path).
  - req: REQ-172
    event: started
    agent: ORCH
    at: "2026-08-28T14:45:00Z"
    note: >
      WF02-REQ172-20260828 started (queue task 324, GH#624, stage S5). WASM
      host API 2/2 -- write path (write_variable, call_service, fail) at
      parity with Lua's REQ-160/REQ-161, plus the SHARED Lua/Wasm parity
      test suite that satisfies WASM-12's own acceptance criterion (one
      scenario run against both executors, not two similar-looking
      suites). Three sharp constraints: write_variable stages and discards
      on every failure arm (trap, fuel exhaustion, memory cap, timeout,
      fail); call_service returns a structured error on service failure
      but still fails outright on a missing capability (REQ-167); fail
      must terminate by a mechanism the guest cannot intercept. Direction
      of conformance per decision 0014 (4): Lua is the definition, WASM
      conforms -- no Lua host-API file may be modified. SECURITY-REVIEWER
      hard gate. Depends on REQ-171 (done), REQ-161 (done).
  - req: REQ-172
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T17:46:37Z"
    note: >
      WF02-REQ172-20260828 done (queue task 324, GH#624, stage S5). WASM
      host API 2/2 -- write path (write_variable, call_service, fail) at
      parity with Lua's REQ-160/REQ-161, plus the SHARED Lua/Wasm parity
      test harness that satisfies WASM-12's own acceptance criterion (one
      scenario definition executed against both executors, not two
      similar-looking suites). Key live-verified finding, caught via two
      CODE-DESIGN-VALIDATOR rework rounds: fail's real uninterceptability
      mechanism is a process-dictionary stash written just before calling
      exit/1, NOT a crash of the Wasmex instance process -- exit/1 does
      NOT crash that process, which contradicted the design's own first
      draft and had to be corrected before implementation. RELEASE-VALIDATOR
      independently re-verified all 8 acceptance criteria (own compile/test
      runs, own git diff verification) and PASSED. Unblocks REQ-173 (WASM
      module hot reload, depends_on: [REQ-172]).
  - req: REQ-173
    event: started
    agent: ORCH
    at: "2026-08-28T18:41:00Z"
    note: >
      WF02-REQ173-20260828 started (queue task 325, GH#625, stage S5).
      WASM-14 hot reload: when a new module version activates, in-flight
      invocations of the prior version must complete normally against the
      OLD version's behaviour, and new invocations must use the NEW
      version. Concurrency requirement -- the acceptance test must hold an
      invocation open (a guest blocked in a controllable host call),
      activate a new version DURING it, and assert both halves in one
      overlapping scenario, not two sequential invocations. Prior
      version's resources must be released only after its last in-flight
      invocation completes. A version's capability set must not leak
      across an in-flight invocation when the new version's manifest
      declares a different one -- SECURITY-REVIEWER hard gate, version
      confusion is an authorization hazard. NOT IN THIS REQUIREMENT:
      instance pooling is REQ-174's, which depends_on this one. Depends on
      REQ-172 (done).
  - req: REQ-173
    event: done
    agent: DOC-UPDATER
    at: "2026-08-28T21:48:26Z"
    note: >
      WF02-REQ173-20260828 done (queue task 325, GH#625, stage S5). WASM-14
      hot reload shipped as Letflow.Engine.Wasm.ModuleVersionRegistry:
      versioned module registration, activate/2, and invoke/4 built on
      checkout-based version-holding -- an in-flight invocation resolves and
      holds its module version at invocation start, immune to a concurrent
      activation swapping it mid-call -- with reference-counted release so a
      prior version's resources are freed only after its last in-flight
      invocation completes, and per-version capability isolation so an
      in-flight invocation keeps its own version's capability set even when
      the newly activated version's manifest declares a different one.
      Bumpy path worth recording: 2 CODE-DESIGNER rework rounds (first, an
      outer-timeout classification mechanism the design left unspecified,
      live-verified and fixed; second, a real late-discovered defect where
      register_version/3 could never register a module requiring ANY host
      capability, because it delegated entirely to ModuleRegistry.register/1's
      empty-import validation -- fixed with a new capability-aware
      instantiation proof reusing CapabilityGate.build_import_table/1) plus
      1 REVIEWER rework round (a missing decision-0014 citation). RELEASE-
      VALIDATOR independently PASSED all 6 acceptance criteria (own
      compile/test runs including scripts/test_parallel.sh, own git diff
      verification, and its own live trace confirming the capability-
      registration fix is genuinely solid). Unblocks REQ-174 (WASM instance
      pooling, depends_on: [REQ-173]); REQ-173 was a dependency none of
      REQ-172's siblings needed.
  - req: REQ-174
    event: started
    agent: ORCH
    at: "2026-08-28T22:42:00Z"
    note: >
      WF02-REQ174-20260828 started (queue task 326, GH#626, stage S5).
      WASM-13 restated (Instance Pooling, SHOULD) per decision 0014 (e):
      IF pooling is adopted, per-invocation memory reset is a CORRECTNESS
      requirement (isolation), not a performance detail -- wasmex pooling
      deliberately reuses a Store, and Letflow is schema-per-tenant
      (decision 0006), so a pooled instance carrying residue from a prior
      invocation is a cross-invocation, potentially cross-tenant data
      leak. Because it is a SHOULD, declining pooling entirely is a
      legitimate outcome, but silence is not -- the moduledoc must record
      an explicit decision either way and assert per-invocation isolation
      regardless. If adopted: memory reset by construction, no CI-gated
      latency threshold (record p50 as a benchmark with real numbers
      instead), and REQ-173's own interaction must be honored -- a pooled
      instance of a version REQ-173's activate/2 superseded must never be
      handed to a new invocation. SECURITY-REVIEWER hard gate -- this is
      an isolation boundary. Depends on REQ-173 (done).
  - req: REQ-174
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T00:21:52Z"
    note: >
      WF02-REQ174-20260828 done (queue task 326, GH#626, stage S5). WASM-13
      restated (Instance Pooling, SHOULD) resolved as DECLINE: live
      verification found no pooling API in the installed wasmex dependency,
      and both existing instantiation call sites already give
      isolation-by-construction via a fresh Store per invocation -- so
      declining is the legitimate SHOULD outcome per decision 0014 (e), not a
      shortfall, and the moduledoc records the decision and reason plus
      asserts per-invocation isolation regardless, as required. Path: 1
      CODE-DESIGN-VALIDATOR rework round for two mechanical fixes (literal
      code fences copying real .ex content, and a wrong function name --
      substance confirmed sound both times); 1 SECURITY-REVIEWER rework round
      correcting an overstated "unconditional teardown" claim to accurately
      name the two known skip paths and attribute isolation to
      never-sharing-a-Store, not to teardown; 1 REVIEWER rework round
      catching one leftover stray instance of that same overstatement the
      first sweep missed; TEST-DESIGN-VALIDATOR independently applied real,
      reversible mutations to BOTH production call paths to confirm the
      isolation tests genuinely discriminate a real defect, not passing
      vacuously. RELEASE-VALIDATOR independently PASSED all 7 acceptance
      criteria (own full-suite run via scripts/test_parallel.sh, own re-read
      of design/decision text, confirmed the 4 adopt-only ACs are correctly
      reasoned as inapplicable given the DECLINE, not silently skipped).
      Nothing in docs/requirements.yaml declares depends_on: [REQ-174] --
      confirmed by grep; nothing is unblocked by this requirement yet.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-29T01:18:01Z"
    note: >
      Queue reconciliation for letflow-queue task 327 (REQ-175, GH#627).
      get_next_task returned this task as open/lockable, but
      docs/requirements.yaml already had REQ-175's status as cancelled, and
      docs/status/requirement_status.v5.yaml already records the cancellation
      (event: cancelled, agent: DOC-UPDATER, at: 2026-08-28T00:50:32Z, as
      REQ-164's own Step 6 consequence -- REQ-164 decided NO on OQ-3, so
      REQ-175, conditional on a YES, was correctly cancelled at that time,
      not left pending). The queue's own mirror of that outcome was never
      updated, leaving task 327 open. No new design/code work was performed
      -- ORCH re-confirmed REQ-164's NO decision and REQ-175's already-
      cancelled status directly from disk, then released task 327 via
      POST /tasks/327/release with status: done (per
      docs/agents/protocols/TASK_QUEUE.md's stale-task reconciliation path)
      to bring the queue back in sync with the already-settled outcome.
  - req: REQ-176
    event: started
    agent: ORCH
    at: "2026-08-29T01:45:00Z"
    note: >
      WF02-REQ176-20260829 started (queue task 351, GH#696, stage S6).
      First requirement of Stage 6 (operational cross-cutting), drafted this
      session via WF-01 (REQ-ANALYST draft, REQ-VALIDATOR hard gate -- 1
      rework round fixing REQ-178's depends_on and splitting the original
      oversized REQ-179/180 into REQ-181..184, then registered as queue
      tasks 351-357). REQ-176: dead-letter queue schema (dlq_entries,
      tenant-scoped per decision 0003 Decision B) and core entry lifecycle
      (enqueue/list/get/retry/discard) -- the generic origin-agnostic DLQ
      core that REQ-056/REQ-061's own "do NOT build a partial DLQ" hooks
      were left for. Bound to the already-shipped S8 frontend contract
      (web/src/api/dlq.ts, web/src/types/api.ts's DlqEntry/DlqRetryAttempt/
      CursorPage<T>) since R-Co is unreachable from this sandbox. No route/
      controller in this requirement -- that is REQ-178's. Depends on
      REQ-072 (done).
  - req: REQ-176
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T03:46:44Z"
    note: >
      WF02-REQ176-20260829 done (queue task 351, GH#696, stage S6). DLQ
      schema and core entry lifecycle shipped: Letflow.Dlq context module +
      Letflow.Dlq.Entry schema + tenant-scoped dlq_entries migration (per
      decision 0003 Decision B) -- enqueue/list/get/retry/discard, cursor-
      paginated list/2, row-lock-then-check state transitions so retry/2 and
      discard/2 correctly reject a "resolved"/"discarded" entry with a
      conflict error rather than racing another writer. Path: 1
      CODE-DESIGN-VALIDATOR rework round caught a fabricated
      Letflow.IdentityMigration precedent citation for the new
      UUID-to-text-cast-for-ILIKE search pattern -- corrected to honestly
      frame that cast as new rather than a false reuse claim.
      SECURITY-REVIEWER and REVIEWER both passed with no rework; REVIEWER
      filed ISS-0353 for a real, correctly-deferred idiom gap in the
      changeset's cast list. TEST-DESIGN-VALIDATOR independently
      re-verified with 3 of its own mutations applied directly to
      production code, confirming the tests genuinely discriminate real
      defects. RELEASE-VALIDATOR independently PASSED all 6 acceptance
      criteria (own full-suite run, own git diff verification, own
      field-by-field cross-check against web/src/types/api.ts). Unblocks
      REQ-177 (wiring REQ-056/REQ-061's existing hooks into this DLQ core)
      and REQ-178 (the route layer), both depends_on: [REQ-176]. Stage 6's
      FIRST done requirement: S6 moves from "present but inactive" to
      "active" per the T-LIVE-ACTIVITY detector -- the test literal ORCH
      already updated in commit e72b5c4 anticipated exactly this
      transition.
  - req: REQ-177
    event: started
    agent: ORCH
    at: "2026-08-29T04:34:00Z"
    note: >
      WF02-REQ177-20260829 started (queue task 352, GH#697, stage S6).
      Closes the two named hooks REQ-056 (EXT-01 AC4, SERVICE_TASK
      retry-exhaustion) and REQ-061 (EE-10, instance ERROR) each explicitly
      left for this stage rather than building a partial DLQ themselves.
      Deliberately narrow scope: LANDING only, not re-execution. Two hook
      call sites: (1) SERVICE_TASK retry-exhaustion -> REQ-176's enqueue/1,
      entry_type "event", full_reason from the classified failure kind; (2)
      instance ERROR transition -> same enqueue/1, full_reason read back
      from the just-persisted EXECUTION_ERROR event (re-derive from source,
      not recomputed), committed in the SAME transaction as REQ-061's own
      status flip and event append. Explicitly does NOT make retry/2
      actually re-execute the original dispatch or move an instance out of
      ERROR -- must be stated as a named, deferred follow-up in the
      moduledoc. Depends on REQ-176 (done), REQ-056 (done), REQ-061 (done).
      No file outside the two hook call sites and their tests may be
      modified.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-29T04:57:23Z"
    note: >
      WF01-S6-EXPANSION-20260829 complete -- Stage 6's SECOND batch expanded
      and registered: REQ-185..REQ-203 (19 requirements), letflow-queue tasks
      358-376. This closes the expansion of S6, which the first batch
      (REQ-176..184, DLQ + webhooks, tasks 351-357) had deliberately left
      half-done: its own header comment deferred src/scheduler and
      src/ordering to "the next batch" pending R-Co access and a timer-firing
      decision record. R-Co WAS reachable this session, so both blockers were
      addressable and every citation in this batch is freshly verified rather
      than inherited. Coverage: scheduler (REQ-185 firing-architecture
      decision, 186 core, 187 engine wiring, 188 recurrence+retention),
      secrets (REQ-189 backend decision -> decisions/0016, 190 core),
      repository (REQ-191/192 service catalog core+routes, REQ-202/203
      artifact store+activation), observability (REQ-193 logging, 194
      metrics, 195/196 audit store+route, 200 timeline, 201 alerting), expr
      (REQ-197 arithmetic+errors, 198 builtins), ordering (REQ-199).
      REQ-VALIDATOR hard gate took 2 rework rounds, both finding the SAME
      defect class -- a bare file-count range asserting coverage that did not
      exist (first src/obs's alerts.zig/timeline.zig, then src/repository's
      artifacts/canonicaliser/activation/schemas.zig). Fixed at the root: the
      stage doc now maps every subsystem PER FILE and its exhaustiveness
      claim is machine-verified across all 37 .zig files, independently
      re-run by ORCH over 41 including the first batch's trees.
      Three of ORCH's own dispatch-brief claims were WRONG and were corrected
      by REQ-ANALYST against real R-Co source, then confirmed by
      REQ-VALIDATOR: (1) SCH-02's per-timer advisory lock was REMOVED by R-Co
      (ISS-301, regression tests at scheduler.zig:1310/1318 assert its
      absence); porting SCH-02's literal text would have reintroduced deleted
      code. (2) R-Co's src/expr is NOT CEL -- EXP-102 cut over away from
      vendored CEL, so the CEL vocabulary ORCH listed exists in neither
      system. (3) The SPA already parses Prometheus TEXT at an unversioned
      /metrics, settling one of REQ-078's three documented divergences with
      evidence.
      ALSO SURFACED, and acted on: REQ-183 (first batch, already registered
      as task 356) is UNIMPLEMENTABLE AS DRAFTED -- REQ-181 stores the
      webhook secret hashed, and a hash cannot serve as an HMAC key, making
      REQ-183's own verification acceptance criterion unsatisfiable. ORCH
      locked task 356 to this host so no concurrent host could claim it, and
      REQ-183 was amended in place (depends_on gains REQ-189; the signing
      clause and verification AC now resolve the key through REQ-189's
      mechanism) rather than deleted -- its retry/backoff, auto-pause and
      DLQ-landing halves are sound. That edit created a
      REQ-183 -> REQ-189 -> REQ-183 cycle, caught by REQ-ANALYST's own
      re-validation and resolved by dropping REQ-183 from REQ-189's
      depends_on: depends_on encodes build order, not reading order, and
      REQ-189's deliverable is a document whose input is REQ-183's already-
      written text. REQ-181 was never touched.
      Verified before and after registration: 177 entries, zero duplicate
      ids, zero orphaned depends_on, zero cycles; every impl_order written
      back from register_task's real response, never guessed;
      mix letflow.check_requirements_registration reports 177 registered /
      0 deferred / 0 unclassified, and mix letflow.check_deferral_staleness
      reports 0 stale. Requirements remain pending -- WF-01 does not flip
      status; WF-02 claims them via get_next_task.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-29T05:01:11Z"
    note: >
      Self-reported operator error and its immediate correction, recorded
      rather than left silent. After WF01-S6-EXPANSION-20260829 pushed, ORCH
      called get_next_task with a disposable agent_id ("probe-readonly-check")
      purely to confirm the queue would hand out the expected next task. That
      is precisely the misuse docs/agents/protocols/TASK_QUEUE.md's
      "Reachability checks must not have side effects" section forbids:
      get_next_task is NOT read-only -- it atomically claims and locks
      whatever it returns. The call claimed and locked task 353 (REQ-178,
      DLQ route layer), which a concurrent host could otherwise have taken.
      Detected immediately from the response and released within seconds via
      POST /tasks/353/release with NO status field, returning it to
      status: open with locked_by: null -- a clean hand-back, not a
      transition, so the task is fully claimable and its GitHub issue
      untouched. No work was performed against it and no other queue state
      changed. This is the same failure TASK_QUEUE.md already records
      happening once before (2026-08-20, ISS-0086/GH#303's own resolution
      run); the correct probe is GET /health, which requires no auth and
      touches nothing, and which this session had in fact used correctly
      earlier in the run. Noted here because the protocol's own warning
      exists because this mistake recurs, and a second unrecorded occurrence
      would make the warning look theoretical.
      Unrelated to the error, and the reason the check was attempted at all:
      the queue's next claimable task is task 353 / REQ-178, which is
      correct -- REQ-176 is done, REQ-177 is in flight on a sibling session,
      and this run's newly-registered tasks 358-376 sit behind them in
      requirement-type FIFO order as intended.
  - req: REQ-177
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T07:30:29Z"
    note: >
      WF02-REQ177-20260829 done (queue task 352, GH#697, stage S6). Both
      named hooks REQ-056 and REQ-061 explicitly left for this stage are
      now closed: Hook A, SERVICE_TASK retry-exhaustion (attempt_index ==
      retry_limit) calls REQ-176's enqueue/1 with entry_type "event",
      instance_id set, full_reason naming the classified failure kind;
      Hook B, instance ERROR transition, adds a new
      :execution_error_dlq_landing Ecto.Multi step to
      Letflow.Engine.ExecutionError.append_multi/3, landing in the SAME
      transaction as REQ-061's own status flip and EXECUTION_ERROR append,
      with full_reason read back from the just-persisted event rather than
      recomputed. Both hooks are landing-only, per design -- retry/2 does
      not re-invoke the original SERVICE_TASK dispatch and does not move an
      instance out of ERROR, stated as a named, deferred follow-up in both
      hook sites' moduledocs, because no SERVICE_TASK dispatch orchestrator
      exists yet in this codebase (confirmed by CODE-DESIGNER's own grep).
      Path: 1 CODE-DESIGN-VALIDATOR rework round caught a false EventStore
      payload return-shape claim in the design; 1 SECURITY-REVIEWER rework
      round caught a real grapheme-vs-codepoint truncation bug in
      full_reason handling, empirically confirmed by ORCH with real Elixir
      before routing back; REVIEWER passed clean; and a genuine Step 5
      rework cycle where RELEASE-VALIDATOR's own re-derivation found AC3
      (same-transaction atomicity) lacked real test coverage despite the
      code already being correct -- closed by TEST-DESIGNER adding a real
      fault-injection test (commit cc59d8d), independently re-verified by
      TEST-DESIGN-VALIDATOR with its own mutation, and RELEASE-VALIDATOR's
      re-check gave the final PASS on all 6 acceptance criteria. Checked
      docs/requirements.yaml for depends_on: [REQ-177] -- none found;
      REQ-178 (the DLQ route layer) depends only on REQ-176/REQ-069/REQ-072,
      not REQ-177, so nothing is newly unblocked by this requirement.
  - req: REQ-178
    event: started
    agent: ORCH
    at: "2026-08-29T08:27:00Z"
    note: >
      WF02-REQ178-20260829 started (queue task 353, GH#698, stage S6).
      Third and last requirement of this batch's DLQ half -- the route
      layer atop REQ-176's core and REQ-177's landing sites (does NOT
      depend on REQ-177 per its own depends_on list -- the route layer
      builds and tests identically whether or not REQ-177's hooks have
      landed). GET /api/v1/dlq (list, filterable by status/source_type/
      search/cursor/page_size), GET /api/v1/dlq/:id, POST .../retry,
      POST .../discard. Bound to the already-shipped S8 frontend contract
      (web/src/api/dlq.ts, web/src/types/api.ts's DlqEntry/CursorPage<T>)
      since R-Co's dlq.zig is unreachable from this sandbox. Every route
      requires DlqOperate (REQ-069's matrix, currently unreachable
      pending exactly this route). Tenant scoping follows REQ-072's
      cross-tenant-404 mechanism (never 403). Terminal-state retry/
      discard calls must return a 409-shaped conflict, not 500. No
      change to REQ-176/177's context module -- route/controller layer
      only. Depends on REQ-176 (done), REQ-069 (done), REQ-072 (done).
      Note: task 353 was briefly claimed and immediately released by a
      sibling session's own get_next_task-probe-misuse self-correction
      (recorded in this same volume, 2026-08-29T05:01:11Z) -- genuinely
      open when ORCH claimed it here, no concurrent work in flight.
  - req: REQ-178
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T11:28:53Z"
    note: >
      WF02-REQ178-20260829 done (queue task 353, GH#698, stage S6). DLQ
      route layer shipped in lib/letflow/routers/dlq.ex atop REQ-176's
      core and REQ-177's landing sites: GET /api/v1/dlq (list, filterable
      by status/source_type/search/cursor/page_size/instance_id) with
      body shape {items: [DlqEntry], next_cursor} verified field-by-field
      against web/src/types/api.ts; GET /api/v1/dlq/:id; POST .../retry
      and .../discard. Every route requires DlqOperate (REQ-069's matrix,
      now reachable for the first time); tenant scoping follows REQ-072's
      cross-tenant-404 mechanism (a real id in another tenant returns 404,
      never 403, confirmed by test); terminal-state retry/discard returns
      a 409-shaped conflict rather than 500. Moduledoc discloses R-Co's
      dlq.zig was not inspected (unreachable from this sandbox) and names
      web/src/api/dlq.ts + web/src/types/api.ts as the binding contract
      instead. Clean pipeline -- CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER,
      REVIEWER, TEST-DESIGN-VALIDATOR all passed with zero rework rounds.
      Two mechanical bookkeeping defects were found and fixed along the
      way, neither touching REQ-178's substance: an H6 handoff-lint
      validator-report naming trap (db29e45, now documented in
      docs/anti-patterns.md) and a status-vocab defect ("DONE" is not a
      valid top-level handoff status) in step-01b/step-02c/step-05
      (fbe5f42). RELEASE-VALIDATOR independently re-verified all 6
      acceptance criteria (own compile/test runs including
      scripts/test_parallel.sh -- 2488/2491, same 3 pre-existing
      rustc-absent environmental failures, none touching this branch's
      diff) and PASSED. This closes Stage 6's first batch DLQ half: all
      three of REQ-176 (core), REQ-177 (hooks), and REQ-178 (routes) are
      now done.
  - req: REQ-181
    event: started
    agent: ORCH
    at: "2026-08-29T12:19:00Z"
    note: >
      Queue task 354, GH#699, stage S6. REQ-VALIDATOR-rework requirement (the
      core-schema+context half of REQ-179's split; REQ-182 is the route-layer
      half). REQ-179 itself was never written to disk in this batch's final
      form (superseded during WF-01 drafting before validation, per the prior
      run's rework), so no separate cancellation entry is needed here.
  - req: REQ-181
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T13:36:37Z"
    note: >
      WF02-REQ181-20260829 done (queue task 354, GH#699, stage S6). Webhook
      subscription core shipped: Letflow.Webhooks context module +
      Letflow.Webhooks.Subscription schema + tenant-scoped
      webhook_subscriptions migration (per decision 0003 Decision B),
      registered in tenant_provisioning.ex's manifest -- create/2 (generates
      and hashes the HMAC secret when none is supplied, returning the
      plaintext exactly once as hmac_secret_once, never retrievable again),
      list/1 (tenant-scoped), update/2 (reconciles WebhookSubscription's dual
      status/is_active representation to one stored ACTIVE/PAUSED state),
      delete/2. No route/controller file added, per this requirement's own
      scope -- that is REQ-182's. Path: 1 CODE-DESIGN-VALIDATOR rework round
      (a literal Elixir code fence in the design doc, removed twice before it
      was fully gone); 2 real bugs found and fixed mid-pipeline -- TEST-
      DESIGNER caught a missing :created_at cast in the changeset, and
      TEST-RUNNER caught a tenant_fixture oracle-list gap that was masking a
      real tenant-scoping defect in list/1's test coverage; plus 2 direct
      ORCH bookkeeping fixes to this same volume's REQ-181 started-event
      (field name "by:" corrected to "agent:", then field order corrected),
      recorded here rather than left silent. SECURITY-REVIEWER and REVIEWER
      both passed. RELEASE-VALIDATOR independently re-verified all 7
      acceptance criteria on commit 53f6c06: mix test
      test/letflow/webhooks_test.exs -> 14/14 passed; mix compile
      --warnings-as-errors --force -> clean, exit 0; scripts/test_parallel.sh
      (N=8) -> 2502/2505, the same 3 pre-existing rustc-absent/wasmex-NIF
      environmental failures seen on every prior REQ-176/177/178 run, none
      touching this branch's diff. Unblocks REQ-182 (webhook route layer,
      depends_on: [REQ-181, REQ-069, REQ-072]), still pending.
  - req: REQ-182
    event: started
    agent: ORCH
    at: "2026-08-29T14:25:55Z"
    note: >
      Queue task 355, GH#700, stage S6. REQ-VALIDATOR-rework requirement (the
      route-layer half of REQ-179's split; REQ-181, the core-schema+context
      half, shipped and released). Mirrors REQ-176/REQ-178's DLQ core/route
      split for this exact shape.
  - req: REQ-182
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T15:55:51Z"
    note: >
      WF02-REQ182-20260829 done (queue task 355, GH#700, stage S6).
      RELEASE-VALIDATOR PASS: all 7 acceptance criteria independently
      re-verified. Webhook subscription CRUD route layer shipped atop
      REQ-181's core -- GET/POST/PATCH/DELETE /api/v1/webhooks/subscriptions
      (WebhooksManage-gated), mirroring REQ-176/REQ-178's DLQ core/route
      split for this exact shape. Path: zero rework rounds through
      design/security/reviewer/test-design gates; one real gap found and
      fixed proactively by CODE-DESIGNER/CODE-DESIGN-VALIDATOR -- a missing
      PATCH authorization clause in lib/letflow/api/authorization.ex, caught
      before implementation even started; one stale test fixed by REVIEWER
      -- test/letflow/webhooks_test.exs's REQ-181-era structural assertion,
      same class as REQ-176/178's precedent; two occurrences of the
      recurring status:"DONE" handoff bug caught and fixed by ORCH before
      reaching CI. Closes the webhooks half of this batch (REQ-181 +
      REQ-182), mirroring the DLQ half's REQ-176/177/178 completion.
  - req: REQ-185
    event: started
    agent: ORCH
    at: "2026-08-29T17:09:29Z"
    note: >
      Queue task 358, GH#704, stage S6. First requirement of S6's scheduler
      half -- decision + design artefact only (no migration, no engine
      code, no mix.exs change), gating REQ-186/187/188. Mirrors
      REQ-148/149/150's S5 open-question shape: Step 1 CODE-DESIGNER ->
      Step 1b CODE-DESIGN-VALIDATOR -> SECURITY-REVIEWER -> REVIEWER
      (explicit sign-off on the Oban adoption decision) -> TEST-DESIGNER
      -> RELEASE-VALIDATOR -> DOC-UPDATER, no ELIXIR-DEV/TEST-RUNNER steps.
  - req: REQ-185
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T17:27:30Z"
    note: >
      WF02-REQ185-20260829 done (queue task 358, GH#704, stage S6).
      Decision-and-design-only requirement -- no migration, no engine
      code, no mix.exs change -- gating REQ-186/187/188 (S6's scheduler
      half). lib/letflow/design/req185-scheduler-firing-architecture.md
      settles: (1) firing mechanism -- a supervised GenServer ticker
      (Process.send_after/3 in lib/letflow/application.ex's tree), Oban
      and a per-timer process both evaluated and rejected on stated
      grounds; (2) Oban adoption decided NO, with REVIEWER's genuine
      sign-off recorded directly in the artefact's own text (section 3),
      not rubber-stamped -- following REQ-148's own precedent that a
      new-dependency decision lives in the design artefact rather than
      spawning a separate docs/migration/decisions/*.md record; (3) claim
      mechanism is FOR UPDATE SKIP LOCKED, with the artefact stating in
      its own words that R-Co REMOVED the per-timer advisory lock SCH-02's
      literal text mandates (ISS-301); (4) no ISS-302-equivalent
      session-level startup-sweep lock is adopted -- the artefact states
      what prevents double-sweeping on simultaneous multi-node restart
      instead; (5) poller iterates tenant schemas per tick, with a
      quantified cost at a stated tenant count rather than a qualitative
      claim; (6) locked/nothing-due/hard-error are distinguished from one
      another per section 7, with an explicit statement of why
      propagating every error out of the fire path is the wrong fix
      (ISS-303); (7) an exhausted-retry timer lands in REQ-176's
      dlq_entries with entry_type "timer", answered yes rather than left
      to REQ-186. TEST-DESIGNER's role here was empirical-claim
      verification (confirming the artefact's factual claims against real
      R-Co source and the live Letflow tree) rather than ExUnit tests,
      since no code shipped. Clean pipeline -- zero rework rounds through
      design/security/reviewer gates. RELEASE-VALIDATOR independently
      re-verified all 8 acceptance criteria against the actual design
      artefact and the actual referenced code (application.ex, mix.exs,
      the dlq_entries migration, lib/letflow/dlq.ex, decision
      0003-ecto-schema-strategy.md, and git diff --stat main...HEAD
      confirming only the design file and handoff/report files are
      touched) and PASSED. No docs/migration/decisions/*.md record was
      created for this requirement -- checked REQ-148/149/150's own S5
      precedent (decision 0014-scripting-plugin-runtime-strategy.md):
      REQ-149/150's own open-question resolutions were folded into 0014's
      "Open questions" section as "SETTLED BY REQ-149/150" pointers to
      their design docs, not given their own decision files, and this
      artefact follows that same established pattern rather than the
      new-decision-file path. Unblocks REQ-186 (timers schema + scheduler
      core), REQ-187 (engine wiring) and REQ-188 (recurrence + retention),
      all of which depend on this requirement.
  - req: REQ-186
    event: started
    agent: ORCH
    at: "2026-08-29T17:53:02Z"
    note: >
      Queue task 359, GH#705, stage S6. First implementation requirement of
      S6's scheduler half, building the core REQ-185's design artefact
      specifies (GenServer ticker, FOR UPDATE SKIP LOCKED, per-tenant-schema
      iteration, dlq_entries entry_type "timer"). Core only -- no route/
      controller, mirroring REQ-176/178 and REQ-181/182's core/route split.
  - req: REQ-186
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T19:30:59Z"
    note: >
      WF02-REQ186-20260829 done (queue task 359, GH#705, stage S6). Timers
      schema and scheduler core shipped per REQ-185's design artefact:
      tenant-scoped timers migration (decision 0003 Decision B) with the
      status/recurrence CHECK constraints and the partial index on
      (fire_at) WHERE status = 'pending', plus a scheduler core doing
      FOR UPDATE SKIP LOCKED claim-and-fire, missed-timer recovery
      (fired_late: true with both fire_at and the actual firing time),
      and failure accounting (fire_error_count increments in a separate
      transaction, remaining due timers in the cycle still attempted,
      terminal "failed" landing in REQ-176's dlq_entries with entry_type
      "timer" once fire_error_count reaches the configured max). Core
      only -- no route/controller, per this requirement's own scope.
      Path: zero rework rounds through design/security/reviewer/
      test-design gates; ORCH directly ran the full suite twice mid-
      pipeline and fixed two stale tests after the original ELIXIR-DEV
      dispatch left a background verification run unfinished --
      tenant_fixture.ex's table oracle and
      tenant_provisioning_event_seed_test.exs's hardcoded seed counts;
      REVIEWER corrected one design-doc inconsistency (actor_id)
      post-implementation; TEST-DESIGN-VALIDATOR accepted one disclosed
      row-lock-concurrency coverage gap as non-blocking, consistent with
      REQ-181's own precedent for the identical idiom. RELEASE-VALIDATOR
      independently re-verified all 10 acceptance criteria -- PASS, no
      gap found (handoffs/WF02-REQ186-20260829/release-validation-req186.md).
      Unblocks REQ-187 (transition.ex wiring + cancellation) and
      REQ-188 (recurrence + escalation), both depends_on: [REQ-186].
  - req: REQ-187
    event: started
    agent: ORCH
    at: "2026-08-29T19:54:54Z"
    note: >
      Queue task 360, GH#706, stage S6. Wires REQ-186's timers core into the
      engine (arm on TIMER-node token arrival, advance on fire, cancel on
      completion/cancellation) -- closes transition.ex L294's TIMER
      not-yet-implemented stub, task_activation.ex's cancel_pending_timers/2
      no-op, and engine.ex's L234-242 recorded EE-08 deferral (timer half
      only). Strict purity constraint: transition.ex must gain no Repo call.
  - req: REQ-187
    event: done
    agent: DOC-UPDATER
    at: "2026-08-29T22:18:17Z"
    note: >
      WF02-REQ187-20260829 done (queue task 360, GH#706, stage S6). TIMER
      nodes wired into the engine: transition.ex L294's catch-all now arms
      a REQ-186 timer on token arrival (fire_at = arrival + the node's
      duration_iso8601, persisted in the SAME transaction as the
      state-transition event), task_activation.ex's cancel_pending_timers/2
      no-op replaced with a real update cancelling every PENDING timer for
      the instance (unmoved call site, still inside engine.ex's completion
      transaction), and engine.ex L234-242's EE-08 deferral closed for the
      timer half via an equivalent cancellation call inside
      cancel_instance/3's own transaction. Strict transition.ex purity
      preserved throughout: no Repo call, timer-arming carried as a data
      description in an existing return shape extended for this purpose,
      independently re-verified at every stage by ORCH/SECURITY-REVIEWER/
      REVIEWER/RELEASE-VALIDATOR. Most architecturally complex requirement
      in this batch: a nested SAVEPOINT transaction, a genuine AB-BA
      lock-ordering deadlock hazard found by CODE-DESIGN-VALIDATOR and
      fixed, and a multi-timer-in-one-hop-chain Ecto.Multi step-name
      collision hazard found and closed with a typed guard -- yet the
      pipeline ran with zero rework rounds through design/security/
      reviewer/test-design gates. RELEASE-VALIDATOR independently
      re-verified all 10 acceptance criteria, including the fragile
      lock-ordering fix -- PASS, no gap found
      (handoffs/WF02-REQ187-20260829/release-validation-req187.md).
      Full suite re-run via scripts/test_parallel.sh (8 partitions):
      2552/2555 passed, 3 failures all pre-existing/environmental (2x
      Mix.Tasks.Letflow.CheckToolchainTest rust-pin tests, rustc absent
      from this sandbox; 1x Letflow.Engine.Wasm.PluginHandlerTest AC7
      wasmex-Rust-NIF-source test), none touching REQ-187's diff. mix
      compile --warnings-as-errors clean; mix format --check-formatted
      clean; mix letflow.lint_handoffs OK (0 new violations, 25
      pre-existing grandfathered). This closes REQ-186's engine-integration
      half; REQ-188 (recurrence, SCH-07, plus escalation and the retention
      runner) is now the last remaining piece of S6's scheduler half.
  - req: REQ-188
    event: started
    agent: ORCH
    at: "2026-08-29T23:09:11Z"
    note: >
      Queue task 361, GH#707, stage S6. Third and last requirement of S6's
      scheduler half: recurring timer re-arm (SCH-07) atop REQ-186/187's
      poll loop, plus a periodic retention runner invoking the already-shipped
      but zero-caller EventStore.archive/1, disabled by default. Explicitly
      defers R-Co's partition-based retention (ISS-0014's adopted option (a))
      and SCH-04 escalation timers (no escalation_timer_duration attribute
      on Letflow's Graph yet).
  - req: REQ-189
    event: started
    agent: ORCH
    at: "2026-08-30T00:00:00Z"
    note: >
      Queue task 362, GH#708, stage S6. Dispatched while routing GitHub
      issue #701 (REQ-183, webhook delivery dispatch core): #701's issue
      text is stale and REQ-183 is correctly blocked pending this
      requirement's decision record per WF01-S6-EXPANSION-20260829 rework 1
      (REQ-183's depends_on gained REQ-189 because REQ-183 as drafted signed
      HMAC with REQ-181's HASHED stored secret, which is not usable as a
      key). This local checkout was found 15 commits behind origin/main
      (REQ-181/182/185/186/187 already merged upstream by other sessions/
      hosts); fast-forwarded cleanly before this dispatch so REQ-189's
      depends_on (REQ-181 done, REQ-072 done) is genuinely satisfied, not
      stale-mirror satisfied. Queue's own get_next_task independently
      confirmed task 362 eligible; a first get_next_task call (disposable
      probe agent_id, before this session held any real intent) claimed it
      by surprise and was released immediately per the anti-pattern
      warning against using get_next_task as a reachability probe. A
      second get_next_task call (real agent_id, meant to claim real work)
      instead claimed task 364 (REQ-191) -- task 362 had been momentarily
      claimed by a concurrent sibling session in between (its GH#708 issue
      is still open, not done, ruling out "already completed"); released
      364 back to open immediately since it wasn't the intended target,
      then set_lock'd task 362 directly by its now-known id. Routed to
      CODE-DESIGNER for the WF-01/WF-02 design step (REQ-189's own scope is
      a decision record, not code). REQ-190 and REQ-183 both remain
      pending behind this.
  - req: REQ-189
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T00:13:35Z"
    note: >
      WF02-REQ189-20260830 done (queue task 362, GH#708, stage S6).
      docs/migration/decisions/0016-secrets-storage-backend.md written and
      settled: Postgres table (not external KMS/vault) under envelope
      encryption; master key from LETFLOW_SECRETS_MASTER_KEY (64-hex-char,
      32-byte, startup fails hard on absent/malformed/all-zeros/all-f's,
      directly rejecting R-Co's four hardcoded call sites and the
      zero-match MASTER_KEY/WRAPPING_KEY grep); table placed GLOBAL in
      public (divergence from decision 0003 Decision B, reasoned from
      resolveSecret's reference-tenant-vs-caller-tenant check needing one
      query surface before any per-tenant connection prefix is known);
      reference syntax sec://tenant/<tenant>/<namespace>/<name>#<key_id>
      adopted verbatim from R-Co; crypto is AES-256-GCM twice (payload +
      key-wrap), stored wrapped_key_algorithm corrected to "aes_256_gcm"
      (fixing R-Co's dishonest "aes_kw_256" claim); unpinned resolution
      fixed to select newest status="active" row in the query itself
      (fixing R-Co's defect where disabling the newest version broke
      resolution instead of falling back); webhook HMAC key collision
      resolved by superseding REQ-181's hashed secret column with
      secret_ref/secret_key_id columns resolved through this same secrets
      mechanism, REQ-190 named as the implementing requirement. Both
      divergences (global table placement, superseding REQ-181's storage)
      carry REVIEWER sign-off recorded directly in the decision file's own
      "REVIEWER sign-off" section (2026-08-30, ad-hoc gate), GRANTED with
      no conditions on both. CODE-DESIGN-VALIDATOR passed all 8 acceptance
      criteria with zero rework rounds. RELEASE-VALIDATOR independently
      re-verified all 8 acceptance criteria and PASSED, confirming no
      migration/lib/requirements.yaml-edit diff beyond this requirement's
      own scope. No lib/, migration, or route work -- decision-record-only
      requirement, so Steps 2a-4/3b were correctly skipped per WF-02's
      scope test. README.md checked: no mention of secrets/0016/MASTER_KEY
      anywhere in it, confirmed no update needed -- this is an internal
      decision record, not documented user-facing behavior. Unblocks
      REQ-190 (secrets core implementation) and REQ-183 (webhook delivery
      dispatch, HMAC signing), both depends_on: [REQ-189] (REQ-183 also
      depends_on REQ-181, done).
  - req: REQ-188
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T00:53:33Z"
    note: >
      WF02-REQ188-20260829 done (queue task 361, GH#707, stage S6). Third
      and last requirement of S6's scheduler half: SCH-07 recurring timer
      re-arm shipped atop REQ-186/187's poll loop (a fired recurring timer
      creates its NEW pending successor in the SAME transaction as the
      firing, honors repeat_total exhaustion, stops re-arming a cancelled
      instance's chain, and fires at most once per poll cycle when its
      interval is shorter than the poll interval), plus a periodic
      retention runner on REQ-186's existing scheduler process invoking
      the already-shipped but previously zero-caller
      Letflow.EventStore.archive/1, DISABLED by default. Partition-based
      retention (partition_maintenance.zig/partition_retention.zig) and
      SCH-04 escalation timers both named as deliberately deferred in the
      moduledoc, per ISS-0014's adopted option (a) and the missing
      escalation_timer_duration Graph attribute respectively. Path: 2 real
      rework rounds -- CODE-DESIGN-VALIDATOR caught literal Elixir code
      fences in the design doc; TEST-DESIGN-VALIDATOR caught a
      factually-wrong claim about REQ-187's auto-rearm side-effect in test
      documentation, independently traced and empirically confirmed via
      real code tracing rather than trusted. SECURITY-REVIEWER and
      REVIEWER both passed cleanly, no rework. TEST-RUNNER's own agent
      dispatch left a real background test process running without
      waiting for it; ORCH recovered by observing the still-running
      process, waiting for it, and completing the remaining verification
      steps directly. RELEASE-VALIDATOR independently re-verified all 10
      acceptance criteria against the real shipped code
      (lib/letflow/scheduler.ex, lib/letflow/scheduler/poller.ex) and a
      self-run test pass (32/32 on the three named test files; full suite
      via scripts/test_parallel.sh 2566/2569, same 3 pre-existing
      rustc/wasmex-absent-from-sandbox environmental failures, none
      touching this branch's diff); mix compile --warnings-as-errors,
      mix format --check-formatted, and mix letflow.lint_handoffs all
      clean -- PASS, no gap found
      (handoffs/WF02-REQ188-20260829/release-validation-req188.md). This
      closes S6's SCHEDULER HALF IN FULL: REQ-185 (firing-architecture
      decision), REQ-186 (timers schema + scheduler core), REQ-187 (engine
      wiring), and REQ-188 (this recurrence + retention runner) are all
      done. S6's SECRETS HALF (REQ-189 onward) is separate and remains
      open -- REQ-189 has itself since landed on main (decision record
      0016), unblocking REQ-190/REQ-183, but REQ-190 onward is still
      pending. Candidate for a future WF-04 stage-gate check of the
      scheduler half specifically, per RELEASE-VALIDATOR's own handoff
      note -- not performed here since this is a WF-02 run, not WF-04.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-08-30T01:20:54Z"
    note: >
      Queue task 377, WF03-ISS0377-20260830, GH#730. Issue filed by a sibling
      session's RELEASE-VALIDATOR (WF02-REQ189-20260830 Step 5): a Windows
      test-run environment defect -- test/letflow/engine/wasm/plugin_handler_test.exs
      hardcodes a Unix .so/.rs assertion that fails on Windows'
      .dll-shaped wasmex NIF artifact, plus a MIX_OS_CONCURRENCY_LOCK hard-link
      contention between two scripts/test_parallel.sh partitions on Windows.
      This session's sandbox is Linux; diagnosis proceeds via source reading
      per core-directives.md's no-speculation rule, not a literal Windows repro.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-08-30T02:22:00Z"
    note: >
      WF03-ISS0377-20260830: closed ISS-0377 (Windows test-run environment --
      wasmex .so-vs-.dll assertion and MIX_OS_CONCURRENCY_LOCK hard-link
      contention). Fixed test/letflow/engine/wasm/plugin_handler_test.exs's
      cross-platform artifact assertions and scripts/test_parallel.sh's
      per-partition build-path isolation, with a mutation-based fail-then-pass
      regression proof (the fix's "does not touch every :external_resource
      entry" correction turned out to be a real, reproducible failure on this
      Linux sandbox too, not merely a Windows-only symptom). GH#730 closed
      with evidence. docs/issues/ISS-0377.yaml status: resolved. PR #732
      merged. This SCOPE-CHANGE done-event was missing from ISSUE-FIXER's own
      Step 5 -- appended by ORCH before starting the next task, per this
      volume's own started/done pairing convention for SCOPE-CHANGE entries.
  - req: REQ-191
    event: started
    agent: ORCH
    at: "2026-08-30T02:23:30Z"
    note: >
      Queue task 364, GH#710, stage S6. Service catalog core -- DB-backed
      registry that three already-shipped seams (ServiceScopeValidator,
      solution_pack.ex, PinResolver) already depend on via an injectable
      Lookup, plus REQ-069's already-shipped SVC-04 permission surface with
      no backing subsystem. Notable: this table is GLOBAL (not per-tenant-
      schema), a deliberate, reasoned divergence from decision 0003 Decision
      B that requires REVIEWER sign-off in this run.
  - req: REQ-183
    event: started
    agent: ORCH
    at: "2026-08-30T03:34:25Z"
    note: >
      Queue task 356, GH#701, stage S6. Webhook delivery dispatch core --
      HMAC-SHA256 signing, retry/backoff, auto-pause threshold, and
      dead-letter landing via REQ-176's DLQ core. depends_on REQ-181
      (done), REQ-176 (done), REQ-189 (done, settles the webhook HMAC
      key ownership REQ-183 needs). No route/controller in scope --
      REQ-184 is the route-layer half. Queue task 356's lock was
      recovered from a stale state (no branch/commit/handoff trace of
      any prior REQ-183 run existed in this checkout) before this
      dispatch, per handoffs/orchestrator.log's own recovery entry.
  - req: REQ-183
    event: blocked
    agent: ORCH
    at: "2026-08-30T03:47:13Z"
    note: >
      CODE-DESIGN-VALIDATOR (Step 1b) found a genuine circular
      dependency: REQ-183's AC3 requires resolving the webhook signing
      key through the REAL secrets mechanism (REQ-189's decided
      mechanism, "the test and the signer obtain the key the same
      way"), but that mechanism's implementation is REQ-190, which was
      not yet built and whose own depends_on named REQ-183 (REQ-190's
      scope item 6 assumed REQ-183's signing code already existed to be
      "made to resolve through resolve/2"). Neither could legitimately
      go first as originally worded -- CODE-DESIGN-VALIDATOR verdict:
      FAIL, remedy is ORCH resequencing, not CODE-DESIGNER rework (the
      design artefact itself was not defective). User confirmed:
      resequence REQ-190 before REQ-183. Corrected in
      docs/requirements.yaml: REQ-183.depends_on gains REQ-190;
      REQ-190.depends_on drops REQ-183; REQ-190's scope item 6 and AC9
      reworded (REQ-190 now adds the columns and proves resolve/2 works
      for a webhook-shaped reference itself, rather than assuming
      REQ-183's code already exists to retrofit); REQ-183's description
      gets a REWORK 2 note recording this. REQ-183 reverted
      in_progress -> pending, queue task 356 released back to open
      (unlocked, not force-completed) pending REQ-190. Design artefact
      lib/letflow/design/req183-webhook-delivery-dispatch.md on branch
      feature/WF02-REQ183-20260830 is NOT wasted -- its assumed
      Letflow.Secrets.resolve/2 contract is exactly what REQ-190 must
      implement; that branch stays open, unmerged, for reuse when
      REQ-183 resumes.
  - req: REQ-190
    event: started
    agent: ORCH
    at: "2026-08-30T03:48:30Z"
    note: >
      Queue task 363, GH#709, stage S6. Secrets core -- envelope-
      encrypted per-tenant secret storage and resolution by reference
      (EXP-501), builds what REQ-189's decision record 0016 specifies.
      depends_on REQ-189 (done), REQ-072 (done), REQ-181 (done).
      Resequenced ahead of REQ-183 (WF02-REQ183-20260830, paused) per
      CODE-DESIGN-VALIDATOR's Step 1b finding and user confirmation --
      see this volume's own REQ-183 blocked-event entry immediately
      above for the full rationale. Scope item 6 and AC9 were reworded
      as part of that resequencing: this requirement adds
      webhook_subscriptions.secret_ref/.secret_key_id and proves
      resolve/2 works for a webhook-shaped reference itself, rather
      than assuming REQ-183's signing code already exists to retrofit.
  - req: REQ-191
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T04:08:13Z"
    note: >
      WF02-REQ191-20260830 done (queue task 364, GH#710, stage S6). Service
      catalog core shipped: Letflow.ServiceCatalog context module +
      Letflow.ServiceCatalog.Entry schema + GLOBAL (not per-tenant-schema)
      service_catalog migration -- register/2, get_for_tenant/2 (SVC-01
      resolution: global visible to all, tenant-scoped visible only to its
      owner, a different tenant's entry returns NOT FOUND indistinguishable
      from non-existent), list_for_tenant/2 with REQ-067 cursor pagination,
      update_scope/2, delete/2, both with referential guards checking real
      SERVICE_TASK service_id attributes structurally rather than R-Co's
      substring-over-match LIKE approach. Satisfies seam (a) with a real
      Lookup for Letflow.Definitions.ServiceScopeValidator.validate/3, with
      NO change to service_scope_validator.ex itself (confirmed by git diff).
      Seam (b) resolved explicitly: solution_pack.ex's service_catalog_entries
      hard-fail is retained with the owning follow-up named in the moduledoc,
      not left dangling. Path: 1 real rework round -- SECURITY-REVIEWER caught
      an unhandled Ecto.StaleEntryError on a concurrent-delete race in
      delete/1, fixed with a case+rescue pattern matching this codebase's own
      established idiom. REVIEWER's sign-off on the global-table decision-0003
      Decision B divergence was genuine, not rubber-stamped: REVIEWER found
      the cited precedent's analogy imperfect but constructed its own
      independent sufficient justification (a global-scope entry is
      referenceable by every tenant and service_id is unique across tenants,
      so no per-tenant-schema copy could express either property) before
      granting AGREE. TEST-DESIGNER's concurrent-delete-race regression test
      is deterministic -- built on a real Postgres row lock, not a sleep --
      independently reproduced by TEST-DESIGN-VALIDATOR. RELEASE-VALIDATOR
      independently re-verified all 12 acceptance criteria (own mix compile
      --warnings-as-errors, own scripts/test_parallel.sh run -- 2592 tests, 5
      properties, 2595/2597 passed, both failures the pre-existing
      Mix.Tasks.Letflow.CheckToolchainTest rustc-absent-from-sandbox
      environmental baseline, unrelated to this diff -- plus
      test/letflow/service_catalog_test.exs directly at 26/26, mix
      letflow.lint_handoffs 0 new violations, mix format --check-formatted
      clean, and confirmed service_scope_validator.ex's diff is empty) and
      PASSED. No route or controller file added, confirmed by git diff
      --stat scoped to this requirement's commits, per this requirement's
      own scope boundary. docs/migration/stage-4-api-surface.md's
      `services.zig` fronting-subsystems row updated with an "Update
      (2026-08-30, REQ-191 done)" annotation (matching the file's own
      REQ-131 precedent) noting the core has shipped and REQ-192 (the HTTP
      route layer) is the one remaining piece; docs/migration/
      stage-6-operational-cross-cutting.md and stage-3-instance-engine.md
      were checked and need no update -- both already correctly describe
      REQ-191/REQ-192 as the owning requirements and neither asserts a
      now-false claim (stage-3's injectable-Lookup pattern for
      ServiceScopeValidator is explicitly unchanged by this requirement).
      Unblocks REQ-192 (service catalog route surface, SVC-04), still
      pending.
  - req: REQ-183
    event: started
    agent: ORCH
    at: "2026-08-30T04:37:05Z"
    note: >
      Queue task 356, GH#701, stage S6. REQ-VALIDATOR-rework requirement (the
      dispatch-core half of REQ-180's split; REQ-184 is the route-layer
      half). Webhook delivery: HMAC-SHA256 signing, retry/backoff,
      auto-pause on consecutive failures, DLQ landing via REQ-176's
      enqueue/1 (entry_type "webhook"). HMAC header format and pause
      threshold are Letflow's own choices, not ported from R-Co (unreachable
      from this session).
  - req: REQ-183
    event: blocked
    agent: ORCH
    at: "2026-08-30T04:47:07Z"
    note: >
      Queue task 356 released as blocked. CODE-DESIGNER's own investigation
      found REQ-183's deliver/3 needs Letflow.Secrets.resolve/2 (REQ-190) to
      obtain a usable HMAC key for REQ-181's webhook secrets, which are
      one-way-hashed per decision 0016. REQ-190 does not exist on main yet
      (status in_progress, mid-rework per its own git history). This task's
      own depends_on (queue ids 354, 351 = REQ-181, REQ-176) did not surface
      this real dependency on REQ-190. A design artefact was produced
      against REQ-190's already-drafted contract and pushed to
      feature/WF02-REQ183-20260830 for reuse once REQ-190 merges; no
      implementation was started. Queue task 356 re-releasable once REQ-190
      ships.
  - req: REQ-192
    event: started
    agent: ORCH
    at: "2026-08-30T04:49:06Z"
    note: >
      Queue task 365, GH#711, stage S6. Route layer atop REQ-191's already-
      shipped service catalog core -- GET /services (SVC-04 :ServicesRead),
      GET/POST/PATCH/DELETE /admin/services (SVC-04 :AdminServicesRead/
      :AdminServicesManage). REQ-069's permission surface already ships
      these three permission names with no backing route until now.
  - req: REQ-190
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T05:06:28Z"
    note: >
      WF02-REQ190-20260830 done (queue task 363, GH#709, stage S6). Secrets
      core shipped per decision 0016: envelope-encrypted per-tenant secret
      storage and resolution by reference (EXP-501), plus the webhook HMAC
      key reconciliation. Letflow.Secrets context module -- put/2 (returns
      only reference, key_id, timestamp; never plaintext/ciphertext),
      resolve/2 (rejects a reference naming a different tenant than the
      caller before any query; enforces the purpose/consumer matrix;
      resolves an unpinned reference to the newest ACTIVE version, fixing
      the R-Co defect where the newest row could be a disabled one),
      disable/2 (preserves original created_by, does not overwrite it with
      the disabling actor as R-Co does). AES-256-GCM envelope encryption
      with a fresh random data key per secret; master key from
      LETFLOW_SECRETS_MASTER_KEY, startup fails hard on absent or malformed
      value, no all-zeros or other default key. Redaction: reference
      rendering masks the key id; Logger configuration redacts values under
      the R-Co exact-key list (authorization, password, password_hash,
      token, access_token, refresh_token, bootstrap_token, api_token,
      secret, client_secret, credential, credentials, set-cookie, cookie)
      plus the _token/_secret/_password/_credential suffixes, moduledoc
      honestly states redaction keys on field NAME only. Webhook HMAC
      reconciliation: secret_ref/secret_key_id columns added to
      webhook_subscriptions per decision 0016, migration applied to every
      tenant schema, idempotent/re-runnable, asserted (not assumed) that no
      existing webhook_subscriptions row needed migrating since REQ-181 was
      still pending at the time. No route/controller added (REQ-191's
      scope), confirmed by git diff --stat scoped to this requirement's
      commits. Path: 1 ELIXIR-DEV rework cycle fixing an empty-plaintext
      defect. CODE-DESIGNER, CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER,
      REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR, TEST-RUNNER,
      RELEASE-VALIDATOR all PASS. RELEASE-VALIDATOR independently
      re-verified all 12 acceptance criteria. Full suite via
      scripts/test_parallel.sh: 2589/2590 passed, 1 pre-existing
      environmental failure (ISS-0378, filed and forwarded), not touching
      this branch's diff. docs/migration/decisions/0016-secrets-storage-backend.md
      confirmed unchanged and accurately reflecting what shipped -- no
      edits needed, it was already fully written/signed-off under REQ-189.
      README.md checked for an env-var reference table pattern to extend
      with LETFLOW_SECRETS_MASTER_KEY: none exists (env vars are documented
      inline in specific sections -- dev DB setup, port config -- not in a
      central table), and this requirement adds no HTTP-visible surface, so
      no README change made. Unblocks REQ-183 (webhook delivery dispatch,
      depends_on: [REQ-190, REQ-181, REQ-176, REQ-189], all now done) and
      REQ-191 (secrets route layer).

# ─────────────────────────────────────────────────────────────────────────────
# VOLUME 6 — CLOSED 2026-08-30. DO NOT APPEND TO THIS FILE.
#
# This volume holds the complete requirement run history from 2026-08-28 through
# 2026-08-30 (42 entries, lines 1-1202). It is frozen: every line above this
# comment is preserved byte-for-byte. Closed under WF02-REQ190-20260830's Step
# Final rebase composition (ELIXIR-DEV), which merged this volume's own REQ-190
# done-event append against origin/main's concurrently-appended REQ-190 started,
# REQ-191 done, REQ-183 started, REQ-183 blocked, and REQ-192 started events (all
# composed in chronological order by `at:` timestamp, none dropped, reordered or
# overwritten), bringing this file to 1202 lines -- over its declared roll_rule
# ceiling (max_lines: 1200) -- confirmed via `wc -l` on the working-tree (CRLF)
# file per this volume's own "WHEN THIS VOLUME IS FULL" procedure. Reads of this
# volume are TARGETED reads only -- a line range or a pattern match, never a
# whole-file read. Use whichever your shell has:
#   Read tool with offset/limit (works everywhere), or
#   Bash:       grep -n PATTERN FILE   /   sed -n 'START,ENDp' FILE
#   PowerShell: Select-String -Path FILE -Pattern PATTERN
#               (Get-Content FILE)[START..END]
#
# NEW ENTRIES GO TO THE CURRENT VOLUME. Find it in:
#   docs/status/requirement_status.index.yaml
# which, as of this closure, names:
#   docs/status/requirement_status.v7.yaml
#
# Why this file was closed: composing WF02-REQ190-20260830's Step Final rebase
# (REQ-190 done-event append plus origin/main's five concurrently-appended
# events) brought the on-disk count to 42 entries across 1202 lines -- over the
# roll ceiling. No entry above this footer was moved, edited or deleted.
#
# KNOWN ANOMALIES IN THIS VOLUME: none declared beyond what volumes 1-3 already
# declare against themselves.
# ─────────────────────────────────────────────────────────────────────────────
