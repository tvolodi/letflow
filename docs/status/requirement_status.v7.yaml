# Letflow — Requirement run history — VOLUME 7 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v6.yaml (volume 6, closed,
#   1,202 frozen-prefix lines / 1,236 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v7.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v7.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.

history:
  - req: REQ-192
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T06:20:17Z"
    note: >
      WF02-REQ192-20260830 done (queue task 365, GH#711, stage S6). Service
      catalog route surface shipped atop REQ-191's core: GET /api/v1/services
      (tenant-scoped, :ServicesRead) and GET/POST/PATCH/DELETE
      /api/v1/admin/services (:AdminServicesRead/:AdminServicesManage), with
      the new Letflow.ServiceCatalog.list_all/1 added to REQ-191's already-
      merged context module to resolve a genuine INV-RT-1 conflict (no
      Repo-querying from a router), REVIEWER-signed-off rather than silently
      decided. Path: 2 CODE-DESIGNER/CODE-DESIGN-VALIDATOR rework rounds
      settling that scope expansion, 1 TEST-DESIGN-VALIDATOR rework round
      fixing a fixture-sizing bug (commit 09402cd6). RELEASE-VALIDATOR
      independently re-verified all 10 acceptance criteria (own re-run of
      test/letflow/service_catalog_test.exs + test/letflow/routers/
      admin_services_test.exs at 39/39, own scripts/test_parallel.sh run at
      2610/2612 with only the pre-existing rustc-absent baseline gap, own
      grep confirming zero live-code Repo. matches under lib/letflow/routers/,
      own git diff --stat confirming authorization.ex and web/ untouched) and
      PASSED. This closes S6's repository half's route layer: REQ-191 (core)
      and REQ-192 (routes) are both done. This entry was moved from volume 6
      to volume 7 during Step Final rebase composition (ORCH), since volume 6
      was concurrently closed/frozen by a sibling session's WF02-REQ190
      Step Final before this branch rebased -- no entry in volume 6 was
      moved, edited or deleted, this REQ-192 done-event simply never
      landed there and lands here instead.
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-08-30T06:51:14Z"
    note: >
      WF03-ISS0378-20260830 started (queue task 378, GH#734, stage S6).
      ISS-0378: REQ-186's own poller_test.exs:340 (AC7) hardcodes a
      zero-diff assertion against lib/letflow/application.ex as if it were
      a repo-wide invariant, when it's really just an artifact of when
      REQ-186 happened to be written -- trips on any legitimate future
      application.ex change (first hit by REQ-190's own already-approved
      :logger primary filter registration). git blame confirms the test
      originates from commit 77637268 (REQ-186, already on main), not a
      REQ-190 regression. Fix: scope/remove the zero-diff assertion to
      check only what REQ-186 actually needs.
  - req: SCOPE-CHANGE
    event: done
    agent: ISSUE-FIXER
    at: "2026-08-30T07:19:11Z"
    note: >
      WF03-ISS0378-20260830 done (queue task 378, GH#734, stage S6). Root
      cause: REQ-186's own poller_test.exs:340 (AC7) hardcoded a zero-diff
      assertion of lib/letflow/application.ex against main as a repo-wide
      invariant, when it was really just an artifact of when REQ-186 was
      written (commit 77637268, confirmed via git blame) -- broke on
      REQ-190's own already-approved :logger primary-filter registration.
      Third occurrence of this defect class; documented in
      docs/anti-patterns.md. Fix (commit cb9faa77): deleted the defective
      test and its unused helper, corrected a mislabeled describe title --
      no structural replacement was provable without exporting a
      currently-private function (see
      lib/letflow/design/iss0378-poller-ac7-test-fix.md secs 1-2); the
      sibling GenServer-count test already covers the only mappable
      acceptance criterion. Gates: ISSUE-FIXER diagnosis -> CODE-DESIGNER/
      CODE-DESIGN-VALIDATOR (2 rework-free rounds) -> SECURITY-REVIEWER
      PASS -> REVIEWER PASS -> TEST-DESIGNER (no coverage gap, no new test
      needed) -> TEST-DESIGN-VALIDATOR PASS -> TEST-RUNNER PASS (full
      suite 2628/2630, 2 pre-existing rustc-absent baseline failures
      unrelated to this diff; targeted poller_test.exs +
      scheduler_test.exs 23/23; mix compile --warnings-as-errors clean).
      docs/issues/ISS-0378.yaml set to status: resolved. GH#734 closed
      with evidence comment. Queue task 378 released via
      POST /tasks/378/release {"status":"done"} -- HTTP 200, locked_by
      null in response.
  - req: REQ-183
    event: started
    agent: ORCH
    at: "2026-08-30T07:24:20Z"
    note: >
      RESUMED. Queue task 356, GH#701, stage S6. Webhook delivery
      dispatch core. Paused 2026-08-30 pending REQ-190 (secrets core) --
      see requirement_status.v6.yaml's REQ-183 blocked-event for the
      full resequencing rationale. All depends_on now done: REQ-181,
      REQ-176, REQ-189, REQ-190. Resuming on the preserved branch
      feature/WF02-REQ183-20260830 (design artefact
      lib/letflow/design/req183-webhook-delivery-dispatch.md already
      committed) -- re-dispatching Step 1b with instructions to
      reconcile its assumed Letflow.Secrets.resolve/2 contract against
      REQ-190's actual shipped module (2 flagged mismatches:
      consumer: :webhook_dispatcher opt, {:ok, %{plaintext:, key_id:,
      purpose:}} return shape).
  - req: REQ-197
    event: started
    agent: ORCH
    at: "2026-08-30T08:19:08Z"
    note: >
      Queue task 370, GH#716, stage S6. Extends lib/letflow/engine/expr.ex
      with arithmetic (+ - * / %), unary negation, and a structured
      parse-error surface, without breaking EE-05's catch-false gateway
      contract or expr.ex's zero-impurity grep. R-Co's expr is NOT a CEL
      implementation (EXP-102 cut over from vendored CEL); scope is R-Co's
      actual arithmetic/precedence/error semantics only -- no CEL macros,
      collections, ternary, or `in`. now()/date_add/date_diff explicitly
      excluded as clock-impure. Depends_on [REQ-050, REQ-111], both done.
  - req: REQ-183
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T09:28:43Z"
    note: >
      WF02-REQ183-20260830 done (queue task 356, GH#701, stage S6). Webhook
      delivery dispatch core shipped: Letflow.Webhooks.deliver/3 computes an
      HMAC-SHA256 signature (X-Letflow-Signature: sha256=<hex>, Letflow's own
      choice) of the JSON-encoded payload using the signing key resolved
      through REQ-190's real, shipped Letflow.Secrets.resolve/2 (not a
      stub -- rework 2 fixed the implementation-dependency gap rework 1's
      depends_on fix alone did not close), POSTs to target_url, classifies
      SUCCESS/FAILED, and persists one webhook_delivery_attempts row per
      attempt. On FAILED, increments consecutive_failures and retries with
      exponential backoff (base 2s, capped at 4 attempts); reaching the
      documented threshold (5) flips the subscription to PAUSED and sets
      paused_at; exhausting max_attempts lands exactly one DLQ entry via
      Letflow.Dlq.enqueue/2 with entry_type "webhook" and reference_id =
      delivery_id. No route/controller touched (REQ-184 remains the deferred
      route half); automatic post-event triggering remains a named, deferred
      follow-up, not silently implemented. Gate path: CODE-DESIGNER +
      CODE-DESIGN-VALIDATOR (1 rework round reconciling the design against
      REQ-190's real shipped Secrets module), ELIXIR-DEV, SECURITY-REVIEWER
      (PASS -- flagged a genuine SSRF gap on target_url, filed as new
      requirement REQ-204 rather than blocking here, since no acceptance
      criterion in this requirement covers it), REVIEWER (PASS, 2
      non-blocking notes), TEST-DESIGNER (1 rework round fixing a real bug
      in its own hand-rolled WebhookTestServer, commit 2007d9aa),
      TEST-DESIGN-VALIDATOR, TEST-RUNNER (full suite 2638/2638,
      TEST_PARALLEL_N=4), RELEASE-VALIDATOR (independently re-verified: full
      suite pass, all 7 acceptance criteria confirmed against real
      code/tests, REQ-204 tracking confirmed). Design artefact
      lib/letflow/design/req183-webhook-delivery-dispatch.md already
      reflects what shipped (§0.3 records REQ-190's resolution, §8's AC
      checklist matches) -- no touch-up needed at this step.

      Step Final's own post-rebase CI run caught two `mix letflow.check.test`
      warnings-as-errors defects this branch's own test file introduced (a
      dead default argument on provisioned_tenant/1, an unused `alias
      Letflow.Dlq`) that had not surfaced in this run's own pre-push local
      checks (which ran `mix compile --warnings-as-errors` and `mix test`
      separately, never `mix test --warnings-as-errors` together) -- fixed
      in ELIXIR-DEV commit a43d4116, re-verified clean both narrowly
      (`mix test test/letflow/webhooks_delivery_test.exs --warnings-as-errors`,
      7/7) and via the full suite (scripts/test_parallel.sh, 2637/2637, 0
      failures) before re-pushing.
  - req: REQ-197
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T10:08:27Z"
    note: >
      WF02-REQ197-20260830 done (queue task 370, GH#716, stage S6). expr.ex
      extended with arithmetic (+ - * / %), unary negation, and a structured
      parse-error surface, preserving EE-05's catch-false gateway contract
      and the zero-impurity grep. Path: 2 CODE-DESIGN-VALIDATOR rework
      rounds (rework 1 fixed a real AC5 null-asymmetry gap -- ordering
      comparisons now propagate nil while arithmetic still errors on nil),
      1 REVIEWER rework round fixing a genuine defect in implementation
      (a signed-infinity sign-flip bug: float division-by-zero originally
      used one unsigned :infinity marker regardless of dividend sign,
      corrected to the 3-way :infinity/:neg_infinity/:nan split), 1
      SECURITY-REVIEWER PASS, 7 TEST-DESIGNER coverage gaps found/filled,
      2 TEST-DESIGN-VALIDATOR-confirmed mutations caught. TEST-RUNNER and
      RELEASE-VALIDATOR both independently PASSED all 12 acceptance
      criteria (RELEASE-VALIDATOR's own re-run: scripts/test_parallel.sh
      2655/2657 with the 2 failures independently re-diagnosed as the
      pre-existing rustc-absent letflow_check_toolchain_test.exs gap, not
      a REQ-197 regression; targeted expr_test.exs/transition_test.exs
      100/100; mix compile --warnings-as-errors clean; purity grep zero
      real-code matches). Full detail:
      handoffs/WF02-REQ197-20260830/release-validation-req197.md.
  - req: REQ-198
    event: started
    agent: ORCH
    at: "2026-08-30T10:24:24Z"
    note: >
      Queue task 371, GH#717, stage S6. Second and last of S6's expr half,
      atop REQ-197's extended grammar. Adds a function-call form and 8 pure
      builtins (length, lower, upper, trim, contains, startsWith, endsWith,
      coalesce) to lib/letflow/engine/expr.ex, ported from R-Co's closed
      lex-time whitelist design (src/expr/lexer.zig). now()/date_add/
      date_diff explicitly excluded (clock-impure, per REQ-197's own
      decision). Must not weaken @unsupported_call_markers. Depends_on
      [REQ-197], done.
  - req: REQ-198
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T11:08:03Z"
    note: >
      WF02-REQ198-20260830 done (queue task 371, GH#717, stage S6). Added
      expr.ex's function-call form and 8 pure builtins (length, lower, upper,
      trim, contains, startsWith, endsWith, coalesce) atop REQ-197's
      extended grammar, preserving the lex-time closed whitelist (unknown
      names fall to the bare-variable path and surface as parse errors, not
      evaluated), EE-05's catch-false gateway contract, and the zero-impurity
      grep. now()/date_add/date_diff remain excluded as clock-impure.
      TEST-DESIGNER found and filled one real coverage gap: wrong-arity
      tests for 6 of the 8 builtins were initially missing (only length and
      coalesce had them); all 8 now have both-direction (too few/too many)
      arity coverage. TEST-DESIGN-VALIDATOR confirmed 3 real mutations
      caught. RELEASE-VALIDATOR independently re-verified all 11 acceptance
      criteria against the real code and a self-run test pass (targeted
      expr_test.exs + transition_test.exs 160/160; mix compile
      --warnings-as-errors clean; mix format --check-formatted clean; full
      suite via scripts/test_parallel.sh 2715/2717, the 2 failures being the
      pre-existing rustc-absent letflow_check_toolchain_test.exs baseline,
      not a regression). This closes S6's expr subsystem in full: REQ-197
      (arithmetic/parse-error surface) and REQ-198 (builtins) are both done.
      Full detail: handoffs/WF02-REQ198-20260830/release-validation-report-req198.md.

  - req: REQ-184
    event: started
    agent: ORCH
    at: "2026-08-30T11:27:33Z"
    note: >
      Queue task 357, GH#702, stage S6. Route layer atop REQ-183's just-
      merged webhook delivery-dispatch core -- GET /api/v1/webhooks/
      subscriptions/:id/deliveries, WebhooksManage-gated, REQ-072 cross-
      tenant-404 mechanism (mirrors REQ-182's subscription-route pattern).
      REQ-VALIDATOR rework of REQ-180's split. Depends_on [REQ-183,
      REQ-181, REQ-069, REQ-072], all done.

  - req: REQ-193
    event: started
    agent: ORCH
    at: "2026-08-30T11:46:43Z"
    note: >
      WF02-REQ193-20260830 started (queue task 366, GH#712, stage S6).
      Structured JSON logging with trace-id propagation (OBS-01,
      XC-01 non-HTTP half). Stale lock force-released (no branch, no
      handoff files, no registry entry -- confirmed dead session).
      REQ-072 (depends_on) is done. No owned_modules conflicts with
      active runs. Feature branch feature/WF02-REQ193-20260830 created
      from origin/main at 3a16095f. REQ-190 (secrets core, done) did
      NOT implement the sensitive-value redaction mechanism -- grep of
      lib/letflow/ returns zero redact/REDACTED matches -- so REQ-193
      implements it and its moduledoc will name that fact. Step 01
      dispatched to CODE-DESIGNER.

  - req: REQ-184
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T12:12:23Z"
    note: >
      WF02-REQ184-20260830 done (queue task 357, GH#702, stage S6). Route
      layer atop REQ-183's delivery-dispatch core: GET /api/v1/webhooks/
      subscriptions/:id/deliveries, WebhooksManage-gated, REQ-072
      cross-tenant-404 mechanism (same private get/2 path as PATCH/DELETE,
      confirmed genuinely indistinguishable for non-existent vs.
      other-tenant ids, not a weaker proxy), limit-param fallback/truncation,
      9-field response allowlist diffed by hand against
      web/src/types/api.ts's WebhookDeliveryAttempt (exact match). Path:
      design + CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER PASS, REVIEWER PASS
      (caught and fixed a real bug via ELIXIR-DEV's own failing test: the
      design assumed conn.params carried query-string values, but this
      router's pipeline never called fetch_query_params/1 -- fixed to
      fetch_query_params(conn) explicitly, matching the dlq.ex/tasks.ex/
      definitions.ex idiom; REVIEWER also signed off on the ordering choice
      against the real SPA component), TEST-DESIGNER (2 coverage gaps
      found/filled), TEST-DESIGN-VALIDATOR (3 real mutations confirmed
      caught), TEST-RUNNER (43/43 targeted, full suite clean modulo the
      pre-existing rustc-absent baseline), RELEASE-VALIDATOR (independently
      re-verified all 6 acceptance criteria against the real code and its
      own re-run of the target tests -- PASS, see
      handoffs/WF02-REQ184-20260830/req184-release-validation-report.md).
      This completes the webhooks vertical in full: REQ-181 (core),
      REQ-182 (subscription routes), REQ-183 (delivery dispatch), REQ-184
      (deliveries route) are all done.
  - req: REQ-200
    event: started
    agent: ORCH
    at: "2026-08-30T12:38:22Z"
    note: >
      Queue task 373, GH#719, stage S6. Fixes a real broken contract: the
      already-shipped SPA's TimelineFeedItem.tsx reads actor_display_name,
      description, timestamp, sequence_num from GET /instances/:id/timeline
      (REQ-080, done S4), but timeline_item/1 never emits the first two and
      names the latter two differently. Ports R-Co's src/obs/timeline.zig
      actor-fallback chain (actor id -> token_description -> actor_label ->
      "system") and per-event-type description rendering, N+1-safe (one
      batched user lookup per page). Route/pagination/auth/404 unchanged
      per REQ-080. No web/ changes, no authorization.ex changes. Depends_on
      [REQ-080, REQ-072, REQ-069], all done.

  - req: REQ-193
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T13:23:32Z"
    note: >
      WF02-REQ193-20260830 done (queue task 366, GH#712, stage S6).
      Structured JSON logging with trace-id propagation: Letflow.Obs.Logger
      (:logger_formatter behaviour), single-line JSON output (timestamp/level/
      trace_id/component/message), ISO 8601 UTC microsecond timestamps, level
      configurable via LOG_LEVEL env var with startup-error on invalid value,
      with_trace/1 for background trace origination, sensitive-value redaction
      (R-Co's 14-key + 4-suffix list). REQ-193 implements the shared redaction
      mechanism first (REQ-190 confirmed not implemented). config scoped:
      backends: [] in dev/prod only (test env preserved for CaptureLog).
      Pipeline: CODE-DESIGNER (1 rework-free pass) -> CODE-DESIGN-VALIDATOR PASS ->
      ELIXIR-DEV (2 rework rounds: security fallback-path redaction fix, CaptureLog
      regression fix) -> SECURITY-REVIEWER PASS (1 rework) -> REVIEWER PASS ->
      TEST-DESIGNER PASS (10/10) -> TEST-DESIGN-VALIDATOR PASS -> TEST-RUNNER
      PASS (2735/2737, 2 pre-existing Windows grep flakes) -> RELEASE-VALIDATOR PASS.
      Full detail: handoffs/WF02-REQ193-20260830/.

  - req: REQ-200
    event: done
    agent: DOC-UPDATER
    at: "2026-08-30T13:47:40Z"
    note: >
      WF02-REQ200-20260830 done (queue task 373, GH#719, stage S6). Instance
      timeline rendering shipped: actor-display-name 4-level fallback chain
      (actor_id lookup -> metadata token_description -> metadata actor_label
      -> literal "system", TOTAL over all paths including a deleted-user
      actor_id) and per-event-type description rendering with a mandatory
      generic fallback for Lua-emitted event types outside the fixed
      whitelist, plus the timestamp/sequence_num field renames, all N+1-safe
      via one batched actor lookup per page. This closes a real broken
      contract left by REQ-080 (S4): the already-shipped SPA's
      TimelineFeedItem.tsx was reading actor_display_name, description,
      timestamp and sequence_num, none of which the REQ-080 route ever
      emitted. Gate path: CODE-DESIGNER (found the complete 7-event-type
      enumeration plus the open-ended Lua-emitted-event-type structural
      finding that necessitated the generic fallback clause),
      CODE-DESIGN-VALIDATOR, ELIXIR-DEV, SECURITY-REVIEWER (traced the
      batched actor lookup's prefix threading to confirm cross-tenant
      isolation), REVIEWER, TEST-DESIGNER (1 real gap: an AC2-split rework
      hit and fixed a genuine DB null-constraint violation -- actor_id: nil
      is not valid against the events table's null: false column -- caught
      mid-rework), TEST-DESIGN-VALIDATOR (4 real mutations confirmed
      caught), TEST-RUNNER, RELEASE-VALIDATOR (independently re-verified
      all 11 acceptance criteria against the real lib/letflow/instances.ex
      and lib/letflow/routers/instances.ex code and a self-run of the
      suite -- PASS, see
      handoffs/WF02-REQ200-20260830/req200-release-validation-report.md).
      docs/migration/stage-6-operational-cross-cutting.md updated to record
      REQ-200 done.
