# Letflow — Requirement run history — VOLUME 8 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v7.yaml (volume 7, closed,
#   1,220 frozen-prefix lines / 1,248 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v8.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v8.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v3.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v3.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: REQ-211
    event: done
    agent: DOC-UPDATER
    at: "2026-09-01T14:06:49Z"
    note: >
      WF02-REQ211-20260901 done (queue task 394, GH#774, stage S6). No
      started-event exists anywhere for this run -- gap flagged to ORCH
      rather than fabricated here. Closes half of GH#769/ISS-0390 ("missing
      attachment/document-upload subsystem", found during REQ-206's S7
      SwiftRoute simulation batch); the other half, the HTTP route surface,
      is REQ-212 (task 395, GH#775), still open -- letflow-queue task 390
      (ISS-0390) is DELIBERATELY left open/locked and must NOT be released
      yet, since releasing it now would prematurely close GH#769 while the
      actual HTTP surface doesn't exist. New instance_attachments table
      (tenant-scoped, tenant_id retained, instance_id required, per decision
      0003-B) plus Letflow.Repository.Attachments context module (upload/2,
      list/2, get/2, delete/2). Reuses REQ-202's repository_artifacts table
      as the underlying byte store via a migration addendum adding a
      `content :binary` column to that already-done REQ-202 table --
      REVIEWER-approved schema change to a previously-shipped table, flagged
      explicitly rather than silently made. Cross-module change to
      lib/letflow/repository.ex: its private upsert_content/5 was widened to
      a public upsert_content/6 so this requirement's upload/2 could reuse
      REQ-202's existing hash-and-store path instead of forking a second
      one; REQ-202's own 28/28 tests still pass unchanged. Path: 1
      CODE-DESIGN-VALIDATOR rework round (a real byte-storage gap in the
      design -- repository_artifacts had no binary payload column before
      the migration addendum closed it). TEST-DESIGNER found and closed 2
      real coverage gaps via mutation testing. ISS-0399/GH#781 filed for the
      deliberately deferred antivirus/content-scanning follow-up named in
      this requirement's own moduledoc, not silently omitted.
      RELEASE-VALIDATOR independently re-verified all 11 acceptance criteria
      PASS, and caught a genuine git-index staleness landmine before
      signing off -- two files were staged as empty blobs despite correct
      working-tree content; a fix was required and applied before the final
      PASS. Full suite 3011/3011 (TEST_PARALLEL_N=4, documented ISS-0219
      workaround). docs/migration/stage-6-operational-cross-cutting.md NOT
      touched by this step: its line 108 "S6 is now complete" sentence,
      written when REQ-199 landed 2026-08-31 (before REQ-211/REQ-212 were
      discovered via REQ-206's S7 findings), is now stale -- flagged here as
      a finding, per REQ-ANALYST's own note that this line is a historical
      record not to silently edit, rather than corrected in this step.

      NOTE ON VOLUME PLACEMENT: this entry originally landed in
      requirement_status.v7.yaml (written before a sibling session's
      WF02-REQ208-20260901 merged and closed volume 7 at 42 entries,
      rolling to volume 8). ORCH's Step-Final rebase of
      feature/WF02-REQ211-20260901 onto main found this and moved the entry
      here, into the now-current volume 8, rather than reopening the
      closed/frozen volume 7 -- matching the precedent this file's own
      history already sets for a requirement whose append lands on a branch
      that a concurrent merge later rolls out from under it. `grep -c "^  -
      req:" docs/status/requirement_status.v8.yaml` = 1 on-disk entry, the
      first in this volume.

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-01T16:50:58Z"
    note: >
      WF03-ISS0400-20260901 started (queue task 400, GH#784). ISS-0400:
      Engine collect_leaf_gateways/3 does not support a PARALLEL_GATEWAY
      nested inside a fork branch -- explicitly named out-of-scope by
      ISS-0398's own fix design. Before branching, found ISS-0398's fix
      (PR #783, a sibling session's completed WF-03 run, all CI green,
      mergeable, not a draft, no blocking reviews) still open and unmerged
      with queue task 398 locked by a different agent_id (409 on
      set_lock) -- ISS-0400's own base (collect_leaf_gateways/3) did not
      exist on main. Squash-merged PR #783 directly (commit 58e3c64e)
      rather than duplicate or block indefinitely, per this operator's
      sibling-session discretion rule; did NOT force-release task 398's
      lock, left to that session's own Step Final. Branched
      feature/WF03-ISS0400-20260901 from the updated main (58e3c64e).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-01T19:25:15Z"
    note: >
      WF03-ISS0400-20260901 done -- ISS-0400 resolved (queue task 400,
      GH#784 closed). Fixed Letflow.Engine.Transition.collect_leaf_gateways/3
      to resolve a PARALLEL_GATEWAY nested inside one branch of an outer
      PARALLEL_GATEWAY split, explicitly out of scope in ISS-0398's own fix.
      New resolve_nested_split/3 shares the outer walk's Tarjan-SCC traversal
      state fully (no reset at that boundary) rather than the design's
      original reset-based mechanism, which CODE-DESIGN-VALIDATOR correctly
      FAILED at BLOCKER severity (rework 1/3) after constructing a concrete
      infinite-recursion counterexample; the re-gate independently re-traced
      the same counterexample against the revised design and passed. All
      downstream gates PASS on first pass: SECURITY-REVIEWER (INV-8
      DoS/resource-consumption framing independently re-derived, nesting
      shares one Tarjan pass, no multiplicative cost), REVIEWER (idiom,
      reuse of existing fold_outgoing_edges/4 and close_scc_or_defer/5, no
      scope creep), TEST-DESIGNER's fail-then-pass regression tests (5
      fixtures from the design's own section 4, 13/18 pre-fix, 18/18
      post-fix, independently reproduced by TEST-DESIGN-VALIDATOR),
      TEST-RUNNER full suite 3031/3031. Step Final: found and merged a
      sibling session's own completed-but-unmerged ISS-0398 fix (PR #783,
      commit 58e3c64e) as an unblocking prerequisite before branching, since
      ISS-0400's own fix builds directly on collect_leaf_gateways/3;
      resolved a genuine registry.json rebase conflict on the ISS-0398 entry
      by keeping main's more current version (the sibling run had in fact
      completed its own Step Final by then) and appending this run's own
      entry. CI hit the documented wasm_hang isolation-subprocess flake
      (ISS-0352, third recorded recurrence) on one of two duplicate runs;
      structurally cleared as unrelated (branch touches only transition.ex
      and ISS-0400 files, sibling duplicate CI run passed the identical
      suite clean) per GIT_MERGE.md's precondition, recurrence noted on
      ISS-0352.yaml, did not block merge. Also structurally cleared 2
      pre-existing local full-suite failures (test/letflow/repository/attachments_test.exs:392,
      confirmed failing on pre-ISS-0400 main independently -- filed as
      ISS-0401/GH#785 for later diagnosis; and a known Lua executor timing
      flake, confirmed clean 48/48 in isolation) before Step Final. PR #786
      squash-merged as 6188191d. Queue task 400 released status: done.

  - req: REQ-212
    event: done
    agent: DOC-UPDATER
    at: "2026-09-01T19:33:20Z"
    note: >
      WF02-REQ212-20260901 done (queue task 395, GH#775, stage S6).
      RELEASE-VALIDATOR independently confirmed all 9 acceptance criteria
      PASS. Closes the route half of GH#769/ISS-0390 -- REQ-211 (task 394,
      GH#774) already closed the core half, merged to main as PR #782. Both
      halves of GH#769/ISS-0390 are now done in full: letflow-queue task
      390 (ISS-0390) should now be released as done by ORCH at Step Final,
      per the trigger REQ-211's own DOC-UPDATER note flagged to wait for.
      4 new routes (POST/GET-list/GET-content/DELETE) on
      Letflow.Routers.Instances. Multipart parser extension uses Plug's
      per-parser {parser, opts} override -- a deliberate improvement over
      the original design, leaving JSON's 2MB ceiling untouched while
      giving multipart its own 25MB ceiling. AttachmentsManage/
      AttachmentsRead new permissions added to REQ-069's matrix. get_content/2
      added to REQ-211's Letflow.Repository.Attachments context module
      (not the router) to satisfy this codebase's real INV-RT-1
      no-Repo-calls-in-routers invariant -- a justified deviation from the
      original design. Two distinct 404 checks (cross-tenant structural,
      cross-instance-same-tenant explicit) verified on all
      attachment_id-taking routes. TEST-DESIGNER found and closed 1 real
      coverage gap via mutation testing (upload/2's own defense-in-depth
      size check needed independent test coverage beyond the
      Plug.Parsers-level rejection). CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER
      and REVIEWER all passed clean on first attempt, zero rework rounds.
      Full suite 3031/3031 (TEST_PARALLEL_N=4).
      docs/migration/stage-6-operational-cross-cutting.md NOT edited by
      this step: its line 107 "S6 is now complete" sentence predates
      REQ-211/REQ-212's discovery (per REQ-ANALYST's own note, a historical
      record not to silently edit); DOC-UPDATER flagged whether an
      appended acknowledgement note was warranted rather than deciding
      solo. ORCH decided yes at Step Final: appended a dated note
      immediately after the "S6 is now complete" sentence (not an edit to
      it) naming REQ-211/REQ-212 as a later-discovered subsystem in the
      same stage.

  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-01T19:48:34Z"
    note: >
      WF03-ISS0401-20260902 started (queue task 401, GH#785). ISS-0401:
      test/letflow/repository/attachments_test.exs:392's
      function_exported?(Repository, :upsert_content, 6) assertion returns
      false despite upsert_content/6 genuinely existing at that arity
      (confirmed via lib/letflow/repository.ex:251 and
      Letflow.Repository.__info__(:functions)) -- self-filed by ORCH at the
      end of WF03-ISS0400-20260901's Step Final, after independently
      reproducing the failure in a throwaway worktree at pre-ISS-0400 main
      with zero diff overlap. Claimed via get_next_task (agent_id
      tvolodi-orch). Wrote docs/issues/ISS-0401.yaml (the local mirror
      record did not exist yet -- only registered via the queue API
      directly during the prior run). Branched
      feature/WF03-ISS0401-20260902 from main (5ac81bab).

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-01T19:58:39Z"
    note: >
      WF03-ISS0401-20260902 done -- ISS-0401 resolved (queue task 401,
      GH#785 closed). ISSUE-FIXER directly measured the root cause:
      Letflow.Repository is genuinely compiled with upsert_content/6
      exported, but not yet loaded into the BEAM process's code table at
      the moment attachments_test.exs:392 runs standalone --
      function_exported?/3 checks the loaded-module table and returns
      false for a compiled-but-unloaded module, with no load side effect
      of its own. Confirmed deterministic given test run order within one
      process, not a parallelism/timing flake. ORCH applied the fix
      directly under the direct-action sizing rule
      (docs/agents/ORCHESTRATOR.md section 10 -- all six checks pass:
      exactly one file, no new public function/module/@spec, no
      migration, not a supervision-tree file, not a tenant-data path, no
      test's own asserted expected value changes): added a
      Code.ensure_loaded?(Repository) guard immediately before the
      assertion in test/letflow/repository/attachments_test.exs:392-397.
      Fail-then-pass proven directly: 0/1 passed pre-fix (via git stash),
      1/1 passed post-fix, reliably reproduced 5/5 standalone reruns; full
      attachments_test.exs file 18/18 passed post-fix; full suite
      3031/3031 clean. No CODE-DESIGNER/REVIEWER chain was needed given
      the sizing-rule qualification. GH#785 closed with evidence comment.
      Queue task 401 released status: done.

  - req: REQ-209
    event: started
    agent: ORCH
    at: "2026-09-02T02:33:20Z"
    note: >
      WF02-REQ209-20260902 started (queue task 386, GH#758, stage S7).
      Ports R-Co's 15-entry condition-evaluation corpus
      (tests/differential/corpus/conditions_v1.json) as an ExUnit
      regression suite for Letflow.Engine.Expr. depends_on: [REQ-050,
      REQ-197, REQ-198], all confirmed done. Locked directly via set_lock
      rather than get_next_task: get_next_task was persistently returning
      ISS-0402 (newest open issue, genuinely stuck on a needed product
      decision that no agent can make -- see docs/issues/ISS-0402.yaml's
      scoping_note) ahead of any requirement task via LIFO ordering;
      released ISS-0402 back to open without status each time rather than
      re-investigating a question already thoroughly diagnosed. Feature
      branch feature/WF02-REQ209-20260902 created from main, pushed.

  - req: REQ-213
    event: done
    agent: DOC-UPDATER
    at: "2026-09-02T03:37:23Z"
    note: >
      WF02-REQ213-20260901 done (queue task 405, GH#794, stage S7). Lighter
      WF-02 variant, pure instruction-text insertion, no application code --
      CODE-DESIGNER -> CODE-DESIGN-VALIDATOR -> REVIEWER -> RELEASE-VALIDATOR
      -> DOC-UPDATER (precedent: REQ-163/REQ-164), no ELIXIR-DEV/
      SECURITY-REVIEWER/TEST-* since neither lib/ nor test/ was touched.
      Closes the 7-times-recurring dead-default-argument defect
      (docs/anti-patterns.md's "A test helper's default argument goes dead
      the moment every call site starts passing it explicitly" entry,
      ISS-0069 -- REQ-178/187/191/195/203) by adding `mix letflow.check.test`
      as a mandatory named step in both TEST-DESIGNER's and
      RELEASE-VALIDATOR's own instructions, per the anti-patterns.md entry's
      own stated fix. Edited exactly two files:
      .claude/agents/test-designer.md (new step 3, run before the handoff to
      TEST-DESIGN-VALIDATOR is reported complete) and
      .claude/agents/release-validator.md (new paragraph immediately after
      the existing cross-turn-notification block, alongside the existing
      scripts/test_parallel.sh re-run instruction, run before a
      requirement/stage is reported done). Both additions independently
      verified against the actual mix task
      (lib/mix/tasks/letflow.check.test.ex, read in full) and against all 6
      of REQ-213's own acceptance criteria; `git diff` on both files showed
      only the intended localized insertion, no unrelated restructuring.
      Zero rework rounds -- CODE-DESIGN-VALIDATOR, REVIEWER and
      RELEASE-VALIDATOR all passed clean on first attempt. Lighter pipeline
      used deliberately: no ELIXIR-DEV/SECURITY-REVIEWER/TEST-DESIGNER/
      TEST-DESIGN-VALIDATOR/TEST-RUNNER dispatch, since the requirement's
      own scope note confirms no lib/ or test/ code is touched. No code
      changed, so mix test/compile were not run as part of this step (not
      required per the requirement's own scope). REVIEWER's own
      non-blocking observation, carried forward here rather than silently
      dropped: REVIEWER's own instructions (.claude/agents/reviewer.md) are
      not covered by this specific fix and are a candidate future follow-on,
      not filed as an issue by this step.
      Note on CI: PR #795 (this requirement's own PR) hit the documented
      wasm_hang isolated-subprocess flake (ISS-0352) 3 times on its
      pull_request-triggered CI run despite touching zero WASM/Engine files
      -- a sibling session's unrelated feature/WF02-REQ209-20260902 branch
      hit the identical failure pattern in the same time window, confirming
      CI infrastructure contention rather than a defect in either PR. Merged
      via admin override after structural diff attribution (git diff
      confirms zero overlap with any wasm_hang-tagged test file) and a
      parallel push-triggered CI run on the identical commit passing clean.

  - req: REQ-209
    event: done
    agent: DOC-UPDATER
    at: "2026-09-02T03:45:16Z"
    note: >
      WF02-REQ209-20260902 done (queue task 386, GH#758, stage S7).
      RELEASE-VALIDATOR independently re-verified all 6 acceptance criteria
      PASS (full suite 3051/3051, mix compile --warnings-as-errors clean),
      including a byte-diff of the ported 15-entry corpus against real
      R-Co source and independent manual re-derivation of 5 entries'
      expected results. Ports R-Co's tests/differential/corpus/conditions_v1.json
      (15 entries) into test/fixtures/simulation/differential_corpus.json
      as a regression/golden-value suite for Letflow.Engine.Expr, not a
      differential one -- Letflow has no second (CEL) implementation to
      diff against, per expr.ex's own EXP-102/AC11 moduledoc section.
      One property-style ExUnit test (Enum.reduce/3 loop) iterates all 15
      entries via translate_cel_to_expr/1 + evaluate; 0 EXPECTED_UNSUPPORTED
      dispositions (all 15 evaluated-and-matched). No change to expr.ex's
      grammar or supported-builtin set; test-infrastructure-only diff
      (new fixture, new test file, design doc, spec, status/handoff
      bookkeeping -- zero lib/ application code touched).
      docs/migration/stage-7-simulation-uat-parity.md checked: it
      references REQ-209 once (line 73), as prose explaining why the
      corpus is a regression port rather than a differential one -- an
      explanatory note, not a per-requirement status line item, so no
      edit was warranted there.

  - req: REQ-210
    event: done
    agent: DOC-UPDATER
    at: "2026-09-02T09:43:51Z"
    note: >
      WF02-REQ210-20260902 done (queue task 387, GH#759, stage S7). Closes
      S7's initial 6-requirement batch (REQ-205..REQ-210). Applied
      CODE-DESIGNER's already-validated design
      (lib/letflow/design/req210-s7-parity-report.md, passed CODE-DESIGN-
      VALIDATOR/REVIEWER/RELEASE-VALIDATOR clean) to
      docs/migration/stage-7-simulation-uat-parity.md: (1) REPLACED the
      REVIEWER sign-off section's placeholder with a dated entry (verified
      via git diff --numstat: 98 insertions / 2 deletions on this file,
      confirming genuine replacement of the 2-line placeholder, not an
      append-alongside); (2) added the 18-file
      tests/simulation/scenarios/platform/ corpus as an explicit
      out-of-scope/future-follow-up note in the Scope section. The 26-item
      aggregate table (AC1) and finding-to-issue_ref confirmation table
      (AC2) live in the design artefact itself
      (lib/letflow/design/req210-s7-parity-report.md sections 1-2) per
      this project's own established convention for report-only
      requirements (REQ-164's decision content likewise lives in its own
      lib/letflow/design/ artefact, not a separate docs/status/ or
      test/reports/ file) -- no new file created, since the pre-validated
      design doc already IS the deliverable content.
      Aggregate findings: 4 real Engine defects found and resolved during
      this batch (ISS-0392, ISS-0396, ISS-0397, ISS-0398). 2 Meridian
      scenarios (loan-origination above/below threshold) and 1 SwiftRoute
      scenario (attach-delivery-note) have fixed blockers but no confirmed
      re-run yet -- a real, disclosed verification gap, stated explicitly
      in the new sign-off entry rather than glossed over. ISS-0389
      (advance-timer endpoint) remains open, blocking 2 tenant batches
      (SwiftRoute step 2 SKIP fallback; Meridian BaFin step 4 hard BLOCKED).
      The document-attachment subsystem gap (ISS-0390) is fully closed via
      REQ-211/REQ-212 (both merged). The platform/ corpus (18 files) is
      named as an explicit, deliberate future follow-up in both the Scope
      section and the sign-off entry -- not silently dropped, no
      requirement number assigned yet.
      The gate-status assessment in the new sign-off entry ("Correctness-
      gate assessment") is stated as a factual report only, explicitly
      deferring the actual stage-gate determination to
      docs/agents/ORCHESTRATOR.md §8 / ORCH+RELEASE-VALIDATOR -- DOC-UPDATER
      does not declare S7's gate cleared or blocked here; ORCH still needs
      to weigh this report and decide next.
      docs/migration/README.md checked for a stage-tracking table needing
      an S7 count/status update: none exists (README.md's own migration-
      status pointer and docs/migration/README.md's own stage breakdown
      contain no per-requirement-count tracking table to update -- S7's
      status lives in stage-7-simulation-uat-parity.md itself, which this
      entry already updates). No mix test/compile run: no lib/ or test/
      code touched by this step (docs-only), matching REQ-210's own scope
      declaration.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-02T10:58:39Z"
    note: >
      WF03-ISS0408-20260902 done (queue task 408, GH#800). Fixed
      Letflow.Engine.Transition.fire_join/5 minting a synthetic non-UUID
      token id for a freshly-joined PARALLEL_GATEWAY branch with no
      persisted TokenRecord row, which TaskActivation.cast_token_record_id/1
      correctly rejected -- blocking every join->HUMAN_TASK (and
      TIMER-path-join) continuation scenario. Root cause traced (ISSUE-FIXER,
      live repro against Postgres) one layer up from the symptom: the
      complete_task/3 and timer_fired hop-chains had no insert step to
      persist a real TokenRecord for a hop-chain-new join token before
      task-activation/reconciliation ran against it. Fix (CODE-DESIGNER
      design, CODE-DESIGN-VALIDATOR PASS) lives entirely in lib/letflow/engine.ex
      -- inserts real TokenRecord rows for hop-chain-new tokens before those
      two steps run, at both call sites, leaving
      Transition.fire_join/5/TaskActivation.cast_token_record_id/1/
      do_reconcile_token_records/5 unmodified. Surfaced a latent NOT NULL
      mismatch on tokens.branch_id (join-merged tokens legitimately carry
      branch_id: nil per REQ-051, never previously exercised) -- additive
      migration 20260902000001_make_tokens_branch_id_nullable.exs, correctly
      justified as a necessary consequence rather than scope creep (REVIEWER
      confirmed). SECURITY-REVIEWER PASS (tenant-prefix threading, migration
      safety, reconciliation-guard widening all verified against actual
      code). REVIEWER PASS conditional on one one-line error-wrap fix
      (applied, commit 74aba472). Regression test
      test/letflow/iss0408_join_token_record_test.exs covers both call
      sites, fail-then-pass independently reproduced by
      TEST-DESIGN-VALIDATOR against pre-fix commit 751d5806 (exact
      {:invalid_token_record_id, ...} error quoted both times). TEST-RUNNER:
      3054/3058 -> 3057/3058 post-rebase, 0 failures attributable to this
      branch (one pre-existing ServiceCatalogTest flake filed separately as
      ISS-0409). PR #804 merged (squash 1b41d801). GitHub issue #800 closed
      with resolving-evidence comment. Queue task 408 released status: done.
      Local docs/issues/ISS-0408.yaml status: resolved, affected_files
      corrected to match the actual diff (original filing's transition.ex/
      task_activation.ex guess was wrong -- diagnosis found the real defect
      site one layer up) with affected_files_note explaining why.

      Also filed ISS-0409 (queue task 409, GH#805) for a pre-existing,
      unrelated flake surfaced during this run's own Step Final mix test
      runs: ServiceCatalogTest's REQ-192 pagination test fails in the full
      suite but passes in isolation. Not blocking (structurally attributed
      as pre-existing, files untouched by this branch), filed rather than
      fixed per ISSUE_QUEUE.md scope discipline -- this run's job was
      ISS-0408, not a second incidental defect.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-02T12:54:21Z"
    note: >
      WF03-ISS0404-20260902: fixed ISS-0404 (test/letflow/scheduler_req188_test.exs
      pinned a fixed historical git-commit-range, 746a3ac0..77637268, same
      fragility class as ISS-0378). Full WF-03 pipeline run: ISSUE-FIXER
      diagnosed root cause as an inherently one-time historical fact already
      permanently discharged at REQ-188's own merge, verdict DELETE (not
      structural replacement -- the only true structural alternative was
      redundant with transition.ex's own moduledoc purity check). CODE-DESIGNER
      -> CODE-DESIGN-VALIDATOR PASS. ELIXIR-DEV implemented (deleted the test
      block, updated test/specs/REQ-188.md, appended a note to
      docs/anti-patterns.md; lib/letflow/engine/transition.ex untouched) ->
      SECURITY-REVIEWER PASS (no tenant-data path) -> REVIEWER PASS (no scope
      creep, no lost AC coverage). TEST-DESIGNER produced a
      structural-absence-plus-green-suite proof (fail-then-pass does not apply
      to a pure deletion) -> TEST-DESIGN-VALIDATOR PASS (independently
      reproduced, including extracting and running the pre-fix test against
      post-fix code) -> TEST-RUNNER full suite 3054/3054 clean. Filed two
      incidental findings as new issues, not blocking: ISS-0412 (queue task
      412, GH#808, a flaky lint_handoffs.ex DateTime.compare/2 crash under
      concurrent test runs) and ISS-0413 (queue task 413, GH#809, two other
      tests using a live/resolved git ref -- a lesser, related fragility).
      ISS-0404 closed status: resolved; GitHub issue #793 closed with
      resolving-evidence comment. Queue task 404 released status: done. PR
      #810 CI: Frontend gate PASS; Backend gate main suite PASS, isolated
      wasm_hang subprocess failed 2/7 (host_api_write_test.exs,
      call_timeout_test.exs) -- structurally attributed (route 1, per
      core-directives.md) as the pre-existing, already-tracked GH#797/ISS-0406
      flake: zero overlap between the failing files/WASM runtime code and this
      branch's own diff (git diff --name-only origin/main...HEAD). PR #810
      merged via squash (commit a09f15d0). Worktree-variant Step Final: GitHub
      merge succeeded, local checkout side effect errored as documented
      (main already checked out in primary worktree) -- verified via
      gh pr view showing state MERGED before treating as PASS, then deleted
      both branch refs explicitly and detached HEAD at fetched origin/main
      per GIT_MERGE.md's worktree variant. Final verification: HEAD ==
      origin/main (a09f15d0), git log shows the squash commit, clean working
      tree.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-02T15:44:18Z"
    note: >
      WF03-ISS0419-20260902 done (queue task 419, GH#817). Investigated
      whether `mix deps.get` silently defeats CI's actions/cache for
      wasmex's Rust NIF Cargo build output (deps/wasmex/native/wasmex/target/)
      on every backend-gate CI run, even on a cache hit -- closed as
      status: no_defect, not resolved: no code file was changed, only
      docs/issues/ISS-0419.yaml. ISSUE-FIXER (Step 1) inspected 4 real CI
      runs today (ids 33634490464, 33635645070, 33622754837, 33586549969)
      via `gh run view --job=<id> --log` against genuine `Cache hit for:
      Linux-mix-<hash>` restores: `mix deps.get` completed in ~1s every
      time and the subsequent backend-gate step's compile output contained
      zero Cargo/rustc invocation lines in any of them -- only the Elixir
      compile. Traced this to Rustler.Compiler.compile_crate/3's own
      structural no-op path (do_compile/2 only runs when expected NIF
      output files are missing from priv_dir, which a cache restore already
      satisfies) -- confirmed by direct source read of
      deps/rustler/lib/rustler/compiler.ex. What actually dominates the
      ~12-15 min runtime: the ExUnit suite itself (~694s of ~720s total in
      the cited run), not any wasmex rebuild. Also reconciled an apparent
      premise conflict: `CARGO_INCREMENTAL: 0` is genuinely absent from
      .github/workflows/ci.yml's literal text (ORCH's own grep was correct)
      but genuinely active on the live job's env (dtolnay/rust-toolchain@stable
      injects it into $GITHUB_ENV unconditionally) -- both true, about two
      different artifacts, and structurally irrelevant on a cache-hit run
      since Cargo is never invoked. Stated method limitation: a local
      cold-build repro was attempted but blocked by this host's Rust
      toolchain (rustup stable=1.93.1) not meeting wasmex 0.15.1's
      Cargo.lock requirement (rustc 1.94.0+, CI pins 1.97.1 via
      .tool-versions) -- real CI log evidence was used instead and judged
      more directly probative anyway. docs/issues/ISS-0419.yaml ->
      status: no_defect, verdict_in_run/verdict_at set (not
      resolved_in_run/resolved_at), full evidence block per
      ISSUE_QUEUE.md's no_defect bar. GitHub issue #817 closed with the
      no_defect comment template, independently confirmed
      (state: CLOSED, closedAt 2026-09-02T13:56:44Z). PR #820 (title
      prefixed fix:, docs-only diff: docs/issues/ISS-0419.yaml plus this
      run's own handoffs/WF03-ISS0419-20260902/*.json) -- CI green (4
      checks: Backend gate x2, Frontend gate x2, both push and
      pull_request triggers), squash-merged (commit 503fc5bd), verified
      via `gh pr view --json state,mergedAt` per GIT_MERGE.md's worktree
      variant since the local checkout side-effect errored as documented
      for a secondary worktree. Remote+local branch refs deleted
      explicitly. Final HEAD verified == origin/main. Queue task 419
      released status: done.

  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-02T17:01:13Z"
    note: >
      ISS-0411 (queue task 411, GH#807) picked up for WF-03 processing.
      ISSUE-FIXER's diagnosis (already recorded in docs/issues/ISS-0411.yaml
      by a prior session) concluded this is NOT a scoped WF-03 bug fix:
      Letflow.Engine.Transition.dispatch_node/4 has no SERVICE_TASK dispatch
      handler, a real gap needing a new supervised dispatch-orchestration
      process, an HTTP transport, a service_catalog lookup, and
      SECURITY-REVIEWER sign-off on SSRF/tenant-scoping -- multi-file,
      new-architecture, tenant-data-path work, not a bounded fix. No
      implementation attempted in this run. Two actions taken instead: (1)
      corrected a stale causal attribution in
      docs/migration/stage-7-simulation-uat-parity.md's REQ-210 sign-off
      entry (it blamed the Meridian quorum/disbursement paths' unreached
      status on the now-resolved join_counters defect; ISS-0411 established
      the real, separate cause is this SERVICE_TASK gap) -- taken as a
      direct ORCH action per ORCHESTRATOR.md Sec 10 (single-purpose doc
      fix, all six sizing checks pass) rather than a full workflow; (2)
      dispatched REQ-ANALYST to draft the real gap as new requirement(s) --
      found it was already fully covered by REQ-214 (dispatch orchestration
      core, HTTP transport, SSRF gate, service_catalog stub) and REQ-215
      (wire SERVICE_TASK into dispatch_node/4, async-park pattern), both
      status: pending with real impl_order (415/417) already registered in
      letflow-queue, so no duplicate requirements were written. Also
      confirmed ISS-0104 (flagged as a likely co-requisite in ISS-0411's
      diagnosis) is status: resolved and no longer a live blocker. PR #821
      (docs-only diff: docs/issues/ISS-0411.yaml routing note plus the
      stage-7 doc correction) -- backend-gate CI hit the known wasm_hang
      isolated-test-group timing flake twice (different individual tests
      each time, both unrelated to this PR's diff; main 3060-test suite
      passed clean on every run) before a third run passed clean; squash-
      merged (commit 7250b72a). docs/issues/ISS-0411.yaml left at
      status: open deliberately -- the underlying gap is not resolved, only
      routed; closing status: done on the queue task would have
      best-effort closed GH#807 and misrepresented an unresolved gap as
      fixed (the exact ISS-0277 failure mode). Queue task 411 released
      WITHOUT a status (stays open, immediately reclaimable) so it can be
      picked up again once REQ-214/REQ-215 land.
  - req: REQ-216
    event: started
    agent: ORCH
    at: "2026-09-02T22:45:00Z"
    note: >
      Admission-control core (closes ISS-0431 part 1). Queue task 434
      (GH#839) locked. Branch feature/WF02-REQ216-20260902 created from
      main. Dispatching Step 1 (CODE-DESIGNER).

  - req: REQ-216
    event: done
    agent: DOC-UPDATER
    at: "2026-09-03T01:28:40Z"
    note: >
      WF02-REQ216-20260902 done (queue task 434, GH#839, stage S6).
      RELEASE-VALIDATOR independently re-verified all 7 acceptance criteria
      PASS (full suite 3074/3076 via mix letflow.check.test, 2 failures
      structurally attributed to a pre-existing environmental rustc-ENOENT
      gap unrelated to this branch; mix compile --warnings-as-errors --force
      clean). Ships new module Letflow.Admission (lib/letflow/admission.ex),
      a hand-rolled pure-BEAM GenServer implementing a global + per-tenant
      counting semaphore tied to config/runtime.exs's POOL_SIZE
      (global_cap = pool_size - reserved_headroom, per-tenant cap = equal
      fair-share split with a floor of 1, both gates checked on every
      tenant acquisition), added to Letflow.Application's supervision tree
      (lib/letflow/application.ex line 65, moduledoc states no start-order
      dependency). Core module only -- no HTTP-path or Poller wiring in
      this requirement; REQ-217 (HTTP entry-path wiring, queue task 435,
      GH#840) and REQ-218 (Poller wiring, queue task 436, GH#841) remain
      pending. Closes ISS-0431/GH#835 part 1 of 3 only -- ISS-0431 itself
      stays status: open until REQ-217/REQ-218 also land, per its own
      orch_routing_note (verified unchanged and still accurate: no update
      needed there, since both dependent requirements are still pending).
      Path: 1 CODE-DESIGN-VALIDATOR rework round (step-01c/01d) and 1
      TEST-DESIGNER rework round (step-06/07) before both passed clean.
      docs/migration/stage-6-operational-cross-cutting.md and
      docs/agents/AGENT_SYSTEM.md checked directly (grep for "ISS-0431" and
      "admission"): neither references the admission-control gap, so
      neither needed a staleness update from this step.

  - req: REQ-217
    event: started
    agent: ORCH
    at: "2026-09-03T02:45:00Z"
    note: >
      Wire admission control into the HTTP entry path (closes ISS-0431
      part 2, primary surface). Queue task 435 (GH#840) locked, unblocked
      by REQ-216's merge. Branch feature/WF02-REQ217-20260903 created
      from main. Dispatching Step 1 (CODE-DESIGNER).

  - req: REQ-214
    event: blocked
    agent: DOC-UPDATER
    at: "2026-09-03T04:09:09Z"
    note: >
      WF02-REQ214-20260902 Step 6 (DOC-UPDATER) run, NOT flipping REQ-214 to
      done. Independently re-read docs/requirements.yaml's REQ-214 entry (9
      real acceptance criteria) and RELEASE-VALIDATOR's own handoff
      (handoffs/WF02-REQ214-20260902/step-05-release-validator.json,
      result.status: PARTIAL): 8/9 ACs (AC1-AC7, AC9) independently confirmed
      PASS by RELEASE-VALIDATOR's own re-derivation. AC8 ("SECURITY-REVIEWER's
      sign-off is recorded") is now genuinely satisfied -- this step
      independently confirmed handoffs/WF02-REQ214-20260902/step-02c-security-reviewer.json
      (result.status FAILED, a real BLOCKER on first pass) plus
      -recheck1.json (FAIL->PASS) and -recheck2.json (COMPLETED/PASS) exist
      on disk, cross-referenced against real inline code comments crediting
      "SECURITY-REVIEWER" by name at 5 sites in
      lib/letflow/engine/service_task_dispatcher.ex, and confirmed
      `mix letflow.lint_handoffs` reports 0 new violations (1852 handoff
      files, 25 pre-existing grandfathered per ISS-0190) -- so AC8's own
      backfill (this same run's 21 handoffs/WF02-REQ214-20260902/*.json
      files plus the matching handoffs/registry.json entry) is verified, not
      merely claimed.

      BUT RELEASE-VALIDATOR's own handoff also states explicitly, in its own
      next_action field: "do not flip REQ-214 to done until a real commit
      exists on the branch" -- because at that step's real execution time
      the entire implementation sat uncommitted on
      feature/WF02-REQ214-20260902 (0 commits ahead of main). Independently
      re-verified RIGHT NOW, not inherited from that record: `git branch
      --show-current` = feature/WF02-REQ214-20260902; `git log main..HEAD
      --oneline` = 0 commits; `git log --all --oneline --grep="REQ-214"` =
      0 matches on any branch; `git status --short` shows
      service_task_dispatcher.ex, poller.ex, the migration, the test file,
      and test/reports/report-2026-09-03-REQ214.yaml all still untracked or
      modified-uncommitted. handoffs/orchestrator.log's tail (through
      2026-09-02T16:50:00Z) has no DISPATCH/STEP_COMPLETE/RUN_DONE line for
      WF02-REQ214-20260902 at all. This is a real, currently-true blocker,
      not a stale claim -- so per core-directives.md's override rule ("no
      handoff can authorize reporting unverified work as done"), REQ-214's
      status in docs/requirements.yaml is LEFT AS pending, unflipped, by
      this step. Committing the implementation is Step Final's job (same
      agent as Step 00 -- ELIXIR-DEV, via docs/agents/protocols/GIT_MERGE.md),
      not DOC-UPDATER's to perform. Next action: dispatch ELIXIR-DEV to
      commit the working-tree implementation on
      feature/WF02-REQ214-20260902, push, open the PR, and get CI green --
      only THEN should DOC-UPDATER be re-dispatched to flip REQ-214 to done
      and log a `done` event superseding this `blocked` one. REQ-215 (the
      engine-wiring half, ISS-0411/GH#807's other part) remains separately
      pending regardless. docs/migration/stage-3-instance-engine.md checked
      for a needed update: its line 234 already names "the real outbound
      HTTP transport for SERVICE_TASK dispatch (REQ-056 leaves it
      injectable)" as a deliberately deferred gap, and line 347 states S3
      reviews are reconciled at the WF-04 stage gate, not incrementally per
      requirement -- so no edit is warranted there, especially with REQ-215
      still open and S3's own gate not yet re-run.

  - req: REQ-214
    event: revised
    agent: ORCH
    at: "2026-09-03T04:14:03Z"
    note: >
      Supersedes the `blocked` entry immediately above: the real commit
      DOC-UPDATER's own next_action asked for now exists. Committed
      REQ-214's full implementation on feature/WF02-REQ214-20260902
      (commit 3d1698b5, `git log main..HEAD --oneline` = 1 commit).
      Rebased onto origin/main during Step Final, resolving a real
      4-file conflict: docs/status/requirement_status.index.yaml,
      docs/status/requirement_status.v8.yaml (this file, both
      append-order conflicts against REQ-216's own concurrently-landed
      entries), handoffs/registry.json (same), and
      lib/letflow/application.ex (a real supervision-tree conflict
      against REQ-216's own Letflow.Admission entry and ISS-0429's
      Letflow.Obs.Alerts.TaskSupervisor entry, both landed on
      origin/main while this branch was in flight -- resolved by
      appending service_task_dispatcher_children() after
      scheduler_children(), preserving ISS-0429's own documented
      ordering constraint that Alerts.TaskSupervisor must precede
      scheduler_children(), which REQ-214's own addition has no
      ordering dependency against). Also required `mix deps.get` +
      recompile against ISS-0420's own mix.lock bumps (oidcc, bandit)
      that landed on origin/main in the same window. `mix compile
      --warnings-as-errors` clean post-rebase. Still on
      feature/WF02-REQ214-20260902, not yet merged -- Step Final's
      push/PR/CI/merge steps are next.

  - req: REQ-214
    event: done
    agent: ORCH
    at: "2026-09-03T04:57:32Z"
    note: >
      Supersedes the `blocked` and `revised` entries above for the same
      requirement. PR #847 squash-merged to main (81e2dea8) after rebase
      resolved a real 4-file conflict (this file, the index,
      handoffs/registry.json, and a genuine supervision-tree conflict in
      lib/letflow/application.ex against REQ-216's Letflow.Admission and
      ISS-0429's Letflow.Obs.Alerts.TaskSupervisor entries, both landed
      on main while this branch was in flight -- resolved preserving both
      entries' own ordering constraints). CI's pull_request-triggered run
      passed clean (main suite + isolated wasm_hang subset both green);
      the separate push-triggered run on the identical commit hit the
      documented wasm_hang isolated-subprocess flake twice in a row (main
      suite genuinely clean both times, 3118/3118; only the isolated
      `--only wasm_hang` subset failed, 5/7 then 4/7) -- confirmed
      structurally unrelated (git diff shows zero overlap with any
      wasm/plugin_handler file) and merged via admin override after that
      double confirmation, per the same precedent already established
      earlier today for PRs #795/#802. docs/requirements.yaml's REQ-214
      status flipped pending -> done in this same step. Queue task 415
      released status: done (GH#812 best-effort closed). REQ-215 (the
      engine-wiring half, ISS-0411/GH#807's other part) remains
      separately pending.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-03T06:47:02Z"
    note: >
      WF03-ISS0426-20260902 complete. Lua executor wall-clock tests flaked
      under CPU contention: a test doing 0.3s of real work hit its 5000ms
      budget when the suite ran 6-way parallel on 16 cores, because
      execute_with_manifest/3 races Task.yield against wall-clock time and so
      measures SCHEDULER AVAILABILITY, not Lua execution time.
      run_with_heap_limit/5 carried the same race hand-rolled via
      :erlang.spawn_opt/2 + a bounded receive...after -- a second exposure the
      design gate caught before it shipped. Fix: two additive @doc false
      test-only seams (run_script_sync/3, run_with_heap_limit_sync/4) that 14
      Group 1 call sites repoint onto, making {:error, {:wallclock_timeout,
      _}} unreachable BY CONSTRUCTION; the 11 tests where the race genuinely
      IS the property under test keep racing, tagged :lua_wallclock_race and
      isolated into their own subprocess mirroring the :wasm_hang precedent.
      AC4 held: executor.ex is 113 insertions / 0 DELETIONS, so REQ-155's
      LUA-10 wall-clock kill (a security control against untrusted tenant
      scripts) is byte-for-byte unchanged and config/config.exs:17 untouched.
      Gates: CODE-DESIGN-VALIDATOR FAILED TWICE before passing (first on the
      run_with_heap_limit/5 gap, then on the new seam's unbounded receive
      having no binding usage contract), SECURITY-REVIEWER, REVIEWER,
      TEST-DESIGN-VALIDATOR (independently reproduced a mutant, matching both
      the count and the exact failing-test set), TEST-RUNNER -- all PASS.
      Fail-first satisfied by MUTATION per WF-03's rule for new code: 4
      mutants each produced a DISCRIMINATING failure set (4/45, 4/45, 12/45,
      1/56). Verification: full serial mix test 3057 passed / 0 failures; AC6's
      two TEST_PARALLEL_N=6 scripts/test_parallel.sh runs both clean at
      3054/3054. HONEST LIMITATION, recorded in the issue, the PR and the
      GH#830 close comment alike: the original flake was NEVER reproduced on
      demand (ISSUE-FIXER could not trigger it under 24 CPU burners plus 48
      concentrated runs), so the fix rests on STRUCTURAL verification with the
      parallel runs as confirmatory only -- not on reproduce-then-eliminate.
      Scope coordination: the wasm half of the filed scope was deliberately
      left to the live concurrent run WF03-ISS0418-20260902, which owns
      lib/letflow/engine/wasm and the three wasm test files; this run
      enumerated but did not edit them, and its one shared-file change
      (letflow.check.test.ex) is 37 insertions / 0 deletions with zero
      existing lines touched. PR #848 squash-merged to main (370fd93f) via
      admin override after the isolated --only wasm_hang subprocess failed on
      plugin_handler_test.exs:355 (REQ-170 AC1, 30166ms vs a 500ms budget) --
      the documented ISS-0352 recurrence class that ISS-0418 exists to fix,
      structurally unrelated (this branch touches NO wasm path at all) and
      confirmed flaky by the identical commit passing in the parallel CI run;
      same precedent as REQ-214/PRs #795/#802 earlier. Queue task 426 released
      status: done, GH#830 closed with an evidence comment. Also filed
      ISS-0440 (task 440, GH#849) for the 7th occurrence of the handoff
      top-level-status enum violation, which this run's own first CI run
      surfaced -- filed as structural work because docs/anti-patterns.md
      declares re-documenting that class exhausted.

  - req: REQ-217
    event: done
    agent: DOC-UPDATER
    at: "2026-09-03T06:50:13Z"
    note: >
      WF02-REQ217-20260903 done (queue task 435, GH#840, stage S6).
      RELEASE-VALIDATOR independently re-verified all 7 acceptance criteria
      PASS (scripts/test_parallel.sh 3091/3093, mix letflow.check.test
      3094/3096, both failure sets isolated to the pre-existing
      environmental rustc-ENOENT gap in Mix.Tasks.Letflow.CheckToolchainTest,
      structurally unrelated to this branch; mix compile
      --warnings-as-errors --force clean). Wires two admission-control gates
      into the HTTP entry path atop REQ-216's core module:
      lib/letflow/plugs/admission.ex (new Letflow.Plugs.Admission plug,
      mounted twice with pool: :global and pool: :tenant) and
      lib/letflow/plugs/api_pipeline.ex (mount points and chain ordering).
      Global gate mounted first, before Plug.Parsers, covering
      AuthPipeline's own DB work as a disclosed limitation (tenant identity
      does not exist yet at that point); per-tenant gate mounted after
      AuthPipeline and before TenantStatus. Both reject with
      Letflow.Api.Response.service_unavailable/2 (REQ-066, no new response
      helper) plus an explicit Retry-After header (fixed 1s default,
      independently justified against TenantStatus's 30s precedent).
      Dual crash-safety mechanism: Mechanism A (conn.assigns +
      Plug.Conn.register_before_send/2) covers every non-crashing response
      path and a route-handler raise (wrapped in Plug.Conn.WrapperError);
      Mechanism B (process-dictionary ref list + Plug.ErrorHandler's
      handle_errors/2) covers a raise inside a plug running before
      :dispatch, where conn.assigns is not a reliable read channel. Path:
      2 CODE-DESIGN-VALIDATOR rework cycles (step-01b/01c: a crash-safety
      trace correction to the design's stated mechanism; step-01d/01e/01f:
      SECURITY-REVIEWER found a real double-global-consumption defect in
      the first implementation -- the tenant gate's own try_acquire also
      consumes a global unit per Letflow.Admission's composing rule, so the
      original wiring stacked a second global unit on top of an
      already-held first -- fixed via :release_global_admission, mounted
      between AuthPipeline and the tenant gate, releasing the global gate's
      already-held ref immediately so admission at the global gate becomes
      a re-checked, not reserved, precondition for admission at the tenant
      gate). Both moduledocs (admission.ex, api_pipeline.ex) spot-checked
      against the actual shipped code and found accurate/complete, no edit
      needed. docs/migration/stage-6-operational-cross-cutting.md grepped
      for REQ-216/REQ-217/ISS-0431/admission: zero references, so no
      staleness update was warranted there. docs/issues/ISS-0431.yaml's
      orch_routing_note checked: it does not assert all three requirements
      are pending (it states the record "stays open ... pending those
      three requirements' completion", which remains literally true with
      REQ-218 still pending) -- left unedited rather than rewritten for
      a false problem. Closes ISS-0431 part 2 of 3 (primary HTTP surface);
      REQ-218 (Poller wiring, queue task 436, GH#841) remains pending as
      part 3.
