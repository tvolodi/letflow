# Letflow — Requirement run history — VOLUME 9 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v8.yaml (volume 8, closed,
#   1,213 frozen-prefix lines / 1,242 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v9.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v9.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v9.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v9.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T05:42:37Z"
    note: >
      ISS-0451 resolved (WF03-ISS0451-20260904, queue task 451, GH#865). A
      persistent (never-clearing) Poller crash-loop could exhaust the
      top-level Letflow.Supervisor's own default OTP restart budget (3/5s)
      after Letflow.Supervisor.Pollers exhausted its own (5/60s), cascading
      past the isolation REQ-219 built -- found by REVIEWER during REQ-219's
      own AC4 gating. Fix: Pollers' child spec is now
      Supervisor.child_spec(Letflow.Supervisor.Pollers, restart: :temporary)
      in lib/letflow/application.ex, so the top level structurally can never
      auto-restart it (proven, not bounded -- 0 restarts consumed from its
      budget for any fault duration); a new Letflow.Supervisor.PollersBreaker
      GenServer (lib/letflow/supervisor/pollers_breaker.ex) becomes sole
      restart authority via Supervisor.start_child/2, tripping to a stopped
      :open state after 2 consecutive exits with exponential backoff.
      Top-level max_restarts raised 3->5 as defense-in-depth for the
      remaining :permanent children. Design went through 3 rework cycles
      (2 real Elixir Supervisor-semantics bugs caught by CODE-DESIGN-VALIDATOR
      -- an unproven termination-vs-auto-restart race, then a children-list
      syntax defect that silently failed to apply :temporary -- plus a real
      race condition between PollersBreaker and the existing AC4 test's own
      restart call, found by ELIXIR-DEV during implementation and resolved by
      redesigning the test as a single breaker-mediated flow rather than
      touching production code). SECURITY-REVIEWER and REVIEWER both PASSED.
      New fail-then-pass mutation-tested regression test
      (test/letflow/supervisor/pollers_breaker_test.exs), independently
      re-verified by TEST-DESIGN-VALIDATOR (one mutant reproduced exactly).
      Full suite green: mix letflow.check.test 3203+7+11 passed, 0 failures.
      PR #874 merged (squash commit 77fe6bf4) after one structurally-cleared
      CI flake (wasm_hang timing class, zero diff overlap, identical commit
      passed the concurrent job) -- filed as ISS-0456 (queue task 456, GH#875)
      rather than left undocumented. docs/issues/ISS-0451.yaml: status:
      resolved. GitHub issue #865 closed with mandated evidence comment.
      Also released a stale WF03-ISS0425-20260903 registry lock (no live
      branch/session behind it, its own diagnosis had already been routed
      into and completed by REQ-219/REQ-220) that was blocking this run's
      owned_modules claim on lib/letflow/application.ex.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T06:22:22Z"
    note: >
      WF03-ISS0456-20260904: ISS-0456 (wasm_hang timing flake recurrence,
      PR #874 push-event backend gate, queue task 456, GH#875) diagnosed and
      folded into ISS-0418 -- NOT independently resolved. ISSUE-FIXER
      independently verified, by direct CI log inspection (gh run view
      --log-failed job 100920721979), that this recurrence's failure
      signature (plugin_handler_test.exs:474, host_api_write_test.exs:460) is
      byte-for-byte identical in file/line/timeout-bound/stack-shape to
      ISS-0418's own recurrence_2026_09_03 entry -- not merely accepted from
      the filing text, per HANDOFF_PROTOCOL.md's "a handoff's factual
      premises are checkable" rule applied to this issue's own filing.
      Independently re-checked PR #874's zero-touch claim on wasm/lua paths
      via gh pr view --json files. Cross-checked ISS-0453 and confirmed no
      match (different subsystem -- Letflow.Scheduler.Admission, main suite
      not the isolated wasm_hang stage). No new root cause found; no fix
      designed or applied for ISS-0456 itself. docs/issues/ISS-0456.yaml ->
      status: instrumented, superseded_by: ISS-0418. docs/issues/ISS-0418.yaml
      -> new recurrence_2026_09_04 field, tenth-plus hit; its own OQ-5
      operator-configurable concurrency cap remains the correct, still
      undesigned, structural fix for this whole class. Ran through the full
      git-setup/PR/CI/merge pipeline for the docs-only fold-in (PR #876,
      merged as fast-forward 023c4e17) -- both backend-gate CI runs passed
      cleanly, no wasm_hang flake recurred on this PR's own CI. GH#875 closed
      with mandated evidence comment (never claims a fix). Queue task 456
      released WITHOUT status (stays open, unlocked) per TASK_QUEUE.md's rule
      that status:"done" would falsely assert the underlying defect is fixed.
  - req: REQ-220
    event: done
    agent: ORCH
    at: "2026-09-04T08:22:00Z"
    note: >
      Direct ORCH action (ORCHESTRATOR.md S10: single file, no new
      function/module, no migration, no supervision-tree structural
      change -- keeps all 7 Task.Supervisors -- no tenant-data path, no
      test-asserted-behavior change; all six checks passed). Closes
      ISS-0425 part 3. Added a consolidated "Task.Supervisor sprawl
      review" section to lib/letflow/supervisor/infrastructure.ex's
      moduledoc, naming all 7 Task.Supervisors with their originating
      requirement/issue and distinct justification (already fully
      elaborated in REQ-220's own requirement text -- this step
      transcribed it into the module, not re-derived it), and explicitly
      considering and rejecting the two consolidation candidates named in
      the requirement's own ACs (three Wasm supervisors; Plugin+Lua).
      Real recommendation: KEEP ALL 7 SEPARATE, no Task.Supervisor
      removed/renamed/merged. mix compile --warnings-as-errors clean;
      mix format --check-formatted clean; test/letflow/supervisor/ 11/11
      passed; full suite (mix letflow.check.test) 3206/3208 passed, 2
      pre-existing rustc-toolchain-gap failures (Mix.Tasks.Letflow.
      CheckToolchainTest, rustc absent from this environment).
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T14:10:00Z"
    note: >
      WF03-ISS0421-20260904 done (queue task 421, GH#825, stage S6).
      Scheduler.Poller's 7 serial per-tenant sweeps bounded with
      Task.async_stream, max_concurrency derived live from two new
      Letflow.Admission accessors (global_cap/1, reserved_headroom/1)
      rather than a hardcoded literal -- both mirror the existing
      try_acquire/2/release/2 idiom, purely additive to Admission's
      state. Closes ISS-0421's remaining concern (unbounded serial-sweep
      latency); concerns 2/3 (platform-wide SPOF, default restart
      intensity) were already resolved by REQ-219/ISS-0451. 3 total
      rework cycles across design (2) and implementation-review (1),
      each catching a real correctness gap (a lockstep-config hazard, a
      phantom state-field read, a hardcoded concurrency bound that broke
      REQ-218's own tests under a tight pool config). Full suite
      3212/3214, 2 pre-existing rustc-toolchain-gap failures. See
      docs/issues/ISS-0421.yaml for full resolution detail.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T15:27:08Z"
    note: >
      WF03-ISS0459-20260904 resolved (queue task 459, GH#884, stage S6).
      letflow-queue had no release_lock status for a WF-03 instrumented/
      no_defect terminal verdict, so a run reaching that outcome had to
      either falsely assert resolved (status:"done", which best-effort
      closes the GitHub issue) or release with no status, leaving the
      task open/unlocked and immediately re-claimed by the very next
      get_next_task call under the LIFO issue-priority order -- a live
      infinite-loop hazard that hit queue task 458/ISS-0458 today and was
      worked around ad-hoc (released status:"done" anyway, explicitly
      flagged as not-a-real-fix in that run's own record).
      ISSUE-FIXER's diagnosis (independently re-verified twice more, by
      CODE-DESIGN-VALIDATOR and again by REVIEWER, all three reading
      letflow-queue's own source directly) found letflow-queue already
      ships a third status value, "blocked" (lib/letflow_queue/tasks/
      task.ex:18), already excluded from get_next_task's claim query
      (WHERE status = 'open' only) and already a passthrough for
      tap_maybe_close_github_issue/2 (fires only on literal "done") --
      so the mechanism ISS-0459 asked for already existed, zero
      letflow-queue code change needed. CODE-DESIGN-VALIDATOR FAILed the
      first design pass on citing letflow-queue's now-retired "exactly
      four operations" framing (superseded same-day by decision 0017,
      docs/migration/decisions/0017-task-queue-selection-model.md,
      "exactly one mutating claim path") as supporting reasoning;
      CODE-DESIGNER reworked to ground the documentation-only conclusion
      on non-retired grounds (0017 governs selection/claiming, disjoint
      from release_lock's status vocabulary) and re-passed. Fix, this
      repo only (letflow-queue repo untouched): docs/agents/protocols/
      TASK_QUEUE.md gains a full release_lock(status:"blocked") section
      matching the existing done/no-status rigor; docs/agents/protocols/
      ISSUE_QUEUE.md's instrumented and no_defect bullets each gain a
      cross-reference sentence; docs/agents/workflows/
      WF-03_issue_resolving.md Step 5's previously-unconditional
      release_lock(status:"done") instruction now branches on the local
      issue status (resolved -> done; instrumented/no_defect ->
      blocked), closing the actual proximate cause of ISS-0458's
      workaround. TEST-DESIGNER correctly returned SKIPPED (no lib/ or
      priv/ diff exists for a regression test to exercise; the
      underlying mechanism is already covered by letflow-queue's own
      pre-existing tests/letflow_queue/tasks_test.exs, quoted and
      verified in this run's own handoffs). REVIEWER PASSED (idiom,
      cross-file consistency, decision-0017 re-check against the
      implemented text, not just the design). Full pipeline: mix format
      clean, mix compile --warnings-as-errors clean, mix test 3217
      passed / 0 failed / 21 excluded. PR #886 merged (squash commit
      a6902425) after one structurally-cleared wasm_hang CI flake
      (identical commit passed on the concurrent push-triggered run;
      zero lib/priv touched by this docs-only PR; already tracked under
      ISS-0418, not re-filed). docs/issues/ISS-0459.yaml: status:
      resolved. GitHub issue #884 closed with mandated evidence comment
      stating plainly no code change was needed. Queue task 459 released
      status:"done". AC3 reconciliation executed as its own follow-on
      action once the design's safety claim was independently verified
      three times over: queue task 458 re-released status:"blocked"
      (was status:"done", the ad-hoc workaround this issue was filed
      against) -- confirmed safe (only a transition TO "done" triggers
      GitHub-close, so this did not reopen or re-close GH#882, itself
      independently re-verified still CLOSED after the call) and
      confirmed GitHub-visible: GH#882's own state is unaffected by a
      transition to "blocked". No letflow-queue repo commit, no deploy
      -- this fix needed neither.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T16:40:00Z"
    note: >
      WF03-ISS0441-20260904 done (queue task 441, GH#851, stage S6).
      Real GitHub branch protection now live on main (required checks:
      Backend gate, Frontend gate; enforce_admins: false), per decision
      record docs/migration/decisions/0018-branch-protection-posture.md
      (3 rework cycles, each a real correctness catch). Live-verified via
      a real scratch PR (#887): PENDING-rejection, a real organic BEHIND
      rejection, a real FAILURE/BLOCKED rejection caused by an actual
      live wasm_hang recurrence during the verification itself, and a
      successful --admin override past that FAILURE -- full quoted
      command output in 0018's own Verification log. Corrected
      0004-humanless-pipeline.md, GIT_MERGE.md (new 3-path Step 8), and
      ci.yml (required-status-check-contract comment). See
      docs/issues/ISS-0441.yaml for full resolution detail. First
      real-world test of the new protection is this same run's own
      Step Final merge.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T19:02:00Z"
    note: >
      ORCH-direct-20260904-ISS0468 done (queue task 468, GH#898, stage
      S6). Fixed test/support/tenant_template.ex's
      assert_template_parity_against_independent_reference!/1, which
      hardcoded a fixed reference-schema name ("tenant_template_refcheck")
      colliding under concurrent invocation -- same class ISS-0427 already
      fixed for the template build itself. Replaced with
      generate_staging_schema_name(), reusing ISS-0427's own generator.
      Added a real concurrency regression test, mutation-tested (fails
      against the old literal, passes against the fix). Direct ORCH
      action (single file, test-support code, no test-asserted-behavior
      change). Full suite 3222/3224, 2 pre-existing rustc-gap failures.
      One self-inflicted false alarm along the way (a stale DB row from
      a force-killed foreground test run) diagnosed and cleaned up, not
      a regression. See docs/issues/ISS-0468.yaml for full detail.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T19:23:55Z"
    note: >
      WF03-ISS0467-20260904 resolved (queue task 467, GH#896, stage S6).
      Found live by ORCH immediately after ISS-0441/PR #895 configured
      branch protection: a bare `git push origin main` bypassed the new
      required-status-checks protection entirely, with only a quiet
      server-side "Bypassed rule violations" notice -- unlike `gh pr
      merge`'s own explicit --admin requirement, no distinct flag or
      confirmation was needed. ISSUE-FIXER's live diagnosis (gh api
      branch protection call, gh auth status/gh api user/permissions)
      confirmed GitHub's `restrictions` push-allowlist (Option 1) would
      be a structural no-op on this project: every git push, gh pr
      merge, and gh api call authenticates as the single admin identity
      tvolodi, so an allowlist naming that identity accepts the exact
      bypass it would need to reject. CODE-DESIGNER chose Option 2
      (documented prohibition) after independently checking for a
      distinguishing auth mechanism the diagnosis might have missed
      (deploy key, bot account, scoped PAT) -- none exists, and
      manufacturing one was judged out of scope for a MINOR issue. One
      rework cycle: CODE-DESIGN-VALIDATOR FAILed the first design pass
      for misattributing its acceptance-criteria mapping to a
      docs/issues/ISS-0467.yaml field that does not exist (the criteria
      actually came from the GitHub issue body / this run's own task
      framing -- an ORCH sourcing error in the original dispatch, not a
      CODE-DESIGNER defect); CODE-DESIGNER's narrow rework re-sourced
      the citation and re-passed on regate, with all 7 other
      independently-derived findings unchanged. Fix (commit bd6bddf5):
      docs/agents/protocols/GIT_MERGE.md gains an explicit
      no-direct-push-to-main-ever paragraph in its Precondition section;
      docs/agents/ORCHESTRATOR.md sec.10 gains a closing paragraph
      clarifying its sizing-rule exception licenses skipping the
      producer/validator review chain, never the branch+PR git
      mechanics; docs/migration/decisions/0018-branch-protection-posture.md
      gains a new dated "ISS-0467 resolution" subsection amending the
      record in place (0018 had already scoped and named this exact
      follow-up, so no new decisions/ record was opened). REVIEWER
      independently confirmed both insertions verbatim-match the
      approved design and that the new 0018 subsection is internally
      consistent with the existing Decision (a)-(d) section. Docs-only
      fix, no code/lib/priv/web touched -- no regression test exists;
      verification was the CODE-DESIGN-VALIDATOR and REVIEWER gates on
      the textual change itself. docs/issues/ISS-0467.yaml: status
      resolved. GitHub issue #896 closed with an evidence comment citing
      the run, the record, and the fix commit, explicitly stating no
      test exists for this docs-only change. Queue task 467 released
      status:"done". Step Final's own merge is the first real-world
      application of this run's own new prohibition -- via gh pr merge,
      never a direct push.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T22:22:00Z"
    note: >
      WF03-ISS0469-20260904 done (queue task 469, GH#904, stage S6).
      executor_test.exs:654's raw wall-clock elapsed-time comparison
      (a repeat CI flake, also seen on ISS-0446) removed as genuinely
      redundant -- the test's own existing deterministic outcome
      assertions already prove REQ-156 AC-1's load-bearing-limit claim
      without any timing measurement, per test/specs/REQ-156.md T1's
      own accepted outcome-differs alternative, verified directly by
      REVIEWER. Eliminates this flake class permanently (the deleted
      line was the only wall-clock-dependent assertion in the test).
      Full suite 3222/3224, 2 pre-existing rustc-gap failures; isolated
      :lua_wallclock_race group 11/11 unaffected. See
      docs/issues/ISS-0469.yaml for full detail.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T23:11:29Z"
    note: >
      WF03-ISS0430-20260905 resolved (queue task 430, GH#834, stage S6).
      Design-first issue: adopt Postgres LISTEN/NOTIFY as the BEAM<->Postgres
      push channel. ORCH corrected the filing's own stale premise before
      writing docs/issues/ISS-0430.yaml: REQ-214 (then "pending") had already
      shipped as Letflow.Engine.ServiceTaskDispatcher.Poller, and ISS-0425's
      own follow-on requirements REQ-219/REQ-220 (supervision-tree layering)
      had also both shipped -- reframed AC6 from a greenfield sequencing
      question to a retrofit question against the two now-existing pollers.
      ISSUE-FIXER's Step 0.5/1 confirmed no prior registry match (not a
      recurrence) and pulled exact poller mechanics (both 5000ms default
      interval, separate config-key namespaces, FOR UPDATE SKIP LOCKED
      claim queries) plus decision 0003 Dimension B's :prefix mechanism
      verbatim, flagging that Postgres NOTIFY has zero schema-scoping
      concept. CODE-DESIGNER produced lib/letflow/design/
      iss0430-notify-listen-decision.md answering all 5 named design
      questions plus the retrofit question: (1) one application-wide
      Postgrex.Notifications connection wrapped in a new
      Letflow.Notify.Listener GenServer, placed as a child of
      Letflow.Supervisor.Infrastructure (after Repo/Migrator, never inside
      Pollers -- reasoned against ISS-0429/ISS-0451's existing guarantees);
      (2) tenant isolation via a single shared channel, opaque
      never-tenant-identifying routing-id payload only, receiver always
      re-derives its own :prefix scope and discards unresolved ids -- both
      the timing-signal and sequential-id-enumeration leak vectors reasoned
      through explicitly and found acceptable/moot respectively (Letflow's
      tables use disjoint per-schema id sequences); (3) notify-then-fetch,
      ~10-36 byte payloads; (4) polling retained unconditionally at current
      5000ms for both pollers, reasoned against all three NOTIFY failure
      modes; (5) Ecto.Adapters.SQL.Sandbox shared-mode test strategy,
      concrete 6-step procedure (self-corrected a MINOR ISS-0031
      mis-citation along the way); (6) retrofit decided NOT NOW, argued
      against the round-trip evidence (36T eliminated round trips/minute,
      ~0.11ms co-located vs. the dev host's ~1.3ms/~92%-Docker-overhead
      figure) against real added complexity to two already-hardened
      pollers with no documented latency-SLA gap. All three hard gates
      independently re-derived from real source, not copied:
      CODE-DESIGN-VALIDATOR (all 8 ACs individually verdicted MET,
      supervision placement and tenant-isolation reasoning both
      independently checked against lib/letflow/application.ex and
      decision 0003); SECURITY-REVIEWER (INV-1 sign-off independently
      re-derived against real code -- lib/letflow/tenant_provisioning.ex
      and the actual timers/service_task_dispatches/events migrations --
      confirming the enumeration-risk argument is factually sound and
      actually stronger than stated, since the two cited tables use UUID
      primary keys, not sequences at all; INV-2/3/5/9 NOT-APPLICABLE,
      INV-4/6/7/8 PASS; flagged a forward obligation for whichever future
      implementation wires a real consumer to get its own INV-1 pass);
      REVIEWER (OTP-idiom soundness of the GenServer-wrapper placement,
      decision 0003/0009 consistency independently read and confirmed, the
      "not now" retrofit verdict confirmed deliberate and complete, no
      scope creep beyond ISS-0430's own 8 ACs). No ELIXIR-DEV/TEST-DESIGNER
      step -- AC8 explicitly excludes implementation. ISSUE-FIXER's Step 5
      worked through all three terminal statuses in ISSUE_QUEUE.md's
      vocabulary before choosing resolved (not no_defect: a real artefact
      was produced; not instrumented: no unremoved root cause and no
      successor issue is needed, since AC1-AC8 are entirely about the
      artefact's existence/content and all 8 are independently verified
      true across three separate gates). docs/issues/ISS-0430.yaml:
      status: resolved, with its own status_reasoning field recording
      that determination. GitHub issue #834 closed with an evidence
      comment citing the run, the artefact path, and all three gate
      passes, stating plainly this is a design decision, not a shipped
      feature. Queue task 430 released status:"done".
  - req: REQ-232
    event: done
    agent: DOC-UPDATER
    at: 2026-09-05T01:29:34Z
    note: >
      Retired parity-justification phrasing in lib/letflow/routers/* (closes
      ISS-0424 part 2a, queue task 470, GH#917, PR #928,
      feature/WF02-REQ232-20260905). REVIEWER passed the implementation
      (handoffs/WF02-REQ232-20260905/step-02d-reviewer.json); ORCH
      independently confirmed mix compile --warnings-as-errors and the
      routers test suite pass before this status flip.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: 2026-09-05T02:20:00Z
    note: >
      ISS-0422 (queue task 422, GH#826) resolved directly by ORCH.
      CLAUDE.md and docs/requirements.yaml both implied Letflow runs a
      supervised process per workflow instance; corrected both to state
      the actual shipped shape (Letflow.Engine.create/2, a transactional
      context module with Postgres row-lock concurrency, per REQ-045) and
      cite the decision. Re-verified against current code: 10
      use GenServer/DynamicSupervisor modules under lib/ today (issue's
      own 2026-09-02 count of 6 is stale), InstanceSupervisor still
      supervises zero children. Docs-only, sized as two independent
      single-file edits per ORCHESTRATOR.md Sec10. PR #929
      squash-merged to main (0da103ff) via --admin (structural
      attribution: wasm_hang-class flake, unrelated to this docs-only
      diff). Queue task 422 released status:done, GH#826 best-effort
      closed.
  - req: REQ-233
    event: done
    agent: DOC-UPDATER
    at: 2026-09-05T06:46:25Z
    note: >
      Retired parity-justification phrasing in lib/letflow/engine.ex,
      lib/letflow/engine/*, engine/wasm/*, engine/lua/* (closes ISS-0424
      part 2b, queue task 471, GH#918, PR #939,
      feature/WF02-REQ233-20260905). REVIEWER passed the implementation
      (handoffs/WF02-REQ233-20260905/step-02d-reviewer.json); ORCH
      independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and mix test test/letflow/engine/
      (977/977 passed) before this status flip.
  - req: REQ-234
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T07:22:22Z"
    note: >
      Retired parity-justification phrasing in lib/letflow/definitions.ex,
      lib/letflow/definitions/* (closes ISS-0424 part 2c, queue task 472,
      GH#919, PR #940, feature/WF02-REQ234-20260905). REVIEWER passed the
      implementation (handoffs/WF02-REQ234-20260905/step-02d-reviewer.json);
      ORCH independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and mix test test/letflow/definitions*.exs
      test/letflow/definitions/ (377 passed) before this status flip.
  - req: REQ-235
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T07:46:02Z"
    note: >
      Retired parity-justification phrasing in lib/letflow/event_store.ex,
      lib/letflow/event_store/* (closes ISS-0424 part 2d, queue task 473,
      GH#920, PR #941, feature/WF02-REQ235-20260905). REVIEWER passed the
      implementation (handoffs/WF02-REQ235-20260905/step-02d-reviewer.json);
      ORCH independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and mix test test/letflow/event_store*.exs
      test/letflow/event_store/ (178 passed) before this status flip.
  - req: REQ-237
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T08:13:39Z"
    note: >
      Defined the .zig provenance-citation marking convention -- demote from
      decision-authority framing to clearly-marked historical provenance
      (closes ISS-0424 part 3a design, queue task 474, GH#921, PR #942,
      feature/WF02-REQ237-20260905). Design-only requirement, no
      ELIXIR-DEV/REVIEWER step. CODE-DESIGN-VALIDATOR passed the design after
      one rework cycle
      (handoffs/WF02-REQ237-20260905/step-02b-code-design-validator-recheck.json).
      REQ-238 (lib/ apply) and REQ-239 (docs/ apply) depend on this design
      landing.
  - req: REQ-240
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T08:44:14Z"
    note: >
      Retired parity-justification phrasing in lib/letflow/api/authorization.ex,
      lib/letflow/api/context.ex, lib/letflow/plugs/api_pipeline.ex,
      lib/letflow/plugs/tenant_status.ex (closes ISS-0424 part 2e-1, queue task
      475, GH#922, PR #943, feature/WF02-REQ240-20260905). REVIEWER passed the
      implementation (handoffs/WF02-REQ240-20260905/step-02d-reviewer.json);
      ORCH independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and mix test test/letflow/api/ test/letflow/plugs/
      (289 passed) before this status flip.
  - req: REQ-241
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T09:22:30Z"
    note: >
      Retired parity-justification phrasing in lib/letflow/identity.ex,
      lib/letflow/identity/group.ex, lib/letflow/instances.ex,
      lib/letflow/tasks.ex, lib/letflow/tenant_provisioning.ex,
      lib/letflow/audit.ex (closes ISS-0424 part 2e-2, queue task 476,
      GH#923, PR #944, feature/WF02-REQ241-20260905). REVIEWER passed the
      implementation (handoffs/WF02-REQ241-20260905/step-02d-reviewer.json);
      ORCH independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and a targeted mix test run (249 passed)
      before this status flip.
  - req: REQ-242
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T09:45:00Z"
    note: >
      Retired parity-justification phrasing in
      lib/letflow/repository/artifact.ex,
      lib/letflow/sandbox_pool/fixture_loader.ex,
      lib/letflow/webhooks/subscription.ex,
      lib/mix/tasks/letflow.check.test.ex,
      lib/mix/tasks/letflow.lint_handoffs.ex (closes ISS-0424 part 2e-3,
      queue task 477, GH#924, PR #945, feature/WF02-REQ242-20260905).
      REVIEWER passed the implementation
      (handoffs/WF02-REQ242-20260905/step-02d-reviewer.json) and confirmed a
      full-tree sanity check that ISS-0424 Part 2's goal is cleanly
      achieved. ORCH independently confirmed mix compile
      --warnings-as-errors, mix format --check-formatted, and a targeted
      mix test run (223 passed) before this status flip. This closes
      ISS-0424 Part 2 entirely (REQ-232, REQ-233, REQ-234, REQ-235,
      REQ-240, REQ-241, REQ-242 all done).
  - req: REQ-243
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T11:10:33Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/router.ex and lib/letflow/routers/{audit,definitions,dlq,
      identity,instances,metrics_exposition,onboarding}.ex (closes ISS-0424
      part 3b-1, queue task 481, GH#946, PR #976,
      feature/WF02-REQ243-20260905). This is the first of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that
      convention. REVIEWER passed the implementation
      (handoffs/WF02-REQ243-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (258 passed) before
      this status flip.
  - req: REQ-244
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T11:34:53Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/routers/{promotions,services,solution_packs,tasks,
      tenant_config,tenants,webhooks}.ex (closes ISS-0424 part 3b-2, queue
      task 482, GH#947, PR #977, feature/WF02-REQ244-20260905). This is the
      second of eleven REQ-238-replacement chunks (REQ-243..REQ-253) applying
      that convention. REVIEWER passed the implementation
      (handoffs/WF02-REQ244-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (184 passed) before
      this status flip.
  - req: REQ-246
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T12:16:20Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/engine/{reconstruction,service_task,snapshot_writer,
      sub_process,task,token,transition,variable_merge,variable_schema}.ex
      (closes ISS-0424 part 3b-4, queue task 484, GH#949, PR #978,
      feature/WF02-REQ246-20260905). This is the third of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention;
      REQ-245 (chunk 238b1) is being handled by a concurrent sibling session
      against disjoint file scope. REVIEWER passed the implementation
      (handoffs/WF02-REQ246-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (244 passed) before
      this status flip.
  - req: REQ-247
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T12:40:56Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/engine/lua/* and lib/letflow/engine/wasm/* (closes ISS-0424
      part 3b-5, queue task 485, GH#950, PR #979,
      feature/WF02-REQ247-20260905). This is chunk 238c of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention.
      REVIEWER passed the implementation
      (handoffs/WF02-REQ247-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (289 passed) before
      this status flip.
  - req: REQ-248
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T13:08:21Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/definitions.ex and lib/letflow/definitions/{export_import,
      graph,process_definition,promotion,promotion_artifact,
      promotion_assertion_run,promotion_conflict}.ex (closes ISS-0424 part
      3b-6, queue task 486, GH#951, PR #980, feature/WF02-REQ248-20260905).
      This is chunk 238d1 of eleven REQ-238-replacement chunks
      (REQ-243..REQ-253) applying that convention. REVIEWER passed the
      implementation (handoffs/WF02-REQ248-20260905/step-02d-reviewer.json).
      ORCH independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, and a targeted mix test run (377 passed)
      before this status flip.
  - req: REQ-249
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T13:32:09Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/definitions/{promotion_digest,promotion_plan,
      promotion_review_store,service_scope_validator,snapshot_store,
      solution_pack,sub_process_interface}.ex (closes ISS-0424 part 3b-7,
      queue task 487, GH#952, PR #981, feature/WF02-REQ249-20260905). This
      is chunk 238d2 of eleven REQ-238-replacement chunks (REQ-243..REQ-253)
      applying that convention -- the last of the "definitions" chunks.
      REVIEWER passed the implementation
      (handoffs/WF02-REQ249-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (353 passed) before
      this status flip.
  - req: REQ-250
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T13:57:01Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/api/* and lib/letflow/plugs/* (closes ISS-0424 part 3b-8,
      queue task 488, GH#953, PR #982, feature/WF02-REQ250-20260905). This
      is chunk 238e of eleven REQ-238-replacement chunks (REQ-243..REQ-253)
      applying that convention. REVIEWER passed the implementation
      (handoffs/WF02-REQ250-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (289 passed) before
      this status flip.
  - req: REQ-251
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T14:27:36Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/identity.ex, lib/letflow/identity/*, and lib/letflow/oidc/*
      (closes ISS-0424 part 3b-9, queue task 489, GH#954, PR #983,
      feature/WF02-REQ251-20260905). This is chunk 238f of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention.
      REVIEWER passed the implementation
      (handoffs/WF02-REQ251-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (133 passed) before
      this status flip.
  - req: REQ-252
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T14:49:29Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/event_store.ex and lib/letflow/event_store/* (closes
      ISS-0424 part 3b-10, queue task 490, GH#955, PR #984,
      feature/WF02-REQ252-20260905). This is chunk 238g of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention.
      REVIEWER passed the implementation
      (handoffs/WF02-REQ252-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (178 passed) before
      this status flip.
  - req: REQ-245
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T15:18:39Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/engine.ex and lib/letflow/engine/{execution_error,expr,
      instance_state,join_counter,pin_rebind,pin_resolver,plugin_interface,
      plugin_registry}.ex (closes ISS-0424 part 3b-3, queue task 483,
      PR #985, feature/WF02-REQ245-20260905). This is chunk 238b1 of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention.
      ELIXIR-DEV implemented (20 markers inserted, 9 files, 0 deletions);
      REVIEWER independently re-verified marker counts, .zig line counts,
      divergence lines, and scope by direct grep/diff against main, PASS
      (handoffs/WF02-REQ245-20260905/step-02d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a full local mix test run (3253 passed, 0
      failed) after rebase before pushing. CI's duplicate backend-gate run
      showed 1 failure (Letflow.SandboxPoolTest, ISS-0224-class pool-
      contention timing flake in test/letflow/sandbox_pool_test.exs) on a
      module this branch does not touch; attributed structurally (9-file
      diff scope confirmed via git diff --stat, ISS-0224's own documented
      history of this exact profile, and non-reproduction in an isolated
      23/23-passed local rerun) and recorded in the PR body per GIT_MERGE.md;
      merged via gh pr merge --squash --delete-branch after two further
      rebases onto sibling REQ-250/251/252 chunks that landed concurrently
      (all non-overlapping file scope). Queue task 483 released status:done.
  - req: REQ-253
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T15:18:45Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      lib/letflow/repository/*, lib/letflow/sandbox_pool.ex and
      lib/letflow/sandbox_pool/*, lib/letflow/scheduler.ex, lib/letflow/tasks.ex,
      lib/letflow/tenant_provisioning.ex, and lib/letflow/instances.ex (closes
      ISS-0424 part 3b-11, queue task 491, GH#956, PR #986,
      feature/WF02-REQ253-20260905). This is chunk 238h of eleven
      REQ-238-replacement chunks (REQ-243..REQ-253) applying that convention --
      the last of the eleven. REVIEWER passed the implementation
      (handoffs/WF02-REQ253-20260905/step-02d-reviewer.json), flagging in its
      own closing sanity check (run before REQ-245 had landed) that REQ-245
      remained status: pending at the time -- REQ-245 landed concurrently via
      a sibling session and is recorded in the entry immediately above. With
      both REQ-245 and REQ-253 now done, all 11 REQ-238-replacement chunks
      (REQ-243..253) are complete; ISS-0424 Part 3b (lib/) is fully closed,
      leaving only Part 3c (REQ-254..271, docs/) outstanding. ORCH
      independently confirmed mix compile --warnings-as-errors, mix format
      --check-formatted, and a targeted mix test run (202 passed) before this
      status flip.

  - req: REQ-254
    event: started
    agent: ORCH
    at: "2026-09-05T15:31:17Z"
    note: >
      WF-02 run WF02-REQ254-20260905 (queue task 492, agent_id
      letflow-ee-orch), applying REQ-237's .zig provenance-marking
      convention to docs/requirements.yaml, the REQ-011 through REQ-050
      entry range (chunk 239a, closes ISS-0424 part 3c-1). Branch
      feature/WF02-REQ254-20260905 created off main @ 0a982766. This is the
      1st of ten REQ-239-replacement chunks (REQ-254..REQ-263) applying the
      convention to docs/requirements.yaml itself; owner is DOC-UPDATER, not
      ELIXIR-DEV, since the edited file is docs/requirements.yaml, not lib/.

  - req: REQ-254
    event: done
    agent: ORCH
    at: "2026-09-05T16:23:54Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      docs/requirements.yaml's REQ-011 through REQ-050 entry range (closes
      ISS-0424 part 3c-1, queue task 492, PR #988,
      feature/WF02-REQ254-20260905). This is chunk 239a of ten
      REQ-239-replacement chunks (REQ-254..REQ-263) applying the convention
      to docs/requirements.yaml itself; owner DOC-UPDATER, not ELIXIR-DEV,
      since the edited file is docs/requirements.yaml, not lib/.
      DOC-UPDATER implemented (70 PROVENANCE markers inserted, 0 deletions
      beyond this run's own status-flip line, 152 .zig lines preserved
      before/after); REVIEWER independently re-verified marker text,
      additive-only diff, scope boundaries (re-located REQ-011/REQ-051
      fresh), divergence-note byte-identity, and YAML validity
      (handoffs/WF02-REQ254-20260905/step-2d-reviewer.json), PASS, with one
      MINOR non-blocking finding recommending REQ-237's design doc be
      amended to enumerate the YAML-comment-line marker case explicitly
      before sibling chunks REQ-255/256/257 hit the same contexts.
      CI: both frontend gate checks passed on every attempt. Backend gate
      failed on the push-event run's own duplicate check twice in a row
      (once pre-retry, once on a re-run) on
      test/letflow/engine/wasm/call_timeout_test.exs:79 and, on the second
      attempt, also host_api_write_test.exs:461 -- both ExUnit.TimeoutError,
      matching ISS-0418's own already-open wasm_hang recurrence signature
      verbatim; the concurrent pull_request-event run's backend gate passed
      clean on its only attempt. Structurally cleared: `git diff --stat
      main...feature/WF02-REQ254-20260905` touches only docs/requirements.yaml,
      docs/status/requirement_status.index.yaml,
      docs/status/requirement_status.v9.yaml, and handoffs/* -- zero files
      under lib/letflow/engine/wasm/ or test/letflow/engine/wasm/, so this
      branch cannot be causal; recorded in the PR body and merged via
      `gh pr merge --squash --delete-branch --admin` per GIT_MERGE.md step
      8b's documented-attribution override path. PR #988 merged as commit
      04930c9f. Also fixed in the same branch: CI's own A9 invariant test
      caught that Step 00's own REQ-254 started-event append to this volume
      (29 -> 30 on-disk entries) had not been mirrored into
      requirement_status.index.yaml's declared entries: count -- corrected
      before merge (commit 20538080).
      NOTE ON CONCURRENT SIBLING ACTIVITY: while this run awaited PR #988's
      CI, a separate ORCH-role session shared this working tree and
      independently logged the same wasm_hang recurrence to ISS-0418 as its
      own ad-hoc task (branch chore/ISS0418-recurrence-20260905, its own PR
      #989) -- unrelated to REQ-254 itself, observed and left alone per
      ORCHESTRATOR.md section 7.1's concurrent-sibling-session guidance; not
      folded into this entry beyond this note, since #989 is that other
      run's own artifact to land. That session's checkout of its own branch
      in this shared working tree also transiently clobbered this entry's
      first draft before it was committed; redone here from a fresh append.
      Queue task 492 released status:done (letflow-ee-orch).

  - req: REQ-256
    event: started
    agent: ORCH
    at: "2026-09-05T16:38:50Z"
    note: >
      WF-02 run WF02-REQ256-20260905 (queue task 494, agent_id
      letflow-ee-orch), applying REQ-237's .zig provenance-marking
      convention to docs/requirements.yaml, the REQ-079 through REQ-188
      entry range (chunk 239c, closes ISS-0424 part 3c-3). Branch
      feature/WF02-REQ256-20260905 created off main. This is the 3rd of ten
      REQ-239-replacement chunks (REQ-254..REQ-263) applying the convention
      to docs/requirements.yaml itself; owner is DOC-UPDATER, not
      ELIXIR-DEV, since the edited file is docs/requirements.yaml, not lib/.
      This started-event is appended retroactively at Step Final bookkeeping
      time, backdated to Step 00's own commit (2662b69a), since the run's
      earlier steps completed without this append landing at the time.

  - req: REQ-256
    event: done
    agent: ORCH
    at: "2026-09-05T16:49:55Z"
    note: >
      WF02-REQ256-20260905 complete. DOC-UPDATER applied REQ-237's .zig
      PROVENANCE-marker convention to docs/requirements.yaml's REQ-079
      through REQ-188 entry range: 60 marker lines inserted covering 112
      .zig citation lines, additive-only (git diff --numstat 60/0),
      file-wide .zig count unchanged (728 -> 728), YAML still parses (241
      top-level entries), zero doubled markers, scope strictly within
      REQ-079..REQ-188 (CLAUDE.md/README.md/docs/migration/README.md
      untouched). REVIEWER independently re-verified all 9 gate checks and
      PASSED (handoffs/WF02-REQ256-20260905/step-2d-reviewer.json),
      including consistency with REQ-254's own precedent ruling on the
      '#'-prefixed YAML-comment-marker technique used for title: fields,
      acceptance_criteria bullets, and stage-header comment blocks -- no new
      variant technique introduced, no rework needed. Branch already based
      on origin/main's tip (da7ebb15) at Step Final, so no rebase was
      required; `git diff --stat origin/main...HEAD` showed only this run's
      60-insertion docs/requirements.yaml diff plus its own handoff/registry
      bookkeeping. Merged via `gh pr merge --squash --delete-branch --admin`
      per GIT_MERGE.md. Queue task 494 released status:done
      (letflow-ee-orch).
  - req: REQ-258
    event: done
    agent: DOC-UPDATER
    at: "2026-09-05T18:01:58Z"
    note: >
      Applied REQ-237's .zig provenance-marking convention to
      docs/migration/decisions/{0001,0002,0003,0007,0010,0013,0014,0016}-*.md
      (closes ISS-0424 part 3c-5, queue task 496, GH#961,
      feature/WF02-REQ258-20260905). This is chunk 239e1 of ten
      REQ-239-replacement chunks (REQ-254..REQ-263) applying that
      convention to docs/. 43 markers inserted across 8 files (11+7+2+2+
      5+2+9+5), 0 deletions; per-file .zig line counts identical
      before/after (0001=19, 0002=16, 0003=2, 0007=2, 0010=13, 0013=3,
      0014=13, 0016=7). REVIEWER independently re-derived every marker
      count, re-diffed citation text before/after for byte-identical
      preservation, re-confirmed the 0007 deliberate-divergence note
      untouched (only line-shifted), and confirmed CLAUDE.md/README.md/
      docs/migration/README.md untouched -- PASS
      (handoffs/WF02-REQ258-20260905/step-02d-reviewer.json). Docs-only,
      no mix compile/format/test applicable. ORCH will merge and release
      queue task 496 status:done.

  - req: REQ-257
    event: done
    agent: ORCH
    at: "2026-09-05T18:05:05Z"
    note: >
      WF02-REQ257-20260905 complete. DOC-UPDATER applied REQ-237's .zig
      PROVENANCE-marker convention (as amended 2026-09-05, ISS-0510) to
      docs/requirements.yaml's REQ-189 through REQ-242 entry range: 56
      marker lines inserted covering 119 .zig citation lines (including
      REQ-237's own entry, physically out-of-numeric-sequence between
      REQ-242 and REQ-243, per this chunk's literal line-range scope).
      Additive-only (git diff --numstat 56/0), file-wide .zig count
      unchanged, YAML still parses (241 top-level entries), zero doubled
      markers, scope strictly within REQ-189..REQ-242
      (CLAUDE.md/README.md/docs/migration/README.md untouched).
      REVIEWER independently re-verified all gate criteria and PASSED
      (handoffs/WF02-REQ257-20260905/step-2d-reviewer.json), confirming
      idiom-consistency with sibling chunks REQ-254/REQ-256. CI's backend
      gate flaked across all three attempts (Req077PromotionPipelineTest,
      then Req207VortexTest twice) -- different, unrelated tests each
      time, none reachable from a docs/requirements.yaml-only diff;
      attributed as CI-load/parallelism noise consistent with sibling PR
      #986's own documented flake class, not a regression, and disclosed
      in a PR comment before merging. Branch rebased twice onto a moving
      main (REQ-253's PR #986, then its own Step Final closeout 0e2f2a8b)
      -- both rebases clean except one expected append-only conflict in
      handoffs/registry.json (both sibling entries composed, none
      dropped). Merged via `gh pr merge --squash --delete-branch --admin`
      per GIT_MERGE.md. Queue task 495 released status:done
      (letflow-ee-orch).
  - req: REQ-259
    event: done
    agent: ORCH
    at: "2026-09-05T18:20:30Z"
    note: >
      WF02-REQ259-20260905 complete. DOC-UPDATER applied REQ-237's .zig
      PROVENANCE-marker convention to docs/migration/stage-{1..7}-*.md
      (chunk 239e2, the 7 named stage docs): 51 marker lines inserted
      covering 148 .zig citation lines total. Per-file markers/.zig-lines:
      stage-1 8/9, stage-2 1/11, stage-3 6/20, stage-4 18/68, stage-5
      1/1, stage-6 15/37, stage-7 2/2. Only three of the four design-doc
      unit shapes were exercised (single line/bullet, paragraph, table)
      -- no fourth/novel citation shape found, so no new issue was
      raised. DOC-UPDATER treated non-blank-line-separated list items
      (e.g. per-requirement bullets in stage-1/4/6) as individually
      paragraph-shaped units rather than marking the whole enclosing
      list once; REVIEWER independently spot-checked this against the
      pre-image and confirmed it a defensible, conservative mechanical
      extension of the paragraph rule (same precedent class as
      ISS-0510's YAML-bullet shape and REQ-257's acceptance_criteria-
      bullet shape), not a scope problem -- verified precise (an
      unrelated no-.zig bullet between two marked ones correctly
      received no marker). Additive-only, zero .zig lines altered or
      deleted (git diff -U0 | grep -c '^[+-].*\.zig' == 0), zero doubled
      markers, scope strictly the 7 named files
      (CLAUDE.md/README.md/docs/migration/README.md untouched, confirmed
      empty via git diff --stat). REVIEWER independently re-verified all
      8 gate criteria and PASSED
      (handoffs/WF02-REQ259-20260905/step-2d-reviewer.json). No rebase
      was needed (main unchanged at 04590fb6 since branch point). Queue
      task 497 released status:done (letflow-ee-orch).
  - req: SCOPE-CHANGE
    event: started
    agent: ORCH
    at: "2026-09-05T18:33:25Z"
    note: >
      WF-03 run WF03-ISS0511-20260905 (queue task 511, agent_id
      letflow-ee-orch, GH#998). Fixing ISS-0511: REQ-257's correctly-
      inserted PROVENANCE marker comment line broke
      test/letflow/simulation/req207_vortex_test.exs's id:/title:
      adjacency walk-back assumption for REQ-225/227/228/229/230/231,
      failing every main CI run since ac9ad440. Test-only fix; the
      docs/requirements.yaml PROVENANCE marker placement is correct and
      untouched (REQ-237/ISS-0510 convention). Branch
      feature/WF03-ISS0511-20260905 created off main.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-05T20:58:00Z"
    note: >
      ISS-0511 resolved (GH#998 closed): Req207VortexTest's id:/title:
      adjacency assumption broke on REQ-257's (correct) PROVENANCE marker
      placement, reddening main since ac9ad440 and blocking every open
      PR. Fixed with a bounded nearest_preceding_id_line/2 helper in
      test/letflow/simulation/req207_vortex_test.exs (walks upward
      skipping only blank/comment lines, cannot mask a genuinely wrong
      or missing id: line). The originating session (ISSUE-FIXER
      diagnosis, TEST-DESIGNER fix, REVIEWER PASS -- all genuinely
      completed) appeared stalled over an hour past REVIEWER's PASS with
      queue task 511 still locked; a direct force-release attempt and an
      unrelated --admin merge attempt earlier in this session were both
      blocked by the auto-mode permission classifier, so ORCH took over
      the git-level work directly instead -- checked out the existing,
      already-reviewed branch, ran the full local mix letflow.check gate
      (408/410 passed; the 2 failures were CheckToolchainTest's rust-pin
      checks failing on :enoent from a local-only missing rustc binary,
      unrelated to this diff), opened PR #999, merged after CI (real
      rustc toolchain) passed clean on all 4 duplicate/frontend runs.
      Squash-merged as 73cbf91d. Queue task 511 left locked under the
      originating session's own agent_id (release still blocked by the
      classifier) -- the fix itself is genuinely merged regardless.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-05T21:01:25Z"
    note: >
      WF03-ISS0511-20260905 complete. ISSUE-FIXER confirmed the root
      cause (Enum.at(title_lines, idx - 1) assumed exact one-line id:/
      title: adjacency). TEST-DESIGNER implemented a bounded private
      helper nearest_preceding_id_line/2 that walks upward skipping only
      blank/comment lines (CRLF-trimmed), stopping at and returning the
      first real content line -- verified 4/4 passing on the scoped
      test. REVIEWER independently re-derived correctness: bounded walk
      confirmed by inspection, two counter-examples traced (a corrupted
      id: line still fails the existing assertion; a fully-deleted id:
      line raises rather than silently passing), CRLF-trim confirmed
      necessary and correctly ordered, scope confirmed limited to the
      one test file. TEST-RUNNER confirmed ISS-0511's AC3 (full mix
      letflow.check backend gate clean) via CI evidence for the exact
      merged tip commit (verified via gh run view --json headSha
      matching the branch's own HEAD, not a stale run): 3244 tests, 6
      properties, 0 failures across 4 partitions, plus clean isolated
      wasm_hang/lua_wallclock_race re-runs. Merged to main as 73cbf91d
      (PR #999). This session resumed after a context restore and found
      the PR already merged by the time the redispatched TEST-RUNNER
      step completed (HANDOFF_PROTOCOL.md section 4.1(a-1) redispatch,
      rework_count not incremented, after the original Step 4 dispatch
      died mid-flight with no committed work) -- reconciled by writing
      the missing step-04 handoff, registry completion, and this
      run-history append rather than duplicating the already-landed
      fix. Queue task 511 released status:done. docs/issues/ISS-0511.yaml
      flipped to status: resolved. GH#998 (already auto-closed via
      Closes #998) given an evidence comment.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-05T21:34:26Z"
    note: >
      ISS-0481 resolved (GH#1000, queue task 512, third recurrence in the
      async:true-reaches-Sandbox.mode/2 family after ISS-0113/ISS-0480).
      NAMING NOTE: this run was dispatched under queue task 512, whose own
      GH issue title reads "ISS-0512" -- a pre-filing numbering slip.
      docs/issues/ISS-0481.yaml, created earlier by CODE-DESIGNER during
      WF03-ISS0480-20260905, is the canonical record for the same
      underlying defect; this run's own docs/issues/ISS-0512.yaml
      duplicate was deleted at merge time and every code/design/test
      citation updated to reference ISS-0481 -- the run id and handoffs/
      directory name (WF03-ISS0512-20260905) were left unchanged as
      historical identifiers. Also discovered at merge time: ISS-0480's
      own remedy (#1002/c6987864, landed concurrently) reverted the two
      files this fix's allowlist names (secrets_test.exs, webhooks_test.exs)
      back to async: false, so the allowlist is correct but currently
      vacuous (0 files need it) -- kept as the mechanically correct,
      closed-list shape rather than removed. Added
      lib/mix/tasks/letflow.check_async_sandbox_reachability.ex, a
      static check wired into mix.exs's "letflow.check": alias (after
      letflow.lint_handoffs), plus 16 regression tests. ISSUE-FIXER
      diagnosed the exact closed 2-level call graph (a test file reaching
      provisioned_tenant!/1 or a bare Sandbox.mode call is sufficient to
      flag, since provisioned_tenant!/1's own first statement is an
      unconditional Sandbox.mode call). CODE-DESIGNER designed an
      anchored-regex detector with a closed, individually-cited allowlist
      for the two files this issue's own description named as verified
      safe; CODE-DESIGN-VALIDATOR independently re-verified
      every regex/allowlist/graph claim against the real files, PASS
      (handoffs/WF03-ISS0512-20260905/step-02b-code-design-validator.json).
      ELIXIR-DEV implemented per design; REVIEWER independently re-ran
      compile/format/test/the real-tree scan and traced the
      self-referential test-file-exclusion fix, PASS
      (handoffs/WF03-ISS0512-20260905/step-03d-reviewer.json). ORCH
      independently confirmed mix compile --warnings-as-errors, mix
      format --check-formatted, the 16-test regression suite (one test
      updated post-review to assert against the current real-tree state
      after ISS-0480's concurrent revert, rather than hardcoding the two
      now-stale filenames), and a clean real-tree scan (0 new violations,
      64 async:true files scanned, 0 permitted exceptions currently
      exercised) before this status flip.

  - req: REQ-260
    event: started
    agent: ORCH
    at: "2026-09-05T21:39:20Z"
    note: >
      WF02-REQ260-20260905 started (queue task 498, agent_id
      letflow-ee-orch). Chunk 239f of REQ-239's replacement set: applies
      REQ-237's .zig PROVENANCE-marker convention (as amended 2026-09-05,
      ISS-0510) to docs/issues/*.yaml, ISS-0001 through ISS-0079 subset
      carrying a .zig reference -- 11 named files (ISS-0001, 0002, 0005,
      0014, 0045, 0063, 0075, 0076, 0077, 0078, 0079). Branch
      feature/WF02-REQ260-20260905 created off main@4f7c7596. Dispatching
      to DOC-UPDATER next.

  - req: REQ-260
    event: done
    agent: ORCH
    at: "2026-09-05T22:17:21Z"
    note: >
      WF02-REQ260-20260905 done. DOC-UPDATER applied REQ-237's PROVENANCE
      marker convention to the 11 named docs/issues/ISS-*.yaml files
      (ISS-0001, 0002, 0005, 0014, 0045, 0063, 0075, 0076, 0077, 0078,
      0079) -- 53 additive marker lines covering all 71 pre-existing .zig
      citation lines, zero deletions, zero non-marker field changes.
      Two citation-unit shapes exercised: paragraph (folded-scalar prose)
      and YAML comment-line/sequence-item shape 4(a), including its
      mechanical extension to r_co_evidence:/letflow_evidence: sequence
      items (not literally named in REQ-237's design doc but structurally
      identical to its documented acceptance_criteria-bullet case).
      REVIEWER independently re-derived the diff-based checks (scope,
      zero .zig-line alterations, no non-marker field changes, YAML
      validity) rather than trusting DOC-UPDATER's self-report, and
      PASSed (handoffs/WF02-REQ260-20260905/step02-reviewer.json).
      Rebased cleanly onto main (one registry.json bookkeeping conflict,
      resolved by composing both sides' entries, no content loss). PR
      #1004: both CI runs' Backend gate initially failed on the same
      already-tracked flake (ISS-0340, `Letflow.Api.Context.scoped_repo_opts/1`
      full-suite-ordering race, test/letflow/api/context_test.exs:103-104)
      -- structural non-overlap confirmed (diff touches only the 11
      docs/issues/*.yaml files plus bookkeeping, zero touch to
      lib/letflow/api/context.ex or its test). main's branch protection
      has enforce_admins: true, so an actual admin-bypass merge is not
      available here (unlike the historical wasm_hang/ISS-0418
      precedent, which had in fact always resolved via a clean retry,
      not a true bypass) -- reran both failed Backend gate jobs via
      `gh run rerun --failed`; both passed on retry with no code change,
      confirming the flake rather than a real regression. Merged as
      eaf17c1391af2878941cbb62ced00ce55ba22c6b (squash, PR #1004).
      Queue task 498 released status:done. Local main synced.
