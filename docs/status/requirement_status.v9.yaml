# Letflow — Requirement run history — VOLUME 9 (CURRENT)
#
# docs/requirements.yaml's `status:` field is the current state of each
# requirement (pending/in_progress/done/blocked) — read that first. This file is
# the history behind it: one entry per time an agent picked up, finished, or got
# blocked on a piece of work.
#
# APPEND ONE ENTRY PER EVENT. NEVER REWRITE, REORDER, EDIT OR DELETE A PAST
# ENTRY — in this volume or any closed one. An agent once rewrote this history
# from scratch with different field names and silently destroyed prior entries;
# see docs/anti-patterns.md, "Overwriting docs/status/requirement_status.yaml
# instead of appending". That is what this rule exists to prevent, and it is not
# negotiable.
#
# VOLUMES. The history is split across volumes because a file too large to read
# cannot show you its own schema. Index (start here):
#   docs/status/requirement_status.index.yaml
# Predecessor: docs/status/requirement_status.v8.yaml (volume 8, closed,
#   1,213 frozen-prefix lines / 1,242 total lines with closure footer --
#   over its own roll ceiling, so targeted reads only; see the index).
#
# ── HOW TO APPEND (this is the procedure; it is executable, run it) ───────────
#
# 1. Confirm you are writing to the current volume. Never assume this file is it.
#    Run the ONE LINE for YOUR shell — grep does not exist in PowerShell, and
#    Select-String does not exist in bash. Each is a single line; do not wrap it:
#      Bash:
#        grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml
#      PowerShell:
#        Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0
#    Both print the `- volume:` / `path:` lines above the `status: current`
#    marker. The `path:` value they show is the file you append to.
#
# 2. Read THIS volume in full — one Read call on this path, no offset, no limit.
#    That is possible by construction: the roll rule below keeps every current
#    volume under 1,200 lines / 120,000 bytes, inside the Read tool's 256KB and
#    2,000-line limits. If that read is refused or truncated, STOP: the roll rule
#    has failed and that is itself a defect to file, not to work around.
#    Confirm both bounds before you rely on the read (lines AND bytes — a
#    line-truncated read is silent):
#      Bash:
#        wc -l < FILE ; wc -c < FILE
#      PowerShell:
#        (Get-Content FILE).Count ; (Get-Item FILE).Length
#    (FILE = docs/status/requirement_status.v9.yaml. PowerShell has no `&&`;
#    `;` chains unconditionally in both shells.)
#
# 3. Get the timestamp from the clock, never from memory:
#      Bash:       date -u +"%Y-%m-%dT%H:%M:%SZ"
#      PowerShell: (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
#
# 4. Append your entry at the end of the file. Append — do not open the file for
#    rewriting, do not regenerate it, do not reflow existing entries.
#    If you append via a Bash heredoc, the delimiter MUST be quoted (`<<'EOF'`,
#    never `<<EOF`): your note text will routinely contain backticks and colons
#    because it is prose ABOUT this file's own field names, and an unquoted
#    delimiter lets Bash execute a backtick span as a command substitution,
#    silently deleting it from what lands on disk (ISS-0199, hit live). In
#    PowerShell, use a literal here-string (`@'...'@`), never an interpolating
#    one (`@"..."@`), for the matching reason with `$`.
#
# 5. Verify you appended and did not replace, AND that what landed is what you
#    wrote — two checks, neither optional:
#      a. The line count must GROW:
#           git diff --numstat docs/status/requirement_status.v9.yaml
#         Any nonzero deletion count on this file is a defect. Revert and redo.
#         This proves you appended; it proves nothing about WHAT you appended —
#         a shell that silently ate a backtick span still line-balances.
#      b. Re-read what you just wrote:
#           Bash:       tail -n 8 docs/status/requirement_status.v9.yaml
#           PowerShell: Get-Content docs/status/requirement_status.v9.yaml -Tail 8
#         Confirm the note text reads exactly as you intended — no missing
#         words, no blank span where a backtick-quoted field name belonged.
#         This is the check (a) structurally cannot make: ISS-0199 was a real
#         append that passed (a) at `43\t0` while its note text had already
#         been silently gutted by an unquoted-heredoc command substitution.
#
# 6. Update this volume's `entries:` count in
#      docs/status/requirement_status.index.yaml
#    to match — increment it by one, in the SAME commit as your append. A
#    declared count nobody updates is a number nobody can trust; ISS-0199
#    finding 2 found this volume's `entries:` at 0 after real entries had
#    already landed. test/docs/requirement_status_invariants_test.exs's A9
#    asserts this count against what is actually on disk.
#
# 7. Apply the roll rule below.
#
# ── ENTRY SHAPE — all five fields, in this order, every time ──────────────────
#
#   - req: <REQ-NNN | SCOPE-CHANGE>
#     event: <started | done | blocked | cancelled | revised | verified>
#     agent: <AGENT_ID>
#     at: <UTC timestamp from the clock, not memory>
#     note: short free-text (what shipped, or what's blocking)
#
#   ALL FIVE FIELDS ARE REQUIRED, INCLUDING `note:`. The timestamp field is named
#   `at:` — never `timestamp:`. Three volume-1 entries break this: line 4803
#   (REQ-053) and line 5050 (REQ-054) have no `note:` at all, and line 4896
#   (REQ-056) writes `timestamp:` where `at:` belongs. They are declared in
#   docs/status/requirement_status.index.yaml under `known_shape_anomalies:` and
#   are LEFT AS WRITTEN, for the same reason the `event: SCOPE-CHANGE` entries
#   are — correcting a past entry is what this file forbids. Do not copy them,
#   and do not "clean them up": assertion A4b in
#   test/docs/requirement_status_invariants_test.exs fails either way.
#
# ── `req:` — THE COMPLETE LIST. Exactly two legal values ──────────────────────
#
#   REQ-NNN        A requirement in docs/requirements.yaml. Three digits, zero-
#                  padded (REQ-003, not REQ-3). 151 entries in volume 1.
#   SCOPE-CHANGE   ANY work not tied to a requirement id: WF-03 issue
#                  resolutions, queue reconciliation, pipeline/process changes,
#                  manual bookkeeping. 31 entries in volume 1. Put the real
#                  subject (the ISS id, the run id, what was reconciled) in
#                  `note:`.
#
#   THERE IS NO THIRD VALUE. In particular: `req: ISS-NNNN` IS NOT LEGAL and has
#   zero precedent (0 occurrences across volume 1's 182 entries). Recording an
#   issue fix uses `req: SCOPE-CHANGE` with the ISS id named in `note:`. This is
#   written down here because it was previously discoverable only by surveying
#   the whole file, and `req: ISS-NNNN` is the obvious wrong guess.
#
# ── `event:` — THE COMPLETE LIST. Exactly six legal values ────────────────────
#
#   started    work picked up.                         (54 in volume 1)
#   done       work finished and merged.               (107 in volume 1)
#   blocked    work stopped on an external dependency.   (5 in volume 1)
#   cancelled  work abandoned; it will not be finished.  (8 in volume 1)
#   revised    a prior decision or scope was changed.    (4 in volume 1)
#   verified   independently re-verified after `done`.   (1 in volume 1)
#
#   Volume 1's original header listed only started|done|blocked, while
#   cancelled, revised and verified were already in use — an inaccuracy that
#   could only be found by reading 5,766 lines. All six are documented here.
#
#   SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490,
#   3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded
#   as known anomalies in the index, and they are LEFT AS WRITTEN — correcting a
#   past entry is exactly what this file forbids. Do not copy them, and do not
#   "clean them up".
#
#   Adding a seventh event value is a documentation change, not an improvisation:
#   amend this header in the same commit that first uses it, or use an existing
#   value. An undocumented value fails
#   test/docs/requirement_status_invariants_test.exs.
#
# ── WHEN THIS VOLUME IS FULL — the roll rule ──────────────────────────────────
#
# AFTER appending, measure this file (working tree, not the git blob — the Read
# tool measures the working tree, and CRLF checkouts add one byte per line):
#     Bash:       wc -l < FILE ; wc -c < FILE
#     PowerShell: (Get-Content FILE).Count ; (Get-Item FILE).Length
#
# If lines > 1200 OR bytes > 120000, roll before your handoff completes:
#   1. Append this volume's closure footer (copy volume 1's footer, adjusting the
#      volume number, dates, counts and successor path).
#   2. Create docs/status/requirement_status.v<n+1>.yaml containing THIS HEADER
#      TEXT verbatim — with the volume number, opening date and predecessor path
#      updated — followed by `history:` and no entries.
#   3. In docs/status/requirement_status.index.yaml: set this volume's status to
#      `closed`, fill its entries/lines/bytes/frozen_prefix_lines/
#      frozen_prefix_sha256, and add the new volume with `status: current`.
#   4. Commit all three files together. A closed volume with no successor, or two
#      current volumes, fails the invariant test.
#
# Rolling is normal maintenance, not an incident. It is expected roughly every
# 40 entries. It never moves, edits or deletes an existing entry.


history:
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T05:42:37Z"
    note: >
      ISS-0451 resolved (WF03-ISS0451-20260904, queue task 451, GH#865). A
      persistent (never-clearing) Poller crash-loop could exhaust the
      top-level Letflow.Supervisor's own default OTP restart budget (3/5s)
      after Letflow.Supervisor.Pollers exhausted its own (5/60s), cascading
      past the isolation REQ-219 built -- found by REVIEWER during REQ-219's
      own AC4 gating. Fix: Pollers' child spec is now
      Supervisor.child_spec(Letflow.Supervisor.Pollers, restart: :temporary)
      in lib/letflow/application.ex, so the top level structurally can never
      auto-restart it (proven, not bounded -- 0 restarts consumed from its
      budget for any fault duration); a new Letflow.Supervisor.PollersBreaker
      GenServer (lib/letflow/supervisor/pollers_breaker.ex) becomes sole
      restart authority via Supervisor.start_child/2, tripping to a stopped
      :open state after 2 consecutive exits with exponential backoff.
      Top-level max_restarts raised 3->5 as defense-in-depth for the
      remaining :permanent children. Design went through 3 rework cycles
      (2 real Elixir Supervisor-semantics bugs caught by CODE-DESIGN-VALIDATOR
      -- an unproven termination-vs-auto-restart race, then a children-list
      syntax defect that silently failed to apply :temporary -- plus a real
      race condition between PollersBreaker and the existing AC4 test's own
      restart call, found by ELIXIR-DEV during implementation and resolved by
      redesigning the test as a single breaker-mediated flow rather than
      touching production code). SECURITY-REVIEWER and REVIEWER both PASSED.
      New fail-then-pass mutation-tested regression test
      (test/letflow/supervisor/pollers_breaker_test.exs), independently
      re-verified by TEST-DESIGN-VALIDATOR (one mutant reproduced exactly).
      Full suite green: mix letflow.check.test 3203+7+11 passed, 0 failures.
      PR #874 merged (squash commit 77fe6bf4) after one structurally-cleared
      CI flake (wasm_hang timing class, zero diff overlap, identical commit
      passed the concurrent job) -- filed as ISS-0456 (queue task 456, GH#875)
      rather than left undocumented. docs/issues/ISS-0451.yaml: status:
      resolved. GitHub issue #865 closed with mandated evidence comment.
      Also released a stale WF03-ISS0425-20260903 registry lock (no live
      branch/session behind it, its own diagnosis had already been routed
      into and completed by REQ-219/REQ-220) that was blocking this run's
      owned_modules claim on lib/letflow/application.ex.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T06:22:22Z"
    note: >
      WF03-ISS0456-20260904: ISS-0456 (wasm_hang timing flake recurrence,
      PR #874 push-event backend gate, queue task 456, GH#875) diagnosed and
      folded into ISS-0418 -- NOT independently resolved. ISSUE-FIXER
      independently verified, by direct CI log inspection (gh run view
      --log-failed job 100920721979), that this recurrence's failure
      signature (plugin_handler_test.exs:474, host_api_write_test.exs:460) is
      byte-for-byte identical in file/line/timeout-bound/stack-shape to
      ISS-0418's own recurrence_2026_09_03 entry -- not merely accepted from
      the filing text, per HANDOFF_PROTOCOL.md's "a handoff's factual
      premises are checkable" rule applied to this issue's own filing.
      Independently re-checked PR #874's zero-touch claim on wasm/lua paths
      via gh pr view --json files. Cross-checked ISS-0453 and confirmed no
      match (different subsystem -- Letflow.Scheduler.Admission, main suite
      not the isolated wasm_hang stage). No new root cause found; no fix
      designed or applied for ISS-0456 itself. docs/issues/ISS-0456.yaml ->
      status: instrumented, superseded_by: ISS-0418. docs/issues/ISS-0418.yaml
      -> new recurrence_2026_09_04 field, tenth-plus hit; its own OQ-5
      operator-configurable concurrency cap remains the correct, still
      undesigned, structural fix for this whole class. Ran through the full
      git-setup/PR/CI/merge pipeline for the docs-only fold-in (PR #876,
      merged as fast-forward 023c4e17) -- both backend-gate CI runs passed
      cleanly, no wasm_hang flake recurred on this PR's own CI. GH#875 closed
      with mandated evidence comment (never claims a fix). Queue task 456
      released WITHOUT status (stays open, unlocked) per TASK_QUEUE.md's rule
      that status:"done" would falsely assert the underlying defect is fixed.
  - req: REQ-220
    event: done
    agent: ORCH
    at: "2026-09-04T08:22:00Z"
    note: >
      Direct ORCH action (ORCHESTRATOR.md S10: single file, no new
      function/module, no migration, no supervision-tree structural
      change -- keeps all 7 Task.Supervisors -- no tenant-data path, no
      test-asserted-behavior change; all six checks passed). Closes
      ISS-0425 part 3. Added a consolidated "Task.Supervisor sprawl
      review" section to lib/letflow/supervisor/infrastructure.ex's
      moduledoc, naming all 7 Task.Supervisors with their originating
      requirement/issue and distinct justification (already fully
      elaborated in REQ-220's own requirement text -- this step
      transcribed it into the module, not re-derived it), and explicitly
      considering and rejecting the two consolidation candidates named in
      the requirement's own ACs (three Wasm supervisors; Plugin+Lua).
      Real recommendation: KEEP ALL 7 SEPARATE, no Task.Supervisor
      removed/renamed/merged. mix compile --warnings-as-errors clean;
      mix format --check-formatted clean; test/letflow/supervisor/ 11/11
      passed; full suite (mix letflow.check.test) 3206/3208 passed, 2
      pre-existing rustc-toolchain-gap failures (Mix.Tasks.Letflow.
      CheckToolchainTest, rustc absent from this environment).
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T14:10:00Z"
    note: >
      WF03-ISS0421-20260904 done (queue task 421, GH#825, stage S6).
      Scheduler.Poller's 7 serial per-tenant sweeps bounded with
      Task.async_stream, max_concurrency derived live from two new
      Letflow.Admission accessors (global_cap/1, reserved_headroom/1)
      rather than a hardcoded literal -- both mirror the existing
      try_acquire/2/release/2 idiom, purely additive to Admission's
      state. Closes ISS-0421's remaining concern (unbounded serial-sweep
      latency); concerns 2/3 (platform-wide SPOF, default restart
      intensity) were already resolved by REQ-219/ISS-0451. 3 total
      rework cycles across design (2) and implementation-review (1),
      each catching a real correctness gap (a lockstep-config hazard, a
      phantom state-field read, a hardcoded concurrency bound that broke
      REQ-218's own tests under a tight pool config). Full suite
      3212/3214, 2 pre-existing rustc-toolchain-gap failures. See
      docs/issues/ISS-0421.yaml for full resolution detail.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T15:27:08Z"
    note: >
      WF03-ISS0459-20260904 resolved (queue task 459, GH#884, stage S6).
      letflow-queue had no release_lock status for a WF-03 instrumented/
      no_defect terminal verdict, so a run reaching that outcome had to
      either falsely assert resolved (status:"done", which best-effort
      closes the GitHub issue) or release with no status, leaving the
      task open/unlocked and immediately re-claimed by the very next
      get_next_task call under the LIFO issue-priority order -- a live
      infinite-loop hazard that hit queue task 458/ISS-0458 today and was
      worked around ad-hoc (released status:"done" anyway, explicitly
      flagged as not-a-real-fix in that run's own record).
      ISSUE-FIXER's diagnosis (independently re-verified twice more, by
      CODE-DESIGN-VALIDATOR and again by REVIEWER, all three reading
      letflow-queue's own source directly) found letflow-queue already
      ships a third status value, "blocked" (lib/letflow_queue/tasks/
      task.ex:18), already excluded from get_next_task's claim query
      (WHERE status = 'open' only) and already a passthrough for
      tap_maybe_close_github_issue/2 (fires only on literal "done") --
      so the mechanism ISS-0459 asked for already existed, zero
      letflow-queue code change needed. CODE-DESIGN-VALIDATOR FAILed the
      first design pass on citing letflow-queue's now-retired "exactly
      four operations" framing (superseded same-day by decision 0017,
      docs/migration/decisions/0017-task-queue-selection-model.md,
      "exactly one mutating claim path") as supporting reasoning;
      CODE-DESIGNER reworked to ground the documentation-only conclusion
      on non-retired grounds (0017 governs selection/claiming, disjoint
      from release_lock's status vocabulary) and re-passed. Fix, this
      repo only (letflow-queue repo untouched): docs/agents/protocols/
      TASK_QUEUE.md gains a full release_lock(status:"blocked") section
      matching the existing done/no-status rigor; docs/agents/protocols/
      ISSUE_QUEUE.md's instrumented and no_defect bullets each gain a
      cross-reference sentence; docs/agents/workflows/
      WF-03_issue_resolving.md Step 5's previously-unconditional
      release_lock(status:"done") instruction now branches on the local
      issue status (resolved -> done; instrumented/no_defect ->
      blocked), closing the actual proximate cause of ISS-0458's
      workaround. TEST-DESIGNER correctly returned SKIPPED (no lib/ or
      priv/ diff exists for a regression test to exercise; the
      underlying mechanism is already covered by letflow-queue's own
      pre-existing tests/letflow_queue/tasks_test.exs, quoted and
      verified in this run's own handoffs). REVIEWER PASSED (idiom,
      cross-file consistency, decision-0017 re-check against the
      implemented text, not just the design). Full pipeline: mix format
      clean, mix compile --warnings-as-errors clean, mix test 3217
      passed / 0 failed / 21 excluded. PR #886 merged (squash commit
      a6902425) after one structurally-cleared wasm_hang CI flake
      (identical commit passed on the concurrent push-triggered run;
      zero lib/priv touched by this docs-only PR; already tracked under
      ISS-0418, not re-filed). docs/issues/ISS-0459.yaml: status:
      resolved. GitHub issue #884 closed with mandated evidence comment
      stating plainly no code change was needed. Queue task 459 released
      status:"done". AC3 reconciliation executed as its own follow-on
      action once the design's safety claim was independently verified
      three times over: queue task 458 re-released status:"blocked"
      (was status:"done", the ad-hoc workaround this issue was filed
      against) -- confirmed safe (only a transition TO "done" triggers
      GitHub-close, so this did not reopen or re-close GH#882, itself
      independently re-verified still CLOSED after the call) and
      confirmed GitHub-visible: GH#882's own state is unaffected by a
      transition to "blocked". No letflow-queue repo commit, no deploy
      -- this fix needed neither.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T16:40:00Z"
    note: >
      WF03-ISS0441-20260904 done (queue task 441, GH#851, stage S6).
      Real GitHub branch protection now live on main (required checks:
      Backend gate, Frontend gate; enforce_admins: false), per decision
      record docs/migration/decisions/0018-branch-protection-posture.md
      (3 rework cycles, each a real correctness catch). Live-verified via
      a real scratch PR (#887): PENDING-rejection, a real organic BEHIND
      rejection, a real FAILURE/BLOCKED rejection caused by an actual
      live wasm_hang recurrence during the verification itself, and a
      successful --admin override past that FAILURE -- full quoted
      command output in 0018's own Verification log. Corrected
      0004-humanless-pipeline.md, GIT_MERGE.md (new 3-path Step 8), and
      ci.yml (required-status-check-contract comment). See
      docs/issues/ISS-0441.yaml for full resolution detail. First
      real-world test of the new protection is this same run's own
      Step Final merge.
  - req: SCOPE-CHANGE
    event: done
    agent: ORCH
    at: "2026-09-04T19:23:55Z"
    note: >
      WF03-ISS0467-20260904 resolved (queue task 467, GH#896, stage S6).
      Found live by ORCH immediately after ISS-0441/PR #895 configured
      branch protection: a bare `git push origin main` bypassed the new
      required-status-checks protection entirely, with only a quiet
      server-side "Bypassed rule violations" notice -- unlike `gh pr
      merge`'s own explicit --admin requirement, no distinct flag or
      confirmation was needed. ISSUE-FIXER's live diagnosis (gh api
      branch protection call, gh auth status/gh api user/permissions)
      confirmed GitHub's `restrictions` push-allowlist (Option 1) would
      be a structural no-op on this project: every git push, gh pr
      merge, and gh api call authenticates as the single admin identity
      tvolodi, so an allowlist naming that identity accepts the exact
      bypass it would need to reject. CODE-DESIGNER chose Option 2
      (documented prohibition) after independently checking for a
      distinguishing auth mechanism the diagnosis might have missed
      (deploy key, bot account, scoped PAT) -- none exists, and
      manufacturing one was judged out of scope for a MINOR issue. One
      rework cycle: CODE-DESIGN-VALIDATOR FAILed the first design pass
      for misattributing its acceptance-criteria mapping to a
      docs/issues/ISS-0467.yaml field that does not exist (the criteria
      actually came from the GitHub issue body / this run's own task
      framing -- an ORCH sourcing error in the original dispatch, not a
      CODE-DESIGNER defect); CODE-DESIGNER's narrow rework re-sourced
      the citation and re-passed on regate, with all 7 other
      independently-derived findings unchanged. Fix (commit bd6bddf5):
      docs/agents/protocols/GIT_MERGE.md gains an explicit
      no-direct-push-to-main-ever paragraph in its Precondition section;
      docs/agents/ORCHESTRATOR.md sec.10 gains a closing paragraph
      clarifying its sizing-rule exception licenses skipping the
      producer/validator review chain, never the branch+PR git
      mechanics; docs/migration/decisions/0018-branch-protection-posture.md
      gains a new dated "ISS-0467 resolution" subsection amending the
      record in place (0018 had already scoped and named this exact
      follow-up, so no new decisions/ record was opened). REVIEWER
      independently confirmed both insertions verbatim-match the
      approved design and that the new 0018 subsection is internally
      consistent with the existing Decision (a)-(d) section. Docs-only
      fix, no code/lib/priv/web touched -- no regression test exists;
      verification was the CODE-DESIGN-VALIDATOR and REVIEWER gates on
      the textual change itself. docs/issues/ISS-0467.yaml: status
      resolved. GitHub issue #896 closed with an evidence comment citing
      the run, the record, and the fix commit, explicitly stating no
      test exists for this docs-only change. Queue task 467 released
      status:"done". Step Final's own merge is the first real-world
      application of this run's own new prohibition -- via gh pr merge,
      never a direct push.
