{
  "handoff_id": "b3e2f1a0-0001-4a00-9c00-000000000004",
  "run_id": "WF02-REQ054-20260819",
  "workflow_id": "WF-02",
  "step": "02c",
  "from_agent": "ORCH",
  "to_agent": "SECURITY-REVIEWER",
  "file": "handoffs/WF02-REQ054-20260819/step-02c-security-reviewer.json",
  "created_at": "2026-08-19T04:10:00Z",
  "started_at": "2026-08-19T04:07:21Z",
  "completed_at": "2026-08-19T04:07:51Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": "S3",
    "requirement_ids": [
      "REQ-054"
    ],
    "requirement_text": {
      "REQ-054": "Port src/engine/snapshot_writer.zig (348 lines -- R-Co's ISS-601, design artefact src/design/iss601_state_snapshots.md) as Letflow.Engine.SnapshotWriter, plus its instance_state_snapshots migration and Ecto.Schema module (schema-per-tenant per REQ-022, same as every other table this stage adds). THIS REQUIREMENT EXTENDS REQ-053's already-shipped reconstruct path -- it is not a standalone module. REQ-053 builds full-log replay from sequence 1; this requirement adds the snapshot table, the writer, and the replay-source selection that lets that same reconstruct function start from the latest snapshot instead. The deliverable is therefore a modification of REQ-053's function plus new supporting code, and the correctness bar below is that both paths agree. Purpose: reconstruction (REQ-053) replaying the FULL event log from sequence 1 is O(events) and gets slower forever. A periodic snapshot of the InstanceState as a jsonb blob lets reconstruction replay only the events SINCE the latest snapshot. - take_snapshot/N: serialises REQ-044's InstanceState (status, tokens, variables, pending task nodes, error_detail) plus the sequence_number it is current as of, into an instance_state_snapshots row. - maybe_take_snapshot/N: the cadence check -- snapshot every N events, default 1000 per snapshot_writer.zig's DEFAULT_SNAPSHOT_INTERVAL. Called after event appends and on instance completion. - REQ-053's replay-source selection: given a latest snapshot at sequence S, replay only events with sequence_number > S on top of the deserialised snapshot state, versus replaying from 1 when no snapshot exists (reconstruction.zig's determineReplaySourceForSnapshot()). This requirement extends REQ-053's reconstruct path to use snapshots when present -- and the correctness bar is that BOTH paths produce the identical state, since a snapshot that diverges from full replay is worse than no snapshot at all. - This table is NOT the same thing as REQ-027/REQ-033's instance_definition_snapshots (the immutable PD-08 DEFINITION graph bound to an instance at start). These are periodic EXECUTION STATE snapshots, mutable in the sense that new rows accumulate over an instance's life. Say so explicitly in the moduledoc -- the two names are one word apart and confusing them would be a silent data-model error. OPEN QUESTION (flag, do not silently resolve): where the events-since-last-snapshot counter lives. instance_projections.last_event_seq (REQ-023) already tracks the latest sequence per instance, which makes a pure row-based cadence check possible with no in-memory counter at all; R-Co's own SnapshotWriter is a struct holding a pool handle. If REQ-045 resolved to supervised processes, an in-process counter is available but is exactly the kind of state stage-3-instance-engine.md's second Early finding warns is NOT automatically worth a process. Leave the choice to CODE-DESIGNER, citing that finding and REQ-045's resolution."
    },
    "related_handoff_ids": [
      "b3e2f1a0-0001-4a00-9c00-000000000000"
    ],
    "artifacts_in": [],
    "owned_modules": [
      "lib/letflow/engine/",
      "priv/repo/migrations/",
      "lib/letflow/design/"
    ]
  },
  "task": {
    "description": "Security gate on REQ-054's implementation: git diff main...HEAD on feature/WF02-REQ054-20260819. Adds a new schema-per-tenant table (instance_state_snapshots), a writer module, and a modification to Reconstruction's replay-source selection.",
    "acceptance_criteria": [
      "priv/repo/migrations/20260821000001_create_instance_state_snapshots.exs",
      "lib/letflow/engine/instance_state_snapshot.ex",
      "lib/letflow/engine/snapshot_writer.ex",
      "lib/letflow/engine/reconstruction.ex",
      "lib/letflow/tenant_provisioning.ex"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "Scope test: diff adds priv/repo/migrations/20260821000001_create_instance_state_snapshots.exs (new tenant-scoped table) plus lib/letflow/engine/instance_state_snapshot.ex (Ecto schema), lib/letflow/engine/snapshot_writer.ex (writer/reader module), a modification to lib/letflow/engine/reconstruction.ex (replay-source selection), and a registration entry in lib/letflow/tenant_provisioning.ex. Migration + tenant-scoped table => IN SCOPE. Assessed against INV-1..INV-8: INV-1 (tenant data isolation) APPLIES -> PASS. (a) prefix-scoping confirmed: migration wraps table/index creation in `if prefix() do ... end` with `prefix: prefix()` on create table/2 indexes (priv/repo/migrations/20260821000001_create_instance_state_snapshots.exs); SnapshotWriter.take_snapshot/4, maybe_take_snapshot/4, latest_snapshot/2 all require `prefix` in opts (Keyword.fetch!) and pass it through to every Repo.insert/Repo.one call, no default/public-schema code path exists. (b) migration reachable only via schema-per-tenant mechanism: registered in both @tenant_scoped_migration_manifest and the guard pattern, confirmed via lib/letflow/tenant_provisioning.ex diff (new entry {20_260_821_000_001, ..., \"20260821000001_create_instance_state_snapshots.exs\"} appended, entry count updated 27->28 in both places it's stated) -- both required halves present, matching the mandatory-registration pattern documented in the migration's own header and req022's design doc. (c) no tenant_id column: table has no tenant_id column at all (id/instance_id/sequence_number/state/taken_at only, confirmed in both the migration and lib/letflow/engine/instance_state_snapshot.ex schema block) -- per Decision 0006 D2 this is the now-standard shape for tables added since D2, so (c)'s caller-supplied-tenant_id concern is structurally inapplicable rather than skipped. Read-path prefix provenance also checked per the requirement's explicit ask: in lib/letflow/engine/reconstruction.ex, reconstruct_instance/2's `with` chain resolves/validates `prefix` via `TenantProvisioning.tenant_id_for_schema_name(prefix)` as its first step, before `select_replay_source(cast_instance_id, prefix)` and `read_full_log/3` ever run -- the same already-validated prefix flows into SnapshotWriter.latest_snapshot/2, so a snapshot can only ever be read from the resolved/validated tenant schema, never a caller-supplied unvalidated string; no alternate prefix source exists on this path. INV-2 (server-side field authorisation) NOT-APPLICABLE -- no API response type touched, S4 not started. INV-3 (untrusted runtime sandboxing) NOT-APPLICABLE -- no Lua/WASM path touched, S5 not started. INV-4 (secrets by reference only) APPLIES -> PASS -- diff introduces no config/env/secret resolution; `grep -rn \"System.get_env\" config/ lib/ --include=*.ex --include=*.exs` and the hardcoded-secret heuristic grep were re-run, no new hits inside the changed files (instance_state_snapshot.ex, snapshot_writer.ex, reconstruction.ex, the migration, tenant_provisioning.ex diff hunk). INV-5 (not-found/forbidden indistinguishability) NOT-APPLICABLE -- no lookup-by-ID endpoint, S4 not started. INV-6 (new data-access paths prove their scoping) APPLIES -> PASS -- this handoff is that proof, migration header and SnapshotWriter moduledoc both explicitly document INV-1/INV-ISS-4 compliance. INV-7 (no SQL string interpolation) APPLIES -> PASS -- `grep -rn \"Repo.query\" lib/ priv/repo/migrations/ --include=*.ex --include=*.exs` hits are all pre-existing, outside this diff (lib/letflow/sandbox_pool.ex, lib/letflow/tenant_provisioning.ex lines 164/178 unchanged by this diff, lib/letflow/sandbox_pool/fixture_loader.ex) -- zero Repo.query calls in the new/changed files (migration, instance_state_snapshot.ex, snapshot_writer.ex, reconstruction.ex); all snapshot table access goes through Ecto.Query (`where/order_by/limit` + Repo.one/Repo.insert), fully parameterised by construction. INV-8 (no unhandled crashes on realistic failure paths) APPLIES -> PASS -- `grep -rn \"^\\s*{:ok, .*} = \" lib/letflow/engine/snapshot_writer.ex lib/letflow/engine/reconstruction.ex lib/letflow/engine/instance_state_snapshot.ex lib/letflow/tenant_provisioning.ex` produced no bare-match hits in the changed files (the two tenant_provisioning.ex hits are pre-existing `case` clause heads, not bare assignments, and are outside this diff's hunks). take_snapshot/4 and maybe_take_snapshot/4 both return {:ok,_}|{:error,_} and every caller pattern-matches both branches (case ... {:ok,_} -> ...; {:error,_} = error -> error); latest_snapshot/2 exhaustively handles both nil and non-nil Repo.one/2 results; select_replay_source/2's moduledoc/comment correctly documents it as non-failing (SnapshotWriter.latest_snapshot/2's only two outcomes are both handled in its case), consistent with its unwrapped (non-tagged-tuple) @spec return type -- not a violation, a deliberate and accurate typing. No unresolved {:ok,_}= pattern found on any I/O-reachable path in this diff. All 4 APPLIES invariants (INV-1, INV-4, INV-6, INV-7... plus INV-8) PASS, no BLOCKER issues found.",
    "artifacts_out": [],
    "issues": [],
    "git_evidence": {
      "branch_name": "feature/WF02-REQ054-20260819",
      "commit_sha_list": [],
      "remote_branch": "origin/feature/WF02-REQ054-20260819",
      "push_status": "skipped",
      "pr_url": null,
      "pr_create_error": null
    },
    "next_action": "Route to REVIEWER (2d)"
  },
  "rework_count": 0,
  "max_rework": 3
}
