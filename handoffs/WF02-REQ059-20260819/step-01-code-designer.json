{
  "handoff_id": "c2f1a1a0-0001-4a00-9000-000000000059",
  "run_id": "WF02-REQ059-20260819",
  "workflow_id": "WF-02",
  "step": "01",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF02-REQ059-20260819/step-01-code-designer.json",
  "created_at": "2026-08-19T08:01:16Z",
  "started_at": "2026-08-19T08:01:16Z",
  "completed_at": "2026-08-19T09:20:00Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": "S3",
    "requirement_ids": ["REQ-059"],
    "requirement_text": {
      "REQ-059": "Port src/engine/pin_resolver.zig (967 lines, R-Co's PIN-01..PIN-04) as Letflow.Engine.PinResolver -- the mechanism that freezes every versioned dependency an instance uses at start time, so a running instance is never silently upgraded by a catalog change underneath it. resolve/N (PIN-01): enumerates every versioned reference in the definition snapshot and resolves each to a PinnedVersion entry {kind, ref, resolved_id, version, source} with kind in catalog_entry | variable_schema | module and source in resolved | override | inherited. Resolution completes BEFORE the instance row is written, so a failed resolution writes no partial instance (PIN-01 AC1/AC4). pinned_versions is ordered by kind then ref, so two starts of the same definition against the same catalog produce byte-identical payloads (PIN-01 AC5) -- deterministic ordering is load-bearing, not cosmetic. Initial variables violating the resolved variable_schema version are rejected with a distinct VariableSchemaViolation-equivalent listing each failing field (PIN-01 AC3), reusing REQ-024's validator. Recording (PIN-02): the resolved pin set is written as the pinned_versions field of the INSTANCE_STARTED event payload, appended in the SAME transaction as REQ-045's instance row insert. The EVENT LOG is the record of record -- no side table stores pins (PIN-02 AC3). If the INSTANCE_STARTED append fails, the whole thing rolls back so no committed instance has an unrecorded pin set (PIN-02 AC2). A definition with zero catalog and zero module references still records exactly one variable_schema entry (PIN-02 AC4). No fallback (PIN-03): a reference with no pin entry raises a distinct PinMissing error naming the reference, and NO code path substitutes the current active version for a missing or unresolvable pin (PIN-03 AC1/AC5). A newer catalog version published mid-flight does not affect an in-flight instance (PIN-03 AC2). R-Co's PIN-03 AC4 routes an exhausted PinMissing retry budget to the dead-letter queue -- OBS-05's DLQ is S6, the same gap REQ-056 flagged; leave a named hook rather than a partial DLQ. Replay and inheritance (PIN-04): REQ-053's reconstruction reads the effective pin set from INSTANCE_STARTED plus the most recent INSTANCE_PINS_REBOUND event, NEVER from a live catalog (PIN-04 AC1/AC5) -- this is what makes replay deterministic. merge_effective_pins/N computes that effective set. A sub-process child instance inherits its parent's pin set and adds entries only for references the parent does not carry, with inherited entries marked source: inherited (PIN-04 AC2); a child reference resolving to a version differing from an inherited pin uses the INHERITED pin and records the conflict in the child's INSTANCE_STARTED payload (PIN-04 AC3) -- inheritance wins, which is easy to get backwards. SCOPE BOUNDARY: GET /api/v1/instances/{id}/pins (PIN-04 AC4) is S4 (api-surface); this requirement exposes the effective-pin-set function the route will call, including the source event identifier per entry. SCOPE GAP -- carry R-Co's own scoping forward explicitly rather than re-discovering it: PIN-01 AC1 (service_catalog version resolution) and AC2 (module_ref resolution against PLC-01), and PIN-03 AC3 (execution proceeds on a pinned version after that version is DEPRECATED), are NOT satisfiable in Letflow, for the same reason they are unsatisfied in R-Co itself -- R-Co holds PIN-01 and PIN-03 at TESTED rather than RELEASED over exactly this, tracked there as ISS-0672/GH-306, because service_catalog has no version/status column and PLC-01 (the process module catalog) does not exist. In Letflow the gap is wider still: src/repository/ (service_catalog) is S6 and PLC-01 is unscoped to any stage. Scope this requirement to the variable_schema branch plus the resolution/ordering/recording/inheritance MACHINERY against an injectable catalog and module lookup -- the same pattern REQ-031 used for its identical missing-catalog gap -- and have unresolvable references return distinct UnresolvedCatalogRef / UnresolvedModuleRef errors rather than a silent no-op or a stub that pretends to resolve. Name this gap in the moduledoc, with the future stage each dependency belongs to."
    },
    "related_handoff_ids": ["handoffs/WF02-REQ059-20260819/step-00-git-setup.json"],
    "artifacts_in": ["handoffs/WF02-REQ059-20260819/step-00-git-setup.json"],
    "owned_modules": ["lib/letflow/engine/pin_resolver.ex", "lib/letflow/engine.ex (call sites only)"]
  },
  "task": {
    "description": "Design the PIN-01..04 dependency pin resolver: resolve/N's deterministic pinned-version enumeration against an injectable catalog/module/variable_schema lookup (mirroring REQ-031's already-shipped pattern for the identical missing-catalog gap), zero-partial-write integration into REQ-045's create/2 (pin resolution must complete before instance_definition_snapshots or any other row is written), variable_schema validation reusing REQ-024's JsonSchema.validate/2, PIN-03's no-fallback pin_for/3 accessor, and PIN-04's two distinct concerns -- merge_effective_pins/2 (replay-time INSTANCE_STARTED+INSTANCE_PINS_REBOUND fold, for REQ-053's reconstruction) and apply_inheritance/2 (instance-start-time child pin inheritance with conflict recording). Branch feature/WF02-REQ059-20260819 already exists and is checked out (Step 00 PASSED). Read lib/letflow/engine.ex's create/2 in full (the actual shipped shape, not the spec text), lib/letflow/definitions/service_scope_validator.ex (REQ-031's injectable-lookup pattern this must mirror), lib/letflow/event_store/registry.ex + json_schema.ex (the REQ-024 validator to reuse), lib/letflow/engine/reconstruction.ex (REQ-053's replay path this integrates with), and lib/letflow/definitions/promotion_plan.ex (the only existing precedent for which graph node attribute is a catalog vs. module reference) before designing. Explicitly document the SCOPE GAP (service_catalog/PLC-01 resolution deferred, citing R-Co's ISS-0672/GH-306 precedent) in the moduledoc section of the design.",
    "acceptance_criteria": [
      "resolve returns pinned_versions ordered by kind then ref, and two resolutions of the same definition against the same injected lookups produce byte-identical output, demonstrated by an explicit test comparing serialised payloads",
      "a failed resolution writes zero rows -- no instance_projections row, no instance_definition_snapshots row, no events row -- verified by reading all three back after a deliberately unresolvable reference",
      "initial variables violating the resolved variable_schema are rejected with an error listing each failing field, using REQ-024's validator rather than a second implementation",
      "the INSTANCE_STARTED event payload carries the pinned_versions entries, and a definition with zero catalog and zero module references still records exactly one variable_schema entry; no separate pin table exists anywhere in priv/repo/migrations",
      "a reference with no pin entry returns a distinct PinMissing error naming the reference, and no code path anywhere in the module queries a catalog for a current/latest version as a fallback -- confirmed by inspection and stated in the moduledoc",
      "a child instance started from a parent inherits the parent's pins with source: inherited, and a child reference resolving to a different version than an inherited pin uses the INHERITED version while recording the conflict in the child's INSTANCE_STARTED payload",
      "reconstruction via REQ-053 derives the effective pin set from INSTANCE_STARTED (plus INSTANCE_PINS_REBOUND when present) and issues zero reads against any catalog or module registry, demonstrated by an injected lookup that records whether it was called",
      "the moduledoc names the service_catalog (S6) and PLC-01 (unscoped) dependency gap explicitly, cites R-Co's own ISS-0672/GH-306 precedent for holding PIN-01/PIN-03 partially unmet, and states that unresolvable references return typed errors rather than stubs"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "REWORK PASS (post step-01b FAIL). Fixed both findings from handoffs/WF02-REQ059-20260819/step-01b-code-design-validator.json without otherwise changing the design's substance. (1) BLOCKING: section 3's `start_instance/5` block was a complete, real Elixir `with`-chain function body (defp start_instance(...) do ... end, real match patterns and control flow) -- rewritten as a 7-step numbered call-order specification naming each function called, its inputs, and its success/failure contract, with prose stating the short-circuit discipline instead of literal `with`/`case` code. The sibling defstruct-only Lookup block (now-unchanged, field-list only) remains the correct precedent this now matches. Also fixed section 4's `resolve/4` @spec, which still described a 2-tuple return -- corrected to the 3-tuple `{:ok, [pinned_version()], variable_json_schema}` form section 5's INV-PIN-5 and the new section 3 already required, and reworded section 5's stray 'section 3's pseudocode is corrected accordingly' sentence now that section 3 no longer contains pseudocode to correct. (2) MAJOR: PIN-03 AC4 ('leave a named hook rather than a partial DLQ' for an exhausted PinMissing retry budget) had zero corresponding design element -- added new section 12, stating no retry loop/budget/DLQ exists in this module, reserving the event-type name PIN_RETRY_EXHAUSTED as the documented attachment point a future stage-S6 DLQ integration hooks onto (citing REQ-056's identical gap and the same SCOPE GAP pattern already used for PIN-01 AC1/AC2 and PIN-03 AC3), and added moduledoc item 7 (section 2) plus a new row in section 10's AC-to-design-element table. Verified post-edit by grep for `^\\s*def \\|^\\s*defp \\|with {:ok` across the whole file -- zero matches, confirming no real Elixir function body remains anywhere in the document. All content the validator already independently confirmed correct (the start_instance/5 reordering premise itself, the six section 9 open questions, JsonSchema.validate/2 vs Registry.validate_payload/3, the PromotionPlan node-attribute precedent) was left untouched.",
    "artifacts_out": ["lib/letflow/design/req059-pin-resolver.md"],
    "issues": [],
    "git_evidence": {"branch_name": "feature/WF02-REQ059-20260819", "commit_sha_list": [], "remote_branch": "origin/feature/WF02-REQ059-20260819", "push_status": "pending", "pr_url": null, "pr_create_error": null},
    "next_action": "Route to CODE-DESIGN-VALIDATOR (rework re-check)"
  },
  "rework_count": 1,
  "max_rework": 3
}
