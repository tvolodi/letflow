{
  "handoff_id": "step01-WF02-REQ159-20260827",
  "run_id": "WF02-REQ159-20260827",
  "workflow_id": "WF-02",
  "step": "01",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF02-REQ159-20260827/step-01-code-designer.json",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": "S5",
    "requirement_ids": ["REQ-159"],
    "issue_ids": [],
    "requirement_text": {
      "REQ-159": "First of two sibling requirements splitting R-Co's eight-function platform.* surface along the read/write seam. This one covers the functions that only READ or EMIT and stage nothing: platform.read_variable (LUA-11's read half), platform.get_instance_state, and platform.log (LUA-13). platform.now already shipped in REQ-152, not re-implemented here. LUA-11 read half: 'platform.read_variable(name) MUST return the current value or nil.' LUA-13 (Logging, MUST): 'platform.log(level, message, context) MUST emit a structured log entry tagged with the script's identity, instance ID, and trace ID,' acceptance 'Log entry appears with correct correlation IDs.' NUMBER MARSHALLING IS NOT THIS REQUIREMENT'S TO DECIDE -- REQ-150 settles OQ-6 centrally (lib/letflow/design/req150-lua-number-marshalling.md) and names Letflow.Engine.LuaNumberMarshalling (lib/letflow/engine/lua_number_marshalling.ex, does NOT exist yet -- REQ-159 creates it per REQ-150's naming) with to_lua/1 and from_lua/1, both identity functions for numeric/nil cases. This requirement cites REQ-150's normative section by number and uses that conversion; it must not introduce a second rule. CAPABILITY GATES, from R-Co's host_api matrix: read_variable requires variable:read; get_instance_state requires instance:read; log requires audit:log. Each is checked via REQ-157's Capabilities.check!/3 at its own call site -- LUA-06's 'at call site' is structural, not decorative. TENANT BOUNDARY, per decision 0014 (e): neither runtime is given database access. Host functions receive already-resolved values; the tenant prefix is supplied by the calling engine code and is NEVER derived inside a script or read from a script-supplied argument. A read_variable implementation that reaches Letflow.Repo directly with a prefix taken from script input is a tenant-isolation defect, not an optimization. NOT IN THIS REQUIREMENT: platform.write_variable, call_service, emit_event (REQ-160); platform.fail (REQ-161). GATE: SECURITY-REVIEWER is a hard gate. Queue task 311, GH#611. See docs/requirements.yaml REQ-159 for full description and acceptance_criteria."
    },
    "source_text": {},
    "related_handoff_ids": [
      "handoffs/WF02-REQ159-20260827/step-00-git-setup.json"
    ],
    "artifacts_in": [
      "docs/requirements.yaml",
      "lib/letflow/design/req150-lua-number-marshalling.md",
      "lib/letflow/design/req157-lua-capability-model.md",
      "lib/letflow/engine/lua/platform.ex",
      "lib/letflow/engine/lua/capabilities.ex",
      "lib/letflow/instances.ex",
      "lib/letflow/event_store/instance_projection.ex",
      "docs/migration/decisions/0014-scripting-plugin-runtime-strategy.md"
    ],
    "owned_modules": [
      "lib/letflow/engine/lua/platform.ex",
      "lib/letflow/engine/lua_number_marshalling.ex",
      "test/letflow/engine/lua/platform_test.exs",
      "test/letflow/engine/lua_number_marshalling_test.exs",
      "lib/letflow/design/req159-lua-host-api-read.md",
      "test/specs/REQ-159.md"
    ],
    "branch_name": "feature/WF02-REQ159-20260827"
  },
  "task": {
    "description": "CODE-DESIGNER (WF-02 Step 1) for REQ-159. Write lib/letflow/design/req159-lua-host-api-read.md. Read lib/letflow/engine/lua/platform.ex in full (REQ-157's current state -- the 6 gated functions are currently stubs that raise a labeled 'not yet implemented' error after the capability check passes; this requirement replaces 3 of those 6 stubs -- read_variable, get_instance_state, log -- with real implementations, without touching the other 3 (write_variable, call_service, emit_event, which are REQ-160's) or now/fail). Read lib/letflow/design/req150-lua-number-marshalling.md's Section 3 in full (names Letflow.Engine.LuaNumberMarshalling, to_lua/1, from_lua/1 -- this module does not exist yet and REQ-159 must create it, per that design's own instruction that REQ-159/160 create it, not each inventing separately). Read lib/letflow/design/req157-lua-capability-model.md for Capabilities.check!/3's exact signature. Read lib/letflow/instances.ex (Letflow.Instances.get_by_id/2 -- the established pattern for reading an InstanceProjection by id with an explicit prefix, never derived from caller input) and lib/letflow/event_store/instance_projection.ex (confirm the :variables jsonb field and :status field shapes). Also read the current platform.ex's install/2 fold mechanism (from REQ-157/155/156) to understand exactly how a matrix row's stub gets replaced with a real implementation without disturbing the fold's single-registration-point invariant. Also write test/specs/REQ-159.md (test spec skeleton, no test code). Design must specify: (1) the real implementation for platform.read_variable(name) -- reads from the current execution's already-resolved instance variables map (NOT a fresh Repo query per call if the instance's variables are already available in the execution context passed into the Lua sandbox; if a fresh read IS needed, specify exactly how the instance_id and prefix reach this function without ever coming from the Lua script's own arguments), applying LuaNumberMarshalling.to_lua/1 to the returned value, returning nil for an unset variable; (2) the real implementation for platform.get_instance_state(instance_id) -- MUST use a caller-supplied prefix (never derived from script input), MUST return a structured error (not a raise propagating to the Lua caller as an unhandled exception) for an invalid/nonexistent instance id; (3) the real implementation for platform.log(level, message, context) -- emits a structured log entry (Logger.log/3 or similar) tagged with the script's identity, the instance ID, and the trace ID, sourced from the execution context, never from script-supplied identity claims; (4) LuaNumberMarshalling's to_lua/1 and from_lua/1 exactly as REQ-150 Section 3 specifies (identity functions for numeric/nil cases); (5) exactly how each of the 3 functions' capability check is wired into the existing REQ-157 install/2 fold (confirm the required_capability_fun for each of these 3 matrix rows already exists correctly from REQ-157 and doesn't need to change -- only the stub_fun changes); (6) exactly what execution-context value (instance id, trace id, prefix, script identity) is threaded from the caller into Platform.install/2 (or a new parameter) so these 3 functions have what they need without ever trusting script-supplied values for tenant-boundary-relevant data. No implementation code (.ex function bodies) -- signatures, @specs, and type shapes only, describing control flow in prose/tables, never as a literal Elixir expression, however small (this exact rule has cost 3 of the 4 prior Lua-hardening designs this session at least one rework round -- grep your own draft before submitting).",
    "acceptance_criteria": [
      "Design specifies real implementations for read_variable, get_instance_state, and log (not stubs), replacing exactly those 3 of the 6 gated matrix rows without touching write_variable/call_service/emit_event/now/fail",
      "Design specifies how the execution context (instance id, trace id, prefix, script identity) reaches these functions without ever trusting script-supplied values for tenant-boundary-relevant data -- explicit mechanism, not hand-waved",
      "Design creates lib/letflow/engine/lua_number_marshalling.ex per REQ-150 Section 3's exact naming (Letflow.Engine.LuaNumberMarshalling, to_lua/1, from_lua/1) and cites REQ-150's normative section by number rather than inventing a second conversion rule",
      "Design specifies get_instance_state returns a structured error (not an unhandled raise) for an invalid/nonexistent instance id",
      "Design specifies log emits a structured entry carrying script identity, instance ID, and trace ID",
      "Design confirms each function's capability requirement (variable:read, instance:read, audit:log) is unchanged from REQ-157's existing matrix rows -- only the stub_fun changes, not the required_capability_fun",
      "Design covers all 8 acceptance criteria listed in docs/requirements.yaml's REQ-159 entry (read it directly), mapped one-to-one to concrete design elements",
      "No implementation code (.ex function bodies) -- signatures, @specs, and type shapes only; describe control flow in prose/tables, never as a literal Elixir expression, however small -- grep your own draft for fn /raise /case /cond /do:/-> before submitting",
      "Open questions listed explicitly if any exist, not silently resolved"
    ]
  },
  "result": {
    "status": "PASS",
    "artifacts_out": [
      "lib/letflow/design/req159-lua-host-api-read.md",
      "test/specs/REQ-159.md"
    ],
    "next_action": "Route to CODE-DESIGN-VALIDATOR",
    "summary": "Wrote lib/letflow/design/req159-lua-host-api-read.md specifying real implementations for platform.read_variable, platform.get_instance_state, and platform.log, replacing exactly those 3 of Platform's 6 not-yet-implemented gated matrix rows (write_variable/call_service/emit_event untouched, REQ-160; now/fail untouched). Key decision: a new Platform.install/3 arity adds an `execution_context` map (instance_id, prefix, trace_id, script_identity, already-resolved variables) captured once and closed over by the installed wrappers -- the same closure-capture pattern REQ-155/156 use for `capabilities` -- so no tenant-boundary-relevant value ever comes from a Lua-call argument; install/1 and install/2 stay source-compatible by delegating to install/3 with an empty-context sentinel (design's §2.3.1), and the two rejected alternatives (host-injected hidden Lua argument; a Lua-visible context global) are recorded with reasons. read_variable reads directly from the closed-over already-resolved variables map (no per-call Repo query) and applies the new Letflow.Engine.LuaNumberMarshalling.to_lua/1 (created here per REQ-150 Section 3's naming, to_lua/1 and from_lua/1 both identity for numeric/nil cases, cited by section number). get_instance_state accepts a script-supplied instance_id but only ever queries with the context-supplied prefix (mirroring Letflow.Instances.get_by_id/2's cast-then-Repo.get pattern) and returns a two-value Lua result (nil, {reason: ...}) for invalid_id/not_found/no_execution_context rather than raising. log emits a Logger.log/3 entry tagged with script_identity/instance_id/trace_id sourced only from execution_context. required_capability_fun values (variable:read, instance:read, audit:log) confirmed unchanged from REQ-157's matrix -- only each row's stub tag changes. All 8 requirements.yaml acceptance criteria mapped 1:1 in the design's traceability table (Section 8). Two open questions recorded explicitly: OQ-1 (production wiring of a real, non-empty execution_context into Sandbox.new/1 or Executor is deferred, not built here, same pattern as REQ-158's grant-set wiring) and OQ-2 (no size bound proposed for log's context payload). Also wrote test/specs/REQ-159.md, a prose test-spec skeleton (13 numbered groups of planned test cases, no test code). Both files grepped for fn /raise /case /cond /do:/-> before submission; two genuine literal-code fragments found and rewritten to prose (an inline `fn args -> ... end` closure shape and a quoted `do: required` function clause); all remaining matches confirmed as prose false positives (e.g. \"case\", \"raise\" used as ordinary nouns, \"second\"/\"uppercase\" substring hits on \"cond\"/\"case\")."
  },
  "rework_count": 0,
  "max_rework": 3
}
