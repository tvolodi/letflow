{
  "handoff_id": "WF02-REQ172-20260828-step01b-recheck1",
  "run_id": "WF02-REQ172-20260828",
  "workflow_id": "WF-02",
  "step": "Step 1b (re-check, iteration 1)",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGN-VALIDATOR",
  "file": "lib/letflow/design/req172-wasm-host-api-write-path-and-parity-suite.md",
  "status": "PENDING",
  "priority": "normal",
  "context": {
    "note": "Queue task 324, GH#624, stage S5. CODE-DESIGNER reworked the design (commit 8106f3a) after your FAIL verdict on bbc9af9. Revised sections: SS2 (split into 2.1/2.2/2.3), SS5.2, SS5.3, SS7's invariant note, SS8.2, SS9.5, OQ-2 (now resolved), SS11's scenario checklist, Deliverables Summary. New claimed mechanism: (a) exit/1 does NOT crash the Wasmex instance process, confirmed live (Process.alive?(pid)==true after the call); (b) wasmex's Rust NIF discards the caught (false, results) pair and substitutes one FIXED generic message ('the elixir callback threw an exception') -- byte-identical for exit/1, raise, or throw -- so fail's own reason/details never survive Wasmex.call_function/4's return at all; (c) a genuine guest trap IS distinguishable by message shape (extra '(wasm trap: ...)' segment) but the design correctly treats that as too fragile to build AC5 on; (d) the real mechanism: do_fail/5 stashes %{reason:, details:} into its OWN process dictionary via Process.put/2 under a dedicated key immediately before exit/1; readable via Process.info(pid, :dictionary) by another process ONLY before the instance is torn down (reading after GenServer.stop/1 returns nil, also claimed live-verified); (e) PluginHandler.run_guest/3's existing unconditional GenServer.stop/1 (REQ-165, shipped) now applies to fail's path too since it returns normally like a trap -- call_export/3 must check the pdict signal BEFORE that stop call runs. YOU DID NOT COMPLETE 6 OF YOUR ORIGINAL 7 CHECKS LAST TIME (only the fail-mechanism check was done, and it failed) -- this re-check must be a FULL FRESH PASS covering all 7 original checks, not merely a re-check of SS5/SS8.2/SS9.5/SS2."
  },
  "task": {
    "description": "Independently re-validate the REVISED lib/letflow/design/req172-wasm-host-api-write-path-and-parity-suite.md (commit 8106f3a) against every REQ-172 acceptance criterion, as a full fresh pass. Specifically live-verify (do not trust the design's narrative): (1) that exit/1 inside a wasmex host callback genuinely does not crash the instance process and that the caught failure is genuinely reduced to one fixed generic message discarding reason/details -- read deps/wasmex's Rust NIF source (environment.rs/instance.rs, or wherever the callback-failure path is implemented) and/or run your own real repro against the installed wasmex 0.15.1, do not simply trust CODE-DESIGNER's repro description; (2) that Process.put/2 in do_fail/5's process combined with Process.info(pid, :dictionary) read from PluginHandler's process genuinely works as a distinguishing signal, and genuinely races correctly against GenServer.stop/1 -- verify the ordering guarantee (pdict read strictly before stop) is actually enforceable in the design's stated call sequence, not merely asserted; (3) that this mechanism actually makes fail distinguishable from BOTH a guest trap and an accidental Elixir bug inside a callback (a bug would also hit the same catch clause with the same generic message and no pdict signal -- confirm the design accounts for this, i.e. absence of the pdict signal correctly implies 'not a deliberate fail', not 'ambiguous'); (4) the write_variable staging/discard mechanism across all five failure arms (trap, fuel exhaustion, memory cap, wall-clock timeout, fail) for consistency now that fail's real discard path is known; (5) call_service's two-path distinguishability (structured error on service failure vs. instantiation-time capability denial); (6) the shared parity harness's exhaustiveness enforcement; (7) all non-ABI semantic differences in SS9 are genuinely enumerated and justified, and no Lua host-API file is proposed for modification anywhere. If any defect survives, FAIL with specifics; otherwise PASS explicitly.",
    "acceptance_criteria": [
      "ONE shared scenario definition is executed against BOTH the Lua executor and the WASM handler, asserting identical observable outcomes -- WASM-12's own acceptance criterion -- rather than two separately written suites; the shared harness is a single named module or fixture, quoted in the moduledoc",
      "a test asserts a guest that writes variables and then FAILS leaves instance variable state unchanged, asserted separately for each failure arm: a guest trap, fuel exhaustion (REQ-169), memory cap (REQ-169), wall-clock timeout (REQ-170), and fail",
      "a test asserts a guest that writes several variables and succeeds applies all of them with no observable partial state",
      "a test asserts call_service returns a structured error to the guest on service failure rather than trapping, and a separate test asserts a missing service capability fails per REQ-167 -- the two behaviours asserted distinctly",
      "a test asserts a guest that attempts to catch or ignore its own fail STILL yields a failed outcome and does not run to completion -- the WASM analogue of REQ-161's uninterceptable-termination test",
      "any semantic difference between the Lua and WASM host APIs that is not purely ABI (string encoding, return shape) is enumerated explicitly in the moduledoc with its justification; the moduledoc states that WASM-12 limits differences to ABI, so an unlisted one means the requirement is unmet",
      "the moduledoc states that the Lua host API is the definition WASM conforms to per decision 0014 Decision (4), and that no Lua-side behaviour was changed to ease parity; confirmed by `git diff --stat` scoped to this requirement's commits showing no Lua host-API implementation file modified",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
