{
  "handoff_id": "WF02-REQ172-20260828-step03",
  "run_id": "WF02-REQ172-20260828",
  "workflow_id": "WF-02",
  "step": "Step 3",
  "from_agent": "ORCH",
  "to_agent": "TEST-DESIGNER",
  "file": "lib/letflow/engine/wasm/host_api.ex",
  "status": "PENDING",
  "priority": "normal",
  "context": {
    "note": "Queue task 324, GH#624, stage S5. SECURITY-REVIEWER and REVIEWER both PASSED commit eed0e91 (implementation) with no blocking findings. Design: lib/letflow/design/req172-wasm-host-api-write-path-and-parity-suite.md (commit d8a4dd3, approved). take_staged_writes/0 is deliberately unwired (zero call sites) per design OQ-1, mirroring platform.ex's own already-accepted REQ-160 pattern -- your tests should call take_staged_writes/0 directly from the test process to assert %{} on every failure arm and the full accumulated map on success, mirroring platform_test.exs's own REQ-160 cross-process discipline (design SS11's test-strategy table is the authority for this). The shared parity harness scaffolding (test/support/host_api_parity.ex, Letflow.Test.HostApiParity, 7 scenarios: read_variable, log, now, uuid, write_variable, call_service, fail) already exists -- write the actual test bodies that USE it, plus the discard-arm and adversarial fixtures the design's Deliverables Summary assigns to this step (e.g. new .wat fixtures for guest_trap/fuel_exhaustion/memory_cap_fail/wallclock_timeout scenarios, following req171_host_api.wat / req172_host_api.wat's established pattern). This project's mutation-testing discipline applies: for each acceptance criterion, mutate the real implementation locally, confirm the pre-existing suite fails to catch it, add a closing test, then revert the mutation (verify via git diff/md5sum) before finishing."
  },
  "task": {
    "description": "Write ExUnit tests (and any needed .wat fixtures) satisfying every REQ-172 acceptance criterion below, using the shared parity harness in test/support/host_api_parity.ex and executing each shared scenario against BOTH the Lua executor and the WASM handler. Cover: write_variable's atomic-apply-on-success and discard-on-every-failure-arm (trap, fuel exhaustion REQ-169, memory cap REQ-169, wall-clock timeout REQ-170, fail) via take_staged_writes/0 inspection per the design's test-strategy table; call_service's two distinct failure paths (structured error on service failure vs. instantiation-time capability denial per REQ-167); fail's uninterceptability (a guest that tries to catch/ignore its own fail still yields a failed outcome and does not run to completion) -- the WASM analogue of REQ-161's uninterceptable-termination test. Enumerate and confirm any non-ABI semantic difference is documented in the moduledoc (design SS9) -- do not silently discover a new one without either fixing it or documenting it. Do not modify any file under lib/letflow/engine/lua/. Do not write test bodies yet beyond what's needed -- follow the design's SS11 test-strategy table as the authority for shape and mechanism. Apply mutation testing to each acceptance criterion per this project's standard discipline. Run mix test and mix compile --warnings-as-errors yourself, quote real output.",
    "acceptance_criteria": [
      "ONE shared scenario definition is executed against BOTH the Lua executor and the WASM handler, asserting identical observable outcomes -- WASM-12's own acceptance criterion -- rather than two separately written suites; the shared harness is a single named module or fixture, quoted in the moduledoc",
      "a test asserts a guest that writes variables and then FAILS leaves instance variable state unchanged, asserted separately for each failure arm: a guest trap, fuel exhaustion (REQ-169), memory cap (REQ-169), wall-clock timeout (REQ-170), and fail",
      "a test asserts a guest that writes several variables and succeeds applies all of them with no observable partial state",
      "a test asserts call_service returns a structured error to the guest on service failure rather than trapping, and a separate test asserts a missing service capability fails per REQ-167 -- the two behaviours asserted distinctly",
      "a test asserts a guest that attempts to catch or ignore its own fail STILL yields a failed outcome and does not run to completion -- the WASM analogue of REQ-161's uninterceptable-termination test",
      "any semantic difference between the Lua and WASM host APIs that is not purely ABI (string encoding, return shape) is enumerated explicitly in the moduledoc with its justification; the moduledoc states that WASM-12 limits differences to ABI, so an unlisted one means the requirement is unmet",
      "the moduledoc states that the Lua host API is the definition WASM conforms to per decision 0014 Decision (4), and that no Lua-side behaviour was changed to ease parity; confirmed by `git diff --stat` scoped to this requirement's commits showing no Lua host-API implementation file modified",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
