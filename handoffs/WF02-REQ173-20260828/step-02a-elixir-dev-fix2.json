{
  "handoff_id": "WF02-REQ173-20260828-step02a-fix2",
  "run_id": "WF02-REQ173-20260828",
  "workflow_id": "WF-02",
  "step": "Step 2a (fix, post-design-rework-2)",
  "from_agent": "ORCH",
  "to_agent": "ELIXIR-DEV",
  "file": "lib/letflow/engine/wasm/module_version_registry.ex",
  "status": "PENDING",
  "priority": "high",
  "context": {"note": "Queue task 325, GH#625, stage S5. The design (lib/letflow/design/req173-wasm-module-hot-reload.md, commit 721930a) was reworked and re-approved by CODE-DESIGN-VALIDATOR to fix a real defect: register_version/3 previously could never register a module requiring any host capability. Your prior implementation (commit 8d4157a, plus doc/name fix 4fd8183) needs updating to match the new SS5 mechanism. Key changes to implement: (1) register_version/3 first calls ModuleRegistry.register(bytes) unmodified; ONLY when that call's failure is specifically {:error, {:instantiation_failed, _}} (proving stage-1 ABI check already passed) does register_version/3 run its own capability-aware proof: CapabilityGate.build_import_table(manifest) + a bounded Task.Supervisor.async_nolink/2 instantiation attempt under the EXISTING ModuleVersionRegistryTaskSupervisor (no new supervisor), with its own @registration_instantiation_timeout_ms (design specifies 5_000ms) and Task.yield/2 classification (mirroring ModuleRegistry.instantiate/2's and CapabilityGate.start_instance/2's own already-shipped shape), GenServer.stop'ping the proving instance unconditionally on success since it's a throwaway proof; (2) version_entry()/version_snapshot() now store bytes :: binary() directly instead of ModuleRegistry.registered_module() -- update the struct/map shape and every place that read .module or .bytes off the old type; (3) invoke/4's own instantiation (already using CapabilityGate.build_import_table + Wasmex.start_link with imports) should now read snapshot.bytes directly, no change needed there beyond the field rename if applicable. TEST-DESIGNER's existing test suite (test/letflow/engine/wasm/module_version_registry_test.exs, commit f8ec561) and fixtures (req173_v1_gated.wat, req173_v2_gated.wat, req173_v1_capless.wat, req173_v2_capless.wat) already exist and encode the target behavior -- the core blocked test should now pass once your fix lands; do not modify the test file yourself, just make the implementation satisfy it (if the test needs a genuine adjustment because your fix's exact error/return shapes differ from what TEST-DESIGNER guessed, note it explicitly and keep the change minimal)."},
  "task": {
    "description": "Update lib/letflow/engine/wasm/module_version_registry.ex per the design's reworked SS5 (register_version/3's two-step validation) and SS2 (bytes-based data model). Read the full current design doc (commit 721930a) before editing -- SS5.3/SS5.4 give the exact mechanism and worked trace. Do NOT modify lib/letflow/engine/wasm/module_registry.ex, capability_gate.ex, or plugin_handler.ex. Run mix compile --warnings-as-errors and mix test test/letflow/engine/wasm/ yourself and quote real output -- specifically confirm the previously-blocked core overlapping-activation scenario test in module_version_registry_test.exs now passes.",
    "acceptance_criteria": [
      "a test holds an invocation open (a guest blocked in a controllable host call), activates a new module version DURING it, and asserts the held invocation completes normally against the OLD version's observable behaviour -- WASM-14's own acceptance criterion, with the overlap real rather than sequential",
      "the same test asserts an invocation started AFTER the activation observes the NEW version's behaviour, so both halves of WASM-14 are proven in one overlapping scenario",
      "the two module versions used are observably different in output, so the test can distinguish which version served an invocation rather than merely completing",
      "a test asserts the prior version's resources are released only after its last in-flight invocation completes, not at activation time",
      "a test asserts that when the new version's manifest declares a DIFFERENT capability set, an in-flight invocation of the prior version keeps the prior version's capability set, and the new invocation gets the new one -- so activation cannot silently widen or narrow a running invocation's authority",
      "mix compile --warnings-as-errors passes with real output quoted",
      "mix test test/letflow/engine/wasm/ passes with real output quoted, including the previously-blocked core scenario test",
      "a module requiring at least one host capability can be successfully registered via register_version/3 -- verified by the actual test passing, not merely asserted"
    ]
  },
  "result": null
}
