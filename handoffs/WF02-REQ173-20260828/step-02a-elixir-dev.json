{
  "handoff_id": "WF02-REQ173-20260828-step02a",
  "run_id": "WF02-REQ173-20260828",
  "workflow_id": "WF-02",
  "step": "Step 2a",
  "from_agent": "ORCH",
  "to_agent": "ELIXIR-DEV",
  "file": "lib/letflow/design/req173-wasm-module-hot-reload.md",
  "status": "PENDING",
  "priority": "normal",
  "context": {"note": "Queue task 325, GH#625, stage S5. Design APPROVED by CODE-DESIGN-VALIDATOR (commit c7d10ea) after one rework round -- the outer timeout-bounding mechanism for invoke/4 was independently live-verified: Wasmex.call_function/4's own GenServer.call timeout crashes with exit({:timeout, {GenServer, :call, [server, request, timeout]}}) (traced through real gen_server.ex/gen.erl source); Process.monitor/:DOWN fires unconditionally for any termination reason (normal, uncaught exit, or :killed via Task.shutdown(:brutal_kill)); Task.yield/2 is exhaustively {:ok, reply}|{:exit, reason}|nil for a locally-spawned Task.Supervisor task. Design adds a NEW module, Letflow.Engine.Wasm.ModuleVersionRegistry, composing ModuleRegistry (REQ-166)/CapabilityGate (REQ-167)/PluginHandler (REQ-165/170/171/172)'s existing public functions rather than modifying those already-gate-approved modules -- read the full design doc before writing any code. Read lib/letflow/engine/wasm/module_registry.ex, capability_gate.ex, plugin_handler.ex, and lib/letflow/engine/plugin_interface.ex (the pattern decision 0014 mandates invoke/4 mirror: async_nolink -> Task.yield/2 -> Task.shutdown(:brutal_kill))."},
  "task": {
    "description": "Implement REQ-173 exactly as designed in lib/letflow/design/req173-wasm-module-hot-reload.md: new module Letflow.Engine.Wasm.ModuleVersionRegistry (a supervised singleton GenServer per the design), with register_version/3, activate/2, invoke/4 (the checkout-before-mutation + reference-counted release + outer/inner timeout classification mechanism the design specifies in full), and any release/monitor bookkeeping the design's SS4/SS6 specify. Add the GenServer to this project's supervision tree per the design's own placement guidance. Do not modify lib/letflow/engine/wasm/module_registry.ex, capability_gate.ex, or plugin_handler.ex unless the design explicitly calls for a specific, named change to one of them -- the design's stated approach is composition via their existing public functions, not modification. Do not write test files yet beyond what the design assigns to this step (check the design's own Deliverables/Files summary). Run mix compile --warnings-as-errors and mix test yourself and quote real output; do not claim success without running it.",
    "acceptance_criteria": [
      "a test holds an invocation open (a guest blocked in a controllable host call), activates a new module version DURING it, and asserts the held invocation completes normally against the OLD version's observable behaviour -- WASM-14's own acceptance criterion, with the overlap real rather than sequential",
      "the same test asserts an invocation started AFTER the activation observes the NEW version's behaviour, so both halves of WASM-14 are proven in one overlapping scenario",
      "the two module versions used are observably different in output, so the test can distinguish which version served an invocation rather than merely completing",
      "a test asserts the prior version's resources are released only after its last in-flight invocation completes, not at activation time",
      "a test asserts that when the new version's manifest declares a DIFFERENT capability set, an in-flight invocation of the prior version keeps the prior version's capability set, and the new invocation gets the new one -- so activation cannot silently widen or narrow a running invocation's authority",
      "mix compile --warnings-as-errors passes with real output quoted",
      "mix test passes with real output quoted (existing suite; new tests are TEST-DESIGNER's step)"
    ]
  },
  "result": null
}
