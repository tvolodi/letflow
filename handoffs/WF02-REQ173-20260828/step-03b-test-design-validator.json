{
  "handoff_id": "WF02-REQ173-20260828-step03b",
  "run_id": "WF02-REQ173-20260828",
  "workflow_id": "WF-02",
  "step": "Step 3b",
  "from_agent": "ORCH",
  "to_agent": "TEST-DESIGN-VALIDATOR",
  "file": "test/letflow/engine/wasm/module_version_registry_test.exs",
  "status": "PENDING",
  "priority": "normal",
  "context": {"note": "Queue task 325, GH#625, stage S5. SECURITY-REVIEWER and REVIEWER both PASSED commit 77c1d1e (full fresh checks). ORCH independently ran mix test test/letflow/engine/wasm/module_version_registry_test.exs --trace: 17/17 passed. IMPORTANT: TEST-DESIGNER's original mutation-testing pass (documented in test/specs/REQ-173.md, 4 mutants against commit f8ec561's version of module_version_registry.ex) was performed BEFORE ELIXIR-DEV's fix (commit 77c1d1e) added the new private prove_capability_instantiation/2 function and reworked register_version/3's validation logic. That new function and logic path have NO documented mutation-testing coverage yet. The three tests TEST-DESIGNER originally wrote for capability-gated registration were also rewritten by ELIXIR-DEV (from asserting the old, defective {:error, {:instantiation_failed, ...}} outcome to the correct {:ok, _version_id} outcome) -- REVIEWER confirmed this rewrite was narrow and legitimate, but did not re-run mutation testing against it."},
  "task": {
    "description": "Hard gate: verify every REQ-173 acceptance criterion has a genuine, non-skipped test (re-verify all 6, not just the ones affected by the recent fix). Then specifically address the mutation-testing gap: independently mutate the NEW prove_capability_instantiation/2 function and/or register_version/3's new branching logic (e.g. invert the {:error, {:instantiation_failed, _}} pattern match so it never triggers the capability-aware retry, or make the capability-aware proof always report success regardless of actual instantiation outcome) and confirm the current test suite (specifically the 3 capability-gated registration tests) actually catches at least one such mutant -- do not just trust TEST-DESIGNER's original 4-mutant report, which predates this code path. Revert any mutation you apply and verify the revert (git diff/md5sum) before finishing. Also confirm the shared parity/isolation assertions (SS8.4's overlapping scenario) still hold their full evidentiary weight post-fix -- re-read the test to confirm it wasn't weakened. Run mix test and mix compile --warnings-as-errors yourself and quote real output.",
    "acceptance_criteria": [
      "a test holds an invocation open (a guest blocked in a controllable host call), activates a new module version DURING it, and asserts the held invocation completes normally against the OLD version's observable behaviour -- WASM-14's own acceptance criterion, with the overlap real rather than sequential",
      "the same test asserts an invocation started AFTER the activation observes the NEW version's behaviour, so both halves of WASM-14 are proven in one overlapping scenario",
      "the two module versions used are observably different in output, so the test can distinguish which version served an invocation rather than merely completing",
      "a test asserts the prior version's resources are released only after its last in-flight invocation completes, not at activation time",
      "a test asserts that when the new version's manifest declares a DIFFERENT capability set, an in-flight invocation of the prior version keeps the prior version's capability set, and the new invocation gets the new one -- so activation cannot silently widen or narrow a running invocation's authority",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
