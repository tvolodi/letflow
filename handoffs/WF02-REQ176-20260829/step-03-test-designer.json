{
  "handoff_id": "WF02-REQ176-20260829-step03",
  "run_id": "WF02-REQ176-20260829",
  "workflow_id": "WF-02",
  "step": "Step 3",
  "from_agent": "ORCH",
  "to_agent": "TEST-DESIGNER",
  "file": "lib/letflow/dlq.ex",
  "status": "PENDING",
  "priority": "normal",
  "context": {"note": "Queue task 351, GH#696, stage S6. SECURITY-REVIEWER and REVIEWER both PASSED commit b5a028d with no blocking findings. REVIEWER filed ISS-0353 (a real but correctly-deferred idiom gap in Entry.insert_changeset/2's cast list -- not exploitable today since enqueue/2 pre-sanitizes attrs, not something you need to fix). Design: lib/letflow/design/req176-dlq-core.md (commit 1959adf, approved after 1 rework round). This is a new, greenfield module -- Letflow.Dlq context + Letflow.Dlq.Entry schema + a new tenant-scoped migration."},
  "task": {
    "description": "Write ExUnit tests satisfying every REQ-176 acceptance criterion below. Cover: enqueue/1 creates a pending entry with retry_count 0 and a created_at timestamp; list/2 is genuinely tenant-scoped (entry enqueued under tenant A absent from tenant B's list/2); retry/2's four starting-status cases (pending->retrying, retrying->retrying, resolved->conflict-unchanged, discarded->conflict-unchanged), each asserting the exact DlqRetryAttempt field names (attempt_no/attempted_at/outcome/error_message) and retry_count increment; discard/2's terminal-transition and already-terminal-conflict cases; list/2's independent entry_type and status filtering (against a result set containing more than one value of each) plus cursor pagination (first page's next_cursor, fed back, returns the next distinct page with no repeated/skipped ids). Also assert no route/controller file exists (a structural check, e.g. via git diff --stat or a directory-listing assertion, matching this project's existing 'no X was added' assertion style). Apply this project's mutation-testing discipline: for each acceptance criterion, mutate the real implementation locally, confirm the pre-existing suite fails to catch it, add a closing test, then revert the mutation and verify the revert (git diff/md5sum) before finishing. Run mix format on touched files, then mix compile --warnings-as-errors and mix test yourself, quote real output.",
    "acceptance_criteria": [
      "the dlq_entries table lives inside each tenant's Postgres schema with a tenant_id column retained, matching decision 0003 Decision B's pattern -- verified by inspecting the migration",
      "enqueue/1 creates an entry with status \"pending\", retry_count 0, and a created_at timestamp; list/2 is tenant-scoped, demonstrated by an entry enqueued under tenant A being absent from a list/2 call scoped to tenant B",
      "retry/2 on a \"pending\" or \"retrying\" entry transitions it to \"retrying\", appends a retry_history entry with attempt_no/attempted_at/outcome/error_message field names matching DlqRetryAttempt exactly, and increments retry_count; retry/2 on a \"resolved\" or \"discarded\" entry returns a conflict error and leaves status, retry_count and retry_history unchanged -- four tests, one per starting status",
      "discard/2 on a \"pending\" or \"retrying\" entry transitions it to \"discarded\"; discard/2 on an already \"resolved\" or \"discarded\" entry returns a conflict error rather than silently succeeding",
      "list/2 filters by entry_type and by status independently, each demonstrated narrowing a result set that contains rows of more than one value, and supports cursor pagination: given more rows than one page, the first page's next_cursor, passed back in, returns the next distinct page with no repeated or skipped ids",
      "no route or controller file is added or modified, confirmed by git diff --stat scoped to this requirement's commits",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
