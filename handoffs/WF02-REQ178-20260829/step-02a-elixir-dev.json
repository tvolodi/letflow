{
  "handoff_id": "WF02-REQ178-20260829-step02a",
  "run_id": "WF02-REQ178-20260829",
  "workflow_id": "WF-02",
  "step": "Step 2a",
  "from_agent": "CODE-DESIGN-VALIDATOR",
  "to_agent": "ELIXIR-DEV",
  "file": "lib/letflow/design/req178-dlq-routes.md",
  "status": "PENDING",
  "priority": "normal",
  "context": {
    "note": "Queue task 353, GH#698, stage S6, branch feature/WF02-REQ178-20260829 (pushed). CODE-DESIGN-VALIDATOR passed lib/letflow/design/req178-dlq-routes.md with zero rework rounds (see handoffs/WF02-REQ178-20260829/req178-design-validation-report.md for the full independent-verification record: policy key, permission matrix, Letflow.Dlq signatures, DlqEntry field parity, and absence of code fences were all re-derived from lib/letflow/api/authorization.ex, lib/letflow/dlq.ex, lib/letflow/dlq/entry.ex, web/src/types/api.ts, lib/letflow/api/response.ex, and lib/letflow/api/authorized_router.ex directly). Implement the design exactly as written -- it is the approved contract, not a suggestion. Two flagged open questions in the design's own §7 require a decision from you (record which you picked and why in your commit/PR description, do not silently pick one without noting it): OQ-1 (no has_more key in the list body -- design says do NOT add one unless you find dlqApi.list's actual frontend usage reads it), OQ-2 (item_type/original_payload/processor_metadata/max_retries are NOT emitted -- no schema column backs them), OQ-3 (:invalid_id on GET/:id, retry, discard folds to 404, diverging from tasks.ex's 400-for-invalid_id precedent, because DLQ ids are cross-tenant-probeable UUIDs -- design's own recommended answer, follow it unless you find a concrete reason not to).",
    "requirement_text": "Third and last requirement of this batch's DLQ half -- the route layer atop REQ-176's core and REQ-177's landing sites. Build exactly to the already-shipped SPA consumer contract (web/src/api/dlq.ts's dlqApi object, web/src/types/api.ts's DlqEntry/CursorPage<T>): GET /api/v1/dlq (list, filters status/source_type-mapped-to-entry_type/search/instance_id/cursor/page_size), GET /api/v1/dlq/:id, POST /api/v1/dlq/:id/retry, POST /api/v1/dlq/:id/discard. Every route requires the DlqOperate permission via the existing :DlqReadRetryDiscard policy key (REQ-069's matrix, unchanged). Tenant scoping follows REQ-072's cross-tenant-404 mechanism structurally, via Letflow.Dlq's own prefix-scoped Repo calls -- no new mechanism in the router. NOT IN THIS REQUIREMENT: no change to Letflow.Dlq, Letflow.Dlq.Entry, or the dlq_entries migration (REQ-176/177 already shipped and gate-approved)."
  },
  "task": {
    "description": "Implement lib/letflow/design/req178-dlq-routes.md exactly: create lib/letflow/routers/dlq.ex (defmodule Letflow.Routers.Dlq, use Letflow.Api.AuthorizedRouter, four authz-declared routes GET '/', GET '/:id', POST '/:id/retry', POST '/:id/discard', plus a plain match fallback), add the one new forward(\"/dlq\", to: Letflow.Routers.Dlq) line to lib/letflow/plugs/api_pipeline.ex's existing forward list (lib/letflow/plugs/api_pipeline.ex:57-66), and write the tests needed to satisfy AC1-AC4 and AC6 below (this design step itself does not run mix test/mix compile -- that is this step's job). Follow the design's §3 query-param mapping table, §3 DlqEntry JSON serialization allowlist (hand-built, never a raw Jason.Encoder derivation over the Ecto struct), §4/§6 error-mapping (404 for not_found/invalid_id, 409 for {:error, {:invalid_state, _}} via Response.conflict/2 with a fixed non-interpolated detail string, 400 for bad page_size/cursor, 422 for invalid_filter), and §0's moduledoc-mandated 'Contract source' section stating R-Co's dlq.zig was not inspected. Do not invent any new permission/policy-key atom -- :DlqReadRetryDiscard and :DlqOperate already exist in lib/letflow/api/authorization.ex, confirmed by CODE-DESIGN-VALIDATOR.",
    "acceptance_criteria": [
      "GET /api/v1/dlq returns a body shaped exactly {items: [...], next_cursor: string|null}, and each item's field names (id, entry_type, instance_id, reason, full_reason, retry_count, status, created_at at minimum) match DlqEntry's, verified against web/src/types/api.ts rather than a hand-invented shape",
      "GET /api/v1/dlq honors status, source_type, search, cursor and page_size independently, each with its own test demonstrating the filter narrows the result set",
      "POST /api/v1/dlq/:id/retry and .../discard both require the DlqOperate permission from REQ-069's matrix -- a caller holding no role with that permission receives 403, and a caller from a different tenant naming a real id belonging to tenant A receives 404, never 403 -- three explicit tests",
      "POST /api/v1/dlq/:id/retry and .../discard against a non-existent id return 404, and against an id already \"resolved\" or \"discarded\" return a 409-shaped conflict rather than a generic 500 -- covering the not-found and terminal-state cases each with their own test",
      "the route module's moduledoc states explicitly that R-Co's dlq.zig was not inspected in this batch (unreachable from the drafting environment) and that web/src/api/dlq.ts / web/src/types/api.ts were the binding contract instead, so a future reader does not assume R-Co parity was verified when it wasn't",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ],
    "artifacts_in": ["lib/letflow/design/req178-dlq-routes.md"],
    "artifacts_out": ["lib/letflow/routers/dlq.ex", "lib/letflow/plugs/api_pipeline.ex"],
    "next_action": "On completion -> SECURITY-REVIEWER (this touches a tenant-data-path API route) -> REVIEWER -> TEST-DESIGNER/TEST-DESIGN-VALIDATOR if further coverage is needed -> RELEASE-VALIDATOR. Quote real mix test / mix compile --warnings-as-errors output in your handoff -- no speculation about whether it would pass."
  },
  "result": null
}
