{
  "handoff_id": "WF02-REQ182-20260829-step03",
  "run_id": "WF02-REQ182-20260829",
  "workflow_id": "WF-02",
  "step": "Step 3",
  "from_agent": "REVIEWER",
  "to_agent": "TEST-DESIGNER",
  "file": "lib/letflow/routers/webhooks.ex",
  "status": "PENDING",
  "priority": "normal",
  "context": {
    "note": "REVIEWER PASS on REQ-182 (queue task 355, GH#700, stage S6). Full report at handoffs/WF02-REQ182-20260829/reviewer-report-req182.md. Idiom, supervision, scope-creep, decision-record-consistency, and design-vs-implementation fidelity all checked directly against the real diff (lib/letflow/routers/webhooks.ex new, lib/letflow/api/authorization.ex one PATCH clause, lib/letflow/plugs/api_pipeline.ex one forward line) -- no scope creep found (git diff main...HEAD --stat confirmed no changes to lib/letflow/webhooks.ex, lib/letflow/webhooks/subscription.ex, lib/letflow/dlq.ex, or any REQ-176/177/178/181 test file). Router mirrors lib/letflow/routers/dlq.ex's idiom exactly (AuthorizedRouter macros, hand-built JSON allowlist, cross-tenant-404 folding).",
    "stale_test_fixed": "test/letflow/webhooks_test.exs's AC6 describe block had a stale REQ-181-era assertion ('no router file for webhooks exists anywhere') that REQ-182 correctly and intentionally falsifies -- same class as REQ-176's AC6 test breaking on REQ-178 (docs/anti-patterns.md). Fixed by REVIEWER directly, following TEST-DESIGN-VALIDATOR's precedent fix on REQ-178/dlq_test.exs verbatim: removed the false structural assertion, kept the still-true narrower claim (Letflow.Webhooks/Subscription themselves have no route/controller-shaped constructs), retitled the describe block, added a NOTE explaining the history, and updated the file's top moduledoc. Verified: `mix test test/letflow/webhooks_test.exs` -> 13 passed. `mix compile --warnings-as-errors` -> clean, no output. This fix is included in the current working tree (uncommitted -- TEST-DESIGNER or the next committing step should include test/letflow/webhooks_test.exs's diff in its commit).",
    "design_doc": "lib/letflow/design/req182-webhooks-routes.md",
    "precedent_router": "lib/letflow/routers/dlq.ex (REQ-178)",
    "context_module": "lib/letflow/webhooks.ex / lib/letflow/webhooks/subscription.ex (REQ-181, unchanged by this requirement)"
  },
  "task": {
    "description": "Write mutation-driven test coverage for REQ-182's HTTP route layer (lib/letflow/routers/webhooks.ex), against the original acceptance criteria in docs/requirements.yaml's REQ-182 entry:",
    "acceptance_criteria": [
      "POST /api/v1/webhooks/subscriptions with no secret supplied returns a 2xx response whose body includes hmac_secret_once carrying the plaintext secret exactly once; a subsequent GET/list of the same subscription never includes the plaintext secret or an hmac_secret_once field at all -- two explicit tests",
      "PATCH /api/v1/webhooks/subscriptions/:id accepts {status: \"PAUSED\"} and, separately, {is_active: false}, and both result in a subscription that subsequently reads back as PAUSED via GET/list",
      "every subscription route requires the WebhooksManage permission from REQ-069's matrix -- a caller lacking it receives 403, and a caller from a different tenant naming a real subscription id belonging to tenant A receives 404, never 403",
      "GET /api/v1/webhooks/subscriptions returns {items: [...]} with each item's target_url, event_types, status and created_at present, matching webhooksApi.list's consumer shape",
      "DELETE /api/v1/webhooks/subscriptions/:id removes the subscription such that a subsequent list no longer includes it, and a second DELETE of the same id returns 404 rather than a duplicate success",
      "the route module's moduledoc states explicitly that R-Co's webhooks.zig was not inspected in this batch (unreachable from the drafting environment) and that web/src/api/dlq.ts / web/src/types/api.ts were the binding contract instead"
    ],
    "notes": "This is the HTTP-layer test file for REQ-182 -- expected to live alongside or as a new test/letflow/routers/webhooks_test.exs (mirroring test/letflow/routers/dlq_test.exs's own shape, if that file exists as a precedent -- check before inventing a new layout). test/letflow/webhooks_test.exs (context-module level, REQ-181) is out of scope for new coverage here beyond the stale-assertion fix REVIEWER already applied; do not duplicate REQ-181's own context-level tests at the route layer. Do not run mix test yourself if not required by your role -- TEST-DESIGN-VALIDATOR and TEST-RUNNER own that."
  },
  "result": null
}
