{
  "handoff_id": "WF02-REQ186-20260829-step01",
  "run_id": "WF02-REQ186-20260829",
  "workflow_id": "WF-02",
  "step": "Step 1",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "docs/requirements.yaml#REQ-186",
  "status": "PENDING",
  "priority": "normal",
  "context": {"note": "Queue task 359, GH#705, stage S6. Feature branch feature/WF02-REQ186-20260829 created off main at 747782e. Read the full REQ-186 entry in docs/requirements.yaml (around line 9624) in full -- it is long and dense, carrying exact schema columns, CHECK constraints, config defaults, and citations. Read lib/letflow/design/req185-scheduler-firing-architecture.md in full -- this IS the architecture you must implement, not a suggestion: GenServer ticker, FOR UPDATE SKIP LOCKED claim mechanism, no startup-sweep lock, per-tenant-schema iteration, timer-exhausted-retry lands in dlq_entries with entry_type \"timer\". Read lib/letflow/dlq.ex and its migration (REQ-176) for the tenant-scoped-schema migration idiom, the row-lock-then-check Ecto.Multi idiom, and how enqueue/2 is called for the DLQ-landing behavior this design must reuse. Read lib/letflow/engine/execution_error.ex (REQ-177) for a second precedent of how an Ecto.Multi step composes with an existing event-append flow. Read lib/letflow/engine/transition.ex around L294's :TIMER catch-all dispatch (do not change it -- that's REQ-187 -- but understand what it currently does). Read lib/letflow/definitions/graph.ex's check_timer_duration/1 (CHK-12) to understand the duration_iso8601 attribute already validated at definition time. Read REQ-025's event-append path (find it via grep for how other event types call EventStore/append, e.g. lib/letflow/engine/execution_error.ex's own append call) and REQ-023's event_type_registry registration pattern (find an existing migration or module that registers a new event type the way this requirement's scope item 6 requires)."},
  "task": {
    "description": "Produce the design artefact (lib/letflow/design/req186-scheduler-core.md or similar) for REQ-186's full scope: (1) the timers Ecto schema + tenant-scoped migration with the exact columns/CHECK constraints/partial index docs/requirements.yaml's REQ-186 entry specifies verbatim -- do not invent different column names or constraint shapes; (2) a context module with create/2 (accepting a caller-supplied Ecto.Multi or repo so REQ-187 can enlist it in the same transaction as the state transition that arms the timer) and a poll-and-fire function using REQ-185's FOR UPDATE SKIP LOCKED claim mechanism, one transaction per timer marking it fired + appending its event; (3) missed-timer recovery (fired_late: true plus scheduled-vs-actual timestamps when late); (4) failure accounting matching R-Co's ISS-303/ISS-0618 fix exactly -- a raising fire attempt rolls back only that timer's transaction, increments fire_error_count in a SEPARATE transaction, does NOT stop the poll cycle (remaining due timers in the same cycle still attempted), and on reaching max fire retries transitions the timer to failed + lands it in dlq_entries with entry_type \"timer\" via Letflow.Dlq.enqueue/2; (5) configuration (poll interval 5000ms, jitter 0ms applied only to interval never fire_at, max timers per cycle 64, max fire retries 3, all overridable via application config); (6) registering the timer-fired event type in the per-tenant event_type_registry following REQ-025's existing pattern. Design only -- no fenced code blocks reproducing real .ex content (this project's mechanical no-exceptions rule, violated and caught on REQ-157/174/176/177/181's own designs in this exact pipeline -- be deliberate about avoiding it).",
    "acceptance_criteria": [
      "the timers table lives inside each tenant's Postgres schema with a tenant_id column retained (decision 0003 Decision B), verified by inspecting the migration, and carries a partial index on (fire_at) WHERE status = 'pending'",
      "the status CHECK constraint admits exactly pending, fired, cancelled and failed -- a test attempting to insert a timer with status 'expired' fails at the database level, not only in the changeset",
      "the recurrence CHECK constraint is all-or-nothing: a test inserting repeat_expression without repeat_interval_us is rejected by the database, and a test inserting all four recurrence columns consistently succeeds",
      "a timer created with fire_at in the past is fired by the first poll after it exists, and the appended event carries fired_late: true together with both the scheduled fire_at and the actual firing timestamp; a timer created with fire_at in the future is NOT fired by that same poll -- two explicit tests",
      "polling twice over the same due timer fires it exactly once: the second poll appends no second event and the timers row remains status 'fired' with its original fired_at",
      "a fire attempt that raises increments fire_error_count and leaves the timer 'pending', AND the remaining due timers in that same poll cycle are still attempted -- one test asserts both, by making the first of two due timers fail and asserting the second still fired",
      "a timer reaching the configured max fire retries transitions to 'failed' with failed_at set and produces exactly one dlq_entries row with entry_type \"timer\", and is not attempted again by subsequent polls",
      "the poll interval, jitter, max timers per cycle and max fire retries are all read from application config with the documented defaults (5000 ms, 0 ms, 64, 3), demonstrated by a test overriding at least the interval and asserting the poller uses the override",
      "no route or controller file is added or modified, confirmed by git diff --stat scoped to this requirement's commits",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
