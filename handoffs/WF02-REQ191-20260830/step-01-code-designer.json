{
  "handoff_id": "WF02-REQ191-20260830-step01",
  "run_id": "WF02-REQ191-20260830",
  "workflow_id": "WF-02",
  "step": "Step 1",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "docs/requirements.yaml#REQ-191",
  "status": "PENDING",
  "priority": "normal",
  "context": {"note": "Queue task 364, GH#710, stage S6. Feature branch feature/WF02-REQ191-20260830 created off main at c3cc76e. Read the full REQ-191 entry in docs/requirements.yaml (around line 10125) in full -- it is long and precise about schema columns/constraints. Read lib/letflow/definitions/service_scope_validator.ex L55-65 in full (the injectable Lookup struct your design must supply a real implementation for, WITHOUT changing that module). Read lib/letflow/definitions/solution_pack.ex around L35/L512 (the service_catalog_entries hard-fail you must explicitly resolve one way or the other). Read lib/letflow/engine/pin_resolver.ex around L27 (the scope-gap comment). Read lib/letflow/api/authorization.ex L100-102/L299-304 for the already-shipped SVC-04 permission surface (:ServicesRead, :AdminServicesRead, :AdminServicesManage) -- do not invent new permission names. Read docs/migration/decisions/0003-ecto-schema-strategy.md in full (Decision B's per-tenant-schema default, which this requirement deliberately diverges from). Read web/src/api/services.ts and web/src/types/api.ts for ServiceRecord/ServiceScope/RegisterServiceBody/UpdateServiceScopeBody -- the binding contract. Read REQ-067's cursor pagination pattern (find it via grep for an existing list_*/2 function using cursor-based pagination, e.g. Letflow.Dlq.list/2 or Letflow.Tasks.list_tasks/2) to reuse, not reinvent."},
  "task": {
    "description": "Produce the design artefact (lib/letflow/design/req191-service-catalog-core.md or similar) covering REQ-191's full scope: (1) the service_catalog migration/schema -- a GLOBAL (not per-tenant-schema) table with the exact columns/CHECK constraints the requirement specifies verbatim (service_id string <=255 PK globally unique, endpoint_url <=2048, request_schema/response_schema text, required_auth CHECK NONE|API_KEY|OAUTH2|MUTUAL_TLS, timeout_ms CHECK 1-3600000, retry_policy nullable text, scope CHECK global|tenant default global, owner_tenant_id nullable uuid, created_at/updated_at, plus the table-level scope/owner_tenant_id consistency CHECK). The migration and moduledoc must explicitly state the R-Co-grounded reason this table is global (referenceable by every tenant when scope=global; service_id globally unique) and flag it for REVIEWER sign-off as a deliberate divergence from decision 0003 Decision B, not an oversight. (2) A context module: register/2, get_for_tenant/2 (SVC-01 resolution: global OR owner_tenant_id match visible; a tenant-scoped service owned by someone else is NOT FOUND, never forbidden -- indistinguishable from non-existent), list_for_tenant/2 with cursor pagination, update_scope/2, delete/2. (3) Referential guards: delete/2 and update_scope/2 (narrowing global->tenant) must check for ACTIVE definitions referencing the service via a STRUCTURAL check of actual SERVICE_TASK service_id attributes (not R-Co's LIKE-on-serialized-JSON over-matching bug -- state explicitly why this design diverges from R-Co's implementation). (4) A real Lookup implementation for ServiceScopeValidator.validate/3's injectable struct, without modifying that module. (5) An explicit resolution of solution_pack.ex's service_catalog_entries hard-fail -- either make it functional now, or explicitly retain the hard-fail with the owning follow-up requirement named. Design only -- no fenced code blocks reproducing real .ex content (this project's mechanical no-exceptions rule, hit repeatedly in this pipeline including twice on the immediately preceding requirement's design -- be deliberate about avoiding it).",
    "acceptance_criteria": [
      "the service_catalog table carries the scope/owner consistency CHECK: an insert with scope 'global' and a non-null owner_tenant_id is rejected by the DATABASE, and so is one with scope 'tenant' and a null owner_tenant_id -- two explicit tests, not changeset-level validation",
      "required_auth and timeout_ms are CHECK constrained at the database level: an insert with required_auth 'BASIC' is rejected, and so is one with timeout_ms 0 and one with timeout_ms 3600001",
      "get_for_tenant/2 returns a global service to any tenant; returns a tenant-scoped service to its owner; and returns NOT FOUND -- not a forbidden error -- for a tenant-scoped service owned by a different tenant, so the two cases are indistinguishable to the caller (three explicit tests)",
      "service_id is globally unique: registering the same service_id for a second, different tenant fails, even when the first is tenant-scoped",
      "register/2 with scope 'tenant' naming a tenant that does not exist is rejected without creating a row",
      "delete/2 of a service referenced by an ACTIVE definition is refused and the error names the referencing definition ids; delete/2 of an unreferenced service succeeds -- and a test confirms the reference check matches an actual SERVICE_TASK service_id attribute rather than a substring of the serialized graph, by registering a service whose id appears only inside an unrelated string in some definition's graph and asserting that definition does NOT block the delete",
      "update_scope/2 narrowing a global service to tenant-scoped is refused, naming the conflicting tenant ids, when another tenant's ACTIVE definition references it; widening a tenant-scoped service to global is always allowed",
      "Letflow.Definitions.ServiceScopeValidator.validate/3 works unchanged when handed a Lookup backed by this catalog: a definition referencing a service not visible to its tenant is rejected at activation, and one referencing a visible service activates -- with no change to service_scope_validator.ex itself, confirmed by git diff",
      "the migration and the module moduledoc both state that this table is GLOBAL rather than per-tenant-schema, give the R-Co-grounded reason (a global-scope entry is referenceable by every tenant and service_id is unique across tenants), and flag the divergence from decision 0003 Decision B for REVIEWER sign-off",
      "solution_pack.ex's service_catalog_entries hard-fail is either made reachable-and-functional or is explicitly retained with the owning follow-up named in this requirement's moduledoc -- it is not left unaddressed, asserted by a test covering whichever outcome was chosen",
      "no route or controller file is added or modified, confirmed by git diff --stat scoped to this requirement's commits",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": null
}
