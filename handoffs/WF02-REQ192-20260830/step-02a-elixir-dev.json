{
  "handoff_id": "WF02-REQ192-20260830-step02a-rework2",
  "run_id": "WF02-REQ192-20260830",
  "workflow_id": "WF-02",
  "step": "Step 2a",
  "from_agent": "CODE-DESIGN-VALIDATOR",
  "to_agent": "ELIXIR-DEV",
  "file": "lib/letflow/design/req192-service-catalog-routes.md",
  "status": "PENDING",
  "priority": "normal",
  "context": {
    "note": "Design PASSED Step 1b re-validation, rework iteration 2 (see handoffs/WF02-REQ192-20260830/step-01b-code-design-validator.json's iteration-2 record and this handoff run's validator report). This supersedes the prior step-02a-elixir-dev.json (iteration-1 implementation instructions, which specified a router-local Repo/Ecto.Query against Letflow.ServiceCatalog.Entry -- that path is now abandoned, see below). REVISED section 5 of the design replaces the direct-schema-query approach with a new, minimal, additive Letflow.ServiceCatalog.list_all/1 function in lib/letflow/service_catalog.ex, because implementing the original section 5 caused an existing, unrelated-requirement test to fail: test/letflow/routers/req078_supporting_routes_test.exs's T-19 ('no Repo. call anywhere under lib/letflow/routers/ (INV-RT-1)') -- confirmed by this validator to be a genuinely hard, allowlist-free, repo-wide Path.wildcard scan with assert offenders == [] and zero exception mechanism (read directly, not taken on faith). CODE-DESIGN-VALIDATOR independently confirmed: (1) list_all/1's proposed shape genuinely mirrors list_for_tenant/2's real body in lib/letflow/service_catalog.ex (lines 264-326) -- same list_params() type, same filter_by_list_cursor/2 and split_list_page/2 reuse, same order_by/limit/split-page pipeline, only the where-clause and tenant_id argument omitted; (2) the new 'SCA:' cursor-prefix constant is a real, enforced cross-endpoint isolation mechanism, not cosmetic -- Pagination.check_prefix/2 (lib/letflow/api/pagination.ex) genuinely rejects a cursor whose decoded prefix does not match the caller's hardcoded prefix via String.starts_with?/2, returning {:error, :wrong_endpoint}, so a 'SC:'-minted cursor replayed against list_all/1 (or vice versa) will genuinely be rejected; (3) the scope-expansion reasoning (touching Letflow.ServiceCatalog despite the original 'NOT IN THIS REQUIREMENT' note) is explicit and flagged for REVIEWER sign-off, not silently smuggled in -- and this is legitimately framed as REQ-192 additively extending REQ-191's already-merged, already-shipped context module (a new function, no change to any existing function's signature or body), not a re-opening of REQ-191 itself; (4) the design's git history shows the old section-5 text marked 'Superseded' rather than deleted, preserving the prior reasoning, and no fenced code block (```elixir or otherwise) appears anywhere in the file -- signatures and shapes only. req078_supporting_routes_test.exs and INV-RT-1's own statement are confirmed untouched (git diff main...HEAD on that file and lib/letflow/design/req078-supporting-routes.md is empty).",
    "reusable_from_prior_attempt": "The prior implementation attempt's router file (lib/letflow/routers/admin_services.ex) and lib/letflow/api/error.ex additions (service_referenced_by_active_definitions/1, service_scope_narrowing_conflict/1) are likely still correct and reusable as-is for everything EXCEPT the 'list all' data-access path inside admin_services.ex's GET / handler -- that one handler must be rewritten to call Letflow.ServiceCatalog.list_all(params) instead of building its own Ecto.Query against Entry via Repo. Remove any alias Letflow.ServiceCatalog.Entry, import Ecto.Query, or Letflow.Repo reference from admin_services.ex entirely. lib/letflow/routers/services.ex, lib/letflow/plugs/api_pipeline.ex's two new mounts, and test/letflow/api/authorization_enforcement_test.exs's @routers/@mount_prefix additions are unaffected by this change and do not need to be redone.",
    "carried_forward_findings": [
      "Field gap 1 (design section 8): ServiceRecord.max_retries has no backing Entry column -- implement per the design's stated interim resolution (omit from service_record_json/1 output, following Letflow.Routers.Dlq's precedent). Unchanged from iteration 1.",
      "Field gap 2 (design section 8): request_schema/response_schema serialize as JSON null when the Entry column is nil -- implement per the design's stated interim resolution. Unchanged from iteration 1.",
      "Stale comment (lib/letflow/api/authorization.ex L416-419) -- out of scope for REQ-192, leave untouched, flagged for a future requirement. Unchanged from iteration 1.",
      "list_all/1 needs its own direct context-module-level test coverage (design section 5's 'Consequence for TEST-DESIGNER' note) in addition to router-level GET /admin/services tests -- flag this for TEST-DESIGNER at Step 2b, do not attempt to write that coverage yourself in this step.",
      "The list_all/1 scope expansion into Letflow.ServiceCatalog is explicitly flagged for REVIEWER sign-off at Step 2d -- implement it, but do not treat it as pre-approved; carry the same explicit flag forward into your handoffs to SECURITY-REVIEWER (Step 2c) and REVIEWER (Step 2d) that the prior attempt already used for the (now-abandoned) direct-schema-query exception."
    ]
  },
  "task": {
    "description": "Implement the REVISED lib/letflow/design/req192-service-catalog-routes.md in full, reusing the prior attempt's still-valid files where noted above. New/changed work for this iteration: (1) add Letflow.ServiceCatalog.list_all/1 to lib/letflow/service_catalog.ex per design section 5 -- same list_params() argument type and result shape as list_for_tenant/2, no tenant_id argument, reuses filter_by_list_cursor/2 and split_list_page/2, its own @list_all_cursor_prefix \"SCA:\" module attribute, full @doc stating no tenant/scope filtering is performed and the caller is responsible for authorization; (2) rewrite lib/letflow/routers/admin_services.ex's GET / handler to call Letflow.ServiceCatalog.list_all(params) instead of querying Entry directly -- no Repo, Ecto.Query, or Entry alias anywhere in the file; (3) confirm the rest of admin_services.ex, services.ex, api_pipeline.ex, error.ex, and authorization_enforcement_test.exs from the prior attempt still apply unchanged. Run mix test and mix compile --warnings-as-errors and quote real output, specifically confirming test/letflow/routers/req078_supporting_routes_test.exs's T-19 (INV-RT-1) now passes.",
    "acceptance_criteria": [
      "Letflow.ServiceCatalog.list_all/1 exists with signature @spec list_all(list_params()) :: {:ok, %{items: [Entry.t()], next_cursor: String.t() | nil}} | {:error, :invalid_cursor | :wrong_endpoint | :expired}, one argument, no tenant_id",
      "list_all/1's query pipeline omits the where([e], e.scope == :global or e.owner_tenant_id == ^tenant_id) clause entirely (not replaced by a tautology) while reusing filter_by_list_cursor/2, split_list_page/2, and the same order_by/limit shape as list_for_tenant/2",
      "list_all/1 uses its own \"SCA:\" cursor prefix, distinct from list_for_tenant/2's \"SC:\" -- a cursor minted by one is rejected with {:error, :wrong_endpoint} if replayed against the other, verified by an explicit test",
      "list_for_tenant/2 is byte-for-byte unchanged -- same name, arity, body, where-clause",
      "lib/letflow/routers/admin_services.ex contains zero occurrences of \"Repo.\", \"Ecto.Query\", or \"ServiceCatalog.Entry\" after this change",
      "test/letflow/routers/req078_supporting_routes_test.exs's T-19 (INV-RT-1, no Repo. call under lib/letflow/routers/) passes",
      "All acceptance criteria already carried from the prior step-02a handoff (GET /services tenant scoping, GET /admin/services requires :AdminServicesRead, POST/PATCH/DELETE 403 without :AdminServicesManage, 409 bodies for referenced-service delete and scope-narrowing conflict, POST duplicate-id 409, cross-tenant-404 on non-admin path, authorization.ex and web/ untouched) still hold and are re-verified against the rewritten admin_services.ex",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted",
      "the list_all/1 scope expansion is explicitly flagged (in your Step 2c/2d handoffs) for REVIEWER sign-off, not treated as pre-approved"
    ]
  },
  "result": null,
  "rework_count": 0,
  "max_rework": 3
}
