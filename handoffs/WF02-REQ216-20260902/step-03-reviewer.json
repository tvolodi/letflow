{
  "handoff_id": "step03-WF02-REQ216-20260902",
  "run_id": "WF02-REQ216-20260902",
  "workflow_id": "WF-02",
  "step": "03",
  "from_agent": "REVIEWER",
  "to_agent": "TEST-DESIGNER",
  "file": "handoffs/WF02-REQ216-20260902/step-03-reviewer.json",
  "created_at": "2026-09-02T23:05:00Z",
  "started_at": "2026-09-02T22:55:00Z",
  "completed_at": "2026-09-02T23:05:00Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": "S6",
    "requirement_ids": [
      "REQ-216"
    ],
    "issue_ids": [
      "ISS-0437"
    ],
    "artifacts_in": [
      "lib/letflow/design/req216-admission-control-core.md",
      "handoffs/WF02-REQ216-20260902/step-01d-code-design-validator.json",
      "handoffs/WF02-REQ216-20260902/step-02-elixir-dev.json",
      "lib/letflow/admission.ex",
      "lib/letflow/application.ex",
      "test/letflow/admission_test.exs"
    ],
    "branch_name": "feature/WF02-REQ216-20260902",
    "requirement_text": "Closes part 1 of ISS-0431/GH#835 (queue task 431): Letflow has no\nadmission control or backpressure anywhere. ISSUE-FIXER's diagnosis\n(handoffs/WF03-ISS0431-20260902/step-01-issue-fixer-diagnosis.json)\nconfirmed by direct read that no bounded-concurrency mechanism exists\non any surface -- lib/letflow/plugs/api_pipeline.ex's plug chain has\nnone (Letflow.Plugs.RateLimit is a different, still-unmounted S4\nmechanism -- request-RATE throttling, not concurrency admission --\nporting it would not close this gap); lib/letflow/application.ex's\nfive Task.Supervisors all bound unrelated subsystems (sandbox\nprovisioning, plugin/Lua/Wasm dispatch), none of them HTTP-request or\nPoller concurrency against the Ecto pool; config/runtime.exs:113 sets\n`pool_size` from `POOL_SIZE` with nothing anywhere referencing it.\nFour prior issues (ISS-0194, ISS-0219, ISS-0222, ISS-0224) each solved\na DIFFERENT local instance of the same root cause (unbounded demand vs.\na bounded connection resource) with no shared mechanism; this\nrequirement is the shared mechanism, so a fifth ad hoc shape does not\nget invented. THIS REQUIREMENT IS THE CORE ONLY -- no HTTP-path or\nPoller wiring (REQ-217, REQ-218), mirroring REQ-186/REQ-214's own\n\"core module first, wiring requirements second\" split precedent in\nthis file.\n\nDECISIONS RESOLVED HERE (ISS-0431's own \"TO DECIDE, not prescribed\"\nlist), with reasoning, not left to ELIXIR-DEV to improvise:\n\n1. LIBRARY VS. HAND-ROLLED: hand-rolled, pure-BEAM GenServer-based\ncounting semaphore(s). No new dependency. Reasoning transferred\ndirectly from decision 0014 (new-dependency policy), which the\ndiagnosis already checked: 0014's method is to ask whether a pure-BEAM\noption removes the hazard class outright vs. merely mitigating it, and\nto treat any new native/NIF dependency as a real, disclosed cost\nrequiring its own comparison. Bounding concurrent access to Ecto\nconnections is a problem the BEAM's own primitives already solve\nnatively (a GenServer counter, `:queue` + monitors -- the same shape\n`Letflow.SandboxPool`, REQ-039/ISS-0224, already uses for an adjacent\nbut distinct problem) with no NIF surface at all, so 0014's own\nargument for preferring pure-BEAM mechanisms applies here with no\ncountervailing benefit to a hex dependency (this is a small, fully\nin-house state machine, not a mature external sandboxing runtime the\nway wasmex's Wasmtime binding is).\n\n2. GLOBAL VS. PER-TENANT VS. BOTH: BOTH, composing, not either alone.\nA per-tenant-only cap cannot bound total demand on `pool_size` (N\ntenants each admitted up to a per-tenant cap can jointly exceed\n`pool_size` the same way ISS-0194's N-partitions-times-pool_size math\nalready demonstrated for a structurally identical multiplication). A\nglobal-only cap cannot stop one tenant's burst starving every other\ntenant, which ISS-0431 itself names as \"arguably the more important\nproperty in a multi-tenant BPM platform\" and which is exactly decision\n0006's multi-tenancy isolation posture (schema-per-tenant data\nisolation composed with NO existing resource-fairness isolation) --\nleaving it unaddressed would be a real regression against that\nposture, not a neutral omission. So: a GLOBAL semaphore bounds total\nadmitted concurrent DB-connection-holding work platform-wide, and a\nPER-TENANT semaphore (one counter per active tenant schema, entries\ncreated lazily on first admission attempt, never pre-allocated for\nevery possible tenant) bounds any single tenant's share of that same\nbudget. Default per-tenant share: an equal fair-share split of the\nglobal cap across the CURRENTLY-DISTINCT tenants that have made an\nadmission attempt in a rolling window (avoids the global cap collapsing\nto 1 the instant tenant count exceeds it -- floor of 1 slot per tenant,\nmatching decision 0009/ISS-0194's own \"clamp with a floor\" precedent),\noverridable per environment. Tier-weighted per-tenant caps (e.g. a\npaying tenant getting a larger share) are explicitly OUT OF SCOPE and\nrecorded as a genuinely open product question, not resolved here:\n`Letflow.Identity.Tenant`'s schema (lib/letflow/identity/tenant.ex) has\nno plan/tier field to weight by (verified by reading the schema block\nin full -- only `slug`, `display_name`, `status`, `idp_realm_id`\nexist), so introducing tenant tiering is a genuine product decision\nwith no existing data model to hang it on, not an implementation\ndetail this requirement can quietly decide. Equal fair-share is the\nonly defensible default absent that decision.\n\n3. TIE TO POOL_SIZE/MAX_CONNECTIONS (closing the drift ISS-0194/0219/\n0222/0224 already show happens between independently-tuned numbers):\nthe global cap MUST be expressed as a function of the configured Ecto\n`pool_size` (config/runtime.exs's `POOL_SIZE`/`config :letflow,\nLetflow.Repo, pool_size:` -- read the SAME value this module reads,\nnever a second independently-set number), specifically\n`pool_size - reserved_headroom`, where `reserved_headroom` (config,\ndefault 2) reserves connection budget for non-admission-controlled\nDB consumers this requirement does not touch (REQ-186's\n`Letflow.Scheduler.Poller` before REQ-218 wires it in;\n`Letflow.SandboxPool`'s own already-serialized DB worker; migrations).\nThis function relationship, not a hardcoded number, is the\nacceptance-tested contract -- changing `POOL_SIZE` at boot changes the\ncomputed global cap on the very next boot with no second setting to\nupdate, closing exactly the drift class this issue exists to end.\n\n4. RESPONSE SHAPE: settled by existing precedent, not invented here.\n`Letflow.Api.Response.service_unavailable/2` (REQ-066,\nlib/letflow/api/response.ex) already exists, is already used for an\nanalogous resource-exhaustion rejection\n(`lib/letflow/routers/promotions.ex`'s \"no sandbox free within\ntimeout\"), and REQ-066's own moduledoc documents the caller-sets-\nRetry-After convention identically to `rate_limited/2`. This\nrequirement's core module returns a typed outcome the wiring\nrequirements (REQ-217/218) map onto that EXISTING response helper --\nthis requirement does not touch any router or plug itself (no HTTP\nsurface in scope), so it does not call `Response.service_unavailable/2`\ndirectly, but its return-type design must make that mapping a\none-line call, not require the caller to reconstruct rejection\nsemantics.\n\nNOT ABSORBED, WITH REASON: ISS-0224's own deferred\n`Letflow.SandboxPool` admission-shaped defect (its `handle_call/3`\nblocking the pool mailbox during synchronous provisioning) is a\nDIFFERENT resource -- ephemeral promotion-pipeline sandbox slots, not\nthe tenant-request-serving Ecto pool -- already has its own\nindependently-reasoned wait-queue/slot-bookkeeping shape (REQ-039,\nISS-0048's crash-reclaim invariants), and was deliberately scoped out\nof its own parent fix (ISS-0220 §12) specifically to avoid conflating\ntwo independently-revertable subsystems. Folding it into this\ncross-cutting core module now would repeat exactly that mistake in\nreverse. It stays a separate, still-open concern; REQ-039/ISS-0224's\nmodule is prior art for the wait-queue SHAPE (a FIFO of waiters with\nper-waiter timeout, ISS-0224's own fix for exactly that shape) that\nthis requirement's per-tenant/global semaphores may reuse internally,\nbut is not the same mechanism and is not touched by this requirement.\n\nSCOPE:\n1. New module `Letflow.Admission` (or equivalently named per its own\nmoduledoc) plus a supervised GenServer (or a small set of them --\nstate the shape chosen and why in the moduledoc) implementing:\n`try_acquire(pool :: :global | {:tenant, tenant_schema :: String.t()}) ::\n{:ok, admission_ref()} | {:error, :capacity}` and\n`release(admission_ref()) :: :ok`. No HTTP or Poller-specific\nvocabulary anywhere in this module -- REQ-217/218 are the only\ncallers, and both are equally valid callers of the exact same API.\n2. Global cap computed once at supervised-child start from\n`Application.fetch_env!(:letflow, Letflow.Repo)[:pool_size]` (the\nSAME config key `config/runtime.exs` already writes) minus\n`reserved_headroom` (config, default 2), never re-read from a second\nindependently-configured value.\n3. Per-tenant cap computed as the equal fair-share split described in\ndecision 2 above, with a floor of 1 and recomputed as the set of\ncurrently-tracked tenants changes (a tenant with zero admission\nattempts in the tracking window is not counted against the split).\n4. `try_acquire/1` for `{:tenant, _}` MUST also consume one unit of\nthe SAME global budget (both gates are checked, not just the\nper-tenant one) -- decision 2's own \"both, composing\" requirement made\nconcrete and testable.\n5. Added to `Letflow.Application`'s supervision tree\n(lib/letflow/application.ex), following this file's own established\nTask.Supervisor-ordering discipline (state explicitly in the moduledoc\nwhether any other child must start before or after it, or that no\nordering dependency exists, mirroring REQ-173/REQ-166's own \"no\nordering dependency\" precedent comments in application.ex).\nNOT IN THIS REQUIREMENT: no plug, no route, no change to\napi_pipeline.ex or poller.ex (REQ-217, REQ-218); no tier-weighted caps\n(recorded above as a genuinely open product question); no change to\n`Letflow.SandboxPool` (a different resource, explicitly not absorbed,\nsee above).\n"
  },
  "task": {
    "description": "WF-02 Step 2d (hard gate): independently review ELIXIR-DEV's implementation of Letflow.Admission against the CODE-DESIGN-VALIDATOR-approved design -- verify the atomicity algorithm matches exactly, judge ELIXIR-DEV's flagged release/2 deviation, verify the fair-share recomputation never retroactively revokes an existing ref, spot-check the mutation-testing claim against the actual test, confirm supervision-tree placement and ordering-independence, confirm scope, and independently re-run compile/format/test.",
    "acceptance_criteria": [
      "Letflow.Admission.try_acquire(:global) returns {:ok, ref} up to exactly (pool_size - reserved_headroom) concurrently-held admissions, and {:error, :capacity} on the next attempt while all of them are held -- reserved_headroom and pool_size both read from Application config, verified by a test that overrides pool_size and observes the computed cap change with no other code edit",
      "Letflow.Admission.try_acquire({:tenant, schema}) enforces its own per-tenant cap independently of other tenants: tenant A exhausting its own per-tenant share still leaves tenant B able to acquire up to B's own share, demonstrated with a test asserting A's rejection and B's simultaneous success",
      "a {:tenant, schema} acquisition that succeeds at the per-tenant gate but would exceed the GLOBAL cap is still rejected with {:error, :capacity} -- a test that exhausts the global cap via :global acquisitions alone, then shows a fresh tenant's first-ever {:tenant, _} attempt is rejected despite having zero per-tenant usage of its own",
      "release/1 on a previously-acquired admission_ref() frees exactly one unit of both the global AND (for a {:tenant, _} ref) the per-tenant budget it was acquired against, verified by acquiring to capacity, releasing one, and observing exactly one subsequent try_acquire/1 succeeds",
      "the per-tenant cap for a newly-observed tenant schema recomputes as an equal fair-share split of the global cap with a floor of 1, demonstrated by a test with 3 tracked tenants against a global cap of 10 (each gets floor(10/3)=3, not 1) and a global cap of 2 against 5 tenants (each gets the floor of 1, not 0)",
      "Letflow.Admission is present as its own entry in Letflow.Application's supervision tree, confirmed by reading application.ex, with its moduledoc stating explicitly whether it has any start-order dependency on another child or none",
      "mix test and mix compile --warnings-as-errors both pass with real output quoted"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "VERDICT: PASS. Read lib/letflow/admission.ex, lib/letflow/application.ex, and test/letflow/admission_test.exs in full, plus `git diff main..HEAD --stat` to confirm scope, and independently re-ran compile/format/test rather than trusting ELIXIR-DEV's report.\n\n1. ATOMICITY (verified exact match to design): the `{:tenant, {:tenant, schema} = pool}` `handle_call/3` clause (admission.ex:251-276) is genuinely one clause: `ensure_tenant_tracked/2` runs first (lazy creation, unconditional), then `tenant_in_use` and `per_tenant_cap` are read as plain local bindings with zero mutation, then a single `if state.global_in_use < state.global_cap and tenant_in_use < per_tenant_cap` guards a single branch that mutates `global_in_use`, the tenant's `in_use`, and `refs` together in one `%{state | ...}` update before `{:reply, {:ok, ref}, new_state}`. The `else` branch returns `{:error, :capacity}` against the untouched `state` -- literally the same term passed in, not a copy with partial mutation. There is no second message, no `handle_cast`, no `Process.send`, no rollback branch anywhere in the module. This is exactly the single pure-read-then-mutate-both-or-neither shape CODE-DESIGN-VALIDATOR's step-01d PASS required (design doc §1's atomicity algorithm, restated verbatim in the moduledoc's \"Atomicity algorithm\" section). Confirmed by direct code read, not by accepting the moduledoc's own claim about itself.\n\n2. THE FLAGGED DEVIATION (release/2 deriving `pool` from the server's own `refs` map, keyed by `id`, rather than trusting the caller-supplied `Ref.pool` field) -- independent judgment, not ELIXIR-DEV's framing accepted at face value: this is a legitimate, safety-improving implementation detail, not a contract change requiring a design re-approval. Reasoning: (a) the PUBLIC, caller-observable contract of `release/2` is unchanged for every legitimately-issued ref -- `%Ref{id: id, pool: pool}` is only ever constructed inside this module's own `handle_call` clauses with `id` and `pool` set consistently (id freshly minted via `make_ref/0`, pool set to the exact `pool_selector()` the call was made with), so `refs[id]` and `ref.pool` are guaranteed equal for any ref that ever leaves `try_acquire/2` honestly; the two lookups are observationally identical on the only input the module's own type contract permits. (b) The design doc's §2.1 rationale for putting `pool` ON the ref at all is explicitly about making `release/1` a true single-argument operation with 'no possibility of the caller passing a mismatched selector' -- deriving the trusted value from server-side state instead of the wire copy is MORE faithful to that stated intent, not less, since it closes the one channel (a hand-mutated struct) through which a mismatch could otherwise occur. (c) The design's own §2.1 idempotency rule keys release exclusively off `id`-membership in `refs` ('idempotent up to the ref's own state membership') -- it never says release must dispatch on the caller-supplied `pool` field, only that the ref must carry enough information for release to know what to free; deriving that information from the authoritative server-side copy of the same information the ref itself was populated from satisfies that requirement without contradicting any explicit design statement. (d) This is not scope creep or a new abstraction -- it is a one-line change of which of two equal-by-construction values a `case` pattern-matches on, with no new API, no new field, no new state. I do not agree this needed CODE-DESIGN-VALIDATOR sign-off first: it does not touch the ref's external shape (`%Ref{id:, pool:}` unchanged), the caller-facing signature (`release(admission_ref(), server) :: :ok` unchanged), or any acceptance criterion's observable behavior (AC4's test passes either way, and does pass). Judgment: legitimate strengthening, not a contract change. Concur with ELIXIR-DEV's own self-assessment on this point after independently re-deriving it.\n\n3. FAIR-SHARE RETROACTIVITY (verified never retroactively revokes an existing ref): read the `{:release, %Ref{id: id}}` clause (admission.ex:278-300) directly -- it does a `Map.pop(state.refs, id)` and, on a hit, decrements `global_in_use` by exactly 1 and calls `release_tenant/2` which does `Map.update!(tenants, schema, &%{&1 | in_use: &1.in_use - 1})`. Nothing in this path reads or calls `per_tenant_cap/1`, reads `global_cap`, or consults the CURRENT tracked-tenant count at all -- the release path is entirely cap-independent, only ever decrementing a counter keyed by the ref's own recorded selector. A ref issued while a tenant's cap was larger, released after the cap has since shrunk (because more tenants joined), decrements the same way any other release would; nothing rejects, errors, or short-circuits based on the cap currently being smaller than the tenant's `in_use`. Confirmed via test/letflow/admission_test.exs's own AC5 test 'the per-tenant cap recomputes as the tracked-tenant set grows (never retroactively revokes)' (lines 191-211): tenant A holds 3 refs under a 1-tenant-tracked state (cap 10), two more tenants then attempt (shrinking A's next computed cap to floor(10/3)=3, at A's current in_use exactly), A's NEXT attempt is correctly rejected, and ALL THREE of A's already-held refs still release cleanly with `assert :ok`. This exercises exactly the scenario the design's §2.2 invariant describes and the release path shows no cap-dependent code exists to violate it.\n\n4. MUTATION-TESTING CLAIM SPOT-CHECK: read the two tests ELIXIR-DEV's report says the guard-mutation would catch. AC3's dedicated test ('a fresh tenant with zero usage is rejected once :global acquisitions alone exhaust the global cap', lines 90-100) exhausts `:global` cap 3/3 then asserts `{:error, :capacity}` for a brand-new tenant with zero per-tenant usage -- this genuinely only passes if the global-cap half of the guard is evaluated; dropping it would make this tenant's own per-tenant cap (floor(3/1)=3, unused) admit it, flipping this assertion to a false PASS-turned-FAIL as claimed. The dedicated atomicity test ('a rejected {:tenant, _} acquisition mutates neither counter', lines 250-266) reads `:sys.get_state/1` before and after a rejected attempt and asserts `state_after.global_in_use == state_before.global_in_use` and the tenant's `in_use` stays 0 -- this specifically targets the 'zero mutation on rejection' half of the atomicity property, and would also flip to a false success (a spurious grant) if the global gate were dropped and the tenant's own cap admitted it. Both tests assert on the right thing; the 2-test mutation-failure count is plausible and not overclaimed. AC2's and AC5's tests, by contrast, do not touch the global gate at all (global cap is set generously non-binding in both), consistent with only these 2 of 15 tests being sensitive to that specific line.\n\n5. SUPERVISION-TREE PLACEMENT: `{Letflow.Admission, []}` (application.ex:65) sits between `Letflow.Metrics.Registry` and `Letflow.InstanceSupervisor`, exactly the design's suggested (non-binding) position. Independently re-derived the no-ordering-dependency claim rather than accepting it: grepped the full `lib/` and `test/` trees for `Letflow.Admission` outside admission.ex/admission_test.exs/application.ex itself -- zero hits. No other child's `init/1`, `start_link/1`, or first-tick/first-message logic calls `try_acquire/2` or otherwise references this module (REQ-217/218, the only intended callers, do not exist yet). `Letflow.Admission.init/1` reads only `Application.fetch_env!/2` and `Application.get_env/3` (static config, available before `Application.start/2` runs), performs no `Repo`, `Registry`, or `GenServer.call` to any other process. The claim is accurate, not merely asserted.\n\n6. SCOPE: `git diff main..HEAD --stat` shows exactly `lib/letflow/admission.ex` (new), `lib/letflow/application.ex` (+8/-0, additive only), and `test/letflow/admission_test.exs` (new) among code/test files -- everything else in the diff is the design doc, requirements.yaml status flip, status-history append, ISS-0437 issue record, and this run's own handoff files, all expected non-code artifacts for this workflow step. No other module touched.\n\n7. INDEPENDENT VERIFICATION (real output, this session, foreground):\n   - `mix compile --warnings-as-errors --force` -> 'Compiling 183 files (.ex)' / 'Generated letflow app', zero warnings, zero errors.\n   - `mix format --check-formatted` -> exit 0, no output (fully formatted).\n   - `mix test test/letflow/admission_test.exs test/letflow/application_test.exs` -> 'Result: 19 passed', 0 failures.\n   - Broader sweep (other suites reading Letflow.Application's live supervision tree or global config surface): `mix test test/letflow/sandbox_pool_test.exs test/letflow/metrics/registry_test.exs test/letflow/obs/alerts_test.exs` -> 'Result: 45 passed', 0 failures.\n\nSCOPE-CREEP CHECK: no abstractions, behaviours, or generic plumbing introduced ahead of what REQ-216 needs -- a single GenServer with two public functions, no wait-queue machinery (explicitly deferred, matching the design), no eviction machinery (explicitly deferred as ISS-0437, already filed by CODE-DESIGN-VALIDATOR/ORCH). No decision-record conflict: the hand-rolled-GenServer choice (decision 0014's method), the pool_size/reserved_headroom config surface, and the no-new-dependency posture are all consistent with existing decisions; nothing here silently re-decides anything on record.\n\nTYPE-SAFETY NOTE (does not block PASS, no new issue filed): `pool_selector()` is `:global | {:tenant, String.t()}` and `tenant_schema` is an unvalidated `String.t()` -- an empty string or a schema that doesn't exist in `Letflow.Identity` is tracked as a valid, ordinary map key with no compile-time or runtime rejection. This is explicitly out of scope per the design doc §7 ('schema-string validity is a caller concern'), so not a defect, but worth noting as the same category of representable-invalid-state the module's own §7 already flags -- no action needed at this stage.\n\nNo rework items.",
    "artifacts_out": [
      "handoffs/WF02-REQ216-20260902/step-03-reviewer.json"
    ],
    "next_action": "Route to TEST-DESIGNER to independently verify the existing 15-test suite's coverage completeness against all 7 ACs (already appears complete on this read) and add anything found missing -- per WF-02's own step sequence, this step is not skipped even though ELIXIR-DEV already wrote substantial coverage."
  },
  "rework_count": 0,
  "max_rework": 3
}
