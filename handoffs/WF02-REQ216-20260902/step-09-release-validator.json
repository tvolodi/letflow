{
  "handoff_id": "WF02-REQ216-20260902-step-09",
  "run_id": "WF02-REQ216-20260902",
  "workflow_id": "WF-02",
  "step": "09",
  "from_agent": "RELEASE-VALIDATOR",
  "to_agent": "DOC-UPDATER",
  "file": "handoffs/WF02-REQ216-20260902/step-09-release-validator.json",
  "created_at": "2026-09-03T01:26:26Z",
  "started_at": "2026-09-03T00:50:00Z",
  "completed_at": "2026-09-03T01:26:26Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": "S6",
    "requirement_ids": [
      "REQ-216"
    ],
    "issue_ids": [
      "ISS-0437"
    ],
    "artifacts_in": [
      "handoffs/WF02-REQ216-20260902/step-00-git-setup.json",
      "handoffs/WF02-REQ216-20260902/step-01-code-designer.json",
      "handoffs/WF02-REQ216-20260902/step-01b-code-design-validator.json",
      "handoffs/WF02-REQ216-20260902/step-01c-code-designer-rework.json",
      "handoffs/WF02-REQ216-20260902/step-01d-code-design-validator.json",
      "handoffs/WF02-REQ216-20260902/step-02-elixir-dev.json",
      "handoffs/WF02-REQ216-20260902/step-03-reviewer.json",
      "handoffs/WF02-REQ216-20260902/step-04-test-designer.json",
      "handoffs/WF02-REQ216-20260902/step-05-test-design-validator.json",
      "handoffs/WF02-REQ216-20260902/step-06-test-designer-rework.json",
      "handoffs/WF02-REQ216-20260902/step-07-test-design-validator.json",
      "handoffs/WF02-REQ216-20260902/step-08-test-runner.json",
      "lib/letflow/admission.ex",
      "lib/letflow/application.ex",
      "test/letflow/admission_test.exs",
      "docs/status/requirement_status.v8.yaml",
      "docs/status/requirement_status.index.yaml",
      "docs/requirements.yaml"
    ],
    "branch_name": "feature/WF02-REQ216-20260902",
    "requirement_text": "Closes part 1 of ISS-0431/GH#835 (queue task 431): Letflow has no\nadmission control or backpressure anywhere. ISSUE-FIXER's diagnosis\n(handoffs/WF03-ISS0431-20260902/step-01-issue-fixer-diagnosis.json)\nconfirmed by direct read that no bounded-concurrency mechanism exists\non any surface -- lib/letflow/plugs/api_pipeline.ex's plug chain has\nnone (Letflow.Plugs.RateLimit is a different, still-unmounted S4\nmechanism -- request-RATE throttling, not concurrency admission --\nporting it would not close this gap); lib/letflow/application.ex's\nfive Task.Supervisors all bound unrelated subsystems (sandbox\nprovisioning, plugin/Lua/Wasm dispatch), none of them HTTP-request or\nPoller concurrency against the Ecto pool; config/runtime.exs:113 sets\n`pool_size` from `POOL_SIZE` with nothing anywhere referencing it.\nFour prior issues (ISS-0194, ISS-0219, ISS-0222, ISS-0224) each solved\na DIFFERENT local instance of the same root cause (unbounded demand vs.\na bounded connection resource) with no shared mechanism; this\nrequirement is the shared mechanism, so a fifth ad hoc shape does not\nget invented. THIS REQUIREMENT IS THE CORE ONLY -- no HTTP-path or\nPoller wiring (REQ-217, REQ-218), mirroring REQ-186/REQ-214's own\n\"core module first, wiring requirements second\" split precedent in\nthis file.\n\nDECISIONS RESOLVED HERE (ISS-0431's own \"TO DECIDE, not prescribed\"\nlist), with reasoning, not left to ELIXIR-DEV to improvise:\n\n1. LIBRARY VS. HAND-ROLLED: hand-rolled, pure-BEAM GenServer-based\ncounting semaphore(s). No new dependency. Reasoning transferred\ndirectly from decision 0014 (new-dependency policy), which the\ndiagnosis already checked: 0014's method is to ask whether a pure-BEAM\noption removes the hazard class outright vs. merely mitigating it, and\nto treat any new native/NIF dependency as a real, disclosed cost\nrequiring its own comparison. Bounding concurrent access to Ecto\nconnections is a problem the BEAM's own primitives already solve\nnatively (a GenServer counter, `:queue` + monitors -- the same shape\n`Letflow.SandboxPool`, REQ-039/ISS-0224, already uses for an adjacent\nbut distinct problem) with no NIF surface at all, so 0014's own\nargument for preferring pure-BEAM mechanisms applies here with no\ncountervailing benefit to a hex dependency (this is a small, fully\nin-house state machine, not a mature external sandboxing runtime the\nway wasmex's Wasmtime binding is).\n\n2. GLOBAL VS. PER-TENANT VS. BOTH: BOTH, composing, not either alone.\nA per-tenant-only cap cannot bound total demand on `pool_size` (N\ntenants each admitted up to a per-tenant cap can jointly exceed\n`pool_size` the same way ISS-0194's N-partitions-times-pool_size math\nalready demonstrated for a structurally identical multiplication). A\nglobal-only cap cannot stop one tenant's burst starving every other\ntenant, which ISS-0431 itself names as \"arguably the more important\nproperty in a multi-tenant BPM platform\" and which is exactly decision\n0006's multi-tenancy isolation posture (schema-per-tenant data\nisolation composed with NO existing resource-fairness isolation) --\nleaving it unaddressed would be a real regression against that\nposture, not a neutral omission. So: a GLOBAL semaphore bounds total\nadmitted concurrent DB-connection-holding work platform-wide, and a\nPER-TENANT semaphore (one counter per active tenant schema, entries\ncreated lazily on first admission attempt, never pre-allocated for\nevery possible tenant) bounds any single tenant's share of that same\nbudget. Default per-tenant share: an equal fair-share split of the\nglobal cap across the CURRENTLY-DISTINCT tenants that have made an\nadmission attempt in a rolling window (avoids the global cap collapsing\nto 1 the instant tenant count exceeds it -- floor of 1 slot per tenant,\nmatching decision 0009/ISS-0194's own \"clamp with a floor\" precedent),\noverridable per environment. Tier-weighted per-tenant caps (e.g. a\npaying tenant getting a larger share) are explicitly OUT OF SCOPE and\nrecorded as a genuinely open product question, not resolved here:\n`Letflow.Identity.Tenant`'s schema (lib/letflow/identity/tenant.ex) has\nno plan/tier field to weight by (verified by reading the schema block\nin full -- only `slug`, `display_name`, `status`, `idp_realm_id`\nexist), so introducing tenant tiering is a genuine product decision\nwith no existing data model to hang it on, not an implementation\ndetail this requirement can quietly decide. Equal fair-share is the\nonly defensible default absent that decision.\n\n3. TIE TO POOL_SIZE/MAX_CONNECTIONS (closing the drift ISS-0194/0219/\n0222/0224 already show happens between independently-tuned numbers):\nthe global cap MUST be expressed as a function of the configured Ecto\n`pool_size` (config/runtime.exs's `POOL_SIZE`/`config :letflow,\nLetflow.Repo, pool_size:` -- read the SAME value this module reads,\nnever a second independently-set number), specifically\n`pool_size - reserved_headroom`, where `reserved_headroom` (config,\ndefault 2) reserves connection budget for non-admission-controlled\nDB consumers this requirement does not touch (REQ-186's\n`Letflow.Scheduler.Poller` before REQ-218 wires it in;\n`Letflow.SandboxPool`'s own already-serialized DB worker; migrations).\nThis function relationship, not a hardcoded number, is the\nacceptance-tested contract -- changing `POOL_SIZE` at boot changes the\ncomputed global cap on the very next boot with no second setting to\nupdate, closing exactly the drift class this issue exists to end.\n\n4. RESPONSE SHAPE: settled by existing precedent, not invented here.\n`Letflow.Api.Response.service_unavailable/2` (REQ-066,\nlib/letflow/api/response.ex) already exists, is already used for an\nanalogous resource-exhaustion rejection\n(`lib/letflow/routers/promotions.ex`'s \"no sandbox free within\ntimeout\"), and REQ-066's own moduledoc documents the caller-sets-\nRetry-After convention identically to `rate_limited/2`. This\nrequirement's core module returns a typed outcome the wiring\nrequirements (REQ-217/218) map onto that EXISTING response helper --\nthis requirement does not touch any router or plug itself (no HTTP\nsurface in scope), so it does not call `Response.service_unavailable/2`\ndirectly, but its return-type design must make that mapping a\none-line call, not require the caller to reconstruct rejection\nsemantics.\n\nNOT ABSORBED, WITH REASON: ISS-0224's own deferred\n`Letflow.SandboxPool` admission-shaped defect (its `handle_call/3`\nblocking the pool mailbox during synchronous provisioning) is a\nDIFFERENT resource -- ephemeral promotion-pipeline sandbox slots, not\nthe tenant-request-serving Ecto pool -- already has its own\nindependently-reasoned wait-queue/slot-bookkeeping shape (REQ-039,\nISS-0048's crash-reclaim invariants), and was deliberately scoped out\nof its own parent fix (ISS-0220 \u00a712) specifically to avoid conflating\ntwo independently-revertable subsystems. Folding it into this\ncross-cutting core module now would repeat exactly that mistake in\nreverse. It stays a separate, still-open concern; REQ-039/ISS-0224's\nmodule is prior art for the wait-queue SHAPE (a FIFO of waiters with\nper-waiter timeout, ISS-0224's own fix for exactly that shape) that\nthis requirement's per-tenant/global semaphores may reuse internally,\nbut is not the same mechanism and is not touched by this requirement.\n\nSCOPE:\n1. New module `Letflow.Admission` (or equivalently named per its own\nmoduledoc) plus a supervised GenServer (or a small set of them --\nstate the shape chosen and why in the moduledoc) implementing:\n`try_acquire(pool :: :global | {:tenant, tenant_schema :: String.t()}) ::\n{:ok, admission_ref()} | {:error, :capacity}` and\n`release(admission_ref()) :: :ok`. No HTTP or Poller-specific\nvocabulary anywhere in this module -- REQ-217/218 are the only\ncallers, and both are equally valid callers of the exact same API.\n2. Global cap computed once at supervised-child start from\n`Application.fetch_env!(:letflow, Letflow.Repo)[:pool_size]` (the\nSAME config key `config/runtime.exs` already writes) minus\n`reserved_headroom` (config, default 2), never re-read from a second\nindependently-configured value.\n3. Per-tenant cap computed as the equal fair-share split described in\ndecision 2 above, with a floor of 1 and recomputed as the set of\ncurrently-tracked tenants changes (a tenant with zero admission\nattempts in the tracking window is not counted against the split).\n4. `try_acquire/1` for `{:tenant, _}` MUST also consume one unit of\nthe SAME global budget (both gates are checked, not just the\nper-tenant one) -- decision 2's own \"both, composing\" requirement made\nconcrete and testable.\n5. Added to `Letflow.Application`'s supervision tree\n(lib/letflow/application.ex), following this file's own established\nTask.Supervisor-ordering discipline (state explicitly in the moduledoc\nwhether any other child must start before or after it, or that no\nordering dependency exists, mirroring REQ-173/REQ-166's own \"no\nordering dependency\" precedent comments in application.ex).\nNOT IN THIS REQUIREMENT: no plug, no route, no change to\napi_pipeline.ex or poller.ex (REQ-217, REQ-218); no tier-weighted caps\n(recorded above as a genuinely open product question); no change to\n`Letflow.SandboxPool` (a different resource, explicitly not absorbed,\nsee above).\n"
  },
  "task": {
    "description": "WF-02 Step 5: independent release validation of REQ-216 (Letflow.Admission core). Re-ran scripts/test_parallel.sh (TEST_PARALLEL_N=8) and mix letflow.check.test myself, in the foreground, rather than trusting TEST-RUNNER's step-08 report; re-derived the structural cause of every failure rather than accepting TEST-RUNNER's attribution by label; re-checked all 7 acceptance criteria directly against lib/letflow/admission.ex and test/letflow/admission_test.exs; confirmed docs/status/ and docs/requirements.yaml are not stale relative to what actually shipped.",
    "acceptance_criteria": [
      "Independent scripts/test_parallel.sh re-run, run in the foreground and waited on synchronously, real output quoted",
      "Independent mix letflow.check.test re-run, run in the foreground and waited on synchronously, real output quoted",
      "Every failure re-derived structurally (not accepted by count-matching or by TEST-RUNNER's label)",
      "All 7 of REQ-216's acceptance_criteria confirmed individually against current code/tests",
      "docs/status/ and docs/requirements.yaml checked for staleness relative to shipped code",
      "PASS/FAIL verdict with correct next-agent routing"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "VERDICT: PASS. Pulled latest (already up to date, includes commit 35443238 bumping requirement_status.index.yaml's declared v8.yaml entry count 14->15). Independently re-ran the suite myself, foreground, blocking (no run_in_background, no cross-turn-notification reliance -- polled each long-running process with a blocking `while kill -0 <pid>; do sleep N; done` loop across successive tool calls when the harness's own 590s per-call cap auto-backgrounded the shell):\n\n1. `TEST_PARALLEL_N=8 scripts/test_parallel.sh`: 'combined: 3067 tests, 6 properties, 2 failures (3071/3073 passed)'. Per-partition: partition 4 exit 2, all others exit 0. Both failures independently opened and read at test/mix/tasks/letflow_check_toolchain_test.exs:274 and :289 (Mix.Tasks.Letflow.CheckToolchainTest, 'rust pin (REQ-165)' describe block): both are `** (ErlangError) Erlang error: :enoent` raised from `System.cmd(\"rustc\", [\"--version\"], ...)` at lib/system.ex:1141. Independently confirmed the structural cause myself (not TEST-RUNNER's label): `which rustc` -> exit 1, `command -v rustc` -> exit 1, `rustc --version` -> 'command not found', exit 127. No rust toolchain installed on this host -- genuinely environmental, unconnected to Letflow.Admission/lib/letflow/application.ex or any file this requirement touched.\n\n2. Re-ran `Letflow.Docs.RequirementStatusInvariantsTest` in isolation (the file containing TEST-RUNNER's third, previously-reported A9 failure): 'Result: 20 passed', 0 failures -- independently confirms the just-pushed index-count fix (docs/status/requirement_status.index.yaml volume 8 `entries: 15`) resolved it. Also independently verified the counts match by direct read: `entries: 15` in the index vs. `grep -c '^  - req:' docs/status/requirement_status.v8.yaml` = 15 on-disk. TEST-RUNNER's combined run therefore had 3 failures; mine had 2 -- exactly the expected count drop from the index fix landing between runs, itself an instance of this project's own 'failure attribution is structural, never by count-matching' rule (I did not simply note '2 instead of 3, must be fixed' -- I opened the specific test file and re-ran it in isolation to confirm the fix directly).\n\n3. `mix letflow.check.test` (single-process, fresh warnings-as-errors-sensitive `mix test` subprocess, run separately per the RELEASE-VALIDATOR role's own stricter-check instructions): 'Finished in 1485.6 seconds ... Result: 3074/3076 passed (6/6 properties, 3068/3070 tests), 10 excluded / Failed: 2 tests', task exits with '** (Mix) mix letflow.check.test: FAILED -- underlying `mix test` run exited 2'. Read the full failure detail: the SAME 2 rust-pin :enoent failures, same file/lines, same stacktrace. Independently grepped the entire captured output for this task's own fixed detection substring, 'default values for the optional arguments' (the ISS-0069 dead-test-helper-default-argument class this stricter check exists to catch): zero matches. So `mix letflow.check.test`'s FAILED exit is attributable solely to the same 2 pre-existing environmental rustc-ENOENT failures, not to a warnings-as-errors/dead-default-argument regression -- confirmed structurally, not assumed.\n\n4. `mix compile --warnings-as-errors --force` (forced full recompile of all 183 .ex files, not relying on incremental-compiler cache reuse across separate invocations, which is exactly the mechanism ISS-0069 exploits): clean, zero output beyond 'Compiling 183 files (.ex)' / 'Generated letflow app' -- no warnings.\n\n5. `mix test test/letflow/admission_test.exs` in isolation: 'Result: 15 passed', 0.2s, 0 failures -- reconfirms independently of the full-suite runs.\n\nAC-BY-AC CONFIRMATION (against docs/requirements.yaml's REQ-216 entry, lines 160-167, and the actual current lib/letflow/admission.ex / test/letflow/admission_test.exs, not any prior agent's summary):\nAC1 (global cap = pool_size - reserved_headroom, both from config, cap changes with override): admission.ex:221-231 `init/1` computes `global_cap = max(pool_size - reserved_headroom, 1)` from `start_link/1`'s config-sourced values (admission.ex:152-171); test/letflow/admission_test.exs:30-61 'AC1' describe block, 3 tests, all pass, including the override-changes-the-cap test at line 41. CONFIRMED.\nAC2 (per-tenant caps independent across tenants): admission_test.exs:65-83, 1 test, passes -- tenant A's exhaustion at its own 5-unit share does not block tenant B's own acquisition. CONFIRMED.\nAC3 (tenant acquisition also gated by the global cap, even a fresh tenant with zero usage): admission.ex:261 `if state.global_in_use < state.global_cap and tenant_in_use < per_tenant_cap` -- both conditions required in one clause; admission_test.exs:89-101, 1 test, passes. CONFIRMED.\nAC4 (release frees exactly the acquired budget(s), idempotent on unknown/already-released ref): admission.ex:278-300 `{:release, ...}` clause keys off the server's own `refs` map, never the caller-supplied struct's `pool` field; admission_test.exs:105-172, 3 tests (global-only release, tenant-ref release frees both budgets, idempotent no-op verified against the actual `global_in_use` counter via `:sys.get_state/1`, not just the return value), all pass. CONFIRMED.\nAC5 (per-tenant cap = equal fair-share split, floor 1, recomputed live, never retroactive): admission.ex:310-311 `per_tenant_cap/1` = `max(div(global_cap, map_size(tenants)), 1)`, computed fresh on every call, never cached; admission_test.exs:176-234, 3 tests including the exact floor(10/3)=3 and floor(9/5)=1 cases from the requirement text and the never-retroactive-revocation case, all pass. CONFIRMED.\nAC6 (present in Letflow.Application's supervision tree, moduledoc states ordering stance): `grep -n Letflow.Admission lib/letflow/application.ex` -> line 65 `{Letflow.Admission, []},` with a comment at line 60 referencing 'no ordering dependency'; moduledoc's own 'Supervision-tree placement -- no ordering dependency (AC6)' section (admission.ex:100-113) states explicitly no start-order dependency in either direction; admission_test.exs:240-266, 3 tests (supervised-child assertion via Supervisor.which_children/1, live end-to-end usability, and the moduledoc-text assertion via Code.fetch_docs/1) all pass. CONFIRMED.\nAC7 (mix test and mix compile --warnings-as-errors both pass with real output quoted): see independent re-runs 1-4 above -- mix test's only 2 failures are the pre-existing rustc-ENOENT environmental flake, unconnected to this requirement; mix compile --warnings-as-errors --force is clean. CONFIRMED (with the pre-existing/environmental carve-out this project's own norms already treat as non-blocking, same as TEST-RUNNER's step-08 and consistent with how other merged requirements in this file's history have treated this exact same rustc gap).\n\nSTALENESS CHECK: docs/requirements.yaml REQ-216 entry (line 12613) still reads `status: in_progress` -- accurate, not yet flipped, correctly left for DOC-UPDATER next. docs/status/requirement_status.v8.yaml has exactly one REQ-216 event (line 698, `event: started`, 2026-09-02T22:45:00Z) -- no premature 'done' event exists. docs/status/requirement_status.index.yaml's volume-8 entry now correctly declares `entries: 15`, matching the 15 on-disk entries (independently counted). No docs/migration/decisions/ record is contradicted: decision 0014 (new-dependency policy) is honored (no new dependency, hand-rolled pure-BEAM GenServer); decision 0006 (multi-tenancy isolation posture) is honored (per-tenant fair-share composed with the global cap, not either alone). This is a plain WF-02 requirement run, not a stage-gate check, so no docs/migration/stage-N-*.md REVIEWER sign-off section applies here.\n\nNo rework needed. Routing to DOC-UPDATER to flip REQ-216's status to done and append the done event to docs/status/requirement_status.v8.yaml.",
    "artifacts_out": [
      "handoffs/WF02-REQ216-20260902/step-09-release-validator.json"
    ],
    "gaps": [],
    "next_action": "DOC-UPDATER: flip docs/requirements.yaml REQ-216 status in_progress -> done; append a 'done' event to docs/status/requirement_status.v8.yaml with a real UTC timestamp; release queue task 434 as done. The 2 rustc-ENOENT CheckToolchainTest failures remain pre-existing/environmental and out of scope for this requirement -- do not rework ELIXIR-DEV/TEST-DESIGNER for them; confirm they are already tracked as a queue issue (or file one) but this is not a blocking condition for REQ-216 itself."
  },
  "rework_count": 0,
  "max_rework": 3
}
