{
  "handoff_id": "58c85883-087c-48cb-a552-c73a57b8d81c",
  "run_id": "WF03-ISS0119-20260821",
  "workflow_id": "WF-03",
  "step": "02",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF03-ISS0119-20260821/step-02-code-designer.json",
  "created_at": "2026-08-21T04:59:07Z",
  "started_at": "2026-08-21T04:59:07Z",
  "completed_at": "2026-08-21T05:08:10Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "requirement_text": {},
    "related_handoff_ids": [
      "b7ba0a2e-ca5a-433d-bbcb-ba5832c19f33",
      "087f7db0-c969-4daa-983e-094ec3f7ae1f"
    ],
    "artifacts_in": [
      "handoffs/WF03-ISS0119-20260821/step-01-issue-fixer-diagnose.json",
      "handoffs/WF03-ISS0119-20260821/step-005-issue-fixer-registry-lookup.json",
      "docs/issues/ISS-0119.yaml",
      "docs/status/requirement_status.yaml",
      "docs/anti-patterns.md",
      "docs/agents/instructions/core-directives.md",
      ".claude/agents/doc-updater.md",
      "docs/agents/protocols/TASK_QUEUE.md"
    ],
    "owned_modules": [
      "docs/status/requirement_status.yaml",
      "docs/status/",
      "docs/agents/instructions/core-directives.md",
      "docs/anti-patterns.md",
      "docs/issues/ISS-0119.yaml",
      "docs/agents/protocols/TASK_QUEUE.md",
      ".claude/agents/doc-updater.md",
      "lib/letflow/design/"
    ],
    "branch_name": "feature/WF03-ISS0119-20260821",
    "issue_ids": ["ISS-0119"]
  },
  "task": {
    "description": "WF-03 Step 2 (fix design) for ISS-0119. Write the design artefact to `lib/letflow/design/iss-0119-status-file-readability.md`. Design only -- no implementation, no finished replacement prose pasted in as if it were the artefact's own content is fine ONLY where the design is specifying exact required wording (see 'What a design means here' below).\n\nREAD THE STEP 1 DIAGNOSIS FIRST (`step-01-issue-fixer-diagnose.json`, result.summary). It is long and it is load-bearing. Do not re-derive its measurements; do re-check anything you intend to rely on structurally. Its established facts, which you may take as given:\n  - The file is 361,376 bytes / 5,766 lines / 182 entries. A whole-file Read is REFUSED outright (error quoted verbatim in the handoff), not truncated. It first crossed the limit at commit 3d19e8c on 2026-08-18T15:32:52Z, so the safeguard has been unexecutable for ~2.5 days.\n  - MACHINE-PARSED CONSUMERS: ZERO. mix.exs:50 and scripts/timed_test.sh:8 are comments; .github/ has no reference. A split breaks no code and no gate. This is real design freedom.\n  - 23 LIVE INSTRUCTION SITES across 12 files tell an agent to read or append to this file (doc-updater.md carries 4, core-directives.md 3, WF-02 3, AGENT_SYSTEM.md 3). The full enumeration is in the Step 1 handoff -- use it.\n  - ~103 immutable historical references under handoffs/ and test/reports/ that cannot be retro-edited, plus ONE line-number citation that a split would silently falsify: lib/letflow/design/iss-0078-pin-rebind-provenance.md:367 cites `docs/status/requirement_status.yaml:5158`.\n  - SCHEMA HAS ALREADY DRIFTED, and pre-dates the size problem: `SCOPE-CHANGE` appears 31x as a `req:` value and 3x as an `event:` value (lines 3490/3577/3744, all ORCH, 2026-08-17). The header documents 3 `event` values; 7 are in use (done 107, started 54, cancelled 8, blocked 5, revised 4, SCOPE-CHANGE 3, verified 1). `req: ISS-NNNN` has zero precedent.\n  - docs/agents/protocols/TASK_QUEUE.md:133 instructs recording a reconciliation 'as a SCOPE-CHANGE event' -- the WRONG FIELD versus 31 entries of `req:` practice. The 3 divergent entries are the ones that followed the written rule. This is a documented instruction actively producing the drift.\n\nSCOPE DECISION MADE BY ORCH, do not relitigate: ISSUE-FIXER reported two MINOR incidental findings. Finding (1) -- the `event` vocabulary drift and the TASK_QUEUE.md:133 wrong-field instruction -- IS IN SCOPE for this run. It is the same header-documentation defect as ISS-0119's own second gap, in the same header, and shipping a rewritten header that still under-documents the vocabulary would leave the file wrong in the same way. Finding (2) -- ISS-0119's own stale line/precedent counts -- is bookkeeping for DOC-UPDATER at Step 6, not a design input.\n\nYOUR DECISION TO MAKE. ISS-0119 proposes two remedies and explicitly declines to prescribe: (a) make the file readable again -- split by period with an index, or otherwise bound its growth, preserving the full history since this is audit trail and nothing may be dropped; (b) rewrite the header so it prescribes an EXECUTABLE safeguard rather than one assuming a full read -- naming the exact scoped-read technique that reveals the schema, and stating the conventions explicitly. Choose (a), (b), or both, and JUSTIFY the choice against the diagnosis. Two constraints on that choice: ISS-0119 explicitly forbids resolving this by relaxing the append-only rule (the rule is correct; the mechanism supporting it degraded), and whatever you choose MUST close the core-directives.md:173 vs :328 precedence gap -- as it stands an agent has two rules pointing opposite ways for this one file and nothing saying which wins.\n\nWHAT A DESIGN MEANS HERE. This is a docs/process change, not Elixir, so the usual 'interfaces and @specs' shape does not apply literally. Your artefact must instead specify, precisely enough that ELIXIR-DEV/DOC-UPDATER can execute it without inventing anything:\n  - Every file to be changed or created, by path, and for each: exactly what changes.\n  - If you choose a split: the exact split boundary rule, the index file's structure, what happens to the original path, and how a reader resolves an old citation (including the :5158 line reference and the ~103 historical mentions). State explicitly how the split is performed such that NO entry is dropped, and how that is verified afterwards.\n  - The exact required NEW header text for whatever file(s) an agent appends to. Quote it in full -- this is the one place finished prose belongs in this artefact, because the whole defect is that the current header prescribes an unexecutable procedure. The new procedure must be one an agent can actually run today and must be stated as concrete commands or tool calls, not as an aspiration.\n  - The full documented vocabulary: every legal `req:` value including SCOPE-CHANGE, every legal `event:` value with the 7 actually in use accounted for, and what to do about the 3 malformed entries. NOTE CONSTRAINT C2 from the diagnosis: nothing may be dropped and the 3 malformed entries must NOT be silently normalized, since that would itself be the rewrite this whole safeguard exists to prevent. Design how they are handled honestly.\n  - Which of the 23 instruction sites must be edited and what each must say afterwards, including the TASK_QUEUE.md:133 field correction.\n  - How the fix behaves at the NEXT growth threshold (diagnosis constraint C7) -- a fix that merely resets the clock is a worse outcome than one that defines what happens when the limit is next approached.\n  - What the Step 4 regression test can actually assert. This is docs, so there is no ExUnit assertion of prose -- but a mechanically checkable invariant may well exist (for example: that no appendable status file exceeds the read limit, or that every entry's `req`/`event` value is in the documented vocabulary). Propose what is checkable and where it would live; if you conclude nothing mechanical is checkable, say so explicitly and justify it, because that materially changes what Step 4 can do.\n\nDo not write the implementation. Do not edit docs/status/requirement_status.yaml, core-directives.md, or any of the 23 sites in this step -- the artefact describes the change; ELIXIR-DEV performs it at Step 3.",
    "acceptance_criteria": [
      "lib/letflow/design/iss-0119-status-file-readability.md exists and is the only file created or modified outside this handoff",
      "The artefact states which remedy is chosen -- (a), (b), or both -- and justifies the choice against the Step 1 diagnosis rather than against ISS-0119's prose alone",
      "The artefact closes the core-directives.md:173 vs :328 precedence gap explicitly, stating which rule wins for this file and where that ruling is written down",
      "The artefact does NOT relax the append-only rule and says so",
      "Every file to be changed or created is named by path with its exact required change",
      "If a split is chosen: the split boundary rule, index structure, fate of the original path, old-citation resolution (including the :5158 line reference), the no-entry-dropped method and its verification are all specified",
      "The exact required new header text is quoted in full, and the procedure it prescribes is executable today as concrete commands or tool calls",
      "The full req: and event: vocabularies are documented, all 7 in-use event values accounted for, and the handling of the 3 malformed entries is designed without silently normalizing them",
      "The TASK_QUEUE.md:133 wrong-field instruction is addressed",
      "Behaviour at the next growth threshold is specified",
      "A mechanically checkable invariant for Step 4 is proposed with its location, or its absence is explicitly justified",
      "No implementation is performed -- no edits to docs/status/, core-directives.md, or any instruction site"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "WF-03 Step 2 fix design for ISS-0119 complete. Artefact: lib/letflow/design/iss-0119-status-file-readability.md (the only file created or modified by this step besides this handoff). No implementation performed: docs/status/requirement_status.yaml, core-directives.md and all 23 instruction sites are untouched.\n\n=== REMEDY CHOSEN: BOTH (a) AND (b), AS ONE STEP 3 COMMIT ===\n\nJustified against the Step 1 diagnosis, not ISS-0119's prose. (b) alone fails on the diagnosis's C7 (\"it leaves C7 unaddressed -- the file keeps growing and the scoped-read window keeps covering a smaller fraction\"): a header-only fix converts today's HARD failure (a refusal that announces itself) into a SOFT one (a compliant-looking partial read), which is strictly worse. (b) alone also cannot answer the diagnosis's strongest finding -- the three event: SCOPE-CHANGE entries were written 2026-08-17 at 234,147 bytes, while the full read still WORKED, proving \"the read-in-full link was already weak as a schema-transmission mechanism before it broke outright.\" Prescribing a SCOPED read of a file whose schema a FULL read already failed to transmit cannot be the whole answer. (a) alone transmits no schema -- the diagnosis warns a split \"multiplies the L2 problem\" without the header carried into every shard -- and leaves the 23 sites, TASK_QUEUE.md:133 and the precedence gap untouched. One commit, because (a)'s site edits and (b)'s edits land in the same files; splitting across runs would leave instructions describing a layout that does not exist.\n\n=== THE SHAPE: FREEZE-AND-ROLL, NOT A RETROACTIVE SPLIT ===\n\nDriven by C4. Re-checked structurally for this design: line 5158 of the status file falls inside the note: prose of a REQ-059/PIN-05 entry, so ANY line inserted or removed above it silently falsifies iss-0078-pin-rebind-provenance.md:367. Therefore the existing file is NOT split, renamed, reordered, or edited above its last line. It is closed in place as VOLUME 1, byte-identical through line 5,766, with a closure footer APPENDED AT EOF (appending shifts nothing). New appends go to a new small VOLUME 2. C4 is satisfied by construction: all ~103 immutable historical references still resolve, and :5158 still points at the same characters -- no old-citation resolution scheme is needed because no citation is broken.\n\nLayout: docs/status/requirement_status.yaml (v1, closed/frozen) + docs/status/requirement_status.v2.yaml (current) + docs/status/requirement_status.index.yaml (NEW fixed-name entry point that every instruction site now points at, so no agent ever needs to know the volume-naming scheme).\n\nNO-ENTRY-DROPPED METHOD: there is no data-moving step at all. All 182 entries stay in the file and at the line numbers they already occupy; the only write to v1 is comment lines at EOF. Verified at Step 3 by six quoted checks: git diff --numstat deletions must be 0; grep -c '^  - req: ' must still be 182; sed -n '5158p' must return the same text; sed -n '3490p;3577p;3744p' must still return three malformed lines; the frozen-prefix SHA-256 recorded in the index must verify; whole-commit numstat must show insertions-only for v1.\n\n=== APPEND-ONLY RULE: NOT RELAXED, AND THE ARTEFACT SAYS SO EXPLICITLY (design section 1) ===\n\nStrengthened rather than relaxed. Volume 1 becomes MORE protected: assertion A6 hashes its 5,766-line prefix, so a silent rewrite now fails the suite -- the first mechanical enforcement this rule has ever had (the diagnosis found \"there is no second line of defence\"). What changes is only which file the next append lands in and how large an APPENDABLE file may become -- the mechanism, not the rule.\n\n=== PRECEDENCE GAP (core-directives.md:173 vs :328) CLOSED ===\n\nRULING: :173's scoped-read rule is the general rule and governs every CLOSED volume and every other large file. The full-read requirement survives ONLY for the CURRENT volume, and only because the roll rule bounds that volume so the read stays executable. The design does not re-litigate the original resolution (the diagnosis showed it was deliberate and correct); it makes the winning branch AVAILABLE again and then states the fallback that was missing -- closed volumes are never appended to, so they never need a full read. Written down in THREE places so an agent hits it from any direction: core-directives.md:328 (primary, with a forward-reference to :173), core-directives.md:173 (back-reference to item 3, so an agent reading the general rule learns of the exception), and the index file's header. Both sites' exact replacement text is quoted in design section 10.3.\n\n=== NEW HEADER TEXT: QUOTED IN FULL, EXECUTABLE TODAY (design section 4) ===\n\nA six-step HOW TO APPEND procedure in concrete commands for both shells: (1) grep -n 'status: current' -B 2 on the index to confirm the target volume; (2) one un-scoped Read of the current volume, possible BY CONSTRUCTION, with an explicit STOP-and-file instruction if it is ever refused or truncated -- the fallback whose total absence was the diagnosis's core finding; (3) date -u / (Get-Date).ToUniversalTime() for the clock; (4) append; (5) git diff --numstat as a POSITIVE verification that the deletions count is 0; (6) the roll rule. Nothing is aspirational.\n\n=== VOCABULARY FULLY DOCUMENTED; 3 MALFORMED ENTRIES HANDLED WITHOUT NORMALIZING ===\n\nreq: exactly two legal values -- REQ-NNN (151) and SCOPE-CHANGE (31). The header states outright that req: ISS-NNNN IS NOT LEGAL and has zero precedent across 182 entries, and that issue fixes use req: SCOPE-CHANGE with the ISS id in note: -- killing the obvious wrong guess ISS-0119 identified.\nevent: exactly six legal values, all 7 in-use values accounted for -- done 107, started 54, blocked 5, cancelled 8, revised 4, verified 1 documented as legal (cancelled/revised/verified were previously undocumented); SCOPE-CHANGE 3 declared NOT legal as an event.\nThe three malformed entries (v1 lines 3490/3577/3744) are NOT edited, now or ever; declared three times over (v1's closure footer, the index's known_anomalies:, the current volume's header); ATTRIBUTED to TASK_QUEUE.md:133 so a reader sees they were compliance with a wrong instruction; and MACHINE-PINNED by assertion A5, which asserts the on-disk anomaly set equals the declared set EXACTLY. That protects C2 in both directions: new drift fails, and a silent later normalization ALSO fails.\n\n=== TASK_QUEUE.md:133 ADDRESSED ===\n\nExact replacement quoted (design 10.4): record as an entry with req: SCOPE-CHANGE and a normal event: value, plus an explicit \"SCOPE-CHANGE is a req value, never an event value\" and a note that the earlier wording produced the three malformed entries. TASK_QUEUE.md:379 also corrected to point at volume 1's req: SCOPE-CHANGE entries.\n\n=== NEXT GROWTH THRESHOLD (C7) ===\n\nBound: current volume <= 1,200 lines AND <= 120,000 working-tree bytes -- ~60% of the Read tool's 2,000-line default and ~46% of its 262,144-byte limit. BOTH limits are respected, because a line-truncated read is the silent failure mode a byte-only bound would miss. Trigger: checked by DOC-UPDATER immediately AFTER its own append, so the volume that fills is closed by the agent that filled it, in the same commit -- no deferred cleanup and no chance for the next agent to hit a refusal. Second, independent trigger: assertion A3 fails mix test if any current volume is over bound, so the safeguard no longer rests solely on agent diligence. Unbounded: volume n names n+1, the index always names the current one, so there is no future threshold at which ISS-0119 recurs.\n\n=== STEP 4 MECHANICAL INVARIANT: YES, ONE EXISTS ===\n\ntest/docs/requirement_status_invariants_test.exs -- ExUnit, no database, no new deps (stdlib line parsing plus :crypto). Eight assertions, all derived from the index so no volume list is hardcoded. A3 IS THE ISS-0119 REGRESSION ASSERTION: the current volume must be within the roll rule's bounds -- it would have FAILED on 2026-08-18 at commit 3d19e8c, 2.5 days before ISS-0119 was filed by hand. A6 (SHA-256 of each closed volume's frozen prefix, CRLF-normalized for platform stability) is the first mechanical enforcement of the append-only rule in this repo's history. Also A1 index/disk agreement, A2 exactly-one-current, A4 vocabulary conformance, A5 exact anomaly set, A7 volume-chain integrity, A8 header completeness.\n\n=== SCOPE ===\n\nORCH's scope decision honoured: incidental finding (1) (event-vocabulary drift + TASK_QUEUE.md:133) is designed in full; finding (2) (ISS-0119's stale counts) is left to DOC-UPDATER at Step 6 and is not a design input. All 23 instruction sites are dispositioned in a table -- 14 EDIT with required post-edit text, 9 NO CHANGE with the reason each remains true (they survive precisely because v1's path and line numbers are unchanged). Step 3 touches 3 new files and 10 modified files, all named by path. Four open questions are recorded rather than silently resolved (volume naming, the ceiling values, whether the test also becomes a standalone mix letflow.check step, and the permanence of the anomalies); none blocks Step 3.",
    "artifacts_out": [
      "lib/letflow/design/iss-0119-status-file-readability.md",
      "handoffs/WF03-ISS0119-20260821/step-02-code-designer.json"
    ],
    "issues": [],
    "next_action": "Route to CODE-DESIGN-VALIDATOR (WF-03 Step 2 gate). On PASS, route to ELIXIR-DEV for Step 3 implementation -- the design's section 11 is the exact file list, and section 8's six verification checks must be run and quoted in ELIXIR-DEV's handoff. ELIXIR-DEV must compute the frozen_prefix_sha256 placeholder in the index. TEST-DESIGNER at Step 4 implements test/docs/requirement_status_invariants_test.exs per design section 7 (A3 and A6 are the load-bearing assertions). DOC-UPDATER at Step 6 also corrects ISS-0119's stale line/precedent counts per ORCH's scope decision."
  },
  "rework_count": 0,
  "max_rework": 3
}
