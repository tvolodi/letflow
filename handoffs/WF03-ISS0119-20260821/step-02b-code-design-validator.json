{
  "handoff_id": "f3ae8489-fa1d-40bb-9450-ec40945d3dc6",
  "run_id": "WF03-ISS0119-20260821",
  "workflow_id": "WF-03",
  "step": "02b",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGN-VALIDATOR",
  "file": "handoffs/WF03-ISS0119-20260821/step-02b-code-design-validator.json",
  "created_at": "2026-08-21T05:09:50Z",
  "started_at": "2026-08-21T05:09:50Z",
  "completed_at": "2026-08-21T05:31:17Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "requirement_text": {},
    "related_handoff_ids": [
      "58c85883-087c-48cb-a552-c73a57b8d81c",
      "b7ba0a2e-ca5a-433d-bbcb-ba5832c19f33"
    ],
    "artifacts_in": [
      "lib/letflow/design/iss-0119-status-file-readability.md",
      "handoffs/WF03-ISS0119-20260821/step-02-code-designer.json",
      "handoffs/WF03-ISS0119-20260821/step-01-issue-fixer-diagnose.json",
      "docs/issues/ISS-0119.yaml",
      "docs/status/requirement_status.yaml",
      "docs/agents/instructions/core-directives.md",
      "docs/agents/protocols/TASK_QUEUE.md",
      ".claude/agents/doc-updater.md",
      "docs/anti-patterns.md"
    ],
    "owned_modules": [
      "lib/letflow/design/"
    ],
    "branch_name": "feature/WF03-ISS0119-20260821",
    "issue_ids": [
      "ISS-0119"
    ]
  },
  "task": {
    "description": "WF-03 Step 2 gate (hard gate on CODE-DESIGNER) for ISS-0119. Verdict from the ARTEFACT ITSELF -- `lib/letflow/design/iss-0119-status-file-readability.md`, 737 lines. Do NOT validate from CODE-DESIGNER's result.summary; reading a producer's own summary and echoing it back is not validation (core-directives.md, 'Every producing step has a validating step').\n\nThe design chose BOTH remedies via a 'freeze-and-roll' scheme: close `docs/status/requirement_status.yaml` in place as Volume 1 byte-identical through line 5,766, create `requirement_status.v2.yaml` as the current volume, and create a fixed-name `requirement_status.index.yaml` as the entry point every instruction site points at. Your job is to establish whether that design is correct, complete, and unambiguous enough to build from -- not whether it is the remedy you would have picked.\n\nCHECK EACH OF THESE INDEPENDENTLY, and record a per-item verdict:\n\n1. AC COVERAGE. The 12 acceptance criteria in `step-02-code-designer.json`'s task.acceptance_criteria. Map each to the specific design section that satisfies it. Any criterion with no concrete design element is a BLOCKER.\n\n2. THE CENTRAL CLAIM IS ACTUALLY TRUE. The design asserts C4 is satisfied 'by construction' because Volume 1 is frozen in place, so `lib/letflow/design/iss-0078-pin-rebind-provenance.md:367`'s citation of `requirement_status.yaml:5158` still resolves. VERIFY THIS YOURSELF: open that design doc at line 367, confirm what it actually cites, then confirm the design's freeze rule genuinely inserts nothing above line 5,766 -- including its own closure footer. If the footer is appended at EOF, state the current EOF line number and confirm 5,158 is unaffected. If any part of this does not hold, it is a BLOCKER: the whole justification for freeze-and-roll over a retroactive split rests on it.\n\n3. THE HEADER PROCEDURE IS EXECUTABLE TODAY. §4 quotes the required new header in full and claims a six-step procedure with real commands for both shells. Read those commands and judge whether an agent could literally run them right now on this repo. Specifically: do they work on Windows/PowerShell AND bash (this repo runs both), do they depend on any tool not already present, and does the STOP-and-file fallback trigger on the real failure mode (a refused read) rather than on a hypothetical one? A procedure that is merely better-worded but still not runnable fails this gate -- that is the exact defect ISS-0119 is about.\n\n4. THE APPEND-ONLY RULE IS NOT RELAXED. The design claims it strengthens the rule via assertion A6 (a sha256 over Volume 1's 5,766-line prefix). Confirm the design nowhere permits editing a past entry, and confirm A6 as specified would actually detect a retroactive edit. Check A6 is well-defined enough to implement: what exactly is hashed, with what line-ending handling. THIS REPO IS CRLF ON DISK AND LF IN GIT -- a hash spec that does not say which it covers is ambiguous and is at least a MAJOR issue, because the assertion would then pass or fail depending on checkout settings rather than on content.\n\n5. THE 3 MALFORMED ENTRIES ARE HANDLED HONESTLY. Diagnosis constraint C2: nothing dropped, and the 3 `event: SCOPE-CHANGE` entries must NOT be silently normalized. The design claims A5 pins them in BOTH directions (new drift fails, later silent normalization also fails). Verify that is what §5 and §7 actually specify.\n\n6. VOCABULARY COMPLETENESS. §5 must account for all 7 in-use `event` values (done 107, started 54, cancelled 8, blocked 5, revised 4, SCOPE-CHANGE 3, verified 1) and both `req` values. The design says 'exactly six legal values' for `event` while 7 are in use -- confirm the seventh is the 3 malformed entries being excluded by design and not an oversight. If any in-use value is silently dropped from the documented vocabulary without an explicit ruling, that is a BLOCKER, because the next append would then be told its own precedent is illegal.\n\n7. THE 23 SITES ARE FULLY DISPOSITIONED. §10 claims 14 EDIT with post-edit text and 9 NO CHANGE with reasons. Count them. 14 + 9 = 23 must actually hold, each EDIT must carry text specific enough to apply without invention, and each NO CHANGE must carry a reason. Cross-check the site list against the Step 1 diagnosis's enumeration -- a site present in the diagnosis but missing from §10 is a BLOCKER.\n\n8. THE PRECEDENCE RULING IS COHERENT. §9 rules that core-directives.md:173 (scoped reads) governs closed volumes and everything else, while the full read survives only for the CURRENT volume, bounded by the roll rule. Judge whether that is self-consistent and whether writing it in three places creates a restatement hazard this project otherwise avoids (its convention is one canonical home plus pointers).\n\n9. NO IMPLEMENTATION LEAKED IN. The artefact is a design. Quoted required file content (the new header, replacement text for instruction sites) is legitimate and expected here -- that is the specification. What is NOT legitimate is the design having actually edited any of the target files. Verify with `git show --stat 24e7087` that the commit touched only the artefact and the handoff.\n\n10. STEP 4 IS BUILDABLE. §7 proposes `test/docs/requirement_status_invariants_test.exs`, ExUnit, no DB, no new deps, 8 assertions, and claims A3 would have FAILED at commit 3d19e8c on 2026-08-18. Judge whether TEST-DESIGNER could write that file from §7 alone. The fail-first proof matters here: WF-03 Step 4 requires a test that fails pre-fix and passes post-fix, so confirm §7 gives TEST-DESIGNER a concrete way to demonstrate that.\n\n11. OPEN QUESTIONS. §12 records 4, claiming none blocks Step 3. ORCH has accepted the defaults in #1, #2 and #4 as design decisions within CODE-DESIGNER's authority, and routes #3 (whether the invariant test should also be a standalone `mix letflow.check` step) to REVIEWER at Step 3d. Confirm none of the 4 in fact hides an unresolved decision ELIXIR-DEV would have to invent an answer to.\n\nVERDICT RULES. PASS only if every check above passes. FAIL if any BLOCKER stands -- and on FAIL, name each gap by acceptance criterion with the specific missing element, per HANDOFF_PROTOCOL.md §2's validator-fail worked example. Do not fix the design yourself and do not edit the artefact.\n\n=== RE-GATE PASS 2 (dispatched by ORCH 2026-08-21T05:27Z, after CODE-DESIGNER completed REWORK ITERATION 1 at commit 5b6333d) ===\n\nYour pass-1 FAIL was correct and it did the job a hard gate exists to do -- it caught a design about executability that was itself not executable on this host's primary shell. The artefact has grown 737 -> 905 lines. This is a TARGETED re-gate, not a full re-read: checks 2, 4, 5, 6, 8, 9 and 11 passed on pass 1 and the freeze-and-roll shape is unchanged, so do not re-derive them from scratch. Re-run check 9 only (via `git show --stat 5b6333d`, to confirm the rework commit still touched nothing but the artefact and the handoff), and spot-check anything the rework plausibly disturbed.\n\nRE-VERIFY THE TWO BLOCKING FINDINGS INDEPENDENTLY. Do not accept CODE-DESIGNER's rework account as evidence that either is fixed -- read the artefact.\n\nR1 (was BLOCKER, AC7). CODE-DESIGNER reports Section 4 step 1 is now a labelled Bash/PowerShell pair, the Section 4 preamble now states why shell coverage is part of the spec and quotes the CommandNotFoundException text, and that the sweep went BEYOND the two sections you named -- into 3.1's closure footer, Section 8's six verification checks (previously bash-only, now claimed to be pairs), and 10.2/10.3's targeted-read prose. VERIFY BY EXECUTION, the same way you found the defect: actually run the PowerShell form the design now specifies (`Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0`) and confirm it is a real cmdlet invocation that parses -- the index file does not exist yet, so expect a not-found error from the PATH/file layer, NOT a CommandNotFoundException or a parser error; those two would mean the replacement is still unrunnable. Then sweep the WHOLE artefact yourself for any remaining single-shell command: grep the artefact for grep/sed/awk/wc/head/date/sha256sum occurrences that are not inside a labelled Bash block or paired with a PowerShell form. CODE-DESIGNER also reports removing two fragile line-continuations (a backslash and a backtick inside a YAML comment block) that it had introduced mid-rework -- confirm none remain, since a continuation character inside a header that gets copied verbatim into every future volume is exactly the propagating defect the preamble's new forward-binding rule is meant to prevent.\n\nR2 (was MAJOR, WF-03 Step 4 fail-first). The claimed fix is a design decision, and it is the one to scrutinise hardest because it is clever enough to be wrong in a way that reads as right. The argument: freeze-and-roll leaves docs/status/requirement_status.yaml byte-identical forever, that file IS the pre-fix current volume, so a live assertion A3b -- within_bounds?(\"docs/status/requirement_status.yaml\", max_lines, max_bytes) returning {:error, %{over: [:lines, :bytes]}} -- demonstrates the regression on every mix test run without any checkout of 3d19e8c. JUDGE WHETHER THAT ACTUALLY SATISFIES WF-03 Step 4's requirement, quoting Step 4's own words. Specifically: does asserting that a now-FROZEN file is over the ceiling prove the test would have caught the bug, given that under the fix that file is no longer the append target and is expected to be over the ceiling by design? Is A3b a regression test or a tautology restating the freeze? If you judge it sufficient, say why in terms Step 4's language supports. If you judge it insufficient, that is a BLOCKER and the optional git-show corroboration in 7.1 does not rescue it, because 'optional' means it may not run.\n\nAlso re-check R2's second-order effects, which pass 1 could not have seen: Section 7 now specifies a SECOND new file, test/support/status_history.ex (module Letflow.Test.StatusHistory, six functions given by signature only). Confirm (a) the six signatures are complete enough for TEST-DESIGNER to implement without inventing behaviour, (b) the claim that mix.exs:20 already compiles test/support in :test is TRUE -- open mix.exs and check, do not take it on report, since if false Step 3 needs a build-config change nobody has planned, and (c) that no function in that module hardcodes the index location, as the design claims, since that is what makes the helper reusable against an arbitrary path.\n\nR3/R4/R5 (the three MINORs). Confirm each is actually addressed: the frozen_prefix_sha256 terminator convention is now normative and unambiguous (CODE-DESIGNER claims map_join with a terminator after EVERY line including the last, explicitly not Enum.join, with a NORMATIVE Bash reference form); A8 now ties the volume header's quoted ceilings to the index's roll_rule AND the three unchecked prose copies became pointers, leaving exactly two machine-checked copies and zero unchecked ones; Section 11's counts now read 4 created / 9 modified and Section 4 step 2 reports both bounds in both shells. For the ceilings in particular, verify the claim of ZERO remaining unchecked prose copies by grepping the artefact for the literal numbers 1,200 and 120,000 -- a fourth copy surviving somewhere would mean the fix is partial.\n\nCOUNT CORRECTION. CODE-DESIGNER reports writing the corrected arithmetic (15 EDIT + 8 NO CHANGE across 23 numbered sites; 16 EDIT across 24 rows including the added core-directives.md:173 back-reference) into Section 10.1 above the table, with an explicit retraction. Confirm it is there and that it matches your own pass-1 count, so Step 3 checks itself against the right number.\n\nVERDICT RULES UNCHANGED. PASS only if R1 and R2 are both genuinely fixed and no new defect was introduced by the rework. On FAIL, this becomes rework iteration 2 of 3, and ORCH has already told CODE-DESIGNER that a second FAIL on the same executability ground must trigger a change of strategy rather than a third patch -- so if R1 is still broken, say so plainly and name the strategy problem rather than the line.",
    "acceptance_criteria": [
      "The verdict is derived from lib/letflow/design/iss-0119-status-file-readability.md itself, and result.summary names the sections read",
      "All 12 of Step 2's acceptance criteria are mapped to specific design sections, with any uncovered criterion raised as a BLOCKER",
      "Check 2 is independently verified by opening iss-0078-pin-rebind-provenance.md:367 and confirming the :5158 citation still resolves under the freeze rule",
      "Check 3 states whether the header's commands are runnable today under both PowerShell and bash on this repo",
      "Check 4 states whether A6's hash spec is unambiguous about CRLF-vs-LF, flagging at least MAJOR if not",
      "Checks 5 and 6 confirm the 3 malformed entries are pinned without normalization and that all 7 in-use event values are explicitly accounted for",
      "Check 7 verifies the 14 EDIT + 9 NO CHANGE sites sum to 23 and cross-checks them against the Step 1 diagnosis enumeration",
      "Check 9 verifies via git show --stat 24e7087 that no target file was edited by the design step",
      "Check 10 states whether TEST-DESIGNER could build the Step 4 test from §7 alone, including the fail-first demonstration",
      "A per-item verdict is recorded for all 11 checks",
      "The design artefact is not modified by this step"
    ]
  },
  "result": {
    "status": "FAIL",
    "summary": "WF-03 Step 2 GATE VERDICT: **FAIL** (1 BLOCKER, 1 MAJOR, 3 MINOR). Rework to CODE-DESIGNER; rework_count 0 -> 1 of 3.\n\nVerdict derived from the artefact, not from CODE-DESIGNER's result.summary. Sections read in full: title block, S1 (decision), S2 (target layout), S3.1/3.2/3.3 (exact per-file changes incl. both quoted new files), S4 (the quoted volume header, all six HOW-TO-APPEND steps, both vocabulary blocks, the roll rule), S5 (vocabulary/anomalies), S6 (growth threshold), S7 (A1-A8), S8 (no-entry-dropped + 6 verification commands), S9 (precedence ruling), S10.1-10.6 (all 24 site rows + every replacement text), S11 (file list), S12 (4 open questions). Cross-read: step-01 diagnosis result.summary sections 0-5, step-02 handoff task.acceptance_criteria, core-directives.md, HANDOFF_PROTOCOL.md S1/S2/S4, WF-03 Step 4.\n\nThe design is strong and mostly buildable. It fails on exactly the thing ISS-0119 is about: one step of its own \"executable\" procedure is not executable on this host's primary shell.\n\n=== PER-ITEM VERDICTS (11/11 recorded) ===\n\nCHECK 1 - AC COVERAGE: FAIL (1 of 12 uncovered). Mapping:\n AC1 -> title block + verified by git show (check 9). PASS.\n AC2 -> S1 \"Why not (b) alone\" / \"Why not (a) alone\" / \"Why both\" - each justified by direct quotation of the Step 1 diagnosis (C7, the 2026-08-17 L2 finding, the \"multiplies the L2 problem\" warning), not ISS-0119 prose. PASS.\n AC3 -> S9 + S10.3. PASS.\n AC4 -> S1 final para, S7 A6, S10.2 (doc-updater :47). PASS.\n AC5 -> S3, S10.2-10.6, S11. PASS.\n AC6 (conditional) -> boundary/roll rule S6, index structure S3.3, fate of original path S2+S3.1, :5158 resolution S8, no-entry-dropped method + 6 verification commands S8. PASS.\n AC7 -> S4. **BLOCKER, see check 3.** Header text is quoted in full (that half is satisfied); the \"executable today as concrete commands\" half is not.\n AC8 -> S4 vocabulary blocks + S5 table. PASS.\n AC9 -> S10.4. PASS.\n AC10 -> S6. PASS.\n AC11 -> S7 (test/docs/requirement_status_invariants_test.exs, A1-A8). PASS on the letter; see check 10 MAJOR.\n AC12 -> verified by git show (check 9). PASS.\n\nCHECK 2 - THE CENTRAL CLAIM: **PASS, independently verified.**\n - lib/letflow/design/iss-0078-pin-rebind-provenance.md:367 reads verbatim: \"  `docs/status/requirement_status.yaml:5158`,\" - it sits inside the \"Prose-only references, not call sites\" bullet. Citation confirmed, by line number, exactly as the design describes.\n - sed -n '5158p' docs/status/requirement_status.yaml returns \"      merge_effective_pins, returning a distinct UnknownPinRef and\" - inside the note: prose of the REQ-059/PIN-05 entry. The design's structural claim is accurate.\n - wc -l docs/status/requirement_status.yaml = 5766. Current EOF line = 5766, matching the design's frozen-prefix figure exactly. The S3.1 footer is specified as an append \"after the current last line (5,766)\", \"Nothing above line 5,766 may be touched\". An append at EOF inserts only at 5767+, so line 5,158 does not move. C4 holds by construction; :5158 still resolves. Nothing in S3.1, S3.2, S3.3 or S10 writes above line 5,766, and S10 explicitly forbids editing iss-0078-pin-rebind-provenance.md:367.\n\nCHECK 3 - HEADER PROCEDURE EXECUTABLE TODAY: **FAIL - BLOCKER.**\n S4 opens with the absolute claim \"Every command below is runnable today on this repo.\" That claim is false on this host's primary shell, and I verified it rather than reasoning about it. Ran in PowerShell: `grep --version` -> \"The term 'grep' is not recognized as the name of a cmdlet, function, script file, or operable program.\" CommandNotFoundException.\n S4 HOW-TO-APPEND step 1 is the only step of the procedure given as a single unlabelled command, and it is a GNU-grep invocation:\n     grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml\n Steps 2, 3 and the roll-rule block are correctly given as labelled Bash/PowerShell pairs; step 5 (`git diff --numstat`) is genuinely cross-shell. So the author knew the both-shells requirement and step 1 is the omission.\n Consequence: a DOC-UPDATER turn on PowerShell hits a CommandNotFoundException on the FIRST step of the procedure whose entire purpose is to be executable, and must improvise a substitute unaided - the identical failure shape the Step 1 diagnosis documented (\"no written rule tells it what to do next... it must invent a substitute unaided\"). This is ISS-0119 reproduced inside ISS-0119's own fix. It is not a wording nit; a hard gate that lets this through leaves the mechanism unexecutable for half the sessions that run it.\n STOP-and-file fallback: this part is CORRECT and triggers on the real failure mode - S4 step 2 says \"If that read is refused or truncated, STOP: the roll rule has failed and that is itself a defect to file, not to work around\", and S10.3's core-directives.md:328 replacement repeats it with ISS-0119 named as precedent. That is the refusal, not a hypothetical.\n No new tooling is required anywhere else in the procedure: git, wc, sed, Get-Item, Get-Content and Get-Date are all present.\n REQUIRED FIX (one line, no redesign): give step 1 a labelled shell pair as steps 2/3 already do, e.g.\n     Bash:       grep -n 'status: current' -B 2 docs/status/requirement_status.index.yaml\n     PowerShell: Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0\n and sweep S10.2/S10.3's prose (\"targeted grep/sed\", \"read those with grep/sed/awk only\") for the same assumption - core-directives.md:156-161 already sets the both-shells precedent this repo follows.\n\nCHECK 4 - APPEND-ONLY NOT RELAXED: **PASS.** Nothing in the artefact permits editing a past entry. The prohibition is stated affirmatively in S1 (\"Nothing in this design permits rewriting any past entry, in any volume, ever\"), S3.1 (footer: \"Nothing here is rewritten, corrected, renumbered or moved\"), S3.3 (index header: \"appended, never edited, never reordered, never deleted, in any volume, closed or current\"), S4 (header: \"NEVER REWRITE, REORDER, EDIT OR DELETE A PAST ENTRY\"), S10.2 (doc-updater :47 extension to every volume), S10.3 and S10.5. The rule is strengthened, not relaxed: it gains its first mechanical enforcement.\n A6 would detect a retroactive edit: SHA-256 over volume 1's first 5,766 lines covers every one of the 182 entries, so any character change inside the frozen prefix changes the digest and fails the suite. The footer is deliberately placed beyond the prefix so it can be written once without invalidating the digest - that is coherent.\n CRLF-vs-LF: **UNAMBIGUOUS, criterion satisfied.** Stated identically in three places - S3.3 index comment \"SHA-256 of lines 1..5766 with CRLF normalised to LF, i.e. the frozen prefix\"; S7 A6 \"(CRLF normalized to LF, so the digest is platform-stable)\"; S8 step 5 \"Compute frozen_prefix_sha256 over lines 1-5766 with CRLF normalized to LF\". This repo is CRLF on disk / LF in git (diagnosis section 0: blob 355,610 LF bytes vs 361,376 working-tree bytes), and the spec names which one it covers, so the assertion is checkout-independent. Not flagged as MAJOR. One MINOR residue below (trailing-terminator handling).\n Note the design correctly does NOT apply the same normalisation to the roll rule, which deliberately measures the WORKING TREE (\"because that is what the Read tool measures\", S3.3 + S4 + S6). Two different measurements for two different purposes, each stated - self-consistent.\n\nCHECK 5 - THE 3 MALFORMED ENTRIES: **PASS.** S5 specifies four dispositions: not edited (\"Not in this run, not ever\"), not hidden (declared in volume 1's footer S3.1, the index's known_anomalies: S3.3, and the current volume's header S4), attributed (each declaration names TASK_QUEUE.md:133 as cause), machine-pinned. S7 A5 pins both directions verbatim: \"A new violation fails (drift detected); a missing one fails (a past entry was silently normalized or deleted)\". S3.3's known_anomalies: carries all three (3490/REQ-031, 3577/REQ-042, 3744/REQ-038) with path, line, field, value, should_have_been and cause. S8 verification step 4 (`sed -n '3490p;3577p;3744p'` must still return three `event: SCOPE-CHANGE` lines) makes the non-normalisation checkable at Step 3, before the test even exists. S10.2's doc-updater :47 text forbids \"cleaning up\" them by name. Diagnosis C2 is satisfied in the strong form.\n\nCHECK 6 - VOCABULARY COMPLETENESS: **PASS.** S5's table accounts for all 7 in-use event values with the diagnosis's exact counts (done 107, started 54, cancelled 8, blocked 5, revised 4, verified 1, SCOPE-CHANGE 3). The seventh is an EXPLICIT RULING, not an oversight: S4 states \"SCOPE-CHANGE IS NOT AN EVENT VALUE. Three volume-1 entries (lines 3490, 3577, 3744) carry `event: SCOPE-CHANGE`. They are wrong, they are recorded as known anomalies in the index, and they are LEFT AS WRITTEN\". So the next append is never told its own precedent is illegal without also being told why and where the exception is recorded. Both req: values covered (REQ-NNN 151, SCOPE-CHANGE 31), plus an explicit zero-precedent ruling that `req: ISS-NNNN` is illegal - which directly pre-empts the wrong guess this very run would otherwise make at Step 6. S4 also states the extension procedure for a seventh value (amend the header in the same commit that first uses it), so vocabulary growth is not left to improvisation.\n\nCHECK 7 - THE 23 SITES: **PASS on substance; the handoff's own 14+9 figure is wrong.** Counted mechanically over S10.1: 25 table lines = 1 header + 24 data rows; 16 rows marked **EDIT**, 8 marked NO CHANGE. The 23 NUMBERED rows are 15 EDIT + 8 NO CHANGE = 23; the 24th row is the unnumbered `core-directives.md:173` back-reference EDIT the design adds on top of the diagnosis's list. So the correct arithmetic is 15+8=23 numbered sites (16 EDITs including the added one), not 14+9. The set is complete and correctly totalled - only the count quoted in this handoff's task text (inherited from the producer's summary) is off. Recorded MINOR.\n Cross-check against diagnosis section 3(ii) items 1-23: 1:1, same file and same line for every one (CLAUDE.md:85; core-directives :290/:328/:354; doc-updater :17/:23/:31/:47; release-validator :23/:32; AGENT_SYSTEM :46/:105/:136; ORCHESTRATOR :129; WF-01:59; WF-02 :106/:377/:390; WF-04:66; ISSUE_QUEUE:156; TASK_QUEUE :133/:379; anti-patterns :68-83). No diagnosis site is missing.\n Every one of the 16 EDITs carries applicable post-edit text: S10.2 (doc-updater :17/:31/:47), S10.3 (core-directives :328 and :173), S10.4 (TASK_QUEUE :133/:379), S10.5 (anti-patterns rewrite + a new entry), S10.6 (CLAUDE.md:85, core-directives:290, release-validator:23, AGENT_SYSTEM :46/:105/:136, WF-02 :106/:390). AGENT_SYSTEM :46/:136 are given as a cell-level instruction (\"widen to docs/status/requirement_status*.yaml (index + all volumes), owner DOC-UPDATER\") rather than a full quoted row - specific enough to apply without invention. Every one of the 8 NO CHANGE rows carries a reason. The design also dispositions diagnosis section 3(i) prose referrers, including requirements.yaml:444 (left as-is, resolved through the index) and iss-0078-pin-rebind-provenance.md:367 (must NOT be edited).\n\nCHECK 8 - PRECEDENCE RULING: **PASS, with a MINOR restatement hazard.** Self-consistent: :173 (scoped reads) is the general rule governing closed volumes and every other large file; the full read survives only for the CURRENT volume and only because the roll rule bounds it - and S10.3 closes the loop by ruling that a refused full read of the current volume is a roll-rule defect to file, not a licence to append from a fragment. That last clause is what makes the ruling non-circular; without it the exception would re-open the same gap on the next threshold.\n On the restatement convention: three sites is acceptable here and does not violate one-canonical-home-plus-pointers - S9 names :328 as \"the primary statement\", :173 as \"a back-reference\", and the index header as the entry point, i.e. one home and two pointers. Carrying the schema into every volume header is required by the diagnosis itself (a split without it \"multiplies the L2 problem\").\n MINOR hazard: the numeric ceilings 1,200 lines / 120,000 bytes are written out in FIVE places (index roll_rule, every volume header, core-directives:328 replacement, doc-updater.md:31 replacement, anti-patterns.md replacement), while A3 reads them only from the index and A8 checks only that the header names the vocabulary - not the ceilings. Changing a ceiling later would leave up to four prose copies stale with nothing to detect it. Recorded MINOR; a natural fix is an A8 extension asserting the header's quoted ceilings match roll_rule, or replacing the prose numbers with a pointer to the index.\n\nCHECK 9 - NO IMPLEMENTATION LEAKED IN: **PASS, verified.** `git show --stat 24e7087` returns exactly two files: handoffs/WF03-ISS0119-20260821/step-02-code-designer.json (74 +) and lib/letflow/design/iss-0119-status-file-readability.md (737 +), \"2 files changed, 811 insertions(+)\". No target file was edited - docs/status/requirement_status.yaml, the index, v2, core-directives.md, doc-updater.md, TASK_QUEUE.md, anti-patterns.md and all remaining instruction sites are untouched by the design step. Confirmed independently: `git status --porcelain` on this branch shows no modification to any of them. The verbatim file content in S3.1/S3.3/S4/S10 is specification, which the handoff sanctions; it is quoted inside fenced blocks in the design, not written to the target paths. AC1 and AC12 satisfied.\n\nCHECK 10 - STEP 4 BUILDABLE: **PARTIAL - MAJOR.** A1-A8 are individually buildable from S7 alone: ExUnit, no DB, no new deps, :crypto for the digest, index-derived volume list, and the entry/field grammar is stated (\"entries start with `  - req: ` and fields with four-space indent\"). A3's inputs are fully concrete (File.stat! bytes vs roll_rule.max_bytes, line count vs roll_rule.max_lines). TEST-DESIGNER can write the file.\n What is MISSING is the fail-first mechanism, which WF-03 Step 4 requires explicitly: \"Step 4's test must be shown to fail against the pre-fix code and pass against the post-fix code... (checked out the pre-fix commit, ran the new test, confirms it failed)\". S7 asserts A3 \"would have failed on 2026-08-18 at commit 3d19e8c\" - a counterfactual that is arithmetically well-founded (the diagnosis measured 4,217 lines / 264,861 working-tree bytes there, over both ceilings) but is NOT a runnable demonstration, because every assertion including A3 is derived from docs/status/requirement_status.index.yaml, and that file does not exist at 3d19e8c or at any pre-fix commit. Checking out the pre-fix tree and running the test yields a missing-index error, not an A3 size failure - a failure that any test of any new file would produce, and which therefore proves nothing about whether the test covers this bug (the precise objection WF-03 Step 4 raises).\n S7 names no way to close this. TEST-DESIGNER would have to invent one - a fixture index under test/fixtures/ pointing at a copy of the pre-fix file, an A3 size-check helper callable against an arbitrary path, or `git show 3d19e8c:docs/status/requirement_status.yaml` piped into a temp file. Which of those is chosen changes the test's shape, so it is a design decision, not a mechanical detail. REQUIRED FIX: S7 states the fail-first procedure concretely - which artefact stands in for the pre-fix state, where it lives, and how A3 is exercised against it - so TEST-DESIGNER can demonstrate a MEANINGFUL failure rather than a missing-file failure.\n\nCHECK 11 - OPEN QUESTIONS: **PASS.** None hides an unresolved decision ELIXIR-DEV must invent an answer to. #1 (volume naming) states the choice made (.v2.yaml), its reason, and that nothing depends on it because every consumer goes through the index - a made decision with an alternative recorded. #2 (ceilings) states the chosen values, their derivation from the tool limits, and a hard constraint on any change (\"must never be raised to the tool limits themselves\") - decided, with a guardrail. #3 (standalone `mix letflow.check` step) is correctly framed as additive: the test runs under plain `mix test` either way, so Step 3 is unblocked regardless of REVIEWER's answer at 3d. #4 (anomalies permanent) is a decision taken, not deferred, and names the only append-only-compatible future route. Separately, S11's \"Step 4 may own the test instead, at ORCH's routing discretion; the design specifies it either way\" is a routing question, not a design gap - the file path and contents are pinned regardless of who writes it.\n\n=== WHAT REWORK MUST DO (minimal, no redesign) ===\n1. [BLOCKER, AC7] S4 HOW-TO-APPEND step 1: replace the bare grep with a labelled Bash/PowerShell pair (grep is verified absent from PowerShell on this host), and sweep S10.2/S10.3's grep/sed prose for the same assumption.\n2. [MAJOR, WF-03 Step 4] S7: specify the concrete fail-first procedure for A3 against the pre-fix state, given the test is index-driven and no index exists pre-fix.\n3. [MINOR] S8/S3.3: state whether the frozen-prefix digest includes a trailing line terminator after line 5,766 (join-with-LF vs terminate-with-LF give different digests; harmless today because ELIXIR-DEV pastes whatever the test computes, but it should be written down, since the digest's whole job is to be reproducible by a later agent).\n4. [MINOR] S11: \"Modified (10)\" enumerates 9 distinct files (requirement_status.yaml, CLAUDE.md, core-directives.md, doc-updater.md, release-validator.md, AGENT_SYSTEM.md, WF-02, TASK_QUEUE.md, anti-patterns.md). The list is exhaustive; only the count is wrong. Fix the count so ELIXIR-DEV's own file-count check at Step 3 does not disagree with the design.\n5. [MINOR] S8/S4 step 2: the two commands given measure size, and the Bash form reports lines only while the PowerShell form reports bytes only, although step 2 states both bounds. Give both measurements in both shells, as the roll-rule block already does.\nNot blocking, recorded for the source: this handoff's task text says S10 claims \"14 EDIT and 9 NO CHANGE\"; the artefact's table is 15 EDIT + 8 NO CHANGE over 23 numbered sites (16 EDIT over 24 rows, including the added core-directives.md:173 back-reference). All 23 diagnosis sites are present, so the design is not at fault - the figure came from the producer's summary.\n\nThe artefact was not modified by this step; the only file this step writes is its own handoff.\n\n || RE-GATE PASS 2 (2026-08-21T05:31:17Z) - WF-03 Step 2 GATE VERDICT: **FAIL** (1 BLOCKER, 1 new MINOR). R1 FIXED and verified by execution; R3, R4, R5 FIXED, one of them verified by execution; the count correction is present and matches this gate's own pass-1 arithmetic. R2 is NOT fixed: the replacement mechanism is substantively useful but does not satisfy WF-03 Step 4's stated requirement, and Section 7.1 affirmatively rules the one element that would satisfy it \"optional\". Rework iteration 1 -> 2 of 3. Pass-1's issue list is superseded by result.issues below; the pass-1 record above is preserved verbatim.\n\nArtefact re-read at commit 5b6333d (905 lines, was 737). Sections read this pass: 3.1's closure footer (lines 96-133), Section 4 preamble + the full quoted header incl. all six HOW-TO-APPEND steps and the roll rule (244-394), Section 5 vocabulary (420), Section 6 bound (440-458), Section 7 incl. the test/support module spec (460-530), Section 7.1 in full (532-585), Section 8 incl. the six verification checks and the frozen_prefix_sha256 byte-stream convention (590-650), Section 10.1 count paragraph + all 24 table rows (682-719), 10.2/10.3 targeted-read prose (730-790), Section 11 (858-874). Cross-read: WF-03_issue_resolving.md \"Steps 2-4\" (lines 97-104), mix.exs (all 72 lines).\n\n=== CHECK 9 RE-RUN: PASS ===\n`git show --stat 5b6333d` returns exactly two files - handoffs/WF03-ISS0119-20260821/step-02-code-designer.json (10 +/-) and lib/letflow/design/iss-0119-status-file-readability.md (260 +/-), \"2 files changed, 219 insertions(+), 51 deletions(-)\". No target file was touched by the rework. AC12 still satisfied.\n\n=== R1 (was BLOCKER, AC7): **FIXED** - verified by execution, not by report ===\n(a) EXECUTED THE REPLACEMENT. Ran in PowerShell on this host:\n    Select-String -Path docs/status/requirement_status.index.yaml -Pattern 'status: current' -Context 2,0\n  Result: \"Select-String : Cannot find path 'C:\\Users\\tvolo\\dev\\ai-dala\\letflow-2\\docs\\status\\requirement_status.index.yaml' because it does not exist.\" / \"CategoryInfo : ObjectNotFound: ... [Select-String], ItemNotFoundException\" / \"FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.SelectStringCommand\".\n  That is exactly the expected outcome: the cmdlet name RESOLVED (the error is attributed to SelectStringCommand), the parameters -Path / -Pattern / -Context 2,0 PARSED, and the only failure came from the file layer because the index does not exist yet. No CommandNotFoundException, no parser error. The replacement is genuinely runnable and will succeed the moment Step 3 creates the index.\n(b) STEP 1 IS NOW A LABELLED PAIR (lines 286-295), with the operative warning inline (\"grep does not exist in PowerShell, and Select-String does not exist in bash. Each is a single line; do not wrap it\"). Both forms sit on their own unwrapped line.\n(c) THE PREAMBLE IS NOW NORMATIVE AND FORWARD-BINDING (lines 250-262). It states the reason (\"Shell coverage is part of the specification, not a formatting preference\"), quotes the real error text verbatim (\"The term 'grep' is not recognized as the name of a cmdlet, function, script file, or operable program\"), states the rule (\"Every command in the header below is given as a labelled Bash/PowerShell pair, or is a `git` invocation that is identical in both\"), cites the core-directives.md:156-161 precedent, and - the part that matters most - binds every FUTURE volume: \"a new volume is opened by copying this text verbatim, so a single-shell command introduced here would propagate forever.\" That converts a one-off patch into a rule the copy-paste roll procedure cannot erode.\n(d) MY OWN FULL-ARTEFACT SWEEP, independent of CODE-DESIGNER's account. Grepped all 905 lines for grep|sed|awk|wc|head|tail|date|cat|sha256sum and inspected every one of the 22 hits in context. Every executable occurrence is either a labelled Bash line whose PowerShell twin sits directly beneath it, or prose naming the tools. Specifically:\n  - 3.1's closure footer (109-111): now a three-way read menu - Read tool with offset/limit, Bash grep/sed, PowerShell Select-String / (Get-Content FILE)[START..END]. Previously bash-only. FIXED.\n  - Header step 1 (290/292), step 2 (304/306), step 3 (311/312), roll rule (375/376): all pairs. Step 5 is `git diff --numstat`, identical in both shells.\n  - Section 8's six verification checks (596-624): the preamble now says \"Each check is given for both shells, for the same reason Section 4 is\", and checks 2, 3 and 4 are Bash/PowerShell pairs (grep -c / (Select-String ...).Count; sed -n '5158p' / (Get-Content $SF)[5157]; sed -n '3490p;3577p;3744p' / (Get-Content $SF)[3489,3576,3743]). Checks 1 and 6 are git; check 5 is mix. Previously bash-only. FIXED.\n  - Section 8's frozen_prefix_sha256 reference forms (632-638): Elixir + Bash + PowerShell, all three given.\n  - 10.2/10.3 targeted-read prose (739, 774-776): now \"`grep`/`sed` on Bash, or `Select-String` on PowerShell\", and \"under Bash ... `grep` does not exist in PowerShell - see the pairs at `:156-161` above\". FIXED.\n  - Section 7.1's git-show block (569-575) is given as a bash block AND a powershell block.\n  ZERO surviving single-shell commands. Sweep clean.\n(e) LINE CONTINUATIONS: none survive. Grepped every line ending in a backslash or a backtick; all hits are markdown inline-code delimiters or fence markers, and none is inside either quoted YAML block (3.1's footer, lines 96-133, or Section 4's header, lines 264-394). The propagating-defect risk CODE-DESIGNER flagged is genuinely closed. One cosmetic residue is recorded as a new MINOR below.\n\n=== R2 (was MAJOR, WF-03 Step 4 fail-first): **NOT FIXED - BLOCKER** ===\nWF-03_issue_resolving.md, \"Steps 2-4\", says in full: \"**Step 4's test must be shown to fail against the pre-fix code and pass against the post-fix code** - TEST-DESIGNER states this explicitly in the test spec (checked out the pre-fix commit, ran the new test, confirms it failed; then confirms it passes on the fix branch). A test that only ever ran against already-fixed code proves nothing about whether it actually covers the bug.\"\n\nTwo obligations are stated there. A3b meets neither.\n\n(i) NOT DIRECTIONAL. Step 4's requirement is directional: one artefact must produce two DIFFERENT verdicts in the two states. A3b produces the same verdict in both. Pre-fix at 3d19e8c the file measured 4,217 lines / 264,861 working-tree bytes - over both ceilings. Post-fix it measures 5,766 lines / 361,376 bytes - over both ceilings. within_bounds?/3 therefore returns {:error, %{over: [:lines, :bytes]}} in BOTH worlds, so the A3b assertion is GREEN in both worlds. It is never \"shown to fail against the pre-fix code\"; it cannot be, because its expected value IS the error.\n\n(ii) TAUTOLOGY, to answer the question directly. Under the fix, docs/status/requirement_status.yaml is frozen, is no longer the append target, is pinned byte-for-byte by A6, and is EXPECTED to be over the ceiling by design - Sections 2 and 3.1 say so explicitly. An assertion that a file the design has just declared permanently frozen and permanently oversized is oversized restates the freeze. It encodes the design's own intent, not the defect. Section 7.1's own sentence - \"Volume 1 is frozen and covered by A6, so these numbers cannot drift\" - is the proof: an assertion whose subject provably cannot change is not a regression test on that subject.\n\nWHAT A3b ACTUALLY IS, stated fairly, because it is worth keeping: a DETECTOR-CALIBRATION assertion. It proves within_bounds?/3, at the index's real ceilings, fires on a real 5,766-line / 361,376-byte artefact. That is not vacuous - it would fail if the helper's byte or line measurement regressed, or if a future agent raised the ceilings toward the tool limits (which Section 12 #2 forbids). Keep it. But it is a sanity check on the detector, not the fail-first demonstration, and Section 7.1 must stop labelling it \"the design's fail-first mechanism ... sufficient on its own\".\n\nWHY THE EXISTING ESCAPE HATCH DOES NOT RESCUE IT. Section 7.1's git-show block is the one element that would satisfy Step 4's literal parenthetical, and Section 7.1 disqualifies it in its own words: \"This is **optional corroboration**; A3b is the design's fail-first mechanism and is sufficient on its own.\" Optional means it may not run, and a TEST-DESIGNER that follows Section 7.1 as written will produce a test spec with NO pre-fix run recorded - which TEST-DESIGN-VALIDATOR must then fail at 4b for the same reason. Worse, this is not an omission a downstream agent may quietly repair: it is an affirmative design RULING that a mandatory workflow obligation is satisfied without being performed. TEST-DESIGNER has no authority to override a design ruling, so the defect propagates two steps unless it is fixed here.\n\nSection 7.1's DIAGNOSIS of the problem is CORRECT and must be preserved - a naive checkout of 3d19e8c errors on a missing index and proves nothing, and the \"Rejected alternatives\" paragraph (a synthetic oversized fixture; git-show as the suite's PRIMARY mechanism, which breaks a shallow clone) is well-reasoned and should not be re-proposed. Only the conclusion is wrong.\n\nREQUIRED FIX - either form is acceptable; the design must pick one, because the choice changes the test's shape and TEST-DESIGNER must not invent it:\n (A) PROMOTE, DO NOT DELETE. Make Section 7.1's extraction MANDATORY and part of the Step 4 record: TEST-DESIGNER (or TEST-RUNNER) runs `git show 3d19e8c:docs/status/requirement_status.yaml` into a temp path, calls Letflow.Test.StatusHistory.within_bounds?/3 against it with the index's ceilings, quotes the ACTUAL output in the test spec, then quotes the same call against the post-fix current volume returning :ok. That is the two-verdict pair Step 4 demands, in the only form available given no index exists pre-fix, and Section 7.1's own reason for making the helper path-parameterised already anticipates it. Delete \"optional corroboration\" and \"sufficient on its own\"; keep A3b, relabelled as the live detector-calibration guard.\n (B) MAKE A3 ITSELF DIRECTIONAL. Specify that the test's current-volume RESOLUTION degrades when no index is present - absent requirement_status.index.yaml, the current volume is docs/status/requirement_status.yaml. A3 then fails on the pre-fix tree for the RIGHT reason (size, not a missing file) and passes post-fix, satisfying Step 4 under a literal checkout. This is the stronger option and removes the git-show entirely, but it adds a fallback rule to the resolution logic, so it must be written into Section 7 rather than left to TEST-DESIGNER.\n\n=== R2 SECOND-ORDER EFFECTS (test/support/status_history.ex): ALL THREE PASS ===\n(a) SIX SIGNATURES COMPLETE: PASS. parse_index/1 gives its full return map including roll_rule, volumes (with frozen_prefix_lines / frozen_prefix_sha256) and known_anomalies; measure/1 names File.stat!/1 and \"working tree\"; within_bounds?/3 gives both the :ok and the {:error, %{lines:, bytes:, max_lines:, max_bytes:, over: [...]}} shape; entries/1 and anomalies/3 give their element shapes and take their vocabularies as PARAMETERS rather than baking them in; frozen_prefix_digest/2 points at the Section 8 byte-stream convention and specifies lowercase hex. Section 7's preamble supplies the parsing contract the signatures rely on (\"entries start with `  - req: ` and fields with four-space indent\"). TEST-DESIGNER can implement all six without inventing behaviour.\n(b) THE mix.exs:20 CLAIM IS TRUE - verified by opening the file, not taken on report. mix.exs line 20 reads exactly: defp elixirc_paths(:test), do: [\"lib\", \"test/support\"], wired in at line 10 via elixirc_paths: elixirc_paths(Mix.env()). test/support is compiled in :test today. No build-config change is needed at Step 3.\n(c) NO HARDCODED INDEX LOCATION: PASS. Every one of the six takes an explicit path or explicit bounds; the design states it outright (\"Nothing in the module knows that docs/status/requirement_status.index.yaml exists\") and the signatures bear it out. That is what makes option (A) above mechanically possible, which is the point of the module.\n\n=== R3 (frozen_prefix_sha256 terminator): **FIXED - and verified by execution** ===\nSection 8 now fixes the convention normatively: \"re-join with a single \\n **after every line, including the last** - i.e. the hashed stream is Enum.map_join(lines, \"\", &(&1 <> \"\\n\")), which ends in \\n, **not** Enum.join(lines, \"\\n\"), which does not. The two differ by one byte and yield different digests.\" Encoding is pinned (UTF-8, no BOM handling, no trimming beyond the terminator). Three reference implementations are given, with \"**The Bash form is normative**\" and the reason (head emits a terminator after every line it prints, including the last).\nI did not take the \"all of which must agree\" claim on report - I ran two of the three:\n  Bash:       head -n 5766 docs/status/requirement_status.yaml | tr -d '\\r' | sha256sum\n              -> 5a1a64ab0b999da3fd86be90109ecee46b9d538d9e9945c0d39fddb10075a804\n  PowerShell: the Section 8 form verbatim (Get-Content -TotalCount 5766 | ForEach-Object appending a backtick-n, -join '', written via [IO.File]::WriteAllText with UTF8Encoding($false), then Get-FileHash -Algorithm SHA256)\n              -> 5a1a64ab0b999da3fd86be90109ecee46b9d538d9e9945c0d39fddb10075a804\nIDENTICAL. The convention is reproducible across shells on this host, as specified. Recorded here so Step 3 has an independent cross-check for the value ELIXIR-DEV writes into the index - one that did not come from the producer.\n\n=== R4 (ceiling restatement hazard): **FIXED - zero unchecked prose copies, verified by grep** ===\nA8 is extended verbatim: it \"additionally asserts that the two ceiling numbers quoted in the current volume's header text (`under 1,200 lines / 120,000 bytes` in HOW-TO-APPEND step 2, and `lines > 1200 OR bytes > 120000` in the roll rule) equal roll_rule.max_lines and roll_rule.max_bytes from the index\", and states the resolution: \"The three prose copies in Sections 10.2/10.3/10.5 are deliberately written to point at the index rather than restate the numbers ... so after this design there are exactly two machine-checked copies and no unchecked prose copy.\"\nVERIFIED INDEPENDENTLY by grepping all 905 lines for 1,200 / 120,000 / 1200 / 120000. Nine hits, every one accounted for: lines 171-172 = the index roll_rule (the single source of truth); 298 and 378 = the two volume-header copies, both now pinned by A8; 442, 520-521, 890 = the design document's OWN prose (Section 6's bound statement, A8's specification of what it checks, Section 12 #2's open question), which ships to no instruction file; 570/574 = the illustrative commands in Section 7.1. CRITICALLY, the Section 10.2 / 10.3 / 10.5 replacement texts (lines 730-840) contain ZERO occurrences of either number - confirming the three prose copies really did become index pointers rather than being left behind. No fourth copy survives. The fix is complete, not partial.\n\n=== R5 (counts and step-2 bounds): **FIXED** ===\nSection 11 now reads \"**Created (4)**\" - v2.yaml, index.yaml, test/support/status_history.ex, test/docs/requirement_status_invariants_test.exs (4 named, 4 counted) - and \"**Modified - 9 distinct files** (the per-site breakdown is Section 10.1's table; count files here, sites there)\", enumerating exactly 9 with their per-file site counts. Count and enumeration now agree, so ELIXIR-DEV's own file-count check at Step 3 will not disagree with the design.\nSection 4 step 2 now reports BOTH bounds in BOTH shells: `wc -l < FILE ; wc -c < FILE` and `(Get-Content FILE).Count ; (Get-Item FILE).Length`, matching the roll-rule block, plus a correct note that PowerShell has no && while ; chains unconditionally in both.\n\n=== COUNT CORRECTION: **PRESENT AND CORRECT** ===\nSection 10.1, lines 688-691, above the table: \"**Count, stated so Step 3 can check itself against it:** 23 numbered sites = **15 EDIT + 8 NO CHANGE**. The table has 24 rows: the 24th is the unnumbered core-directives.md:173 back-reference, giving **16 EDIT rows over 24 rows**. (An earlier summary of this design said '14 EDIT + 9 NO CHANGE'; that was wrong ...)\" - an explicit retraction, as required.\nI RE-COUNTED THE TABLE MECHANICALLY rather than accepting the paragraph. Numbered EDIT rows: 1, 2, 3, 5, 7, 8, 9, 11, 12, 13, 16, 18, 21, 22, 23 = 15. NO CHANGE rows: 4, 6, 10, 14, 15, 17, 19, 20 = 8. 15 + 8 = 23. Plus the unnumbered core-directives.md:173 row = 16 EDIT across 24 rows. This matches my pass-1 count exactly. Step 3 now has the right number to check itself against.\n\n=== NEW DEFECTS INTRODUCED BY THE REWORK: one MINOR, cosmetic ===\nSee result.issues. Nothing in the rework disturbed the freeze-and-roll shape, the append-only rule, the anomaly handling, the vocabulary ruling, the precedence ruling or the site dispositions - checks 2, 4, 5, 6, 8 and 11 were not re-derived, per this handoff's scoping, and spot-checks of the areas the rework touched (3.1's footer, Section 4's header, Section 8, Section 10.1, Section 11) found no regression in them.\n\n=== IS THIS A STRATEGY PROBLEM? NO ===\nORCH's standing instruction is that a second FAIL on the same executability ground must trigger a change of strategy rather than a third patch. That condition is NOT met. R1, the executability BLOCKER, is genuinely and thoroughly fixed - I proved it by running the replacement, and CODE-DESIGNER swept further than this gate asked (3.1, Section 8, 10.2/10.3) and bound the rule forward into every future volume. This FAIL rests on a DIFFERENT ground: a design ruling in Section 7.1 that declares a mandatory WF-03 Step 4 obligation satisfied without being performed. It is a single, well-localised decision - choose option (A) or (B) above and write it into Section 7.1 - and it does not call the freeze-and-roll design into question. Iteration 2 is the right instrument here, not a change of strategy.\n\nThe artefact was not modified by this step; the only file this step writes is its own handoff.",
    "artifacts_out": [
      "handoffs/WF03-ISS0119-20260821/step-02b-code-design-validator.json"
    ],
    "issues": [
      {
        "severity": "BLOCKER",
        "description": "R2 NOT FIXED. Section 7.1 does not satisfy WF-03 Step 4, which states: \"Step 4's test must be shown to fail against the pre-fix code and pass against the post-fix code -- TEST-DESIGNER states this explicitly in the test spec (checked out the pre-fix commit, ran the new test, confirms it failed; then confirms it passes on the fix branch). A test that only ever ran against already-fixed code proves nothing about whether it actually covers the bug.\" (1) NOT DIRECTIONAL: A3b asserts within_bounds?(\"docs/status/requirement_status.yaml\", max_lines, max_bytes) == {:error, %{over: [:lines, :bytes]}}. That file was over both ceilings pre-fix (4,217 lines / 264,861 bytes at 3d19e8c) AND is over both ceilings post-fix (5,766 / 361,376), so the assertion is GREEN in both states. It is never shown to fail against the pre-fix code; its expected value IS the error. (2) TAUTOLOGY: under the fix that file is frozen, is no longer the append target, is pinned by A6, and is EXPECTED to be oversized by design (Sections 2 and 3.1 say so). Asserting it is oversized restates the freeze -- Section 7.1's own \"Volume 1 is frozen and covered by A6, so these numbers cannot drift\" proves the subject provably cannot change. (3) THE ESCAPE HATCH IS DISQUALIFIED BY THE DESIGN ITSELF: the git-show extraction, the one element that would meet Step 4's parenthetical, is labelled \"optional corroboration; A3b is the design's fail-first mechanism and is sufficient on its own\" -- an affirmative RULING that a mandatory workflow obligation needs no performance, which TEST-DESIGNER has no authority to override, so the defect would propagate to Step 4 and be re-caught at 4b. MISSING ELEMENT -- the design must choose one, because the choice changes the test's shape: (A) promote Section 7.1's `git show 3d19e8c:docs/status/requirement_status.yaml` extraction to MANDATORY and part of the Step 4 record (run within_bounds?/3 against the extracted file with the index ceilings, quote the actual output in the test spec, then quote the same call against the post-fix current volume returning :ok -- the two-verdict pair Step 4 demands), deleting \"optional corroboration\" and \"sufficient on its own\"; or (B) specify in Section 7 that current-volume RESOLUTION degrades to docs/status/requirement_status.yaml when no index file is present, so A3 itself fails on the pre-fix tree for the right reason (size, not a missing file) and passes post-fix under a literal checkout. KEEP A3b either way, relabelled honestly as a detector-calibration guard -- it usefully proves within_bounds?/3 fires on a real oversized artefact at the index's ceilings, and would fail if the helper's measurement regressed or the ceilings were raised toward the tool limits -- but it is not the fail-first demonstration. Section 7.1's diagnosis of why a naive 3d19e8c checkout proves nothing, and its \"Rejected alternatives\" paragraph, are correct and must be preserved; only the conclusion is wrong.",
        "affected_requirement": null
      },
      {
        "severity": "MINOR",
        "description": "NEW, introduced by the rework; cosmetic only. Section 8's PowerShell reference form for frozen_prefix_sha256 (around line 636) is written as a markdown INLINE code span that itself contains a literal backtick -- the ForEach-Object body appends PowerShell's backtick-n newline escape. Markdown terminates the inline code span at that backtick, so the command renders broken and an agent copying it from rendered output gets a truncated line. THE COMMAND ITSELF IS CORRECT: I ran it verbatim on this host and it reproduced the normative Bash digest exactly (both yield 5a1a64ab0b999da3fd86be90109ecee46b9d538d9e9945c0d39fddb10075a804 over volume 1's first 5,766 lines). Fix is one edit: move it into a fenced powershell block, as Section 7.1 already does for its own PowerShell form, so the backtick survives. Not blocking -- the Bash form is declared normative and is unaffected -- but it sits in the one part of the design whose entire purpose is byte-exact reproducibility by a later agent.",
        "affected_requirement": null
      }
    ],
    "next_action": "Route back to CODE-DESIGNER for rework iteration 2 of 3 with the single BLOCKER above. This is NOT the strategy-change condition ORCH set: that condition was a second FAIL on the executability ground, and R1 is genuinely fixed (verified by running the replacement in PowerShell -- the cmdlet resolves and parses, failing only at the file layer because the index does not exist yet), as are R3, R4 and R5, and the Section 10.1 count correction matches this gate's own arithmetic (15 EDIT + 8 NO CHANGE over 23 numbered sites, 16 EDIT over 24 rows). The remaining defect is one localised design ruling in Section 7.1 that declares WF-03 Step 4's fail-first obligation satisfied without being performed; CODE-DESIGNER picks option (A) or (B) from the BLOCKER and writes it into Section 7.1, keeping A3b as a relabelled live guard. Fold in the cosmetic MINOR in the same pass. Re-gate at Step 2b on return; do not advance to Step 3."
  },
  "rework_count": 1,
  "max_rework": 3
}