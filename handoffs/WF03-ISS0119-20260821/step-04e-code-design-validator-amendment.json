{
  "handoff_id": "ecbd9147-3f44-410e-8e88-617cfbca450d",
  "run_id": "WF03-ISS0119-20260821",
  "workflow_id": "WF-03",
  "step": "04e",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGN-VALIDATOR",
  "file": "handoffs/WF03-ISS0119-20260821/step-04e-code-design-validator-amendment.json",
  "created_at": "2026-08-21T07:01:30Z",
  "started_at": "2026-08-21T07:01:30Z",
  "completed_at": "2026-08-21T07:15:58Z",
  "status": "COMPLETED",
  "priority": "HIGH",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "requirement_text": {},
    "related_handoff_ids": [
      "eb7d48e4-50f3-4392-b5c5-36652fefe7d3",
      "72f8fa22-e1ac-4cf0-9726-172444a0481a"
    ],
    "artifacts_in": [
      "lib/letflow/design/iss-0119-status-file-readability.md",
      "handoffs/WF03-ISS0119-20260821/step-04d-code-designer-amendment.json",
      "handoffs/WF03-ISS0119-20260821/step-04-test-designer.json",
      "docs/status/requirement_status.index.yaml",
      "docs/status/requirement_status.yaml",
      "test/docs/requirement_status_invariants_test.exs",
      "test/support/status_history.ex"
    ],
    "owned_modules": [
      "lib/letflow/design/"
    ],
    "branch_name": "feature/WF03-ISS0119-20260821",
    "issue_ids": [
      "ISS-0119"
    ]
  },
  "task": {
    "description": "HARD GATE on CODE-DESIGNER's POST-GATE AMENDMENT to ISS-0119's design -- new §13 at line 1060 of `lib/letflow/design/iss-0119-status-file-readability.md`, plus a banner under the title and cross-reference markers on §5, A4 and A5, committed at 6c999e4.\n\nWHY THIS EXISTS. You passed §§1-12 on re-gate pass 3. Then TEST-DESIGNER implemented assertion A4 and RAN it for the first time, and it went red on three previously undeclared entries in FROZEN volume 1: line 4803 REQ-053 (no `note:`), line 4896/4899 REQ-056 (`timestamp:` instead of `at:`), line 5050 REQ-054 (no `note:`). Real drift -- ORCH corroborated by field-count arithmetic and CODE-DESIGNER re-corroborated with anchored patterns (182 req/event/agent, 181 at, 180 note, one `^    timestamp: ` at line 4899). TEST-DESIGNER refused to weaken A4 to clear the red, which is why an amendment exists rather than a quiet edit. §13 is NEW design and has NOT been validated by anyone. Gate it as you gated the original.\n\nSCOPE: gate §13 and the amendment's cross-reference markers. §§1-12 are already gated -- do NOT re-derive them, EXCEPT to verify the amendment did not disturb them (see check 1).\n\nCHECKS, each with its own recorded verdict:\n\n1. NOTHING ELSE MOVED. CODE-DESIGNER claims §§1-12 are 'otherwise byte-unchanged' apart from the banner and three cross-reference markers. Verify with `git show 6c999e4` / `git diff` that the change is additive and confined to §13 plus those markers, and that §8's erratum, the freeze-and-roll shape, the header text and the 23 instruction sites were not revisited. Also confirm the commit touched only the artefact and the handoff.\n\n2. THE CHOICE IS SOUND. Candidate (i) was chosen: a top-level `known_shape_anomalies:` key, keyed {path, entry_line, req, event, kind, field, found_as, found_line, should_have_been, cause}, with A4 splitting into A4a (vocabulary) and A4b (shape). Judge the reasoning on its merits, not its confidence. The central claim to test: does (i) genuinely NOT WEAKEN A4? CODE-DESIGNER argues it strengthens it, because the shape rule still applies to every volume, the three stop being red by being DECLARED rather than by ceasing to be checked, and bidirectional set equality newly makes a silent 'cleanup' of one of them FAIL -- something A4 could not detect before. Verify that bidirectionality is actually specified in §13.5 and not merely asserted in prose.\n\n3. THE EXCEPTION LIST CANNOT GROW SILENTLY. The claim is that the list is bounded and permanent by construction because volume 1 is frozen and digest-pinned. Test the other half: what stops a future agent from making a NEW shape violation acceptable by simply adding a record to `known_shape_anomalies:`? The vocabulary mechanism has the same theoretical hole. If the design has no answer, that is worth a finding at MAJOR -- a declare-to-silence mechanism with no friction is a gate an agent can satisfy by editing what it measures, which core-directives.md forbids outright.\n\n4. VISIBILITY, THE BINDING CONSTRAINT ORCH SET. The three entries must remain visible where a reader LANDS, not merely be technically recorded. §13 claims four places: the index's `known_shape_anomalies:`, the current volume's header via §13.8's addendum, `mix test` output whenever A4b fails, and §13.1's table. Verify each claim is actually specified -- particularly that §13.8 gives verbatim header text and that A4b's failure output really does name all three.\n\n5. A5 IS UNTOUCHED AND DISJOINT. §13 claims A5 keeps the same text, behaviour and same three vocabulary anomalies (3490/3577/3744, field `event`, value SCOPE-CHANGE), must NOT read `known_shape_anomalies:`, and is arithmetically unaffected because the three shape-anomalous entries carry legal vocabulary (req REQ-053/056/054, event `started`) so they never enter A5's on-disk set. VERIFY THE DISJOINTNESS ON THE REAL DATA rather than accepting the argument -- read those entries.\n\n6. THE DIGEST DOES NOT MOVE. §13.7 states frozen_prefix_sha256 is unchanged because the amendment writes zero bytes into `docs/status/requirement_status.yaml` -- neither the prefix nor the footer -- and the digest never covered the index. Confirm the amendment truly requires no edit to volume 1, and that A6 therefore stays green with no index change to the digest field.\n\n7. APPLICABLE WITHOUT INVENTION. §13.4 (verbatim index YAML), §13.8 (verbatim volume-header addendum), §13.5 (A4a/A4b behaviour, the four `kind:` values, the misnamed_field-is-ONE-violation rule, the {path, entry_line, kind, field, found_as} identity key, the req/event cross-check, the preserved ISO-8601 parse at line 4899) and §13.7 (two signature-only additions to test/support/status_history.ex) must be executable as written. Judge whether ELIXIR-DEV and TEST-DESIGNER could apply them without inventing anything. Check §13.4's YAML is well-formed and its three records match the ACTUAL on-disk entries -- line numbers, req ids, events and the `timestamp:`-vs-`at:` detail.\n\n8. A8 CLAUSE (c). A8 gains a clause requiring the current volume's header to contain the literal token `known_shape_anomalies:`, so a volume opened by copy cannot silently drop the declaration at roll time. Judge whether that actually closes the roll-time hole.\n\n9. THE TWO OPEN QUESTIONS in §13.11 -- whether `note:` is genuinely mandatory or only conventional, and whether `known_anomalies:` should be renamed. Confirm neither hides a decision the applying agents would have to invent. The first is load-bearing: if `note:` is only conventional, then two of the three 'anomalies' are not anomalies at all and the correct fix is to A4's rule rather than to the exception list. State your own view.\n\n10. NO IMPLEMENTATION LEAKED IN. §13 should be signatures, YAML content and specified behaviour -- not written test code.\n\nSCHEMA NOTE, for your record and already corrected by ORCH's request to CODE-DESIGNER: step-04d's handoff was completed with `result.status: \"SUCCESS\"`, which is not one of HANDOFF_PROTOCOL.md §2's five legal values. Confirm it now reads PASS when you read that file; if it still says SUCCESS, note it as MINOR.\n\nPASS only if every check passes. On FAIL, name each gap with its specific missing element. Do not fix the design yourself and do not edit the artefact.\n\n=== RE-GATE PASS 2 (dispatched by ORCH 2026-08-21T07:13Z, after CODE-DESIGNER completed RE-AMENDMENT ITERATION 1 at commit 21701b1) ===\n\nYour MAJOR-1 was correct and CODE-DESIGNER accepted it in full. NARROW RE-GATE: re-run CHECK 3, plus a disturbance check that iteration 1 did not disturb the nine checks that passed. Do not re-derive checks 1,2,4,5,6,7,8,9,10 from scratch -- confirm only that what you verified first-hand last pass still holds where this diff could have touched it. CODE-DESIGNER reports six hunks, all at line 1077 or later, all inside §13: the iteration-1 marker under §13's Status paragraph, §13.3's bullet, §13.4's comment paragraph, two additions in §13.5, and §13.11 Q1's answer. Verify that with `git show --stat 21701b1` and `git diff 6c999e4 21701b1`, and confirm §§1-12 still show zero diff.\n\nCHECK 3, RE-RUN. The claimed fix: A4b ADDITIONALLY asserts that every `known_shape_anomalies:` record's `path:` names a volume whose index `volumes:` entry has `status: closed` AND carries a `frozen_prefix_sha256:`. A record citing the current (open, unpinned) volume, or a path that is not a volume in the index at all, FAILS A4b -- independently of set equality, and even when the on-disk violation it cites genuinely exists. TEST THE DEFEAT YOU FOUND LAST PASS AGAINST THE NEW TEXT: walk the concrete attack again -- append a malformed entry to the current volume, add a matching record -- and confirm the new assertion actually stops it rather than appearing to. Then look for the NEXT hole rather than stopping at the one you named: does the rule hold if the index's `volumes:` entry for a path is missing entirely; if a volume is marked `status: closed` but carries no digest, or carries one and is not marked closed; if someone declares a record against a closed volume for a violation that is NOT on disk (does set equality still catch it); and -- the one worth the most attention -- can the current volume be ROLLED to closed in the same commit that declares a record against it, which would launder a fresh violation into a permanently-declared one? If that last route is open, the fix is incomplete and that is a MAJOR again. If it is closed, say by what.\n\nALSO CONFIRM: (a) all three existing §13.4 records still pass the new rule -- they cite `docs/status/requirement_status.yaml`, which must be `status: closed` with a `frozen_prefix_sha256:` in the index as it stands on disk; check the actual index file rather than the design's claim about it. (b) §13.3's bullet now CITES the assertion rather than asserting the property, i.e. the prose was actually rewritten and not merely supplemented. (c) MINOR-2 is folded in: A4b's failure output is specified to name every element of the symmetric difference, one per line, with path/entry_line/req/kind, labelled by side, plus every record failing the closed-volume rule -- and a count or 'the sets differ' is explicitly declared insufficient. (d) §13.11 Q1 is answered on the record as `note:` being mandatory.\n\nTWO THINGS ORCH DISPOSITIONED, so you do not re-litigate them but SHOULD sanity-check the consequence. MINOR-2b was DECLINED by CODE-DESIGNER, with a reason ORCH accepts: a citation-level A8(c) needs its own parse of header comment text against index records, which is a different kind of assertion from A8(a)/(b)'s literal-token checks, so it does not in fact cost 'a clause'. Confirm you agree it is genuinely optional rather than quietly load-bearing. MINOR-3 (the same declare-to-silence hole in `known_anomalies:`/A5) is OUT OF SCOPE and ORCH is filing it as its own issue. CODE-DESIGNER flagged a consequence ORCH considers worth your verdict: after this iteration, shape anomalies are constrained to closed volumes and vocabulary anomalies are NOT, so §13.3's parity argument ('the vocabulary mechanism works the same way') is now weaker than the shape mechanism rather than equal to it. CODE-DESIGNER recorded that asymmetry as deliberate and temporary. State whether the surviving parity citation in §13.3 is acceptable as written pending MINOR-3's own run, or whether it is now misleading enough to need a word changed in this pass.\n\nPASS only if check 3 genuinely closes and nothing else regressed. On FAIL, name the specific missing element as before.",
    "acceptance_criteria": [
      "The verdict is derived from §13 of the artefact itself, and result.summary names what was read",
      "Check 1 verifies via git that the amendment is additive and confined to §13 plus the banner and cross-reference markers",
      "Check 2 states whether candidate (i) genuinely does not weaken A4, and confirms bidirectional set equality is specified in §13.5 rather than only claimed in prose",
      "Check 3 states what, if anything, stops a future agent from silencing a new violation by adding a record, and raises it at MAJOR if there is no answer",
      "Check 4 verifies each of the four claimed visibility surfaces is actually specified, including §13.8's verbatim header text",
      "Check 5 verifies A5's disjointness against the real on-disk entries, not from the argument",
      "Check 6 confirms no edit to volume 1 is required and A6 stays green",
      "Check 7 confirms §13.4's YAML is well-formed and its three records match the actual on-disk entries by line, req, event and field detail",
      "Checks 8, 9 and 10 each carry a recorded verdict, including the validator's own view on whether note: is mandatory or conventional",
      "A per-item verdict is recorded for all 10 checks",
      "The design artefact is not modified by this step"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "HARD GATE on CODE-DESIGNER's post-gate amendment (§13 of lib/letflow/design/iss-0119-status-file-readability.md, commit 6c999e4). VERDICT: FAIL on one check of ten (check 3, MAJOR). Checks 1,2,4,5,6,7,8,9,10 PASS. READ, first-hand, not from any producer summary: §13 in full (artefact lines 1060-1400) plus the banner and the §5/A4/A5 markers; `git show --stat 6c999e4` and the full `git show 6c999e4` diff of the artefact; `git diff 6c999e4 HEAD`; docs/status/requirement_status.index.yaml in full; docs/status/requirement_status.yaml lines 3488-3493, 3575-3580, 3742-3747, 4799-4812, 4894-4906, 5046-5058 and its header (1-45) and closure footer; docs/status/requirement_status.v2.yaml lines 1-100; §7's A6/A7/A8 text (artefact 548-575); test/docs/requirement_status_invariants_test.exs (A4 block) and test/support/status_history.ex signatures; handoffs/WF03-ISS0119-20260821/step-04d-code-designer-amendment.json. Independent re-derivation: I wrote my own field-set scan of volume 1 (entry = '^  - req: ' line, fields = '^    <name>: ') and it reports 182 entries and EXACTLY three non-conforming ones -- 4803 [event,agent,at], 4896 [event,agent,timestamp,note], 5050 [event,agent,at] -- no fourth, no extra_field, no field_order. §13.4's three records match that scan exactly, and I parsed §13.4's YAML block with a real YAML loader (well-formed, three records, keys as specified).\n\nPER-CHECK VERDICTS.\n1. NOTHING ELSE MOVED -- PASS. `git show --stat 6c999e4` = 2 files only (the artefact +362/-3, and step-04d's handoff). The artefact diff is exactly four hunks: @@ -8 (the banner), @@ -404 (§5 cross-reference marker), @@ -518 (markers on A4 and A5; the three '-' lines are the A4/A5 bullets re-emitted with the markers, substance preserved), @@ -1042 (§13, purely additive at EOF). No other line moved: §8's erratum, the freeze-and-roll shape, the header text and the 23 instruction sites are byte-unchanged. `git diff 6c999e4 HEAD` on the artefact = 0 lines, so nothing edited it afterwards, and this step did not modify it either.\n2. CANDIDATE (i) DOES NOT WEAKEN A4 -- PASS. The shape rule still runs on every volume, closed and current (§13.5's closing sentence states this explicitly; §13.3 gives the reasoning for rejecting (ii)). The three stop being red by being DECLARED, not by ceasing to be checked. Bidirectionality is genuinely SPECIFIED, not merely asserted in prose: §13.5's A4b carries 'the set of shape violations found on disk equals the index's known_shape_anomalies: set EXACTLY, in both directions', names both failure directions (a new violation fails; a missing one fails = a past entry was silently normalized), and pins the comparison with an explicit identity key {path, entry_line, kind, field, found_as} plus a req/event cross-check against the entry actually at entry_line, plus the preserved ISO-8601 parse at found_line: for the 'field: at' record. That is strictly more detection than pre-amendment A4 had.\n3. THE EXCEPTION LIST CANNOT GROW SILENTLY -- FAIL (MAJOR-1). §13.3 claims the list is 'bounded and permanent, not a growing suppression list' on two grounds: volume 1 is frozen and digest-pinned, and volumes 2 onward are 'checked live from their first entry, so a defect there is caught while the volume is still current and appendable'. The first ground is sound. The SECOND ground is exactly the answer this check asks for -- and it is prose only. Nothing in §13.4, §13.5 or §13.7 stops a future agent from appending a malformed entry to the CURRENT volume and then adding a matching known_shape_anomalies: record, at which point A4b is green again and the defect is permanent. The only barriers are §13.10's written instruction ('Do not hand-tune the declared list to make A4b green') and core-directives.md's 'Never Satisfy a Gate by Editing What It Measures'. Under this project's own weak-model-tolerance rule (core-directives.md, Humanless operation: 'every gate below has a checklist instead of \"use good judgement\"'), a prose prohibition is not the guard this needs when a mechanical one costs one clause and follows directly from §13.3's own argument. MISSING ELEMENT, named precisely: A4b must additionally assert that every known_shape_anomalies: record's path: names a volume whose index entry has 'status: closed' AND carries a frozen_prefix_sha256: -- i.e. a record may never cite the current volume. That makes 'bounded by construction' machine-enforced instead of claimed, and it changes nothing about the three records now proposed (all cite volume 1, closed, digest-pinned). It also makes the correct route the only route: a shape defect in the current volume must be fixed in the working tree before it is committed, which is what §13.3 already says happens. The same theoretical hole exists in known_anomalies:/A5 today; that is not a reason to reproduce it in new design, and closing it there is out of this amendment's scope (see MINOR-3).\n4. VISIBILITY -- PASS, with MINOR-2. Four surfaces claimed in §13.9, checked one at a time: (a) index -- specified verbatim in §13.4, all three records with cause:, and I confirmed the insertion point ('after the existing known_anomalies: block, at top level') is unambiguous against the real index file; (b) current volume's header -- §13.8 gives VERBATIM text in a fenced block (nine comment lines naming 4803/REQ-053, 5050/REQ-054, 4896/REQ-056, the index key, the leave-as-written rule and the A4b consequence), with a real insertion point that exists on disk: docs/status/requirement_status.v2.yaml:60, the '-- ENTRY SHAPE --' block; (c) §13.1's table in this artefact -- present, with req, event and defect per row; (d) `mix test` -- present but failure-only by construction, and §13.5 does not specify what A4b's failure message must contain (see MINOR-2). Three of the four are always-on, which meets ORCH's binding constraint that a reader LANDS on them.\n5. A5 DISJOINTNESS -- PASS, verified on the real data, not from the argument. I read the three vocabulary entries: 3489/3490 REQ-031 'event: SCOPE-CHANGE', 3576/3577 REQ-042, 3743/3744 REQ-038 -- all field: event, value: SCOPE-CHANGE, matching the index's three known_anomalies: records by line (the index keys the FIELD line, 3490/3577/3744; §13.4 keys the ENTRY line for shape records -- two different, internally consistent conventions, each documented in its own block). I read the three shape entries: all carry req: REQ-053 / REQ-056 / REQ-054 and event: started, all legal vocabulary, so none of them enters A5's on-disk set and A5's verdict is arithmetically unaffected. The two sets share no entry. §13.6 states A5's text and behaviour are unchanged and that A5 must not read known_shape_anomalies:; §13.7 confirms anomalies/3 is unchanged and must not start reporting shape violations.\n6. THE DIGEST DOES NOT MOVE -- PASS. §13.3 explicitly rejects the sub-option of also writing the three into volume 1's closure footer, so the amendment writes zero bytes into docs/status/requirement_status.yaml -- neither inside frozen_prefix_lines: 5766 nor past it. Every applied change lands in the index (a new top-level key) and in v2's header. The digest covers volume 1's prefix only and never covered the index, so A6 stays green with no edit to frozen_prefix_sha256: 5a1a64ab...804 and no edit to frozen_prefix_lines/entries/lines/bytes_working_tree/roll_rule. Confirmed against the real index file: those fields exist with the values §13.4 quotes.\n7. APPLICABLE WITHOUT INVENTION -- PASS. §13.4's YAML parses cleanly under a real YAML loader (I ran it): one top-level key, three mapping records, found_as: null / found_line: null load as null, the folded cause: scalars are valid, and should_have_been: 'at: \"2026-08-19T02:40:35Z\"' is correctly quoted so the embedded colon does not break the mapping. All three records match the ACTUAL on-disk entries: 4803 is '  - req: REQ-053' with event started and fields [event, agent, at] -- no note:; 4896 is '  - req: REQ-056' with event started and fields [event, agent, timestamp, note], and line 4899 is '    timestamp: \"2026-08-19T02:40:35Z\"' -- so field: at, found_as: timestamp, found_line: 4899 and the quoted should_have_been value are all exactly right; 5050 is '  - req: REQ-054' with event started and fields [event, agent, at] -- no note:. §13.5's four kind: values, the misnamed_field-is-ONE-violation rule, the identity key, the req/event cross-check and the preserved ISO-8601 parse are all stated as behaviour an implementer can code directly; §13.7's two additions to test/support/status_history.ex are signature-only and path-parameterised ('it must not know the index exists'), consistent with the module's existing shape (entries/1, anomalies/3, measure/1 all take a path). §13.8 is verbatim text with a real anchor. §13.10 assigns each part to a named role. ELIXIR-DEV/DOC-UPDATER and TEST-DESIGNER can apply this without inventing anything.\n8. A8 CLAUSE (c) -- PASS, with MINOR-2b. It does close the hole it targets: headers propagate to future volumes by copy, and requiring the literal token known_shape_anomalies: in the current volume's header makes a copy that drops the declaration FAIL rather than quietly shrinking the declaration surface -- the same mechanism A8(a)/(b) already use for the six event values and the two ceilings, so it is idiom-consistent. Its limit, recorded rather than treated as a failure: a header that keeps the token but drops the three line citations still passes clause (c).\n9. THE TWO OPEN QUESTIONS -- PASS; neither hides a decision the applying agents would have to invent, because §13.4/§13.5/§13.8 fully specify the behaviour under the mandatory reading and Q2 is explicitly not proposed. MY OWN VIEW ON Q1, since it is load-bearing: note: IS mandatory, and the exception list is the correct fix -- not a change to A4's rule. Evidence I checked directly rather than reasoning from the amendment: volume 1's OWN header, in force on 2026-08-19 when these two entries were written, already documents the five-field entry shape with 'note: short free-text (what shipped, or what's blocking)' as the fifth field (docs/status/requirement_status.yaml lines 12-17); volume 2's header states it in mandatory words -- 'ENTRY SHAPE -- all five fields, in this order, every time' (v2.yaml:60); and 180 of 182 entries carry it. So the field was documented, not merely conventional, at the time of the violation, and the two entries are genuine drift. The one fair concession to the question is a wording one, not a rule one: v1's header phrased the shape as a template rather than using the word 'required', which is precisely what §13.4's cause: records ('no mechanical check on entry shape existed at the time'). Weakening A4b to make note: optional would retire a rule 98.9 percent of the corpus follows in order to accommodate two entries -- the attrition §13.11 itself warns against.\n10. NO IMPLEMENTATION LEAKED IN -- PASS. §13 contains three fenced blocks and none is Elixir: shell grep invocations used as corroborating evidence in §13.1, the index YAML in §13.4, and the verbatim header comment text in §13.8. §13.5 is specified behaviour; §13.7's two module additions are signatures and return-map shapes only. No test code, no function bodies.\nSCHEMA NOTE -- clear. step-04d's handoff now reads result.status: \"PASS\" (corrected in commit 5bc5882, after 6c999e4). No MINOR raised.\nSCOPE DISCIPLINE: §§1-12 were not re-derived, only checked for disturbance per check 1. The design artefact was not modified by this step. || RE-GATE PASS 2 (2026-08-21T07:15:58Z) -- VERDICT: PASS. Narrow re-gate of CODE-DESIGNER's re-amendment iteration 1 at commit 21701b1, on CHECK 3 plus a disturbance check. MAJOR-1 is CLOSED and MINOR-2 is FOLDED IN. Two new MINORs recorded (MINOR-4, MINOR-5); neither blocks. READ first-hand this pass: `git show --stat 21701b1`, the full `git diff 6c999e4 21701b1` (both files), the artefact at lines 1077-1086 (iteration marker), 1140-1164 (13.3), 1200-1275 (13.4 block comment and all three records), 1282-1347 (13.5 in full), 1345-1440 (13.6-13.10), 1442-1456 (13.11); and docs/status/requirement_status.index.yaml lines 1-80 on disk. DISTURBANCE CHECK -- PASS. `git show --stat 21701b1` = 2 files: the artefact (+61/-5) and step-04d's handoff (a completed_at bump only, +4/-4 across three fields, no result change; result.status still reads PASS, so the schema note stays clear). The artefact's six hunks are @@1077, @@1151, @@1219, @@1312, @@1335, @@1445 -- every one at or after line 1077, i.e. entirely inside 13 (which opens at 1060). Sections 1-12 show ZERO diff, confirmed from the hunk headers rather than from CODE-DESIGNER's claim: no hunk touches the banner, 5, A4/A5's markers, 8's erratum, the freeze-and-roll shape, the header text or the 23 instruction sites. Nothing I verified first-hand on pass 1 lies in the diff's reach, so checks 1,2,4,5,6,7,8,9,10 stand: the three 13.4 records are byte-identical to the set my own field-scan derived (4803/REQ-053/missing note, 4896/REQ-056/timestamp at 4899, 5050/REQ-054/missing note), 13.4's YAML is unchanged apart from an added comment paragraph inside the existing comment block (still well-formed), A5/13.6 is untouched, 13.7's digest paragraph and A8 clause (c) are untouched, and 13 still contains no Elixir. CHECK 3, RE-RUN -- PASS. THE ORIGINAL ATTACK IS GENUINELY STOPPED, not apparently stopped. Walked concretely: append a malformed entry to docs/status/requirement_status.v2.yaml (current), add a matching known_shape_anomalies: record citing that path. Under the new text at artefact 1312-1323, A4b asserts every record's path: names a volume whose volumes: entry has status: closed AND a frozen_prefix_sha256:. v2's index entry is `status: current` with no digest (verified on disk, index lines 55-59), so the record fails A4b -- and the text is explicit that this fires INDEPENDENTLY of set equality and EVEN WHEN the cited violation genuinely exists on disk, which is exactly the branch the attack relies on. The one-step, zero-friction route is closed. THE THREE SECONDARY PROBES -- all closed, by these mechanisms. (a) path not a volume in the index at all: closed by the rule's own words ('or a path that is not a volume in the index at all, fails A4b'), and belt-and-braces by A1, which asserts every file matching docs/status/requirement_status*.yaml appears in volumes: -- a record cannot cite a hidden file. (b) status: closed with no digest, or a digest with no status: closed: closed by the conjunction -- the rule requires BOTH, so either half alone fails. (c) a record declared against a closed volume for a violation NOT on disk: caught by set equality's 'declared but not on disk' direction (1306-1311), which iteration 1 left intact and whose failure output is now specified to name that element by side. THE ROLL-BOUNDARY PROBE, the one ORCH asked me to weight most -- the route is NARROW BUT NOT FULLY CLOSED, and I judge it MINOR, not MAJOR. Concretely: an agent appends a malformed entry to current volume 2, and IN THE SAME COMMIT closes volume 2 (writes the closure footer, computes frozen_prefix_sha256 over the now-malformed prefix, flips status: closed), opens volume 3, and adds a record citing volume 2. At the moment the suite runs, volume 2 IS closed and digest-pinned, so A4b's new rule passes; set equality passes because the violation is really on disk; A1/A2/A6/A7/A8 all pass on a correctly-performed roll. Nothing in 7 or 13 asserts that a roll was WARRANTED -- A3 bounds the current volume from above only, and roll_rule.on_exceed is prose in the index. So the window exists, and 13.4's own new comment concedes its shape ('this list can never grow either except when a volume is closed with a defect already in it'). WHY IT IS NOT THE SAME FINDING AS MAJOR-1, i.e. why I do not fail on it: MAJOR-1's route was SILENT and one-step -- a single line added to the index, nothing else in the diff. This route cannot be taken silently. It requires fabricating a volume closure: a footer naming the successor (A7), a correct SHA-256 over the CRLF-normalised prefix (A6 goes red on any error), a new volume file carrying the full header including A8(a)/(b)/(c)'s tokens, and a restructured volumes: block -- the loudest, most conspicuous multi-file operation this system has, and one that permanently pins the defect into a published digest. It is strictly more work than the honest route (fix the field in the working tree, where the entry is still uncommitted), and it is self-defeating for exactly the weak model core-directives.md's checklist rule is written for. AND the residual cannot simply be legislated away: the same window is the LEGITIMATE case -- a real defect appended shortly before a genuine roll and noticed only after closure is one where declaring is the correct outcome, because the append-only rule forbids fixing a closed volume. A rule forbidding all records added at closure time would make that case a permanent red. Closing the abusive half needs a different instrument (e.g. asserting every closed volume's lines/bytes_working_tree actually exceeded roll_rule -- volume 1's 5766/361376 pass it comfortably), which would kill the PREMATURE-roll variant while leaving the genuinely-full case declarable. Recorded as MINOR-4 for a later run rather than held against this amendment: the fix ORCH asked for was specified precisely, and it does exactly what it claims. ALSO-CONFIRM ITEMS. (a) PASS, checked against the actual index file on disk and not the design's claim about it: all three 13.4 records cite docs/status/requirement_status.yaml; the index's volumes: entry for that path reads `status: closed` and carries `frozen_prefix_sha256: \"5a1a64ab0b999da3fd86be90109ecee46b9d538d9e9945c0d39fddb10075a804\"` alongside frozen_prefix_lines: 5766 -- so all three satisfy the new rule as the tree stands, and the amendment is self-consistent on day one. Volume 2 (v2.yaml) is status: current with no digest field, which is what makes the attack fail. (b) PASS -- 13.3's bullet was genuinely REWRITTEN, not supplemented: the diff replaces 5 lines with 12, the old text's 'Volume 2 onward are checked live... before it is ever frozen' is gone, and the replacement now CITES the assertion ('That second half is enforced by A4b's closed-volume-only rule (13.5)') and states what the bullet would rest on without it. The claim is no longer load-bearing prose. (c) PASS -- MINOR-2 is folded in at 1335-1344: on any failure, either direction of set equality or the closed-volume rule, the message must name EVERY element of the symmetric difference, one per line, with path, entry_line, req, kind (and field/found_as where set), LABELLED BY SIDE with the two verbatim labels 'on disk but not declared' / 'declared but not on disk', plus every record failing the closed-volume rule with the disqualifying status -- and 'reporting only a count, or only that the sets differ, is not sufficient' is stated explicitly. That makes 13.9's mix-test landing-point claim true by specification rather than by an implementer's choice of message. (d) PASS -- 13.11 Q1 is answered on the record at 1445-1452 as `note:` MANDATORY, citing volume 1's own header lines 12-17, volume 2's mandatory wording and 180-of-182, and closing with 'No change to A4b follows from this.' The question is now a settled record, not an open decision an applying agent could resolve either way. THE TWO DISPOSITIONED ITEMS, sanity-checked as ORCH asked. MINOR-2b -- I AGREE IT IS GENUINELY OPTIONAL, not quietly load-bearing. A8(c)'s literal-token check defends the failure it was written for (a header copied forward that drops the declaration entirely). The residue -- a header keeping the token but dropping the three line citations -- degrades ONE of 13.9's four landing points while the other three (the index's records with cause, A4b's now-fully-specified failure output, and 13.1's table) still name all three entries by line and req id. And CODE-DESIGNER's reason is correct on the mechanics: a citation-level clause would have to parse header COMMENT TEXT against index RECORDS, a different assertion kind from A8(a)/(b)'s token-presence checks, so it does not cost 'a clause' as my pass-1 note implied. Nothing depends on it. MINOR-3's consequence -- THE SURVIVING PARITY CITATION IS ACCEPTABLE AS WRITTEN; no word needs changing in this pass. I checked where parity actually survives. It is NOT in the bounding bullet any more -- that bullet now cites the assertion and nothing else. What survives is (1) line 1180's 'that is precisely what known_anomalies: already is for vocabulary, and it has been uncontroversial', which sits inside the argument for REJECTING candidate (ii) and supports a different proposition -- that a permanent, declared, machine-checked exception list is a desirable outcome rather than a cost -- a proposition the closed-volume asymmetry does not touch; and (2) line 1148's 'a reader who already understands one understands the other'. Only (2) is now imprecise, since a reader of known_anomalies: does not thereby learn about the extra closed-volume constraint. It is a comprehension nit, not a misleading claim: the constraint is stated twice more, prominently, in the two places an agent actually acts -- 13.4's block comment in the verbatim index YAML they paste, and 13.5's A4b text the test author implements. I would leave both sentences alone and let MINOR-3's own run revisit 1148 when it applies the constraint to A5, at which point parity becomes true again rather than needing a caveat now that would be deleted then. NEW THIS PASS -- MINOR-5, an internal inconsistency iteration 1 created and did not clean up: 13.10's 'one instruction that is not optional' still reads, unconditionally, 'If it reports a fourth shape violation, that is a new finding of the same kind -- declare it in known_shape_anomalies: with its cause.' After iteration 1 that instruction is wrong in one branch: a fourth violation found in the CURRENT volume must be fixed in the working tree, never declared, and an agent following 13.10 literally will add a record A4b now rejects. It fails CLOSED -- the gate stops the bad state rather than admitting it -- and the correct action is stated verbatim two sections earlier (13.4's comment: 'A shape defect in the CURRENT volume must be FIXED in the working tree before it is committed'), so no invention is required, only cross-reading. Recorded, not failed. SCOPE DISCIPLINE: checks 1,2,4,5,6,7,8,9,10 were re-confirmed only where this diff could reach them, per ORCH's narrow instruction, and were not re-derived. The design artefact was not modified by this step, and handoffs/registry.json and handoffs/orchestrator.log were not touched.",
    "artifacts_out": [
      "handoffs/WF03-ISS0119-20260821/step-04e-code-design-validator-amendment.json"
    ],
    "issues": [
      {
        "severity": "MAJOR",
        "id": "MAJOR-1",
        "summary": "§13's bounding claim for known_shape_anomalies: is prose only -- nothing mechanically stops a future agent from declaring away a NEW shape defect in the CURRENT volume.",
        "detail": "§13.3 claims the list is 'bounded and permanent, not a growing suppression list' because volume 1 is frozen and because 'volumes 2 onward are checked live from their first entry, so a defect there is caught while the volume is still current and appendable'. A4b as specified in §13.5 asserts only set equality between on-disk shape violations and the declared list -- it places no constraint on WHICH volume a declared record may cite. An agent that appends a malformed entry to the current volume and adds a matching known_shape_anomalies: record turns A4b green, permanently, with no gate tripped. §13.10's 'Do not hand-tune the declared list to make A4b green' and core-directives.md's 'Never Satisfy a Gate by Editing What It Measures' are the only barriers, and both are prose addressed to an agent's judgement -- which core-directives.md's Humanless-operation section says explicitly must not be what a gate rests on.",
        "missing_element": "A4b must additionally assert: every known_shape_anomalies: record's path: names a volume whose index entry has 'status: closed' and carries a frozen_prefix_sha256:. Equivalently: no record may cite the current volume. State it in §13.5 alongside the identity key and the req/event cross-check, and add the corresponding sentence to §13.4's block comment (which currently justifies entry_line stability by volume 1's frozen-ness without making that a rule). This changes none of the three proposed records -- all cite volume 1, closed and digest-pinned -- and makes §13.3's own argument machine-enforced.",
        "action": "Return to CODE-DESIGNER for a narrow §13-only amendment."
      },
      {
        "severity": "MINOR",
        "id": "MINOR-2",
        "summary": "§13.9 claims 'A4b names all three whenever it fails in either direction', but §13.5 does not specify A4b's failure-message content.",
        "detail": "A set-equality failure need only report the symmetric difference, not the full declared set. §13.10 ('Run A4b, read the full set it reports') assumes a full-set report, and the current A4 implementation in test/docs/requirement_status_invariants_test.exs already interpolates whole violation lists into its assert message -- so this is very likely to come out right, but it is claimed as a visibility surface without being specified. One sentence in §13.5 requiring the failure message to print BOTH the full on-disk shape-violation set and the full declared set (not just the difference) makes §13.9's claim true by specification.",
        "action": "Fold into the same §13 amendment."
      },
      {
        "severity": "MINOR",
        "id": "MINOR-2b",
        "summary": "A8 clause (c) asserts only the literal token, so a header that keeps known_shape_anomalies: while dropping the three line citations still passes.",
        "detail": "Clause (c) closes the hole it was written for (a copied header that drops the declaration entirely). Consider strengthening it the way A8(b) treats the ceilings: assert that each entry_line in the index's known_shape_anomalies: appears in the current volume's header text, so the header addendum is a machine-checked copy rather than a prose one. Optional; recorded so the asymmetry with A8(b) is on record rather than a surprise.",
        "action": "CODE-DESIGNER's discretion; not required to clear this gate."
      },
      {
        "severity": "MINOR",
        "id": "MINOR-3",
        "summary": "known_anomalies:/A5 carries the same declare-to-silence hole as MAJOR-1, and §13.3 cites that parity as support.",
        "detail": "§13.3 argues (i) is safe partly because it mirrors known_anomalies:, 'and it has been uncontroversial'. The vocabulary mechanism has never been constrained to closed volumes either. Out of scope for this amendment -- flagged so it is not treated as settled by the parity argument, and so a later run can apply the MAJOR-1 constraint to A5 as well.",
        "action": "File/forward for a later run; do not expand this amendment's scope."
      },
      {
        "severity": "MINOR",
        "id": "MINOR-4",
        "summary": "Roll-boundary residual: a fresh shape defect can still be declared permanently if the current volume is closed in the same commit that adds the record.",
        "detail": "Raised and dispositioned at re-gate pass 2. A4b's new closed-volume-only rule is evaluated against the index AS IT STANDS WHEN THE SUITE RUNS, so an agent that appends a malformed entry to the current volume and, in the same commit, performs a full volume closure (footer, correct frozen_prefix_sha256 over the malformed prefix, status: closed, successor opened) can then cite that now-closed volume and pass both the new rule and set equality. Nothing in 7 or 13 asserts that a roll was warranted: A3 bounds the current volume from above only, and roll_rule.on_exceed is prose. NOT failed, for two reasons. First, the route is not silent -- unlike MAJOR-1's single added index line, it demands a conspicuous multi-file volume closure that must survive A1, A2, A6, A7 and A8, including a correct CRLF-normalised SHA-256, and it is strictly more work than the honest route of fixing an uncommitted field in the working tree. Second, the same window is the legitimate case: a defect noticed only after a genuine roll is one where declaring IS correct, because append-only forbids editing a closed volume, so a blanket ban on closure-time records would create a permanent red. 13.4's own comment already concedes the shape of the residual ('except when a volume is closed with a defect already in it').",
        "missing_element": "None required to clear this gate. For a later run: an assertion that every CLOSED volume's lines/bytes_working_tree exceeded roll_rule.max_lines/max_bytes at closure would kill the premature-roll variant (volume 1's 5766/361376 pass it comfortably) while leaving the genuinely-full case declarable.",
        "action": "File alongside MINOR-3 for a later run; do not expand this amendment's scope."
      },
      {
        "severity": "MINOR",
        "id": "MINOR-5",
        "summary": "13.10's 'declare a fourth violation' instruction was not updated for the closed-volume-only rule iteration 1 added.",
        "detail": "13.10 still reads unconditionally: 'If it reports a fourth shape violation, that is a new finding of the same kind -- declare it in known_shape_anomalies: with its cause.' That is now wrong when the fourth violation is in the CURRENT volume, where the required action is to fix it in the working tree before committing. An agent following 13.10 literally adds a record A4b rejects. It fails closed rather than admitting bad state, and the correct action is stated verbatim in 13.4's block comment and again in 13.5, so no invention is required -- but 13.10 is the section labelled 'one instruction that is not optional' and is the one an applying agent treats as authoritative.",
        "missing_element": "One clause in 13.10: '...unless the violation is in the CURRENT volume, in which case fix it in the working tree before committing -- it can never be declared (13.5's closed-volume rule).'",
        "action": "CODE-DESIGNER's discretion; may be folded into any later 13 touch. Not required to clear this gate."
      }
    ],
    "next_action": "ORCH: 13 is GATED. Proceed -- ELIXIR-DEV or DOC-UPDATER applies 13.4 (index) and 13.8 (volume-2 header); TEST-DESIGNER applies 13.5-13.7 to test/support/status_history.ex and test/docs/requirement_status_invariants_test.exs, including A4b's closed-volume-only rule and its specified failure output. Sections 1-12 remain gated and must not be revisited. MINOR-4 (roll-boundary residual) and MINOR-5 (13.10's stale declare-a-fourth instruction) are recorded, not blocking; MINOR-4 belongs with MINOR-3 in the follow-up issue ORCH is filing, since both are the same class of bound-the-exception-list work. MINOR-2b stays declined."
  },
  "rework_count": 1,
  "max_rework": 3
}