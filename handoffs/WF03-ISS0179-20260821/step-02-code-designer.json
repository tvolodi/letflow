{
  "handoff_id": "1d2a6bd3-99b1-431f-94ea-70967aa14add",
  "run_id": "WF03-ISS0179-20260821",
  "workflow_id": "WF-03",
  "step": "02",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF03-ISS0179-20260821/step-02-code-designer.json",
  "created_at": "2026-08-21T13:55:55Z",
  "started_at": "2026-08-21T13:55:55Z",
  "completed_at": "2026-08-21T14:04:41Z",
  "status": "COMPLETED",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "requirement_text": {},
    "related_handoff_ids": [],
    "artifacts_in": [
      "docs/issues/ISS-0111.yaml",
      "docs/issues/ISS-0107.yaml",
      "docs/issues/ISS-0110.yaml",
      "test/letflow/identity_migration_test.exs",
      "priv/repo/migrations/20260819000004_drop_legacy_public_identity_tables.exs"
    ],
    "owned_modules": [
      "test/letflow/identity_migration_test.exs"
    ]
  },
  "task": {
    "description": "Design a fix for ISS-0111. Read `docs/issues/ISS-0111.yaml` in full -- it is thorough and this dispatch does not restate it. `with_only_this_tenant_visible!/2` in `test/letflow/identity_migration_test.exs` (lines 263-292) bulk-DELETEs every OTHER tenant's row from `public.tenant_schemas` into a backup table, runs a guarded migration, then restores the rows -- sound within one serialized `mix test` invocation, unsafe the moment a second invocation touches the same database (which `ISS-0107`, still open, demonstrably allows via a nested `mix test` spawned by `Mix.Tasks.Letflow.Check.Test`).\n\nWHY THIS ISN'T A SIMPLE ONE-LINE FIX. `priv/repo/migrations/20260819000004_drop_legacy_public_identity_tables.exs` (read it in full) is a REAL, ALREADY-SHIPPED Ecto migration whose `up/0` does `FOR v_tenant IN SELECT tenant_id, schema_name FROM public.tenant_schemas LOOP` -- an unscoped, whole-table scan, BY DESIGN: in a real deployment it must see every tenant to decide whether it's safe to drop the shared legacy tables. The test wraps it in a global mutation (`with_only_this_tenant_visible!/2`) specifically because there is no way to hand the migration's dynamic SQL a WHERE-clause scope without changing the migration's own production semantics -- and this project does not casually edit an already-shipped migration.\n\nTHE OPTIONS, WEIGH THEM RATHER THAN PICKING THE FIRST PLAUSIBLE ONE:\n\n(a) **Mutual exclusion via a Postgres advisory lock**, held by `with_only_this_tenant_visible!/2` for the duration of the snapshot/restore window, acquired with `pg_advisory_lock` (session-level, blocking) before the DELETE and released after the restore. This does NOT require ISS-0107 to be fixed first: it makes the destructive window mutually exclusive across ANY concurrent Postgres session touching the same database, including a nested `mix test` invocation, because the lock lives in Postgres itself, not in this process. Consider: does the SQL Sandbox's connection-per-test model interact badly with holding a session-level advisory lock across `Repo.query!/2` calls that may use different pooled connections? (The file's own `Ecto.Adapters.SQL.Sandbox.mode(Letflow.Repo, :auto)` comment says `:auto` mode is used specifically to escape the sandbox's single shared connection -- read why, and confirm whether an advisory lock acquired on one connection and released on another would silently no-op, which would be worse than no lock at all.)\n\n(b) **Do not touch shared state at all** -- give the guarded migration a genuinely isolated view, e.g. running the ENTIRE guarded scenario (fixture setup, migration, assertions) inside a database transaction that is rolled back rather than committed, so no other session ever observes the intermediate state regardless of what else touches the database. Consider: does `Ecto.Migrator.run/4` work correctly inside an already-open transaction, and does the DDL involved (dropping tables) behave transactionally in Postgres (it does, DDL is transactional here) versus requiring a fresh connection outside any transaction?\n\n(c) **Defer this entirely to ISS-0107** on the reasoning that fixing the actual enabler (stop a nested mix test from silently sharing its parent's database) removes the precondition for every hazard in this family (ISS-0110, ISS-0111) at once, making a local fix here redundant work. Weigh this seriously -- it may be the more efficient path -- but note ISS-0107 has been open since before this session started and nothing indicates it will be picked up soon; a local, self-contained mitigation may be worth shipping regardless as defense in depth, per ISS-0110's own analogous reasoning (\"real liveness tracking... is the direction that removes the guess rather than retuning it\").\n\nDECIDE, with reasoning stated in the design doc, then specify the fix precisely: exact SQL/Ecto calls, exact synchronization primitive and its acquire/release points, and what changes (if anything) about the restore-on-crash self-heal logic (`restore_orphaned_guard_backup_rows!/0`) given the new mechanism. If you choose (c), the design's deliverable is a clear written justification plus a specific, actionable recommendation for ORCH (defer, mark instrumented pointing at ISS-0107, or file a dependency) -- not an implementation.\n\nDESIGN-ARTEFACT DISCIPLINE: signatures, type shapes, and prose only if you choose (a) or (b) and it's a genuine design decision needing CODE-DESIGN-VALIDATOR's gate; if you conclude (c) is correct, write the recommendation directly into a design note and say so, since there is nothing to implement.",
    "acceptance_criteria": [
      "All three options (advisory lock, transactional isolation, defer to ISS-0107) are weighed on their actual merits against this specific code -- the migration's real SQL, the sandbox's :auto-mode comment, and ISS-0107's status -- not assumed",
      "If (a) is chosen: the advisory-lock acquire/release points are exact, and the sandbox connection-pooling question (same session for acquire and release) is explicitly resolved with evidence, not assumed safe",
      "If (b) is chosen: whether Ecto.Migrator.run/4's DDL behaves correctly inside an open transaction is verified against this specific migration, not assumed",
      "If (c) is chosen: a specific, actionable recommendation is given (defer/instrumented/dependency-file), not a vague punt",
      "Whichever is chosen, the interaction with restore_orphaned_guard_backup_rows!/0's crash self-heal logic is addressed explicitly",
      "No .ex implementation body is written if the design goes to a further implementation step -- signatures and prose only in that case",
      "No file outside test/letflow/identity_migration_test.exs (or a new design doc under lib/letflow/design/, if warranted) is modified"
    ]
  },
  "result": {
    "status": "PASS",
    "summary": "Chose option (a): mutual exclusion via a Postgres session-level pg_advisory_lock/pg_advisory_unlock pair, keyed on a fixed literal ('letflow:test:iss0060_tenant_schemas_guard' via hashtext/1, same idiom lib/letflow/tenant_provisioning.ex already uses), held for the full move-out/guarded-migration/move-back window in with_only_this_tenant_visible!/2, wrapped in one Repo.checkout/2 call so acquire and release land on the identical Postgres session (traced Ecto's checkout_or_transaction/get_conn/put_conn process-keyed pinning in deps/ecto_sql/lib/ecto/adapters/sql.ex to confirm a bare Repo.query!/2 in :auto mode is NOT guaranteed same-session across calls -- the risk the task flagged is real without this wrapper). restore_orphaned_guard_backup_rows!/0 must acquire/release the SAME lock (also via its own Repo.checkout/2) or it can wrongly restore another invocation's legitimately-hidden rows mid-critical-section -- addressed explicitly in the design (section 3.3). Option (b) (wrap the whole scenario in one rolled-back transaction) is REJECTED on hard evidence, not preference: deps/ecto_sql/lib/ecto/migrator.ex's own documented execution model shows Ecto.Migrator.run/4 dispatches the actual migration work via Task.async onto a SEPARATELY-checked-out connection/transaction (async_migrate_maybe_in_transaction/run_maybe_in_transaction), which commits independently of whatever transaction the calling test process holds -- an outer rollback could not undo it. This is exactly why test/specs/REQ-022.md documents replay_migrations/2 hitting a DBConnection queue_timeout under sandbox mode and fixing it by switching to :auto mode rather than trying to keep everything on one connection. Option (c) (defer to ISS-0107) was weighed and rejected as the sole action: re-checked docs/issues/ISS-0107.yaml directly, status is still 'open' with no sign of imminent work, so a known reproducible corruption path (ISS-0111's own cited evidence: 10/14 failures in the run that discovered ISS-0109 traced to this mechanism) would stay open indefinitely; the option-(a) fix is small, self-contained to one test file, doesn't depend on ISS-0107 landing, and remains correct defense-in-depth afterward -- consistent with ISS-0110's own recorded reasoning. Full evidence, exact fix specification (signatures, acquire/release points, control-flow nesting in prose, acceptance-criteria-2 table), and design written to lib/letflow/design/iss0111-with-only-this-tenant-visible-advisory-lock-fix.md -- no .ex implementation written, only that new design doc plus this handoff/status change.",
    "artifacts_out": [
      "lib/letflow/design/iss0111-with-only-this-tenant-visible-advisory-lock-fix.md"
    ],
    "issues": [],
    "git_evidence": {
      "branch_name": "feature/WF03-ISS0179-20260821",
      "commit_sha_list": [],
      "remote_branch": "origin/feature/WF03-ISS0179-20260821",
      "push_status": "skipped",
      "pr_url": null,
      "pr_create_error": null
    },
    "next_action": "Route to CODE-DESIGN-VALIDATOR to gate this design before ELIXIR-DEV implements section 3's fix in test/letflow/identity_migration_test.exs."
  },
  "rework_count": 0,
  "max_rework": 3
}
