{
  "handoff_id": "step02-WF03-ISS0216-20260826",
  "run_id": "WF03-ISS0216-20260826",
  "workflow_id": "WF-03",
  "step": "02",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF03-ISS0216-20260826/step-02-code-designer.json",
  "created_at": "2026-08-26T12:36:00Z",
  "started_at": "2026-08-26T12:36:00Z",
  "completed_at": null,
  "status": "PENDING",
  "priority": "NORMAL",
  "context": {
    "stage": null,
    "requirement_ids": [],
    "issue_ids": ["ISS-0216"],
    "related_handoff_ids": ["handoffs/WF03-ISS0216-20260826/step-01-issue-fixer.json"],
    "artifacts_in": ["lib/letflow/api/pagination.ex"],
    "owned_modules": ["lib/letflow/api/pagination.ex"],
    "branch_name": "feature/WF03-ISS0216-20260826",
    "note": "ISSUE-FIXER's diagnosis (step-01): check_prefix/2 (lib/letflow/api/pagination.ex:211-218) is vacuously true for prefix == \"\", defeating cross-endpoint cursor isolation. Not currently exploitable (all 9 real call sites pass hardcoded non-empty literals) but the guard itself is structurally missing -- defense-in-depth fix needed per ISS-0216's acceptance criteria."
  },
  "task": {
    "description": "Design a fix per WF-03 Step 2 (same procedure as WF-02 Step 1). Claim this handoff by setting status to IN_PROGRESS only. Write lib/letflow/design/iss0216-pagination-prefix-guard.md specifying the exact change to check_prefix/2 (or decode_cursor/4) that rejects an empty-string prefix outright, before the existing size/starts_with? check -- design only, no implementation code. State the error shape (reuse {:error, :wrong_endpoint}, or introduce a new tag -- your call, state which and why). Confirm the design doesn't change behavior for any of the 9 existing non-empty-prefix call sites (their behavior must be unchanged).",
    "acceptance_criteria": [
      "design specifies check_prefix/2 (or decode_cursor/4) rejecting prefix == \"\" outright, stated as a concrete function-signature-level change (which clause, what it returns)",
      "the chosen error shape is stated explicitly with reasoning ({:error, :wrong_endpoint} reuse vs. a new tag)",
      "design states explicitly that all 9 existing call sites' behavior is unchanged (they never pass an empty prefix)",
      "no implementation code present -- signatures/type shapes/prose only"
    ]
  },
  "result": null,
  "rework_count": 0,
  "max_rework": 3
}
