{
  "handoff_id": "WF03-ISS0426-20260902-step-02-rework2",
  "run_id": "WF03-ISS0426-20260902",
  "workflow_id": "WF-03",
  "step": "02",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF03-ISS0426-20260902/step-02-code-designer-rework2.json",
  "created_at": "2026-09-02T19:11:58Z",
  "started_at": "2026-09-02T19:11:58Z",
  "completed_at": null,
  "status": "PENDING",
  "priority": "HIGH",
  "context": {
    "stage": "S6",
    "requirement_ids": [],
    "issue_ids": [
      "ISS-0426"
    ],
    "artifacts_in": [
      "handoffs/WF03-ISS0426-20260902/step-02b-code-design-validator-regate.json",
      "lib/letflow/design/iss426-wallclock-test-contention.md",
      "lib/letflow/engine/lua/executor.ex",
      "test/letflow/engine/lua/executor_test.exs"
    ],
    "branch_name": "feature/WF03-ISS0426-20260902",
    "owned_modules": [
      "lib/letflow/design/iss426-wallclock-test-contention.md"
    ]
  },
  "task": {
    "description": "REWORK 2 of the ISS-0426 fix design -- NARROW AND FINAL-SHAPED. CODE-DESIGN-VALIDATOR re-gated and returned FAIL, but a much narrower one: checks 1(a), 1(b), 2, 3, the scope header and AC4 ALL PASSED and are settled. Do not revisit them, do not restructure the design, and do not change either seam's mechanism. Exactly one defect blocks the gate, and it is a CONTRACT/DOCUMENTATION fix, not a redesign.\n\nTHE BLOCKING DEFECT (check 1(c), the deadlock question). Section 2.1.2's seam removes the `after` clause, so the caller waits unboundedly. The validator confirmed -- and ORCH independently confirmed by reading test/letflow/engine/lua/executor_test.exs:568-597 -- that this is genuinely SAFE for the seam's two current call sites: @moderate_allocating_script and @gigabyte_allocating_script are both fixed-iteration `for` loops with no trap-and-continue construct, so they terminate independently of any wall-clock enforcement. The defect is NOT that the seam is unsafe today. The defect is that the design states this only as an OBSERVATION ABOUT CURRENT USAGE, never as a BINDING CONTRACT ON THE SEAM. That makes it a loaded gun for the next caller: safe because of what happens to be passed to it, not because the mechanism prevents an unsafe call.\n\nWhy this matters concretely, from the production module's own moduledoc (executor.ex:54-80, which ORCH read and quotes here so you do not have to rediscover it): `:max_instructions` is an IN-BAND, pcall-CATCHABLE VM counter -- 'when it is exhausted, control returns to the Lua script itself... and the script decides what happens next -- that is the definition of relying on Lua to cooperate.' The module then names the exact hostile case: 'a script that pcalls its own budget exhaustion and loops again is still killed, because the outer timeout has no counter of its own for the script to reset or dodge.' For such a script the wall-clock kill is the ONLY bound. Pass one to section 2.1.2's seam and the test hangs until ExUnit's 60s default timeout -- a materially worse failure mode than the flake this whole run is fixing.\n\nREQUIRED FIX (this is the whole of it):\n  1. State an EXPLICIT, BINDING USAGE CONTRACT on section 2.1.2's seam, in the design text AND as the @doc false declaration's own stated content that ELIXIR-DEV will write. Wording along the lines the validator proposed: callers MUST pass a workload that terminates independently of wall-clock enforcement (fixed-iteration or otherwise self-bounding, or bounded by the heap limit); this seam deliberately does NOT bound wall-clock time, and passing a script that can trap its own budget and continue will hang the caller. Say what the contract IS, why it exists, and what breaks if violated -- so a future caller reading only the @doc false knows the rule without reconstructing this run's reasoning.\n  2. WHILE YOU ARE THERE -- the validator noted section 2.1.1's nil-heap seam has a genuinely STRONGER bound that the design ALSO never states: `run_script/3` is always bounded by `:max_instructions` for any non-`goto` input, which is categorical rather than usage-dependent. State that bound explicitly too, and state the DIFFERENCE between the two seams' guarantees, so the design does not leave a reader assuming both seams are equally safe when one is categorically bounded and the other is bounded only by its caller's discipline. This is not a new gate item -- it is the same defect class the gate found, in the sibling section, and fixing it now avoids a third cycle.\n\nSCOPE DISCIPLINE. Change nothing else. Both seams keep their current mechanisms. The counts, the table, the mixed-tests decision (section 2.4), the tag wiring, the letflow.check.test.ex change and the scope header are all gate-approved as they stand. Design only -- no implementation code. Update lib/letflow/design/iss426-wallclock-test-contention.md in place, complete this handoff, and commit both to branch feature/WF03-ISS0426-20260902.\n\nrework_count is 2 of max 3. This is the last rework before ORCH must escalate per ORCHESTRATOR.md, so make the contract statement complete and unambiguous on this pass."
  },
  "result": {
    "status": null,
    "summary": null,
    "issues": [],
    "artifacts_out": [],
    "next_action": null
  },
  "rework_count": 2,
  "max_rework": 3
}