{
  "handoff_id": "WF03-ISS0451-20260904-step-02",
  "run_id": "WF03-ISS0451-20260904",
  "workflow_id": "WF-03",
  "step": "02",
  "from_agent": "ORCH",
  "to_agent": "CODE-DESIGNER",
  "file": "handoffs/WF03-ISS0451-20260904/step-02-code-designer.json",
  "created_at": "2026-09-04T02:50:00Z",
  "started_at": "2026-09-04T03:32:34Z",
  "completed_at": "2026-09-04T03:34:30Z",
  "status": "COMPLETED",
  "priority": "HIGH",
  "context": {
    "stage": "S6",
    "requirement_ids": [],
    "issue_ids": ["ISS-0451"],
    "requirement_text": {},
    "artifacts_in": [
      "docs/issues/ISS-0451.yaml",
      "handoffs/WF03-ISS0451-20260904/step-01-issue-fixer-diagnosis.json",
      "lib/letflow/application.ex",
      "lib/letflow/supervisor/pollers.ex",
      "lib/letflow/supervisor/infrastructure.ex",
      "lib/letflow/supervisor/http.ex",
      "lib/letflow/design/req219-supervision-layering.md",
      "test/letflow/supervisor/pollers_test.exs"
    ],
    "branch_name": "fix/ISS-0451-persistent-poller-crash-budget-20260904"
  },
  "task": {
    "description": "Read ISSUE-FIXER's diagnosis in full at handoffs/WF03-ISS0451-20260904/step-01-issue-fixer-diagnosis.json (result.summary) -- it confirmed empirically (real code read plus a live throwaway probe) that a PERSISTENT Poller fault cascades from Letflow.Supervisor.Pollers' own budget exhaustion (max_restarts: 5, max_seconds: 60) into the top-level Letflow.Supervisor's default OTP restart budget (3 restarts / 5 seconds, never overridden in lib/letflow/application.ex) within about 96ms wall-clock, taking down the whole application (Repo, Bandit included) -- the exact incident REQ-219 exists to prevent, just for a persistent rather than single-shot fault.\n\nProduce a design artefact in lib/letflow/design/ (follow this project's existing convention, e.g. lib/letflow/design/req219-supervision-layering.md's shape) choosing and fully specifying a fix. ISS-0451's own description names two candidate options, neither prescribed: (a) raise the top-level Letflow.Supervisor's own restart intensity so it tolerates at least one full Pollers-budget-exhaustion cycle without exiting itself, and/or (b) give Pollers a circuit-breaker/backoff so a persistent fault degrades to a stopped-but-not-crash-looping state rather than repeatedly re-exhausting its own budget and repeatedly costing the top level a restart. You choose the design -- this is a real architectural decision with an availability tradeoff (see the design considerations below), not a default to pick without reasoning.\n\nDesign considerations to work through and document (this is what ISS-0451's AC2 requires -- concrete arithmetic, not just a chosen number):\n- If you raise the top-level's restart intensity alone: what value tolerates at least one full Pollers-exhaustion-and-restart cycle (Pollers' own 5-in-60s budget means Pollers can legitimately exit and be restarted by the top level at most once per 60s under NORMAL bounded-transient-fault operation before Pollers' own budget resets -- but under a PERSISTENT fault, Pollers re-exhausts almost immediately after each restart, so the top level would need to tolerate MANY such Pollers-exits within its own window, not just one, unless something also slows the retry rate). State the exact max_restarts/max_seconds you choose for the top level and show the arithmetic for why it survives a persistent fault indefinitely (or for how long, if not indefinitely) rather than merely surviving one extra restart.\n- If you add a circuit-breaker/backoff to Pollers instead (or in addition): what state machine, what backoff schedule, what makes it degrade to 'stopped, not crash-looping' rather than continuing to consume top-level restart budget at all. Consider that Pollers currently holds TWO independently-gated poller GenServers as peers (Letflow.Scheduler.Poller and Letflow.Engine.ServiceTaskDispatcher.Poller) -- a circuit-breaker design must state whether it applies per-poller or to the whole Pollers supervisor, and why.\n- Whichever you choose (or if you choose both), the design must not weaken REQ-219's own AC2 (boot-order: Infrastructure before Pollers, structural via list order) or AC4 (single-crash isolation: Pollers exhausting its budget once must still leave Infrastructure/Http alone) -- state explicitly how your design preserves both.\n- The design must specify exactly what test(s) TEST-DESIGNER will need to write: a regression test simulating a PERSISTENT Poller fault (never cleared, unlike REQ-219's AC4 test) proving Letflow.Supervisor (top-level), Letflow.Repo, and the Bandit listener all remain alive after Pollers repeatedly exhausts its budget -- name the exact mechanism the test will use to keep the fault persistent (a test-only flag, following the existing force_poller_crash precedent in pollers_test.exs, or something else) and what observable proves the top-level supervisor never exited (e.g. Process.whereis(Letflow.Supervisor) staying the same pid, or a monitor).\n\nThis is a design artefact ONLY -- no implementation code, no .ex file changes outside lib/letflow/design/. Signatures/specs/config-value choices only, per this project's design-vs-implementation convention (see CODE-DESIGN-VALIDATOR's own gate criteria, which will check for implementation code present and fail the gate if found).",
    "acceptance_criteria": [
      "Design artefact created under lib/letflow/design/, named appropriately (e.g. iss0451-poller-crash-budget-isolation.md)",
      "Design states the exact top-level restart-intensity value chosen (if that lever is used) with the arithmetic showing it tolerates at least one full Pollers-budget-exhaustion cycle, per ISS-0451 AC2",
      "Design states the exact circuit-breaker/backoff mechanism (if that lever is used) with its state machine and thresholds",
      "Design explicitly confirms REQ-219 AC2 (boot order) and AC4 (single-crash isolation) are preserved and states why",
      "Design specifies the exact test mechanism and observable(s) for the new persistent-fault regression test",
      "No implementation code (.ex function bodies) present in the design document -- signatures/specs/config values only"
    ],
    "rework_note": "REWORK ITERATION 3 (FINAL -- max_rework is 3; escalation follows if this fails). This time the trigger is NOT a CODE-DESIGN-VALIDATOR gate FAIL -- the design passed that gate clean on iteration 2. It is an ELIXIR-DEV implementation-stage finding: handoffs/WF03-ISS0451-20260904/step-03-elixir-dev.json's result.issues[0] (read it there for the complete detail -- this is a summary). ELIXIR-DEV implemented the design exactly as specified -- lib/letflow/supervisor/pollers_breaker.ex and lib/letflow/application.ex are both believed correct and are NOT in question. The gap is narrower and specific to section 5's guidance for adapting the EXISTING test/letflow/supervisor/pollers_test.exs AC4 test: implementing that guidance literally surfaces a genuine, previously-unflagged race the design never anticipated, because PollersBreaker now ALSO monitors Pollers and reacts to the SAME terminate_child-induced :DOWN the AC4 test uses to force a crash-loop. Two failure modes, both empirically reproduced: (a) if the test issues its own start_child/2 call per section 5's literal instruction, it races PollersBreaker's OWN start_child/2 call for the identical child spec -- whichever loses gets {:error, {:already_started, pid}} instead of {:ok, pid}, breaking the design's own literal {:ok, pid} = ... pattern (reproduced deterministically at --seed 0, flaky ~40% otherwise); (b) if the test instead waits for the breaker's own restart, Letflow.Scheduler.Poller's zero-delay first tick means the breaker-restarted Pollers can already be crash-looping and trip the breaker to :open, inside its 2-second observation window, before the test process is even scheduled to run its next assertion -- there is no synchronous window left, unlike the ORIGINAL test's reliance on ITS OWN synchronous restart call completing before control returned to it. ELIXIR-DEV's own three recommended directions (full detail in their handoff): (1) give PollersBreaker a test-only synchronization hook (double-gated like the existing pollers_init_probe/force_poller_crash precedent) letting a test coordinate with the breaker's :closed-state handler before triggering its own terminate_child; (2) redefine the AC4 test's own procedure to not attempt a manual pre-restart at all, instead driving the entire scenario (healthy -> crash flag -> terminate -> breaker restarts -> crash-loop -> breaker's own exhaustion detection -> :open -> :half_open recovery once cleared) as a single breaker-mediated flow, matching the shape of ISS-0451's OWN new regression test (your own section 6.2) rather than preserving the original test's exact structure; (3) something else you judge better. Choose one, specify it concretely and unambiguously enough for TEST-DESIGNER/ELIXIR-DEV to apply without inventing anything, and update section 5 (and any other section whose text depends on the old guidance) accordingly. The production-code sections (pollers_breaker.ex, application.ex) do not need further changes unless your chosen fix requires one (e.g. option 1's synchronization hook would need a small addition to pollers_breaker.ex's own spec) -- state explicitly whether it does. Finish this handoff normally per HANDOFF_PROTOCOL.md section 4 (status COMPLETED, full result block, completed_at from the real clock)."
  },
  "result": {
    "status": "PASS",
    "summary": "Resolved the iteration-3 rework: chose ELIXIR-DEV's own named option 2 (redefine the EXISTING pollers_test.exs AC4 test's procedure to drive the entire scenario as a single breaker-mediated flow, matching this design's own §6.2 regression-test shape) over their option 1 (a test-only synchronization hook added to PollersBreaker). Root cause of the race: the original AC4 test's structure -- test process performs one manual synchronous restart, then a SEPARATE automatic top-level restart is what gets exercised -- correctly described the PRE-breaker world (two non-competing actors), but no longer describes the real system once PollersBreaker became the SOLE restart authority for Pollers from the very first exit onward (design §3.4a): there is no longer a 'test does one restart, a separate mechanism does the next' story to preserve, so any attempt to keep that shape competes with the one actor that now legitimately owns every restart. Option 1 was rejected because it would add production code (a coordination hook in pollers_breaker.ex) whose only purpose is to make the breaker step aside so the test can perform a restart that, in the real system, the breaker always performs itself -- verifying a synthetic path that never occurs outside the test. Option 2 requires zero production-code changes and exercises the real, always-on path directly. Rewrote lib/letflow/design/iss0451-poller-crash-budget-isolation.md's rework-iteration preamble (new ITERATION 3 block) and §5 (full replacement of the AC4-adaptation guidance: removed the test-issued start_child/2 call entirely; the test now triggers via terminate_child/2 alone -- the same single trigger §6.2's own new regression test uses -- then waits for PollersBreaker's own :closed-state restart via breaker_state()==:closed plus a pid-appears poll, clears the fault, and asserts Repo/Infrastructure untouched, a new distinct Pollers pid, AND a new confirming assertion that breaker_state() stayed :closed (proving the single-shot fault never tripped the breaker) -- one assertion added beyond the original AC4 test, not a weakening). Explicitly confirmed and stated in the design doc: this fix requires NO addition to lib/letflow/supervisor/pollers_breaker.ex or lib/letflow/application.ex -- both remain exactly as ELIXIR-DEV already implemented them; the gap was entirely in the existing test's adaptation, not the production mechanism. No implementation code (.ex function bodies) added -- the revised §5 specifies the test's procedure as a numbered list of calls/assertions/observables (signatures and OTP call names only), same convention as every other section of this design doc; the actual test-file edit (pollers_test.exs) is left for TEST-DESIGNER/ELIXIR-DEV's next step per this handoff's own scope.",
    "artifacts_out": [
      "lib/letflow/design/iss0451-poller-crash-budget-isolation.md"
    ],
    "issues": [],
    "next_action": "Route to ELIXIR-DEV (or TEST-DESIGNER, per ORCH's judgement of WF-03's normal step ordering) to apply the revised design doc §5 procedure to test/letflow/supervisor/pollers_test.exs's AC4 test body (remove the test-issued Supervisor.start_child/2 call and the assert_receive :DOWN step; replace with a breaker_state()/pid-appears poll per §5's numbered steps 3-8), then re-run test/letflow/supervisor/pollers_test.exs, infrastructure_test.exs, and http_test.exs to confirm all pass deterministically (multiple --seed-varied runs, given the race this rework closes was itself seed-sensitive) before proceeding to SECURITY-REVIEWER/REVIEWER."
  },
  "gate_history": [
    {
      "iteration": 0,
      "gated_at": "2026-09-04T02:56:22Z",
      "status": "FAIL",
      "summary": "Prior attempt's design chose a PollersBreaker circuit-breaker (2-consecutive-:DOWN trip) plus a top-level max_restarts 3->5 raise, with margin arithmetic '5 - 2 = 3' assuming the breaker's terminate_child call always beats the top-level Supervisor's own automatic restart-on-exit for the same child exit. CODE-DESIGN-VALIDATOR independently re-derived this and found the assumption unproven -- see handoffs/WF03-ISS0451-20260904/step-02b-code-design-validator.json result.issues[0] for the full BLOCKER text.",
      "issues": ["Unproven race: PollersBreaker's monitor-message-then-terminate_child chain is strictly longer than the top-level Supervisor's direct in-process EXIT reaction, with no cited ordering guarantee it wins; section 3.6 was cited as resolving this but analyzes a different race instead."],
      "next_action": "Rework CODE-DESIGNER: either make Pollers' child spec restart: :temporary so the top-level Supervisor cannot auto-restart it at all (breaker becomes solely responsible via explicit restart_child), or cite an actual OTP ordering guarantee and re-derive the margin arithmetic under the real worst-case restart count."
    },
    {
      "iteration": 1,
      "gated_at": "2026-09-04T03:20:00Z",
      "status": "FAIL",
      "summary": "Iteration-1 revision chose restart: :temporary on Pollers' child spec (correct concept, confirmed by the validator against Elixir's own Supervisor source) but expressed it via children-list syntax {Letflow.Supervisor.Pollers, restart: :temporary}, which does not actually apply the override -- Supervisor's 2-tuple {module, arg} children-list form threads arg into the start_link argument, not into child_spec override keys. Verified live (both statically against lib/elixir/lib/supervisor.ex and via a running probe) that this syntax silently keeps Pollers :permanent, so the original iteration-0 race is unresolved, and 3 further sections (3.6/3.7/4's arithmetic, 5's AC4-preservation claim, 6.2 step 6's test observable) all inherit the same defect since they depend on the broken snippet's claimed effect.",
      "issues": ["4 BLOCKERs, full text in handoffs/WF03-ISS0451-20260904/step-02b-code-design-validator.json's own result.issues -- root cause is one syntax defect in section 3.4a, propagating downstream."],
      "next_action": "Rework CODE-DESIGNER: fix section 3.4a to use Supervisor.child_spec(Letflow.Supervisor.Pollers, restart: :temporary) as the children-list entry itself (confirmed live by the validator to correctly apply :temporary), then re-derive/restate every downstream section that depended on the broken syntax's claimed effect."
    },
    {
      "iteration": 2,
      "gated_at": "2026-09-04T03:20:33Z",
      "status": "PASS (design gate) -- then FAIL at implementation (Step 3, not this gate)",
      "summary": "Iteration-2 revision passed CODE-DESIGN-VALIDATOR's gate clean (handoffs/WF03-ISS0451-20260904/step-02b-code-design-validator.json, gated_at 2026-09-04T03:20:33Z) after the children-list syntax fix plus a self-discovered second fix (start_child/2 instead of restart_child/2 for a :temporary child). The design itself was not faulted again. But ELIXIR-DEV's Step 3 implementation of section 5's guidance for adapting the EXISTING pollers_test.exs AC4 test surfaced a genuine race the design never anticipated -- full detail in handoffs/WF03-ISS0451-20260904/step-03-elixir-dev.json's result.issues[0], summarized in this handoff's own current task.rework_note.",
      "issues": ["A previously-unflagged race between PollersBreaker and the test process, both reacting to the same terminate_child-induced :DOWN and racing to restart Pollers -- reproduced deterministically at --seed 0 and flaky otherwise."],
      "next_action": "Rework CODE-DESIGNER (iteration 3, FINAL): resolve the AC4-test race via one of ELIXIR-DEV's three named directions (test-only synchronization hook on PollersBreaker; redefine the AC4 test as a single breaker-mediated flow; or another concrete mechanism), specified unambiguously enough to implement without further invention."
    }
  ],
  "rework_count": 3,
  "max_rework": 3
}
