# Ported from R-Co tests/simulation/companies/swiftroute/company.yaml (ISS-0388).
# Values reflect the real R-Co fixture; format is Letflow's simplified seed schema
# (slug/display_name/hostname) rather than the full R-Co YAML, which carries
# fields Letflow.Simulation.Seed does not consume.
slug: swiftroute
display_name: SwiftRoute Ltd
hostname: swiftroute.simulation.test
