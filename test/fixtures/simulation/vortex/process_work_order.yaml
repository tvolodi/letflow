# SYNTHETIC FIXTURE -- NOT ported from R-Co. See
# test/fixtures/simulation/swiftroute/process_shipment_dispatch.yaml for the full
# explanation.
name: vortex-work-order
version: "1.0.0"
description: Synthetic START -> HUMAN_TASK -> END process for scenario harness smoke coverage.
graph:
  nodes:
    - id: start
      node_type: START
    - id: plan_task
      node_type: HUMAN_TASK
      attributes:
        role: planner
    - id: end
      node_type: END
  edges:
    - id: e1
      source: start
      target: plan_task
    - id: e2
      source: plan_task
      target: end
