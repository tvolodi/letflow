# Test run report — WF-02 Step 4

req_id: REQ-029
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ029-20260817
date_utc: 2026-08-17T10:50:00Z

git:
  branch: feature/WF02-REQ029-20260817
  branch_confirmed: true
  commit: d07aff6a8ba2a48def215ea17b2cc68ae428c99d
  commit_subject: "chore(WF02-REQ029): Step 3b test-design-validator -- PASS"
  note: >
    Confirmed via `git branch --show-current` at start of this run. Prior gates
    (CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER, TEST-DESIGN-VALIDATOR)
    already passed on this branch per the dispatching handoff
    (handoffs/WF02-REQ029-20260817/step-04-test-runner.json). New test surface for
    this run per `git diff main...HEAD --stat -- test/`:
    test/letflow/definitions/graph_test.exs (437 lines) and test/specs/REQ-029.md
    (166 lines, spec doc, not executable).

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Mix 1.18.3 / Elixir 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: letflow-postgres-1 (confirmed Up/healthy via `docker compose ps`, port 5462 -> 5432)
  command_run: >
    `mix test` (full suite, default/unpinned random seed) and `mix test --seed 0`
    (full suite, second independent run) — the `test` alias runs ecto.create +
    ecto.migrate --quiet first automatically. Plus one targeted isolated run of
    REQ-029's own new test file, test/letflow/definitions/graph_test.exs.

verdict: PASS

checks:
  test_suite_full_run1:
    result: PASS
    exit_code: 0
    seed: 224070 (unpinned/random — ExUnit's own default)
    summary_line_verbatim: "4 properties, 401 tests, 0 failures"
    finished_line_verbatim: "Finished in 10.7 seconds (1.1s async, 9.6s sync)"
    failures: 0
    log_saved_to: /tmp/req029_run1.log (5020 lines, grepped for failure markers)
    grep_check: >
      Regex covering "Failures:", "Failure:", numbered failure blocks
      ("  1) ..."), "Excluded:", "Skipped:", "Doctests:", "** (" — zero matches
      beyond the "0 failures" summary line itself.

  test_suite_full_run2:
    result: PASS
    exit_code: 0
    seed: 0 (pinned, second independent run per this project's own established
      double-run practice given its flaky-test history)
    summary_line_verbatim: "4 properties, 401 tests, 0 failures"
    finished_line_verbatim: "Finished in 11.2 seconds (1.0s async, 10.1s sync)"
    failures: 0
    log_saved_to: /tmp/req029_run2.log (4989 lines)
    grep_check: "Same regex as run1 — zero matches beyond known noise."

  req029_own_test_file_isolated:
    file: test/letflow/definitions/graph_test.exs
    command_run: "mix test test/letflow/definitions/graph_test.exs"
    summary_line_verbatim: "40 tests, 0 failures"
    exit_code: 0
    seed: 630291 (unpinned)
    note: >
      New test file covering node-attribute and edge-condition validators added
      on this branch (REQ-029). No StreamData property in this file — the
      requirement's own acceptance criteria are example-based validator checks
      per test/specs/REQ-029.md, not additional legal/illegal state-machine
      transitions, so extending the existing "no sequence of actions produces an
      invalid state" property test did not apply here.

  known_noise_not_a_failure:
    - >
      [error] Metadata load failed for issuer
      https://placeholder-keycloak.invalid/realms/bpm-default. Retrying in NNNN ms.
      — REQ-016's Ueberauth OIDC worker attempting to reach a deliberately
      unreachable placeholder issuer configured in config/test.exs. Pre-existing
      since REQ-016, already documented as expected noise in every prior WF-02
      test report. Does not fail, skip, or affect any test. Appears in both
      full-suite run logs and the isolated run's log.
    - >
      Verbose SQL query debug logging and Elixir compiler deprecation warnings
      (e.g. `ExUnit.Case.register_test/4 is deprecated`) appear in raw mix test
      output — expected, pre-existing, not indicative of any problem introduced
      by this branch.

pre_existing_failures_found: none
issues_filed: none
issues_referenced_not_refiled: none

test_file_breakdown:
  new_req_029:
    items:
      - file: test/letflow/definitions/graph_test.exs
        detail: >
          Example-based coverage of node-attribute and edge-condition validators
          in lib/letflow/definitions/graph.ex, per test/specs/REQ-029.md's
          acceptance criteria (CHK-09..17 per the REVIEWER handoff on commit
          0470854).
    subtotal: 40
  full_suite_grand_total: 401
  cross_check: >
    401 total (4 properties, 397 example tests) reproduced identically across
    both full-suite runs (unpinned seed 224070, and pinned seed 0). This is up
    from the 363-test baseline recorded in the prior REQ-041 report
    (test/reports/report-2026-08-17-WF02-REQ041-20260817.yaml, itself written
    mid-run before REQ-041 was merged). Verified via
    `git merge-base --is-ancestor <sha> HEAD` that commits 3443c96
    (REQ-035 promotion_reviews schema, PR #95), 85ca904 (REQ-039 sandbox
    pool/fixture loader, PR #97), and 286fab6 (REQ-041 pack-update three-way
    diff, PR #96) are all ancestors of this branch's HEAD — i.e. this branch was
    cut after all three merged to main, which accounts for the delta between
    363 and the 361 pre-existing tests this run's own 40 new graph_test.exs
    cases build on top of.

diagnosis: "N/A — no failures observed in this run. Nothing routed back to ELIXIR-DEV or TEST-DESIGNER."

regression_check_vs_baseline: >
  Both full-suite runs (unpinned seed 224070, pinned seed 0) on commit
  d07aff6a8ba2a48def215ea17b2cc68ae428c99d produced an identical
  "4 properties, 401 tests, 0 failures" result, with output captured to log
  files and grepped for failure markers rather than eyeballed. The isolated
  re-run of this branch's own new file (graph_test.exs, 40 tests) also passed
  cleanly. No pre-existing or newly-introduced failures found; nothing to file
  via ISSUE_QUEUE.md.

handoff:
  verdict: PASS
  artifacts_out:
    - test/reports/report-2026-08-17-WF02-REQ029-20260817.yaml
  next_action: "Route to RELEASE-VALIDATOR (WF-02 Step 5) — no rework needed."
