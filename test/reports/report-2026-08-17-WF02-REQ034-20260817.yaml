# Test run report — WF-02 Step 4

req_id: REQ-034
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ034-20260817
date_utc: 2026-08-17T21:01:30Z

git:
  branch: feature/WF02-REQ034-20260817
  branch_confirmed: true
  commit: 557350d0d6488196ce7eac890c9a063b0afcebaf
  commit_subject: "chore(WF02-REQ034): log Step 3b PASS, dispatch Step 4"
  note: >
    Confirmed via `git branch --show-current` and `git log -1` at start of this run.
    Prior gates (CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER,
    TEST-DESIGN-VALIDATOR) already passed on this commit per dispatching handoff
    (step-03b-test-design-validator.json).

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Elixir 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: letflow-postgres-1 (already running, shared with primary ~/letflow
    checkout; confirmed healthy via `docker ps` only — `docker compose up`/`up -d` was
    NEVER run in this worktree, per this session's explicit standing instruction and
    the docs/anti-patterns.md entry "Running `docker compose up` from a secondary
    worktree checkout")
  partition: "MIX_TEST_PARTITION=2 (isolates onto letflow_test2, per this worktree's
    shared-Postgres setup — used for every invocation in this run, never bare mix test)"
  command_run: >
    `MIX_TEST_PARTITION=2 mix test` (full suite, default/unpinned random seed) and
    `MIX_TEST_PARTITION=2 mix test --seed 0` (full suite, pinned-seed independent
    run) — the `test` alias runs ecto.create + ecto.migrate --quiet first
    automatically. Plus a targeted isolated re-run of REQ-034's own new test file.

verdict: PASS

checks:
  test_suite_full_run1:
    result: PASS
    exit_code: 0
    seed: unpinned/random — ExUnit's own default
    summary_line_verbatim: "4 properties, 598 tests, 0 failures"
    finished_line_verbatim: "Finished in 26.3 seconds (1.5s async, 24.8s sync)"
    failures: 0

  test_suite_full_run2:
    result: PASS
    exit_code: 0
    seed: 0 (pinned, second independent run per this project's established
      double-run practice given its flaky-test history)
    summary_line_verbatim: "4 properties, 598 tests, 0 failures"
    finished_line_verbatim: "Finished in 26.0 seconds (1.4s async, 24.5s sync)"
    failures: 0

  req034_own_test_file_isolated:
    files:
      - test/letflow/definitions/export_import_test.exs
    command_run: >
      MIX_TEST_PARTITION=2 mix test test/letflow/definitions/export_import_test.exs --seed 0
    summary_line_verbatim: "5 tests, 0 failures"
    exit_code: 0

  docker_compose_not_run:
    result: PASS
    note: >
      `docker ps` was used to confirm the pre-existing container
      (letflow-postgres-1, from the primary ~/letflow checkout) was already Up
      and port-mapped (0.0.0.0:5462->5432/tcp) before any test invocation.
      `docker compose up`/`up -d` was never invoked in this worktree during
      this step.

  known_noise_not_a_failure:
    - >
      Verbose SQL query debug logging appears in raw mix test output
      (config/test.exs's default test log level) — expected, not indicative of
      any problem.
    - >
      `[error] Metadata load failed for issuer
      https://placeholder-keycloak.invalid/realms/bpm-default. Retrying in NNNN
      ms. Error Details: {:configuration_load_failed, {:failed_connect, ...,
      :nxdomain}}` appears twice (once per full run) — expected: config/test.exs
      deliberately points :oidc at a non-resolving RFC 2606 .invalid placeholder
      host (see e.g. test/reports/report-2026-08-16-REQ-016.yaml criterion_4).
      The worker retries in the background; it does not crash the app or fail
      any test.
    - >
      `ExUnit.Case.register_test/4 is deprecated. Use register_test/6 instead`
      compiler warning appears several times — compiler-level warning, not
      test-runner-caused, not a failure.
    - >
      `redefining module Letflow.Repo.Migrations.CreateEventTypeRegistry
      (current version defined in memory)` warning — expected artifact of
      re-running ecto.migrate across repeated `mix test` invocations in the
      same BEAM session, not a failure.
    - >
      `default values for the optional arguments in start_pool!/1 are never
      used` and `... in valid_review_attrs/1 are never used` — pre-existing
      compiler warnings unrelated to REQ-034, do not affect test outcome.

pre_existing_failures_found: none
issues_filed: none

baseline_comparison:
  step_00_baseline: "not stated as a numeric count in this run's Step 00 handoff
    (handoffs/WF02-REQ034-20260817/step-00-elixir-dev-gitsetup.json) — it covers
    git setup only, no test-count baseline recorded."
  req_034_new_tests_expected: 5
  observed_total_run1: "598 tests, 4 properties"
  observed_total_run2: "598 tests, 4 properties"
  match: true
  origin_main_check: >
    Ran `git fetch origin main` — origin/main HEAD is 03dfb1652e682b1f4ba8a1ed78addd0b50e185b0
    ("chore(WF02-REQ037): log release + done"), the same commit this branch was
    forked from (per Step 00's handoff) plus this run's own REQ-034 commits on
    top. No divergent concurrent main-branch merges to reconcile against.

test_file_breakdown:
  new_req_034:
    items:
      - file: test/letflow/definitions/export_import_test.exs
    subtotal: 5
  full_suite_grand_total: 598
  cross_check: >
    598 total (4 properties, 594 example tests) reproduced identically across
    two full-suite invocations (unpinned default seed, and pinned seed 0), and
    the new REQ-034 file reproduces 5/5 in isolation with no dependency on
    full-suite ordering.

diagnosis: "N/A — no failures observed in this run. Nothing routed back to
  ELIXIR-DEV or TEST-DESIGNER."

regression_check_vs_baseline: >
  No numeric baseline was recorded in this run's own Step 00 handoff, so this
  report establishes 598 tests, 4 properties, 0 failures (including REQ-034's
  5 new tests) as the observed state at this commit, reproduced identically
  across two independent full-suite runs (unpinned default seed, pinned seed
  0) plus an isolated run of the new test file alone. Zero failures observed
  anywhere. docker compose up was NOT run in this worktree at any point during
  this step.

handoff:
  verdict: PASS
  artifacts_out:
    - test/reports/report-2026-08-17-WF02-REQ034-20260817.yaml
  next_action: "Route to RELEASE-VALIDATOR (WF-02 Step 5) — no rework needed."
