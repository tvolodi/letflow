# Test run report — WF-02 Step 4

req_id: REQ-035
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ035-20260817
date_utc: 2026-08-17T08:17:30Z

git:
  branch: feature/WF02-REQ035-20260817
  branch_confirmed: true
  commit: f022015
  commit_subject: "test(REQ-035): cover promotion_reviews schema acceptance criteria"
  note: >
    Confirmed via `git branch --show-current` and `git log -1` at start of this run.
    Prior gates (CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER,
    TEST-DESIGN-VALIDATOR) already passed on this commit per dispatching handoff.

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Mix 1.18.3 / Elixir 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: letflow-postgres-1 (confirmed Up/healthy via `docker compose up -d`, port 5462 -> 5432)
  command_run: >
    `mix test` (full suite, default/unpinned random seed) and `mix test --seed 0`
    (full suite, second independent run) — the `test` alias runs ecto.create +
    ecto.migrate --quiet first automatically. Plus two targeted isolated re-runs:
    the historically flaky test/letflow/plugs/tenant_status_test.exs alone, and
    REQ-035's own two new test files alone.

verdict: PASS

checks:
  test_suite_full_run1:
    result: PASS
    exit_code: 0
    seed: 953222 (unpinned/random — ExUnit's own default)
    summary_line_verbatim: "4 properties, 333 tests, 0 failures"
    finished_line_verbatim: "Finished in 9.5 seconds (0.9s async, 8.5s sync)"
    failures: 0
    log_saved_to: /tmp/req035_run1.log (4674 lines, grepped for failure markers)
    grep_check: >
      Regex covering "Failures:", "Failure:", "failure:", "** (", "Excluded:",
      "Skipped:", "Doctests:" — zero matches in the full log.

  test_suite_full_run2:
    result: PASS
    exit_code: 0
    seed: 0 (pinned, second independent run per this project's own established
      double-run practice given its flaky-test history)
    summary_line_verbatim: "4 properties, 333 tests, 0 failures"
    finished_line_verbatim: "Finished in 9.5 seconds (0.8s async, 8.7s sync)"
    failures: 0
    log_saved_to: /tmp/req035_run2.log
    grep_check: "Same regex as run1 — zero matches."

  compile_warnings_as_errors:
    command_run: "mix compile --warnings-as-errors"
    result: PASS
    exit_code: 0
    note: "Clean, zero output, zero warnings promoted to errors."

  historically_flaky_file_isolated_rerun:
    file: test/letflow/plugs/tenant_status_test.exs
    command_run: "mix test test/letflow/plugs/tenant_status_test.exs"
    summary_line_verbatim: "10 tests, 0 failures"
    exit_code: 0
    note: >
      Explicitly checked against ISS-0011 and ISS-0031 (both status: resolved in
      docs/issues/) before attributing anything to them. Both issues concern this
      exact file's telemetry-race under async: true full-suite concurrency, fixed
      on main via commit 40d6bf5 ("fix(test): tenant_status_test's query-telemetry
      handler filters by process (ISS-0031, GH#90)"). Independently confirmed
      40d6bf5 is an ancestor of this branch's HEAD (f022015) via
      `git merge-base --is-ancestor 40d6bf5 HEAD`. Did not resurface in either
      full-suite run (run1 used an unpinned/random seed, 953222, specifically the
      condition class under which this flake previously appeared) nor in this
      isolated re-run.

  req035_own_test_files_isolated:
    files:
      - test/letflow/definitions/promotion_review_migration_test.exs
      - test/letflow/definitions/promotion_review_test.exs
    command_run: >
      mix test test/letflow/definitions/promotion_review_migration_test.exs
      test/letflow/definitions/promotion_review_test.exs --seed 0
    summary_line_verbatim: "27 tests, 0 failures"
    exit_code: 0

  postgres_cleanup_independently_reverified:
    method: >
      Queried letflow-postgres-1 directly via `docker exec ... psql -U letflow
      -d letflow_test` after both full-suite runs completed.
    results:
      - query: "SELECT nspname FROM pg_catalog.pg_namespace WHERE nspname LIKE 'tenant_%';"
        result: "0 rows"
      - query: "SELECT count(*) FROM public.tenant_schemas;"
        result: 0
      - query: "SELECT count(*) FROM public.tenants;"
        result: 0
    conclusion: "No orphan tenant schemas or rows persist after the run."

  known_noise_not_a_failure:
    - >
      [error] Metadata load failed for issuer
      https://placeholder-keycloak.invalid/realms/bpm-default. Retrying in NNNN ms.
      — REQ-016's Ueberauth OIDC worker attempting to reach a deliberately
      unreachable placeholder issuer configured in config/test.exs. Pre-existing
      since REQ-016, already documented as expected noise in every prior WF-02
      test report. Does not fail, skip, or affect any test.
    - >
      Verbose SQL query debug logging appears in raw mix test output
      (config/test.exs's default test log level) — expected, not indicative of
      any problem.

pre_existing_failures_found: none
issues_filed: none
issues_referenced_not_refiled:
  - id: ISS-0011
    status: resolved
  - id: ISS-0031
    status: resolved
  note: >
    Both are the same underlying flake class (tenant_status_test.exs telemetry
    race), fixed on main via commit 40d6bf5, confirmed an ancestor of this
    branch's HEAD. Watched for specifically (unpinned-seed full run plus an
    isolated re-run of the exact file) and did not resurface. Not re-filed —
    nothing to file.

test_file_breakdown:
  new_req_035:
    items:
      - file: test/letflow/definitions/promotion_review_migration_test.exs
        detail: >
          Migration-level coverage: applies cleanly per tenant schema, Ecto.Enum
          status column over the 6 named values, partial unique index on
          (tenant_id, plan_digest) WHERE status IN ('pending_review','approved')
          rejecting a second concurrent pending/approved row for the same digest.
      - file: test/letflow/definitions/promotion_review_test.exs
        detail: >
          Schema-module-level coverage: row_version default of 1, moduledoc's
          documented open questions (def_type text-vs-CHECK-constraint,
          PER_TENANT-vs-GLOBAL classification).
    subtotal: 27
  full_suite_grand_total: 333
  cross_check: >
    333 total (4 properties, 329 example tests) reproduced identically across
    both full-suite runs (unpinned seed 953222, and pinned seed 0), matching
    this session's independently-confirmed pre-existing baseline of 333 tests /
    0 failures at commit f022015 (per TEST-DESIGN-VALIDATOR's own reproduction,
    stated in the dispatching handoff). This Step 4 run is the independent,
    formal re-verification of that number — run separately, twice, output saved
    to log files and programmatically grepped for failure markers rather than
    eyeballed, plus a live Postgres cleanup re-check and two additional targeted
    isolated re-runs (the historically-flaky file, and REQ-035's own new files).

acceptance_criteria_test_coverage_observed:
  criterion_1_migration_applies_cleanly_enum_6_values: "PASS — covered by promotion_review_migration_test.exs, part of the 27/27 isolated pass and the 333/333 full-suite pass."
  criterion_2_partial_unique_index_rejects_second_pending_or_approved: "PASS — covered by promotion_review_migration_test.exs."
  criterion_3_row_version_defaults_1_documented_optimistic_lock: "PASS — covered by promotion_review_test.exs."
  criterion_4_moduledoc_flags_def_type_and_scope_open_questions: "PASS — covered by promotion_review_test.exs (per TEST-DESIGN-VALIDATOR's prior sign-off, re-confirmed passing in this run, not re-derived from scratch)."

diagnosis: "N/A — no failures observed in this run. Nothing routed back to ELIXIR-DEV or TEST-DESIGNER."

regression_check_vs_baseline: >
  This session's own independently-confirmed baseline (TEST-DESIGN-VALIDATOR's
  reproduction) was 333 tests, 0 failures at commit f022015. This run reproduces
  that exact count identically, twice (unpinned default seed 953222 and pinned
  seed 0), on the same HEAD commit, with output captured to log files and
  programmatically grepped for failure markers rather than eyeballed, plus a live
  re-query of Postgres to independently reconfirm the no-orphan-state cleanup
  claim, plus two additional targeted isolated re-runs. Zero regressions
  confirmed. ISS-0011/ISS-0031's known flaky condition was specifically watched
  for (unpinned-seed run, plus isolated re-run of the exact file) and did not
  resurface.

handoff:
  verdict: PASS
  artifacts_out:
    - test/reports/report-2026-08-17-WF02-REQ035-20260817.yaml
  next_action: "Route to RELEASE-VALIDATOR (WF-02 Step 5) — no rework needed."
