# Test run report — WF-02 Step 4

req_id: REQ-036
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ036-20260817
date_utc: 2026-08-17T18:46:10Z

git:
  branch: feature/WF02-REQ036-20260817
  branch_confirmed: true
  commit: f148757
  commit_subject: "chore(WF02-REQ036): dispatch Step 4 (TEST-RUNNER)"
  note: >
    Confirmed via `git branch --show-current` and `git log -1` at start of this run.
    Prior gates (CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER,
    TEST-DESIGN-VALIDATOR) already passed on this commit per dispatching handoff.

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Elixir 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: letflow-postgres-1 (already running, shared with sibling worktree
    per this checkout's secondary-worktree setup; `docker compose up -d` in this
    checkout itself failed only on port 5462 already bound by that same shared
    container — confirmed via `docker ps`, not a failure of the dependency itself)
  partition: "MIX_TEST_PARTITION=2 (isolates onto letflow_test2, per this worktree's
    shared-Postgres setup — used for every invocation in this run, never bare mix test)"
  command_run: >
    `MIX_TEST_PARTITION=2 mix test` (full suite, default/unpinned random seed) and
    `MIX_TEST_PARTITION=2 mix test --seed 0` (full suite, second independent run) —
    the `test` alias runs ecto.create + ecto.migrate --quiet first automatically.
    Plus a targeted isolated re-run of REQ-036's own three new test files.

verdict: PASS

checks:
  test_suite_full_run1:
    result: PASS
    exit_code: 0
    seed: unpinned/random — ExUnit's own default
    summary_line_verbatim: "4 properties, 500 tests, 0 failures"
    finished_line_verbatim: "Finished in 17.9 seconds (1.1s async, 16.8s sync)"
    failures: 0

  test_suite_full_run2:
    result: PASS
    exit_code: 0
    seed: 0 (pinned, second independent run per this project's established
      double-run practice given its flaky-test history)
    summary_line_verbatim: "4 properties, 500 tests, 0 failures"
    finished_line_verbatim: "Finished in 16.3 seconds (1.1s async, 15.1s sync)"
    failures: 0

  req036_own_test_files_isolated:
    files:
      - test/letflow/definitions/promotion_plan_test.exs
      - test/letflow/definitions/promotion_conflict_test.exs
      - test/letflow/definitions/promotion_digest_test.exs
    command_run: >
      MIX_TEST_PARTITION=2 mix test test/letflow/definitions/promotion_plan_test.exs
      test/letflow/definitions/promotion_conflict_test.exs
      test/letflow/definitions/promotion_digest_test.exs --seed 0
    summary_line_verbatim: "33 tests, 0 failures"
    exit_code: 0

  postgres_cleanup_independently_reverified:
    method: >
      Queried letflow-postgres-1 directly via `docker exec ... psql -U letflow
      -d letflow_test2` after both full-suite runs completed.
    results:
      - query: "SELECT nspname FROM pg_catalog.pg_namespace WHERE nspname LIKE 'tenant_%';"
        result: "0 rows"
      - query: "SELECT count(*) FROM public.tenant_schemas;"
        result: 0
      - query: "SELECT count(*) FROM public.tenants;"
        result: 0
    conclusion: "No orphan tenant schemas or rows persist after the run."

  known_noise_not_a_failure:
    - >
      Verbose SQL query debug logging appears in raw mix test output
      (config/test.exs's default test log level) — expected, not indicative of
      any problem.
    - >
      `mix test` in the ExUnit.Case.register_test/4 deprecation warning appears
      several times (compiler-level warning, not test-runner-caused, not a failure).

pre_existing_failures_found: none
issues_filed: none

baseline_comparison:
  step_00_baseline: "467 tests, 4 properties, 0 failures"
  req_036_new_tests_expected: 33
  expected_total: 500
  observed_total_run1: "500 tests, 4 properties"
  observed_total_run2: "500 tests, 4 properties"
  match: true
  origin_main_check: >
    Ran `git fetch origin main` — origin/main HEAD is f212e2d
    ("chore(WF02-REQ026): registry entry + orchestrator log for merge/release"),
    unrelated to REQ-036's own branch work. Observed count (500) matches the
    expected baseline+delta exactly, so no reconciliation against concurrent
    main-branch merges was needed.

test_file_breakdown:
  new_req_036:
    items:
      - file: test/letflow/definitions/promotion_plan_test.exs
      - file: test/letflow/definitions/promotion_conflict_test.exs
      - file: test/letflow/definitions/promotion_digest_test.exs
    subtotal: 33
  full_suite_grand_total: 500
  cross_check: >
    500 total (4 properties, 496 example tests) reproduced identically across
    both full-suite runs (unpinned default seed, and pinned seed 0), matching
    Step 00's baseline of 467 tests / 4 properties plus REQ-036's 33 new tests.

diagnosis: "N/A — no failures observed in this run. Nothing routed back to ELIXIR-DEV or TEST-DESIGNER."

regression_check_vs_baseline: >
  Step 00's baseline was 467 tests, 4 properties, 0 failures. This run reproduces
  467 + 33 = 500 tests, 4 properties, 0 failures identically across two independent
  full-suite runs (unpinned default seed and pinned seed 0), on the same HEAD
  commit (f148757), plus a live re-query of Postgres to independently reconfirm
  no orphan tenant state remains. Zero regressions confirmed.

handoff:
  verdict: PASS
  artifacts_out:
    - test/reports/report-2026-08-17-WF02-REQ036-20260817.yaml
  next_action: "Route to RELEASE-VALIDATOR (WF-02 Step 5) — no rework needed."
