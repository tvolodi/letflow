# Test run report — WF-02 Step 4

req_id: REQ-042
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ042-20260817
date_utc: 2026-08-17T21:53:40Z

git:
  branch: feature/WF02-REQ042-20260817
  branch_confirmed: true
  commit: 86aba149ffd7acd46a1e8bfefc83f5094be25bcb
  commit_subject: "chore(WF02-REQ042): log Step 3b PASS, dispatch Step 4"
  note: >
    Confirmed via `git branch --show-current` and `git log -1` at start of this run.
    Prior gates (CODE-DESIGN-VALIDATOR, SECURITY-REVIEWER, REVIEWER,
    TEST-DESIGN-VALIDATOR) already passed on this commit per dispatching handoff
    (step-03b-test-design-validator.json).

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Elixir 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: letflow-postgres-1 (already running, 14h+ uptime, healthy,
    0.0.0.0:5462->5432/tcp — shared with primary ~/letflow checkout; confirmed via
    `docker ps` only. `docker compose up`/`up -d` was NEVER run in this worktree,
    per explicit standing instruction and docs/anti-patterns.md's entry on this
    exact mistake for secondary worktree checkouts.)
  partition: "MIX_TEST_PARTITION=2 (isolates onto letflow_test2, per this worktree's
    shared-Postgres setup — used for every invocation in this run, never bare mix test)"
  command_run: >
    `MIX_TEST_PARTITION=2 mix test` (full suite, default/unpinned random seed) and
    `MIX_TEST_PARTITION=2 mix test --seed 0` (full suite, pinned-seed independent
    run) — the `test` alias runs ecto.create + ecto.migrate --quiet first
    automatically. Plus a targeted isolated re-run of REQ-042's own test files.

verdict: PASS

checks:
  test_suite_full_run1:
    result: PASS
    exit_code: 0
    seed: unpinned/random — ExUnit's own default
    summary_line_verbatim: "4 properties, 608 tests, 0 failures"
    finished_line_verbatim: "Finished in 29.0 seconds (1.6s async, 27.4s sync)"
    failures: 0

  test_suite_full_run2:
    result: PASS
    exit_code: 0
    seed: 0 (pinned, second independent run per this project's established
      double-run practice given its flaky-test history)
    summary_line_verbatim: "4 properties, 608 tests, 0 failures"
    finished_line_verbatim: "Finished in 29.0 seconds (1.5s async, 27.5s sync)"
    failures: 0

  req042_own_test_files_isolated:
    files:
      - test/letflow/definitions/search_test.exs
      - test/letflow/definitions_test.exs
    command_run: >
      MIX_TEST_PARTITION=2 mix test test/letflow/definitions/search_test.exs
      test/letflow/definitions_test.exs --seed 0
    summary_line_verbatim: "23 tests, 0 failures"
    exit_code: 0
    note: >
      Matches TEST-DESIGN-VALIDATOR's own count in step-03b-test-design-validator.json
      (23 tests, 0 failures), reproduced independently here.

  docker_compose_not_run:
    result: PASS
    note: >
      `docker ps` was used to confirm the pre-existing container
      (letflow-postgres-1, from the primary ~/letflow checkout) was already Up
      (14h+, healthy) and port-mapped (0.0.0.0:5462->5432/tcp) before any test
      invocation. `docker compose up`/`up -d` was never invoked in this worktree
      during this step.

  known_noise_not_a_failure:
    - >
      Verbose SQL query debug logging (including many `[debug] QUERY ERROR ...`
      lines from tests that deliberately exercise unique-constraint /
      validation-error paths) appears in raw mix test output (config/test.exs's
      default test log level) — expected, not indicative of any problem; overall
      run reports 0 failures.
    - >
      `[error] Metadata load failed for issuer
      https://placeholder-keycloak.invalid/realms/bpm-default. Retrying in NNNN
      ms. Error Details: {:configuration_load_failed, {:failed_connect, ...,
      :nxdomain}}` appears twice (once per full run) — expected: config/test.exs
      deliberately points :oidc at a non-resolving RFC 2606 .invalid placeholder
      host (see e.g. test/reports/report-2026-08-16-REQ-016.yaml criterion_4).
      The worker retries in the background; it does not crash the app or fail
      any test.
    - >
      `redefining module Letflow.Repo.Migrations....` warnings — expected
      artifact of re-running ecto.migrate across repeated `mix test` invocations
      in the same BEAM session, not a failure.

pre_existing_failures_found: none
issues_filed: none

baseline_comparison:
  step_00_baseline: "not stated as a numeric count in this run's Step 00 handoff
    (handoffs/WF02-REQ042-20260817/step-00-elixir-dev-gitsetup.json) — it covers
    git setup only, no test-count baseline recorded."
  req_042_new_tests_expected: 23 (per TEST-DESIGN-VALIDATOR's step-03b PASS: 20 in
    search_test.exs's AC1-AC5 coverage + 3 new AC6 doc-content tests in
    definitions_test.exs)
  observed_total_run1: "608 tests, 4 properties"
  observed_total_run2: "608 tests, 4 properties"
  match: true
  origin_main_check: >
    Ran `git fetch origin main` — origin/main HEAD is
    1db9915a75c7b11e8f55e9da194167ea480e4c98
    ("chore(WF02-REQ031): close out registry + orchestrator log for merged run"),
    which has advanced past this branch's fork point
    (db3f5642b447d58d99b0f43fc85356923da9eadd, confirmed via `git merge-base HEAD
    origin/main`) due to other concurrent WF-02 runs merging to main on other
    hosts. This is expected multi-host pipeline behavior, not a rebase requirement
    for this step — ORCH/merge handles reconciliation at merge time; this step
    tests the feature branch as-is per its own handoff scope.

test_file_breakdown:
  new_req_042:
    items:
      - file: test/letflow/definitions/search_test.exs
      - file: test/letflow/definitions_test.exs (3 new AC6 doc-content tests added
          to the pre-existing file)
    subtotal: 23
  full_suite_grand_total: 608
  cross_check: >
    608 total (4 properties, 604 example tests) reproduced identically across two
    full-suite invocations (unpinned default seed, and pinned seed 0), and the
    new REQ-042 test files reproduce 23/23 in isolation with no dependency on
    full-suite ordering.

diagnosis: "N/A — no failures observed in this run. Nothing routed back to
  ELIXIR-DEV or TEST-DESIGNER."

regression_check_vs_baseline: >
  No numeric baseline was recorded in this run's own Step 00 handoff, so this
  report establishes 608 tests, 4 properties, 0 failures (including REQ-042's
  23 new/changed tests) as the observed state at this commit, reproduced
  identically across two independent full-suite runs (unpinned default seed,
  pinned seed 0) plus an isolated run of the new/changed test files alone. Zero
  failures observed anywhere. docker compose up was NOT run in this worktree at
  any point during this step.

handoff:
  verdict: PASS
  artifacts_out:
    - test/reports/report-2026-08-17-WF02-REQ042-20260817.yaml
  next_action: "Route to RELEASE-VALIDATOR (WF-02 Step 5) — no rework needed."
