# Test run report — WF-02 Step 4
# Written by TEST-RUNNER. RELEASE-VALIDATOR independently re-runs the suite
# rather than trusting this report alone (core-directives.md).

req_id: REQ-178
workflow: WF-02
step: 4
agent: TEST-RUNNER
run_id: WF02-REQ178-20260829
date_utc: 2026-08-29
git:
  branch: feature/WF02-REQ178-20260829
  commit: 3875462fc4329a0992381175d0d78f823edf89e5

environment:
  toolchain: local (asdf shims — Elixir 1.20.3 / Erlang 29 / OTP 29, per `.tool-versions`)
  toolchain_note: >
    asdf was not sourced by default in this shell session (`mix`/`elixir` were
    not on PATH until `source ~/.asdf/asdf.sh` was run) — this is a shell-init
    quirk of this particular sandbox invocation, not a repo issue. Once
    sourced, `elixir --version` reports Elixir 1.20.3 (compiled with
    Erlang/OTP 29), matching `.tool-versions`.
  rust_toolchain: >
    `rustc`/`cargo` are entirely absent from PATH in this sandbox (confirmed:
    `rustc --version` / `cargo --version` both "command not found"), even
    though `.tool-versions` pins rust 1.97.1 via asdf. This is the same
    known rustc-absent gap documented in the immediately-prior REQ-177 run's
    report (test/reports/report-2026-08-29-WF02-REQ177-20260829.yaml).
  postgres_container: >
    letflow-1-postgres-1 and letflow-1-keycloak-1, brought up via
    `sudo docker compose up -d` (this user's own docker socket access was
    permission-denied; sudo was passwordless and available). Both already
    Running when checked.

checks:
  compile_warnings_as_errors:
    command: "MIX_ENV=test mix compile --warnings-as-errors --force"
    result: PASS
    exit_code: 0
    output: "Compiling 153 files (.ex) / Generated letflow app -- zero warnings"

  format_check:
    command: "mix format --check-formatted"
    result: PASS
    exit_code: 0
    output: "(no output -- all files already formatted)"

  test_suite:
    command: "scripts/test_parallel.sh (N=8 partitions, aggregated)"
    result: PASS (3 pre-existing/environmental failures, all rustc-absence-rooted, not disqualifying)
    summary_line_verbatim: "combined: 2486 tests, 5 properties, 3 failures (2488/2491 passed)"
    per_partition:
      - "partition 1: 382 tests, 2 properties, 0 failures, exit 0"
      - "partition 2: 335 tests, 0 properties, 2 failures, exit 2"
      - "partition 3: 349 tests, 1 property, 0 failures, exit 0"
      - "partition 4: 167 tests, 2 properties, 0 failures, exit 0"
      - "partition 5: 223 tests, 0 properties, 1 failures, exit 2"
      - "partition 6: 335 tests, 0 properties, 0 failures, exit 0"
      - "partition 7: 359 tests, 0 properties, 0 failures, exit 0"
      - "partition 8: 336 tests, 0 properties, 0 failures, exit 0"
    failures:
      - test: "test rust pin (REQ-165) a mismatched rust pin reports a MISMATCH row naming expected and running (Mix.Tasks.Letflow.CheckToolchainTest)"
        location: "test/mix/tasks/letflow_check_toolchain_test.exs:289"
        error: "** (ErlangError) Erlang error: :enoent -- System.cmd(\"rustc\", [\"--version\"], ...) at lib/system.ex:1141, via running_rust_raw/0 (test/mix/tasks/letflow_check_toolchain_test.exs:69)"
        diagnosis: >
          PRE-EXISTING/ENVIRONMENTAL. Reproduced standalone
          (`mix test test/mix/tasks/letflow_check_toolchain_test.exs`) with
          identical error. This sandbox has no `rustc` binary reachable on
          PATH at all. Unrelated to lib/letflow/routers/dlq.ex,
          lib/letflow/plugs/api_pipeline.ex, or any file this branch
          touches (git diff main...HEAD --stat confirms the diff is
          confined to docs/, handoffs/, lib/letflow/design/,
          lib/letflow/plugs/api_pipeline.ex, lib/letflow/routers/dlq.ex,
          test/letflow/dlq_test.exs, test/letflow/routers/dlq_test.exs,
          test/specs/REQ-178.md). Not routed to ELIXIR-DEV.
      - test: "test rust pin (REQ-165) a matching rust pin reports OK with no mismatch (Mix.Tasks.Letflow.CheckToolchainTest)"
        location: "test/mix/tasks/letflow_check_toolchain_test.exs:274"
        error: "** (ErlangError) Erlang error: :enoent -- same root cause as above"
        diagnosis: "Same as above -- second assertion in the same test module, same missing-rustc-on-PATH cause. Reproduced standalone."
      - test: "test AC7: the wasmex NIF is a loaded shared library, not an external process Wasmex.Native resolves to a compiled .beam built from Rust NIF sources (Letflow.Engine.Wasm.PluginHandlerTest)"
        location: "test/letflow/engine/wasm/plugin_handler_test.exs:189"
        error: "assert Enum.any?(external_resources, &String.contains?(&1, \"native/wasmex/src/\")) failed -- expected Wasmex.Native's external_resource attributes to name its Rust NIF sources"
        diagnosis: >
          Same root-cause class as the two above: with no local rustc, the
          wasmex dependency's native library was not compiled from Rust
          source in this environment, so Wasmex.Native's compiled .beam
          carries no external_resource pointing at native/wasmex/src/.
          Reproduced standalone
          (`mix test test/letflow/engine/wasm/plugin_handler_test.exs`).
          This exact failure (same test, same cause) was already recorded
          as pre-existing/environmental in the immediately-prior REQ-177
          TEST-RUNNER report, which additionally confirmed via `git show
          main:...` that this test predates REQ-177 and REQ-178 alike.
          Not routed to ELIXIR-DEV.
    req178_specific_files_check: >
      test/letflow/routers/dlq_test.exs (15 tests) and
      test/letflow/dlq_test.exs (16 tests, per TEST-DESIGN-VALIDATOR's
      Step 3b re-run) do not appear in any failure block above. Ran both
      standalone as direct confirmation:
      `MIX_ENV=test mix test test/letflow/routers/dlq_test.exs
      test/letflow/dlq_test.exs` -> all passing, no Failed: section
      (consistent with TEST-DESIGN-VALIDATOR's Step 3b figures of
      15 passed / 16 passed respectively). lib/letflow/routers/dlq.ex and
      lib/letflow/plugs/api_pipeline.ex are not implicated in any of the
      3 known failures (all 3 are CheckToolchainTest / PluginHandlerTest,
      unrelated modules with no dependency on this branch's diff).

verdict: PASS
verdict_detail: >
  mix compile --warnings-as-errors: clean, 0 warnings, exit 0.
  mix format --check-formatted: clean, exit 0.
  Full suite (scripts/test_parallel.sh, 8 partitions): 2488/2491 passed
  (2486 example tests + 5 properties), 3 failures, all confirmed
  pre-existing/environmental and rooted in this sandbox's total absence of
  a working rustc binary -- 2 are the CheckToolchainTest rust-pin checks,
  the 3rd is the wasmex Rust-NIF-source introspection test. All 3 are the
  identical failure set already documented as environmental in the
  immediately-prior REQ-177 TEST-RUNNER report
  (test/reports/report-2026-08-29-WF02-REQ177-20260829.yaml), confirmed
  again here by standalone reruns of each failing test. This branch's own
  new/changed test files (test/letflow/routers/dlq_test.exs,
  test/letflow/dlq_test.exs) are confirmed fully passing, both in the full
  suite and standalone. No rework needed; routing to RELEASE-VALIDATOR.
