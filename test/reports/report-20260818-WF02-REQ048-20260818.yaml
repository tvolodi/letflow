# Test run report — WF-02 Step 4 (test run) for REQ-048

req_id: REQ-048
workflow: WF-02
step: "04"
agent: TEST-RUNNER
run_id: WF02-REQ048-20260818
date_utc: 2026-08-18T23:22:10Z

git:
  branch: feature/WF02-REQ048-20260818
  branch_confirmed: true
  commit: 0005eb324668582c41034fdeba263ab44feb8b74
  commit_subject: "test(REQ-048): add complete_task/3 test suite, spec, and file ISS-0057"
  worktree: true
  note: >
    This checkout is a secondary git worktree (~/letflow-wt2), sharing its
    Postgres container with the primary ~/letflow checkout. Per this
    worktree's standing session instructions, `docker compose up` was NOT
    run here (Postgres was already running, container letflow-postgres-1,
    confirmed via `docker ps` — 0.0.0.0:5462->5432/tcp, [::]:5462->5432/tcp).
    Tests were invoked with MIX_TEST_PARTITION=2 as this worktree requires,
    to avoid colliding with the primary checkout's own test database.

environment:
  toolchain: local (not Docker fallback) — mix/elixir reachable directly on PATH
  elixir_erlang: "Erlang/OTP 27 [erts-15.2.7.4] ... Mix 1.18.3 (compiled with Erlang/OTP 27)"
  postgres_container: "letflow-postgres-1 (already running, confirmed via
    \`docker ps\` only — 0.0.0.0:5462->5432/tcp and [::]:5462->5432/tcp;
    \`docker compose up\` was NOT run per this worktree's standing
    instructions, container is shared with the primary ~/letflow checkout)."
  partition: "MIX_TEST_PARTITION=2 (required for this worktree, per standing
    session instructions, to avoid colliding with the primary checkout's
    concurrently-running test database)"
  command_run: "MIX_TEST_PARTITION=2 mix test (full suite, unpinned/default
    random seed — the \`test\` alias runs ecto.create + ecto.migrate --quiet
    first automatically)"

verdict: FAIL

checks:
  test_suite_full_run:
    result: FAIL
    exit_code: 2
    summary_line_verbatim: "5 properties, 881 tests, 2 failures"
    finished_line_verbatim: "Finished in 93.5 seconds (2.1s async, 91.3s sync)"
    failures: 2

  failure_1_req048_own_ac1_blocker:
    result: FAIL
    classification: "REQ-048's OWN acceptance criterion failing — BLOCKING this run"
    test: "test AC1 -- completing a PENDING task at an ordinary
      unconditioned-edge HUMAN_TASK merges output_variables, completes the
      task, activates the next HUMAN_TASK, appends one event
      (Letflow.EngineCompleteTaskTest)"
    location: "test/letflow/engine_complete_task_test.exs:228 (assertion at
      line 254)"
    assertion_output_verbatim: |
      Assertion with == failed
      code:  assert length(tasks) == 2
      left:  1
      right: 2
      stacktrace:
        test/letflow/engine_complete_task_test.exs:254: (test)
    diagnosis: >
      Confirmed reproduction of the already-filed BLOCKER bug
      docs/issues/ISS-0057.yaml (GitHub #202): Letflow.Engine.complete_task/3
      never creates a `tasks` row for the next HUMAN_TASK node when a token
      continues directly from one HUMAN_TASK to another (no intervening
      gateway). Root cause per ISS-0057: TaskActivation.newly_pending_tokens/2
      diffs pending-task lists by token_id only, so a token's stale
      "previous" pending entry masks its genuinely-new position when it
      advances to a second HUMAN_TASK within the same complete_task call.
      This test is REQ-048's own AC1 and its failure is not incidental —
      REQ-048 cannot be marked done with this failing. Not re-diagnosed here
      (already root-caused by TEST-DESIGNER in ISS-0057); routing to
      ELIXIR-DEV for the actual fix per this run's briefing.
    routing: "ELIXIR-DEV (real application bug, not a test-code issue —
      per test-runner.md's rule against loosening tests to force a green
      suite)"

  failure_2_pre_existing_unrelated:
    result: FAIL
    classification: "pre-existing/environmental — NOT blocking this run,
      forwarded per Unblock-Everything"
    test: "test DropLegacyPublicIdentityTables migration guard (REQ-063 §5)
      -- skip-on-uncopied-row once the copy is completed for every
      registered tenant, the guard PROCEEDS and drops all three public
      tables (Letflow.IdentityMigrationTest)"
    location: "test/letflow/identity_migration_test.exs:367 (assertion loop
      at line 394-396)"
    assertion_output_verbatim: |
      expected public.groups to be dropped
      code: for table <- ["groups", "users", "tenant_role"] do
      stacktrace:
        test/letflow/identity_migration_test.exs:396: anonymous fn/2 in
          Letflow.IdentityMigrationTest."test DropLegacyPublicIdentityTables
          migration guard (REQ-063 §5) -- skip-on-uncopied-row once the copy
          is completed for every registered tenant, the guard PROCEEDS and
          drops all three public tables"/1
        (elixir 1.18.3) lib/enum.ex:2546:
          Enum."-reduce/3-lists^foldl/2-0-"/3
        test/letflow/identity_migration_test.exs:394: (test)
    diagnosis: >
      Same failure mode as the already-open docs/issues/ISS-0048.yaml
      (orphaned tenant_*/sandbox_* Postgres schemas accumulating in the
      shared test database across runs, discovered by TEST-DESIGN-VALIDATOR
      during REQ-063's rework). This guard test's "every registered tenant
      has been copied" precondition depends on the full set of tenant rows
      the shared DB happens to hold at run time; stray/orphaned schema state
      left over from other tests/runs in this shared Postgres instance is
      the suspected interference, consistent with ISS-0048's description.
      Not a new failure signature. Confirmed intermittent per ISS-0048's own
      record, not tied to REQ-048's code changes (REQ-048 touched
      lib/letflow/engine.ex and related task-completion modules only; no
      changes to identity migration code in this branch's diff).
    routing: "Pre-existing/environmental — filed under ISS-0048, still open.
      Not re-filed here. Does not block this run's own PASS/FAIL
      determination per Unblock-Everything, but is called out explicitly so
      RELEASE-VALIDATOR does not mistake it for a REQ-048 regression."

known_noise_not_a_failure:
  - >
    Verbose SQL query debug logging (`[debug] QUERY OK ...` lines, including
    tenant-schema DDL/DML from multi-tenant test fixtures) appears throughout
    raw mix test output (config/test.exs's default test log level) —
    expected, not indicative of any problem.

pre_existing_failures_found:
  - id: ISS-0048
    severity: MINOR
    title: "~69-70 orphaned tenant_*/sandbox_* Postgres schemas accumulated
      in the test database, DROP SCHEMA cleanup never ran"
    status: still open (not fixed in this run)
    manifestation_this_run: "test/letflow/identity_migration_test.exs
      DropLegacyPublicIdentityTables skip-on-uncopied-row guard test failed
      — consistent with ISS-0048's known intermittent failure mode, not a
      new signature."

issues_filed_this_run: none (both failures map to pre-existing filed
  issues — ISS-0057 for the blocking REQ-048 defect, ISS-0048 for the
  pre-existing environmental failure — neither required a new filing)

diagnosis: >
  2 of 881 tests failed. Failure 1 (engine_complete_task_test.exs AC1) is
  REQ-048's OWN acceptance criterion failing on a confirmed, already-filed
  BLOCKER application bug (ISS-0057 / GitHub #202) in
  Letflow.Engine.complete_task/3 — this is not test-code fragility, it is a
  real defect in the shipped implementation, and it blocks this requirement
  from being marked done. Routing to ELIXIR-DEV for the fix; TEST-RUNNER
  does not implement it. Failure 2 (identity_migration_test.exs) reproduces
  the same failure mode as the already-open, pre-existing ISS-0048
  (orphaned tenant schemas in the shared test DB) and is unrelated to
  REQ-048's own changes — forwarded per Unblock-Everything, not blocking
  this run's verdict on its own, but the overall run verdict is still FAIL
  because of failure 1.

regression_check: >
  881 tests total, 5 properties, 2 failures. No other tests outside the two
  described above failed. No sign of a new regression introduced by
  REQ-048's own test additions beyond the deliberately-reproduced AC1
  failure that IS this run's finding.

handoff:
  verdict: FAIL
  artifacts_out:
    - test/reports/report-20260818-WF02-REQ048-20260818.yaml
  next_action: "Route to ELIXIR-DEV to fix ISS-0057
    (Letflow.Engine.complete_task/3 / TaskActivation.newly_pending_tokens/2
    token_id-only diffing bug) before this requirement can proceed to
    RELEASE-VALIDATOR. ISS-0048 (identity_migration_test.exs) remains
    separately tracked/open and does not need action as part of this
    requirement's rework."
