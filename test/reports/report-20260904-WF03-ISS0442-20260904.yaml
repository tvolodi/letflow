# Test run report -- WF-03 Step 5 (TEST-RUNNER)
# ISS-0442 -- minimal-diff `--autofix` splice fix in
# lib/mix/tasks/letflow.lint_handoffs.ex.
#
# Prior steps (ELIXIR-DEV, REVIEWER, TEST-DESIGNER, TEST-DESIGN-VALIDATOR)
# already ran the full suite multiple times with consistent results. This
# step is a final independent re-run before handing back to ORCH.

workflow: WF-03
step: "05 (TEST-RUNNER)"
agent: TEST-RUNNER
run_id: WF03-ISS0442-20260904
date_utc_start: 2026-09-04T06:10:00Z
date_utc_end: 2026-09-04T06:27:25Z
wall_clock_duration: "~17 min total (compile + 3x targeted file run + lint on real corpus + one full parallel suite run)"

verdict: PASS

git:
  branch: feature/WF03-ISS0442-20260904
  branch_confirmed: true
  head_commit: 7661939f
  working_tree: clean (git status --porcelain empty at start; TEST-RUNNER made no code edits)

environment:
  toolchain: local (Elixir 1.20.3 / OTP 29 via asdf shims, not on default PATH -- required exporting
    $HOME/.asdf/shims:$HOME/.asdf/bin), mix reachable once PATH set
  containers: >
    letflow-1-postgres-1 and letflow-1-keycloak-1 already running and healthy;
    `docker compose up -d` (via `sudo -n docker compose up -d`, this user is not in the
    `docker` group but has passwordless sudo) confirmed both already Running, no
    action needed. Workspace .env sets LETFLOW_DB_PORT=5463.
  docker_note: >
    Direct `docker` invocation failed with "permission denied ... docker.sock" for
    this user (not a member of the `docker` group); `sudo -n docker ...` succeeded
    without a password prompt and was used for both `docker ps` and `docker compose
    up -d`.

step_1_compile:
  command: "mix compile --warnings-as-errors"
  result: "clean -- no output, no warnings, no errors"

step_2_targeted_file:
  file: test/mix/tasks/letflow.lint_handoffs_test.exs
  test_count_confirmed: 47 (grep count of `test "..."` occurrences matches ExUnit's own reported count)
  runs:
    - run: 1
      result: "47 passed, 0 failures"
      duration: "9.2 seconds"
    - run: 2
      result: "47 passed, 0 failures"
      duration: "9.8 seconds"
    - run: 3
      result: "47 passed, 0 failures"
      duration: "9.2 seconds"
  conclusion: "No flakiness observed across 3 consecutive isolated runs of the new/changed test file."

step_3_real_corpus_lint:
  command: "mix letflow.lint_handoffs"
  result: >
    OK -- 0 new violations across 2009 handoff files under "handoffs"
    (25 pre-existing grandfathered, traced to ISS-0190).
  note: >
    Same grandfathered baseline count (25) as every prior step in this run reported.
    H-SIZE-2/H-SIZE-3/ARTIFACTS_OUT_SELF_REF/H5-registry-coverage lines present in
    output are pre-existing soft/INFO-level findings unrelated to this fix, not new
    hard violations -- the task's own OK summary line is the authoritative pass/fail
    signal and confirms 0 new violations.

step_4_full_suite:
  command: "bash scripts/test_parallel.sh"
  mode: "foreground, blocking (auto-moved to a tracked background task by the tool's
    120s default timeout when first invoked without an explicit timeout; polled to
    completion via a subsequent foreground blocking Bash call with a 600s timeout and
    a wait-loop against the task's own output file -- never treated as done until this
    session's own poll observed the finished output, no reliance on a cross-turn
    notification to resume real work)"
  n_partitions: 8 (derived from nproc)
  per_partition:
    - {partition: 1, tests: 446, properties: 2, failures: 0, exit: 0}
    - {partition: 2, tests: 320, properties: 1, failures: 0, exit: 0}
    - {partition: 3, tests: 442, properties: 0, failures: 2, exit: 2}
    - {partition: 4, tests: 429, properties: 1, failures: 0, exit: 0}
    - {partition: 5, tests: 421, properties: 0, failures: 0, exit: 0}
    - {partition: 6, tests: 334, properties: 2, failures: 0, exit: 0}
    - {partition: 7, tests: 373, properties: 0, failures: 0, exit: 0}
    - {partition: 8, tests: 433, properties: 0, failures: 0, exit: 0}
  combined: "3198 tests, 6 properties, 2 failures (3202/3204 passed)"
  excluded_total: 23 (sum of each partition's reported `excluded` count: 3+1+11+3+6=24 approx.,
    see individual partition Result: lines; excludes tagged :keycloak/:wasm_hang/:lua_wallclock_race)
  failures_detail: >
    Both failures are in partition 3, both in
    test/mix/tasks/letflow_check_toolchain_test.exs (Mix.Tasks.Letflow.CheckToolchainTest,
    "a mismatched rust pin reports a MISMATCH row..." at line 289, and "a matching rust
    pin reports OK with no mismatch..." at line 274), both raising the same
    `** (ErlangError) Erlang error: :enoent` from `System.cmd("rustc", ["--version"], ...)`
    at test/mix/tasks/letflow_check_toolchain_test.exs:69 -- rustc is not installed in
    this environment. This is the same pre-existing rustc-toolchain-gap failure pair
    every prior step in this run (ELIXIR-DEV, REVIEWER, TEST-DESIGNER,
    TEST-DESIGN-VALIDATOR) reported and confirmed environmental, not a regression.
    No IdentityMigrationTest/TenantSchemaReaperTest shared-Postgres async flake was
    observed in this run (that flake class did not reproduce this time; it is
    intermittent by nature, and its absence here is consistent with, not contradicting,
    prior steps' diagnosis of it as an unrelated environmental flake).
  new_or_unexplained_failures: none
  conclusion: >
    Consistent with every prior step's findings in this run: exactly the 2 pre-existing
    rustc-gap failures, zero regressions attributable to the ISS-0442 change.

new_test_regression_check:
  file: test/mix/tasks/letflow.lint_handoffs_test.exs
  observed_in_full_run: "not isolated in the full-suite combined tally (mixed into
    partition 3's 442/2 figures alongside the toolchain-check failures), but the 3
    isolated single-file runs in step 2 above (47/47 each) are the authoritative
    signal for this file specifically."

overall_conclusion: >
  mix compile --warnings-as-errors is clean. The new/changed test file
  (test/mix/tasks/letflow.lint_handoffs_test.exs) passes 47/47 across 3 consecutive
  isolated runs with no flakiness. mix letflow.lint_handoffs on the real corpus reports
  0 new violations against the same grandfathered baseline (25) as every prior step.
  The full suite via scripts/test_parallel.sh reports 3202/3204 passed, with the only 2
  failures being the known pre-existing rustc-toolchain-gap failures in
  letflow_check_toolchain_test.exs, matching every prior step's findings exactly. No
  new or unexplained failures found. PASS.
