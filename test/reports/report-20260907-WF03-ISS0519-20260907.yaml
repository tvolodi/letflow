run_id: WF03-ISS0519-20260907
workflow_id: WF-03
step: "04"
agent: TEST-RUNNER
branch: feature/WF03-ISS0519-20260907
base_commit: 42d218a9
timestamp_utc: "2026-09-07T02:52:01Z"

commands_run:
  - cmd: "mix compile --warnings-as-errors"
    result: PASS
    note: "No output, clean compile."
  - cmd: "mix format --check-formatted"
    result: PASS
    note: "No output, all files formatted."
  - cmd: "mix letflow.check.test"
    result: PASS
    note: >
      First attempt failed environmentally (known local-sandbox hazard #1):
      all 8 partitions hit LETFLOW_TENANT_FIXTURE phase=incomplete_schema,
      versions_missing=[20260907000001] -- a stale tenant_template schema
      built before this branch's own new migration
      (priv/repo/migrations/20260907000001_add_entity_definitions_active_partial_index.exs)
      existed. Confirmed via grep across all 8 partition logs (counts:
      p1=178, p2=230, p3=276, p4=170, p5=107, p6=128, p7=218, p8=162
      occurrences of the versions_missing marker), no clone_failed or
      too_many_connections signature present. Cleared via
      `DROP SCHEMA IF EXISTS tenant_template CASCADE` on letflow_test and
      letflow_test1..8 (pre-authorized idempotent test-cache cleanup per
      TEST-RUNNER instructions), then re-ran.
      Second attempt: Result: 498/500 passed (1/1 property, 497/499 tests),
      14 excluded. Failed: 2 tests, both
      Mix.Tasks.Letflow.CheckToolchainTest ("rust pin (REQ-165) a matching
      rust pin reports OK with no mismatch" and "a mismatched rust pin
      reports a MISMATCH row naming expected and running"), both raising
      `** (ErlangError) Erlang error: :enoent` from
      `System.cmd("rustc", ["--version"], ...)` at
      test/mix/tasks/letflow_check_toolchain_test.exs:69. Root cause
      confirmed: `which rustc` returns exit 1 on this host -- rustc is
      simply not installed in this sandbox. This is a pre-existing
      environment gap unrelated to this branch: `git diff 42d218a9..HEAD
      --stat` touches only entity_definitions/definitions.ex,
      entity_definition.ex, records.ex, tenant_provisioning.ex, the new
      migration, two test files, and handoff/design docs -- nothing in
      test/mix/tasks/letflow_check_toolchain_test.exs or any Rust/wasmex
      toolchain path. Also observed several transient
      phase=incomplete_schema/versions_missing=[20260821000002] log lines
      in partition-5 during this second run; these self-healed (the
      TenantFixture dropped and re-provisioned the schema inline) and did
      not appear in the final failure list -- not a genuine finding.
      Overall verdict: PASS for this branch's own change -- the only 2
      failures are a known, pre-existing, unrelated environmental gap
      (missing rustc binary), not a regression introduced by ISS-0519.

scoped_test_runs:
  - cmd: "mix test test/letflow/entities/definitions_test.exs test/letflow/entities/records_test.exs"
    result: PASS
    note: "Re-confirmed by REVIEWER at step-03d: 46 passed, 0 failures. Not independently re-run here since the full-suite gate already covers these files and passed."

conclusion:
  status: PASS
  summary: >
    Full regression suite (mix letflow.check.test) passes for this branch's
    change set once the pre-existing stale tenant_template cache was
    cleared. compile --warnings-as-errors and format --check-formatted are
    both clean. The only 2 failing tests in the full run are unrelated,
    pre-existing environmental failures (rustc toolchain absent on this
    sandbox host) in REQ-165's toolchain-pin test, outside this branch's
    diff. No regression or defect attributable to ISS-0519's changes.
