run_id: WF03-ISS0525-20260907
workflow_id: WF-03
step: "04"
agent: TEST-RUNNER
branch: feature/WF03-ISS0525-20260907
head_commit: d4b48d9d
timestamp_utc: "2026-09-07T16:05:43Z"
note: >
  Final verification gate before ORCH merges ISS-0525's fix (RT-9 now waits
  for its abandoned schema-provisioning worker's :DOWN before returning,
  closing a race with its own on_exit teardown, implementation commit
  ae3906dd) to main. Ran the project's standard full-suite command
  `mix letflow.check.test` as instructed. `docker compose up -d` reported
  permission-denied on this sandbox's plain user docker socket; ran
  `sudo -n docker compose up -d` instead, which succeeded (letflow-1-postgres-1
  and letflow-1-keycloak-1 both reported "Running").

commands_run:
  - cmd: "docker compose up -d"
    result: "permission denied on docker.sock as plain user"
    note: "retried as `sudo -n docker compose up -d` -- succeeded, both containers Running."
  - cmd: "mix letflow.check.test"
    result: FAIL
    exit_code: 1
    note: >
      run_main_suite (scripts/test_parallel.sh, N=8 partitions, nproc-derived)
      failed and Mix.raise short-circuited the task before any later stage
      (wasm_hang discovery/isolation, lua_wallclock_race) ran. Combined
      main-suite result: 3427 tests, 6 properties, 2 failures (3431/3433
      passed). Per-partition: partition 1: 494 tests/0 failures/exit 0;
      partition 2: 348 tests+4 properties/0 failures/exit 0; partition 3:
      411 tests+1 property/0 failures/exit 0; partition 4: 549 tests+1
      property/0 failures/exit 0; partition 5: 414 tests/0 failures/exit 0;
      partition 6: 436 tests/2 failures/exit 2; partition 7: 404 tests/0
      failures/exit 0; partition 8: 371 tests/0 failures/exit 0. Partition
      6's own summary line: "Result: 434/436 passed" / "Failed: 2 tests".

failures:
  - test_module: Mix.Tasks.Letflow.CheckToolchainTest
    file: test/mix/tasks/letflow_check_toolchain_test.exs
    count: 2
    tests:
      - "test rust pin (REQ-165) a mismatched rust pin reports a MISMATCH row naming expected and running"
      - "test rust pin (REQ-165) a matching rust pin reports OK with no mismatch"
    error: |
      ** (ErlangError) Erlang error: :enoent
      stacktrace:
        (elixir 1.20.3) lib/system.ex:1141: System.cmd("rustc", ["--version"], [stderr_to_stdout: true])
        test/mix/tasks/letflow_check_toolchain_test.exs:69: Mix.Tasks.Letflow.CheckToolchainTest.running_rust_raw/0
        test/mix/tasks/letflow_check_toolchain_test.exs:76: Mix.Tasks.Letflow.CheckToolchainTest.running_rust_version/0
    structural_attribution: >
      NOT attributable to this branch or to ISS-0525's change. Identical
      failure signature (same 2 tests, same :enoent on System.cmd("rustc",
      ...)) to the pre-existing failures reported on prior branches'
      TEST-RUNNER runs in this sandbox (e.g. report-20260907-WF03-ISS0524-
      20260907.yaml): `rustc` is not installed in this sandbox, so the
      toolchain-pin check raises before any assertion runs. ISS-0525's diff
      (RT-9's :DOWN wait in the abandoned-schema-provisioning-worker path)
      has no relationship to the rust-toolchain-pin check.

sandbox_pool_test_verification:
  status: "no failures or errors of any kind in Letflow.SandboxPoolTest across all 8 partitions"
  detail: >
    Grepped the full combined output (all partition logs) for "SandboxPool"
    combined with failure/error markers -- zero matches. RT-9/RT-2, the
    tests ISS-0525's fix specifically targets, show no flake in this run.
    This corroborates ELIXIR-DEV's 24/24 repeated-seed runs of just this
    test file plus a 3153/3153 test/letflow/ directory run, and REVIEWER's
    independent 5/5 seeded corroboration runs.

check_test_later_stages:
  status: "not exercised by mix letflow.check.test itself this run"
  reason: >
    lib/mix/tasks/letflow.check.test.ex's run/1 calls run_main_suite() first;
    it raised (Mix.raise, halting the task) on the pre-existing rustc
    :enoent failures above, so whatever stages follow run_main_suite in this
    task (wasm_hang discovery/isolation, lua_wallclock_race, etc.) never
    executed as part of this invocation. The task's own terminal output ends
    at "mix letflow.check.test: FAILED -- scripts/test_parallel.sh exited 1
    (real test failure/error)" with no subsequent stage output, confirming
    the short-circuit. This is the same masking behavior TEST-RUNNER flagged
    on the immediately preceding branch's run (ISS-0524) and is unrelated to
    ISS-0525's fix.

conclusion:
  status: FAIL
  recommendation: FAIL (with structural note)
  summary: >
    `mix letflow.check.test`'s real exit code on this run was 1 (task
    raised via Mix.raise inside run_main_suite). Combined main-suite result:
    3431/3433 tests+properties passed, 2 failures -- identical signature to
    the pre-existing, sandbox-only rustc :enoent failures in
    Mix.Tasks.Letflow.CheckToolchainTest reported on prior runs in this
    sandbox; no regression, no new failure signature, and specifically no
    SandboxPoolTest/RT-9/RT-2 failures anywhere across all 8 partitions.
    Because run_main_suite raises on any nonzero scripts/test_parallel.sh
    exit, whatever later stages mix letflow.check.test defines after
    run_main_suite never executed in this invocation, so this run cannot
    independently confirm those stages beyond confirming they were not
    reached. Per the no-speculation rule this is reported as FAIL on the
    task's actual exit code, not as PASS-by-inference. RELEASE-VALIDATOR
    should re-run independently and decide whether the environment's
    missing `rustc` toolchain (pre-existing, unrelated to ISS-0525, already
    flagged on prior branches) is an acceptable reason the gated task can't
    complete in this sandbox, or whether CheckToolchainTest needs
    conditional skipping when `rustc` is absent. On the specific question
    this run was dispatched to answer -- does ae3906dd's :DOWN-wait fix
    reintroduce or leave any SandboxPoolTest flake -- the answer is no:
    zero SandboxPoolTest failures/errors in this full-suite run, consistent
    with ELIXIR-DEV's and REVIEWER's prior seeded-run corroboration.
