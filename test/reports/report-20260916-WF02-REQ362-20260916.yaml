run_id: WF02-REQ362-20260916
agent: TEST-RUNNER
workflow: WF-02 Step 4 (requirement implementation, full-suite verification)
requirement: REQ-362 (two-phase visual regression testing) -- entirely a web/
  (TypeScript/Playwright/vitest) diff, plus .claude/agents/uat-runner.md and a
  design doc. No lib/letflow/ or priv/repo/migrations/ files touched.
timestamp_utc: "2026-09-16T12:15:00Z"
branch: feature/WF02-REQ362-20260916
commit: 7cf3d7e33b09416b80e9769b098bfe4717813a65

verdict: PASS

environment:
  docker_compose: "letflow-1-postgres-1 and letflow-1-keycloak-1 already Running
    before this run; `docker compose up -d` (via sudo, passwordless in this
    sandbox) confirmed both, no change needed. Untracked .env sets
    LETFLOW_DB_PORT=5463 (own-workspace override per README) plus
    QUEUE_AUTH_TOKEN."
  host_cores: "8 (nproc)"
  toolchain: "mix/elixir present; no mix deps.get needed."
  backend_process_for_e2e: "A pre-existing `mix run --no-halt` beam process (pid
    1276114, started 2026-09-14, LETFLOW_SECRETS_MASTER_KEY set in that
    process's own environment from an earlier session) was already listening on
    :4000 and answering /health with 200. This session's own shell has no
    LETFLOW_SECRETS_MASTER_KEY set, so a fresh `mix run --no-halt` from this
    session fails fast (by design, REQ-190) -- the pre-existing process was
    reused rather than restarted, to avoid invalidating any already-encrypted
    secrets with a freshly generated key."

diff_scope:
  files: "git diff --name-only main...HEAD: .claude/agents/uat-runner.md,
    docs/agents/AGENT_SYSTEM.md, docs/issues/ISS-0691.yaml,
    docs/requirements.yaml, docs/status/requirement_status.index.yaml,
    docs/status/requirement_status.v18.yaml,
    lib/letflow/design/req362-visual-regression-testing.md,
    test/fixtures/uat/visual-baselines/platform/req362-visual-regression-selfcheck/*
    (baseline PNG/YAML + ISS-0691 disputed-evidence PNGs), test/specs/REQ-362.md,
    web/package.json, web/playwright.config.ts,
    web/tests/e2e/req362-visual-regression.e2e.spec.ts,
    web/tests/support/visual-baseline.ts, web/tests/unit/visual-baseline.test.ts"

commands_run:
  - "scripts/test_parallel.sh -- N=8 (nproc) FAILED to produce a valid combined
    result: 6 of 8 partitions crashed during ecto.create/migrate with Postgres
    `too_many_connections` (this host's known ISS-0219 capacity limit per
    README's own documented caveat), leaving only 2 partitions with real
    Result: lines (623 tests/3 failures and 601 tests/0 failures -- the 3
    'failures' there are themselves partition-crash artifacts of the other 6
    partitions saturating the pool, not real test failures; not used for the
    verdict below)."
  - "TEST_PARALLEL_N=4 scripts/test_parallel.sh -- clean run, all 4 partitions
    produced real Result: lines, 0 failures. Used as the authoritative backend
    result per README's own guidance ('4 is known-good on at least one dev
    host... rather than assuming a code regression')."
  - "mix compile --warnings-as-errors -- clean, no output."
  - "mix format --check-formatted -- exit 0, no output."
  - "cd web && npm run type-check -- clean, no output."
  - "cd web && npm run lint -- clean, no output (--max-warnings 0)."
  - "cd web && npm test (vitest) -- 585 passed, 1 failed (586 total, 83 files)."
  - "cd web && npm run guards -- 48 passed (4 files); this also runs a full
    `vite build` internally as part of bundle-scan, which succeeded (see build
    output below)."
  - "cd web && npm run build (tsc -b && vite build) -- succeeded, dist/ emitted,
    only a routine >500kB chunk-size advisory (not an error)."
  - "cd web && npm run test:e2e (Playwright, --grep-invert @manual) -- run
    twice against the pre-existing backend+Keycloak. Both runs: identical 46
    passed / 11 skipped / 38 did not run / 186 failed. REQ-362's own spec file
    (web/tests/e2e/req362-visual-regression.e2e.spec.ts) passed all 6 of its
    non-@manual cases on both runs (one of the 6 uses test.fail() by design to
    pin documented Playwright auto-fail behavior -- shows as a live '✘' in
    Playwright's running-status line but is NOT in either run's final failed
    list, i.e. it passed as an expected failure, matching TEST-DESIGNER's own
    claim of '6/6 formal e2e tests pass, one via test.fail()')."

full_suite_result:
  final_combined: "4318 tests, 6 properties, 0 failures (4324/4324 passed)"
  per_partition:
    - "partition 1: 1224 tests, 2 properties, 0 failures, exit 0"
    - "partition 2: 1009 tests, 0 properties, 0 failures, exit 0"
    - "partition 3: 1122 tests, 0 properties, 0 failures, exit 0"
    - "partition 4: 963 tests, 4 properties, 0 failures, exit 0"
  note: "Run with TEST_PARALLEL_N=4 after the default N=8 run hit this host's
    documented Postgres-connection ceiling (README's ISS-0219 note). No
    lib/letflow/ or migration files are in this diff, so a clean backend
    result was expected; confirmed rather than skipped."

frontend_unit_test_result:
  vitest: "Test Files 1 failed | 82 passed (83); Tests 1 failed | 585 passed (586)"
  failure: "src/config/__tests__/bilimbagaEntities.test.ts > ISS-0655 -- BILIMBAGA_ENTITY_TYPES
    covers every admin-manageable pack entity type > every admin-manageable
    pack entity type has a BILIMBAGA_ENTITY_TYPES entry -- AssertionError:
    expected [ 'certificate' ] to deeply equal []"
  guards: "48 passed (4 files) -- meta-control, source-scan, bundle-scan
    (includes a real vite build), role-set"
  type_check: "clean"
  lint: "clean (--max-warnings 0)"
  build: "succeeded (tsc -b && vite build)"

frontend_e2e_result:
  formal_suite_req362_only: "6/6 tests in
    web/tests/e2e/req362-visual-regression.e2e.spec.ts's non-@manual describe
    block pass on both runs: phase 1 accept establishes baseline (AC-2), phase
    2 unchanged-screen PASS (AC-3 pass case), phase 2 altered-screen FAIL with
    no issue filed (AC-3 fail case, AC-6), re-baseline recovers to PASS with
    recorded justification (AC-5), phase 2 against a key with no baseline yet
    -- test.fail()-pinned documented Playwright auto-fail behavior, and the
    follow-up assertion that the no-baseline probe wrote the reference PNG but
    no sidecar."
  full_matrix_environment_limitation: "The broader ~281-test Playwright e2e
    matrix (everything outside req362's own spec) is NOT reliably runnable in
    this sandbox: 186/281 fail identically across two consecutive runs (same
    count both times), and the first failure's own error message is explicit
    -- 'A11Y GATE BLOCKER: BPM-API, Keycloak unreachable -- no surface
    scanned' (tests/e2e/a11y-gate.e2e.spec.ts:74). This matches
    web/README.md's own documented status verbatim: 'npm run test:e2e
    (Playwright) was not run: it needs a live backend and a Keycloak realm...
    those specs become runnable during S8.' The one backend process reachable
    in this sandbox answers plain /health but not /health/ready (404) and
    several tenant-realm-scoped endpoints intermittently ECONNREFUSED under
    load, and there is no configured Keycloak realm set for the
    tenant/company scenarios most of these specs assume. This is a structural
    environment gap, not a regression: reproduced identically twice (same 186
    count), and the failing files (a11y-gate, admin/services, tenants,
    tenant-dashboard, onboarding/*, pipelines/*, uat-*, etc.) are all absent
    from git diff --name-only main...HEAD -- none are files this diff
    touches."

findings:
  - id: ISS-0694
    title: "'certificate' entity type is missing from BILIMBAGA_ENTITY_TYPES,
      causing a pre-existing test failure in bilimbagaEntities.test.ts"
    github: "GH#1454 (OPEN)"
    status: "Already filed by TEST-DESIGN-VALIDATOR during Step 3b's own gate
      run. Confirmed present, unchanged, and structurally unrelated to this
      diff: web/src/config/__tests__/bilimbagaEntities.test.ts and
      web/src/config/bilimbagaEntities.ts are both absent from
      git diff --name-only main...HEAD. This run's own npm test output (585
      passed, 1 failed) matches GH#1454's own quoted evidence (585 passed, 1
      failed) exactly -- not used alone as the attribution per
      core-directives.md ('matching a previous count is not evidence'), but
      corroborates the structural (route 1) attribution above."

pre_existing_failures:
  count: 1
  attribution_evidence:
    - "Route 1 (structural): failing file
      src/config/__tests__/bilimbagaEntities.test.ts and the config file it
      asserts against, src/config/bilimbagaEntities.ts, are both absent from
      git diff --name-only main...HEAD -- REQ-362's diff cannot have caused
      this failure."
  note: "Already registered as ISS-0694 / GH#1454; not re-filed here."

regression_check_on_req362_files:
  backend: "N/A -- no lib/letflow/ or migration files in this diff."
  frontend_unit: "web/tests/unit/visual-baseline.test.ts (18 cases) ran as
    part of the 586-test vitest total, all 18 passing, 0 failures."
  frontend_e2e: "web/tests/e2e/req362-visual-regression.e2e.spec.ts's 6
    non-@manual cases all pass on both independent runs (see
    frontend_e2e_result above)."
  guards_and_build: "web/tests/guards/bundle-scan.spec.ts's real `vite build`
    (run twice: once standalone via npm run build, once inside npm run
    guards) succeeded both times with REQ-362's files in the tree."

gate_status:
  full_backend_suite: "4324/4324 passed, 0 pre-existing failures, 0
    regressions from REQ-362 (diff touches no lib/letflow/ files)"
  backend_compile_and_format: "mix compile --warnings-as-errors clean; mix
    format --check-formatted clean"
  frontend_static_gates: "type-check clean, lint clean, build succeeded, 48/48
    guard tests passed"
  frontend_unit_tests: "585/586 passed; the 1 failure is ISS-0694/GH#1454,
    structurally unrelated and already filed"
  frontend_e2e_req362_scope: "6/6 REQ-362 formal e2e tests pass on both runs"
  frontend_e2e_full_matrix: "NOT achievable in this sandbox -- documented
    environment limitation (missing live Keycloak realm / full backend
    readiness), reproduced identically across two runs, structurally
    unrelated to this diff (no failing file appears in
    git diff --name-only main...HEAD)"

next_step: >
  Hands off to RELEASE-VALIDATOR, which re-runs the full suite independently
  per this project's redundancy principle rather than trusting this report
  alone. RELEASE-VALIDATOR should be aware going in that the full Playwright
  e2e matrix (not just REQ-362's own spec) requires a live backend with a
  configured Keycloak realm that this sandbox does not have wired up beyond
  a bare /health-passing process left over from an earlier session --
  matches web/README.md's own long-standing note that npm run test:e2e needs
  infrastructure this checkout doesn't yet provide end-to-end.
