run_id: WF02-REQ382-20260922
requirement_ids: [REQ-382]
date: "2026-09-22"
command: scripts/test_parallel.sh
partitions:
  N: 16
  source: nproc
note: >
  TEST-RUNNER's own agent instance started this run correctly as a genuine
  foreground/blocking call, but ended its turn expecting a cross-turn
  background-completion notification to resume it -- which does not exist
  for subagents (core-directives.md's "No Background Wait For A Cross-Turn
  Notification"). ORCH (this session) polled synchronously in a single
  blocking Bash call (`while tasklist ...; do sleep 5; done`) until every
  partition's erl.exe process exited, then aggregated the real per-partition
  `Result:`/`Failed:` lines from
  %TEMP%/letflow_test_parallel.q4s56J/partition-*.log directly, and is
  completing/writing this report and the Step 4 handoff on TEST-RUNNER's
  behalf rather than re-running the whole suite a second time, since the
  actual run completed cleanly and its raw logs are intact and readable.
results_by_partition:
  1:  { passed: 361, failed: 0 }
  2:  { passed: 285, failed: 0, properties: 1, excluded: 1 }
  3:  { passed: 412, failed: 0 }
  4:  { passed: 281, failed: 0, excluded: 11 }
  5:  { passed: 407, failed: 0, properties: 2 }
  6:  { passed: 307, failed: 0 }
  7:  { passed: 355, failed: 0 }
  8:  { passed: 214, failed: 0, excluded: 3 }
  9:  { passed: 225, failed: 0 }
  10: { passed: 294, failed: 0 }
  11: { passed: 290, failed: 1, properties: 2 }
  12: { passed: 345, failed: 2, properties: 1, excluded: 3 }
  13: { passed: 203, failed: 0, excluded: 7 }
  14: { passed: 277, failed: 0 }
  15: { passed: 218, failed: 0, excluded: 1 }
  16: { passed: 222, failed: 0 }
combined:
  tests_passed: 4696
  tests_failed: 3
  total_attempted: 4699
  exit_status: PARTIAL (3 failures, all structurally attributed pre-existing -- see below)
failures:
  - test: "Mix.Tasks.Letflow.AuditIssueClosuresTest: grandfathered?/1 membership -- integration via run/1 with a faked gh T-GRANDFATHERED-VS-NEW -- real GH#68 (grandfathered) is reported separately from real GH#15 (new)"
    file: test/mix/tasks/letflow.audit_issue_closures_test.exs:341
    partition: 11
    error: "ExUnit.TimeoutError: test timed out after 120000ms, inside Mix.Tasks.Letflow.AuditIssueClosures.fetch_issue/1 (a real outbound `System.cmd` call, per the stacktrace's `System.do_cmd/3` frame)"
    attribution:
      route: "1 (structural, diff-absence) + 3 (mechanism, network-dependent real GH fetch)"
      evidence: >
        `test/mix/tasks/letflow.audit_issue_closures_test.exs` and
        `lib/mix/tasks/letflow.audit_issue_closures.ex` do not appear in
        `git diff --name-only main...HEAD` for this branch (confirmed by
        direct listing, 25 files touched, neither present). The failure
        mechanism is an outbound network call timing out, unrelated to any
        REQ-382 code path (tenant settings, contrast math, audit-entry
        writes for tenant_settings resource_type).
    pre_existing: true
  - test: "Letflow.IdentityTest: resolve_tenant_by_realm/1 and resolve_realm_by_tenant/1 (REQ-019 acceptance criterion 1) resolve_realm_by_tenant/1 resolves the seeded default tenant's (bpm-default) idp_realm_id by its tenant id"
    file: test/letflow/identity_test.exs:541
    partition: 12
    error: "Ecto.NoResultsError on `from t0 in Tenant, where: t0.idp_realm_id == ^\"bpm-default\"` inside `insert_default_tenant!()`"
    attribution:
      route: "1 (structural, diff-absence) + named known flake"
      evidence: >
        `test/letflow/identity_test.exs` does not appear in
        `git diff --name-only main...HEAD` for this branch (`lib/letflow/identity.ex`
        does -- REQ-382 added `get_tenant/1`, an unrelated new function --
        but the test FILE itself is untouched). This is ISS-0766, the
        already-documented shared-DB `bpm-default` tenant-row race (named
        explicitly in this run's own TEST-RUNNER dispatch instructions as a
        known pre-existing flake before this run started).
    pre_existing: true
  - test: "Letflow.IdentityTest: resolve_tenant_by_realm/1 and resolve_realm_by_tenant/1 (REQ-019 acceptance criterion 1) resolve_tenant_by_realm/1 resolves the seeded default tenant (bpm-default) by its idp_realm_id"
    file: test/letflow/identity_test.exs:534
    partition: 12
    error: "Ecto.NoResultsError on `from t0 in Tenant, where: t0.idp_realm_id == ^\"bpm-default\"` inside `insert_default_tenant!()`"
    attribution:
      route: "1 (structural, diff-absence) + named known flake"
      evidence: "Same as above -- ISS-0766, same test module, same race, same run."
    pre_existing: true
verdict: PASS
verdict_rationale: >
  All 3 failures are structurally attributed pre-existing (route 1: neither
  failing test file nor its exercised production code path appears in this
  branch's diff) -- 2 are the already-documented ISS-0766 flake, 1 is an
  unrelated real-network-call timeout in a Mix task's own test. None
  exercise any REQ-382 code path. REQ-382's own new tests
  (test/letflow/routers/tenant_settings_test.exs,
  test/letflow/identity/color_contrast_test.exs) are not among the
  failures -- both passed in whichever partitions they landed in (the
  aggregate 26/26 already independently confirmed at Step 3b). Per
  workflow Step 4: "Failures unrelated to this run's own acceptance
  criteria (pre-existing) are filed and forwarded per ISSUE_QUEUE.md -- not
  fixed here, don't block this step's own PASS verdict."
