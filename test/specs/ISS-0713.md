# Test Spec: ISS-0713 — Playwright strict-mode collision in platform quick-links assertion

**Issue:** ISS-0713 (queued from WF03-ISS0712-20260918's discovery run).
**Design:** `lib/letflow/design/WF03-ISS0713-fix-design.md`.
**Fix:** `web/tests/e2e/pipelines/platform-login-routing-by-role.pipeline.e2e.spec.ts`
lines 96-98, commit `374ae4ea` on `feature/WF03-ISS0713-20260918` — scopes the
`getByRole('link', { name })` locator to `page.getByTestId('platform-quick-links')`.
**Test tier:** e2e (Playwright, `pipeline_test` class — drives real Keycloak OIDC and a
real backend; normally executed by UAT-RUNNER against live QA, per the spec file's own
header comment).

## Why this is unusual for WF-03 Step 4

The defective artefact *is* the test file itself (a locator scoping bug in the test,
not application logic). There is no separate production-code path to write a new
regression test against — the "regression test" IS this one-line diff to the existing
spec. Per the design doc §4, TEST-DESIGNER is not required to author a new spec file;
the fail-then-pass proof is this same edit, verified by re-running the existing test.

## Criterion

The `EO-001: PLATFORM_ADMIN lands on the distinct platform dashboard` test's
quick-links assertion loop (lines 96-98) must resolve each of the five link names
('Tenants', 'Services', 'Health', 'Metrics', 'Users') to exactly one element (the
quick-links nav entry), not two (quick-links nav + sidebar nav), eliminating the
Playwright strict-mode violation.

## Fail-then-pass verification (WF-03 Step 4)

**1. Pre-fix (fail) — already on record, not re-derived here.** The strict-mode
violation was captured live during the discovery run WF03-ISS0712-20260918, with a
saved failure screenshot at
`web/test-results/pipelines-platform-login-r-759b6-distinct-platform-dashboard-chromium/test-failed-1.png`.
Re-deriving this by checking out the pre-fix commit and re-running was considered but
is redundant: the strict-mode violation is a static property of the unscoped locator
against the always-present AppShell sidebar (see design doc §1), not a flaky or
environment-dependent failure — the existing screenshot is sufficient record.

**2. Post-fix (pass) — attempted live in this session, environment-blocked.**

I attempted to actually execute this test against this machine's local stack
(`docker ps` confirms `letflow-postgres-1` and `letflow-keycloak-1` running,
Keycloak reachable on `http://localhost:8093`) rather than merely asserting it would
pass. Real command and real output:

```
$ cd web && BPM_IDP_BASE_URL=http://localhost:8093 npx playwright test \
    --config=playwright.config.ts \
    pipelines/platform-login-routing-by-role.pipeline.e2e.spec.ts \
    -g "PLATFORM_ADMIN lands"
...
TimeoutError: page.waitForURL: Timeout 20000ms exceeded.
    at loginViaRealOidcRedirect (tests/e2e/helpers.ts:146:14)
    at pipelines/platform-login-routing-by-role.pipeline.e2e.spec.ts:71:5
  1 failed
```

This failure is at line 71 — the real-OIDC-redirect login helper, **before** the code
ever reaches the fixed assertion at lines 96-98. It confirms the app on this machine
never initiated an off-origin redirect to Keycloak, which traces to the Letflow
backend (Elixir app on port 8080) not running in this environment — only Postgres and
Keycloak containers are up; `curl http://127.0.0.1:8080/health` returns connection
refused. There is also no confirmation that this local Keycloak realm has the
`admin-user`/`worker-user` accounts and seeded tenant data the spec's credentials
(`resolveCredential('UAT_QA_ADMIN_PASSWORD', 'admin-pass')`) assume — that provisioning
is normally UAT-RUNNER's/live-QA's responsibility per the spec file's own header
comment ("Drives ... for UAT-RUNNER").

**Conclusion: this test cannot be run to real pass/fail completion from this
environment.** Standing up the full backend plus correctly seeding Keycloak/tenant
data to match live QA is out of TEST-DESIGNER's scope and risks manufacturing a false
pass/fail against a differently-configured stack rather than proving the actual fix.

## Required follow-up (flagged for ORCH)

A **live UAT-RUNNER re-run** of this spec (`EO-001: PLATFORM_ADMIN lands on the
distinct platform dashboard`) against real QA is the remaining requirement before
ISS-0713 can be marked genuinely `resolved`. Until that re-run passes, the
fail-then-pass proof is incomplete: the "fail" half is on record (pre-fix screenshot),
but the "pass" half is not yet produced — only reasoned about (the diff is a pure
locator-scoping change with no other side effects per design doc §5, so it is expected
to pass, but "expected" is not "run and quoted" per this project's no-speculation
rule).

## Coverage check

- [x] The single acceptance criterion (strict-mode-violation-free assertion) has its
      fail-then-pass proof structure documented above.
- [x] No new test file needed (design doc §4) — the fix itself is the regression test.
- [ ] Post-fix live pass evidence — **not yet produced**, requires UAT-RUNNER against
      live QA (see "Required follow-up" above). This checkbox must be completed before
      RELEASE-VALIDATOR/DOC-UPDATER treat ISS-0713 as closed.
