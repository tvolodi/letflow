# Test spec: REQ-160 — Lua host API 2/2 — write path — platform.write_variable,
call_service, emit_event (LUA-11 write half, LUA-12)

**Requirement:** REQ-160 (`docs/requirements.yaml`)
**Implementation:** `lib/letflow/engine/lua/platform.ex` (extended)
**Design:** `lib/letflow/design/req160-lua-host-api-write.md`
**Test code:** `test/letflow/engine/lua/platform_test.exs` (extended, a new REQ-160
section below the existing REQ-159 divider)

This is a pre-implementation skeleton, written by CODE-DESIGNER (WF-02 Step 1), for
TEST-DESIGNER (Step 3) to reconcile against the actual implementation once it lands —
following the same convention REQ-159's own test spec used for its own reconciliation
note.

## Construction convention

Same as REQ-159's spec: tests needing a specific execution context call
`Letflow.Engine.Lua.Platform.install/3` directly against a fresh
`Letflow.Engine.Lua.Sandbox.new/0` VM, granting whichever capability the test needs via
`Letflow.Engine.Lua.Capabilities.new/1`.

## Planned test cases

### 1. `platform.write_variable` — stages without applying (AC1's own acceptance text)

- Calling `platform.write_variable("x", 1)` inside a script, then reading back
  `Letflow.Engine.Lua.Platform.take_staged_writes/0` from the *same* process the script
  ran in, returns `%{"x" => 1}` — proving the write reached the staging buffer.
- A `write_variable` call whose first argument is not a string is a no-op: the staged
  buffer is unaffected (still whatever it was before the call), and the call itself
  returns without raising.
- `write_variable` never calls `Letflow.Repo` — asserted the same way REQ-159's own
  "no `Repo` call attempted" property was asserted for `get_instance_state`'s forbidden
  arm (a telemetry-handler-based assertion, per that spec's own mutation-testing
  addendum, or an equivalent mechanism TEST-DESIGNER selects).

### 2. `platform.write_variable` — discarded on every failure arm (AC1)

Four sub-cases, each asserting the identical outcome — the staged-writes buffer, once
retrieved via a mechanism equivalent to `take_staged_writes/0`, never reflects a write
a failed script attempted. **All four are now directly, behaviorally implemented**
(TEST-DESIGNER, rework 1) — see `test/letflow/engine/lua/platform_test.exs`'s
`"REQ-160: platform.write_variable stages into a process-local buffer"` describe block:

- **Raised script error.** A script calls `platform.write_variable("x", 1)` and then
  raises a Lua runtime error. Test: `"a script that writes then raises leaves the
  buffer un-drained by anything in platform.ex itself (AC1)"` — asserts the write
  stays in the SAME process's buffer (nothing forwards it anywhere on a failure path).
- **REQ-154's instruction budget.** A script calls `write_variable` then enters a loop
  that exhausts a small, explicitly configured `:max_instructions` budget (500), driven
  directly via `Sandbox.new(max_instructions: 500)` — the real REQ-154 mechanism, not a
  simulation. Test: `"REQ-154's instruction-budget exhaustion discards the staged write
  -- never observed from any other process (AC1)"`. The script runs inside a `Task`;
  the task's own process confirms internally that the write did reach its buffer
  (proving the write was genuinely staged, not merely never attempted), then the test
  process — a DIFFERENT process — asserts its own `take_staged_writes/0` returns `%{}`.
  Process dictionaries are per-process in the BEAM, so this is a direct observation,
  not an inference.
- **REQ-155's wall-clock kill.** A script calls `write_variable` then blocks in an
  infinite loop with a generous instruction budget (so only the wall-clock layer can
  end it). Mirrors `test/letflow/engine/lua/executor_test.exs`'s own REQ-155 harness
  exactly: `Task.async` → `Task.yield(task, timeout_ms)` returning `nil` →
  `Task.shutdown(task, :brutal_kill)`. Test: `"REQ-155's wall-clock kill discards the
  staged write -- the killed process's buffer is simply gone (AC1)"` — asserts
  `Process.alive?(task.pid) == false` (genuinely dead, not abandoned) and that this
  test process's `take_staged_writes/0` returns `%{}`.
- **REQ-156's memory limit.** A script calls `write_variable` then attempts a large
  allocation under a small (`1 MB`) configured `:max_heap_words` limit. Mirrors
  `executor_test.exs`'s own REQ-156 harness exactly: `:erlang.spawn_opt/2` with
  `[:monitor, max_heap_size: %{size: ..., kill: true, error_logger: false}]` (the same
  mechanism `lib/letflow/engine/lua/executor.ex`'s `run_with_heap_limit/4` uses, since
  `Task`/`Task.Supervisor` cannot carry a `max_heap_size` spawn option). Test: `"REQ-156's
  memory-limit kill discards the staged write -- the killed process's buffer is simply
  gone (AC1)"` — asserts the `:DOWN` reason is `:killed` and this test process's
  `take_staged_writes/0` returns `%{}`.

The structural guard test (`"platform.ex's source has zero call sites of
take_staged_writes() other than its own @spec/def lines (AC1)"`) is retained as
supplementary evidence that no code path in `platform.ex` itself ever reads the buffer
out on any of these paths — it is no longer the sole evidence for the three harder
arms, per TEST-DESIGN-VALIDATOR's rework-1 finding
(`handoffs/WF02-REQ160-20260827/step-03-test-designer-rework1.json`).

### 3. `platform.write_variable` — multiple writes accumulate, then apply atomically on success (AC2)

- A script calls `write_variable` three times with three distinct names, then completes
  successfully (no error, no kill). `take_staged_writes/0`, read from the same process
  immediately after the script's normal completion, returns a map containing all three
  key/value pairs — none missing, none stale.
- A script writes the same name twice with two different values, then completes
  successfully. `take_staged_writes/0` returns only the second (last) value for that
  name — proving "last write wins" (design §2.4), not both, not neither.
- **Atomicity, not merely eventual application.** Using `Letflow.Engine.VariableMerge.merge/3`
  directly against the staged-writes map `take_staged_writes/0` returned (current
  variables `%{}`, no validations), assert the single `merge/3` call's return is the
  whole `{:ok, new_variables, events}` tuple with all three keys present in
  `new_variables` — there is no intermediate call, no partial map, and no way for a
  test (or any other process) to observe a state with only one or two of the three keys
  applied, because `merge/3` is a single, non-yielding function call (design §2.6).
  **Implemented** (TEST-DESIGNER, rework 1):
  `test/letflow/engine/lua/platform_test.exs`'s `"REQ-160/AC2: VariableMerge.merge/3
  applies all staged writes atomically in a single call (design §2.6)"` test stages
  three writes via a successful script, calls `VariableMerge.merge(%{}, staged, nil)`
  directly, and asserts the return is `{:ok, new_variables, events}` with all three
  keys present.

### 4. `platform.write_variable` — capability denial

- Against `Platform.install/3` called with an empty grant set, calling
  `platform.write_variable` raises a Lua error whose structured fields show `required`
  equal to `"variable:write"` — same three-field assertion style as every other gated
  function's denial test (REQ-157/159 precedent).

### 5. `platform.call_service` — round trip through a registered service (AC3, LUA-12's own text)

- Configure a test double implementing `Letflow.Engine.Lua.Platform.ServiceCaller`
  (via `Application.put_env(:letflow, :lua_platform_service_caller, <double module>)`)
  that returns `{:ok, %{"status" => "ok", "count" => 3}}` for a known `service_id`.
  Granting `"service:call:<that service_id>"`, calling
  `platform.call_service(service_id, {...})` from inside a script and reading the
  returned table's fields inside the script (e.g. via a second `platform.log` call
  echoing them, or returning them as the script's own result) shows the exact values
  the double returned — proving the response is both a real Lua table (LUA-12's own
  "MUST be returned as Lua table") and actually readable from the script, not merely
  returned as an opaque host value.
- The double receives the decoded, `from_lua/1`-converted payload the script passed —
  asserted by having the double itself assert on its received arguments (or by a
  test-only capturing double), proving the payload actually round-trips through the
  host, not merely that some table comes back.

### 6. `platform.call_service` — number-marshalling round trip (AC6)

- A double returns a response map containing one integer-valued and one
  whole-number-float-valued field. The script reads both back via `math.type` (mirrors
  REQ-159's own read-direction round-trip test, applied here to `call_service`'s
  response instead of `read_variable`'s value) and asserts `"integer"`/`"float"`
  respectively — proving `LuaNumberMarshalling.to_lua/1` runs on this call path.
- A script passes one integer-valued and one whole-number-float-valued argument as part
  of `call_service`'s payload; the double captures what it received and asserts the
  Elixir-side types are `integer()`/`float()` respectively (not collapsed) — proving
  `from_lua/1` runs on the write direction of this same call. **Implemented**
  (TEST-DESIGNER, rework 1): `test/letflow/engine/lua/platform_test.exs`'s new
  `"REQ-160: platform.call_service -- write-direction marshalling (AC6)"` describe
  block reuses `SpyServiceCaller` (already defined for the mutation-testing gap on
  capability-gate ordering) as a capturing double — its test asserts
  `is_integer(payload["amount"])` and `is_float(payload["rate"])` directly on the
  payload the double received, not on a value that round-tripped back through Lua.

### 7. `platform.call_service` — service failure returns structured error, not a raise (AC4, part 1)

- Configure the double to return `{:error, :downstream_unavailable}` for a granted
  `service_id`. Calling `platform.call_service` from the script does **not** raise —
  the call returns normally, and the script observes a `nil` first value and a second
  value that is a table with a `reason` field derived from `:downstream_unavailable`.
  Assert explicitly that no `Lua.RuntimeException`/`Lua.CompilerException` propagates
  out of the evaluating call for this case.

### 8. `platform.call_service` — missing capability DOES raise (AC4, part 2)

- Against `Platform.install/3` called with a grant set that does **not** include
  `"service:call:<the id the script names>"` (either empty, or granting a
  *different* service's capability — proving the parameterisation is per-service, not
  all-or-nothing), calling `platform.call_service` raises a Lua error whose structured
  `required` field equals `"service:call:<the id the script names>"`. This test and
  test 7 above must be two distinct test cases in the same file, each asserting the
  opposite of what the other asserts (raises vs. returns), per REQ-160's explicit "both
  behaviours must be tested and must differ" text.

### 9. `platform.emit_event` — capability denial and successful emission (AC5)

- Against an empty grant set, calling `platform.emit_event` raises a Lua error whose
  structured `required` field equals `"event:emit"`.
- With `"event:emit"` granted and a populated execution context (`instance_id`,
  `prefix`, `actor_id` all set to real, fixture-created values under an explicit
  tenant prefix), and an `event_type` pre-registered in that tenant's
  `Letflow.EventStore.Registry` with a schema the test's payload satisfies, calling
  `platform.emit_event(event_type, payload, idempotency_key)` from a script returns
  Lua `true`, and a subsequent, independent read of the tenant's event stream (e.g.
  `Letflow.EventStore.read_global/1`, or a direct query against the `events` table
  under that prefix) shows exactly one row matching `event_type`/`instance_id`/
  `actor_id`/`idempotency_key` — proving the event is genuinely "observable by the
  host," not merely that the Lua call returned successfully.

### 10. `platform.emit_event` — no execution context / malformed arguments (design §5.3 steps 1–2)

- With `Platform.install/2` (empty execution context) and `"event:emit"` granted,
  calling `platform.emit_event` returns `nil` plus a table with `reason` equal to
  `"no_execution_context"` — no `Letflow.EventStore.append/2` call is attempted (an
  assertion mechanism equivalent to REQ-159's own Repo-call-avoidance test, applied to
  `EventStore.append/2` instead of `Repo.get/3`).
- With a populated execution context, calling `platform.emit_event` with a non-string
  `event_type` or a non-string `idempotency_key` returns `nil` plus a table with
  `reason` equal to `"invalid_arguments"` — no `EventStore.append/2` call attempted.

### 11. `platform.emit_event` — host-side append failure returns structured error, not a raise

- With a populated execution context and `"event:emit"` granted, calling
  `platform.emit_event` with an `event_type` that has no registered schema for that
  tenant returns `nil` plus a table whose `reason` reflects
  `Letflow.EventStore.append/2`'s own `:unknown_event_type` error — asserted not to
  raise, mirroring test 7's "service failure does not raise" shape for the third of
  this requirement's three write-path functions.

### 12. Tenant-prefix discipline (AC7)

- A grep-style or code-inspection-based assertion (mirroring `VariableMerge`'s own
  "grep-checkable" recipe convention) that no call site inside `write_variable`'s or
  `call_service`'s implementation ever references `Letflow.Repo` at all, and that
  `emit_event`'s one `Letflow.EventStore.append/2` call site always passes
  `prefix: execution_context.prefix` textually, never a value built from `event_type`,
  `payload`, or `idempotency_key`. **Implemented** — the structural guard test in
  `test/letflow/engine/lua/platform_test.exs`'s
  `"REQ-160: structural guard -- Repo.get/3's prefix is always execution_context.prefix"`-
  style describe blocks, combined with test 9's single-tenant successful-emission test
  (which confirms `prefix: execution_context.prefix` actually reaches
  `EventStore.append/2` and a row lands under that tenant), covers the literal text of
  AC7 ("no cross-tenant leakage" as "no Repo call with a script-derived prefix").
- **Descoped** (TEST-DESIGNER, rework 1 — corrected from the earlier draft, which
  implied this was covered): a *functional*, two-tenant test — constructing two
  execution contexts that differ only in `prefix` (pointing at two distinct, real
  tenant schemas, each with the same `event_type` registered), and asserting
  `platform.emit_event` called under each context writes into the correspondingly
  different tenant's `events` table and never the other one — does **not** exist in
  `platform_test.exs`. Reason: AC7's literal requirement text (`docs/requirements.yaml`)
  is "no `Letflow.Repo` call bypassing the tenant `prefix`" — a source-level/structural
  property about which value the call site passes, not a claim about cross-tenant
  read/write isolation as observed through two live tenant schemas. The grep-style
  guard plus the single-tenant functional test already prove the narrower, literal AC7
  text: `prefix` is textually `execution_context.prefix` and a real row lands in that
  tenant's schema when it runs. The broader two-tenant isolation property this section
  originally planned is a strictly stronger claim that AC7's own text does not require
  and that Postgres schema-per-tenant isolation (established at the migration/schema
  level, not by this Lua-host-API requirement) already provides independently of
  `platform.ex`'s code. Adding it here would be testing the schema-provisioning
  mechanism, not REQ-160's own contribution — left out of scope for this requirement.

### 13. Number-marshalling module identity (AC6)

- `write_variable`'s staged value for an integer- and a whole-number-float-valued
  script argument, read back via `take_staged_writes/0`, are `is_integer/1`/`is_float/1`
  true respectively (not collapsed) — proving `LuaNumberMarshalling.from_lua/1` (the
  same module/function REQ-159 already created and REQ-160 reuses, per REQ-150 §2.1) is
  actually applied on this call path, not a second, independently-written conversion.

### 14. Compile/test gate (AC8)

- `mix compile --warnings-as-errors` passes with the REQ-160 changes applied.
- `mix test` passes, quoting the real pass count for `platform_test.exs`'s REQ-160
  section, at TEST-RUNNER's Step 4.

## Resolution of open items (TEST-DESIGNER, Step 3; corrected rework 1)

- **Test 2's four failure-arm sub-cases.** The original draft here argued the
  structural guard alone ("`platform.ex`'s source has zero call sites of
  `take_staged_writes()` other than its own `@spec`/`def` lines") was sufficient
  evidence for all four sub-cases uniformly, since none of the four failure paths is
  distinguished by this module's own code. TEST-DESIGN-VALIDATOR's rework-1 findings
  (`handoffs/WF02-REQ160-20260827/step-03b-test-design-validator.json`) correctly
  identified that argument as an inference substituting for concrete tests AC1's own
  text names by name, given that REQ-154/155/156 are `status: done` and their real
  kill mechanisms were readily reusable. **All four sub-cases are now directly,
  behaviorally implemented** in `platform_test.exs` (see section 2 above) —
  driving the real `Sandbox.new(max_instructions: ...)` budget path, the real
  `Task.async`/`Task.yield`/`Task.shutdown(:brutal_kill)` wall-clock path (mirroring
  `executor_test.exs`'s own REQ-155 harness), and the real `:erlang.spawn_opt/2` +
  `max_heap_size` memory-limit path (mirroring `executor_test.exs`'s own REQ-156
  harness) — each against `Platform.install/3`'s own composition rather than driving
  `Letflow.Engine.Lua.Executor` directly (design §2.5 OQ-1's wiring gap between
  `Executor` and this buffer still stands; these tests only needed `Platform.install/3`
  plus the same underlying REQ-154/155/156 primitives `Executor` itself uses). The
  structural guard test is retained as supplementary evidence, not the sole evidence.
- **Test 9/11/12's tenant fixture**: resolved by reusing `provisioned_tenant/0`,
  `seed_projection!/3`, and a new `register_event_type!/2` fixture that mirrors
  `event_store_test.exs`'s own `register_event_type!/2` convention (permissive
  `{"type" => "object"}` JSON schema) — see the REQ-160 section of
  `test/letflow/engine/lua/platform_test.exs`.

## Mutation-testing findings (TEST-DESIGNER, WF-02 Step 3)

Each mutation below was applied directly to `lib/letflow/engine/lua/platform.ex`, `mix
test test/letflow/engine/lua/platform_test.exs` was run against the mutated code, the
result was recorded, and the mutation was reverted (confirmed back to a clean `git
diff`) before moving to the next one. Baseline before any mutation: 82 tests passing
(ELIXIR-DEV's own suite). State after the original Step 3 pass: **85 tests passing**
(independently re-verified by TEST-DESIGN-VALIDATOR, which is why the mutation table
below was not re-run in rework 1 per its own instructions). **Final state after
rework 1's coverage-gap fixes (no new mutation pass required): 90 tests passing** — 5
new tests added: the three REQ-154/155/156 kill-arm behavioral tests, the
`VariableMerge.merge/3` atomicity test, and the `call_service` write-direction
marshalling test (fixes 1-3 below); no mutation was involved in adding them, they close
real-coverage gaps against REQ-160's own acceptance-criteria text found by
TEST-DESIGN-VALIDATOR, not gaps found by mutation.

| # | Mutation | Existing suite caught it? | Action |
|---|---|---|---|
| 1 | `write_variable`'s `stage_write/2`: `Map.put/3` → `Map.put_new/3` (last-write-wins broken) | Yes — 81/82, 1 failure (the existing "last-write-wins" test) | None needed |
| 2 | `take_staged_writes/0`: removed the `Process.delete/1` clear-after-read call | Yes — 81/82, 1 failure (the existing "clears the buffer" test) | None needed |
| 3 | `call_service`'s `{:error, reason}` arm: `encode_error(...)` → `raise "..."` | Yes — 78/82, 4 failures (multiple existing tests exercise this arm, including the default `NoServiceCaller` path) | None needed |
| 4 | `install/3`'s fold wrapper: reordered `run_stub/5` to execute BEFORE `Capabilities.check!/3` (gate still eventually raises, just too late) | **No — 82/82 still passed.** Every existing capability-denial test only asserts "some `Lua.RuntimeException` was eventually raised," never that the stub body was never entered. | **Gap closed**: added `SpyServiceCaller` (a `ServiceCaller` double that `send`s a message when invoked) plus a test asserting `refute_received` after a denied `call_service`; and a Repo-query telemetry-counter test asserting zero queries fire for a denied `emit_event` call made with a FULLY POPULATED execution context (so the body, if reached, would genuinely attempt a `Repo` call via `EventStore.append/2`). Both new tests fail against the reordered mutation (82/84) and pass against the real code. |
| 5 | `emit_event`'s `EventStore.append/2` call: `prefix: execution_context.prefix` → `prefix: event_type` (script-supplied value) | Yes — 81/84, 3 failures (the existing functional + structural "tenant-prefix discipline" tests) | None needed — this is REQ-160's single highest-value property (SECURITY-REVIEWER's PASS rests on it) and it is already well covered |
| 6 | `write_variable`: removed the `LuaNumberMarshalling.from_lua/1` call, passing the raw Lua value straight to `stage_write/2` | **No — 85/85 (pre-addition-of-test-6 baseline: 84/84) still passed, including the float-specific round-trip test.** `LuaNumberMarshalling.from_lua/1`/`to_lua/1` are both literal identity functions (`def from_lua(value), do: value`) per REQ-150's own normative rule — no behavioral test, of any input shape, can ever distinguish "called the identity function" from "didn't call it." | **Gap closed, but by a structural test, not a behavioral one** — since REQ-150's rule makes behavioral detection impossible by construction, added a source-inspection test asserting every REQ-160 numeric-crossing call site (`do_write_variable/2`, `normalize_from_lua/1`, `convert_map_to_lua/1`) textually invokes `LuaNumberMarshalling.from_lua`/`.to_lua` by name, plus a floor count on total occurrences. This test fails (84/85) when the call is removed and passes (85/85) against the real code. |
| 7 | `write_variable`: inserted a real `Repo.query("SELECT 1")` call into `do_write_variable/2` to confirm the existing "zero Repo calls" telemetry-counter test is a genuine spy, not a no-op | Yes — 84/85, 1 failure (the existing telemetry-counter test) | None needed — confirms the existing zero-Repo-calls assertion mechanism (mirroring REQ-159's own precedent) is real, not merely inferred from a correct return value |

### Net result

- 3 real gaps found (mutations 4 and 6, plus mutation 4 covering both `call_service`
  and `emit_event`'s capability-gate-precedes-body property) — closed by 3 new tests:
  - `REQ-160: call_service -- missing capability raises...` → "a MISSING capability
    means the ServiceCaller spy is never invoked at all"
  - `REQ-160: emit_event -- capability denial...` → "a MISSING capability means
    EventStore.append/2 is never reached (Repo-query spy)"
  - `REQ-160: structural guard -- LuaNumberMarshalling call sites are genuinely
    present` → "every REQ-160 numeric-crossing call site routes through
    LuaNumberMarshalling.from_lua/1 or .to_lua/1 by name"
- 4 mutations were already caught by ELIXIR-DEV's own 82 tests (last-write-wins,
  clear-after-read, call_service never-raises, emit_event prefix discipline) — no
  action needed beyond confirming the catch.
- State at end of the original Step 3 pass: `test/letflow/engine/lua/platform_test.exs`,
  **85 tests**, all passing against the real (unmutated) implementation, independently
  re-verified by TEST-DESIGN-VALIDATOR (mutations 4 and 6 re-applied and re-reverted).
- **Rework 1 (this pass)**: TEST-DESIGN-VALIDATOR's rework-1 findings identified real
  coverage gaps against REQ-160's own 8 acceptance criteria (AC1, AC2, and spec
  sections 6/12's own planned-but-unimplemented test cases) — not mutation gaps. Five
  tests were added to close them:
  - `"REQ-154's instruction-budget exhaustion discards the staged write -- never
    observed from any other process (AC1)"`
  - `"REQ-155's wall-clock kill discards the staged write -- the killed process's
    buffer is simply gone (AC1)"`
  - `"REQ-156's memory-limit kill discards the staged write -- the killed process's
    buffer is simply gone (AC1)"`
  - `"REQ-160/AC2: VariableMerge.merge/3 applies all staged writes atomically in a
    single call (design §2.6)"`
  - `"the double receives real Elixir integer()/float() types for an integer- and a
    whole-number-float-valued argument, not collapsed"` (call_service write-direction)
- Final test file: `test/letflow/engine/lua/platform_test.exs`, **90 tests**, all
  passing against the real (unmutated) implementation (`mix test` confirmed clean this
  run — see below). Re-running the full mutation-testing pass was not required for
  this rework per TEST-DESIGN-VALIDATOR's own instructions (the two independently
  re-verified mutations from the original pass still hold; nothing in this rework
  touched `lib/letflow/engine/lua/platform.ex` itself).
